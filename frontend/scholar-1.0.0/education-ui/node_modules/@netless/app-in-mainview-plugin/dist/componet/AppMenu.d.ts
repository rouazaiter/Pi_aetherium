import { AppId } from '../types';
import './style.less';
import { AppManager, AppValue } from '../appManager';
import { Language } from './locale';
export interface AppMenuProps {
    manager: AppManager;
    onlyShowHidden: boolean;
    language: Language;
    theme: 'light' | 'dark';
}
export declare class AppMenu {
    private readonly namespace;
    readonly container: HTMLDivElement;
    private readonly badge;
    private readonly menuView;
    private readonly manager;
    private readonly onlyShowHidden;
    private readonly language;
    private i18n;
    private theme;
    private isBindContainer;
    constructor(props: AppMenuProps);
    private c;
    private containerClickHandler;
    private menuViewClickHandler;
    private bindContainer;
    private removeContainer;
    private createDefaultAppMenu;
    private init;
    private appMenuChangeHandler;
    private onPrefersColorSchemeChangeHandler;
    private onMainViewMountedHandler;
    private onMainViewRebindHandler;
    private onFullscreenChangeHandler;
    observe(): void;
    unobserve(): void;
    private createItem;
    private createShowBtn;
    private createHidBtn;
    private updateMenuView;
    render(apps: Map<AppId, AppValue>): void;
    destroy(): void;
}

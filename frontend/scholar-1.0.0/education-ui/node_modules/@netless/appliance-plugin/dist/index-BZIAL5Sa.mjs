var _e = Object.defineProperty;
var Ve = (T, i, t) => i in T ? _e(T, i, { enumerable: !0, configurable: !0, writable: !0, value: t }) : T[i] = t;
var y = (T, i, t) => Ve(T, typeof i != "symbol" ? i + "" : i, t);
import { g as Jt, _ as Pe, i as te, p as St, e as Tt, D as J, f as E, h as Rt, j as Vt, k as st, P as O, l as W, s as Ht, m as ee, n as Nt, o as se, r as He, q as qe, c as k, t as Mt, u as z, a as w, b as x, v as A, w as G, x as tt, y as $, z as Zt, A as je, B as Ze, C as Ke, F as Qe, G as Je, H as ts, I as es, J as ss, K as X, L as os, M as he, N as Wt, O as We, T as Bt, Q as yt, R as is, U as Le, W as rs, X as ns, Y as ct, Z as _, $ as ut, a0 as as, a1 as cs, a2 as ls, a3 as hs, a4 as bt, a5 as ps, E as Y, a6 as pe, V as de, S as ds } from "./index-BrdMbdqX.mjs";
import { Group as j, Path as wt, Sprite as Ce, Rect as Kt, Polyline as ht, Ellipse as us, Label as fs, Scene as oe, Layer as ms } from "spritejs";
import it from "clipper-lib";
import ie from "lineclip";
import "lz-string";
import "xss";
import { cloneDeep as be, xor as ys, isNumber as zt, isEqual as ue } from "lodash";
import { O as fe } from "./ObserverMap-BudneEfB.mjs";
import "white-web-sdk";
import "react-dom";
import "react";
var ws = Pe, gs = te, ks = "[object Number]";
function Ss(T) {
  return typeof T == "number" || gs(T) && ws(T) == ks;
}
var Ts = Ss;
const H = /* @__PURE__ */ Jt(Ts);
function mt(T, i = !0) {
  const t = T.length;
  if (t < 2)
    return "";
  let e = T[0], s = T[1];
  if (t === 2)
    return `M${St(e)}L${St(s)}`;
  let o = "";
  for (let r = 2, n = t - 1; r < n; r++)
    e = T[r], s = T[r + 1], o += Tt(e, s);
  return i ? `M${Tt(T[0], T[1])}Q${St(T[1])}${Tt(
    T[1],
    T[2]
  )}T${o}${Tt(T[t - 1], T[0])}${Tt(T[0], T[1])}Z` : `M${St(T[0])}Q${St(T[1])}${Tt(T[1], T[2])}${T.length > 3 ? "T" : ""}${o}L${St(T[t - 1])}`;
}
const et = class et {
  constructor(i) {
    y(this, "maxImageWidth", J.pencilEraser.maxImageWidth);
    y(this, "maxImageHeight", J.pencilEraser.maxImageHeight);
    y(this, "syncUnitTime", J.syncOpt.interval);
    y(this, "vNodes");
    y(this, "drawLayer");
    y(this, "fullLayer");
    y(this, "workId");
    y(this, "isDelete", !1);
    const { vNodes: t, fullLayer: e, drawLayer: s, workId: o, toolsOpt: r } = i;
    this.vNodes = t, this.fullLayer = e, this.drawLayer = s, this.workId = o, this.syncUnitTime = r.syncUnitTime || this.syncUnitTime;
  }
  get baseConsumeResult() {
    return {
      workId: this.workId,
      toolsType: this.toolsType,
      opt: this.workOptions
    };
  }
  filterSamePoints(i, t = 0.01) {
    return i.reduce((e, s) => {
      const o = e[e.length - 1];
      return (s && !o || s && o && !s.isNear(o, t)) && e.push(s), e;
    }, []);
  }
  /** 设置工作id */
  setWorkId(i) {
    this.workId = i;
  }
  getWorkId() {
    return this.workId;
  }
  /** 获取工作选项配置 */
  getWorkOptions() {
    return this.workOptions;
  }
  /** 设置工作选项配置 */
  setWorkOptions(i) {
    var s, o, r;
    this.workOptions = i, this.syncUnitTime = i.syncUnitTime || this.syncUnitTime;
    const t = (s = this.workId) == null ? void 0 : s.toString(), e = t && ((o = this.vNodes) == null ? void 0 : o.get(t)) || void 0;
    t && e && (e.opt = i, (r = this.vNodes) == null || r.setInfo(t, e));
  }
  /** 更新服务端同步配置,返回绘制结果 */
  updataOptService(i) {
    var s, o;
    let t;
    const e = (s = this.workId) == null ? void 0 : s.toString();
    if (e && i) {
      const r = this.fullLayer.getElementsByName(e) || this.drawLayer && this.drawLayer.getElementsByName(e) || [];
      if (r.length !== 1)
        return;
      const n = r[0], { pos: a, zIndex: c, scale: l, angle: h, translate: p } = i, d = {};
      H(c) && (d.zIndex = c), a && (d.pos = [a[0], a[1]]), l && (d.scale = l), h && (d.rotate = h), p && (d.translate = p), n.attr(d);
      const u = n == null ? void 0 : n.getBoundingClientRect();
      return u && (t = E(t, {
        x: Math.floor(u.x - et.SafeBorderPadding),
        y: Math.floor(u.y - et.SafeBorderPadding),
        w: Math.floor(u.width + et.SafeBorderPadding * 2),
        h: Math.floor(u.height + et.SafeBorderPadding * 2)
      })), (o = this.vNodes) == null || o.setInfo(e, {
        rect: t,
        centerPos: a
      }), t;
    }
  }
  drawEraserlines(i, t) {
    const { group: e, eraserlines: s, pos: o, layer: r } = i, n = r.parent;
    if (t)
      try {
        const c = (r.renderer.glRenderer || r.renderer.canvasRenderer).options.displayRatio, l = e.getBoundingClientRect(), h = l.width * c * r.worldScaling[0], p = l.height * c * r.worldScaling[1];
        let d = 1;
        (h > this.maxImageWidth || p > this.maxImageHeight) && (d = Math.min(
          this.maxImageWidth / h,
          this.maxImageHeight / p
        ));
        let u = r.getAttribute("scale");
        u = [u[0] * d, u[1] * d];
        const f = r.parent.parent, m = r.getAttribute("translate"), g = this.createVmRenderNode(
          `${this.workId}_bitMapLayer`,
          f,
          {
            offscreen: n.offscreen,
            width: n.width,
            height: n.height,
            contextType: "2d",
            autoRender: !1,
            bufferSize: 500
          },
          u,
          m
        ), I = e.cloneNode(!0);
        if (!s || !s.length)
          return;
        this.addEraserlines(I, s, o, !1), g.appendChild(I);
        const S = g.parent;
        S.render();
        const v = g.renderer.canvasRenderer, L = v.context;
        if (!L)
          return;
        const C = v.options.displayRatio, N = I.getBoundingClientRect(), R = {
          x: N.x,
          y: N.y,
          w: N.width,
          h: N.height
        }, P = {
          x: 0,
          y: 0,
          w: f.width,
          h: f.height
        };
        if (Rt(R, P) === Vt.outside) {
          Q(I, S), f.removeChild(S), this.isDelete = !0;
          return;
        }
        const D = st(R);
        D.x = Math.floor(Math.max(R.x, 0)), D.y = Math.floor(Math.max(R.y, 0)), D.w = Math.min(R.x + R.w, f.width) - D.x, D.h = Math.min(R.y + R.h, f.height) - D.y;
        const B = this.getGroupRect(D, C), F = B.x, U = B.y, V = B.w, Z = B.h;
        let K = L.getImageData(F, U, V, Z);
        if (this.isTransparentRectByCanvas(K)) {
          Q(I, S), f.removeChild(S), this.isDelete = !0, K = null;
          return;
        }
        Q(e, n);
        const gt = this.createSpriteNode({
          imageData: K,
          safariRect: D,
          originRect: R,
          worldScaling: g.worldScaling
        });
        gt && e.append(gt), Q(I, S), f.removeChild(S), n.deleteTexture(K), K = null;
      } catch (a) {
        console.error("[BaseShapeTool] drawEraserlines error:", a);
        return;
      }
  }
  createVmRenderNode(i, t, e, s, o) {
    const { width: r, height: n } = e, a = `bitMap-${i}`, c = t.layer(a, e), l = new j({
      anchor: [0.5, 0.5],
      pos: [r * 0.5, n * 0.5],
      size: [r, n],
      name: "viewport",
      id: i
    });
    return l.setAttribute("scale", s), l.setAttribute("translate", o), c.append(l), l;
  }
  getGroupRect(i, t) {
    const e = Math.floor(i.x * t), s = Math.floor(i.y * t), o = Math.floor(i.w * t), r = Math.floor(i.h * t);
    return {
      x: e,
      y: s,
      w: o,
      h: r
    };
  }
  addEraserlines(i, t, e, s = !0) {
    for (const o of t) {
      const { thickness: r, op: n } = o;
      for (const a of n) {
        const c = a.map((u, f) => f % 2 ? u - e[1] : u - e[0]), l = this.computEraserPoints(c, r), h = mt(l, !0), p = {
          pos: [0, 0],
          d: h,
          fillColor: "rgba(0,0,0,1)"
        }, d = new wt(p);
        d.addEventListener("beforerender", ({ detail: u }) => {
          const f = u.context;
          s ? f.blendFuncSeparate(f.ZERO, f.ZERO, f.ZERO, f.ZERO) : f.globalCompositeOperation = "destination-out";
        }), d.addEventListener("afterrender", ({ detail: u }) => {
          const f = u.context;
          s ? f.blendFuncSeparate(
            f.SRC_ALPHA,
            f.ONE_MINUS_SRC_ALPHA,
            f.ONE,
            f.ONE_MINUS_SRC_ALPHA
          ) : f.globalCompositeOperation = "source-over";
        }), i.append(d);
      }
    }
  }
  isTransparentRectByCanvas(i) {
    let t = !0;
    for (let e = 0; e < i.data.length; e += 4)
      if (i.data[e + 3] !== 0) {
        t = !1;
        break;
      }
    return t;
  }
  createSpriteNode(i) {
    const { imageData: t, worldScaling: e, safariRect: s, originRect: o } = i;
    let r;
    if (typeof OffscreenCanvas == "function") {
      r = new OffscreenCanvas(t.width, t.height);
      const l = r.getContext("2d");
      l && l.putImageData(t, 0, 0);
    } else if (typeof document < "u") {
      r = document.createElement("canvas"), r.width = t.width, r.height = t.height;
      const l = r.getContext("2d");
      l && l.putImageData(t, 0, 0);
    }
    if (!r)
      return console.warn("Failed to create imageEraserBitmap Sprite"), null;
    const n = [
      Math.floor(o.w / e[0]),
      Math.floor(o.h / e[1])
    ], a = [
      Math.floor((s.x - o.x) / e[0]),
      Math.floor((s.y - o.y) / e[1]),
      Math.floor(s.w / e[0]),
      Math.floor(s.h / e[1])
    ], c = {
      name: "eraserTexture",
      anchor: [0.5, 0.5],
      pos: [0, 0],
      size: n,
      texture: r,
      textureRect: a
    };
    return new Ce(c);
  }
  computEraserPoints(i, t) {
    const e = Math.ceil(t / 2);
    return i.length === 2 ? this.computDot(i, e) : this.computLine(i, e);
  }
  computDot(i, t) {
    const e = new O(i[0], i[1]);
    return O.GetDotStroke(e, t, 8);
  }
  computLine(i, t) {
    const e = [], s = [];
    let o, r;
    for (let n = 0; n < i.length; n += 2) {
      const a = new O(i[n], i[n + 1]);
      let c;
      if (n == i.length - 2) {
        const l = new O(i[n - 2], i[n - 1]);
        c = W.Sub(a, l).uni(), o = a;
      } else {
        n === 0 && (r = a);
        const l = new O(i[n + 2], i[n + 3]);
        c = W.Sub(l, a).uni();
      }
      if (c) {
        const l = W.Per(c).mul(t);
        e.push(O.Sub(a, l)), s.push(O.Add(a, l));
      }
    }
    if (o && r) {
      const n = O.GetSemicircleStroke(
        o,
        e[e.length - 1],
        -1,
        8
      ), a = O.GetSemicircleStroke(
        r,
        s[0],
        -1,
        8
      );
      return e.concat(n, s.reverse(), a);
    }
    return [];
  }
  replace(i, t, e) {
    var o;
    if (!t) {
      e && i.append(e);
      return;
    }
    const s = i.getElementsByName(t);
    if (s.length)
      for (const r of s)
        e ? oo(e, r, i) : (r.remove(), q(r, i.parent));
    else e && i.append(e);
    this.fullLayer !== this.drawLayer && (this.fullLayer === i ? (o = this.drawLayer) == null || o.getElementsByName(t).forEach((n) => {
      var a;
      n.remove(), q(n, (a = this.drawLayer) == null ? void 0 : a.parent);
    }) : this.fullLayer.getElementsByName(t).forEach((n) => {
      n.remove(), q(n, this.fullLayer.parent);
    }));
  }
  removeDrawCountNodes(i, t) {
    const e = [];
    i.getElementsByName(this.workId).forEach((s) => {
      s.id && t && Number(s.id) < t && e.push(s);
    });
    for (const s of e)
      s.remove(), q(s, i.parent);
  }
  static updateNodeOpt(i) {
    var I;
    const { node: t, opt: e, vNodes: s, willSerializeData: o, targetNode: r } = i, {
      zIndex: n,
      translate: a,
      angle: c,
      originPoint: l,
      scenePoint: h,
      scale: p,
      pointMap: d,
      thickness: u
    } = e;
    let f;
    const m = r && st(r) || s.get(t.name);
    if (!m) return;
    H(n) && (t.setAttribute("zIndex", n), m.opt.zIndex = n);
    const g = t.parent;
    if (g) {
      if (l && p && r) {
        const S = [m.op[0], m.op[1]];
        Ht(m.op, h, p, a);
        const v = [m.op[0], m.op[1]], L = [
          v[0] - S[0],
          v[1] - S[1]
        ];
        if (m.centerPos = [
          m.centerPos[0] + L[0],
          m.centerPos[1] + L[1]
        ], m.opt.translate = void 0, m.opt.scale = void 0, m.opt.eraserlines)
          for (let C = 0; C < m.opt.eraserlines.length; C++) {
            const { op: N, thickness: R } = m.opt.eraserlines[C];
            m.opt.eraserlines[C].thickness = Math.round(
              R * Math.max(p[0], p[1])
            );
            for (let P = 0; P < N.length; P++)
              ee(N[P], h, p, a);
          }
      } else if (a)
        if (t.setAttribute("translate", a), m.opt.translate = a, r) {
          const S = [
            a[0] * g.worldScaling[0],
            a[1] * g.worldScaling[1]
          ];
          f = Nt(m.rect, S), m.rect = f;
        } else {
          const S = et.getRectFromLayer(g, t.name);
          m.rect = S || m.rect;
        }
      else if (H(c))
        if (t.setAttribute("rotate", c), m.opt.rotate = c, r)
          f = se(m.rect, c), m.rect = f;
        else {
          const S = et.getRectFromLayer(g, t.name);
          m.rect = S || m.rect;
        }
      if (d) {
        const S = d.get(t.name);
        if (S)
          for (let v = 0, L = 0; v < m.op.length; v += 3, L++)
            m.op[v] = S[L][0], m.op[v + 1] = S[L][1];
      }
      if (u && ((I = m == null ? void 0 : m.opt) != null && I.thickness) && (m.opt.thickness = u), o && !(l && p && r)) {
        if (a) {
          const S = m.op.map((v, L) => {
            const C = L % 3;
            return C === 0 ? v + a[0] : C === 1 ? v + a[1] : v;
          });
          if (m.op = S, m.centerPos = [
            m.centerPos[0] + a[0],
            m.centerPos[1] + a[1]
          ], m != null && m.opt && (m.opt.translate = void 0), m.opt.eraserlines)
            for (let v = 0; v < m.opt.eraserlines.length; v++) {
              const { op: L } = m.opt.eraserlines[v];
              for (let C = 0; C < L.length; C++) {
                const N = L[C].map((R, P) => P % 2 ? R + a[1] : R + a[0]);
                m.opt.eraserlines[v].op[C] = N;
              }
            }
        } else if (H(c)) {
          const S = m.op;
          if (He(S, m.centerPos, c), m.op = S, m != null && m.opt && (m.opt.rotate = void 0), m.opt.eraserlines)
            for (let v = 0; v < m.opt.eraserlines.length; v++) {
              const { op: L } = m.opt.eraserlines[v];
              for (let C = 0; C < L.length; C++)
                qe(L[C], m.centerPos, c);
            }
        }
      }
      m && s.setInfo(t.name, m);
    }
  }
  static getCenterPos(i, t) {
    const { worldPosition: e, worldScaling: s } = t;
    return [
      (i.x + i.w / 2 - e[0]) / s[0],
      (i.y + i.h / 2 - e[1]) / s[1]
    ];
  }
  static getRectFromLayer(i, t) {
    const e = i.getElementsByName(t)[0];
    if (e) {
      const s = e.getBoundingClientRect();
      return {
        x: Math.floor(s.x - et.SafeBorderPadding),
        y: Math.floor(s.y - et.SafeBorderPadding),
        w: Math.floor(s.width + et.SafeBorderPadding * 2),
        h: Math.floor(s.height + et.SafeBorderPadding * 2)
      };
    }
  }
  static isWillRefresh(i) {
    const { toolsType: t, opt: e, node: s, updateOpt: o, willSerializeData: r } = i;
    return !!(r && (o.angle || o.translate) || o.thickness && e.thickness && e.thickness !== o.thickness || o.strokeType && e.strokeType && e.strokeType !== o.strokeType || o.originPoint && o.scenePoint && o.scale || o.pointMap && o.pointMap.has(s.name) || t === k.Text && (o.fontSize || o.translate || o.textInfos && o.textInfos.get(s.name)) || t === k.BackgroundSVG && (o.translate || o.scale) || t === k.Image && (o.angle || o.translate || o.scale || o.strokeColor && e.type === Mt.Iconify) || t === o.toolsType && o.willRefresh || e.eraserlines && e.eraserlines.length && (o.strokeColor || o.fillColor));
  }
};
y(et, "SafeBorderPadding", 10);
let b = et;
const re = Object.freeze([
  Object.freeze({ width: 18, height: 26 }),
  Object.freeze({ width: 26, height: 34 }),
  Object.freeze({ width: 34, height: 50 }),
  Object.freeze({ width: 48, height: 74 })
]);
function Re(T, i, t = 0.01) {
  return Math.abs(T[0] - i[0]) < t && Math.abs(T[1] - i[1]) < t;
}
function vs(T, i = 0.01) {
  if (T.length === 0) return [];
  const t = [[T[0][0], T[0][1]]];
  for (let e = 1; e < T.length; e++)
    Re([T[e][0], T[e][1]], t[t.length - 1], i) || t.push([T[e][0], T[e][1]]);
  return t;
}
function Is(T, i = 0.01) {
  if (T.length <= 2) return T;
  const t = T[0], e = T[T.length - 1];
  return Re(t, e, i) ? T.slice(0, -1) : T;
}
function Ps(T) {
  const i = T.reduce((e, s) => e + s[0], 0) / T.length, t = T.reduce((e, s) => e + s[1], 0) / T.length;
  return [i, t];
}
function me(T, i) {
  return Math.atan2(i[1] - T[1], i[0] - T[0]);
}
function Ws(T, i = 0.01) {
  if (T.length <= 2) return T;
  const t = [];
  for (const s of T)
    t.some(
      (r) => Math.abs(r[0] - s[0]) < i && Math.abs(r[1] - s[1]) < i
    ) || t.push([s[0], s[1]]);
  const e = Ps(t);
  return t.sort((s, o) => {
    const r = me(e, s), n = me(e, o);
    return r - n;
  });
}
function Ls(T, i) {
  const t = i[0].x < i[i.length - 1].x;
  return T.map((s) => {
    const r = s.map((n) => {
      let a = -1, c = 1 / 0, l = 0;
      for (let h = 0; h < i.length; h++) {
        const p = i[h], d = Math.sqrt(
          Math.pow(p.x - n[0], 2) + Math.pow(p.y - n[1], 2)
        );
        d < c && (c = d, a = h, l = p.z);
      }
      return { point: n, index: a, bestZ: l };
    }).sort((n, a) => n.index - a.index).map((n) => [...n.point, n.bestZ]);
    return t && r[0][0] > r[r.length - 1][0] || !t && r[0][0] < r[r.length - 1][0] ? r.reverse() : r;
  }).sort((s, o) => {
    const r = s[0][0], n = o[0][0];
    return t ? r - n : n - r;
  });
}
function Cs(T, i, t = 0.01) {
  const s = T.map((h) => [h.x, h.y]), o = (h, p) => h.map(([d, u]) => ({
    X: Math.round(d * p),
    Y: Math.round(u * p)
  })), r = new it.ClipperOffset();
  r.AddPath(
    o(s, 1e3),
    it.JoinType.jtRound,
    it.EndType.etOpenButt
  );
  const n = [];
  r.Execute(n, 1);
  const a = new it.Clipper();
  a.AddPaths(n, it.PolyType.ptSubject, !0), i.forEach((h) => {
    a.AddPath(
      o(h, 1e3),
      it.PolyType.ptClip,
      !0
    );
  });
  const c = new it.Paths();
  a.Execute(
    it.ClipType.ctDifference,
    c,
    it.PolyFillType.pftNonZero,
    it.PolyFillType.pftNonZero
  );
  const l = c.map(
    (h) => Is(
      Ws(
        vs(
          h.map(
            (p) => [p.X / 1e3, p.Y / 1e3]
          ),
          t
        ),
        t
      ),
      t
    )
  ).filter((h) => h.length >= 2);
  return Ls(l, T);
}
class Ne extends b {
  constructor(t) {
    super(t);
    y(this, "canRotate", !0);
    y(this, "scaleType", z.all);
    y(this, "toolsType", k.Pencil);
    y(this, "syncTimestamp");
    y(this, "syncIndex", 0);
    y(this, "tmpPoints", []);
    y(this, "MAX_REPEAR", 10);
    /** 合并原始点的灵敏度 */
    y(this, "uniThickness");
    y(this, "workOptions");
    y(this, "centerPos", [0, 0]);
    this.workOptions = t.toolsOpt, this.uniThickness = this.MAX_REPEAR / this.workOptions.thickness / 10, this.syncTimestamp = 0;
  }
  /** 批量合并消费本地数据,返回绘制结果 */
  combineConsume() {
    var n;
    const t = (n = this.workId) == null ? void 0 : n.toString();
    if (this.tmpPoints.length < 2)
      return {
        type: w.None
      };
    const e = this.transformDataAll(!0), s = {
      name: t
    };
    let o;
    const r = this.drawLayer || this.fullLayer;
    return e.length && (o = this.draw({ attrs: s, tasks: e, replaceId: t, layer: r })), {
      rect: o,
      type: w.DrawWork,
      dataType: x.Local
    };
  }
  setWorkOptions(t) {
    super.setWorkOptions(t), this.syncTimestamp = Date.now();
  }
  consume(t) {
    const {
      data: e,
      isFullWork: s,
      isSubWorker: o,
      isMainThread: r,
      drawCount: n,
      removeDrawCount: a,
      isSimpleWorker: c
    } = t, { workId: l, syncUnitTime: h } = e;
    h && (this.syncUnitTime = h);
    const { tasks: p, effects: d, consumeIndex: u } = this.transformData(e, !1);
    this.syncIndex = Math.min(
      this.syncIndex,
      u,
      Math.max(0, this.tmpPoints.length - 2)
    );
    const f = {
      name: l,
      id: H(n) && n.toString() || void 0
    };
    let m, g = !1;
    const I = this.syncIndex;
    if (this.syncTimestamp === 0 && (this.syncTimestamp = Date.now()), p.length && (p[0].taskId - this.syncTimestamp > this.syncUnitTime && (g = !0, this.syncTimestamp = p[0].taskId, this.syncIndex = this.tmpPoints.length), o || r || c)) {
      const v = s ? this.fullLayer : this.drawLayer || this.fullLayer;
      m = this.draw({ attrs: f, tasks: p, effects: d, layer: v, removeDrawCount: a });
    }
    if (c) {
      const v = [];
      return this.tmpPoints.slice(I).forEach((L) => {
        v.push(L.x, L.y, this.computRadius(L.z, this.workOptions.thickness));
      }), {
        ...this.baseConsumeResult,
        type: w.DrawWork,
        dataType: x.Local,
        op: g ? v : void 0,
        index: g ? I * 3 : void 0,
        rect: m,
        updateNodeOpt: {
          useAnimation: !0
        }
      };
    }
    if (o)
      return u > 10 && this.tmpPoints.splice(0, u - 10), {
        rect: m,
        type: w.DrawWork,
        dataType: x.Local
      };
    const S = [];
    return this.tmpPoints.slice(I).forEach((v) => {
      S.push(v.x, v.y, this.computRadius(v.z, this.workOptions.thickness));
    }), {
      ...this.baseConsumeResult,
      type: w.DrawWork,
      dataType: x.Local,
      rect: m,
      op: g ? S : void 0,
      index: g ? I * 3 : void 0,
      updateNodeOpt: {
        useAnimation: !0
      }
    };
  }
  consumeAll(t) {
    var l;
    const e = this.workId;
    if (t.data) {
      const { op: h, workState: p } = t.data;
      h != null && h.length && p === A.Done && this.workOptions.strokeType === G.Stroke && this.updateTempPointsWithPressureWhenDone(h);
    }
    const s = this.transformDataAll(!0), o = {
      name: e
    };
    let r;
    const n = this.fullLayer;
    if (s.length && (r = this.draw({ attrs: o, tasks: s, replaceId: e, layer: n })), this.tmpPoints.length < 2)
      return this.replace(n, e), {
        type: w.RemoveNode,
        removeIds: [e],
        rect: r
      };
    const a = [];
    this.tmpPoints.map((h) => {
      a.push(h.x, h.y, h.z);
    }), this.syncTimestamp = 0, delete this.workOptions.syncUnitTime;
    const c = tt(a);
    return (l = this.vNodes) == null || l.setInfo(e, {
      rect: r,
      op: a,
      opt: this.workOptions,
      toolsType: this.toolsType,
      scaleType: this.scaleType,
      canRotate: this.canRotate,
      centerPos: r && b.getCenterPos(r, n)
    }), {
      ...this.baseConsumeResult,
      rect: r,
      type: w.FullWork,
      dataType: x.Local,
      ops: c,
      updateNodeOpt: {
        pos: this.centerPos,
        useAnimation: !0
      }
    };
  }
  clearTmpPoints() {
    this.tmpPoints.length = 0, this.syncTimestamp = 0, this.syncIndex = 0;
  }
  consumeService(t) {
    var h, p;
    const {
      op: e,
      isFullWork: s,
      replaceId: o,
      workState: r = A.Done
    } = t;
    this.tmpPoints.length = 0;
    for (let d = 0; d < e.length; d += 3) {
      const u = new O(e[d], e[d + 1], e[d + 2]);
      if (this.tmpPoints.length > 0) {
        const f = this.tmpPoints[this.tmpPoints.length - 1], m = W.Sub(u, f).uni();
        u.setv(m);
      }
      this.tmpPoints.push(u);
    }
    if (this.tmpPoints.length < 2)
      return;
    const n = this.transformDataAll(!0), a = (h = this.workId) == null ? void 0 : h.toString(), c = {
      name: a
    };
    let l;
    if (a && n.length) {
      const d = s ? this.fullLayer : this.drawLayer || this.fullLayer;
      l = this.draw({
        attrs: c,
        tasks: n,
        replaceId: o,
        layer: d,
        isDrawEraserlines: r === A.Done
      }), (p = this.vNodes) == null || p.setInfo(a, {
        rect: l,
        op: e,
        opt: this.workOptions,
        toolsType: this.toolsType,
        scaleType: this.scaleType,
        canRotate: this.canRotate,
        centerPos: l && b.getCenterPos(l, d)
      });
    }
    return l;
  }
  computPencilPoints(t) {
    const e = [], { op: s, eraserPolylines: o, eraserThickness: r } = t;
    this.tmpPoints.length = 0;
    for (let a = 0; a < s.length; a += 3) {
      const c = new O(s[a], s[a + 1], s[a + 2]);
      if (this.tmpPoints.length > 0) {
        const l = this.tmpPoints[this.tmpPoints.length - 1], h = W.Sub(c, l).uni();
        c.setv(h);
      }
      this.tmpPoints.push(c);
    }
    if (this.tmpPoints.length < 2)
      return;
    const n = this.transformDataAll(!0);
    if (n.length)
      for (let a = 0; a < n.length; a++) {
        const { pos: c, points: l } = n[a], h = l.map((u) => u.point.addXY(c[0], c[1])), p = [];
        for (const u of o) {
          const f = [];
          for (let g = 0; g < u.length; g += 2) {
            const I = new O(u[g], u[g + 1]);
            if (f.length > 0) {
              const S = f[f.length - 1].point, v = W.Sub(I, S).uni();
              I.setv(v);
            }
            f.push({
              point: I,
              radius: r
            });
          }
          const { ps: m } = this.computStroke(f, !1);
          p.push(m.map((g) => g.XY));
        }
        Cs(
          h,
          p,
          0.01
        ).forEach((u) => {
          e.push(u.map((f) => [f[0], f[1], f[2]]).flat(1));
        });
      }
    return e;
  }
  transformDataAll(t = !0) {
    return this.getTaskPoints(
      this.tmpPoints,
      t && this.workOptions.thickness || void 0
    );
  }
  draw(t) {
    const {
      attrs: e,
      tasks: s,
      replaceId: o,
      effects: r,
      layer: n,
      removeDrawCount: a,
      isDrawEraserlines: c = !0
    } = t, {
      strokeColor: l,
      strokeType: h,
      thickness: p,
      zIndex: d,
      scale: u,
      rotate: f,
      translate: m,
      eraserlines: g,
      lineCap: I,
      lineDash: S
    } = this.workOptions;
    r != null && r.size && (r.forEach((P) => {
      var M;
      (M = n.getElementById(P + "")) == null || M.remove();
    }), r.clear()), a && this.removeDrawCountNodes(n, a);
    let v;
    const L = [], C = n.worldPosition, N = n.worldScaling;
    for (let P = 0; P < s.length; P++) {
      const { pos: M, points: D } = s[P], { ps: B, rect: F } = this.computDrawPoints(D);
      let U;
      const V = D.length === 1;
      h === G.Stroke || V ? U = mt(B, !0) : U = mt(B, !1);
      const Z = {
        pos: M,
        d: U,
        fillColor: h === G.Stroke || V ? l : void 0,
        lineDash: V ? void 0 : h === G.Dotted ? [
          S && S[0] || 1,
          (S && S[1] || 2) * p
        ] : h === G.LongDotted ? [
          (S && S[0] || 1) * p,
          (S && S[1] || 2) * p
        ] : void 0,
        strokeColor: l,
        lineCap: V ? void 0 : I,
        lineWidth: h === G.Stroke || V ? 0 : p
      };
      v = E(v, {
        x: Math.floor(
          (F.x + M[0]) * N[0] + C[0] - b.SafeBorderPadding
        ),
        y: Math.floor(
          (F.y + M[1]) * N[1] + C[1] - b.SafeBorderPadding
        ),
        w: Math.floor(
          F.w * N[0] + 2 * b.SafeBorderPadding
        ),
        h: Math.floor(
          F.h * N[1] + 2 * b.SafeBorderPadding
        )
      }), L.push(Z);
    }
    u && (e.scale = u), f && (e.rotate = f), m && (e.translate = m);
    const R = new j();
    if (v) {
      this.centerPos = b.getCenterPos(v, n);
      const P = h === G.Stroke && !g;
      R.attr({
        ...e,
        normalize: !0,
        anchor: [0.5, 0.5],
        bgcolor: P ? l : void 0,
        pos: this.centerPos,
        size: [
          (v.w - 2 * b.SafeBorderPadding) / N[0],
          (v.h - 2 * b.SafeBorderPadding) / N[1]
        ],
        zIndex: d
      });
      const M = L.map((D) => (D.pos = [
        D.pos[0] - this.centerPos[0],
        D.pos[1] - this.centerPos[1]
      ], new wt(D)));
      R.append(...M), P && R.seal(), g && (this.scaleType = z.proportional, this.drawEraserlines(
        {
          group: R,
          eraserlines: g,
          pos: this.centerPos,
          layer: n
        },
        c
      )), this.replace(n, o, R);
    }
    if (u || f || m) {
      const P = R == null ? void 0 : R.getBoundingClientRect();
      P && (v = {
        x: Math.floor(P.x - b.SafeBorderPadding),
        y: Math.floor(P.y - b.SafeBorderPadding),
        w: Math.floor(P.width + b.SafeBorderPadding * 2),
        h: Math.floor(P.height + b.SafeBorderPadding * 2)
      });
    }
    return this.isDelete && R.setAttribute("opacity", 0), v;
  }
  computDrawPoints(t) {
    return this.workOptions.strokeType === G.Stroke || t.length === 1 ? this.computStroke(t) : this.computNomal(t);
  }
  computNomal(t) {
    let e = this.workOptions.thickness;
    const s = t.map((o) => (e = Math.max(e, o.radius), o.point));
    return { ps: s, rect: $(s, e) };
  }
  computStroke(t, e = !0) {
    return t.length === 1 ? this.computDotStroke(t[0]) : this.computLineStroke(t, e);
  }
  computLineStroke(t, e = !0) {
    const s = [], o = [];
    for (let a = 0; a < t.length; a++) {
      const { point: c, radius: l } = t[a];
      let h = c.v;
      a === 0 && t.length > 1 && (h = t[a + 1].point.v);
      const p = W.Per(h).mul(l);
      s.push(O.Sub(c, p)), o.push(O.Add(c, p));
    }
    const r = t[t.length - 1];
    if (e) {
      const a = O.GetSemicircleStroke(
        r.point,
        s[s.length - 1],
        -1,
        8
      ), c = O.GetSemicircleStroke(
        t[0].point,
        o[0],
        -1,
        8
      ), l = s.concat(a, o.reverse(), c);
      return { ps: l, rect: $(l) };
    }
    const n = s.concat(o.reverse());
    return { ps: n, rect: $(n) };
  }
  computDotStroke(t) {
    const { point: e, radius: s } = t, o = {
      x: e.x - s,
      y: e.y - s,
      w: s * 2,
      h: s * 2
    };
    return { ps: O.GetDotStroke(e, s, 8), rect: o };
  }
  transformData(t, e) {
    const { op: s, workState: o } = t;
    let r = this.tmpPoints.length - 1, n = [];
    if (s != null && s.length && o) {
      const { strokeType: a, thickness: c } = this.workOptions, l = /* @__PURE__ */ new Set();
      r = a === G.Stroke ? this.updateTempPointsWithPressure(s, c, l) : this.updateTempPoints(s, c, l);
      const h = e ? this.tmpPoints : this.tmpPoints.slice(r);
      return n = this.getTaskPoints(h, c), { tasks: n, effects: l, consumeIndex: r };
    }
    return { tasks: n, consumeIndex: r };
  }
  /** 压力渐变公式 */
  computRadius(t, e) {
    return t * 0.03 * e + e * 0.5;
  }
  getMinZ(t, e) {
    return ((e || Math.max(1, Math.floor(t * 0.3))) - t * 0.5) * 100 / t / 3;
  }
  getTaskPoints(t, e) {
    var h;
    const s = [];
    if (t.length === 0)
      return [];
    let o = 0, r = t[0].x, n = t[0].y, a = [r, n], c = [], l = t[0].t;
    for (; o < t.length; ) {
      const p = t[o], d = p.x - r, u = p.y - n, f = p.z, m = e ? this.computRadius(f, e) : f;
      if (c.push({
        point: new O(d, u, f, t[o].v),
        radius: m
      }), o > 0 && o < t.length - 1) {
        const g = t[o].getAngleByPoints(
          t[o - 1],
          t[o + 1]
        );
        if (g < 60 || g > 300) {
          const I = (h = c.pop()) == null ? void 0 : h.point.clone();
          I && s.push({
            taskId: l,
            pos: a,
            points: [
              ...c,
              {
                point: I,
                radius: m
              }
            ]
          }), r = t[o].x, n = t[o].y, a = [r, n];
          const S = p.x - r, v = p.y - n;
          c = [
            {
              point: new O(S, v, f),
              radius: m
            }
          ], l = Date.now();
        }
      }
      o++;
    }
    return s.push({
      taskId: l,
      pos: a,
      points: c
    }), s;
  }
  updateTempPointsWithPressure(t, e, s) {
    const o = Date.now(), r = this.tmpPoints.length;
    let n = r;
    for (let c = 0; c < t.length; c += 2) {
      n = Math.min(n, r);
      const l = this.tmpPoints.length, h = new O(
        t[c],
        t[c + 1]
      );
      if (l === 0) {
        this.tmpPoints.push(h);
        continue;
      }
      const p = l - 1, d = this.tmpPoints[p], u = W.Sub(h, d).uni();
      if (h.isNear(d, e)) {
        if (d.z < this.MAX_REPEAR) {
          if (d.setz(Math.min(d.z + 1, this.MAX_REPEAR)), n = Math.min(n, p), l > 1) {
            let g = l - 1;
            for (; g > 0; ) {
              const I = this.tmpPoints[g].distance(
                this.tmpPoints[g - 1]
              ), S = Math.max(
                this.tmpPoints[g].z - this.uniThickness * I,
                0
              );
              if (this.tmpPoints[g - 1].z >= S)
                break;
              this.tmpPoints[g - 1].setz(S), n = Math.min(n, g - 1), g--;
            }
          }
        } else
          n = 1 / 0;
        continue;
      }
      h.setv(u);
      const f = h.distance(d), m = Math.max(d.z - this.uniThickness * f, 0);
      l > 1 && W.Equals(u, d.v, 0.02) && (m > 0 || d.z <= 0) && (s && d.t && s.add(d.t), this.tmpPoints.pop(), n = Math.min(p, n)), h.setz(m), this.tmpPoints.push(h);
    }
    if (n === 1 / 0)
      return this.tmpPoints.length;
    let a = r;
    if (n === r) {
      a = Math.max(a - 1, 0);
      const c = this.tmpPoints[a].t;
      c && (s == null || s.add(c));
    } else {
      let c = r - 1;
      for (a = n; c >= 0; ) {
        const l = this.tmpPoints[c].t;
        if (l && (s == null || s.add(l), c <= n)) {
          a = c, c = -1;
          break;
        }
        c--;
      }
    }
    return this.tmpPoints[a].setT(o), a;
  }
  updateTempPoints(t, e, s) {
    var c;
    const o = Date.now(), r = this.tmpPoints.length;
    let n = r;
    for (let l = 0; l < t.length; l += 2) {
      const h = this.tmpPoints.length, p = new O(
        t[l],
        t[l + 1]
      );
      if (h === 0) {
        this.tmpPoints.push(p);
        continue;
      }
      const d = h - 1, u = this.tmpPoints[d], f = W.Sub(p, u).uni();
      if (p.isNear(u, e / 2)) {
        n = Math.min(d, n);
        continue;
      }
      W.Equals(f, u.v, 0.02) && (s && u.t && s.add(u.t), this.tmpPoints.pop(), n = Math.min(d, n)), p.setv(f), this.tmpPoints.push(p);
    }
    let a = r;
    if (n === r) {
      a = Math.max(a - 1, 0);
      const l = this.tmpPoints[a].t;
      l && (s == null || s.add(l));
    } else {
      let l = Math.min(r - 1, n);
      for (a = n; l >= 0; ) {
        const h = (c = this.tmpPoints[l]) == null ? void 0 : c.t;
        if (h && (s == null || s.add(h), l <= n)) {
          a = l, l = -1;
          break;
        }
        l--;
      }
    }
    return this.tmpPoints[a].setT(o), a;
  }
  updateTempPointsWithPressureWhenDone(t) {
    const { thickness: e } = this.workOptions, s = t.length, o = this.getMinZ(e);
    for (let r = 0; r < s; r += 2) {
      const n = this.tmpPoints.length, a = new O(
        t[r],
        t[r + 1]
      );
      if (n === 0) {
        this.tmpPoints.push(a);
        continue;
      }
      const c = n - 1, l = this.tmpPoints[c], h = W.Sub(a, l).uni(), p = a.distance(l);
      if (n > 1 && l.z === o)
        break;
      if (a.isNear(l, e / 2)) {
        if (s < 3 && l.z < this.MAX_REPEAR && (l.setz(Math.min(l.z + 1, this.MAX_REPEAR)), n > 1)) {
          let u = n - 1;
          for (; u > 0; ) {
            const f = this.tmpPoints[u].distance(
              this.tmpPoints[u - 1]
            ), m = Math.max(
              this.tmpPoints[u].z - this.uniThickness * f,
              -e / 4
            );
            if (this.tmpPoints[u - 1].z >= m)
              break;
            this.tmpPoints[u - 1].setz(m), u--;
          }
        }
        continue;
      }
      a.setv(h);
      const d = Math.max(l.z - this.uniThickness * p, o);
      n > 1 && W.Equals(h, l.v, 0.02) && l.z <= 0 && this.tmpPoints.pop(), a.setz(d), this.tmpPoints.push(a);
    }
  }
  static updateNodeOpt(t) {
    var h, p;
    const { node: e, opt: s, vNodes: o } = t, { strokeColor: r, strokeType: n, lineCap: a, lineDash: c } = s, l = o.get(e.name);
    return r && (e.tagName === "GROUP" ? so(e) ? e.setAttribute("bgcolor", r) : e.children.forEach((d) => {
      d.setAttribute("strokeColor", r), d.getAttribute("fillColor") && d.setAttribute("fillColor", r);
    }) : (e.setAttribute("strokeColor", r), e.setAttribute("fillColor", r)), (h = l == null ? void 0 : l.opt) != null && h.strokeColor && (l.opt.strokeColor = r)), n && (l != null && l.opt) && ((p = l.opt) != null && p.strokeType) && (l.opt.strokeType = n), a && (l != null && l.opt) && (l.opt.lineCap = a), c && (l != null && l.opt) && (l.opt.lineDash = c), l && o.setInfo(e.name, l), b.updateNodeOpt(t);
  }
}
class xe extends b {
  constructor(t) {
    super(t);
    y(this, "toolsType", k.LaserPen);
    y(this, "canRotate", !1);
    y(this, "scaleType", z.none);
    y(this, "syncTimestamp");
    y(this, "syncIndex", 0);
    y(this, "tmpPoints", []);
    y(this, "workOptions");
    y(this, "consumeIndex", 0);
    this.workOptions = t.toolsOpt, this.syncTimestamp = 0;
  }
  combineConsume() {
  }
  setWorkOptions(t) {
    super.setWorkOptions(t), this.syncTimestamp = Date.now();
  }
  consume(t) {
    const { data: e, isSubWorker: s } = t, { workId: o, op: r, syncUnitTime: n } = e;
    if ((r == null ? void 0 : r.length) === 0)
      return { type: w.None };
    if (n && (this.syncUnitTime = n), this.updateTempPoints(r || []), this.consumeIndex > this.tmpPoints.length - 4)
      return { type: w.None };
    const { strokeColor: a, thickness: c, strokeType: l, lineDash: h, lineCap: p } = this.workOptions, d = $(this.tmpPoints, c);
    let u = !1;
    const f = this.syncIndex, m = this.tmpPoints.slice(this.consumeIndex);
    this.consumeIndex = this.tmpPoints.length - 1, this.syncTimestamp === 0 && (this.syncTimestamp = Date.now());
    const g = {
      name: o == null ? void 0 : o.toString(),
      opacity: 1,
      lineDash: l === G.Dotted ? [
        h && h[0] || 1,
        (h && h[1] || 2) * c
      ] : l === G.LongDotted ? [
        (h && h[0] || 1) * c,
        (h && h[1] || 2) * c
      ] : void 0,
      strokeColor: a,
      lineCap: p,
      lineWidth: c,
      anchor: [0.5, 0.5]
    }, I = this.getTaskPoints(m);
    if (I.length) {
      const v = Date.now();
      v - this.syncTimestamp > this.syncUnitTime && (u = !0, this.syncTimestamp = v, this.syncIndex = this.tmpPoints.length), s && this.draw({
        attrs: g,
        tasks: I,
        isDot: !1,
        layer: this.drawLayer || this.fullLayer
      });
    }
    const S = [];
    return this.tmpPoints.slice(f).forEach((v) => {
      S.push(v.x, v.y);
    }), {
      rect: {
        x: d.x * this.fullLayer.worldScaling[0] + this.fullLayer.worldPosition[0],
        y: d.y * this.fullLayer.worldScaling[1] + this.fullLayer.worldPosition[1],
        w: d.w * this.fullLayer.worldScaling[0],
        h: d.h * this.fullLayer.worldScaling[1]
      },
      type: w.DrawWork,
      dataType: x.Local,
      op: u ? S : void 0,
      index: u ? f * 2 : void 0,
      ...this.baseConsumeResult
    };
  }
  consumeAll() {
    var r;
    const t = (r = this.workId) == null ? void 0 : r.toString();
    let e;
    if (this.tmpPoints.length - 1 > this.consumeIndex) {
      let n = this.tmpPoints.slice(this.consumeIndex);
      const a = n.length === 1, { strokeColor: c, thickness: l, strokeType: h } = this.workOptions;
      if (a) {
        const u = this.computDotStroke({
          point: n[0],
          radius: l / 2
        });
        n = u.ps, e = u.rect;
      } else
        e = $(this.tmpPoints, l);
      const p = {
        name: t == null ? void 0 : t.toString(),
        fillColor: a ? c : void 0,
        opacity: 1,
        lineDash: h === G.Dotted && !a ? [1, l * 2] : h === G.LongDotted && !a ? [l, l * 2] : void 0,
        strokeColor: c,
        lineCap: a ? void 0 : "round",
        lineWidth: a ? 0 : l,
        anchor: [0.5, 0.5]
      }, d = this.getTaskPoints(n);
      d.length && this.draw({
        attrs: p,
        tasks: d,
        isDot: a,
        layer: this.drawLayer || this.fullLayer
      });
    }
    const s = [];
    this.tmpPoints.forEach((n) => {
      s.push(n.x, n.y);
    });
    const o = tt(s);
    return {
      rect: e && {
        x: e.x * this.fullLayer.worldScaling[0] + this.fullLayer.worldPosition[0],
        y: e.y * this.fullLayer.worldScaling[1] + this.fullLayer.worldPosition[1],
        w: e.w * this.fullLayer.worldScaling[0],
        h: e.h * this.fullLayer.worldScaling[1]
      },
      type: w.FullWork,
      dataType: x.Local,
      ops: o,
      index: this.syncIndex * 2,
      ...this.baseConsumeResult
    };
  }
  clearTmpPoints() {
    this.tmpPoints.length = 0, this.syncTimestamp = 0, this.syncIndex = 0;
  }
  consumeService(t) {
    var f;
    const { op: e, replaceId: s, isFullWork: o } = t, { strokeColor: r, thickness: n, strokeType: a } = this.workOptions;
    if (!e.length) {
      const m = $(this.tmpPoints, n);
      return {
        x: m.x * this.fullLayer.worldScaling[0] + this.fullLayer.worldPosition[0],
        y: m.y * this.fullLayer.worldScaling[1] + this.fullLayer.worldPosition[1],
        w: m.w * this.fullLayer.worldScaling[0],
        h: m.h * this.fullLayer.worldScaling[1]
      };
    }
    const c = Math.max(0, this.tmpPoints.length - 1);
    this.updateTempPoints(e || []);
    let l, h = this.tmpPoints.slice(c);
    const p = h.length === 1;
    if (p) {
      const m = this.computDotStroke({
        point: h[0],
        radius: n / 2
      });
      h = m.ps, l = m.rect;
    } else
      l = $(this.tmpPoints, n);
    const d = {
      name: (f = this.workId) == null ? void 0 : f.toString(),
      fillColor: p ? r : void 0,
      opacity: 1,
      lineDash: a === G.Dotted && !p ? [1, n * 2] : a === G.LongDotted && !p ? [n, n * 2] : void 0,
      strokeColor: r,
      lineCap: p ? void 0 : "round",
      lineWidth: p ? 0 : n,
      anchor: [0.5, 0.5]
    }, u = this.getTaskPoints(h);
    if (u.length) {
      const m = o ? this.fullLayer : this.drawLayer || this.fullLayer;
      this.draw({ attrs: d, tasks: u, isDot: p, replaceId: s, layer: m });
    }
    return {
      x: l.x * this.fullLayer.worldScaling[0] + this.fullLayer.worldPosition[0],
      y: l.y * this.fullLayer.worldScaling[1] + this.fullLayer.worldPosition[1],
      w: l.w * this.fullLayer.worldScaling[0],
      h: l.h * this.fullLayer.worldScaling[1]
    };
  }
  computDotStroke(t) {
    const { point: e, radius: s } = t, o = {
      x: e.x - s,
      y: e.y - s,
      w: s * 2,
      h: s * 2
    };
    return { ps: O.GetDotStroke(e, s, 8), rect: o };
  }
  updateTempPoints(t) {
    const e = this.tmpPoints.length;
    for (let s = 0; s < t.length; s += 2) {
      if (e) {
        const o = this.tmpPoints.slice(-1)[0];
        o && o.x === t[s] && o.y === t[s + 1] && this.tmpPoints.pop();
      }
      this.tmpPoints.push(
        new O(t[s], t[s + 1])
      );
    }
  }
  async draw(t) {
    const { attrs: e, tasks: s, isDot: o, layer: r } = t, { duration: n } = this.workOptions;
    for (const a of s) {
      const c = new wt(), { pos: l, points: h } = a;
      let p;
      o ? p = mt(h, !0) : p = mt(h, !1), c.attr({
        ...e,
        pos: l,
        d: p
      }), r.appendChild(c), c.transition(n).attr({
        scale: o ? [0.1, 0.1] : [1, 1],
        lineWidth: o ? 0 : 1
      }).then(() => {
        c.remove();
      });
    }
  }
  getTaskPoints(t) {
    var c;
    const e = [];
    if (t.length === 0)
      return [];
    let s = 0, o = t[0].x, r = t[0].y, n = [o, r], a = [];
    for (; s < t.length; ) {
      const l = t[s], h = l.x - o, p = l.y - r;
      if (a.push(new O(h, p)), s > 0 && s < t.length - 1) {
        const d = t[s].getAngleByPoints(
          t[s - 1],
          t[s + 1]
        );
        if (d < 60 || d > 300) {
          const u = (c = a.pop()) == null ? void 0 : c.clone();
          u && e.push({
            pos: n,
            points: [...a, u]
          }), o = t[s].x, r = t[s].y, n = [o, r];
          const f = l.x - o, m = l.y - r;
          a = [new O(f, m)];
        }
      }
      s++;
    }
    return e.push({
      pos: n,
      points: a
    }), e;
  }
  removeLocal() {
  }
  removeService(t) {
    let e;
    const s = [];
    return this.fullLayer.getElementsByName(t).forEach((o) => {
      if (o.name === t) {
        const r = o.getBoundingClientRect();
        e = E(e, {
          x: r.x,
          y: r.y,
          w: r.width,
          h: r.height
        }), s.push(o);
      }
    }), s.length && s.forEach((o) => o.remove()), e;
  }
}
const vt = class vt extends b {
  constructor(t, e) {
    super(t);
    y(this, "canRotate", !1);
    y(this, "scaleType", z.none);
    y(this, "toolsType", k.Eraser);
    y(this, "serviceWork");
    y(this, "tmpPoints", []);
    y(this, "workOptions");
    y(this, "worldPosition");
    y(this, "worldScaling");
    y(this, "eraserRect");
    y(this, "eraserPolyline");
    this.serviceWork = e, this.workOptions = t.toolsOpt, this.worldPosition = this.fullLayer.worldPosition, this.worldScaling = this.fullLayer.worldScaling;
  }
  combineConsume() {
  }
  consumeService() {
  }
  setWorkOptions(t) {
    super.setWorkOptions(t);
  }
  createEraserRect(t) {
    const e = t[0] * this.worldScaling[0] + this.worldPosition[0], s = t[1] * this.worldScaling[1] + this.worldPosition[1], { width: o, height: r } = vt.eraserSizes[this.workOptions.thickness];
    this.eraserRect = {
      x: e - o * 0.5,
      y: s - r * 0.5,
      w: o,
      h: r
    }, this.eraserPolyline = [
      this.eraserRect.x,
      this.eraserRect.y,
      this.eraserRect.x + this.eraserRect.w,
      this.eraserRect.y + this.eraserRect.h
    ];
  }
  computRectCenterPoints() {
    const t = this.tmpPoints.slice(-2);
    if (this.tmpPoints.length === 4) {
      const e = new W(this.tmpPoints[0], this.tmpPoints[1]), s = new W(this.tmpPoints[2], this.tmpPoints[3]), o = W.Sub(s, e).uni(), r = W.Dist(e, s), { width: n, height: a } = vt.eraserSizes[this.workOptions.thickness], c = Math.min(n, a), l = Math.round(r / c);
      if (l > 1) {
        const h = [];
        for (let p = 0; p < l; p++) {
          const d = W.Mul(o, p * c);
          h.push(this.tmpPoints[0] + d.x, this.tmpPoints[1] + d.y);
        }
        return h.concat(t);
      }
    }
    return t;
  }
  isNear(t, e) {
    const s = new W(t[0], t[1]), o = new W(e[0], e[1]), { width: r, height: n } = vt.eraserSizes[this.workOptions.thickness];
    return W.Dist(s, o) < Math.hypot(r, n) * 0.5;
  }
  remove(t) {
    const { curNodeMap: e, removeIds: s } = t;
    let o;
    for (const r of e.values())
      if (r.rect && this.eraserRect && this.eraserPolyline && Zt(this.eraserRect, r.rect)) {
        const { op: n } = r, a = [], c = [];
        for (let h = 0; h < n.length; h += 3) {
          const p = new W(
            n[h] * this.worldScaling[0] + this.worldPosition[0],
            n[h + 1] * this.worldScaling[1] + this.worldPosition[1],
            n[h + 2]
          );
          c.push(p), a.push(new O(p.x, p.y));
        }
        const l = a.length && $(a) || r.rect;
        Zt(l, this.eraserRect) && (c.length > 1 ? ie.polyline(
          c.map((p) => p.XY),
          this.eraserPolyline
        ).length && s.add(r.name) : s.add(r.name), o = E(o, r.rect || l));
      }
    return s.forEach((r) => {
      var a;
      const n = this.fullLayer.getElementsByName(r);
      n[0] && (n[0].remove(), q(n[0], this.fullLayer.parent), (a = this.vNodes) == null || a.delete(r));
    }), o && (o.x -= b.SafeBorderPadding, o.y -= b.SafeBorderPadding, o.w += b.SafeBorderPadding * 2, o.h += b.SafeBorderPadding * 2), o;
  }
  consume(t) {
    const { op: e, disableEraseImage: s, disableEraseText: o } = t.data;
    if (!e || e.length === 0)
      return {
        type: w.None,
        ...this.baseConsumeResult
      };
    const r = this.tmpPoints.length;
    if (r > 1 && this.isNear(
      [e[0], e[1]],
      [this.tmpPoints[r - 2], this.tmpPoints[r - 1]]
    ))
      return {
        type: w.None,
        ...this.baseConsumeResult
      };
    r < 3 ? this.tmpPoints.push(e[0], e[1]) : this.tmpPoints.splice(2, 2, e[0], e[1]);
    const n = this.computRectCenterPoints();
    let a;
    const c = /* @__PURE__ */ new Set();
    if (!this.vNodes)
      return {
        type: w.None,
        ...this.baseConsumeResult
      };
    const l = this.getCanEraserNodeMap(
      this.vNodes.getCanEraserNodes(this.vNodes.curNodeMap, {
        disableEraseImage: s,
        disableEraseText: o
      })
    );
    for (let h = 0; h < n.length - 1; h += 2) {
      this.createEraserRect(n.slice(h, h + 2));
      const p = this.remove({ curNodeMap: l, removeIds: c });
      a = E(a, p);
    }
    return a && c.size ? {
      type: w.RemoveNode,
      rect: a,
      removeIds: [...c]
    } : {
      type: w.None,
      ...this.baseConsumeResult
    };
  }
  consumeAll(t) {
    return this.consume(t);
  }
  clearTmpPoints() {
    this.tmpPoints.length = 0;
  }
  getCanEraserNodeMap(t) {
    var e;
    if (this.serviceWork) {
      const s = new Map(
        t
      ), o = this.serviceWork.selectorWorkShapes, r = this.serviceWork.workShapes;
      for (const n of o.values())
        if ((e = n.selectIds) != null && e.length)
          for (const a of n.selectIds)
            s.delete(a);
      for (const n of r.keys())
        s.delete(n);
      return s;
    }
    return t;
  }
};
// 来源于white-sdk
y(vt, "eraserSizes", re);
let Ut = vt;
var ye = je, bs = 1, Rs = Object.prototype, Ns = Rs.hasOwnProperty;
function xs(T, i, t, e, s, o) {
  var r = t & bs, n = ye(T), a = n.length, c = ye(i), l = c.length;
  if (a != l && !r)
    return !1;
  for (var h = a; h--; ) {
    var p = n[h];
    if (!(r ? p in i : Ns.call(i, p)))
      return !1;
  }
  var d = o.get(T), u = o.get(i);
  if (d && u)
    return d == i && u == T;
  var f = !0;
  o.set(T, i), o.set(i, T);
  for (var m = r; ++h < a; ) {
    p = n[h];
    var g = T[p], I = i[p];
    if (e)
      var S = r ? e(I, g, p, i, T, o) : e(g, I, p, T, i, o);
    if (!(S === void 0 ? g === I || s(g, I, t, e, o) : S)) {
      f = !1;
      break;
    }
    m || (m = p == "constructor");
  }
  if (f && !m) {
    var v = T.constructor, L = i.constructor;
    v != L && "constructor" in T && "constructor" in i && !(typeof v == "function" && v instanceof v && typeof L == "function" && L instanceof L) && (f = !1);
  }
  return o.delete(T), o.delete(i), f;
}
var Ds = xs, jt = Ze, Ms = Ke, Os = Qe, As = Ds, we = Je, ge = ss, ke = ts, Es = es, Bs = 1, Se = "[object Arguments]", Te = "[object Array]", Ft = "[object Object]", Fs = Object.prototype, ve = Fs.hasOwnProperty;
function zs(T, i, t, e, s, o) {
  var r = ge(T), n = ge(i), a = r ? Te : we(T), c = n ? Te : we(i);
  a = a == Se ? Ft : a, c = c == Se ? Ft : c;
  var l = a == Ft, h = c == Ft, p = a == c;
  if (p && ke(T)) {
    if (!ke(i))
      return !1;
    r = !0, l = !1;
  }
  if (p && !l)
    return o || (o = new jt()), r || Es(T) ? Ms(T, i, t, e, s, o) : Os(T, i, a, t, e, s, o);
  if (!(t & Bs)) {
    var d = l && ve.call(T, "__wrapped__"), u = h && ve.call(i, "__wrapped__");
    if (d || u) {
      var f = d ? T.value() : T, m = u ? i.value() : i;
      return o || (o = new jt()), s(f, m, t, e, o);
    }
  }
  return p ? (o || (o = new jt()), As(T, i, t, e, s, o)) : !1;
}
var Us = zs, Gs = Us, Ie = te;
function De(T, i, t, e, s) {
  return T === i ? !0 : T == null || i == null || !Ie(T) && !Ie(i) ? T !== T && i !== i : Gs(T, i, t, e, De, s);
}
var Xs = De, Ys = Xs;
function $s(T, i) {
  return Ys(T, i);
}
var _s = $s;
const lt = /* @__PURE__ */ Jt(_s), _t = class _t extends b {
  constructor(t) {
    super(t);
    y(this, "toolsType", k.Selector);
    y(this, "tmpPoints", []);
    y(this, "subTmpPoints", []);
    y(this, "workOptions");
    y(this, "vNodes");
    y(this, "selectIds");
    y(this, "selectorColor");
    y(this, "strokeColor");
    y(this, "fillColor");
    y(this, "oldSelectRect");
    y(this, "oldSubSelectRect");
    y(this, "canRotate", !1);
    y(this, "canTextEdit", !1);
    y(this, "canLock", !1);
    y(this, "scaleType", z.all);
    y(this, "toolsTypes");
    y(this, "shapeOpt");
    y(this, "textOpt");
    y(this, "isLocked");
    y(this, "thickness");
    y(this, "strokeType");
    y(this, "useStroke");
    this.workOptions = t.toolsOpt, this.vNodes = t.vNodes;
  }
  computSelector(t = !0) {
    const e = $(this.tmpPoints);
    if (e.w === 0 || e.h === 0)
      return {
        selectIds: [],
        intersectRect: void 0,
        subNodeMap: /* @__PURE__ */ new Map()
      };
    const { rectRange: s, nodeRange: o } = this.vNodes.getRectIntersectRange(
      e,
      t
    );
    return {
      selectIds: [...o.keys()],
      intersectRect: s,
      subNodeMap: o
    };
  }
  updateTempPoints(t) {
    const e = this.tmpPoints.length, s = t.length;
    if (s > 1) {
      const o = new O(
        t[s - 2] * this.fullLayer.worldScaling[0] + this.fullLayer.worldPosition[0],
        t[s - 1] * this.fullLayer.worldScaling[0] + this.fullLayer.worldPosition[1]
      );
      e === 2 ? this.tmpPoints.splice(1, 1, o) : this.tmpPoints.push(o);
    }
  }
  drawSelector(t) {
    const { drawRect: e, subNodeMap: s, selectorId: o, layer: r, isService: n } = t, a = new j({
      pos: [e.x, e.y],
      anchor: [0, 0],
      size: [e.w, e.h],
      id: o,
      name: o,
      zIndex: 9999
    }), c = [];
    if (n) {
      const l = new Kt({
        normalize: !0,
        pos: [e.w / 2, e.h / 2],
        lineWidth: 1,
        strokeColor: this.selectorColor || this.workOptions.strokeColor,
        width: e.w,
        height: e.h,
        name: _t.selectorBorderId
      });
      c.push(l);
    }
    s.forEach((l, h) => {
      const p = [
        l.rect.x + l.rect.w / 2 - e.x,
        l.rect.y + l.rect.h / 2 - e.y
      ], d = new Kt({
        normalize: !0,
        pos: p,
        lineWidth: 1,
        strokeColor: s.size > 1 ? this.selectorColor || this.workOptions.strokeColor : void 0,
        width: l.rect.w,
        height: l.rect.h,
        id: `selector-${h}`,
        name: `selector-${h}`
      });
      c.push(d);
    }), c && a.append(...c), (r == null ? void 0 : r.parent).appendChild(a);
  }
  draw(t, e, s, o = !1) {
    var a, c;
    const { intersectRect: r, subNodeMap: n } = s;
    (c = (a = e.parent) == null ? void 0 : a.getElementById(t)) == null || c.remove(), r && this.drawSelector({
      drawRect: r,
      subNodeMap: n,
      selectorId: t,
      layer: e,
      isService: o
    });
  }
  getSelecteorInfo(t) {
    this.scaleType = z.all, this.canRotate = !1, this.textOpt = void 0, this.strokeColor = void 0, this.fillColor = void 0, this.canTextEdit = !1, this.canLock = !1, this.isLocked = !1, this.toolsTypes = void 0, this.shapeOpt = void 0, this.thickness = void 0, this.strokeType = void 0, this.useStroke = !1;
    const e = /* @__PURE__ */ new Set();
    let s, o = !0;
    for (const r of t.values()) {
      const { opt: n, canRotate: a, scaleType: c, toolsType: l } = r;
      this.selectorColor = this.workOptions.strokeColor, n.strokeColor && (this.strokeColor = n.strokeColor), n.fillColor && (this.fillColor = n.fillColor), n.textOpt && (this.textOpt = n.textOpt), n.thickness && (this.thickness = n.thickness), l !== k.Pencil && (o = !1), n.strokeType && (this.strokeType = n.strokeType), l === k.SpeechBalloon && (e.add(l), this.shapeOpt || (this.shapeOpt = {}), this.shapeOpt.placement = n.placement), l === k.Polygon && (e.add(l), this.shapeOpt || (this.shapeOpt = {}), this.shapeOpt.vertices = n.vertices), l === k.Star && (e.add(l), this.shapeOpt || (this.shapeOpt = {}), this.shapeOpt.vertices = n.vertices, this.shapeOpt.innerRatio = n.innerRatio, this.shapeOpt.innerVerticeStep = n.innerVerticeStep), l === k.Text && (this.textOpt = n), t.size === 1 && (this.textOpt && (this.canTextEdit = !0), this.canRotate = a, this.scaleType = c), (l === k.Image || l === k.BackgroundSVG) && (s = r), (c === z.proportional && this.scaleType !== z.none || c === z.none) && (this.scaleType = c);
    }
    o && (this.useStroke = !0), e.size && (this.toolsTypes = [...e]), s && (t.size === 1 ? (this.canLock = !0, s.opt.locked && (this.isLocked = !0, this.scaleType = z.none, this.canRotate = !1, this.textOpt = void 0, this.fillColor = void 0, this.selectorColor = "rgb(177,177,177)", this.strokeColor = void 0, this.canTextEdit = !1, this.thickness = void 0, this.strokeType = void 0, this.useStroke = void 0)) : t.size > 1 && !s.opt.locked && (this.canLock = !1, this.canRotate = !1));
  }
  getChildrenPoints() {
    var t, e;
    if (this.scaleType === z.both && ((t = this.selectIds) == null ? void 0 : t.length) === 1) {
      const s = this.selectIds[0], o = (e = this.vNodes.get(s)) == null ? void 0 : e.op;
      if (o) {
        const r = [];
        for (let n = 0; n < o.length; n += 3)
          r.push([o[n], o[n + 1]]);
        return r;
      }
    }
  }
  consume(t) {
    if (t.isSubWorker)
      return this.subWorkerConsume(t);
    let e = {
      type: w.Select,
      dataType: x.Local,
      ...this.baseConsumeResult
    };
    if (t.isSimpleWorker) {
      const l = this.subWorkerConsume(t);
      e.subRect = l.rect;
    }
    const { op: s, workState: o } = t.data;
    let r = this.oldSelectRect;
    if (o === A.Start && (r = this.unSelectedAllIds()), !(s != null && s.length) || !this.vNodes.curNodeMap.size)
      return t.isSimpleWorker ? e : { type: w.None };
    this.updateTempPoints(s);
    const n = this.computSelector();
    if (this.selectIds && lt(this.selectIds, n.selectIds))
      return t.isSimpleWorker ? e : { type: w.None };
    this.selectIds = n.selectIds;
    const a = n.intersectRect;
    this.getSelecteorInfo(n.subNodeMap), this.draw(X, this.fullLayer, n), this.oldSelectRect = a;
    const c = this.getChildrenPoints();
    return e = {
      ...e,
      rect: E(a, r),
      selectIds: n.selectIds,
      selectRect: a,
      selectorColor: this.selectorColor,
      strokeColor: this.strokeColor,
      fillColor: this.fillColor,
      textOpt: this.textOpt,
      canTextEdit: this.canTextEdit,
      canRotate: this.canRotate,
      canLock: this.canLock,
      scaleType: this.scaleType,
      willSyncService: !0,
      points: c,
      isLocked: this.isLocked,
      toolsTypes: this.toolsTypes,
      shapeOpt: this.shapeOpt,
      thickness: this.thickness,
      useStroke: this.useStroke,
      strokeType: this.strokeType
    }, e;
  }
  consumeAll() {
    var e, s;
    let t = this.oldSelectRect;
    if (!((e = this.selectIds) != null && e.length) && this.tmpPoints[0] && this.selectSingleTool(this.tmpPoints[0].XY, X, !1), (s = this.selectIds) != null && s.length && (t = this.selectedByIds(this.selectIds)), t) {
      const o = this.getChildrenPoints();
      return {
        type: w.Select,
        dataType: x.Local,
        rect: this.oldSelectRect,
        selectIds: this.selectIds,
        selectorColor: this.selectorColor,
        selectRect: this.oldSelectRect,
        strokeColor: this.strokeColor,
        fillColor: this.fillColor,
        textOpt: this.textOpt,
        canTextEdit: this.canTextEdit,
        canRotate: this.canRotate,
        canLock: this.canLock,
        scaleType: this.scaleType,
        willSyncService: !0,
        points: o,
        isLocked: this.isLocked,
        toolsTypes: this.toolsTypes,
        shapeOpt: this.shapeOpt,
        thickness: this.thickness,
        useStroke: this.useStroke,
        strokeType: this.strokeType,
        ...this.baseConsumeResult
      };
    }
    return { type: w.None };
  }
  consumeService() {
  }
  updateTempPointsForSubWorker(t) {
    const e = t.slice(-2), s = new O(e[0], e[1]);
    if (this.subTmpPoints[0].isNear(s, 1))
      return !1;
    if (this.subTmpPoints.length === 2) {
      if (s.isNear(this.subTmpPoints[1], 1))
        return !1;
      this.subTmpPoints[1] = s;
    } else
      this.subTmpPoints.push(s);
    return !0;
  }
  computDrawPoints(t) {
    const { thickness: e } = this.workOptions, s = [];
    for (const n of t)
      s.push(new W(...n));
    const o = $(s, e), r = [
      o.x + o.w / 2,
      o.y + o.h / 2
    ];
    return {
      rect: o,
      pos: r,
      points: s.map((n) => n.XY).flat(1)
    };
  }
  drawForSubWorker(t) {
    const { workId: e, layer: s, ps: o } = t, { strokeColor: r, scale: n, rotate: a, translate: c } = this.workOptions, l = s.worldPosition, h = s.worldScaling, { points: p, rect: d, pos: u } = this.computDrawPoints(o), f = 1 / s.worldScaling[0], m = r && os(r) || [0, 0, 0, 0], g = {
      close: !0,
      normalize: !0,
      points: p,
      lineWidth: f,
      fillColor: he(m[0], m[1], m[2], 0.1),
      strokeColor: he(m[0], m[1], m[2], 1),
      lineJoin: "round",
      lineCap: "round"
    };
    let I = {
      x: Math.floor(
        d.x * h[0] + l[0] - b.SafeBorderPadding
      ),
      y: Math.floor(
        d.y * h[1] + l[1] - b.SafeBorderPadding
      ),
      w: Math.floor(
        d.w * h[0] + 2 * b.SafeBorderPadding
      ),
      h: Math.floor(
        d.h * h[0] + 2 * b.SafeBorderPadding
      )
    };
    const S = new j({
      name: e,
      id: e,
      pos: u,
      anchor: [0.5, 0.5],
      size: [d.w, d.h],
      scale: n,
      rotate: a,
      translate: c
    }), v = new ht({
      ...g,
      pos: [0, 0]
    });
    if (S.appendChild(v), this.replace(s, e, S), n || a || c) {
      const L = S.getBoundingClientRect();
      I = {
        x: Math.floor(L.x - b.SafeBorderPadding),
        y: Math.floor(L.y - b.SafeBorderPadding),
        w: Math.floor(L.width + 2 * b.SafeBorderPadding),
        h: Math.floor(L.height + 2 * b.SafeBorderPadding)
      };
    }
    return I;
  }
  transformData(t) {
    const e = $(t);
    return [
      [e.x, e.y, 0],
      [e.x + e.w, e.y, 0],
      [e.x + e.w, e.y + e.h, 0],
      [e.x, e.y + e.h, 0]
    ];
  }
  subWorkerConsume(t) {
    const { data: e, isFullWork: s } = t, { op: o, syncUnitTime: r } = e;
    r && (this.syncUnitTime = r);
    const n = o == null ? void 0 : o.length;
    if (!n || n < 2)
      return { type: w.None, rect: void 0 };
    let a;
    if (this.subTmpPoints.length === 0 ? (this.subTmpPoints = [new O(o[0], o[1])], a = !1) : a = this.updateTempPointsForSubWorker(o), !a)
      return { type: w.None, rect: void 0 };
    const c = this.transformData(this.subTmpPoints), l = s ? this.fullLayer : this.drawLayer || this.fullLayer, h = this.drawForSubWorker({
      ps: c,
      workId: this.workId,
      layer: l
    }), p = E(h, this.oldSubSelectRect);
    return this.oldSubSelectRect = h, {
      rect: p,
      type: w.DrawWork,
      dataType: x.Local,
      ...this.baseConsumeResult
    };
  }
  subWorkerConsumeAll(t) {
    const { isFullWork: e } = t, s = e ? this.fullLayer : this.drawLayer || this.fullLayer;
    return this.replace(s, this.workId), {
      rect: this.oldSelectRect,
      type: w.FullWork,
      dataType: x.Local,
      ...this.baseConsumeResult
    };
  }
  clearSubTmpPoints() {
    this.subTmpPoints.length = 0;
  }
  clearTmpPoints() {
    this.tmpPoints.length = 0, this.clearSubTmpPoints();
  }
  clearSubSelectData() {
    this.oldSubSelectRect = void 0;
  }
  clearSelectData() {
    this.selectIds = void 0, this.oldSelectRect = void 0, this.clearSubSelectData();
  }
  selectSingleTool(t, e = X, s = !1) {
    if (t.length === 2) {
      const o = t[0], r = t[1];
      let n;
      const { nodeRange: a } = this.vNodes.getRectIntersectRange(
        { x: o, y: r, w: 0, h: 0 },
        !1
      ), c = [...a.values()].sort(
        (l, h) => (h.opt.zIndex || 0) - (l.opt.zIndex || 0)
      );
      for (const l of c) {
        const h = this.fullLayer.getElementsByName(
          l.name
        );
        if (Ge(h).find((d) => d.isPointCollision(o, r))) {
          n = l;
          break;
        }
      }
      if (n) {
        const l = n.name;
        if (!lt(this.oldSelectRect, n.rect)) {
          const h = /* @__PURE__ */ new Map([[l, n]]);
          this.getSelecteorInfo(h), this.draw(
            e,
            this.fullLayer,
            {
              intersectRect: n.rect,
              subNodeMap: h,
              selectIds: this.selectIds || []
            },
            s
          );
        }
        this.selectIds = [l], this.oldSelectRect = n.rect;
      }
    }
  }
  unSelectedAllIds() {
    let t;
    for (const [e, s] of this.vNodes.curNodeMap.entries())
      s.isSelected && (t = E(t, s.rect), this.vNodes.unSelected(e));
    return t;
  }
  unSelectedByIds(t) {
    let e;
    for (const s of t) {
      const o = this.vNodes.get(s);
      o && o.isSelected && (e = E(e, o.rect), this.vNodes.unSelected(s));
    }
    return e;
  }
  selectedByIds(t) {
    let e;
    for (const s of t) {
      const o = this.vNodes.get(s);
      o && (e = E(e, o.rect), this.vNodes.selected(s));
    }
    return e;
  }
  getSelectorRect(t, e) {
    var n;
    let s;
    const o = (n = t.parent) == null ? void 0 : n.getElementById(
      e
    ), r = o == null ? void 0 : o.getBoundingClientRect();
    return r && (s = E(s, {
      x: Math.floor(r.x),
      y: Math.floor(r.y),
      w: Math.floor(r.width + 1),
      h: Math.floor(r.height + 1)
    })), s;
  }
  isCanFillColor(t) {
    return t === k.Ellipse || t === k.Triangle || t === k.Rectangle || t === k.Polygon || t === k.Star || t === k.SpeechBalloon;
  }
  async updateSelector(t) {
    const {
      updateSelectorOpt: e,
      selectIds: s,
      vNodes: o,
      willSerializeData: r,
      worker: n,
      offset: a
    } = t, c = this.fullLayer;
    if (!c)
      return;
    let l;
    const h = /* @__PURE__ */ new Map(), { originPoint: p, workState: d, angle: u, translate: f, dir: m, scale: g } = e;
    a && (f ? e.translate = [
      f[0] + a[0],
      f[1] + a[1]
    ] : e.translate = a);
    let I;
    if (p || f || H(u)) {
      if (d === A.Start && s)
        return o.setTargetAssignKeys(s), {
          type: w.Select,
          dataType: x.Local,
          selectRect: this.oldSelectRect,
          rect: this.oldSelectRect
        };
      if (I = o.getLastTarget(), !I)
        return;
    }
    if (s)
      for (const L of s) {
        const C = o.get(L);
        if (C) {
          const { toolsType: N, opt: R } = C, P = (c == null ? void 0 : c.getElementsByName(L))[0];
          if (P) {
            const M = { ...e };
            let D;
            const B = b.isWillRefresh({
              toolsType: N,
              opt: R,
              updateOpt: M,
              vNodes: o,
              node: P,
              willSerializeData: r
            });
            if (N) {
              D = I == null ? void 0 : I.get(L);
              const F = Ue(N);
              if (F == null || F.updateNodeOpt({
                node: P,
                opt: M,
                vNodes: o,
                willSerializeData: r,
                targetNode: D
              }), C && n && B) {
                const U = n.createWorkShapeNode({
                  workId: L,
                  toolsType: N,
                  toolsOpt: C.opt
                });
                U == null || U.setWorkId(L);
                let V;
                if (N === k.BackgroundSVG)
                  V = U.consumeService({
                    isFullWork: !0,
                    replaceId: L
                  });
                else if (N === k.Image)
                  V = await U.consumeServiceAsync({
                    isFullWork: !0,
                    replaceId: L,
                    worker: n
                  });
                else if (N === k.Text)
                  V = await U.consumeServiceAsync({
                    isFullWork: !0,
                    replaceId: L,
                    isDrawLabel: !0
                  });
                else
                  try {
                    V = U == null ? void 0 : U.consumeService({
                      op: C.op,
                      isFullWork: !0,
                      replaceId: L,
                      workState: d
                    });
                  } catch (Z) {
                    console.error("consumeService error", Z);
                    continue;
                  }
                V && (C.rect = V);
              }
              C && (h.set(L, C), l = E(l, C.rect));
            }
          }
        }
      }
    I && d === A.Done && (o.deleteLastTarget(), I = void 0);
    const S = l;
    if (p && f && g && m && S && !a) {
      const L = [
        [S.x, S.y],
        [S.x + S.w, S.y],
        [S.x + S.w, S.y + S.h],
        [S.x, S.y + S.h]
      ];
      let C;
      switch (m) {
        case "top":
        case "topLeft":
        case "left":
          f[0] > 0 && f[1] > 0 ? C = L[0] : f[0] > 0 ? C = L[3] : f[1] > 0 ? C = L[1] : C = L[2];
          break;
        case "topRight":
          f[0] < 0 && f[1] > 0 ? C = L[1] : f[0] < 0 ? C = L[2] : f[1] > 0 ? C = L[0] : C = L[3];
          break;
        case "right":
        case "bottomRight":
        case "bottom":
          f[0] < 0 && f[1] < 0 ? C = L[2] : f[0] < 0 ? C = L[1] : f[1] < 0 ? C = L[3] : C = L[0];
          break;
        case "bottomLeft":
          f[0] > 0 && f[1] < 0 ? C = L[3] : f[0] > 0 ? C = L[0] : f[1] < 0 ? C = L[2] : C = L[1];
          break;
      }
      const N = C && [
        p[0] - C[0],
        p[1] - C[1]
      ] || [0, 0];
      if (!lt(N, [0, 0]))
        return await this.updateSelector({
          ...t,
          updateSelectorOpt: { workState: d },
          offset: N
        });
    }
    this.getSelecteorInfo(h), this.draw(X, c, {
      selectIds: s || [],
      subNodeMap: h,
      intersectRect: S
    });
    const v = E(this.oldSelectRect, S);
    return this.oldSelectRect = S, {
      type: w.Select,
      dataType: x.Local,
      selectRect: S,
      renderRect: l,
      rect: E(v, S),
      selectIds: s
    };
  }
  blurSelector() {
    const t = this.unSelectedAllIds();
    return {
      type: w.Select,
      dataType: x.Local,
      rect: t,
      selectIds: [],
      willSyncService: !0
    };
  }
  getRightServiceId(t) {
    return t.replace(Wt, "-");
  }
  selectServiceNode(t, e, s) {
    const { selectIds: o } = e, r = this.getRightServiceId(t), n = this.getSelectorRect(
      this.fullLayer,
      r
    );
    let a;
    const c = /* @__PURE__ */ new Map();
    return o == null || o.forEach((l) => {
      const h = this.vNodes.get(l);
      h && (a = E(a, h.rect), c.set(l, h));
    }), this.getSelecteorInfo(c), this.draw(
      r,
      this.fullLayer,
      {
        intersectRect: a,
        selectIds: o || [],
        subNodeMap: c
      },
      s
    ), E(a, n);
  }
  reRenderSelector() {
    var s;
    let t;
    const e = /* @__PURE__ */ new Map();
    return (s = this.selectIds) == null || s.forEach((o) => {
      const r = this.vNodes.get(o);
      r && (t = E(t, r.rect), e.set(o, r));
    }, this), this.getSelecteorInfo(e), this.draw(X, this.fullLayer, {
      intersectRect: t,
      subNodeMap: e,
      selectIds: this.selectIds || []
    }), this.oldSelectRect = t, t;
  }
  updateSelectIds(t) {
    var r;
    let e;
    const s = (r = this.selectIds) == null ? void 0 : r.filter(
      (n) => !t.includes(n)
    );
    if (s != null && s.length && (e = this.unSelectedByIds(s)), t.length) {
      const n = this.selectedByIds(t);
      e = E(e, n);
    }
    this.selectIds = t;
    const o = this.reRenderSelector();
    return {
      bgRect: e,
      selectRect: o
    };
  }
  cursorHover(t) {
    var r, n;
    const e = this.oldSelectRect;
    this.selectIds = [];
    const s = (r = this.workId) == null ? void 0 : r.toString(), o = [
      t[0] * this.fullLayer.worldScaling[0] + this.fullLayer.worldPosition[0],
      t[1] * this.fullLayer.worldScaling[0] + this.fullLayer.worldPosition[1]
    ];
    if (this.selectSingleTool(o, s, !0), this.oldSelectRect && !lt(e, this.oldSelectRect))
      return {
        type: w.CursorHover,
        dataType: x.Local,
        rect: E(e, this.oldSelectRect),
        selectorColor: this.selectorColor,
        willSyncService: !1
      };
    if ((n = this.selectIds) != null && n.length || (this.oldSelectRect = void 0), e && !this.oldSelectRect)
      return this.cursorBlur(), {
        type: w.CursorHover,
        dataType: x.Local,
        rect: e,
        selectorColor: this.selectorColor,
        willSyncService: !1
      };
  }
  cursorBlur() {
    var e, s;
    this.selectIds = [];
    const t = (e = this.workId) == null ? void 0 : e.toString();
    ((s = this.fullLayer) == null ? void 0 : s.parent).children.forEach((o) => {
      o.name === t && o.remove();
    });
  }
};
y(_t, "selectorBorderId", "selector-border");
let Gt = _t;
class Me extends b {
  constructor(t) {
    super(t);
    y(this, "canRotate", !1);
    y(this, "scaleType", z.both);
    y(this, "toolsType", k.Arrow);
    y(this, "tmpPoints", []);
    y(this, "workOptions");
    y(this, "oldRect");
    y(this, "arrowTipWidth");
    y(this, "syncTimestamp");
    this.workOptions = t.toolsOpt, this.arrowTipWidth = this.workOptions.thickness * 4, this.syncTimestamp = 0, this.syncUnitTime = 50;
  }
  consume(t) {
    const {
      data: e,
      isFullWork: s,
      isSubWorker: o,
      isMainThread: r,
      smoothSync: n,
      isSimpleWorker: a
    } = t, c = this.workId, { op: l, syncUnitTime: h } = e;
    h && (this.syncUnitTime = h);
    const p = l == null ? void 0 : l.length;
    if (!p || p < 2)
      return { type: w.None };
    let d;
    if (this.tmpPoints.length === 0 ? (this.tmpPoints = [new O(l[0], l[1])], d = !1) : d = this.updateTempPoints(l), !d)
      return { type: w.None };
    let u;
    if (o || r || a) {
      const m = s ? this.fullLayer : this.drawLayer || this.fullLayer;
      u = this.draw({ workId: c, layer: m });
    }
    if (a) {
      const m = E(u, this.oldRect);
      this.oldRect = u;
      const g = {
        rect: m,
        type: w.DrawWork,
        dataType: x.Local,
        ...this.baseConsumeResult
      };
      if (n) {
        const I = Date.now();
        I - this.syncTimestamp > this.syncUnitTime && (this.syncTimestamp = I, g.op = this.tmpPoints.map((S) => [...S.XY, 0]).flat(1), g.index = 0, g.isSync = !0);
      }
      return g;
    }
    if (!o && n) {
      const m = Date.now();
      return m - this.syncTimestamp > this.syncUnitTime ? (this.syncTimestamp = m, {
        ...this.baseConsumeResult,
        type: w.DrawWork,
        dataType: x.Local,
        op: this.tmpPoints.map((g) => [...g.XY, 0]).flat(1),
        isSync: !0,
        index: 0
      }) : { type: w.None };
    }
    const f = E(u, this.oldRect);
    return this.oldRect = u, {
      rect: f,
      ...this.baseConsumeResult,
      type: w.DrawWork,
      dataType: x.Local
    };
  }
  consumeAll() {
    var n;
    const t = this.workId;
    if (this.tmpPoints.length < 2)
      return {
        type: w.RemoveNode,
        removeIds: [t]
      };
    const e = this.fullLayer, s = this.draw({ workId: t, layer: e });
    this.oldRect = s;
    const o = this.tmpPoints.map((a) => [...a.XY, 0]).flat(1), r = tt(o);
    return (n = this.vNodes) == null || n.setInfo(t, {
      rect: s,
      op: o,
      opt: this.workOptions,
      toolsType: this.toolsType,
      canRotate: this.canRotate,
      scaleType: this.scaleType,
      centerPos: b.getCenterPos(s, e)
    }), {
      rect: s,
      ...this.baseConsumeResult,
      type: w.FullWork,
      dataType: x.Local,
      ops: r,
      isSync: !0,
      updateNodeOpt: {
        useAnimation: !0
      }
    };
  }
  draw(t) {
    const { workId: e, layer: s, isDrawEraserlines: o } = t, {
      strokeColor: r,
      thickness: n,
      zIndex: a,
      scale: c,
      rotate: l,
      translate: h,
      strokeType: p,
      eraserlines: d,
      lineDash: u,
      lineCap: f
    } = this.workOptions, m = s.worldPosition, g = s.worldScaling, { points: I, pos: S, rect: v, isTriangle: L, trianglePoints: C, trianglePos: N } = this.computDrawPoints(n), R = [
      v.x + v.w / 2,
      v.y + v.h / 2
    ], P = {
      pos: R,
      name: e,
      id: e,
      zIndex: a,
      anchor: [0.5, 0.5],
      size: [v.w, v.h]
    };
    c && (P.scale = c), l && (P.rotate = l), h && (P.translate = h);
    const M = new j(P), D = {
      points: C,
      pos: [N[0] - R[0], N[1] - R[1]],
      fillColor: r,
      strokeColor: r,
      lineWidth: 0,
      normalize: !1
    }, B = new ht(D);
    if (M.append(B), !L && I && S) {
      const U = {
        points: I,
        pos: [S[0] - R[0], S[1] - R[1]],
        fillColor: r,
        strokeColor: r,
        lineDash: p === G.Dotted ? [
          u && u[0] || 1,
          (u && u[1] || 2) * n
        ] : p === G.LongDotted ? [
          (u && u[0] || 1) * n,
          (u && u[1] || 2) * n
        ] : void 0,
        lineCap: p === G.Normal ? void 0 : f,
        lineWidth: n,
        normalize: !1
      }, V = new ht(U);
      M.append(V);
    }
    d && (this.scaleType = z.proportional, this.drawEraserlines(
      {
        group: M,
        eraserlines: d,
        pos: R,
        layer: s
      },
      o
    )), this.replace(s, e, M);
    let F = {
      x: Math.floor(
        v.x * g[0] + m[0] - b.SafeBorderPadding
      ),
      y: Math.floor(
        v.y * g[1] + m[1] - b.SafeBorderPadding
      ),
      w: Math.floor(
        v.w * g[0] + 2 * b.SafeBorderPadding
      ),
      h: Math.floor(
        v.h * g[1] + 2 * b.SafeBorderPadding
      )
    };
    if (c || l || h) {
      const U = M.getBoundingClientRect();
      F = {
        x: Math.floor(U.x - b.SafeBorderPadding),
        y: Math.floor(U.y - b.SafeBorderPadding),
        w: Math.floor(U.width + b.SafeBorderPadding * 2),
        h: Math.floor(U.height + b.SafeBorderPadding * 2)
      };
    }
    return this.isDelete && M.setAttribute("opacity", 0), F;
  }
  computDrawPoints(t) {
    return this.tmpPoints[1].distance(this.tmpPoints[0]) > this.arrowTipWidth ? this.computFullArrowPoints(t) : this.computTrianglePoints();
  }
  computFullArrowPoints(t) {
    const e = W.Sub(this.tmpPoints[1], this.tmpPoints[0]).uni(), s = W.Per(e).mul(t / 2), o = O.Sub(
      this.tmpPoints[0],
      s
    ), r = O.Add(
      this.tmpPoints[0],
      s
    ), n = W.Mul(e, this.arrowTipWidth), a = W.Sub(this.tmpPoints[1], n), c = O.Sub(a, s), l = O.Add(a, s), h = W.Per(e).mul(t * 1.5), p = O.Sub(a, h), d = O.Add(a, h), u = [this.tmpPoints[0], a], f = [p, this.tmpPoints[1], d], m = [
      o,
      r,
      ...f,
      c,
      l
    ];
    return {
      trianglePoints: f.map((g) => O.Sub(g, this.tmpPoints[1]).XY).flat(1),
      trianglePos: this.tmpPoints[1].XY,
      points: u.map((g) => O.Sub(g, this.tmpPoints[0]).XY).flat(1),
      rect: $(m),
      isTriangle: !1,
      pos: this.tmpPoints[0].XY
    };
  }
  computTrianglePoints() {
    const t = W.Sub(this.tmpPoints[1], this.tmpPoints[0]).uni(), e = this.tmpPoints[1].distance(this.tmpPoints[0]), s = W.Per(t).mul(
      Math.floor(e * 3 / 8)
    ), o = O.Sub(this.tmpPoints[0], s), r = O.Add(
      this.tmpPoints[0],
      s
    ), n = [o, this.tmpPoints[1], r];
    return {
      trianglePoints: n.map((a) => O.Sub(a, this.tmpPoints[1]).XY).flat(1),
      trianglePos: this.tmpPoints[1].XY,
      rect: $(n),
      isTriangle: !0
    };
  }
  updateTempPoints(t) {
    const e = t.slice(-2), s = new O(e[0], e[1]), o = this.tmpPoints[0], { thickness: r } = this.workOptions;
    if (o.isNear(s, r))
      return !1;
    if (this.tmpPoints.length === 2) {
      if (s.isNear(this.tmpPoints[1], 1))
        return !1;
      this.tmpPoints[1] = s;
    } else
      this.tmpPoints.push(s);
    return !0;
  }
  consumeService(t) {
    var c, l;
    const { op: e, isFullWork: s, workState: o = A.Done } = t, r = (c = this.workId) == null ? void 0 : c.toString();
    if (!r)
      return;
    this.tmpPoints.length = 0;
    for (let h = 0; h < e.length; h += 3)
      this.tmpPoints.push(new O(e[h], e[h + 1], e[h + 2]));
    const n = s ? this.fullLayer : this.drawLayer || this.fullLayer, a = this.draw({
      workId: r,
      layer: n,
      isDrawEraserlines: o === A.Done
    });
    return this.oldRect = a, (l = this.vNodes) == null || l.setInfo(r, {
      rect: a,
      op: e,
      opt: this.workOptions,
      toolsType: this.toolsType,
      canRotate: this.canRotate,
      scaleType: this.scaleType,
      centerPos: b.getCenterPos(a, n)
    }), a;
  }
  clearTmpPoints() {
    this.tmpPoints.length = 0;
  }
  static updateNodeOpt(t) {
    var c, l;
    const { node: e, opt: s, vNodes: o } = t, { strokeColor: r, strokeType: n } = s, a = o.get(e.name);
    return r && (e.tagName === "GROUP" ? e.children.forEach((h) => {
      h.setAttribute("strokeColor", r), h.getAttribute("fillColor") && h.setAttribute("fillColor", r);
    }) : (e.setAttribute("strokeColor", r), e.setAttribute("fillColor", r)), (c = a == null ? void 0 : a.opt) != null && c.strokeColor && (a.opt.strokeColor = r)), n && (a != null && a.opt) && ((l = a.opt) != null && l.strokeType) && (a.opt.strokeType = n), a && o.setInfo(e.name, a), b.updateNodeOpt(t);
  }
}
class Oe extends b {
  constructor(t) {
    super(t);
    y(this, "canRotate", !1);
    y(this, "scaleType", z.all);
    y(this, "toolsType", k.Ellipse);
    y(this, "tmpPoints", []);
    y(this, "workOptions");
    y(this, "oldRect");
    y(this, "syncTimestamp");
    this.workOptions = t.toolsOpt, this.syncTimestamp = 0, this.syncUnitTime = 50;
  }
  consume(t) {
    const {
      data: e,
      isFullWork: s,
      isSubWorker: o,
      isMainThread: r,
      smoothSync: n,
      isSimpleWorker: a
    } = t, c = this.workId, { op: l, syncUnitTime: h } = e;
    h && (this.syncUnitTime = h);
    const p = l == null ? void 0 : l.length;
    if (!p || p < 2)
      return { type: w.None };
    let d;
    if (this.tmpPoints.length === 0 ? (this.tmpPoints = [new O(l[0], l[1])], d = !1) : d = this.updateTempPoints(l), !d)
      return { type: w.None };
    let u;
    if (o || r || a) {
      const f = s ? this.fullLayer : this.drawLayer || this.fullLayer;
      u = this.draw({ workId: c, layer: f, isDrawing: !0 });
      const m = f.parent.parent, g = {
        x: 0,
        y: 0,
        w: Math.floor(m.width),
        h: Math.floor(m.height)
      }, I = {
        type: w.DrawWork,
        dataType: x.Local,
        ...this.baseConsumeResult
      };
      if (Rt(u, g) !== Vt.outside) {
        const v = E(u, this.oldRect);
        if (this.oldRect = u, I.rect = v, !a)
          return I;
      }
      if (a) {
        const v = Date.now();
        return v - this.syncTimestamp > this.syncUnitTime && (this.syncTimestamp = v, I.op = this.tmpPoints.map((L) => [...L.XY, 0]).flat(1), I.index = 0, I.isSync = !0), I;
      }
      return { type: w.None };
    }
    if (!o && !r && !a && n) {
      const f = Date.now();
      return f - this.syncTimestamp > this.syncUnitTime ? (this.syncTimestamp = f, {
        type: w.DrawWork,
        dataType: x.Local,
        ...this.baseConsumeResult,
        op: this.tmpPoints.map((m) => [...m.XY, 0]).flat(1),
        isSync: !0,
        index: 0
      }) : { type: w.None };
    }
    return { type: w.None };
  }
  consumeAll() {
    var n;
    const t = this.workId;
    if (this.tmpPoints.length < 2)
      return {
        type: w.RemoveNode,
        removeIds: [t]
      };
    const e = this.fullLayer, s = this.draw({
      workId: t,
      layer: e,
      isDrawing: !1
    });
    this.oldRect = s;
    const o = this.tmpPoints.map((a) => [...a.XY, 0]).flat(1), r = tt(o);
    return (n = this.vNodes) == null || n.setInfo(t, {
      rect: s,
      op: o,
      opt: this.workOptions,
      toolsType: this.toolsType,
      canRotate: this.canRotate,
      scaleType: this.scaleType,
      centerPos: s && b.getCenterPos(s, e)
    }), {
      rect: s,
      type: w.FullWork,
      dataType: x.Local,
      ...this.baseConsumeResult,
      ops: r,
      isSync: !0,
      updateNodeOpt: {
        useAnimation: !0
      }
    };
  }
  draw(t) {
    const { workId: e, layer: s, isDrawing: o, isDrawEraserlines: r } = t, {
      strokeColor: n,
      fillColor: a,
      thickness: c,
      zIndex: l,
      scale: h,
      rotate: p,
      translate: d,
      strokeType: u,
      eraserlines: f,
      lineDash: m,
      lineCap: g
    } = this.workOptions, I = s.worldScaling, { radius: S, rect: v, pos: L } = this.computDrawPoints(c), C = {
      closeType: "normal",
      radius: S,
      lineWidth: c,
      fillColor: a !== "transparent" && a || void 0,
      strokeColor: n,
      normalize: !0,
      lineCap: g,
      lineDash: u === G.Dotted ? [
        m && m[0] || 1,
        (m && m[1] || 2) * c
      ] : u === G.LongDotted ? [
        (m && m[0] || 1) * c,
        (m && m[1] || 2) * c
      ] : void 0
    }, N = {
      name: e,
      id: e,
      zIndex: l,
      pos: L,
      anchor: [0.5, 0.5],
      size: [v.w, v.h]
    };
    h && (N.scale = h), p && (N.rotate = p), d && (N.translate = d);
    const R = new j(N);
    if (o) {
      const D = new wt({
        d: "M-4,0H4M0,-4V4",
        normalize: !0,
        pos: [0, 0],
        strokeColor: n,
        lineWidth: 1,
        scale: [1 / I[0], 1 / I[1]]
      });
      R.append(D);
    }
    const P = new us({
      ...C,
      pos: [0, 0]
    });
    R.append(P), f && (this.scaleType = z.proportional, this.drawEraserlines(
      {
        group: R,
        eraserlines: f,
        pos: L,
        layer: s
      },
      r
    )), this.replace(s, e, R);
    const M = R.getBoundingClientRect();
    return this.isDelete && R.setAttribute("opacity", 0), {
      x: Math.floor(M.x - b.SafeBorderPadding),
      y: Math.floor(M.y - b.SafeBorderPadding),
      w: Math.floor(M.width + b.SafeBorderPadding * 2),
      h: Math.floor(M.height + b.SafeBorderPadding * 2)
    };
  }
  computDrawPoints(t) {
    const e = $(this.tmpPoints), s = $(this.tmpPoints, t), o = [
      Math.floor(e.x + e.w / 2),
      Math.floor(e.y + e.h / 2)
    ];
    return {
      rect: s,
      pos: o,
      radius: [Math.floor(e.w / 2), Math.floor(e.h / 2)]
    };
  }
  updateTempPoints(t) {
    const e = t.slice(-2), s = new O(e[0], e[1]), o = this.tmpPoints[0], { thickness: r } = this.workOptions;
    if (o.isNear(s, r))
      return !1;
    if (this.tmpPoints.length === 2) {
      if (s.isNear(this.tmpPoints[1], 1))
        return !1;
      this.tmpPoints[1] = s;
    } else
      this.tmpPoints.push(s);
    return !0;
  }
  consumeService(t) {
    var c, l;
    const { op: e, isFullWork: s, workState: o = A.Done } = t, r = (c = this.workId) == null ? void 0 : c.toString();
    if (!r)
      return;
    this.tmpPoints.length = 0;
    for (let h = 0; h < e.length; h += 3)
      this.tmpPoints.push(new O(e[h], e[h + 1], e[h + 2]));
    const n = s ? this.fullLayer : this.drawLayer || this.fullLayer, a = this.draw({
      workId: r,
      layer: n,
      isDrawing: !1,
      isDrawEraserlines: o === A.Done
    });
    return this.oldRect = a, (l = this.vNodes) == null || l.setInfo(r, {
      rect: a,
      op: e,
      opt: this.workOptions,
      toolsType: this.toolsType,
      canRotate: this.canRotate,
      scaleType: this.scaleType,
      centerPos: b.getCenterPos(a, n)
    }), a;
  }
  clearTmpPoints() {
    this.tmpPoints.length = 0;
  }
  static updateNodeOpt(t) {
    var h, p, d;
    const { node: e, opt: s, vNodes: o } = t, { strokeColor: r, fillColor: n, strokeType: a } = s, c = o.get(e.name);
    let l = e;
    return e.tagName === "GROUP" && (l = e.children[0]), r && (l.setAttribute("strokeColor", r), (h = c == null ? void 0 : c.opt) != null && h.strokeColor && (c.opt.strokeColor = r)), n && (n === "transparent" ? l.setAttribute("fillColor", "rgba(0,0,0,0)") : l.setAttribute("fillColor", n), (p = c == null ? void 0 : c.opt) != null && p.fillColor && (c.opt.fillColor = n)), a && (c != null && c.opt) && ((d = c.opt) != null && d.strokeType) && (c.opt.strokeType = a), c && o.setInfo(e.name, c), b.updateNodeOpt(t);
  }
}
class Ae extends b {
  constructor(t) {
    super(t);
    y(this, "canRotate", !0);
    y(this, "scaleType", z.all);
    y(this, "toolsType", k.Rectangle);
    y(this, "tmpPoints", []);
    y(this, "workOptions");
    y(this, "oldRect");
    y(this, "syncTimestamp");
    this.workOptions = t.toolsOpt, this.syncTimestamp = 0, this.syncUnitTime = 50;
  }
  transformData() {
    const t = $(this.tmpPoints);
    return [
      [t.x, t.y, 0],
      [t.x + t.w, t.y, 0],
      [t.x + t.w, t.y + t.h, 0],
      [t.x, t.y + t.h, 0]
    ];
  }
  computDrawPoints(t) {
    const { thickness: e } = this.workOptions, s = [];
    for (const n of t)
      s.push(new W(...n));
    const o = $(s, e), r = [
      o.x + o.w / 2,
      o.y + o.h / 2
    ];
    return {
      rect: o,
      pos: r,
      points: s.map((n) => n.XY).flat(1)
    };
  }
  consume(t) {
    const {
      data: e,
      isFullWork: s,
      isSubWorker: o,
      isMainThread: r,
      smoothSync: n,
      isSimpleWorker: a
    } = t, c = this.workId, { op: l } = e, h = l == null ? void 0 : l.length;
    if (!h || h < 2)
      return { type: w.None };
    let p;
    if (this.tmpPoints.length === 0 ? (this.tmpPoints = [new O(l[0], l[1])], p = !1) : p = this.updateTempPoints(l), !p)
      return { type: w.None };
    const d = this.transformData();
    let u;
    if (o || r || a) {
      const m = s ? this.fullLayer : this.drawLayer || this.fullLayer;
      u = this.draw({ ps: d, workId: c, layer: m, isDrawing: !0 });
    }
    if (a) {
      const m = E(u, this.oldRect);
      this.oldRect = u;
      const g = {
        rect: m,
        type: w.DrawWork,
        dataType: x.Local,
        ...this.baseConsumeResult
      };
      if (n) {
        const I = Date.now();
        I - this.syncTimestamp > this.syncUnitTime && (this.syncTimestamp = I, g.op = d.flat(1), g.index = 0, g.isSync = !0);
      }
      return g;
    }
    if (!o && n) {
      const m = Date.now();
      return m - this.syncTimestamp > this.syncUnitTime ? (this.syncTimestamp = m, {
        type: w.DrawWork,
        dataType: x.Local,
        op: d.flat(1),
        isSync: !0,
        index: 0,
        ...this.baseConsumeResult
      }) : { type: w.None };
    }
    const f = E(u, this.oldRect);
    return this.oldRect = u, {
      rect: f,
      type: w.DrawWork,
      dataType: x.Local,
      ...this.baseConsumeResult
    };
  }
  consumeAll() {
    var a;
    const t = this.workId;
    if (this.tmpPoints.length < 2)
      return {
        type: w.RemoveNode,
        removeIds: [t]
      };
    const e = this.transformData(), s = this.fullLayer, o = this.draw({
      ps: e,
      workId: t,
      layer: s,
      isDrawing: !1
    });
    this.oldRect = o;
    const r = e.flat(1), n = tt(r);
    return (a = this.vNodes) == null || a.setInfo(t, {
      rect: o,
      op: r,
      opt: this.workOptions,
      toolsType: this.toolsType,
      scaleType: this.scaleType,
      canRotate: this.canRotate,
      centerPos: o && b.getCenterPos(o, s)
    }), {
      rect: o,
      type: w.FullWork,
      dataType: x.Local,
      ops: n,
      isSync: !0,
      ...this.baseConsumeResult,
      updateNodeOpt: {
        useAnimation: !0
      }
    };
  }
  draw(t) {
    const {
      workId: e,
      layer: s,
      isDrawing: o,
      ps: r,
      replaceId: n,
      isDrawEraserlines: a = !0
    } = t, {
      strokeColor: c,
      fillColor: l,
      thickness: h,
      zIndex: p,
      scale: d,
      rotate: u,
      translate: f,
      strokeType: m,
      eraserlines: g,
      lineCap: I,
      lineDash: S
    } = this.workOptions, v = s.worldPosition, L = s.worldScaling, { points: C, rect: N, pos: R } = this.computDrawPoints(r), P = {
      close: !0,
      normalize: !0,
      points: C,
      lineWidth: h,
      fillColor: l !== "transparent" && l || void 0,
      strokeColor: c,
      lineCap: I,
      lineDash: m === G.Dotted ? [
        S && S[0] || 1,
        (S && S[1] || 2) * h
      ] : m === G.LongDotted ? [
        (S && S[0] || 1) * h,
        (S && S[1] || 2) * h
      ] : void 0
    };
    let M = {
      x: Math.floor(
        N.x * L[0] + v[0] - b.SafeBorderPadding
      ),
      y: Math.floor(
        N.y * L[1] + v[1] - b.SafeBorderPadding
      ),
      w: Math.floor(
        N.w * L[0] + 2 * b.SafeBorderPadding
      ),
      h: Math.floor(
        N.h * L[0] + 2 * b.SafeBorderPadding
      )
    };
    const D = new j({
      name: e,
      id: e,
      zIndex: p,
      pos: R,
      anchor: [0.5, 0.5],
      size: [N.w, N.h],
      scale: d,
      rotate: u,
      translate: f
    }), B = new ht({
      ...P,
      pos: [0, 0]
    });
    if (D.appendChild(B), o) {
      const F = new wt({
        d: "M-4,0H4M0,-4V4",
        normalize: !0,
        pos: [0, 0],
        strokeColor: c,
        lineWidth: 1,
        scale: [1 / L[0], 1 / L[1]]
      });
      D.appendChild(F);
    }
    if (g && (this.scaleType = z.proportional, this.drawEraserlines(
      {
        group: D,
        eraserlines: g,
        pos: R,
        layer: s
      },
      a
    )), this.replace(s, n || e, D), d || u || f) {
      const F = D.getBoundingClientRect();
      M = {
        x: Math.floor(F.x - b.SafeBorderPadding),
        y: Math.floor(F.y - b.SafeBorderPadding),
        w: Math.floor(F.width + 2 * b.SafeBorderPadding),
        h: Math.floor(F.height + 2 * b.SafeBorderPadding)
      };
    }
    return this.isDelete && D.setAttribute("opacity", 0), M;
  }
  updateTempPoints(t) {
    const e = t.slice(-2), s = new O(e[0], e[1]), o = this.tmpPoints[0], { thickness: r } = this.workOptions;
    if (o.isNear(s, r))
      return !1;
    if (this.tmpPoints.length === 2) {
      if (s.isNear(this.tmpPoints[1], 1))
        return !1;
      this.tmpPoints[1] = s;
    } else
      this.tmpPoints.push(s);
    return !0;
  }
  consumeService(t) {
    var h, p;
    const {
      op: e,
      isFullWork: s,
      replaceId: o,
      workState: r = A.Done
    } = t, n = (h = this.workId) == null ? void 0 : h.toString();
    if (!n)
      return;
    const a = [];
    for (let d = 0; d < e.length; d += 3)
      a.push([e[d], e[d + 1], e[d + 2]]);
    const c = s ? this.fullLayer : this.drawLayer || this.fullLayer, l = this.draw({
      ps: a,
      workId: n,
      layer: c,
      isDrawing: !1,
      replaceId: o,
      isDrawEraserlines: r === A.Done
    });
    return this.oldRect = l, (p = this.vNodes) == null || p.setInfo(n, {
      rect: l,
      op: e,
      opt: this.workOptions,
      toolsType: this.toolsType,
      scaleType: this.scaleType,
      canRotate: this.canRotate,
      centerPos: l && b.getCenterPos(l, c)
    }), l;
  }
  clearTmpPoints() {
    this.tmpPoints.length = 0;
  }
  static updateNodeOpt(t) {
    var h, p, d;
    const { node: e, opt: s, vNodes: o } = t, { strokeColor: r, fillColor: n, strokeType: a } = s, c = o.get(e.name);
    let l = e;
    return e.tagName === "GROUP" && (l = e.children[0]), r && (l.setAttribute("strokeColor", r), (h = c == null ? void 0 : c.opt) != null && h.strokeColor && (c.opt.strokeColor = r)), n && (n === "transparent" ? l.setAttribute("fillColor", "rgba(0,0,0,0)") : l.setAttribute("fillColor", n), (p = c == null ? void 0 : c.opt) != null && p.fillColor && (c.opt.fillColor = n)), a && (c != null && c.opt) && ((d = c.opt) != null && d.strokeType) && (c.opt.strokeType = a), c && o.setInfo(e.name, c), b.updateNodeOpt(t);
  }
}
class Ee extends b {
  constructor(t) {
    super(t);
    y(this, "canRotate", !1);
    y(this, "scaleType", z.all);
    y(this, "toolsType", k.Star);
    y(this, "tmpPoints", []);
    y(this, "workOptions");
    y(this, "oldRect");
    y(this, "syncTimestamp");
    this.workOptions = t.toolsOpt, this.syncTimestamp = 0, this.syncUnitTime = 50;
  }
  consume(t) {
    const {
      data: e,
      isFullWork: s,
      isSubWorker: o,
      isMainThread: r,
      smoothSync: n,
      isSimpleWorker: a
    } = t, c = this.workId, { op: l, syncUnitTime: h } = e, p = l == null ? void 0 : l.length;
    if (!p || p < 2)
      return { type: w.None };
    h && (this.syncUnitTime = h);
    let d;
    if (this.tmpPoints.length === 0 ? (this.tmpPoints = [new O(l[0], l[1])], d = !1) : d = this.updateTempPoints(l), !d)
      return { type: w.None };
    let u;
    if (o || r || a) {
      const m = s ? this.fullLayer : this.drawLayer || this.fullLayer;
      u = this.draw({ workId: c, layer: m, isDrawing: !0 });
    }
    if (a) {
      const m = E(u, this.oldRect);
      this.oldRect = u;
      const g = {
        rect: m,
        type: w.DrawWork,
        dataType: x.Local,
        ...this.baseConsumeResult
      };
      if (n) {
        const I = Date.now();
        I - this.syncTimestamp > this.syncUnitTime && (this.syncTimestamp = I, g.op = this.tmpPoints.map((S) => [...S.XY, 0]).flat(1), g.index = 0, g.isSync = !0);
      }
      return g;
    }
    if (!o && n) {
      const m = Date.now();
      return m - this.syncTimestamp > this.syncUnitTime ? (this.syncTimestamp = m, {
        type: w.DrawWork,
        dataType: x.Local,
        op: this.tmpPoints.map((g) => [...g.XY, 0]).flat(1),
        isSync: !0,
        index: 0,
        ...this.baseConsumeResult
      }) : { type: w.None };
    }
    const f = E(u, this.oldRect);
    return this.oldRect = u, {
      rect: f,
      type: w.DrawWork,
      dataType: x.Local,
      ...this.baseConsumeResult
    };
  }
  consumeAll() {
    var n;
    const t = this.workId;
    if (this.tmpPoints.length < 2)
      return {
        type: w.RemoveNode,
        removeIds: [t]
      };
    const e = this.fullLayer, s = this.draw({
      workId: t,
      layer: e,
      isDrawing: !1
    });
    this.oldRect = s;
    const o = this.tmpPoints.map((a) => [...a.XY, 0]).flat(1), r = tt(o);
    return (n = this.vNodes) == null || n.setInfo(t, {
      rect: s,
      op: o,
      opt: this.workOptions,
      toolsType: this.toolsType,
      scaleType: this.scaleType,
      canRotate: this.canRotate,
      centerPos: s && b.getCenterPos(s, e)
    }), {
      rect: s,
      type: w.FullWork,
      dataType: x.Local,
      ops: r,
      isSync: !0,
      ...this.baseConsumeResult,
      updateNodeOpt: {
        useAnimation: !0
      }
    };
  }
  draw(t) {
    const { workId: e, layer: s, isDrawing: o, isDrawEraserlines: r = !0 } = t, {
      strokeColor: n,
      fillColor: a,
      thickness: c,
      zIndex: l,
      vertices: h,
      innerVerticeStep: p,
      innerRatio: d,
      scale: u,
      rotate: f,
      translate: m,
      strokeType: g,
      eraserlines: I,
      lineDash: S,
      lineCap: v
    } = this.workOptions, L = s.worldScaling, { rect: C, pos: N, points: R } = this.computDrawPoints(
      c,
      h,
      p,
      d
    ), P = {
      close: !0,
      points: R,
      lineWidth: c,
      fillColor: a !== "transparent" && a || void 0,
      strokeColor: n,
      normalize: !0,
      lineCap: v,
      lineDash: g === G.Dotted ? [
        S && S[0] || 1,
        (S && S[1] || 2) * c
      ] : g === G.LongDotted ? [
        (S && S[0] || 1) * c,
        (S && S[1] || 2) * c
      ] : void 0
    }, M = {
      name: e,
      id: e,
      zIndex: l,
      pos: N,
      anchor: [0.5, 0.5],
      size: [C.w, C.h]
    };
    u && (M.scale = u), f && (M.rotate = f), m && (M.translate = m);
    const D = new j(M);
    if (o) {
      const U = new wt({
        d: "M-4,0H4M0,-4V4",
        normalize: !0,
        pos: [0, 0],
        strokeColor: n,
        lineWidth: 1,
        scale: [1 / L[0], 1 / L[1]]
      });
      D.append(U);
    }
    const B = new ht({
      ...P,
      pos: [0, 0]
    });
    D.append(B), I && (this.scaleType = z.proportional, this.drawEraserlines(
      {
        group: D,
        eraserlines: I,
        pos: N,
        layer: s
      },
      r
    )), this.replace(s, e, D);
    const F = D.getBoundingClientRect();
    return this.isDelete && D.setAttribute("opacity", 0), {
      x: Math.floor(F.x - b.SafeBorderPadding),
      y: Math.floor(F.y - b.SafeBorderPadding),
      w: Math.floor(F.width + b.SafeBorderPadding * 2),
      h: Math.floor(F.height + b.SafeBorderPadding * 2)
    };
  }
  computDrawPoints(t, e, s, o) {
    const r = $(this.tmpPoints), n = [
      Math.floor(r.x + r.w / 2),
      Math.floor(r.y + r.h / 2)
    ], a = We(r.w, r.h), c = Math.floor(Math.min(r.w, r.h) / 2), l = o * c, h = [], p = 2 * Math.PI / e;
    for (let u = 0; u < e; u++) {
      const f = u * p - 0.5 * Math.PI;
      let m, g;
      u % s === 1 ? (m = l * a[0] * Math.cos(f), g = l * a[1] * Math.sin(f)) : (m = c * a[0] * Math.cos(f), g = c * a[1] * Math.sin(f), h.push(m, g)), h.push(m, g);
    }
    return {
      rect: $(this.tmpPoints, t),
      pos: n,
      points: h
    };
  }
  updateTempPoints(t) {
    const e = t.slice(-2), s = new O(e[0], e[1]), o = this.tmpPoints[0], { thickness: r } = this.workOptions;
    if (o.isNear(s, r) || O.Sub(o, s).XY.includes(0))
      return !1;
    if (this.tmpPoints.length === 2) {
      if (s.isNear(this.tmpPoints[1], 1))
        return !1;
      this.tmpPoints[1] = s;
    } else
      this.tmpPoints.push(s);
    return !0;
  }
  consumeService(t) {
    var c, l;
    const { op: e, isFullWork: s, workState: o = A.Done } = t, r = (c = this.workId) == null ? void 0 : c.toString();
    if (!r)
      return;
    this.tmpPoints.length = 0;
    for (let h = 0; h < e.length; h += 3)
      this.tmpPoints.push(new O(e[h], e[h + 1], e[h + 2]));
    const n = s ? this.fullLayer : this.drawLayer || this.fullLayer, a = this.draw({
      workId: r,
      layer: n,
      isDrawing: !1,
      isDrawEraserlines: o === A.Done
    });
    return this.oldRect = a, (l = this.vNodes) == null || l.setInfo(r, {
      rect: a,
      op: e,
      opt: this.workOptions,
      toolsType: this.toolsType,
      scaleType: this.scaleType,
      canRotate: this.canRotate,
      centerPos: a && b.getCenterPos(a, n)
    }), a;
  }
  clearTmpPoints() {
    this.tmpPoints.length = 0;
  }
  static updateNodeOpt(t) {
    var m;
    const { node: e, opt: s, vNodes: o } = t, {
      strokeColor: r,
      fillColor: n,
      toolsType: a,
      vertices: c,
      innerVerticeStep: l,
      innerRatio: h,
      strokeType: p
    } = s, d = o.get(e.name), u = d == null ? void 0 : d.opt;
    let f = e;
    return e.tagName === "GROUP" && (f = e.children[0]), r && (f.setAttribute("strokeColor", r), u != null && u.strokeColor && (u.strokeColor = r)), n && (n === "transparent" ? f.setAttribute("fillColor", "rgba(0,0,0,0)") : f.setAttribute("fillColor", n), u != null && u.fillColor && (u.fillColor = n)), a === k.Star && (c && (u.vertices = c), l && (u.innerVerticeStep = l), h && (u.innerRatio = h)), p && (d != null && d.opt) && ((m = d.opt) != null && m.strokeType) && (d.opt.strokeType = p), d && o.setInfo(e.name, { ...d, opt: u }), b.updateNodeOpt(t);
  }
}
class Be extends b {
  constructor(t) {
    super(t);
    y(this, "canRotate", !1);
    y(this, "scaleType", z.all);
    y(this, "toolsType", k.Polygon);
    y(this, "tmpPoints", []);
    y(this, "workOptions");
    y(this, "oldRect");
    y(this, "syncTimestamp");
    this.workOptions = t.toolsOpt, this.syncTimestamp = 0, this.syncUnitTime = 50;
  }
  consume(t) {
    const {
      data: e,
      isFullWork: s,
      isSubWorker: o,
      isMainThread: r,
      smoothSync: n,
      isSimpleWorker: a
    } = t, { op: c, syncUnitTime: l } = e;
    l && (this.syncUnitTime = l);
    const h = this.workId, p = c == null ? void 0 : c.length;
    if (!p || p < 2)
      return { type: w.None };
    let d;
    if (this.tmpPoints.length === 0 ? (this.tmpPoints = [new O(c[0], c[1])], d = !1) : d = this.updateTempPoints(c), !d)
      return { type: w.None };
    let u;
    if (o || r || a) {
      const m = s ? this.fullLayer : this.drawLayer || this.fullLayer;
      u = this.draw({ workId: h, layer: m, isDrawing: !0 });
    }
    if (a) {
      const m = E(u, this.oldRect);
      this.oldRect = u;
      const g = {
        type: w.DrawWork,
        rect: m,
        dataType: x.Local,
        ...this.baseConsumeResult
      };
      if (n) {
        const I = Date.now();
        I - this.syncTimestamp > this.syncUnitTime && (this.syncTimestamp = I, g.op = this.tmpPoints.map((S) => [...S.XY, 0]).flat(1), g.index = 0, g.isSync = !0);
      }
      return g;
    }
    if (!o && n) {
      const m = Date.now();
      return m - this.syncTimestamp > this.syncUnitTime ? (this.syncTimestamp = m, {
        type: w.DrawWork,
        dataType: x.Local,
        op: this.tmpPoints.map((g) => [...g.XY, 0]).flat(1),
        isSync: !0,
        index: 0,
        ...this.baseConsumeResult
      }) : { type: w.None };
    }
    const f = E(u, this.oldRect);
    return this.oldRect = u, {
      rect: f,
      type: w.DrawWork,
      dataType: x.Local,
      ...this.baseConsumeResult
    };
  }
  consumeAll() {
    var n;
    const t = this.workId;
    if (this.tmpPoints.length < 2)
      return {
        type: w.RemoveNode,
        removeIds: [t]
      };
    const e = this.fullLayer, s = this.draw({
      workId: t,
      layer: e,
      isDrawing: !1
    });
    this.oldRect = s;
    const o = this.tmpPoints.map((a) => [...a.XY, 0]).flat(1), r = tt(o);
    return (n = this.vNodes) == null || n.setInfo(t, {
      rect: s,
      op: o,
      opt: this.workOptions,
      toolsType: this.toolsType,
      scaleType: this.scaleType,
      canRotate: this.canRotate,
      centerPos: s && b.getCenterPos(s, e)
    }), {
      rect: s,
      type: w.FullWork,
      dataType: x.Local,
      ops: r,
      isSync: !0,
      ...this.baseConsumeResult,
      updateNodeOpt: {
        useAnimation: !0
      }
    };
  }
  draw(t) {
    const { workId: e, layer: s, isDrawing: o, isDrawEraserlines: r = !0 } = t, {
      strokeColor: n,
      fillColor: a,
      thickness: c,
      zIndex: l,
      vertices: h,
      scale: p,
      rotate: d,
      translate: u,
      strokeType: f,
      eraserlines: m,
      lineCap: g,
      lineDash: I
    } = this.workOptions, S = s.worldScaling, { rect: v, pos: L, points: C } = this.computDrawPoints(c, h), N = {
      close: !0,
      points: C,
      lineWidth: c,
      fillColor: a !== "transparent" && a || void 0,
      strokeColor: n,
      normalize: !0,
      lineCap: g,
      lineDash: f === G.Dotted ? [
        I && I[0] || 1,
        (I && I[1] || 2) * c
      ] : f === G.LongDotted ? [
        (I && I[0] || 1) * c,
        (I && I[1] || 2) * c
      ] : void 0
    }, R = {
      name: e,
      id: e,
      zIndex: l,
      pos: L,
      anchor: [0.5, 0.5],
      size: [v.w, v.h]
    };
    p && (R.scale = p), d && (R.rotate = d), u && (R.translate = u);
    const P = new j(R);
    if (o) {
      const B = new wt({
        d: "M-4,0H4M0,-4V4",
        normalize: !0,
        pos: [0, 0],
        strokeColor: n,
        lineWidth: 1,
        scale: [1 / S[0], 1 / S[1]]
      });
      P.append(B);
    }
    const M = new ht({
      ...N,
      pos: [0, 0]
    });
    P.append(M), m && (this.scaleType = z.proportional, this.drawEraserlines(
      {
        group: P,
        eraserlines: m,
        pos: L,
        layer: s
      },
      r
    )), this.replace(s, e, P);
    const D = P.getBoundingClientRect();
    return this.isDelete && P.setAttribute("opacity", 0), {
      x: Math.floor(D.x - b.SafeBorderPadding),
      y: Math.floor(D.y - b.SafeBorderPadding),
      w: Math.floor(D.width + b.SafeBorderPadding * 2),
      h: Math.floor(D.height + b.SafeBorderPadding * 2)
    };
  }
  computDrawPoints(t, e) {
    const s = $(this.tmpPoints), o = [
      Math.floor(s.x + s.w / 2),
      Math.floor(s.y + s.h / 2)
    ], r = We(s.w, s.h), n = Math.floor(Math.min(s.w, s.h) / 2), a = [], c = 2 * Math.PI / e;
    for (let h = 0; h < e; h++) {
      const p = h * c - 0.5 * Math.PI, d = n * r[0] * Math.cos(p), u = n * r[1] * Math.sin(p);
      a.push(d, u);
    }
    return {
      rect: $(this.tmpPoints, t),
      pos: o,
      points: a
    };
  }
  updateTempPoints(t) {
    const e = t.slice(-2), s = new O(e[0], e[1]), o = this.tmpPoints[0], { thickness: r } = this.workOptions;
    if (o.isNear(s, r) || O.Sub(o, s).XY.includes(0))
      return !1;
    if (this.tmpPoints.length === 2) {
      if (s.isNear(this.tmpPoints[1], 1))
        return !1;
      this.tmpPoints[1] = s;
    } else
      this.tmpPoints.push(s);
    return !0;
  }
  consumeService(t) {
    var c, l;
    const { op: e, isFullWork: s, workState: o = A.Done } = t, r = (c = this.workId) == null ? void 0 : c.toString();
    if (!r)
      return;
    this.tmpPoints.length = 0;
    for (let h = 0; h < e.length; h += 3)
      this.tmpPoints.push(new O(e[h], e[h + 1], e[h + 2]));
    const n = s ? this.fullLayer : this.drawLayer || this.fullLayer, a = this.draw({
      workId: r,
      layer: n,
      isDrawing: !1,
      isDrawEraserlines: o === A.Done
    });
    return this.oldRect = a, (l = this.vNodes) == null || l.setInfo(r, {
      rect: a,
      op: e,
      opt: this.workOptions,
      toolsType: this.toolsType,
      scaleType: this.scaleType,
      canRotate: this.canRotate,
      centerPos: a && b.getCenterPos(a, n)
    }), a;
  }
  clearTmpPoints() {
    this.tmpPoints.length = 0;
  }
  static updateNodeOpt(t) {
    var u;
    const { node: e, opt: s, vNodes: o } = t, { strokeColor: r, fillColor: n, toolsType: a, vertices: c, strokeType: l } = s, h = o.get(e.name), p = h == null ? void 0 : h.opt;
    let d = e;
    return e.tagName === "GROUP" && (d = e.children[0]), r && (d.setAttribute("strokeColor", r), p != null && p.strokeColor && (p.strokeColor = r)), n && (n === "transparent" ? d.setAttribute("fillColor", "rgba(0,0,0,0)") : d.setAttribute("fillColor", n), p != null && p.fillColor && (p.fillColor = n)), a === k.Polygon && c && (p.vertices = c), l && (h != null && h.opt) && ((u = h.opt) != null && u.strokeType) && (h.opt.strokeType = l), h && o.setInfo(e.name, { ...h, opt: p }), b.updateNodeOpt(t);
  }
}
class ot {
  static bezier(i, t) {
    const e = [];
    for (let s = 0; s < t.length; s += 4) {
      const o = t[s], r = t[s + 1], n = t[s + 2], a = t[s + 3];
      o && r && n && a ? e.push(...ot.getBezierPoints(i, o, r, n, a)) : o && r && n ? e.push(...ot.getBezierPoints(i, o, r, n)) : o && r ? e.push(...ot.getBezierPoints(i, o, r)) : o && e.push(o);
    }
    return e;
  }
  /**
   * @desc 获取点，这里可以设置点的个数
   * @param {number} num 点个数
   * @param {Vec2d} p1 点坐标
   * @param {Vec2d} p2 点坐标
   * @param {Vec2d} p3 点坐标
   * @param {Vec2d} p4 点坐标
   * 如果参数是 num, p1, p2 为一阶贝塞尔
   * 如果参数是 num, p1, c1, p2 为二阶贝塞尔
   * 如果参数是 num, p1, c1, c2, p2 为三阶贝塞尔
   */
  static getBezierPoints(i = 10, t, e, s, o) {
    let r = null;
    const n = [];
    !s && !o ? r = ot.oneBezier : s && !o ? r = ot.twoBezier : s && o && (r = ot.threeBezier);
    for (let a = 0; a < i; a++)
      r && n.push(r(a / i, t, e, s, o));
    return o ? n.push(o) : s && n.push(s), n;
  }
  /**
   * @desc 一阶贝塞尔
   * @param {number} t 当前百分比
   * @param {Vec2d} p1 起点坐标
   * @param {Vec2d} p2 终点坐标
   */
  static oneBezier(i, t, e) {
    const s = t.x + (e.x - t.x) * i, o = t.y + (e.y - t.y) * i;
    return new W(s, o);
  }
  /**
   * @desc 二阶贝塞尔
   * @param {number} t 当前百分比
   * @param {Array} p1 起点坐标
   * @param {Array} p2 终点坐标
   * @param {Array} cp 控制点
   */
  static twoBezier(i, t, e, s) {
    const o = (1 - i) * (1 - i) * t.x + 2 * i * (1 - i) * e.x + i * i * s.x, r = (1 - i) * (1 - i) * t.y + 2 * i * (1 - i) * e.y + i * i * s.y;
    return new W(o, r);
  }
  /**
   * @desc 三阶贝塞尔
   * @param {number} t 当前百分比
   * @param {Array} p1 起点坐标
   * @param {Array} p2 终点坐标
   * @param {Array} cp1 控制点1
   * @param {Array} cp2 控制点2
   */
  static threeBezier(i, t, e, s, o) {
    const r = t.x * (1 - i) * (1 - i) * (1 - i) + 3 * e.x * i * (1 - i) * (1 - i) + 3 * s.x * i * i * (1 - i) + o.x * i * i * i, n = t.y * (1 - i) * (1 - i) * (1 - i) + 3 * e.y * i * (1 - i) * (1 - i) + 3 * s.y * i * i * (1 - i) + o.y * i * i * i;
    return new W(r, n);
  }
}
class Fe extends b {
  constructor(t) {
    super(t);
    y(this, "canRotate", !1);
    y(this, "scaleType", z.all);
    y(this, "toolsType", k.SpeechBalloon);
    y(this, "ratio", 0.8);
    y(this, "tmpPoints", []);
    y(this, "workOptions");
    y(this, "oldRect");
    y(this, "syncTimestamp");
    this.workOptions = t.toolsOpt, this.syncTimestamp = 0, this.syncUnitTime = 50;
  }
  consume(t) {
    var f;
    const {
      data: e,
      isFullWork: s,
      isSubWorker: o,
      isMainThread: r,
      smoothSync: n,
      isSimpleWorker: a
    } = t, c = (f = e == null ? void 0 : e.workId) == null ? void 0 : f.toString();
    if (!c)
      return { type: w.None };
    const { op: l } = e, h = l == null ? void 0 : l.length;
    if (!h || h < 2)
      return { type: w.None };
    let p;
    if (this.tmpPoints.length === 0 ? (this.tmpPoints = [new O(l[0], l[1])], p = !1) : p = this.updateTempPoints(l), !p)
      return { type: w.None };
    let d;
    if (o || r || a) {
      const m = s ? this.fullLayer : this.drawLayer || this.fullLayer;
      d = this.draw({ workId: c, layer: m, isDrawing: !0 });
    }
    if (a) {
      const m = E(d, this.oldRect);
      this.oldRect = d;
      const g = {
        rect: m,
        type: w.DrawWork,
        dataType: x.Local,
        ...this.baseConsumeResult
      };
      if (n) {
        const I = Date.now();
        I - this.syncTimestamp > this.syncUnitTime && (this.syncTimestamp = I, g.op = this.tmpPoints.map((S) => [...S.XY, 0]).flat(1), g.index = 0, g.isSync = !0);
      }
      return g;
    }
    if (!o && n) {
      const m = Date.now();
      return m - this.syncTimestamp > this.syncUnitTime ? (this.syncTimestamp = m, {
        type: w.DrawWork,
        dataType: x.Local,
        op: this.tmpPoints.map((g) => [...g.XY, 0]).flat(1),
        isSync: !0,
        index: 0,
        ...this.baseConsumeResult
      }) : { type: w.None };
    }
    const u = E(d, this.oldRect);
    return this.oldRect = d, {
      rect: u,
      type: w.DrawWork,
      dataType: x.Local,
      ...this.baseConsumeResult
    };
  }
  consumeAll() {
    var n;
    const t = this.workId;
    if (this.tmpPoints.length < 2)
      return {
        type: w.RemoveNode,
        removeIds: [t]
      };
    const e = this.fullLayer, s = this.draw({
      workId: t,
      layer: e,
      isDrawing: !1
    });
    this.oldRect = s;
    const o = this.tmpPoints.map((a) => [...a.XY, 0]).flat(1), r = tt(o);
    return (n = this.vNodes) == null || n.setInfo(t, {
      rect: s,
      op: o,
      opt: this.workOptions,
      toolsType: this.toolsType,
      scaleType: this.scaleType,
      canRotate: this.canRotate,
      centerPos: s && b.getCenterPos(s, e)
    }), {
      rect: s,
      type: w.FullWork,
      dataType: x.Local,
      ops: r,
      isSync: !0,
      ...this.baseConsumeResult,
      updateNodeOpt: {
        useAnimation: !0
      }
    };
  }
  draw(t) {
    const { workId: e, layer: s, isDrawEraserlines: o = !0 } = t, {
      strokeColor: r,
      fillColor: n,
      thickness: a,
      zIndex: c,
      placement: l,
      scale: h,
      rotate: p,
      translate: d,
      strokeType: u,
      eraserlines: f,
      lineDash: m,
      lineCap: g
    } = this.workOptions, { rect: I, pos: S, points: v } = this.computDrawPoints(a, l), L = {
      points: v.map((M) => M.XY),
      lineWidth: a,
      fillColor: n !== "transparent" && n || void 0,
      strokeColor: r,
      normalize: !0,
      className: `${S[0]},${S[1]}`,
      close: !0,
      lineCap: g,
      lineDash: u === G.Dotted ? [
        m && m[0] || 1,
        (m && m[1] || 2) * a
      ] : u === G.LongDotted ? [
        (m && m[0] || 1) * a,
        (m && m[1] || 2) * a
      ] : void 0
    }, C = {
      name: e,
      id: e,
      zIndex: c,
      pos: S,
      anchor: [0.5, 0.5],
      size: [I.w, I.h]
    };
    h && (C.scale = h), p && (C.rotate = p), d && (C.translate = d);
    const N = new j(C), R = new ht({
      ...L,
      pos: [0, 0]
    });
    N.append(R), f && (this.scaleType = z.proportional, this.drawEraserlines(
      {
        group: N,
        eraserlines: f,
        pos: S,
        layer: s
      },
      o
    )), this.replace(s, e, N);
    const P = N.getBoundingClientRect();
    return this.isDelete && N.setAttribute("opacity", 0), {
      x: Math.floor(P.x - b.SafeBorderPadding),
      y: Math.floor(P.y - b.SafeBorderPadding),
      w: Math.floor(P.width + b.SafeBorderPadding * 2),
      h: Math.floor(P.height + b.SafeBorderPadding * 2)
    };
  }
  transformControlPoints(t) {
    const e = $(this.tmpPoints);
    switch (t) {
      case "bottom":
      case "bottomLeft":
      case "bottomRight": {
        const s = e.y + e.h * this.ratio;
        return [
          new W(e.x, e.y, 0),
          new W(e.x + e.w, e.y, 0),
          new W(e.x + e.w, s, 0),
          new W(e.x, s, 0)
        ];
      }
      case "top":
      case "topLeft":
      case "topRight": {
        const s = e.y + e.h * (1 - this.ratio);
        return [
          new W(e.x, s, 0),
          new W(e.x + e.w, s, 0),
          new W(e.x + e.w, e.y + e.h, 0),
          new W(e.x, e.y + e.h, 0)
        ];
      }
      case "left":
      case "leftBottom":
      case "leftTop": {
        const s = e.x + e.w * (1 - this.ratio);
        return [
          new W(s, e.y, 0),
          new W(e.x + e.w, e.y, 0),
          new W(e.x + e.w, e.y + e.h, 0),
          new W(s, e.y + e.h, 0)
        ];
      }
      case "right":
      case "rightBottom":
      case "rightTop": {
        const s = e.x + e.w * this.ratio;
        return [
          new W(e.x, e.y, 0),
          new W(s, e.y, 0),
          new W(s, e.y + e.h, 0),
          new W(e.x, e.y + e.h, 0)
        ];
      }
    }
  }
  computDrawPoints(t, e) {
    const s = $(this.tmpPoints), o = this.transformControlPoints(e), r = Math.floor(s.w * 0.1), n = Math.floor(s.h * 0.1), a = [], c = W.Add(o[0], new W(0, n, 0)), l = W.Add(o[0], new W(r, 0, 0)), h = ot.getBezierPoints(
      10,
      c,
      o[0],
      l
    ), p = W.Sub(o[1], new W(r, 0, 0)), d = W.Add(
      o[1],
      new W(0, n, 0)
    ), u = ot.getBezierPoints(
      10,
      p,
      o[1],
      d
    ), f = W.Sub(o[2], new W(0, n, 0)), m = W.Sub(
      o[2],
      new W(r, 0, 0)
    ), g = ot.getBezierPoints(
      10,
      f,
      o[2],
      m
    ), I = W.Add(
      o[3],
      new W(r, 0, 0)
    ), S = W.Sub(
      o[3],
      new W(0, n, 0)
    ), v = ot.getBezierPoints(
      10,
      I,
      o[3],
      S
    ), L = r * (1 - this.ratio) * 10, C = n * (1 - this.ratio) * 10;
    switch (e) {
      case "bottom": {
        const P = W.Sub(
          o[2],
          new W(r * 5 - L / 2, 0, 0)
        ), M = W.Sub(
          o[2],
          new W(r * 5, -C, 0)
        ), D = W.Sub(
          o[2],
          new W(r * 5 + L / 2, 0, 0)
        );
        a.push(
          M,
          D,
          ...v,
          ...h,
          ...u,
          ...g,
          P
        );
        break;
      }
      case "bottomRight": {
        const P = W.Sub(
          o[2],
          new W(r * 1.1, 0, 0)
        ), M = W.Sub(
          o[2],
          new W(r * 1.1 + L / 2, -C, 0)
        ), D = W.Sub(
          o[2],
          new W(r * 1.1 + L, 0, 0)
        );
        a.push(
          M,
          D,
          ...v,
          ...h,
          ...u,
          ...g,
          P
        );
        break;
      }
      case "bottomLeft": {
        const P = W.Add(
          o[3],
          new W(r * 1.1 + L, 0, 0)
        ), M = W.Add(
          o[3],
          new W(r * 1.1 + L / 2, C, 0)
        ), D = W.Add(
          o[3],
          new W(r * 1.1, 0, 0)
        );
        a.push(
          M,
          D,
          ...v,
          ...h,
          ...u,
          ...g,
          P
        );
        break;
      }
      case "top": {
        const P = W.Sub(
          o[1],
          new W(r * 5 - L / 2, 0, 0)
        ), M = W.Sub(
          o[1],
          new W(r * 5, C, 0)
        ), D = W.Sub(
          o[1],
          new W(r * 5 + L / 2, 0, 0)
        );
        a.push(
          M,
          P,
          ...u,
          ...g,
          ...v,
          ...h,
          D
        );
        break;
      }
      case "topRight": {
        const P = W.Sub(
          o[1],
          new W(r * 1.1, 0, 0)
        ), M = W.Sub(
          o[1],
          new W(r * 1.1 + L / 2, C, 0)
        ), D = W.Sub(
          o[1],
          new W(r * 1.1 + L, 0, 0)
        );
        a.push(
          M,
          P,
          ...u,
          ...g,
          ...v,
          ...h,
          D
        );
        break;
      }
      case "topLeft": {
        const P = W.Add(
          o[0],
          new W(r * 1.1 + L, 0, 0)
        ), M = W.Add(
          o[0],
          new W(r * 1.1 + L / 2, -C, 0)
        ), D = W.Add(
          o[0],
          new W(r * 1.1, 0, 0)
        );
        a.push(
          M,
          P,
          ...u,
          ...g,
          ...v,
          ...h,
          D
        );
        break;
      }
      case "left": {
        const P = W.Add(
          o[0],
          new W(0, n * 5 - C / 2, 0)
        ), M = W.Add(
          o[0],
          new W(-L, n * 5, 0)
        ), D = W.Add(
          o[0],
          new W(0, n * 5 + C / 2, 0)
        );
        a.push(
          M,
          P,
          ...h,
          ...u,
          ...g,
          ...v,
          D
        );
        break;
      }
      case "leftTop": {
        const P = W.Add(
          o[0],
          new W(0, n * 1.1, 0)
        ), M = W.Add(
          o[0],
          new W(-L, n * 1.1 + C / 2, 0)
        ), D = W.Add(
          o[0],
          new W(0, n * 1.1 + C, 0)
        );
        a.push(
          M,
          P,
          ...h,
          ...u,
          ...g,
          ...v,
          D
        );
        break;
      }
      case "leftBottom": {
        const P = W.Sub(
          o[3],
          new W(0, n * 1.1 + C, 0)
        ), M = W.Sub(
          o[3],
          new W(L, n * 1.1 + C / 2, 0)
        ), D = W.Sub(
          o[3],
          new W(0, n * 1.1, 0)
        );
        a.push(
          M,
          P,
          ...h,
          ...u,
          ...g,
          ...v,
          D
        );
        break;
      }
      case "right": {
        const P = W.Add(
          o[1],
          new W(0, n * 5 - C / 2, 0)
        ), M = W.Add(
          o[1],
          new W(L, n * 5, 0)
        ), D = W.Add(
          o[1],
          new W(0, n * 5 + C / 2, 0)
        );
        a.push(
          M,
          D,
          ...g,
          ...v,
          ...h,
          ...u,
          P
        );
        break;
      }
      case "rightTop": {
        const P = W.Add(
          o[1],
          new W(0, n * 1.1, 0)
        ), M = W.Add(
          o[1],
          new W(L, n * 1.1 + C / 2, 0)
        ), D = W.Add(
          o[1],
          new W(0, n * 1.1 + C, 0)
        );
        a.push(
          M,
          D,
          ...g,
          ...v,
          ...h,
          ...u,
          P
        );
        break;
      }
      case "rightBottom": {
        const P = W.Sub(
          o[2],
          new W(0, n * 1.1 + C, 0)
        ), M = W.Sub(
          o[2],
          new W(-L, n * 1.1 + C / 2, 0)
        ), D = W.Sub(
          o[2],
          new W(0, n * 1.1, 0)
        );
        a.push(
          M,
          D,
          ...g,
          ...v,
          ...h,
          ...u,
          P
        );
        break;
      }
    }
    const N = $(this.tmpPoints, t), R = [
      Math.floor(N.x + N.w / 2),
      Math.floor(N.y + N.h / 2)
    ];
    return {
      rect: N,
      pos: R,
      points: a
    };
  }
  updateTempPoints(t) {
    const e = t.slice(-2), s = new O(e[0], e[1]), o = this.tmpPoints[0], { thickness: r } = this.workOptions;
    if (o.isNear(s, r) || O.Sub(o, s).XY.includes(0))
      return !1;
    if (this.tmpPoints.length === 2) {
      if (s.isNear(this.tmpPoints[1], 1))
        return !1;
      this.tmpPoints[1] = s;
    } else
      this.tmpPoints.push(s);
    return !0;
  }
  consumeService(t) {
    var c, l;
    const { op: e, isFullWork: s, workState: o = A.Done } = t, r = (c = this.workId) == null ? void 0 : c.toString();
    if (!r)
      return;
    this.tmpPoints.length = 0;
    for (let h = 0; h < e.length; h += 3)
      this.tmpPoints.push(new O(e[h], e[h + 1], e[h + 2]));
    const n = s ? this.fullLayer : this.drawLayer || this.fullLayer, a = this.draw({
      workId: r,
      layer: n,
      isDrawing: !1,
      isDrawEraserlines: o === A.Done
    });
    return this.oldRect = a, (l = this.vNodes) == null || l.setInfo(r, {
      rect: a,
      op: e,
      opt: this.workOptions,
      toolsType: this.toolsType,
      scaleType: this.scaleType,
      canRotate: this.canRotate,
      centerPos: a && b.getCenterPos(a, n)
    }), a;
  }
  clearTmpPoints() {
    this.tmpPoints.length = 0;
  }
  static updateNodeOpt(t) {
    var u;
    const { node: e, opt: s, vNodes: o } = t, { strokeColor: r, fillColor: n, toolsType: a, placement: c, strokeType: l } = s, h = o.get(e.name), p = h == null ? void 0 : h.opt;
    let d = e;
    return e.tagName === "GROUP" && (d = e.children[0]), r && (d.setAttribute("strokeColor", r), p != null && p.strokeColor && (p.strokeColor = r)), n && (n === "transparent" ? d.setAttribute("fillColor", "rgba(0,0,0,0)") : d.setAttribute("fillColor", n), p != null && p.fillColor && (p.fillColor = n)), a === k.SpeechBalloon && c && (p.placement = c), l && (h != null && h.opt) && ((u = h.opt) != null && u.strokeType) && (h.opt.strokeType = l), h && o.setInfo(e.name, { ...h, opt: p }), b.updateNodeOpt(t);
  }
}
var Vs = Pe, Hs = te, qs = "[object Boolean]";
function js(T) {
  return T === !0 || T === !1 || Hs(T) && Vs(T) == qs;
}
var Zs = js;
const kt = /* @__PURE__ */ Jt(Zs);
class ze extends b {
  constructor(t) {
    super(t);
    y(this, "canRotate", !1);
    y(this, "scaleType", z.both);
    y(this, "toolsType", k.Straight);
    y(this, "tmpPoints", []);
    y(this, "workOptions");
    y(this, "oldRect");
    y(this, "straightTipWidth");
    y(this, "syncTimestamp");
    this.workOptions = t.toolsOpt, this.straightTipWidth = this.workOptions.thickness / 2, this.syncTimestamp = 0, this.syncUnitTime = 50;
  }
  consume(t) {
    const {
      data: e,
      isFullWork: s,
      isSubWorker: o,
      isMainThread: r,
      smoothSync: n,
      isSimpleWorker: a
    } = t, c = this.workId, { op: l, syncUnitTime: h } = e, p = l == null ? void 0 : l.length;
    if (!p || p < 2)
      return { type: w.None };
    h && (this.syncUnitTime = h);
    let d;
    if (this.tmpPoints.length === 0 ? (this.tmpPoints = [new O(l[0], l[1])], d = !1) : d = this.updateTempPoints(l), !d)
      return { type: w.None };
    let u;
    if (o || r || a) {
      const m = s ? this.fullLayer : this.drawLayer || this.fullLayer;
      u = this.draw({ workId: c, layer: m });
    }
    if (a) {
      const m = E(u, this.oldRect);
      this.oldRect = u;
      const g = {
        rect: m,
        type: w.DrawWork,
        dataType: x.Local,
        ...this.baseConsumeResult
      };
      if (n) {
        const I = Date.now();
        I - this.syncTimestamp > this.syncUnitTime && (this.syncTimestamp = I, g.op = this.tmpPoints.map((S) => [...S.XY, 0]).flat(1), g.index = 0, g.isSync = !0);
      }
      return g;
    }
    if (!o && n) {
      const m = Date.now();
      return m - this.syncTimestamp > this.syncUnitTime ? (this.syncTimestamp = m, {
        type: w.DrawWork,
        dataType: x.Local,
        op: this.tmpPoints.map((g) => [...g.XY, 0]).flat(1),
        isSync: !0,
        index: 0,
        ...this.baseConsumeResult
      }) : { type: w.None };
    }
    const f = E(u, this.oldRect);
    return this.oldRect = u, {
      rect: f,
      type: w.DrawWork,
      dataType: x.Local,
      ...this.baseConsumeResult
    };
  }
  consumeAll() {
    var n;
    const t = this.workId;
    if (this.tmpPoints.length < 2)
      return {
        type: w.RemoveNode,
        removeIds: [t]
      };
    const e = this.fullLayer, s = this.draw({ workId: t, layer: e });
    this.oldRect = s;
    const o = this.tmpPoints.map((a) => [...a.XY, 0]).flat(1), r = tt(o);
    return (n = this.vNodes) == null || n.setInfo(t, {
      rect: s,
      op: o,
      opt: this.workOptions,
      toolsType: this.toolsType,
      scaleType: this.scaleType,
      canRotate: this.canRotate,
      centerPos: s && b.getCenterPos(s, e)
    }), {
      rect: s,
      type: w.FullWork,
      dataType: x.Local,
      ops: r,
      isSync: !0,
      ...this.baseConsumeResult,
      updateNodeOpt: {
        useAnimation: !0
      }
    };
  }
  draw(t) {
    const { workId: e, layer: s, isDrawEraserlines: o = !0 } = t, {
      strokeColor: r,
      thickness: n,
      zIndex: a,
      scale: c,
      rotate: l,
      translate: h,
      strokeType: p,
      eraserlines: d,
      lineCap: u,
      lineDash: f
    } = this.workOptions, m = s.worldPosition, g = s.worldScaling, { d: I, rect: S, isDot: v } = this.computDrawPoints(n, p), L = [
      S.x + S.w / 2,
      S.y + S.h / 2
    ], C = new j({
      pos: L,
      anchor: [0.5, 0.5],
      size: [S.w, S.h],
      name: e,
      id: e,
      normalize: !0,
      zIndex: a
    }), N = {
      pos: [0, 0],
      normalize: !0,
      d: I,
      fillColor: r,
      strokeColor: r,
      lineDash: v ? void 0 : p === G.Dotted ? [
        f && f[0] || 1,
        (f && f[1] || 2) * n
      ] : p === G.LongDotted ? [
        (f && f[0] || 1) * n,
        (f && f[1] || 2) * n
      ] : void 0,
      lineCap: v ? void 0 : u,
      lineWidth: p === G.Normal || v ? 0 : n
    };
    c && (N.scale = c), l && (N.rotate = l), h && (N.translate = h);
    const R = new wt(N);
    C.append(R), d && (this.scaleType = z.proportional, this.drawEraserlines(
      {
        group: C,
        eraserlines: d,
        pos: L,
        layer: s
      },
      o
    )), this.replace(s, e, C);
    let P = {
      x: Math.floor(
        S.x * g[0] + m[0] - b.SafeBorderPadding
      ),
      y: Math.floor(
        S.y * g[1] + m[1] - b.SafeBorderPadding
      ),
      w: Math.floor(
        S.w * g[0] + 2 * b.SafeBorderPadding
      ),
      h: Math.floor(
        S.h * g[1] + 2 * b.SafeBorderPadding
      )
    };
    if (l || c || h) {
      const M = R.getBoundingClientRect();
      P = {
        x: Math.floor(M.x - b.SafeBorderPadding),
        y: Math.floor(M.y - b.SafeBorderPadding),
        w: Math.floor(M.width + b.SafeBorderPadding * 2),
        h: Math.floor(M.height + b.SafeBorderPadding * 2)
      };
    }
    return this.isDelete && C.setAttribute("opacity", 0), P;
  }
  computDrawPoints(t, e) {
    return this.tmpPoints[1].distance(this.tmpPoints[0]) > this.straightTipWidth ? this.computFullPoints(t, e) : this.computDotPoints(t);
  }
  computFullPoints(t, e) {
    const s = W.Sub(this.tmpPoints[1], this.tmpPoints[0]).uni(), o = W.Per(s).mul(t / 2), r = O.Sub(
      this.tmpPoints[0],
      o
    ), n = O.Add(
      this.tmpPoints[0],
      o
    ), a = O.Sub(
      this.tmpPoints[1],
      o
    ), c = O.Add(
      this.tmpPoints[1],
      o
    ), l = O.GetSemicircleStroke(
      this.tmpPoints[1],
      a,
      -1,
      8
    ), h = O.GetSemicircleStroke(
      this.tmpPoints[0],
      n,
      -1,
      8
    ), p = [
      r,
      a,
      ...l,
      c,
      n,
      ...h
    ];
    let d;
    return e !== G.Normal ? d = mt(this.tmpPoints, !1) : d = mt(p, !0), {
      d,
      rect: $(p),
      isDot: !1,
      pos: this.tmpPoints[0].XY
    };
  }
  computDotPoints(t) {
    const e = O.GetDotStroke(
      this.tmpPoints[0],
      t / 2,
      8
    );
    return {
      d: mt(e, !0),
      rect: $(e),
      isDot: !0,
      pos: this.tmpPoints[0].XY
    };
  }
  updateTempPoints(t) {
    const e = t.slice(-2), s = new O(e[0], e[1]), o = this.tmpPoints[0], { thickness: r } = this.workOptions;
    if (o.isNear(s, r))
      return !1;
    if (this.tmpPoints.length === 2) {
      if (s.isNear(this.tmpPoints[1], 1))
        return !1;
      this.tmpPoints[1] = s;
    } else
      this.tmpPoints.push(s);
    return !0;
  }
  consumeService(t) {
    var c, l;
    const { op: e, isFullWork: s, workState: o = A.Done } = t, r = (c = this.workId) == null ? void 0 : c.toString();
    if (!r)
      return;
    this.tmpPoints.length = 0;
    for (let h = 0; h < e.length; h += 3)
      this.tmpPoints.push(new O(e[h], e[h + 1], e[h + 2]));
    const n = s ? this.fullLayer : this.drawLayer || this.fullLayer, a = this.draw({
      workId: r,
      layer: n,
      isDrawEraserlines: o === A.Done
    });
    return this.oldRect = a, (l = this.vNodes) == null || l.setInfo(r, {
      rect: a,
      op: e,
      opt: this.workOptions,
      toolsType: this.toolsType,
      scaleType: this.scaleType,
      canRotate: this.canRotate,
      centerPos: a && b.getCenterPos(a, n)
    }), a;
  }
  clearTmpPoints() {
    this.tmpPoints.length = 0;
  }
  static updateNodeOpt(t) {
    var c, l;
    const { node: e, opt: s, vNodes: o } = t, { strokeColor: r, strokeType: n } = s, a = o.get(e.name);
    return r && (e.tagName === "GROUP" ? e.children.forEach((h) => {
      h.setAttribute("strokeColor", r), h.getAttribute("fillColor") && h.setAttribute("fillColor", r);
    }) : (e.setAttribute("strokeColor", r), e.setAttribute("fillColor", r)), (c = a == null ? void 0 : a.opt) != null && c.strokeColor && (a.opt.strokeColor = r)), n && (a != null && a.opt) && ((l = a.opt) != null && l.strokeType) && (a.opt.strokeType = n), a && o.setInfo(e.name, a), b.updateNodeOpt(t);
  }
}
const ft = class ft extends b {
  constructor(t) {
    super(t);
    y(this, "canRotate", !1);
    y(this, "scaleType", z.proportional);
    y(this, "toolsType", k.Text);
    y(this, "tmpPoints", []);
    y(this, "workOptions");
    y(this, "oldRect");
    this.workOptions = t.toolsOpt;
  }
  consume() {
    return {
      type: w.None
    };
  }
  consumeAll() {
    return {
      type: w.None
    };
  }
  consumeService() {
  }
  async draw(t) {
    const { workId: e, layer: s, isDrawLabel: o, boxRect: r } = t, { boxSize: n, boxPoint: a, zIndex: c } = this.workOptions, l = s.worldPosition, h = s.worldScaling;
    if (!a || !n)
      return;
    const p = {
      name: e,
      id: e,
      pos: [a[0] - Bt, a[1] - Bt],
      anchor: [0, 0],
      size: n,
      zIndex: c
    }, d = new j(p), u = {
      x: a[0] - Bt,
      y: a[1] - Bt,
      w: n[0],
      h: n[1]
    }, f = {
      x: Math.floor(u.x * h[0] + l[0]),
      y: Math.floor(u.y * h[1] + l[1]),
      w: Math.floor(u.w * h[0]) + 2,
      h: Math.floor(u.h * h[1]) + 2
    };
    this.replace(s, e, d);
    let m;
    if (r && (m = Rt(f, r)), (o || r && m !== Vt.outside) && s && this.workOptions.text) {
      const g = await ft.createLabels(this.workOptions, s, f), { labels: I, maxWidth: S } = g;
      d.append(...I), f.w = Math.ceil(Math.max(S * s.worldScaling[0], f.w));
    }
    return f;
  }
  async consumeServiceAsync(t) {
    var l, h, p, d;
    const e = (l = this.workId) == null ? void 0 : l.toString();
    if (!e)
      return;
    const { isFullWork: s, replaceId: o, isDrawLabel: r, boxRect: n } = t;
    this.oldRect = o && ((p = (h = this.vNodes) == null ? void 0 : h.get(o)) == null ? void 0 : p.rect) || void 0;
    const a = s ? this.fullLayer : this.drawLayer || this.fullLayer, c = await this.draw({
      workId: e,
      layer: a,
      isDrawLabel: typeof r > "u" && this.workOptions.workState === A.Done || r,
      boxRect: n
    });
    return (d = this.vNodes) == null || d.setInfo(e, {
      rect: c,
      op: [],
      opt: this.workOptions,
      toolsType: this.toolsType,
      scaleType: this.scaleType,
      canRotate: this.canRotate,
      centerPos: c && b.getCenterPos(c, a)
    }), c;
  }
  updataOptService() {
  }
  async updataOptServiceAsync(t, e, s) {
    var f, m;
    if (!this.workId)
      return;
    const o = this.workId.toString(), {
      fontColor: r,
      fontBgColor: n,
      bold: a,
      italic: c,
      lineThrough: l,
      underline: h,
      zIndex: p
    } = t, d = (f = this.vNodes) == null ? void 0 : f.get(o);
    if (!d)
      return;
    r && (d.opt.fontColor = r), n && (d.opt.fontBgColor = n), a && (d.opt.bold = a), c && (d.opt.italic = c), kt(l) && (d.opt.lineThrough = l), kt(h) && (d.opt.underline = h), H(p) && (d.opt.zIndex = p), this.oldRect = d.rect;
    const u = await this.draw({
      workId: o,
      layer: this.fullLayer,
      isDrawLabel: typeof e > "u" && this.workOptions.workState === A.Done || e,
      boxRect: s
    });
    return (m = this.vNodes) == null || m.setInfo(o, {
      rect: u,
      op: [],
      opt: this.workOptions,
      toolsType: this.toolsType,
      scaleType: this.scaleType,
      canRotate: this.canRotate,
      centerPos: u && b.getCenterPos(u, this.fullLayer)
    }), u;
  }
  clearTmpPoints() {
    this.tmpPoints.length = 0;
  }
  static getSafetySnippetRatio(t) {
    const e = (t == null ? void 0 : t.parent).displayRatio || 1, s = Math.ceil(t.worldScaling[0] * 10) / 10;
    let o = s;
    return s <= 2 ? o = s * e * 2 : s > 2 && s <= 3 ? o = s * e * 1.6 : s > 3 && s <= 4 ? o = s * e * 1.2 : s > 4 && (o = s * e), Math.floor(o * 1e3) / 1e3;
  }
  static getSafetySnippetFontLength(t) {
    return Math.floor(ft.textImageSnippetSize * 3 / 4 / t) || 1;
  }
  static async createLabels(t, e, s) {
    var D;
    const o = [], { x: r, y: n } = s, { width: a, height: c } = (D = e.parent) == null ? void 0 : D.parent, l = yt(t.text), h = l.length, {
      fontSize: p,
      lineHeight: d,
      bold: u,
      textAlign: f,
      italic: m,
      fontFamily: g,
      verticalAlign: I,
      fontColor: S,
      fontBgColor: v,
      underline: L,
      lineThrough: C
    } = t, N = ft.getSafetySnippetRatio(e) || 1, R = Math.floor(p * N), P = ft.getSafetySnippetFontLength(R);
    let M = 0;
    for (let B = 0; B < h; B++) {
      const F = l[B], U = d || R * 1.5;
      if (F) {
        const V = is(F), Z = [0, 0], K = [0, p * 1.2];
        I === "middle" && (Z[1] = Math.floor(
          B * p * 1.2 + 6 + p * (1.1 - 1)
        ));
        const nt = [0, Math.floor(-p * 0.15)];
        Z[0] = 6;
        const gt = Math.sin(Math.PI / 180 * 20);
        let pt = 0;
        const Ot = [];
        let Ct = 0;
        for (; Ct < V; ) {
          f === "left" && (nt[0] = pt), Ct === 0 && m === "italic" && (nt[0] = nt[0] - gt / 2 * p);
          const qt = F.slice(Ct, Ct + P), At = {
            anchor: [0, 0],
            pos: nt,
            text: qt,
            fontFamily: g,
            fontSize: R,
            lineHeight: U,
            strokeColor: S,
            fontWeight: u,
            fillColor: S,
            textAlign: f,
            fontStyle: m,
            scale: [1 / N, 1 / N]
          }, dt = new fs(At), at = await dt.textImageReady;
          let Et = !0;
          if (at) {
            const ae = at.rect && at.rect[2], ce = at.rect && at.rect[3];
            if (ae && ce) {
              const le = ae / N, $e = ce / N;
              pt = le + pt, m === "italic" && (u === "bold" ? pt = pt - gt * p * 1.2 : pt = pt - gt * p), ((nt[0] + Z[0] + le) * e.worldScaling[0] + r <= 0 || (nt[0] + Z[0]) * e.worldScaling[0] + r >= a || (nt[1] + Z[1] + $e) * e.worldScaling[1] + n <= 0 || (nt[1] + Z[1]) * e.worldScaling[1] + n >= c) && (dt.disconnect(), Et = !1), Et && Ot.push(dt);
            }
          }
          Ct += P;
        }
        K[0] = pt, m === "italic" && (K[0] = K[0] + gt * p), M = Math.max(M, K[0]);
        let ne = !0;
        if (((Z[0] + K[0]) * e.worldScaling[0] + r <= 0 || Z[0] * e.worldScaling[0] + r >= a || (Z[1] + K[1]) * e.worldScaling[0] + n <= 0 || Z[1] * e.worldScaling[1] + n >= c) && (ne = !1), ne) {
          if (L) {
            const dt = Math.floor(p / 10), at = {
              normalize: !1,
              pos: [0, p * 1.1 + dt / 2],
              lineWidth: dt,
              points: [0, 0, Math.ceil(K[0]), 0],
              strokeColor: S,
              className: "underline"
            }, Et = new ht(at);
            Ot.push(Et);
          }
          if (C) {
            const dt = {
              normalize: !1,
              pos: [0, p * 1.2 / 2],
              lineWidth: Math.floor(p / 10),
              points: [0, 0, Math.ceil(K[0]), 0],
              strokeColor: S,
              className: "lineThrough"
            }, at = new ht(dt);
            Ot.push(at);
          }
          const qt = {
            pos: Z,
            anchor: [0, 0],
            size: K,
            bgcolor: v
          }, At = new j(qt);
          At.append(...Ot), o.push(At);
        }
      }
    }
    return { labels: o, maxWidth: M };
  }
  static updateNodeOpt(t) {
    const { node: e, opt: s, vNodes: o, targetNode: r } = t, {
      fontBgColor: n,
      fontColor: a,
      translate: c,
      originPoint: l,
      scenePoint: h,
      scale: p,
      bold: d,
      italic: u,
      lineThrough: f,
      underline: m,
      fontSize: g,
      textInfos: I,
      zIndex: S
    } = s, v = r && st(r) || o.get(e.name);
    if (!v || !e.parent) return;
    const C = v.opt;
    if (H(S) && (e.setAttribute("zIndex", S), v.opt.zIndex = S), a && C.fontColor && (C.fontColor = a, e.children.forEach((N) => {
      N.tagName === "GROUP" && N.children.forEach((R) => {
        R.tagName === "LABEL" ? (R.setAttribute("fillColor", a), R.setAttribute("strokeColor", a)) : R.tagName === "POLYLINE" && R.setAttribute("strokeColor", a);
      });
    })), n && C.fontBgColor && (C.fontBgColor = n, e.children.forEach((N) => {
      N.tagName === "GROUP" && N.setAttribute("bgcolor", n);
    })), d && (C.bold = d), u && (C.italic = u), kt(f) && (C.lineThrough = f), kt(m) && (C.underline = m), g && (C.fontSize = g), l && h && p && r && C.boxPoint) {
      const N = I == null ? void 0 : I.get(e.name);
      if (N) {
        const { fontSize: D, boxSize: B } = N;
        C.boxSize = B || C.boxSize, C.fontSize = D || C.fontSize;
      }
      const R = [C.boxPoint[0], C.boxPoint[1]];
      Ht(C.boxPoint, h, p, c);
      const P = [v.op[0], v.op[1]], M = [P[0] - R[0], P[1] - R[1]];
      if (v.centerPos = [
        v.centerPos[0] + M[0],
        v.centerPos[1] + M[1]
      ], v.opt.eraserlines)
        for (const D of v.opt.eraserlines) {
          const { op: B, thickness: F } = D;
          D.thickness = Math.round(F * Math.max(p[0], p[1]));
          for (let U = 0; U < B.length; U++)
            ee(B[U], h, p, c);
        }
    } else if (c && C.boxPoint && (C.boxPoint = [
      Math.round(C.boxPoint[0] + c[0]),
      Math.round(C.boxPoint[1] + c[1])
    ], v.centerPos = [
      v.centerPos[0] + c[0],
      v.centerPos[1] + c[1]
    ], v.opt.eraserlines))
      for (const N of v.opt.eraserlines) {
        const { op: R } = N;
        for (let P = 0; P < R.length; P++) {
          const M = R[P].map((D, B) => B % 2 ? D + c[1] : D + c[0]);
          N.op[P] = M;
        }
      }
    return v && o.setInfo(e.name, v), v == null ? void 0 : v.rect;
  }
  static getRectFromLayer(t, e) {
    const s = t.getElementsByName(e)[0];
    if (s) {
      const o = s.getBoundingClientRect();
      let r = {
        x: Math.floor(o.x),
        y: Math.floor(o.y),
        w: Math.floor(o.width + 2),
        h: Math.floor(o.height + 2)
      };
      return s.children.forEach((n) => {
        if (n.tagName === "GROUP") {
          const a = s.getBoundingClientRect();
          r = E(r, {
            x: Math.floor(a.x),
            y: Math.floor(a.y),
            w: Math.floor(a.width + 2),
            h: Math.floor(a.height + 2)
          });
        }
      }), r;
    }
  }
};
// 4k
y(ft, "textImageSnippetSize", 1024 * 4), y(ft, "SafeBorderPadding", 30);
let Xt = ft;
const It = class It extends b {
  constructor(t) {
    super(t);
    y(this, "canRotate", !1);
    y(this, "scaleType", z.none);
    y(this, "toolsType", k.PencilEraser);
    y(this, "tmpPoints", []);
    y(this, "workOptions");
    y(this, "worldPosition");
    y(this, "worldScaling");
    y(this, "eraserRect");
    y(this, "eraserPolyline");
    this.workOptions = t.toolsOpt, this.worldPosition = this.fullLayer.worldPosition, this.worldScaling = this.fullLayer.worldScaling;
  }
  /** 批量合并消费本地数据,返回绘制结果 */
  combineConsume(t) {
    const { workerEngine: e } = t;
    if (this.tmpPoints.length < 2)
      return {
        type: w.None
      };
    const { thickness: s } = this.workOptions, { width: o, height: r } = It.eraserSizes[s], n = Math.round(Math.max(o, r) / this.worldScaling[0]), { rect: a, willNewNodes: c, willDeleteNodes: l } = this.getChangeNodes(
      n,
      e
    );
    return {
      ...this.baseConsumeResult,
      type: w.DrawWork,
      rect: a,
      willNewNodes: c,
      willDeleteNodes: l
    };
  }
  consumeService() {
  }
  updateTempPoints(t, e) {
    let s = this.tmpPoints.length - 1;
    const o = Date.now();
    for (let r = 0; r < t.length; r += 2) {
      const n = this.tmpPoints.length, a = new O(
        t[r],
        t[r + 1]
      );
      if (n === 0) {
        this.tmpPoints.push(a);
        continue;
      }
      const c = n - 1, l = this.tmpPoints[c], h = W.Sub(a, l).uni();
      a.isNear(l, e / 2) || (W.Equals(h, l.v, 0.02) && (this.tmpPoints.pop(), s--), a.setv(h), a.setT(o), this.tmpPoints.push(a));
    }
    return Math.max(s, 0);
  }
  getChangeNodes(t, e) {
    const s = /* @__PURE__ */ new Map(), o = /* @__PURE__ */ new Set();
    let r;
    if (!this.vNodes)
      return { willDeleteNodes: o, willNewNodes: s, rect: r };
    const n = this.worldPosition;
    let a = $(this.tmpPoints, t);
    a = Nt(a, n), a = Le(a, this.worldScaling, n);
    const { nodeRange: c } = this.vNodes.getRectIntersectRange(a);
    if (!c.size)
      return { willDeleteNodes: o, willNewNodes: s, rect: r };
    const l = this.computEraserPointLines(this.tmpPoints, 90);
    for (const [h, p] of c.entries()) {
      if (p.toolsType !== k.Pencil)
        continue;
      let d = Nt(p.rect, [-n[0], -n[1]]);
      d = {
        x: d.x / this.worldScaling[0],
        y: d.y / this.worldScaling[1],
        w: d.w / this.worldScaling[0],
        h: d.h / this.worldScaling[1]
      };
      const u = [
        d.x,
        d.y,
        d.x + d.w,
        d.y + d.h
      ], f = [], m = [];
      for (const g of l)
        ie.polyline(g, u, f);
      if (f.length && p.opt)
        for (const g of f) {
          const I = [];
          for (let S = 0; S < g.length; S++)
            S !== 0 && lt(g[S], g[S - 1]) || I.push(...g[S]);
          m.push(I);
        }
      if (m.length && e) {
        const g = e.createWorkShapeNode({
          workId: h,
          toolsType: k.Pencil,
          toolsOpt: p.opt
        });
        g.setWorkId(h);
        const I = g == null ? void 0 : g.computPencilPoints({
          op: p.op,
          eraserPolylines: m,
          eraserThickness: t
        });
        if (r = E(r, p.rect), o.add(h), e.removeNode(h), I.length)
          for (let S = 0; S < I.length; S++) {
            const v = `${h}_${Date.now()}_${S}`, L = v, N = e.setFullWork({
              workId: L,
              opt: p.opt,
              toolsType: k.Pencil
            }).consumeService({
              op: I[S],
              isFullWork: !0,
              replaceId: L
            });
            e.clearWorkShapeNodeCache(L), r = E(r, N), s.set(v, {
              ...p,
              toolsType: k.Pencil,
              rect: N ?? p.rect,
              name: v,
              op: I[S]
            });
          }
      }
    }
    return { willNewNodes: s, willDeleteNodes: o, rect: r };
  }
  consume(t) {
    const { data: e } = t, { op: s } = e;
    if (!s || s.length === 0)
      return {
        type: w.None,
        ...this.baseConsumeResult
      };
    const { thickness: o } = this.workOptions, { width: r, height: n } = It.eraserSizes[o], a = Math.max(r, n) / this.worldScaling[0];
    return this.updateTempPoints(s, a), {
      type: w.None,
      ...this.baseConsumeResult
    };
  }
  computEraserPointLines(t, e = 60) {
    const s = [];
    let o = 0;
    const r = 360 - e;
    for (let n = 1; n < t.length; n++) {
      const a = t[n - 1], c = t[n];
      if (n === 1 && o === 0 && (s[o] = [a.XY]), s[o].push(c.XY), n < t.length - 1) {
        const l = c.getAngleByPoints(a, t[n + 1]);
        (l < e || l > r) && (o++, s[o] = [c.XY]);
      }
    }
    return t.length === 1 && s.length === 0 && s.push([t[0].XY, t[0].XY]), s;
  }
  consumeAll(t) {
    const { workerEngine: e } = t;
    if (this.replace(this.fullLayer, this.workId), !this.tmpPoints.length)
      return {
        type: w.None,
        ...this.baseConsumeResult
      };
    const { thickness: s } = this.workOptions, { width: o, height: r } = It.eraserSizes[s], n = Math.max(o, r) / this.worldScaling[0], { willDeleteNodes: a, willNewNodes: c, rect: l } = this.getChangeNodes(
      n,
      e
    );
    if (a.size === 0 && c.size === 0)
      return {
        type: w.FullWork,
        ...this.baseConsumeResult,
        rect: l
      };
    const h = [];
    this.tmpPoints.map((d) => {
      h.push(d.x, d.y, n / 2);
    });
    const p = tt(h);
    return {
      ...this.baseConsumeResult,
      type: w.FullWork,
      dataType: x.Local,
      rect: l,
      ops: p,
      willNewNodes: c,
      willDeleteNodes: a
    };
  }
  clearTmpPoints() {
    this.tmpPoints.length = 0;
  }
};
// 来源于white-sdk
y(It, "eraserSizes", re);
let Yt = It;
const Pt = class Pt extends b {
  constructor(t) {
    super(t);
    y(this, "canRotate", !1);
    y(this, "scaleType", z.none);
    y(this, "toolsType", k.BitMapEraser);
    y(this, "tmpPoints", []);
    y(this, "workOptions");
    y(this, "worldPosition");
    y(this, "worldScaling");
    y(this, "eraserRect");
    y(this, "eraserPolyline");
    y(this, "oldRect");
    this.workOptions = t.toolsOpt, this.worldPosition = this.fullLayer.worldPosition, this.worldScaling = this.fullLayer.worldScaling;
  }
  /** 批量合并消费本地数据,返回绘制结果 */
  combineConsume(t) {
    const { workerEngine: e } = t;
    if (this.tmpPoints.length < 2)
      return {
        type: w.None
      };
    const { thickness: s } = this.workOptions, { width: o, height: r } = Pt.eraserSizes[s], n = Math.round(Math.max(o, r) / this.worldScaling[0]), { willDeleteNodes: a, rect: c } = this.getChangeNodes(n, e);
    return {
      ...this.baseConsumeResult,
      type: w.DrawWork,
      rect: c,
      willDeleteNodes: a
    };
  }
  consumeService() {
  }
  updateTempPoints(t, e) {
    let s = this.tmpPoints.length - 1;
    const o = Date.now();
    for (let r = 0; r < t.length; r += 2) {
      const n = this.tmpPoints.length, a = new O(
        t[r],
        t[r + 1]
      );
      if (n === 0) {
        this.tmpPoints.push(a);
        continue;
      }
      const c = n - 1, l = this.tmpPoints[c], h = W.Sub(a, l).uni();
      a.isNear(l, e / 2) || (W.Equals(h, l.v, 0.02) && (this.tmpPoints.pop(), s--), a.setv(h), a.setT(o), this.tmpPoints.push(a));
    }
    return Math.max(s, 0);
  }
  getChangeNodes(t, e) {
    const s = /* @__PURE__ */ new Map(), o = /* @__PURE__ */ new Set();
    let r;
    if (!this.vNodes)
      return { willDeleteNodes: o, willUpdateNodes: s, rect: r };
    const n = this.worldPosition;
    let a = $(this.tmpPoints, t);
    a = Nt(a, n), a = Le(a, this.worldScaling, n);
    const { nodeRange: c } = this.vNodes.getRectIntersectRange(a);
    if (!c.size)
      return { willDeleteNodes: o, willUpdateNodes: s, rect: r };
    const l = this.computEraserPointLines(this.tmpPoints, 120);
    for (const [h, p] of c.entries()) {
      if (p.toolsType === k.Text || p.toolsType === k.Image)
        continue;
      let d = Nt(p.rect, [-n[0], -n[1]]);
      d = {
        x: d.x / this.worldScaling[0],
        y: d.y / this.worldScaling[1],
        w: d.w / this.worldScaling[0],
        h: d.h / this.worldScaling[1]
      };
      const u = [
        d.x,
        d.y,
        d.x + d.w,
        d.y + d.h
      ], f = [], m = [];
      for (const S of l)
        ie.polyline(S, u, f);
      if (f.length && p.opt)
        for (const S of f) {
          const v = [];
          for (let L = 0; L < S.length && !(isNaN(S[L][0]) || isNaN(S[L][1])); L++) {
            const C = S[L].map((N) => Math.round(N * 100) / 100);
            if (L !== 0) {
              const N = S[L - 1].map((R) => Math.round(R * 100) / 100);
              if (lt(C, N))
                continue;
            }
            v.push(...C);
          }
          v.length !== 0 && m.push(v);
        }
      if (!m.length)
        continue;
      const g = p.opt.eraserlines ? p.opt.eraserlines : [], I = g.find((S) => S.workId === this.workId);
      if (I ? I.op = m : g.push({ workId: this.workId, op: m, thickness: t }), p.opt.eraserlines = g, e) {
        const { toolsType: S } = p, v = e.createWorkShapeNode({
          workId: h,
          toolsType: S,
          toolsOpt: p.opt
        });
        v == null || v.setWorkId(h);
        const L = v == null ? void 0 : v.consumeService({
          op: p.op,
          isFullWork: !0,
          replaceId: h
        });
        if (r = E(r, L), v != null && v.isDelete) {
          e.removeNode(h), o.add(h);
          continue;
        }
      }
      s.set(h, p);
    }
    return { willUpdateNodes: s, willDeleteNodes: o, rect: r };
  }
  consume(t) {
    const { data: e } = t, { op: s } = e;
    if (!s || s.length === 0)
      return {
        type: w.None,
        ...this.baseConsumeResult
      };
    const { thickness: o } = this.workOptions, { width: r, height: n } = Pt.eraserSizes[o], a = Math.round(Math.max(r, n) / this.worldScaling[0]);
    return this.updateTempPoints(s, a), {
      type: w.None,
      ...this.baseConsumeResult
    };
  }
  computEraserPointLines(t, e = 60) {
    const s = [];
    let o = 0;
    const r = 360 - e;
    for (let n = 1; n < t.length; n++) {
      const a = t[n - 1], c = t[n];
      if (n === 1 && o === 0 && (s[o] = [a.XY]), s[o].push(c.XY), n < t.length - 1) {
        const l = c.getAngleByPoints(a, t[n + 1]);
        (l < e || l > r) && (o++, s[o] = [c.XY]);
      }
    }
    return t.length === 1 && s.length === 0 && s.push([t[0].XY, t[0].XY]), s;
  }
  consumeAll(t) {
    const { workerEngine: e } = t;
    if (this.replace(this.fullLayer, this.workId), !this.tmpPoints.length)
      return {
        type: w.None,
        ...this.baseConsumeResult
      };
    const { thickness: s } = this.workOptions, { width: o, height: r } = Pt.eraserSizes[s], n = Math.round(Math.max(o, r) / this.worldScaling[0]), { willDeleteNodes: a, willUpdateNodes: c, rect: l } = this.getChangeNodes(
      n,
      e
    );
    if (a.size === 0 && c.size === 0)
      return {
        type: w.FullWork,
        ...this.baseConsumeResult,
        rect: l
      };
    const h = [];
    this.tmpPoints.map((d) => {
      h.push(d.x, d.y, n / 2);
    });
    const p = tt(h);
    return {
      ...this.baseConsumeResult,
      type: w.FullWork,
      dataType: x.Local,
      rect: l,
      ops: p,
      willUpdateNodes: c,
      willDeleteNodes: a
    };
  }
  clearTmpPoints() {
    this.tmpPoints.length = 0;
  }
};
// 来源于white-sdk
y(Pt, "eraserSizes", re);
let $t = Pt;
class xt extends b {
  constructor(t) {
    super(t);
    y(this, "canRotate", !1);
    y(this, "scaleType", z.all);
    y(this, "toolsType", k.BackgroundSVG);
    y(this, "tmpPoints", []);
    y(this, "workOptions");
    y(this, "oldRect");
    this.workOptions = t.toolsOpt, this.scaleType = xt.getScaleType(this.workOptions);
  }
  consume() {
    return { type: w.None };
  }
  consumeAll() {
    return { type: w.None };
  }
  draw(t) {
    const { layer: e, workId: s, replaceId: o } = t, { centerX: r, centerY: n, width: a, height: c, rotate: l, zIndex: h } = this.workOptions, p = (h || 0) - 100, d = new j({
      anchor: [0.5, 0.5],
      pos: [r, n],
      name: s,
      size: [a, c],
      zIndex: p,
      rotate: l
    }), u = new Kt({
      normalize: !0,
      pos: [0, 0],
      width: a,
      height: c
    });
    d.appendChild(u), this.replace(e, o || s, d);
    const f = d.getBoundingClientRect();
    if (f)
      return {
        x: Math.floor(f.x - b.SafeBorderPadding),
        y: Math.floor(f.y - b.SafeBorderPadding),
        w: Math.floor(f.width + b.SafeBorderPadding * 2),
        h: Math.floor(f.height + b.SafeBorderPadding * 2)
      };
  }
  consumeService(t) {
    var r;
    const { replaceId: e } = t, s = this.workId, o = this.draw({ workId: s, layer: this.fullLayer, replaceId: e });
    return (r = this.vNodes) == null || r.setInfo(s, {
      rect: o,
      op: [],
      opt: this.workOptions,
      toolsType: this.toolsType,
      scaleType: this.scaleType,
      canRotate: this.canRotate,
      centerPos: o && b.getCenterPos(o, this.fullLayer)
    }), o;
  }
  clearTmpPoints() {
    this.tmpPoints.length = 0;
  }
  static getScaleType(t) {
    const { uniformScale: e, rotate: s } = t;
    return e !== !1 ? z.proportional : s && Math.abs(s) % 90 > 0 ? z.proportional : z.all;
  }
  static updateNodeOpt(t) {
    const { node: e, opt: s, vNodes: o, targetNode: r } = t, {
      translate: n,
      originPoint: a,
      scenePoint: c,
      scale: l,
      angle: h,
      isLocked: p,
      zIndexForBackgroundSVG: d
    } = s, u = r && st(r) || o.get(e.name);
    if (!u) return;
    const f = e.parent;
    if (f) {
      if (H(d)) {
        const m = (d || 0) - 100;
        e.setAttribute("zIndex", m), u.opt.zIndex = d;
      }
      if (kt(p) && (u.opt.locked = p), a && c && l && n) {
        const { centerX: m, centerY: g, width: I, height: S, uniformScale: v } = u.opt, L = v !== !1 ? [l[0], l[0]] : l, C = [m, g], N = [m, g];
        Ht(N, c, L, n);
        const R = [
          N[0] - C[0],
          N[1] - C[1]
        ];
        u.centerPos = [
          u.centerPos[0] + R[0],
          u.centerPos[1] + R[1]
        ], u.opt.width = Math.round(I * L[0]), u.opt.height = Math.round(S * L[1]), u.opt.centerX = N[0], u.opt.centerY = N[1];
      } else if (n)
        u.opt.centerX = u.opt.centerX + n[0], u.opt.centerY = u.opt.centerY + n[1], u.centerPos = [
          u.centerPos[0] + n[0],
          u.centerPos[1] + n[1]
        ];
      else if (H(h))
        if (u.opt.rotate = h, u.scaleType = xt.getScaleType(
          u.opt
        ), r) {
          const m = se(u.rect, h);
          u.rect = m;
        } else {
          const m = b.getRectFromLayer(f, e.name);
          u.rect = m || u.rect;
        }
      return u && o.setInfo(e.name, u), u == null ? void 0 : u.rect;
    }
  }
}
function Ue(T) {
  switch (T) {
    case k.Arrow:
      return Me;
    case k.Pencil:
      return Ne;
    case k.Straight:
      return ze;
    case k.Ellipse:
      return Oe;
    case k.Polygon:
    case k.Triangle:
      return Be;
    case k.Star:
    case k.Rhombus:
      return Ee;
    case k.Rectangle:
      return Ae;
    case k.SpeechBalloon:
      return Fe;
    case k.Text:
      return Xt;
    case k.LaserPen:
      return xe;
    case k.Eraser:
      return Ut;
    case k.PencilEraser:
      return Yt;
    case k.BitMapEraser:
      return $t;
    case k.Selector:
      return Gt;
    case k.Image:
      return Dt;
    case k.BackgroundSVG:
      return xt;
  }
}
function Lt(T, i) {
  const { toolsType: t, ...e } = T;
  switch (t) {
    case k.Arrow:
      return new Me(e);
    case k.Pencil:
      return new Ne(e);
    case k.Straight:
      return new ze(e);
    case k.Ellipse:
      return new Oe(e);
    case k.Polygon:
    case k.Triangle:
      return new Be(e);
    case k.Star:
    case k.Rhombus:
      return new Ee(e);
    case k.Rectangle:
      return new Ae(e);
    case k.SpeechBalloon:
      return new Fe(e);
    case k.Text:
      return new Xt(e);
    case k.LaserPen:
      return new xe(e);
    case k.Eraser:
      return new Ut(e, i);
    case k.BitMapEraser:
      return new $t(e);
    case k.PencilEraser:
      return new Yt(e);
    case k.Selector:
      return e.vNodes ? new Gt({
        ...e,
        vNodes: e.vNodes,
        drawLayer: e.fullLayer
      }) : void 0;
    case k.Image:
      return new Dt(e);
    case k.BackgroundSVG:
      return new xt({
        ...e,
        toolsOpt: e.toolsOpt
      });
    default:
      return;
  }
}
function Ge(T) {
  const i = [], t = [
    "PATH",
    "SPRITE",
    "POLYLINE",
    "RECT",
    "ELLIPSE",
    "LABEL",
    "SPRITESVG"
  ];
  for (const e of T) {
    if (e.tagName === "GROUP" && e.children.length) {
      const s = Ge(e.children);
      i.push(...s);
    }
    e.tagName && t.includes(e.tagName) && i.push(e);
  }
  return i;
}
class Xe {
  constructor(i, t) {
    y(this, "viewId");
    y(this, "scene");
    y(this, "fullLayer");
    y(this, "curNodeMap");
    y(this, "targetNodeMap", []);
    y(this, "highLevelIds");
    y(this, "canClearUids");
    y(this, "localUid");
    this.viewId = i, this.scene = t, this.curNodeMap = new fe();
  }
  init(i) {
    this.fullLayer = i;
  }
  get(i) {
    return this.curNodeMap.get(i);
  }
  setLocalUid(i) {
    this.localUid = i;
  }
  getLocalUid() {
    return this.localUid;
  }
  setCanClearUids(i) {
    this.canClearUids = i;
  }
  getCanClearUids() {
    return this.canClearUids;
  }
  getCanEraserNodes(i, t) {
    const e = /* @__PURE__ */ new Map();
    for (const [s, o] of i.entries())
      o.toolsType !== k.BackgroundSVG && (o.toolsType === k.Image && o.opt.type === Mt.Image && (o.opt.locked || t && t.disableEraseImage) || o.toolsType === k.Text && (o.opt.workState === A.Doing || o.opt.workState === A.Start || t && t.disableEraseText) || this.isCanClearWorkId(s) && e.set(s, o));
    return e;
  }
  getNodesByType(i) {
    const t = /* @__PURE__ */ new Map();
    return this.curNodeMap.forEach((e, s) => {
      e.toolsType === i && t.set(s, e);
    }), t;
  }
  gethasEraserNodes() {
    const i = /* @__PURE__ */ new Map();
    return this.curNodeMap.forEach((t, e) => {
      var s;
      (s = t.opt.eraserlines) != null && s.length && i.set(e, t);
    }), i;
  }
  hasRenderNodes() {
    return !0;
  }
  has(i) {
    return this.curNodeMap.has(i);
  }
  setInfo(i, t) {
    const e = this.curNodeMap.get(i) || {
      name: i,
      rect: t.rect
    };
    t.rect && (e.rect = st(t.rect)), t.op && rs(t.op) && (e.op = st(t.op)), t.canRotate && (e.canRotate = t.canRotate), t.scaleType && (e.scaleType = t.scaleType), t.opt && (e.opt = st(t.opt)), t.toolsType && (e.toolsType = t.toolsType), t.centerPos && (e.centerPos = st(t.centerPos)), kt(t.isSelected) && (e.isSelected = t.isSelected), e.rect ? this.curNodeMap.set(i, e) : this.curNodeMap.delete(i);
  }
  selected(i) {
    this.setInfo(i, { isSelected: !0 });
  }
  unSelected(i) {
    this.setInfo(i, { isSelected: !1 });
  }
  delete(i) {
    this.curNodeMap.delete(i);
  }
  clear() {
    this.curNodeMap.clear(), this.targetNodeMap.length = 0;
  }
  getRectIntersectRange(i, t = !0, e = !0) {
    let s;
    const o = /* @__PURE__ */ new Map();
    for (const [r, n] of this.curNodeMap.entries())
      if (Zt(i, n.rect)) {
        if (t && n.toolsType === k.Image && n.opt.locked || e && n.toolsType === k.Text && (n.opt.workState === A.Doing || n.opt.workState === A.Start))
          continue;
        s = E(s, n.rect), o.set(r, n);
      }
    return {
      rectRange: s,
      nodeRange: o
    };
  }
  getNodeRectFormShape(i, t) {
    const e = Ue(t.toolsType);
    return this.fullLayer && (e == null ? void 0 : e.getRectFromLayer(this.fullLayer, i));
  }
  updateNodeRect(i) {
    const t = this.curNodeMap.get(i);
    if (t) {
      const e = this.getNodeRectFormShape(i, t);
      if (!e) {
        this.curNodeMap.delete(i);
        return;
      }
      t.rect = e, this.curNodeMap.set(i, t);
    }
  }
  updateHighLevelNodesRect(i) {
    this.highLevelIds = i;
    for (const t of this.highLevelIds.keys())
      this.updateNodeRect(t);
  }
  updateLowLevelNodesRect() {
    var i;
    for (const t of this.curNodeMap.keys())
      (i = this.highLevelIds) != null && i.has(t) || this.updateNodeRect(t);
  }
  clearHighLevelIds() {
    this.highLevelIds = void 0;
  }
  setTargetAssignKeys(i) {
    const t = new fe();
    for (const e of i) {
      const s = this.curNodeMap.get(e);
      s && t.set(e, st(s));
    }
    return this.targetNodeMap.push(st(t)), this.targetNodeMap.length - 1;
  }
  setTarget() {
    const i = this.curNodeMap.keys();
    return this.setTargetAssignKeys(Array.from(i));
  }
  getLastTarget() {
    return this.targetNodeMap[this.targetNodeMap.length - 1];
  }
  deleteLastTarget() {
    this.targetNodeMap.length && (this.targetNodeMap.length = this.targetNodeMap.length - 1);
  }
  getTarget(i) {
    return this.targetNodeMap[i];
  }
  deleteTarget(i) {
    this.targetNodeMap.length = i;
  }
  clearTarget() {
    this.targetNodeMap.length = 0;
  }
  isLocalWorkId(i) {
    return i.split(Wt).length === 1;
  }
  isCanClearWorkId(i) {
    if (this.canClearUids === void 0 || this.canClearUids === !0)
      return !0;
    if (ns(this.canClearUids)) {
      const t = i.split(Wt);
      if (t.length === 1)
        return this.canClearUids.has("localSelf") || this.localUid && this.canClearUids.has(this.localUid);
      if (t.length === 2)
        return this.canClearUids.has(t[0]);
    }
    return !1;
  }
}
class Ks {
  constructor(i, t, e) {
    y(this, "viewId");
    y(this, "type");
    y(this, "scene");
    y(this, "fullLayer");
    y(this, "vNodes");
    y(this, "dpr");
    y(this, "contextType");
    y(this, "opt");
    y(this, "cameraOpt");
    y(this, "isSafari", !1);
    y(this, "combinePostMsg", /* @__PURE__ */ new Set());
    y(this, "workerTaskId");
    y(this, "protectedTask");
    // protected hasPriorityDraw: boolean = true;
    y(this, "delayPostDoneResolve");
    y(this, "cacheImages", /* @__PURE__ */ new Map());
    y(this, "imageResolveMap", /* @__PURE__ */ new Map());
    y(this, "taskUpdateCameraId");
    y(this, "debounceUpdateCameraId");
    var s, o;
    if (this.viewId = i, this.type = e, this.opt = t, this.dpr = t.dpr, this.contextType = this.getSupportContextType(
      e,
      (s = t == null ? void 0 : t.offscreenCanvasOpt) == null ? void 0 : s.contextType
    ), !this.contextType)
      throw new Error(
        "Sorry, your browser doesn't support canvas context type 2d or webgl"
      );
    try {
      this.scene = this.createScene(t), this.createRenderLayer(this.scene, t);
    } catch (r) {
      if (this.contextType !== "2d")
        this.contextType = "2d", (o = this.scene) == null || o.disconnect(), this.scene = this.createScene(t), this.createRenderLayer(this.scene, t);
      else
        throw r;
    }
    this.vNodes = new Xe(i, this.scene);
  }
  createRenderLayer(i, t) {
    const e = J.bufferSize.full, s = J.bufferSize.sub;
    this.fullLayer = this.createLayer("fullLayer", i, {
      ...t.layerOpt,
      bufferSize: this.viewId === ct ? e : s * 2
    });
  }
  async updateDpr(i) {
    this.dpr = i, this.scene.displayRatio = i;
  }
  getCachedImages(i) {
    var t;
    return (t = this.cacheImages.get(i)) == null ? void 0 : t.imageBitmap;
  }
  getCachedImagesByWorkId(i) {
    for (const [t, e] of this.cacheImages.entries())
      if (t === i && e.imageBitmap)
        return e.imageBitmap;
  }
  deleteCachedImagesByWorkId(i, t) {
    for (const [e, s] of this.cacheImages.entries())
      t && e === t || s.workId === i && (s.imageBitmap.close(), this.cacheImages.delete(e));
  }
  clearCacheImages() {
    this.cacheImages.forEach((i) => i.imageBitmap.close()), this.cacheImages.clear();
  }
  clearImageResolveMap() {
    this.imageResolveMap.forEach(({ timer: i }) => {
      i && clearTimeout(i);
    }), this.imageResolveMap.clear();
  }
  setIsSafari(i) {
    this.isSafari = i;
  }
  async on(i) {
    const { msgType: t, toolsType: e, opt: s, imageSrc: o, imageBitmap: r, workId: n, dpr: a } = i;
    switch (t) {
      case w.UpdateDpr: {
        H(a) && await this.updateDpr(a);
        break;
      }
      case w.UpdateCamera: {
        await this.updateCamera(i);
        break;
      }
      case w.Destroy: {
        this.destroy();
        break;
      }
      case w.Clear: {
        await this.clearAll();
        break;
      }
      case w.UpdateTools: {
        if (e && s) {
          const c = {
            toolsType: e,
            toolsOpt: s,
            combineUnitTime: i.combineUnitTime,
            maxCombineEraserTime: i.maxCombineEraserTime
          };
          this.localWork.setToolsOpt(c);
        }
        break;
      }
      case w.GetImageBitMap: {
        if (o && r && n) {
          const c = n.toString();
          this.cacheImages.set(o, {
            imageBitmap: r,
            workId: c
          });
          const l = this.imageResolveMap.get(o);
          if (l) {
            const { resolve: h, timer: p } = l;
            p && clearTimeout(p), h && h(o);
          }
          this.deleteCachedImagesByWorkId(c, o);
        }
        break;
      }
    }
  }
  getIconSize(i, t, e) {
    const s = i * e, o = t * e;
    return s <= 50 || o <= 50 ? [50, 50] : s <= 100 || o <= 100 ? [100, 100] : s <= 200 || o <= 200 ? [200, 200] : s <= 400 || o <= 400 ? [400, 400] : s <= 800 || o <= 800 ? [800, 800] : [1600, 1600];
  }
  async loadImageBitMap(i) {
    const { toolsType: t, opt: e, workId: s, isSubWorker: o } = i;
    if (t === k.Image && e && s) {
      const r = s.toString(), { src: n, type: a, width: c, height: l, strokeColor: h } = e;
      if (!n || !a || !c || !l)
        return;
      let p = n;
      if (a === Mt.Iconify) {
        const [m, g] = this.getIconSize(c, l, this.dpr);
        p = `${n}?width=${m}&height=${g}&color=${h}`;
      }
      if (this.cacheImages.has(p)) {
        const m = this.getCachedImages(p);
        if (m)
          return m;
      }
      if (this.imageResolveMap.has(p)) {
        const m = this.getCachedImagesByWorkId(r);
        if (m)
          return m;
      }
      const f = await new Promise((m) => {
        const g = this.imageResolveMap.get(p) || {
          resolve: void 0,
          timer: void 0
        };
        g.timer && clearTimeout(g.timer), g.resolve = m, g.timer = setTimeout(() => {
          const I = this.imageResolveMap.get(p);
          I != null && I.resolve && I.resolve(p);
        }, 5e3), this.imageResolveMap.set(p, g), this._post({
          sp: [
            {
              imageSrc: p,
              workId: r,
              viewId: this.viewId,
              isgl: !!this.fullLayer.parent.gl,
              isSubWorker: o,
              type: w.GetImageBitMap
            }
          ]
        });
      });
      return this.imageResolveMap.delete(f), this.getCachedImages(p);
    }
  }
  createLocalWork(i) {
    const { workId: t, opt: e, toolsType: s } = i;
    t && e && (!this.localWork.getToolsOpt() && s && this.setToolsOpt({
      toolsType: s,
      toolsOpt: e
    }), this.setWorkOpt({
      workId: t,
      toolsOpt: e
    }));
  }
  updateScene(i, t, e) {
    i.attr({ ...t });
    const { width: s, height: o } = t;
    i.container.width = s, i.container.height = o, i.width = s, i.height = o, this.updateLayer({ width: s, height: o }, e);
  }
  updateLayer(i, t) {
    const { width: e, height: s } = i, o = t || this.fullLayer;
    o && (o.parent.setAttribute("width", e), o.parent.setAttribute("height", s), o.setAttribute("size", [e, s]), o.setAttribute("pos", [e * 0.5, s * 0.5]));
  }
  getSupportContextType(i, t) {
    const e = new OffscreenCanvas(100, 100);
    let s = ["2d"];
    i === Qt.Full && this.viewId === ct && (s = ["webgl2", "webgl", "2d"], t && s.unshift(t));
    for (const o of s)
      try {
        if (e.getContext(o))
          return o;
      } catch (r) {
        throw r;
      }
  }
  createScene(i) {
    const { offscreenCanvasOpt: t } = i, { width: e, height: s } = t, o = new OffscreenCanvas(e, s);
    t.contextType && delete t.contextType;
    const r = new oe({
      container: o,
      displayRatio: this.dpr,
      depth: !1,
      desynchronized: !0,
      failIfMajorPerformanceCaveat: !0,
      ...t,
      contextType: this.contextType,
      id: this.viewId
    });
    return r.setAttribute("id", this.viewId), r;
  }
  createLayer(i, t, e, s) {
    const o = `offscreen-${i}`, { width: r, height: n } = e;
    s && (e.offscreen = !0, e.canvas = s);
    const a = t.layer(o, e), c = new j({
      anchor: [0.5, 0.5],
      pos: [r * 0.5, n * 0.5],
      size: [r, n],
      name: "viewport",
      id: i
    });
    return a.append(c), c;
  }
  async clearAll() {
    var i;
    this.fullLayer && (this.fullLayer.parent.children.forEach((t) => {
      t.name !== "viewport" && t.remove();
    }), Q(this.fullLayer, this.fullLayer.parent)), this.taskUpdateCameraId && (clearTimeout(this.taskUpdateCameraId), this.taskUpdateCameraId = void 0), this.debounceUpdateCameraId && (clearTimeout(this.debounceUpdateCameraId), this.debounceUpdateCameraId = void 0), this.clearCacheImages(), this.clearImageResolveMap(), this.localWork.destroy(), (i = this.serviceWork) == null || i.destroy();
  }
  setToolsOpt(i) {
    this.localWork.setToolsOpt(i);
  }
  setWorkOpt(i) {
    const { workId: t, toolsOpt: e } = i;
    t && e && this.localWork.setWorkOptions(t.toString(), e);
  }
  destroy() {
    var i;
    this.vNodes.clear(), this.fullLayer.remove(), q(this.fullLayer, this.fullLayer.parent), this.clearCacheImages(), this.clearImageResolveMap(), this.scene.remove(), this.localWork.destroy(), (i = this.serviceWork) == null || i.destroy();
  }
  async post(i) {
    this.combinePostMsg.add(i), await this.runBatchPostData();
  }
  async runBatchPostData() {
    this.workerTaskId || (this.workerTaskId = setTimeout(() => {
      this.workerTaskId = void 0, this.combinePost();
    }, 16)), this.type === Qt.Full && !this.delayPostDoneResolve && await new Promise((t) => {
      this.delayPostDoneResolve = t;
    }) && (this.delayPostDoneResolve = void 0);
  }
  combinePostData() {
    var n, a;
    this.workerTaskId = void 0;
    const i = [], t = [];
    let e, s, o;
    const r = /* @__PURE__ */ new Set();
    for (const c of this.combinePostMsg.values()) {
      if ((n = c.render) != null && n.length)
        for (const l of c.render) {
          let h = !1;
          if (l.workId && r.add(l.workId), l.isClearAll && (l.rect = this.getSceneRect(), l.isClear = !0, delete l.isClearAll), l.drawCanvas) {
            const p = this.getLayer(l.drawCanvas);
            if (!p || !(p.parent instanceof ms))
              continue;
            if (p.parent.render(), l.isDrawAll) {
              const d = this.getSceneRect();
              l.rect = d, delete l.isDrawAll;
            }
          }
          for (const p of i)
            if (l.viewId === p.viewId) {
              l.isClear && p.clearCanvas && p.isClear && p.clearCanvas === l.clearCanvas && (p.rect = E(
                p.rect,
                l.rect
              ), h = !0), p.drawCanvas && p.drawCanvas === l.drawCanvas && (p.rect = E(
                p.rect,
                l.rect
              ), h = !0);
              continue;
            }
          h || (l.isClear && !l.drawCanvas ? i.unshift(l) : i.push(l));
        }
      if ((a = c.sp) != null && a.length)
        for (const l of c.sp) {
          let h = !1;
          for (const p of t)
            if (lt(l, p)) {
              h = !0;
              break;
            }
          h || t.push(l);
        }
      H(c.fullWorkerDrawCount) && (e = c.fullWorkerDrawCount), H(c.subWorkerDrawCount) && (s = c.subWorkerDrawCount), H(c.consumeCount) && (o = c.consumeCount);
    }
    return this.combinePostMsg.clear(), {
      render: i,
      sp: t,
      fullWorkerDrawCount: e,
      subWorkerDrawCount: s,
      consumeCount: o,
      workIds: r,
      viewId: this.viewId
    };
  }
  getSceneRect() {
    const { width: i, height: t } = this.scene;
    return {
      x: 0,
      y: 0,
      w: Math.floor(i),
      h: Math.floor(t)
    };
  }
}
class Ye {
  constructor(i) {
    y(this, "viewId");
    y(this, "vNodes");
    y(this, "thread");
    y(this, "fullLayer");
    y(this, "drawLayer");
    y(this, "_post");
    y(this, "tmpOpt");
    y(this, "workShapes", /* @__PURE__ */ new Map());
    y(this, "drawCount", 0);
    y(this, "consumeCount", 0);
    y(this, "syncUnitTime", J.syncOpt.interval);
    y(this, "combineUnitTime", J.bezier.combineUnitTime);
    y(this, "maxCombineEraserTime", J.pencilEraser.maxCombineTime);
    this.thread = i.thread, this.viewId = i.viewId, this.vNodes = i.vNodes, this.fullLayer = i.fullLayer, this.drawLayer = i.drawLayer, this._post = this.thread.post.bind(i.thread);
  }
  setmaxCombineEraserTime(i) {
    this.maxCombineEraserTime = i;
  }
  setCombineUnitTime(i) {
    this.combineUnitTime = i;
  }
  setSyncUnitTime(i) {
    this.syncUnitTime = i;
  }
  destroy() {
    this.workShapes.clear();
  }
  getWorkShapes() {
    return this.workShapes;
  }
  getWorkShape(i) {
    return this.workShapes.get(i);
  }
  createWorkShape(i, t) {
    if (i && this.tmpOpt) {
      const e = {
        toolsType: this.tmpOpt.toolsType,
        toolsOpt: t || this.tmpOpt.toolsOpt
      }, s = this.createWorkShapeNode({ ...e, workId: i });
      s && this.workShapes.set(i, s);
    }
  }
  setWorkOptions(i, t) {
    const e = this.getWorkShape(i);
    e || this.createWorkShape(i, t), e == null || e.setWorkOptions(t);
  }
  createWorkShapeNode(i) {
    var t;
    return Lt(
      {
        ...i,
        vNodes: this.vNodes,
        fullLayer: this.fullLayer,
        drawLayer: this.drawLayer
      },
      (t = this.thread) == null ? void 0 : t.serviceWork
    );
  }
  setToolsOpt(i) {
    var t, e, s;
    ((t = this.tmpOpt) == null ? void 0 : t.toolsType) !== i.toolsType && (e = this.tmpOpt) != null && e.toolsType && this.clearAllWorkShapesCache(), this.tmpOpt = i, (s = i.toolsOpt) != null && s.syncUnitTime && (this.syncUnitTime = i.toolsOpt.syncUnitTime), i.combineUnitTime && (this.combineUnitTime = i.combineUnitTime), i.maxCombineEraserTime && (this.maxCombineEraserTime = i.maxCombineEraserTime);
  }
  getToolsOpt() {
    return this.tmpOpt;
  }
  clearWorkShapeNodeCache(i) {
    var t;
    (t = this.getWorkShape(i)) == null || t.clearTmpPoints(), this.workShapes.delete(i);
  }
  clearAllWorkShapesCache() {
    this.workShapes.forEach((i) => i.clearTmpPoints()), this.workShapes.clear();
  }
  setFullWork(i) {
    const { workId: t, opt: e, toolsType: s } = i;
    if (t && e && s) {
      const o = t.toString();
      let r;
      return t && this.workShapes.has(o) ? (r = this.workShapes.get(o), r == null || r.setWorkOptions(e)) : r = this.createWorkShapeNode({
        toolsOpt: e,
        toolsType: s,
        workId: o
      }), r ? (this.workShapes.set(o, r), r) : void 0;
    }
  }
}
class Qs extends Ye {
  constructor(t) {
    super(t);
    y(this, "drawWorkActiveId");
  }
  runSelectWork(t) {
    var s;
    const e = this.setFullWork(t);
    e && ((s = t.selectIds) != null && s.length) && t.workId && e.selectServiceNode(
      t.workId.toString(),
      { selectIds: t.selectIds },
      !1
    );
  }
  workShapesDone() {
    for (const t of this.workShapes.keys())
      this.clearWorkShapeNodeCache(t);
    Q(this.fullLayer, this.fullLayer.parent);
  }
  async consumeDraw(t) {
    const { workId: e, fullWorkerDrawCount: s, postCount: o } = t, r = e == null ? void 0 : e.toString(), n = r && this.workShapes.get(r);
    if (!n)
      return;
    this.drawWorkActiveId && this.drawWorkActiveId !== r && (await this.consumeDrawAll({
      workId: this.drawWorkActiveId,
      viewId: this.viewId,
      msgType: w.DrawWork,
      dataType: x.Local
    }), this.drawWorkActiveId = void 0), !this.drawWorkActiveId && r !== X && (this.drawWorkActiveId = r);
    const a = n.toolsType;
    H(o) && (this.consumeCount = o);
    const c = n.consume({
      data: t,
      drawCount: this.drawCount,
      isFullWork: !0,
      isSubWorker: !0,
      removeDrawCount: s
    });
    switch (a) {
      case k.Selector:
        c && (this.drawCount++, await this.drawSelector(c));
        break;
      case k.Ellipse:
      case k.Arrow:
      case k.Straight:
      case k.Rectangle:
      case k.Star:
      case k.Polygon:
      case k.SpeechBalloon: {
        c && (this.drawCount++, await this.drawShape(c));
        break;
      }
      case k.Pencil: {
        c && (this.drawCount++, await this.drawPencil(c, e == null ? void 0 : e.toString()));
        break;
      }
      case k.BitMapEraser:
      case k.PencilEraser: {
        c && (this.drawCount++, await this._post({
          subWorkerDrawCount: this.drawCount,
          consumeCount: this.consumeCount
        }));
        break;
      }
    }
  }
  async consumeDrawAll(t) {
    const { workId: e } = t;
    if (e) {
      const s = e.toString();
      this.drawWorkActiveId === s && (this.drawWorkActiveId = void 0);
      const o = this.workShapes.get(s);
      if (!o)
        return;
      switch (o.toolsType) {
        case k.Selector:
          this.drawCount = 0, Q(this.fullLayer, this.fullLayer.parent), this.clearWorkShapeNodeCache(s), this._post({
            render: [
              {
                isClearAll: !0,
                clearCanvas: _.Float,
                viewId: this.viewId
              }
            ]
          });
          break;
        case k.Arrow:
        case k.Straight:
        case k.Ellipse:
        case k.Pencil:
        case k.Rectangle:
        case k.Star:
        case k.Polygon:
        case k.SpeechBalloon:
        case k.BitMapEraser:
        case k.PencilEraser:
          this.drawCount = 0, Q(this.fullLayer, this.fullLayer.parent), this.clearWorkShapeNodeCache(s);
          break;
      }
    }
  }
  async removeWork(t) {
    const { workId: e } = t, s = e == null ? void 0 : e.toString();
    if (s) {
      const o = this.removeNode(s);
      if (o) {
        const r = [];
        r.push(
          {
            rect: ut(o),
            clearCanvas: _.Float,
            isClear: !0,
            viewId: this.viewId
          },
          {
            rect: ut(o),
            drawCanvas: _.Float,
            viewId: this.viewId
          }
        ), await this._post({
          render: r
        });
      }
    }
  }
  removeNode(t) {
    const e = this.workShapes.has(t);
    let s;
    return e && (this.fullLayer.getElementsByName(t).forEach((o) => {
      const r = o.getBoundingClientRect();
      s = E(s, {
        x: r.x - b.SafeBorderPadding,
        y: r.y - b.SafeBorderPadding,
        w: r.width + b.SafeBorderPadding * 2,
        h: r.height + b.SafeBorderPadding * 2
      }), o.remove(), q(o, this.fullLayer.parent);
    }), s && this.clearWorkShapeNodeCache(t)), s;
  }
  async drawPencil(t, e) {
    await this._post({
      subWorkerDrawCount: this.drawCount,
      consumeCount: this.consumeCount,
      render: [
        {
          rect: t == null ? void 0 : t.rect,
          workId: e,
          drawCanvas: _.Float,
          viewId: this.viewId
        }
      ],
      sp: (t == null ? void 0 : t.op) && [t]
    });
  }
  async drawShape(t) {
    await this._post({
      subWorkerDrawCount: this.drawCount,
      consumeCount: this.consumeCount,
      render: [
        {
          rect: (t == null ? void 0 : t.rect) && ut(t.rect),
          isClear: !0,
          clearCanvas: _.Float,
          viewId: this.viewId
        },
        {
          rect: (t == null ? void 0 : t.rect) && ut(t.rect),
          drawCanvas: _.Float,
          viewId: this.viewId
        }
      ]
    });
  }
  async drawSelector(t) {
    await this._post({
      subWorkerDrawCount: this.drawCount,
      consumeCount: this.consumeCount,
      render: [
        {
          rect: (t == null ? void 0 : t.rect) && ut(t.rect),
          isClear: !0,
          clearCanvas: _.Float,
          viewId: this.viewId
        },
        {
          rect: (t == null ? void 0 : t.rect) && ut(t.rect),
          drawCanvas: _.Float,
          viewId: this.viewId
        }
      ]
    });
  }
}
class Js {
  constructor(i) {
    y(this, "viewId");
    y(this, "vNodes");
    y(this, "topLayer");
    y(this, "thread");
    y(this, "post");
    y(this, "serviceWorkShapes", /* @__PURE__ */ new Map());
    y(this, "localWorkShapes", /* @__PURE__ */ new Map());
    y(this, "tmpOpt");
    y(this, "syncUnitTime", J.syncOpt.interval);
    y(this, "animationId");
    this.viewId = i.viewId, this.vNodes = i.vNodes, this.topLayer = i.topLayer, this.thread = i.thread, this.post = i.thread.post.bind(i.thread);
  }
  canUseTopLayer(i) {
    return i === k.LaserPen;
  }
  getWorkShape(i) {
    return this.localWorkShapes.get(i);
  }
  createWorkShape(i, t) {
    if (i && this.tmpOpt) {
      const e = {
        toolsType: this.tmpOpt.toolsType,
        toolsOpt: t || this.tmpOpt.toolsOpt
      }, s = this.createWorkShapeNode({ ...e, workId: i });
      return s && this.localWorkShapes.set(i, {
        node: s,
        toolsType: s.toolsType,
        workState: A.Start
      }), s;
    }
  }
  setWorkOptions(i, t) {
    var s;
    const e = (s = this.localWorkShapes.get(i)) == null ? void 0 : s.node;
    e || this.createWorkShape(i, t), e == null || e.setWorkOptions(t);
  }
  createWorkShapeNode(i) {
    const { toolsType: t } = i;
    if (t === k.LaserPen)
      return Lt({
        ...i,
        vNodes: this.vNodes,
        fullLayer: this.topLayer,
        drawLayer: this.topLayer
      });
  }
  clearAllWorkShapesCache() {
    this.localWorkShapes.forEach((i) => {
      var t;
      return (t = i.node) == null ? void 0 : t.clearTmpPoints();
    }), this.localWorkShapes.clear();
  }
  setToolsOpt(i) {
    var t;
    this.tmpOpt = i, (t = i.toolsOpt) != null && t.syncUnitTime && (this.syncUnitTime = i.toolsOpt.syncUnitTime);
  }
  getToolsOpt() {
    return this.tmpOpt;
  }
  consumeDraw(i) {
    const { workId: t, dataType: e } = i;
    if (e === x.Service)
      this.activeServiceWorkShape(i);
    else {
      const s = t == null ? void 0 : t.toString(), o = s && this.localWorkShapes.get(s);
      if (!o)
        return;
      const r = o.node.consume({
        data: i,
        isFullWork: !1,
        isSubWorker: !0
      });
      r.rect && (o.totalRect = E(
        r.rect,
        o.totalRect
      ), o.result = r, o.workState = A.Doing, s && this.localWorkShapes.set(s, o));
    }
    this.runAnimation();
  }
  consumeDrawAll(i) {
    const { workId: t, dataType: e } = i;
    if (e === x.Service)
      this.activeServiceWorkShape(i);
    else {
      const s = t == null ? void 0 : t.toString(), o = s && this.localWorkShapes.get(s);
      if (!o)
        return;
      const r = o.node.consumeAll({ data: i });
      o.totalRect = E(
        r.rect,
        o.totalRect
      ), o.result = r, o.workState = A.Done, s && this.localWorkShapes.set(s, o);
    }
    this.runAnimation();
  }
  destroy() {
    this.serviceWorkShapes.clear(), this.localWorkShapes.clear();
  }
  setNodeKey(i, t, e, s) {
    return t.toolsType = e, t.node = this.createWorkShapeNode({
      workId: i,
      toolsType: e,
      toolsOpt: s
    }), t;
  }
  activeServiceWorkShape(i) {
    var p, d;
    const { workId: t, opt: e, toolsType: s, type: o, updateNodeOpt: r, ops: n, op: a } = i;
    if (!t)
      return;
    const c = t.toString(), l = (p = this.vNodes.get(c)) == null ? void 0 : p.rect;
    if (!((d = this.serviceWorkShapes) != null && d.has(c))) {
      let u = {
        toolsType: s,
        animationWorkData: a || [],
        animationIndex: 0,
        type: o,
        updateNodeOpt: r,
        ops: n,
        oldRect: l
      };
      s && e && (u = this.setNodeKey(c, u, s, e)), this.serviceWorkShapes.set(c, u);
    }
    const h = this.serviceWorkShapes.get(c);
    o && (h.type = o), n && (h.animationWorkData = yt(n), h.ops = n), r && (h.updateNodeOpt = r), a && (h.animationWorkData = a), h.node && h.node.getWorkId() !== c && h.node.setWorkId(c), l && (h.oldRect = l), s && e && (h.toolsType !== s && s && e && this.setNodeKey(c, h, s, e), h.node && h.node.setWorkOptions(e));
  }
  computNextAnimationIndex(i, t) {
    const e = Math.floor(
      (i.animationWorkData || []).slice(i.animationIndex).length * 32 / t / this.syncUnitTime
    ) * t;
    return Math.min(
      (i.animationIndex || 0) + (e || t),
      (i.animationWorkData || []).length
    );
  }
  async animationDraw() {
    var r, n, a, c;
    this.animationId = void 0;
    let i = !1;
    const t = /* @__PURE__ */ new Map(), e = [], s = [], o = [];
    for (const [l, h] of this.serviceWorkShapes.entries())
      switch (h.toolsType) {
        case k.LaserPen: {
          const d = this.computNextAnimationIndex(
            h,
            8
          ), u = Math.max(0, h.animationIndex || 0), f = (h.animationWorkData || []).slice(
            u,
            d
          );
          if ((h.animationIndex || 0) < d) {
            const m = (r = h.node) == null ? void 0 : r.consumeService({
              op: f,
              isFullWork: !1
            });
            h.totalRect = E(h.totalRect, m), h.animationIndex = d, f.length && t.set(l, {
              workState: u === 0 ? A.Start : d === ((n = h.animationWorkData) == null ? void 0 : n.length) ? A.Done : A.Doing,
              op: f.slice(-2)
            });
          }
          if (s.push({
            isClear: !0,
            rect: h.totalRect,
            clearCanvas: _.TopFloat,
            viewId: this.viewId
          }), e.push({
            rect: h.totalRect,
            drawCanvas: _.TopFloat,
            viewId: this.viewId
          }), h.isDel) {
            (a = h.node) == null || a.clearTmpPoints(), this.serviceWorkShapes.delete(l);
            break;
          }
          h.ops && h.animationIndex === ((c = h.animationWorkData) == null ? void 0 : c.length) && !h.isDel && (this.topLayer.getElementsByName(l.toString())[0] || (h.isDel = !0, this.serviceWorkShapes.set(l, h))), i = !0;
          break;
        }
      }
    for (const [l, h] of this.localWorkShapes.entries()) {
      const { result: p, toolsType: d, totalRect: u, isDel: f, workState: m } = h;
      switch (d) {
        case k.LaserPen: {
          if (u && (s.push({
            isClear: !0,
            rect: u,
            clearCanvas: _.TopFloat,
            viewId: this.viewId
          }), e.push({
            rect: u,
            drawCanvas: _.TopFloat,
            viewId: this.viewId
          })), f) {
            h.node.clearTmpPoints(), this.localWorkShapes.delete(l), o.push({
              removeIds: [l.toString()],
              viewId: this.viewId,
              type: w.RemoveNode
            });
            break;
          }
          p && ((p.op || p.ops) && o.push(p), h.result = void 0), !this.topLayer.getElementsByName(l.toString())[0] && m === A.Done && (h.isDel = !0, this.localWorkShapes.set(l, h)), i = !0;
          break;
        }
      }
    }
    i && this.runAnimation(), t.size && t.forEach((l, h) => {
      o.push({
        type: w.Cursor,
        uid: h.split(Wt)[0],
        op: l.op,
        workState: l.workState,
        viewId: this.viewId
      });
    }), (e.length || s.length || o.length) && this.post({
      render: [...s, ...e],
      sp: o
    });
  }
  runAnimation() {
    this.animationId || (this.animationId = setTimeout(() => {
      this.animationId = void 0, this.animationDraw();
    }, 16));
  }
}
class to extends Ye {
  constructor(t) {
    super(t);
    y(this, "opt");
    y(this, "scene");
    y(this, "cameraOpt");
    y(this, "vNodes");
    this.opt = t, this.vNodes = void 0;
  }
  createSnapshotFullLayer(t, e) {
    this.scene = this.opt.createScene(t), this.fullLayer = this.opt.createLayer("snapshotFullLayer", this.scene, e);
  }
  destroySnapshotFullLayer() {
    var t, e, s;
    Q(this.fullLayer, this.fullLayer.parent), (t = this.fullLayer) == null || t.disconnect(), (e = this.fullLayer) == null || e.remove(), (s = this.scene) == null || s.remove(), this.scene = void 0;
  }
  updateScene(t) {
    if (!this.scene)
      throw new Error("SnapshotWork scene is not initialized");
    this.scene.attr({ ...t });
    const { width: e, height: s } = t;
    this.scene.width = e, this.scene.height = s, this.updateLayer({ width: e, height: s });
  }
  updateLayer(t) {
    if (!this.fullLayer)
      throw new Error("SnapshotWork snapshotFullLayer is not initialized");
    const { width: e, height: s } = t;
    this.fullLayer.parent.setAttribute("width", e), this.fullLayer.parent.setAttribute("height", s), this.fullLayer.setAttribute("size", [e, s]), this.fullLayer.setAttribute("pos", [e * 0.5, s * 0.5]);
  }
  setCameraOpt(t) {
    this.cameraOpt = t;
    const { scale: e, centerX: s, centerY: o, width: r, height: n } = t;
    if (!this.scene)
      throw new Error("SnapshotWork scene is not initialized");
    if (!this.fullLayer)
      throw new Error("SnapshotWork snapshotFullLayer is not initialized");
    (r !== this.scene.width || n !== this.scene.height) && this.updateScene({ width: r, height: n }), this.fullLayer.setAttribute("scale", [e, e]), this.fullLayer.setAttribute("translate", [-s, -o]);
  }
  getOffscreen() {
    var e;
    return ((e = this.fullLayer) == null ? void 0 : e.parent).canvas;
  }
  getRectImageBitmap(t, e) {
    const { rect: s } = t, o = this.thread.dpr, r = Math.floor(s.x * o), n = Math.floor(s.y * o), a = Math.floor(s.w * o), c = Math.floor(s.h * o);
    return createImageBitmap(this.getOffscreen(), r, n, a, c, e);
  }
  async getSnapshot(t) {
    const { scenePath: e, scenes: s, cameraOpt: o, w: r, h: n } = t;
    if (e && s && o && this.fullLayer) {
      this.setCameraOpt(o);
      for (const [c, l] of Object.entries(s))
        if (l != null && l.type)
          switch (l == null ? void 0 : l.type) {
            case w.UpdateNode:
            case w.FullWork: {
              const { opt: h } = l, p = {
                ...l,
                opt: h,
                workId: c,
                msgType: w.FullWork,
                dataType: x.Service,
                viewId: this.viewId
              };
              await this.runFullWork(p);
              break;
            }
          }
      let a;
      r && n && (a = {
        resizeWidth: r,
        resizeHeight: n
      });
      try {
        await this.getSnapshotRender({ scenePath: e, options: a });
      } catch (c) {
        const l = c && c instanceof Error ? c.message : c == null ? void 0 : c.toString();
        console.error("[SnapshotWork] getSnapshotRender error", l), this.thread._post({
          sp: [
            {
              type: w.ReportError,
              reportString: `[SnapshotWork] getSnapshotRender error: ${l}`
            }
          ]
        });
      }
    }
  }
  async runFullWork(t) {
    var o;
    const e = this.setFullWork(t), s = t.ops && yt(t.ops);
    if (e) {
      let r, n;
      const a = (o = e.getWorkId()) == null ? void 0 : o.toString();
      return e.toolsType === k.BackgroundSVG ? r = e.consumeService({
        isFullWork: !0,
        replaceId: a
      }) : e.toolsType === k.Image ? r = await e.consumeServiceAsync({
        isFullWork: !0,
        worker: this.thread
      }) : e.toolsType === k.Text ? r = await e.consumeServiceAsync({
        isFullWork: !0,
        replaceId: a,
        isDrawLabel: !0
      }) : (r = e.consumeService({
        op: s,
        isFullWork: !0,
        replaceId: a
      }), n = (t == null ? void 0 : t.updateNodeOpt) && e.updataOptService(t.updateNodeOpt)), t.workId && this.workShapes.delete(t.workId.toString()), E(r, n);
    }
  }
  getSceneRect() {
    if (!this.scene)
      throw new Error("SnapshotWork scene is not initialized");
    const { width: t, height: e } = this.scene;
    return {
      x: 0,
      y: 0,
      w: Math.floor(t),
      h: Math.floor(e)
    };
  }
  async getSnapshotRender(t) {
    var r;
    const { scenePath: e, options: s } = t;
    ((r = this.fullLayer) == null ? void 0 : r.parent).render();
    const o = await this.getRectImageBitmap(
      { rect: this.getSceneRect(), drawCanvas: _.None },
      s
    );
    o && (this.thread._post(
      {
        sp: [
          {
            type: w.Snapshot,
            scenePath: e,
            imageBitmap: o,
            viewId: this.viewId,
            index: 0
          }
        ]
      },
      [o]
    ), o.close());
  }
  async getBoundingRect(t) {
    const { scenePath: e, scenes: s, cameraOpt: o } = t;
    if (e && s && o && this.fullLayer) {
      this.setCameraOpt(o);
      let r;
      for (const [n, a] of Object.entries(s))
        if (a != null && a.type)
          switch (a == null ? void 0 : a.type) {
            case w.UpdateNode:
            case w.FullWork: {
              const c = await this.runFullWork({
                ...a,
                workId: n,
                msgType: w.FullWork,
                dataType: x.Service,
                viewId: this.viewId
              });
              r = E(r, c);
              break;
            }
          }
      r && this.thread._post({
        sp: [
          {
            type: w.BoundingBox,
            scenePath: e,
            rect: r,
            viewId: this.viewId
          }
        ]
      });
    }
  }
}
var Qt = /* @__PURE__ */ ((T) => (T.Full = "full", T.Sub = "sub", T))(Qt || {});
class eo extends Ks {
  constructor(t, e, s) {
    super(
      t,
      e,
      "sub"
      /* Sub */
    );
    y(this, "type", "sub");
    y(this, "_post");
    y(this, "topLayer");
    y(this, "serviceWork");
    y(this, "localWork");
    y(this, "topLayerWork");
    y(this, "snapshotWork");
    this._post = s;
    const o = J.bufferSize.sub;
    this.topLayer = e.isUseSimple ? void 0 : this.createLayer("topLayer", this.scene, {
      ...e.layerOpt,
      bufferSize: o,
      contextType: "2d"
    });
    const r = {
      thread: this,
      viewId: this.viewId,
      vNodes: this.vNodes,
      fullLayer: this.fullLayer,
      topLayer: this.topLayer,
      isUseSimple: e.isUseSimple
    };
    this.localWork = new Qs(r), e.isUseSimple || (this.topLayerWork = new Js(r)), this.snapshotWork = new to({
      ...r,
      createScene: this.createScene.bind(this),
      createLayer: this.createLayer.bind(this)
    }), this.vNodes.init(this.fullLayer);
  }
  async combinePost() {
    var r, n;
    const { render: t, ...e } = this.combinePostData();
    let s;
    if (t != null && t.length) {
      const a = [];
      for (const c of t)
        if (c.rect) {
          if (c.rect = io(
            st(c.rect),
            this.scene,
            this.dpr
          ), !c.rect)
            continue;
          if (c.drawCanvas && c.rect && c.rect.w > 0 && c.rect.h > 0) {
            const l = await this.getRectImageBitmap(
              c
            );
            c.imageBitmap = l, s || (s = []), s.push(l);
          }
          a.push(c);
        }
      e.render = a;
    }
    const o = (r = e.sp) == null ? void 0 : r.filter(
      (a) => a.type !== w.None || a.isLockSentEventCursor || a.needUndoTicker
    );
    o != null && o.length ? e.sp = o.map((a) => ({ ...a, viewId: this.viewId })) : delete e.sp, e.consumeCount === void 0 && delete e.consumeCount, e.subWorkerDrawCount === void 0 && delete e.subWorkerDrawCount, (o != null && o.length || e.consumeCount || e.subWorkerDrawCount || (n = e == null ? void 0 : e.render) != null && n.length) && this._post(e, s), this.delayPostDoneResolve && this.delayPostDoneResolve(!0);
  }
  async on(t) {
    var c, l;
    const {
      msgType: e,
      toolsType: s,
      opt: o,
      dataType: r,
      workState: n,
      isLockSentEventCursor: a
    } = t;
    switch (e) {
      case w.UpdateTools: {
        if (s && ((c = this.topLayerWork) != null && c.canUseTopLayer(s)) && o) {
          const h = {
            toolsType: s,
            toolsOpt: o
          };
          this.topLayerWork.setToolsOpt(h);
          return;
        }
        break;
      }
      case w.CreateWork: {
        this.createLocalWork(t);
        return;
      }
      case w.DrawWork: {
        n === A.Done && r === x.Local ? (await this.consumeDrawAll(r, t), s === k.LaserPen && a && this.post({
          sp: [
            {
              type: w.None,
              isLockSentEventCursor: a
            }
          ]
        })) : this.consumeDraw(r, t);
        return;
      }
      case w.RemoveNode: {
        await this.removeNode(t);
        return;
      }
      case w.FullWork: {
        s && ((l = this.topLayerWork) != null && l.canUseTopLayer(s)) && await this.consumeDrawAll(r, t);
        return;
      }
      case w.Snapshot: {
        this.snapshotWork.createSnapshotFullLayer(this.opt, {
          ...this.opt.layerOpt,
          bufferSize: this.viewId === ct ? 6e3 : 3e3,
          contextType: "2d"
        }), await this.snapshotWork.getSnapshot(t), this.snapshotWork.destroySnapshotFullLayer();
        return;
      }
      case w.BoundingBox: {
        this.snapshotWork.createSnapshotFullLayer(this.opt, {
          ...this.opt.layerOpt,
          bufferSize: this.viewId === ct ? 6e3 : 3e3,
          contextType: "2d"
        }), await this.snapshotWork.getBoundingRect(t), this.snapshotWork.destroySnapshotFullLayer();
        return;
      }
    }
    await super.on(t);
  }
  createLocalWork(t) {
    var r;
    const { workId: e, toolsType: s, opt: o } = t;
    if (s && ((r = this.topLayerWork) != null && r.canUseTopLayer(s)) && e && o) {
      this.topLayerWork.getToolsOpt() || this.topLayerWork.setToolsOpt({
        toolsType: s,
        toolsOpt: o
      }), this.topLayerWork.setWorkOptions(e.toString(), o);
      return;
    }
    s && super.createLocalWork(t);
  }
  async removeNode(t) {
    const { dataType: e } = t;
    e === x.Local && await this.localWork.removeWork(t);
  }
  getLayer(t) {
    switch (t) {
      case _.TopFloat:
        return this.topLayer || this.fullLayer;
      default:
        return this.fullLayer;
    }
  }
  getOffscreen(t) {
    var s;
    return ((s = this.getLayer(t)) == null ? void 0 : s.parent).canvas;
  }
  async consumeDraw(t, e) {
    var r;
    const { workId: s, toolsType: o } = e;
    if (s) {
      if (o && ((r = this.topLayerWork) != null && r.canUseTopLayer(o))) {
        t === x.Local && (this.topLayerWork.getWorkShape(
          s.toString()
        ) || this.createLocalWork(e)), this.topLayerWork.consumeDraw(e);
        return;
      }
      o && (this.localWork.getWorkShape(s.toString()) || this.createLocalWork(e), await this.localWork.consumeDraw(e));
      return;
    }
  }
  async consumeDrawAll(t, e) {
    var n;
    const { workId: s, toolsType: o, dataType: r } = e;
    if (s) {
      const a = s.toString();
      if (o && ((n = this.topLayerWork) != null && n.canUseTopLayer(o))) {
        r === x.Local && (this.topLayerWork.getWorkShape(a) || this.createLocalWork(e)), this.topLayerWork.consumeDrawAll(e);
        return;
      }
      o && (this.localWork.getWorkShape(a) || super.createLocalWork(e), this.localWork.consumeDrawAll(e));
      return;
    }
  }
  async clearAll() {
    this.vNodes.clear(), super.clearAll(), this.topLayer && (this.topLayer.parent.children.forEach((t) => {
      t.name !== "viewport" && t.remove();
    }), Q(this.topLayer, this.topLayer.parent)), await this.post({
      render: [
        {
          isClearAll: !0,
          clearCanvas: _.TopFloat,
          viewId: this.viewId
        }
      ],
      sp: [
        {
          type: w.Clear
        }
      ]
    });
  }
  getRectImageBitmap(t, e) {
    const { rect: s, drawCanvas: o } = t, r = Math.floor(s.x * this.dpr), n = Math.floor(s.y * this.dpr), a = Math.floor(s.w * this.dpr || 1), c = Math.floor(s.h * this.dpr || 1);
    return createImageBitmap(
      this.getOffscreen(o),
      r,
      n,
      a,
      c,
      e
    );
  }
  updateLayer(t) {
    const { width: e, height: s } = t;
    super.updateLayer(t), this.topLayer && (this.topLayer.parent.setAttribute("width", e), this.topLayer.parent.setAttribute("height", s), this.topLayer.setAttribute("size", [e, s]), this.topLayer.setAttribute("pos", [e * 0.5, s * 0.5]));
  }
  async updateDpr(t) {
    super.updateDpr(t);
    const e = this.topLayerWork && !!this.topLayerWork.localWorkShapes.size || !1, s = !!this.localWork.getWorkShapes().size, o = [];
    s && o.push({
      isClearAll: !0,
      clearCanvas: _.Float,
      viewId: this.viewId
    }), e && o.push({
      isClearAll: !0,
      clearCanvas: _.TopFloat,
      viewId: this.viewId
    }), o.length && await this.post({ render: o });
  }
  async updateCamera(t) {
    var o;
    const e = [], { cameraOpt: s } = t;
    if (s && !lt(this.cameraOpt, s)) {
      const r = this.topLayerWork && !!this.topLayerWork.localWorkShapes.size || !1, n = !!this.localWork.getWorkShapes().size;
      if (n && this.localWork.workShapesDone(), this.setCameraOpt(s), n && e.push({
        isClearAll: !0,
        clearCanvas: _.Float,
        viewId: this.viewId
      }), r && this.topLayerWork) {
        e.push({
          isClearAll: !0,
          clearCanvas: _.TopFloat,
          viewId: this.viewId
        });
        for (const [
          a,
          c
        ] of this.topLayerWork.localWorkShapes.entries() || [])
          if (c.totalRect) {
            let l;
            (o = this.topLayer) == null || o.getElementsByName(a.toString()).forEach((h) => {
              const p = h.getBoundingClientRect(), d = ut({
                x: p.x,
                y: p.y,
                w: p.width,
                h: p.height
              });
              l = E(l, d);
            }), c.totalRect = l, this.topLayerWork.localWorkShapes.set(a, c);
          }
      }
      e.length && await this.post({ render: e });
    }
  }
  setCameraOpt(t, e) {
    var c, l;
    this.cameraOpt = t;
    const { scale: s, centerX: o, centerY: r, width: n, height: a } = t;
    (n !== this.scene.width || a !== this.scene.height) && this.updateScene(this.scene, { width: n, height: a }), e ? (e.setAttribute("scale", [s, s]), e.setAttribute("translate", [-o, -r])) : (this.fullLayer.setAttribute("scale", [s, s]), this.fullLayer.setAttribute("translate", [-o, -r]), (c = this.topLayer) == null || c.setAttribute("scale", [s, s]), (l = this.topLayer) == null || l.setAttribute("translate", [-o, -r]));
  }
}
class Dt extends b {
  constructor(t) {
    super(t);
    y(this, "canRotate", !0);
    y(this, "scaleType", z.all);
    y(this, "toolsType", k.Image);
    y(this, "tmpPoints", []);
    y(this, "workOptions");
    y(this, "oldRect");
    this.workOptions = t.toolsOpt, this.scaleType = Dt.getScaleType(this.workOptions);
  }
  consume() {
    return { type: w.None };
  }
  consumeAll() {
    return { type: w.None };
  }
  draw(t) {
    const { layer: e, workId: s, replaceId: o, imageBitmap: r } = t, { centerX: n, centerY: a, width: c, height: l, rotate: h, zIndex: p, eraserlines: d } = this.workOptions, u = new j({
      anchor: [0.5, 0.5],
      pos: [n, a],
      name: s,
      size: [c, l],
      zIndex: p,
      rotate: h
    }), f = {
      anchor: [0.5, 0.5],
      pos: [0, 0],
      size: [c, l],
      texture: r
    };
    r || (f.bgcolor = "rgba(0,0,0,0.3)");
    const m = new Ce(f);
    u.append(m), d && this.drawEraserlines({
      group: u,
      eraserlines: d,
      pos: [n, a],
      layer: e
    }), this.replace(e, o || s, u);
    const g = u.getBoundingClientRect();
    if (g)
      return {
        x: Math.floor(g.x - b.SafeBorderPadding),
        y: Math.floor(g.y - b.SafeBorderPadding),
        w: Math.floor(g.width + b.SafeBorderPadding * 2),
        h: Math.floor(g.height + b.SafeBorderPadding * 2)
      };
  }
  consumeService() {
  }
  async consumeServiceAsync(t) {
    var l, h, p, d;
    const { isFullWork: e, replaceId: s, worker: o } = t, { src: r, uuid: n } = this.workOptions, a = ((l = this.workId) == null ? void 0 : l.toString()) || n, c = e ? this.fullLayer : this.drawLayer || this.fullLayer;
    if (r) {
      const u = await o.loadImageBitMap({
        toolsType: this.toolsType,
        opt: this.workOptions,
        workId: a,
        isSubWorker: o instanceof eo
      });
      if (u) {
        const f = this.draw({ workId: a, layer: c, replaceId: s, imageBitmap: u });
        return this.oldRect = a && ((p = (h = this.vNodes) == null ? void 0 : h.get(a)) == null ? void 0 : p.rect) || void 0, (d = this.vNodes) == null || d.setInfo(a, {
          rect: f,
          op: [],
          opt: this.workOptions,
          toolsType: this.toolsType,
          scaleType: this.scaleType,
          canRotate: this.canRotate,
          centerPos: f && b.getCenterPos(f, c)
        }), f;
      }
    }
  }
  clearTmpPoints() {
    this.tmpPoints.length = 0;
  }
  static getScaleType(t) {
    const { uniformScale: e, rotate: s } = t;
    return e !== !1 ? z.proportional : s && Math.abs(s) % 90 > 0 ? z.proportional : z.all;
  }
  static updateNodeOpt(t) {
    const { node: e, opt: s, vNodes: o, targetNode: r } = t, {
      translate: n,
      originPoint: a,
      scenePoint: c,
      scale: l,
      angle: h,
      isLocked: p,
      zIndex: d,
      strokeColor: u
    } = s, f = r && st(r) || o.get(e.name);
    if (!f) return;
    const m = e.parent;
    if (m) {
      if (u && (f.opt.strokeColor = u), H(d) && (e.setAttribute("zIndex", d), f.opt.zIndex = d), kt(p) && (f.opt.locked = p), a && c && l && n) {
        const { centerX: g, centerY: I, width: S, height: v, uniformScale: L } = f.opt, C = L !== !1 ? [l[0], l[0]] : l, N = [g, I], R = [g, I];
        Ht(R, c, C, n);
        const P = [
          R[0] - N[0],
          R[1] - N[1]
        ];
        if (f.centerPos = [
          f.centerPos[0] + P[0],
          f.centerPos[1] + P[1]
        ], f.opt.width = Math.round(S * C[0]), f.opt.height = Math.round(v * C[1]), f.opt.centerX = R[0], f.opt.centerY = R[1], f.opt.eraserlines)
          for (const M of f.opt.eraserlines) {
            const { op: D, thickness: B } = M;
            M.thickness = Math.round(B * Math.max(l[0], l[1]));
            for (let F = 0; F < D.length; F++)
              ee(D[F], c, l, n);
          }
      } else if (n) {
        if (f.opt.centerX = f.opt.centerX + n[0], f.opt.centerY = f.opt.centerY + n[1], f.centerPos = [
          f.centerPos[0] + n[0],
          f.centerPos[1] + n[1]
        ], f.opt.eraserlines)
          for (const g of f.opt.eraserlines) {
            const { op: I } = g;
            for (let S = 0; S < I.length; S++) {
              const v = I[S].map((L, C) => C % 2 ? L + n[1] : L + n[0]);
              g.op[S] = v;
            }
          }
      } else if (H(h))
        if (f.opt.rotate = h, f.scaleType = Dt.getScaleType(f.opt), r) {
          const g = se(f.rect, h);
          f.rect = g;
        } else {
          const g = b.getRectFromLayer(m, e.name);
          f.rect = g || f.rect;
        }
      return f && o.setInfo(e.name, f), f == null ? void 0 : f.rect;
    }
  }
}
const so = (T) => {
  if (T.tagName === "GROUP") {
    const i = Object.getOwnPropertySymbols(T).find(
      (t) => t.toString() === "Symbol(sealed)"
    );
    if (i && T[i])
      return !0;
  }
  return !1;
}, q = (T, i) => {
  if (T.mesh && T.mesh.texture && T.name === "eraserTexture") {
    const t = T.mesh.texture.image;
    i.deleteTexture(t);
  } else if (T.tagName === "GROUP" && T && T.children)
    for (const t of T.children)
      q(t, i);
}, Q = (T, i) => {
  q(T, i), T.removeAllChildren();
}, oo = (T, i, t) => {
  const e = t.parent;
  if (i) {
    const s = i.children;
    if (s) {
      for (const o of s)
        if (q(o, e), o.tagName === "GROUP")
          for (const r of o.children)
            q(r, e);
    }
  }
  i.parent.replaceChild(T, i);
}, io = (T, i, t) => {
  if (T.w + T.x <= 0 || T.h + T.y <= 0 || T.w <= 0 || T.h <= 0)
    return;
  const e = i.width, s = i.height, o = {
    x: Math.floor(Math.max(0, T.x)),
    y: Math.floor(Math.max(0, T.y)),
    w: Math.floor(Math.min(e, T.w)),
    h: Math.floor(Math.min(s, T.h))
  };
  if (o.x + o.w > e && (o.w = Math.floor(e - o.x)), o.y + o.h > s && (o.h = Math.floor(s - o.y)), as(t)) {
    const r = cs(t), n = Math.pow(10, r), a = ls(n, t * n), c = o.x % a, l = o.x - c;
    l >= 0 ? (o.x = l, o.w = o.w + c) : (o.x = 0, o.w = o.w + c - l);
    const h = o.y % a, p = o.y - h;
    p >= 0 ? (o.y = p, o.h = o.h + h) : (o.y = 0, o.h = o.h + h - p);
  }
  return o;
};
class ro {
  constructor(i) {
    y(this, "vNodes");
    y(this, "thread");
    y(this, "serviceWorkShapes", /* @__PURE__ */ new Map());
    y(this, "localWorkShapes", /* @__PURE__ */ new Map());
    y(this, "tmpOpt");
    y(this, "animationId");
    y(this, "syncUnitTime", J.syncOpt.interval);
    this.vNodes = i.vNodes, this.thread = i.thread;
  }
  createLocalWork(i) {
    const { workId: t, opt: e, toolsType: s } = i;
    if (t && e) {
      const o = t.toString();
      !this.getToolsOpt() && s && this.setToolsOpt({
        toolsType: s,
        toolsOpt: e
      }), this.setWorkOptions(o, e);
    }
  }
  getLocalWorkShape(i) {
    return this.localWorkShapes.get(i);
  }
  createLocalWorkShape(i, t) {
    if (i && this.tmpOpt) {
      const e = {
        toolsType: this.tmpOpt.toolsType,
        toolsOpt: t || this.tmpOpt.toolsOpt
      }, s = this.createWorkShapeNode({ ...e, workId: i });
      return s && this.localWorkShapes.set(i, {
        node: s,
        toolsType: s.toolsType,
        workState: A.Start
      }), s;
    }
  }
  canUseTopLayer(i) {
    return i === k.LaserPen;
  }
  destroy() {
    this.clearAll();
  }
  clearAll() {
    this.thread.topLayer.children.length && (this.thread.topLayer.parent.children.forEach((i) => {
      i.name !== "viewport" && i.remove();
    }), Q(
      this.thread.serviceLayer,
      this.thread.serviceLayer.parent
    )), this.serviceWorkShapes.clear(), this.localWorkShapes.clear();
  }
  consumeDraw(i) {
    const { workId: t, dataType: e } = i;
    if (e === x.Service)
      this.activeServiceWorkShape(i);
    else {
      const s = t == null ? void 0 : t.toString(), o = s && this.localWorkShapes.get(s);
      if (!o)
        return;
      const r = o.node.consume({
        data: i,
        isFullWork: !1,
        isSubWorker: !0
      });
      r.rect && (o.result = r, o.workState = A.Doing, s && this.localWorkShapes.set(s, o));
    }
    this.runAnimation();
  }
  setToolsOpt(i) {
    var t;
    this.tmpOpt = i, (t = i.toolsOpt) != null && t.syncUnitTime && (this.syncUnitTime = i.toolsOpt.syncUnitTime);
  }
  getToolsOpt() {
    return this.tmpOpt;
  }
  createWorkShapeNode(i) {
    const { toolsType: t } = i;
    if (t === k.LaserPen)
      return Lt({
        ...i,
        vNodes: this.vNodes,
        fullLayer: this.thread.topLayer,
        drawLayer: this.thread.topLayer
      });
  }
  setNodeKey(i, t, e, s) {
    return t.toolsType = e, t.node = this.createWorkShapeNode({
      workId: i,
      toolsType: e,
      toolsOpt: s
    }), t;
  }
  activeServiceWorkShape(i) {
    var p, d;
    const { workId: t, opt: e, toolsType: s, type: o, updateNodeOpt: r, ops: n, op: a } = i;
    if (!t)
      return;
    const c = t.toString(), l = (p = this.vNodes.get(c)) == null ? void 0 : p.rect;
    if (!((d = this.serviceWorkShapes) != null && d.has(c))) {
      let u = {
        toolsType: s,
        animationWorkData: a || [],
        animationIndex: 0,
        type: o,
        updateNodeOpt: r,
        ops: n,
        oldRect: l
      };
      s && e && (u = this.setNodeKey(c, u, s, e)), this.serviceWorkShapes.set(c, u);
    }
    const h = this.serviceWorkShapes.get(c);
    o && (h.type = o), n && (h.animationWorkData = yt(n), h.ops = n), r && (h.updateNodeOpt = r), a && (h.animationWorkData = a), h.node && h.node.getWorkId() !== c && h.node.setWorkId(c), l && (h.oldRect = l), s && e && (h.toolsType !== s && s && e && this.setNodeKey(c, h, s, e), h.node && h.node.setWorkOptions(e));
  }
  computNextAnimationIndex(i, t) {
    var o;
    const e = ((o = i.node) == null ? void 0 : o.syncUnitTime) || this.syncUnitTime, s = Math.floor(
      (i.animationWorkData || []).slice(i.animationIndex).length * 32 / t / e
    ) * t;
    return Math.min(
      (i.animationIndex || 0) + (s || t),
      (i.animationWorkData || []).length
    );
  }
  animationDraw() {
    var s, o, r, n;
    this.animationId = void 0;
    let i = !1;
    const t = /* @__PURE__ */ new Map(), e = [];
    for (const [a, c] of this.serviceWorkShapes.entries())
      switch (c.toolsType) {
        case k.LaserPen: {
          const h = this.computNextAnimationIndex(
            c,
            8
          ), p = Math.max(0, c.animationIndex || 0), d = (c.animationWorkData || []).slice(
            p,
            h
          );
          if ((c.animationIndex || 0) < h && ((s = c.node) == null || s.consumeService({
            op: d,
            isFullWork: !1
          }), c.animationIndex = h, d.length && t.set(a, {
            workState: p === 0 ? A.Start : h === ((o = c.animationWorkData) == null ? void 0 : o.length) ? A.Done : A.Doing,
            op: d.slice(-2)
          })), c.isDel) {
            (r = c.node) == null || r.clearTmpPoints(), this.serviceWorkShapes.delete(a);
            break;
          }
          c.ops && c.animationIndex === ((n = c.animationWorkData) == null ? void 0 : n.length) && !c.isDel && (this.thread.topLayer.getElementsByName(
            a.toString()
          )[0] || (c.isDel = !0, this.serviceWorkShapes.set(a, c))), i = !0;
          break;
        }
      }
    for (const [a, c] of this.localWorkShapes.entries()) {
      const { result: l, toolsType: h, isDel: p, workState: d } = c;
      switch (h) {
        case k.LaserPen: {
          if (p) {
            c.node.clearTmpPoints(), this.localWorkShapes.delete(a), e.push({
              removeIds: [a.toString()],
              type: w.RemoveNode
            });
            break;
          }
          l && ((l.op || l.ops) && e.push(l), c.result = void 0), !this.thread.topLayer.getElementsByName(
            a.toString()
          )[0] && d === A.Done && (c.isDel = !0, this.localWorkShapes.set(a, c)), i = !0;
          break;
        }
      }
    }
    i && this.runAnimation(), t.size && t.forEach((a, c) => {
      e.push({
        type: w.Cursor,
        uid: c.split(Wt)[0],
        op: a.op,
        workState: a.workState,
        viewId: this.thread.viewId
      });
    }), e.length && this.thread.post({ sp: e });
  }
  runAnimation() {
    this.animationId || (this.animationId = requestAnimationFrame(this.animationDraw.bind(this)));
  }
  setWorkOptions(i, t) {
    var s;
    let e = (s = this.localWorkShapes.get(i)) == null ? void 0 : s.node;
    if (!e && this.tmpOpt) {
      const { toolsType: o } = this.tmpOpt;
      this.tmpOpt.toolsOpt = t, e = this.createWorkShapeNode({ workId: i, toolsType: o, toolsOpt: t }), e && this.localWorkShapes.set(i, {
        node: e,
        toolsType: o,
        workState: A.Start
      }), this.setToolsOpt(this.tmpOpt);
    }
    t != null && t.syncUnitTime || (t.syncUnitTime = this.syncUnitTime), e && e.setWorkOptions(t);
  }
  consumeDrawAll(i) {
    const { workId: t, dataType: e } = i;
    if (e === x.Service)
      this.activeServiceWorkShape(i);
    else {
      const s = t == null ? void 0 : t.toString(), o = s && this.localWorkShapes.get(s);
      if (!o)
        return;
      const r = o.node.consumeAll({ data: i });
      o.result = r, o.workState = A.Done, s && this.localWorkShapes.set(s, o);
    }
    this.runAnimation();
  }
}
class no {
  constructor(i) {
    y(this, "vNodes");
    y(this, "thread");
    y(this, "workShapes", /* @__PURE__ */ new Map());
    y(this, "effectSelectNodeData", /* @__PURE__ */ new Set());
    y(this, "batchEraserRemoveNodes", /* @__PURE__ */ new Set());
    y(this, "batchEraserWorks", /* @__PURE__ */ new Set());
    y(this, "tmpOpt");
    y(this, "syncUnitTime", J.syncOpt.interval);
    y(this, "fullWorkerDrawCount", 0);
    y(this, "drawWorkActiveId");
    y(this, "consumeCount", 0);
    y(this, "combineTimerId");
    y(this, "combineDrawResolve");
    y(this, "combineDrawActiveId");
    this.vNodes = i.vNodes, this.thread = i.thread;
  }
  async loadImageBitMap(i) {
    return await this.thread.loadImageBitMap(i);
  }
  createLocalWork(i) {
    const { workId: t, opt: e, toolsType: s } = i;
    if (t && e) {
      const o = t.toString();
      !this.getToolsOpt() && s && this.setToolsOpt({
        toolsType: s,
        toolsOpt: e
      }), this.setWorkOptions(o, e);
    }
  }
  // async workShapesDone(scenePath: string, serviceWork: SubServiceThread) {
  //     for (const key of this.workShapes.keys()) {
  //         await this.consumeDrawAll({
  //             workId: key,
  //             scenePath,
  //             viewId: this.thread.viewId,
  //             msgType: EPostMessageType.DrawWork,
  //             dataType: EDataType.Local
  //         }, serviceWork)
  //     }
  // }
  async updateSelector(i) {
    var d;
    const t = this.workShapes.get(
      X
    );
    if (!((d = t == null ? void 0 : t.selectIds) != null && d.length)) return;
    const { callback: e, ...s } = i, { updateSelectorOpt: o, willSerializeData: r, smoothSync: n } = s, a = await (t == null ? void 0 : t.updateSelector({
      updateSelectorOpt: o,
      selectIds: be(t.selectIds),
      vNodes: this.vNodes,
      willSerializeData: r,
      worker: this
    })), c = /* @__PURE__ */ new Map();
    let l;
    a != null && a.selectIds && (l = ys(t.selectIds, a.selectIds), a.selectIds.forEach((u) => {
      const f = this.vNodes.get(u);
      if (f) {
        const { toolsType: m, op: g, opt: I } = f;
        c.set(u, {
          opt: I,
          toolsType: m,
          ops: (g == null ? void 0 : g.length) && tt(g) || void 0
        });
      }
    }), t.selectIds = a.selectIds);
    const h = [], p = e && e({
      res: a,
      workShapeNode: t,
      param: s,
      postData: { sp: h },
      newServiceStore: c,
      smoothSync: n
    }) || { sp: h };
    l && p.sp.push({
      type: w.RemoveNode,
      removeIds: l,
      viewId: this.thread.viewId
    }), p.sp.length && this.thread.post(p);
  }
  destroy() {
    this.clearAll();
  }
  clearAll() {
    if (this.thread.localLayer.children.length && (this.thread.topLayer.parent.children.forEach((t) => {
      t.name !== "viewport" && t.remove();
    }), Q(
      this.thread.localLayer,
      this.thread.localLayer.parent
    )), this.workShapes.get(
      X
    )) {
      const t = [];
      t.push({
        type: w.Select,
        dataType: x.Local,
        selectIds: [],
        willSyncService: !1
      }), this.thread.post({ sp: t });
    }
    this.workShapes.clear(), this.effectSelectNodeData.clear(), this.batchEraserWorks.clear(), this.batchEraserRemoveNodes.clear();
  }
  async checkTextActive(i) {
    const { op: t, viewId: e, dataType: s } = i;
    if (t != null && t.length) {
      let o;
      for (const r of this.vNodes.curNodeMap.values()) {
        const { rect: n, name: a, toolsType: c, opt: l } = r, h = t[0] * this.thread.fullLayer.worldScaling[0] + this.thread.fullLayer.worldPosition[0], p = t[1] * this.thread.fullLayer.worldScaling[1] + this.thread.fullLayer.worldPosition[1];
        if (c === k.Text && hs([h, p], n) && l.workState === A.Done) {
          o = a;
          break;
        }
      }
      o && (await this.blurSelector({
        viewId: e,
        msgType: w.Select,
        dataType: s,
        isSync: !0
      }), this.thread.post({
        sp: [
          {
            type: w.GetTextActive,
            toolsType: k.Text,
            workId: o
          }
        ]
      }));
    }
  }
  cursorHover(i) {
    const { opt: t, toolsType: e, point: s } = i, o = this.setFullWork({
      workId: bt,
      toolsType: e,
      opt: t
    });
    o && s && o.cursorHover(s);
  }
  cursorBlur() {
    var t;
    const i = this.getWorkShape(bt);
    i && ((t = i.selectIds) != null && t.length) && (i.cursorBlur(), this.clearWorkShapeNodeCache(bt)), this.thread.fullLayer.parent.children.forEach((e) => {
      e.name === "Cursor_Hover_Id" && e.remove();
    });
  }
  updateFullSelectWork(i) {
    var s, o, r, n, a;
    const t = this.workShapes.get(
      X
    ), { selectIds: e } = i;
    if (!(e != null && e.length)) {
      this.blurSelector(i);
      return;
    }
    if (!t) {
      const c = this.setFullWork(i);
      !c && i.workId && this.tmpOpt && ((s = this.tmpOpt) == null ? void 0 : s.toolsType) === k.Selector && this.setWorkOptions(
        i.workId.toString(),
        i.opt || this.tmpOpt.toolsOpt
      ), c && this.updateFullSelectWork(i);
      return;
    }
    if (t && (e != null && e.length)) {
      const { selectRect: c } = t.updateSelectIds(e), l = [
        {
          ...i,
          selectorColor: ((o = i.opt) == null ? void 0 : o.strokeColor) || t.selectorColor,
          strokeColor: ((r = i.opt) == null ? void 0 : r.strokeColor) || t.strokeColor,
          fillColor: ((n = i.opt) == null ? void 0 : n.fillColor) || t.fillColor,
          textOpt: ((a = i.opt) == null ? void 0 : a.textOpt) || t.textOpt,
          canTextEdit: t.canTextEdit,
          canRotate: t.canRotate,
          scaleType: t.scaleType,
          type: w.Select,
          selectRect: c,
          points: t.getChildrenPoints(),
          willSyncService: (i == null ? void 0 : i.willSyncService) || !1,
          opt: (i == null ? void 0 : i.willSyncService) && t.getWorkOptions() || void 0,
          canLock: t.canLock,
          isLocked: t.isLocked,
          toolsTypes: t.toolsTypes,
          shapeOpt: t.shapeOpt,
          thickness: t.thickness,
          useStroke: t.useStroke,
          strokeType: t.strokeType
        }
      ];
      this.thread.post({ sp: l });
    }
  }
  async removeSelector(i) {
    const { willSyncService: t, needUndoTicker: e } = i, s = [], o = [], r = this.workShapes.get(
      X
    );
    if (!r)
      return;
    const n = r.selectIds && [...r.selectIds] || [];
    for (const a of n) {
      const c = this.vNodes.get(a);
      if (c)
        switch (c.toolsType) {
          case k.Text: {
            s.push({
              type: w.TextUpdate,
              toolsType: k.Text,
              workId: a,
              dataType: x.Local
            });
            break;
          }
          case k.BackgroundSVG:
            s.push({
              type: w.BackgroundSVGDelete,
              toolsType: k.BackgroundSVG,
              workId: a,
              dataType: x.Local,
              viewId: this.thread.viewId
            });
            break;
        }
      this.removeNode(a), o.push(a);
    }
    o.length && s.push({
      type: w.RemoveNode,
      removeIds: o
    }), s.push({
      type: w.Select,
      selectIds: [],
      willSyncService: t
    }), await this.blurSelector(), e && s.push({
      type: w.None,
      needUndoTicker: e
    }), s.length && this.thread.post({ sp: s });
  }
  removeWork(i) {
    const { workId: t } = i, e = t == null ? void 0 : t.toString();
    e && this.removeNode(e);
  }
  removeNode(i) {
    var s;
    const t = this.vNodes.get(i);
    t && (t.toolsType === k.BackgroundSVG && this.thread.post({
      sp: [
        {
          type: w.BackgroundSVGDelete,
          toolsType: k.BackgroundSVG,
          workId: i,
          dataType: x.Local,
          viewId: this.thread.viewId
        }
      ]
    }), (s = this.thread.fullLayer) == null || s.getElementsByName(i).forEach((o) => {
      o.remove(), q(o, this.thread.fullLayer.parent);
    }), this.vNodes.delete(i)), this.workShapes.has(i) && (this.thread.localLayer.getElementsByName(i).forEach((o) => {
      o.remove(), q(o, this.thread.localLayer.parent);
    }), this.clearWorkShapeNodeCache(i));
  }
  setFullWork(i) {
    const { workId: t, opt: e, toolsType: s } = i;
    if (t && e && s) {
      const o = t.toString();
      let r;
      return t && this.workShapes.has(o) ? (r = this.workShapes.get(o), r == null || r.setWorkOptions(e)) : r = this.createWorkShapeNode({
        toolsOpt: e,
        toolsType: s,
        workId: o
      }), r ? (this.workShapes.set(o, r), r) : void 0;
    }
  }
  async consumeFull(i) {
    var s;
    const t = this.setFullWork(i), e = i.ops && yt(i.ops);
    if (t) {
      const o = (s = i.workId) == null ? void 0 : s.toString();
      t.toolsType === k.BackgroundSVG ? t.consumeService({
        isFullWork: !0,
        replaceId: o
      }) : t.toolsType === k.Image ? await t.consumeServiceAsync({
        isFullWork: !0,
        replaceId: o,
        worker: this
      }) : t.toolsType === k.Text ? await t.consumeServiceAsync({
        isFullWork: !0,
        replaceId: o,
        boxRect: this.thread.getSceneRect()
      }) : t.consumeService({
        op: e,
        isFullWork: !0,
        replaceId: o
      }), i != null && i.updateNodeOpt && t.updataOptService(i.updateNodeOpt);
      const r = [];
      i.workId && this.workShapes.delete(i.workId.toString()), i.willSyncService && r.push({
        opt: i.opt,
        toolsType: i.toolsType,
        type: w.FullWork,
        workId: i.workId,
        ops: i.ops,
        updateNodeOpt: i.updateNodeOpt,
        viewId: this.thread.viewId
      }), i.needUndoTicker && r.push({
        type: w.None,
        needUndoTicker: i.needUndoTicker
      }), r.length && this.thread.post({ sp: r });
    }
  }
  async colloctEffectSelectWork(i) {
    const t = this.workShapes.get(
      X
    ), { workId: e, msgType: s } = i;
    if (t && e && t.selectIds && t.selectIds.includes(e.toString())) {
      s === w.RemoveNode ? t.selectIds = t.selectIds.filter(
        (o) => o !== e.toString()
      ) : this.effectSelectNodeData.add(i), await new Promise((o) => {
        setTimeout(() => {
          o(!0);
        }, 0);
      }), await this.runEffectSelectWork(!0).then(() => {
        var o;
        (o = this.effectSelectNodeData) == null || o.clear();
      });
      return;
    }
    return i;
  }
  async runEffectSelectWork(i) {
    var t;
    for (const e of this.effectSelectNodeData.values()) {
      const s = this.setFullWork(e);
      if (s) {
        const o = (t = e.workId) == null ? void 0 : t.toString();
        if (s.toolsType === k.BackgroundSVG)
          s.consumeService({
            isFullWork: !0,
            replaceId: o
          });
        else if (s.toolsType === k.Image)
          await s.consumeServiceAsync({
            isFullWork: !0,
            replaceId: o,
            worker: this
          });
        else if (s.toolsType === k.Text)
          await s.consumeServiceAsync({
            isFullWork: !0,
            replaceId: o,
            boxRect: this.thread.getSceneRect()
          });
        else {
          const r = e.ops && yt(e.ops);
          s.consumeService({
            op: r,
            isFullWork: !0,
            replaceId: o
          }), e != null && e.updateNodeOpt && s.updataOptService(e.updateNodeOpt);
        }
        e.workId && this.workShapes.delete(e.workId.toString());
      }
    }
    this.reRenderSelector(i);
  }
  hasSelector() {
    return this.workShapes.has(X);
  }
  getSelector() {
    return this.workShapes.get(X);
  }
  reRenderSelector(i = !1) {
    var s;
    const t = this.workShapes.get(
      X
    );
    if (!t) return;
    if (t && !((s = t.selectIds) != null && s.length))
      return this.blurSelector();
    const e = t.reRenderSelector();
    e && this.thread.post({
      sp: [
        {
          type: w.Select,
          selectIds: t.selectIds,
          selectRect: e,
          willSyncService: i,
          viewId: this.thread.viewId,
          points: t.getChildrenPoints(),
          textOpt: t.textOpt,
          selectorColor: t.selectorColor,
          strokeColor: t.strokeColor,
          fillColor: t.fillColor,
          canTextEdit: t.canTextEdit,
          canRotate: t.canRotate,
          scaleType: t.scaleType,
          opt: t.getWorkOptions() || void 0,
          canLock: t.canLock,
          isLocked: t.isLocked,
          toolsTypes: t.toolsTypes,
          shapeOpt: t.shapeOpt,
          thickness: t.thickness,
          useStroke: t.useStroke,
          strokeType: t.strokeType
        }
      ]
    });
  }
  async blurSelector(i) {
    var s;
    const t = this.workShapes.get(
      X
    ), e = t == null ? void 0 : t.blurSelector();
    if (this.clearWorkShapeNodeCache(X), ((s = this.thread.fullLayer) == null ? void 0 : s.parent).children.forEach((o) => {
      o.name === X && o.remove();
    }), e) {
      const o = [];
      o.push({
        ...e,
        isSync: i == null ? void 0 : i.isSync
      }), this.thread.post({ sp: o });
    }
  }
  clearWorkShapeNodeCache(i) {
    var t;
    (t = this.getWorkShape(i)) == null || t.clearTmpPoints(), this.workShapes.delete(i);
  }
  async drawBitMapEraserFull(i, t, e, s) {
    const { willUpdateNodes: o, willDeleteNodes: r } = t, n = i.getWorkId(), a = [
      {
        type: w.RemoveNode,
        removeIds: [n],
        viewId: this.thread.viewId
      }
    ];
    if (e && a.push({
      type: w.None,
      isLockSentEventCursor: e
    }), o != null && o.size || r != null && r.size) {
      if (o != null && o.size)
        for (const [c, l] of o)
          a.push({
            type: w.UpdateNode,
            dataType: x.Local,
            opt: l.opt,
            workId: c,
            updateNodeOpt: {
              useAnimation: !1
            }
          });
      r != null && r.size && a.push({
        type: w.RemoveNode,
        removeIds: [...r],
        viewId: this.thread.viewId
      });
    }
    s && a.push({
      type: w.None,
      needUndoTicker: s
    }), a.length && this.thread.post({ sp: a });
  }
  drawPencilEraserFull(i, t, e, s) {
    const { willNewNodes: o, willDeleteNodes: r } = t, n = i.getWorkId(), a = [
      {
        type: w.RemoveNode,
        removeIds: [n],
        viewId: this.thread.viewId
      }
    ];
    if (e && a.push({
      type: w.None,
      isLockSentEventCursor: e
    }), o != null && o.size || r != null && r.size) {
      if (o != null && o.size)
        for (const [c, l] of o)
          a.push({
            type: w.FullWork,
            dataType: x.Local,
            toolsType: l.toolsType,
            ops: tt(l.op),
            opt: l.opt,
            workId: c,
            updateNodeOpt: {
              useAnimation: !1
            }
          });
      r != null && r.size && a.push({
        type: w.RemoveNode,
        removeIds: [...r],
        viewId: this.thread.viewId
      });
    }
    s && a.push({
      type: w.None,
      needUndoTicker: s
    }), a.length && this.thread.post({ sp: a });
  }
  drawEraser(i, t, e) {
    const s = [];
    i.removeIds && s.push(i), t && s.push({
      type: w.None,
      isLockSentEventCursor: t
    }), e && s.push({
      type: w.None,
      needUndoTicker: e
    }), this.thread.post({ sp: s, consumeCount: this.consumeCount });
  }
  getWorkShape(i) {
    return this.workShapes.get(i);
  }
  getWorkShapes() {
    return this.workShapes;
  }
  consumeDraw(i, t) {
    const { op: e, workId: s, scenePath: o, postCount: r, smoothSync: n } = i;
    if (e != null && e.length && s) {
      const a = s.toString(), c = this.workShapes.get(a);
      if (!c)
        return;
      const l = c.toolsType;
      if (l === k.LaserPen)
        return;
      switch (this.combineDrawActiveId && this.combineDrawActiveId !== a && (this.combineTimerId && (clearTimeout(this.combineTimerId), this.combineTimerId = void 0, this.combineDrawResolve && this.combineDrawResolve(!1), this.combineDrawActiveId = void 0), this.consumeDrawAll(
        {
          workId: this.combineDrawActiveId,
          scenePath: o,
          viewId: this.thread.viewId,
          msgType: w.DrawWork,
          dataType: x.Local
        },
        t
      )), this.drawWorkActiveId && this.drawWorkActiveId !== a && (this.consumeDrawAll(
        {
          workId: this.drawWorkActiveId,
          scenePath: o,
          viewId: this.thread.viewId,
          msgType: w.DrawWork,
          dataType: x.Local
        },
        t
      ), this.drawWorkActiveId = void 0), !this.drawWorkActiveId && a !== X && (this.drawWorkActiveId = a), zt(r) && (this.consumeCount = r), l) {
        case k.Selector: {
          const h = c.consume({
            data: i,
            isFullWork: !0,
            isMainThread: !0
          });
          this.fullWorkerDrawCount++;
          const p = [];
          h.type === w.Select && (h.selectIds && t.runReverseSelectWork(h.selectIds), p.push(h)), this.thread.post({
            consumeCount: this.consumeCount,
            fullWorkerDrawCount: this.fullWorkerDrawCount,
            sp: p
          });
          break;
        }
        case k.PencilEraser:
        case k.BitMapEraser: {
          c.consume({
            data: i,
            isFullWork: !1,
            isMainThread: !0
          }), this.fullWorkerDrawCount++, this.thread.post({
            sp: void 0,
            consumeCount: this.consumeCount,
            fullWorkerDrawCount: this.fullWorkerDrawCount
          }), this.combineTimerId || new Promise((h) => {
            this.combineDrawActiveId = a, this.combineDrawResolve = h, this.combineTimerId = ps(
              () => {
                this.combineTimerId = void 0, this.combineDrawResolve && this.combineDrawResolve(!0);
              },
              this.thread.master.maxCombineEraserTime,
              this.thread.master.control.hasPolyfillMethod(
                "requestIdleCallback"
              )
            );
          }).then((h) => {
            h && this.drawEraserCombine(a), this.combineDrawResolve = void 0;
          });
          break;
        }
        case k.Eraser:
          {
            const h = c.consume({
              data: i,
              isFullWork: !0
            });
            this.drawEraser(h);
          }
          break;
        case k.Arrow:
        case k.Straight:
        case k.Ellipse:
        case k.Rectangle:
        case k.Star:
        case k.Polygon:
        case k.SpeechBalloon:
        case k.Pencil:
          {
            const h = c.consume({
              data: i,
              isFullWork: !1,
              isMainThread: !0,
              smoothSync: n
            });
            h && (this.fullWorkerDrawCount++, this.thread.post({
              consumeCount: this.consumeCount,
              fullWorkerDrawCount: this.fullWorkerDrawCount,
              sp: h.op && [{ ...h, scenePath: o }] || void 0
            }));
          }
          break;
      }
    }
  }
  drawEraserCombine(i) {
    var e, s, o, r;
    const t = (e = this.workShapes.get(i)) == null ? void 0 : e.combineConsume({ workerEngine: this });
    if (t) {
      const { willDeleteNodes: n, willNewNodes: a } = t, c = {
        render: [],
        sp: []
      };
      if (t != null && t.rect) {
        const l = ut(t.rect);
        (s = c.render) == null || s.push(
          {
            rect: l,
            isClear: !0,
            clearCanvas: _.Bg,
            viewId: this.thread.viewId
          },
          {
            rect: l,
            drawCanvas: _.Bg,
            viewId: this.thread.viewId
          }
        );
      }
      if (n != null && n.size && ((o = c.sp) == null || o.push({
        type: w.RemoveNode,
        removeIds: [...n],
        viewId: this.thread.viewId
      })), a != null && a.size)
        for (const [l, h] of a)
          (r = c.sp) == null || r.push({
            type: w.FullWork,
            dataType: x.Local,
            toolsType: h.toolsType,
            ops: tt(h.op),
            opt: h.opt,
            workId: l,
            updateNodeOpt: {
              useAnimation: !1
            }
          });
      this.thread.post(c);
    }
  }
  consumeDrawAll(i, t) {
    var r, n, a;
    const { workId: e, scenePath: s, isLockSentEventCursor: o } = i;
    if (e) {
      this.combineTimerId && (clearTimeout(this.combineTimerId), this.combineTimerId = void 0, this.combineDrawResolve && this.combineDrawResolve(!1), this.combineDrawActiveId = void 0);
      const c = e.toString();
      this.drawWorkActiveId === c && (this.drawWorkActiveId = void 0);
      const l = this.workShapes.get(c);
      if (!l)
        return;
      const h = l.toolsType;
      if (h === k.LaserPen)
        return;
      const p = this.workShapes.get(bt), d = (r = p == null ? void 0 : p.selectIds) == null ? void 0 : r[0], u = l.consumeAll({ data: i, workerEngine: this });
      switch (h) {
        case k.Selector:
          {
            u.selectIds && d && ((n = u.selectIds) != null && n.includes(d)) && p.cursorBlur();
            const f = [];
            o && f.push({
              type: w.None,
              isLockSentEventCursor: o
            }), u.type === w.Select && (u.selectIds && t.runReverseSelectWork(u.selectIds), f.push({ ...u, scenePath: s })), f.length && this.thread.post({ sp: f }), (a = l.selectIds) != null && a.length ? l.clearTmpPoints() : this.clearWorkShapeNodeCache(c);
          }
          break;
        case k.PencilEraser:
          this.drawPencilEraserFull(
            l,
            u,
            o,
            i.needUndoTicker
          ), this.fullWorkerDrawCount = 0, this.clearWorkShapeNodeCache(c);
          break;
        case k.BitMapEraser:
          this.drawBitMapEraserFull(
            l,
            u,
            o,
            i.needUndoTicker
          ), this.fullWorkerDrawCount = 0, this.clearWorkShapeNodeCache(c);
          break;
        case k.Eraser:
          this.drawEraser(
            { ...u, scenePath: s },
            o,
            i.needUndoTicker
          ), l.clearTmpPoints();
          break;
        case k.Arrow:
        case k.Straight:
        case k.Ellipse:
        case k.Rectangle:
        case k.Star:
        case k.Polygon:
        case k.SpeechBalloon:
        case k.Pencil: {
          const f = [];
          o && f.push({
            type: w.None,
            isLockSentEventCursor: o
          }), u && (f.push(u), this.fullWorkerDrawCount = 0, i.needUndoTicker && f.push({
            type: w.None,
            needUndoTicker: i.needUndoTicker
          }), this.thread.post({
            fullWorkerDrawCount: this.fullWorkerDrawCount,
            sp: f
          })), this.clearWorkShapeNodeCache(c);
          break;
        }
      }
    }
  }
  getToolsOpt() {
    return this.tmpOpt;
  }
  setToolsOpt(i) {
    var t;
    this.tmpOpt = i, (t = i.toolsOpt) != null && t.syncUnitTime && (this.syncUnitTime = i.toolsOpt.syncUnitTime);
  }
  setWorkOptions(i, t) {
    let e = this.workShapes.get(i);
    if (!e && this.tmpOpt) {
      const { toolsType: s } = this.tmpOpt;
      this.tmpOpt.toolsOpt = t, e = this.createWorkShapeNode({ workId: i, toolsType: s, toolsOpt: t }), e && this.workShapes.set(i, e), this.setToolsOpt(this.tmpOpt);
    }
    t.syncUnitTime || (t.syncUnitTime = this.syncUnitTime), e == null || e.setWorkOptions(t);
  }
  createWorkShapeNode(i) {
    return Lt(
      {
        ...i,
        vNodes: this.vNodes,
        fullLayer: this.thread.fullLayer,
        drawLayer: this.thread.localLayer
      },
      this.thread.serviceWork
    );
  }
}
class ao {
  constructor(i) {
    y(this, "vNodes");
    y(this, "thread");
    y(this, "workShapes", /* @__PURE__ */ new Map());
    y(this, "selectorWorkShapes", /* @__PURE__ */ new Map());
    y(this, "willRunEffectSelectorIds", /* @__PURE__ */ new Set());
    y(this, "runEffectId");
    y(this, "animationId");
    y(this, "syncUnitTime", J.syncOpt.interval);
    this.vNodes = i.vNodes, this.thread = i.thread;
  }
  async loadImageBitMap(i) {
    return await this.thread.loadImageBitMap(i);
  }
  destroy() {
    this.clearAll();
  }
  clearAll() {
    this.thread.serviceLayer.children.length && (this.thread.serviceLayer.parent.children.forEach((i) => {
      i.name !== "viewport" && i.remove();
    }), Q(
      this.thread.serviceLayer,
      this.thread.serviceLayer.parent
    )), this.workShapes.clear(), this.selectorWorkShapes.clear(), this.willRunEffectSelectorIds.clear();
  }
  runEffect() {
    this.runEffectId || (this.runEffectId = setTimeout(
      this.effectRunSelector.bind(this),
      0
    ));
  }
  effectRunSelector() {
    this.runEffectId = void 0, this.willRunEffectSelectorIds.forEach((i) => {
      var e, s;
      const t = this.selectorWorkShapes.get(i);
      t && t.selectIds && ((e = t.node) == null || e.selectServiceNode(
        i,
        t,
        !0
      )), (s = t == null ? void 0 : t.selectIds) != null && s.length || this.selectorWorkShapes.delete(i);
    }), this.willRunEffectSelectorIds.clear();
  }
  runSelectWork(i) {
    this.activeSelectorShape(i);
    const { workId: t } = i, e = t == null ? void 0 : t.toString();
    e && this.willRunEffectSelectorIds.add(e), this.runEffect();
  }
  removeWork(i) {
    const { workId: t } = i, e = t == null ? void 0 : t.toString();
    if (e) {
      if (this.workShapes.get(e)) {
        this.workShapes.delete(e), this.removeNode(e, i);
        return;
      }
      this.removeNode(e, i);
    }
  }
  consumeFull(i) {
    this.activeWorkShape(i), this.runAnimation();
  }
  runReverseSelectWork(i) {
    i.forEach((t) => {
      this.selectorWorkShapes.forEach((e, s) => {
        var o;
        if ((o = e.selectIds) != null && o.length) {
          const r = e.selectIds.indexOf(t);
          r > -1 && (e.selectIds.splice(r, 1), this.willRunEffectSelectorIds.add(s));
        }
      });
    }), this.willRunEffectSelectorIds.size && this.runEffect();
  }
  consumeDraw(i) {
    this.activeWorkShape(i), this.runAnimation();
  }
  computNextAnimationIndex(i, t) {
    const e = Math.floor(
      (i.animationWorkData || []).slice(i.animationIndex).length * 32 / t / this.syncUnitTime
    ) * t;
    return Math.min(
      (i.animationIndex || 0) + (e || t),
      (i.animationWorkData || []).length
    );
  }
  async animationDraw() {
    var e, s, o, r, n, a, c, l, h, p, d, u, f, m, g, I, S, v, L, C, N;
    this.animationId = void 0;
    let i = !1;
    const t = /* @__PURE__ */ new Map();
    for (const [R, P] of this.workShapes.entries())
      switch (P.toolsType) {
        case k.BackgroundSVG: {
          (e = P.node) == null || e.consumeService({
            isFullWork: !0,
            replaceId: R
          }), this.selectorWorkShapes.forEach((M, D) => {
            var B;
            (B = M.selectIds) != null && B.includes(R) && (this.willRunEffectSelectorIds.add(D), this.runEffect());
          }), this.workShapes.delete(R);
          break;
        }
        case k.Image: {
          await ((s = P.node) == null ? void 0 : s.consumeServiceAsync({
            isFullWork: !0,
            worker: this
          })), this.selectorWorkShapes.forEach((M, D) => {
            var B;
            (B = M.selectIds) != null && B.includes(R) && (this.willRunEffectSelectorIds.add(D), this.runEffect());
          }), this.workShapes.delete(R);
          break;
        }
        case k.Text: {
          P.node && (await ((o = P.node) == null ? void 0 : o.consumeServiceAsync({
            isFullWork: !0,
            replaceId: R,
            boxRect: this.thread.getSceneRect()
          })), this.selectorWorkShapes.forEach((M, D) => {
            var B;
            (B = M.selectIds) != null && B.includes(R) && (this.willRunEffectSelectorIds.add(D), this.runEffect());
          }), (r = P.node) == null || r.clearTmpPoints(), this.workShapes.delete(R));
          break;
        }
        case k.Arrow:
        case k.Straight:
        case k.Rectangle:
        case k.Ellipse:
        case k.Star:
        case k.Polygon:
        case k.SpeechBalloon: {
          const M = !!P.ops;
          if ((n = P.animationWorkData) != null && n.length) {
            const D = P.oldRect;
            (a = P.node) == null || a.consumeService({
              op: P.animationWorkData,
              isFullWork: M
            }), M && (this.selectorWorkShapes.forEach((B, F) => {
              var U;
              (U = B.selectIds) != null && U.includes(R) && (this.willRunEffectSelectorIds.add(F), this.runEffect());
            }), (c = P.node) == null || c.clearTmpPoints(), this.workShapes.delete(R)), P.isEnableCursor ? t.set(R, {
              workState: D ? P.ops ? A.Done : A.Doing : A.Start,
              op: P.animationWorkData.slice(-3, -1)
            }) : M && !P.useAnimation && ((l = P.updateNodeOpt) != null && l.useAnimation) && t.set(R, {
              workState: A.Done,
              op: P.animationWorkData.slice(-3, -1),
              uid: (h = P.updateNodeOpt) == null ? void 0 : h.uid
            }), P.animationWorkData.length = 0;
          }
          break;
        }
        case k.Pencil: {
          if (P.useAnimation) {
            if (P.useAnimation) {
              if (P.isDel) {
                (I = P.node) == null || I.clearTmpPoints(), this.workShapes.delete(R);
                break;
              }
              const M = 3, D = this.computNextAnimationIndex(
                P,
                M
              ), B = P.isDiff ? 0 : Math.max(0, (P.animationIndex || 0) - M), F = (P.animationWorkData || []).slice(
                B,
                D
              ), U = (v = (S = P.node) == null ? void 0 : S.getWorkId()) == null ? void 0 : v.toString();
              if ((P.animationIndex || 0) < D || P.isDiff) {
                if ((L = P.node) == null || L.consumeService({
                  op: F,
                  isFullWork: !1
                }), P.animationIndex = D, P.isDiff && (P.isDiff = !1), F.length && P.isEnableCursor) {
                  const V = F.slice(-3, -1);
                  t.set(R, {
                    workState: B === 0 ? A.Start : D === ((C = P.animationWorkData) == null ? void 0 : C.length) ? A.Done : A.Doing,
                    op: V
                  });
                }
              } else P.ops && ((N = P.node) == null || N.consumeService({
                op: P.animationWorkData || [],
                isFullWork: !0,
                replaceId: U
              }), P.isDel = !0, P.isEnableCursor && t.set(R, {
                workState: A.Done,
                op: F.slice(-3, -1)
              }));
              i = !0;
              break;
            }
          } else {
            const M = !!P.ops;
            if ((p = P.node) == null || p.consumeService({
              op: P.animationWorkData || [],
              isFullWork: M,
              replaceId: R
            }), (d = P.node) == null || d.updataOptService(P.updateNodeOpt), M) {
              if (!P.isEnableCursor && ((u = P.updateNodeOpt) != null && u.useAnimation) && ((f = P.animationWorkData) != null && f.length)) {
                const D = P.animationWorkData.slice(-3, -1);
                t.set(R, {
                  workState: A.Done,
                  op: D,
                  uid: (m = P.updateNodeOpt) == null ? void 0 : m.uid
                });
              }
              this.selectorWorkShapes.forEach((D, B) => {
                var F;
                (F = D.selectIds) != null && F.includes(R) && (this.willRunEffectSelectorIds.add(B), this.runEffect());
              }), (g = P.node) == null || g.clearTmpPoints(), this.workShapes.delete(R);
            }
          }
          break;
        }
      }
    if (i && this.runAnimation(), t.size) {
      const R = [];
      t.forEach((P, M) => {
        R.push({
          type: w.Cursor,
          uid: P.uid || M.split(Wt)[0],
          op: P.op,
          workState: P.workState,
          viewId: this.thread.viewId
        });
      }), this.thread.post({ sp: R });
    }
  }
  runAnimation() {
    this.animationId || (this.animationId = requestAnimationFrame(this.animationDraw.bind(this)));
  }
  hasDiffData(i, t, e) {
    const s = i.length;
    if (t.length < s)
      return !0;
    switch (e) {
      case k.Pencil: {
        for (let o = 0; o < s; o += 3)
          if (t[o] !== i[o] || t[o + 1] !== i[o + 1])
            return !0;
        break;
      }
      case k.LaserPen: {
        for (let o = 0; o < s; o += 2)
          if (t[o] !== i[o] || t[o + 1] !== i[o + 1])
            return !0;
        break;
      }
    }
    return !1;
  }
  activeWorkShape(i) {
    var f, m, g, I;
    const {
      workId: t,
      opt: e,
      toolsType: s,
      type: o,
      updateNodeOpt: r,
      ops: n,
      op: a,
      useAnimation: c,
      imageBitmap: l,
      isEnableCursor: h
    } = i;
    if (!t)
      return;
    const p = t.toString(), d = (f = this.vNodes.get(p)) == null ? void 0 : f.rect;
    if (!((m = this.workShapes) != null && m.has(p))) {
      let S = {
        toolsType: s,
        animationWorkData: a || [],
        animationIndex: 0,
        type: o,
        updateNodeOpt: r,
        ops: n,
        useAnimation: typeof c < "u" ? c : typeof (r == null ? void 0 : r.useAnimation) < "u" ? r == null ? void 0 : r.useAnimation : !0,
        oldRect: d,
        isDiff: !1,
        imageBitmap: l,
        isEnableCursor: h
      };
      s && e && (S = this.setNodeKey(p, S, s, e)), (g = this.workShapes) == null || g.set(p, S);
    }
    const u = (I = this.workShapes) == null ? void 0 : I.get(p);
    u.isEnableCursor = h, o && (u.type = o), n && (u.animationWorkData = yt(n), u.ops = n), r && (u.updateNodeOpt = r), a && (u.isDiff = this.hasDiffData(
      u.animationWorkData || [],
      a,
      u.toolsType
    ), u.animationWorkData = a), u.node && u.node.getWorkId() !== p && u.node.setWorkId(p), d && (u.oldRect = d), s && e && (e.syncUnitTime && (this.syncUnitTime = e.syncUnitTime), u.toolsType !== s && s && e && this.setNodeKey(p, u, s, e), u.node && u.node.setWorkOptions(e)), l && (u.imageBitmap = l);
  }
  removeNode(i, t) {
    i.indexOf(X) > -1 && this.removeSelectWork(t), this.thread.fullLayer.getElementsByName(i).forEach((s) => {
      s.remove(), q(s, this.thread.fullLayer.parent);
    }), this.thread.serviceLayer.getElementsByName(i).forEach((s) => {
      s.remove(), q(s, this.thread.serviceLayer.parent);
    });
    const e = this.vNodes.get(i);
    e && e.toolsType === k.BackgroundSVG && this.thread.post({
      sp: [
        {
          type: w.BackgroundSVGDelete,
          toolsType: k.BackgroundSVG,
          workId: i,
          dataType: x.Service,
          viewId: this.thread.viewId
        }
      ]
    }), this.vNodes.delete(i);
  }
  removeSelectWork(i) {
    const { workId: t } = i, e = t == null ? void 0 : t.toString();
    e && (this.activeSelectorShape(i), this.willRunEffectSelectorIds.add(e)), this.runEffect();
  }
  activeSelectorShape(i) {
    var c, l, h;
    const { workId: t, opt: e, toolsType: s, type: o, selectIds: r } = i;
    if (!t)
      return;
    const n = t.toString();
    if (!((c = this.selectorWorkShapes) != null && c.has(n))) {
      let p = {
        toolsType: s,
        selectIds: r,
        type: o,
        opt: e
      };
      s && e && (p = this.setNodeKey(n, p, s, e)), (l = this.selectorWorkShapes) == null || l.set(n, p);
    }
    const a = (h = this.selectorWorkShapes) == null ? void 0 : h.get(n);
    o && (a.type = o), a.node && a.node.getWorkId() !== n && a.node.setWorkId(n), a.selectIds = r || [];
  }
  setNodeKey(i, t, e, s) {
    return t.toolsType = e, t.node = Lt(
      {
        toolsType: e,
        toolsOpt: s,
        vNodes: this.vNodes,
        fullLayer: this.thread.fullLayer,
        drawLayer: this.thread.serviceLayer,
        workId: i
      },
      this
    ), t;
  }
}
class rt {
  constructor() {
    y(this, "localWork");
    y(this, "serviceWork");
    y(this, "threadEngine");
  }
  registerMainThread(i) {
    return this.threadEngine = i, this.localWork = i.localWork, this.serviceWork = i.serviceWork, this;
  }
}
class co extends rt {
  constructor() {
    super(...arguments);
    y(this, "emitEventType", Y.CopyNode);
  }
  async consume(t) {
    const { msgType: e, dataType: s, emitEventType: o } = t;
    if (e === w.FullWork && s === x.Local && o === this.emitEventType)
      return this.consumeForLocalWorker(t), !0;
  }
  async consumeForLocalWorker(t) {
    var s;
    const { workId: e } = t;
    e && await ((s = this.localWork) == null ? void 0 : s.consumeFull(t));
  }
}
class lo extends rt {
  constructor() {
    super(...arguments);
    y(this, "emitEventType", Y.SetColorNode);
  }
  async consume(t) {
    const { msgType: e, dataType: s, emitEventType: o } = t;
    if (e === w.UpdateNode && s === x.Local && o === this.emitEventType)
      return this.consumeForLocalWorker(t), !0;
  }
  async consumeForLocalWorker(t) {
    var l;
    const {
      workId: e,
      updateNodeOpt: s,
      willRefreshSelector: o,
      willSyncService: r,
      willSerializeData: n,
      textUpdateForWoker: a,
      needUndoTicker: c
    } = t;
    e === X && s && await ((l = this.localWork) == null ? void 0 : l.updateSelector({
      updateSelectorOpt: s,
      willRefreshSelector: o,
      willSyncService: r,
      willSerializeData: n,
      textUpdateForWoker: a,
      callback: this.updateSelectorCallback,
      needUndoTicker: c
    }));
  }
  updateSelectorCallback(t) {
    const { param: e, postData: s, newServiceStore: o } = t, { willSyncService: r, isSync: n, textUpdateForWoker: a, needUndoTicker: c } = e, l = s.sp || [];
    if (r)
      for (const [h, p] of o.entries())
        a && p.toolsType === k.Text ? l.push({
          ...p,
          workId: h,
          type: w.TextUpdate,
          dataType: x.Local,
          willSyncService: !0
        }) : l.push({
          ...p,
          workId: h,
          type: w.UpdateNode,
          updateNodeOpt: {
            useAnimation: !1
          },
          isSync: n
        });
    return c && l.push({
      type: w.None,
      needUndoTicker: c
    }), {
      sp: l
    };
  }
}
class ho extends rt {
  constructor() {
    super(...arguments);
    y(this, "emitEventType", Y.ZIndexNode);
  }
  async consume(t) {
    const { msgType: e, dataType: s, emitEventType: o } = t;
    if (e === w.UpdateNode && s === x.Local && o === this.emitEventType)
      return this.consumeForLocalWorker(t), !0;
  }
  async consumeForLocalWorker(t) {
    var a;
    const {
      workId: e,
      updateNodeOpt: s,
      willRefreshSelector: o,
      willSyncService: r,
      willSerializeData: n
    } = t;
    e === X && s && await ((a = this.localWork) == null ? void 0 : a.updateSelector({
      updateSelectorOpt: s,
      willRefreshSelector: o,
      willSyncService: r,
      willSerializeData: n,
      callback: this.updateSelectorCallback
    }));
  }
  updateSelectorCallback(t) {
    const { param: e, postData: s, newServiceStore: o } = t, { willSyncService: r, isSync: n } = e, a = s.sp || [];
    if (r && a)
      for (const [c, l] of o.entries())
        l.toolsType === k.BackgroundSVG && a.push({
          ...l,
          workId: c,
          type: w.BackgroundSVGUpdate,
          dataType: x.Local,
          updateNodeOpt: {
            useAnimation: !1
          }
        }), a.push({
          ...l,
          workId: c,
          type: w.UpdateNode,
          updateNodeOpt: {
            useAnimation: !1
          },
          isSync: n
        });
    return {
      sp: a
    };
  }
}
class po extends rt {
  constructor() {
    super(...arguments);
    y(this, "emitEventType", Y.TranslateNode);
  }
  async consume(t) {
    const { msgType: e, dataType: s, emitEventType: o } = t;
    if (e === w.UpdateNode && s === x.Local && o === this.emitEventType)
      return await this.consumeForLocalWorker(t), !0;
  }
  async consumeForLocalWorker(t) {
    var p, d;
    const {
      workId: e,
      updateNodeOpt: s,
      willRefreshSelector: o,
      willSyncService: r,
      willSerializeData: n,
      textUpdateForWoker: a,
      emitEventType: c,
      smoothSync: l,
      needUndoTicker: h
    } = t;
    e === X && s && (s.workState === A.Done && (s != null && s.translate) && (s.translate[0] || s.translate[1]) || s.workState !== A.Done ? await ((p = this.localWork) == null ? void 0 : p.updateSelector({
      updateSelectorOpt: s,
      willRefreshSelector: o,
      willSyncService: r,
      willSerializeData: n,
      isSync: !0,
      textUpdateForWoker: a,
      emitEventType: c,
      callback: this.updateSelectorCallback,
      smoothSync: l,
      needUndoTicker: h
    })) : s.workState === A.Done && ((d = this.localWork) == null || d.vNodes.deleteLastTarget()));
  }
  updateSelectorCallback(t) {
    const { param: e, postData: s, newServiceStore: o, workShapeNode: r, res: n, smoothSync: a } = t, {
      willSyncService: c,
      isSync: l,
      updateSelectorOpt: h,
      textUpdateForWoker: p,
      needUndoTicker: d
    } = e, u = h.workState, f = s.sp || [];
    if (u === A.Start)
      return {
        sp: [],
        render: []
      };
    const m = n == null ? void 0 : n.selectRect;
    if (c) {
      f.push({
        type: w.Select,
        selectIds: r.selectIds,
        selectRect: m,
        willSyncService: u === A.Done ? !0 : a,
        isSync: !0,
        points: u === A.Done && r.getChildrenPoints() || void 0,
        textOpt: r.textOpt
      });
      const g = {
        useAnimation: h.useAnimation || !1
      };
      h.uid && (g.uid = h.uid);
      for (const [I, S] of o.entries())
        S.toolsType === k.BackgroundSVG && f.push({
          ...S,
          workId: I,
          type: w.BackgroundSVGUpdate,
          dataType: x.Local,
          willSyncService: u === A.Done ? !0 : a,
          updateNodeOpt: g
        }), p && S.toolsType === k.Text ? f.push({
          ...S,
          workId: I,
          type: w.TextUpdate,
          dataType: x.Local,
          willSyncService: u === A.Done ? !0 : a,
          updateNodeOpt: g
        }) : (a || u === A.Done) && f.push({
          ...S,
          workId: I,
          type: w.UpdateNode,
          updateNodeOpt: g,
          isSync: l
        });
    }
    return d && f.push({
      type: w.None,
      needUndoTicker: d
    }), {
      sp: f
    };
  }
}
class uo extends rt {
  constructor() {
    super(...arguments);
    y(this, "emitEventType", Y.ScaleNode);
  }
  async consume(t) {
    const { msgType: e, dataType: s, emitEventType: o } = t;
    if (e === w.UpdateNode && s === x.Local && o === this.emitEventType)
      return await this.consumeForLocalWorker(t), !0;
  }
  async consumeForLocalWorker(t) {
    var c;
    const {
      workId: e,
      updateNodeOpt: s,
      willSyncService: o,
      willSerializeData: r,
      smoothSync: n,
      needUndoTicker: a
    } = t;
    e === X && s && await ((c = this.localWork) == null ? void 0 : c.updateSelector({
      updateSelectorOpt: s,
      willSyncService: o,
      willSerializeData: r,
      isSync: !0,
      callback: this.updateSelectorCallback.bind(this),
      smoothSync: n,
      needUndoTicker: a
    }));
  }
  updateSelectorCallback(t) {
    const { param: e, postData: s, workShapeNode: o, res: r, newServiceStore: n, smoothSync: a } = t, { updateSelectorOpt: c, willSyncService: l, needUndoTicker: h } = e, p = c.workState, d = s.sp || [], u = r == null ? void 0 : r.selectRect;
    if (p === A.Start)
      return {
        sp: [],
        render: []
      };
    if (l) {
      d.push({
        type: w.Select,
        selectIds: o.selectIds,
        selectRect: u,
        willSyncService: p === A.Done ? !0 : a,
        isSync: !0,
        points: p === A.Done && o.getChildrenPoints() || void 0,
        textOpt: o.textOpt
      });
      const f = {
        useAnimation: c.useAnimation || !1
      };
      c.uid && (f.uid = c.uid);
      for (const [m, g] of n.entries())
        g.toolsType === k.BackgroundSVG && d.push({
          ...g,
          workId: m,
          type: w.BackgroundSVGUpdate,
          dataType: x.Local,
          willSyncService: p === A.Done ? !0 : a,
          updateNodeOpt: f
        }), g.toolsType === k.Text ? d.push({
          ...g,
          workId: m,
          type: w.TextUpdate,
          dataType: x.Local,
          willSyncService: p === A.Done ? !0 : a,
          updateNodeOpt: f
        }) : (a || p === A.Done) && d.push({
          ...g,
          workId: m,
          type: w.UpdateNode,
          updateNodeOpt: f,
          isSync: !0
        });
    }
    return h && d.push({
      type: w.None,
      needUndoTicker: h
    }), {
      sp: d
    };
  }
}
class fo extends rt {
  constructor() {
    super(...arguments);
    y(this, "emitEventType", Y.RotateNode);
  }
  async consume(t) {
    const { msgType: e, dataType: s, emitEventType: o } = t;
    if (e === w.UpdateNode && s === x.Local && o === this.emitEventType)
      return await this.consumeForLocalWorker(t), !0;
  }
  async consumeForLocalWorker(t) {
    var h;
    const {
      workId: e,
      updateNodeOpt: s,
      willRefreshSelector: o,
      willSyncService: r,
      willSerializeData: n,
      emitEventType: a,
      smoothSync: c,
      needUndoTicker: l
    } = t;
    e === X && s && await ((h = this.localWork) == null ? void 0 : h.updateSelector({
      updateSelectorOpt: s,
      willRefreshSelector: o,
      willSyncService: r,
      willSerializeData: n,
      emitEventType: a,
      isSync: !0,
      callback: this.updateSelectorCallback,
      smoothSync: c,
      needUndoTicker: l
    }));
  }
  updateSelectorCallback(t) {
    const { param: e, postData: s, workShapeNode: o, res: r, newServiceStore: n, smoothSync: a } = t, {
      updateSelectorOpt: c,
      willSyncService: l,
      willSerializeData: h,
      isSync: p,
      needUndoTicker: d
    } = e, u = c.workState, f = s.sp || [], m = r == null ? void 0 : r.selectRect;
    if (l) {
      h && u === A.Done && f.push({
        type: w.Select,
        selectIds: o.selectIds,
        selectRect: m,
        willSyncService: u === A.Done ? !0 : a,
        isSync: p,
        points: o.getChildrenPoints()
      });
      const g = {
        useAnimation: c.useAnimation || !1
      };
      if (c.uid && (g.uid = c.uid), a || u === A.Done)
        for (const [I, S] of n.entries())
          f.push({
            ...S,
            workId: I,
            type: w.UpdateNode,
            updateNodeOpt: g,
            isSync: p
          });
    }
    return d && f.push({
      type: w.None,
      needUndoTicker: d
    }), {
      sp: f
    };
  }
}
class mo extends rt {
  constructor() {
    super(...arguments);
    y(this, "emitEventType", Y.SetFontStyle);
  }
  async consume(t) {
    const { msgType: e, dataType: s, emitEventType: o } = t;
    if (e === w.UpdateNode && s === x.Local && o === this.emitEventType)
      return await this.consumeForLocalWorker(t), !0;
  }
  async consumeForLocalWorker(t) {
    var c;
    const {
      workId: e,
      updateNodeOpt: s,
      willRefreshSelector: o,
      willSyncService: r,
      willSerializeData: n,
      textUpdateForWoker: a
    } = t;
    e === X && s && await ((c = this.localWork) == null ? void 0 : c.updateSelector({
      updateSelectorOpt: s,
      willRefreshSelector: o,
      willSyncService: r,
      willSerializeData: n,
      textUpdateForWoker: a,
      callback: this.updateSelectorCallback
    }));
  }
  updateSelectorCallback(t) {
    const { param: e, postData: s, newServiceStore: o, workShapeNode: r, res: n } = t, { willSyncService: a, isSync: c, updateSelectorOpt: l, textUpdateForWoker: h } = e, p = s.sp || [], d = n == null ? void 0 : n.selectRect;
    if (a && p) {
      l.fontSize && p.push({
        type: w.Select,
        selectIds: r.selectIds,
        selectRect: d,
        willSyncService: a,
        isSync: c,
        points: r.getChildrenPoints()
      });
      for (const [u, f] of o.entries())
        h && f.toolsType === k.Text ? p.push({
          ...f,
          workId: u,
          type: w.TextUpdate,
          dataType: x.Local,
          willSyncService: !0
        }) : p.push({
          ...f,
          workId: u,
          type: w.UpdateNode,
          updateNodeOpt: {
            useAnimation: !1
          },
          isSync: c
        });
    }
    return {
      sp: p
    };
  }
}
class yo extends rt {
  constructor() {
    super(...arguments);
    y(this, "emitEventType", Y.SetPoint);
  }
  async consume(t) {
    const { msgType: e, dataType: s, emitEventType: o } = t;
    if (e === w.UpdateNode && s === x.Local && o === this.emitEventType)
      return this.consumeForLocalWorker(t), !0;
  }
  async consumeForLocalWorker(t) {
    var l;
    const {
      workId: e,
      updateNodeOpt: s,
      willRefreshSelector: o,
      willSyncService: r,
      willSerializeData: n,
      textUpdateForWoker: a,
      needUndoTicker: c
    } = t;
    e === X && s && await ((l = this.localWork) == null ? void 0 : l.updateSelector({
      updateSelectorOpt: s,
      willRefreshSelector: o,
      willSyncService: r,
      emitEventType: this.emitEventType,
      willSerializeData: n,
      isSync: !0,
      textUpdateForWoker: a,
      callback: this.updateSelectorCallback,
      needUndoTicker: c
    }));
  }
  updateSelectorCallback(t) {
    const { param: e, postData: s, newServiceStore: o, workShapeNode: r, res: n } = t, { willSyncService: a, isSync: c, needUndoTicker: l } = e, h = s.sp || [], p = n == null ? void 0 : n.selectRect;
    if (a && h) {
      for (const [d, u] of o.entries())
        h.push({
          ...u,
          workId: d,
          type: w.UpdateNode,
          updateNodeOpt: {
            useAnimation: !1
          },
          isSync: c
        });
      h.push({
        type: w.Select,
        selectIds: r.selectIds,
        selectRect: p,
        willSyncService: a,
        isSync: c,
        points: r.getChildrenPoints()
      });
    }
    return l && h.push({
      type: w.None,
      needUndoTicker: l
    }), {
      sp: h
    };
  }
}
class wo extends rt {
  constructor() {
    super(...arguments);
    y(this, "emitEventType", Y.SetLock);
  }
  async consume(t) {
    const { msgType: e, dataType: s, emitEventType: o } = t;
    if (e === w.UpdateNode && s === x.Local && o === this.emitEventType)
      return this.consumeForLocalWorker(t), !0;
  }
  async consumeForLocalWorker(t) {
    var c;
    const {
      workId: e,
      updateNodeOpt: s,
      willRefreshSelector: o,
      willSyncService: r,
      willSerializeData: n,
      needUndoTicker: a
    } = t;
    e === X && s && await ((c = this.localWork) == null ? void 0 : c.updateSelector({
      updateSelectorOpt: s,
      willRefreshSelector: o,
      willSyncService: r,
      willSerializeData: n,
      callback: this.updateSelectorCallback,
      needUndoTicker: a
    }));
  }
  updateSelectorCallback(t) {
    const { param: e, postData: s, newServiceStore: o, workShapeNode: r, res: n } = t, { willSyncService: a, isSync: c, updateSelectorOpt: l, needUndoTicker: h } = e, p = s.sp || [], d = n == null ? void 0 : n.selectRect;
    if (a && p) {
      for (const [u, f] of o.entries())
        p.push({
          ...f,
          workId: u,
          type: w.UpdateNode,
          updateNodeOpt: {
            useAnimation: !1
          },
          isSync: c
        });
      p.push({
        isLocked: l.isLocked,
        selectorColor: r.selectorColor,
        scaleType: r.scaleType,
        canRotate: r.canRotate,
        type: w.Select,
        selectIds: r.selectIds,
        selectRect: d,
        willSyncService: a,
        isSync: c
      });
    }
    return h && p.push({
      type: w.None,
      needUndoTicker: h
    }), {
      sp: p
    };
  }
}
class go extends rt {
  constructor() {
    super(...arguments);
    y(this, "emitEventType", Y.SetShapeOpt);
  }
  async consume(t) {
    const { msgType: e, dataType: s, emitEventType: o } = t;
    if (e === w.UpdateNode && s === x.Local && o === this.emitEventType)
      return this.consumeForLocalWorker(t), !0;
  }
  async consumeForLocalWorker(t) {
    var c;
    const {
      workId: e,
      updateNodeOpt: s,
      willRefreshSelector: o,
      willSyncService: r,
      willSerializeData: n,
      needUndoTicker: a
    } = t;
    e === X && s && await ((c = this.localWork) == null ? void 0 : c.updateSelector({
      updateSelectorOpt: s,
      willRefreshSelector: o,
      willSyncService: r,
      willSerializeData: n,
      callback: this.updateSelectorCallback,
      needUndoTicker: a
    }));
  }
  updateSelectorCallback(t) {
    const { param: e, postData: s, newServiceStore: o } = t, { willSyncService: r, isSync: n, needUndoTicker: a } = e, c = s.sp || [];
    if (r && c)
      for (const [l, h] of o.entries())
        c.push({
          ...h,
          workId: l,
          type: w.UpdateNode,
          updateNodeOpt: {
            useAnimation: !1
          },
          isSync: n
        });
    return a && c.push({
      type: w.None,
      needUndoTicker: a
    }), {
      sp: c
    };
  }
}
class ko {
  constructor(i) {
    y(this, "builders", /* @__PURE__ */ new Map());
    this.builders = new Map(i.map((t) => [t, this.build(t)]));
  }
  build(i) {
    switch (i) {
      case Y.TranslateNode:
        return new po();
      case Y.ZIndexNode:
        return new ho();
      case Y.CopyNode:
        return new co();
      case Y.SetColorNode:
        return new lo();
      case Y.ScaleNode:
        return new uo();
      case Y.RotateNode:
        return new fo();
      case Y.SetFontStyle:
        return new mo();
      case Y.SetPoint:
        return new yo();
      case Y.SetLock:
        return new wo();
      case Y.SetShapeOpt:
        return new go();
    }
  }
  registerForMainThread(i) {
    return this.builders.forEach((t) => {
      t && t.registerMainThread(i);
    }), this;
  }
  async consumeForMainThread(i) {
    for (const t of this.builders.values())
      if (await (t == null ? void 0 : t.consume(i)))
        return !0;
    return !1;
  }
}
class So {
  constructor(i, t) {
    y(this, "viewId");
    y(this, "fullLayer");
    y(this, "topLayer");
    y(this, "localLayer");
    y(this, "serviceLayer");
    y(this, "snapshotFullLayer");
    y(this, "vNodes");
    y(this, "master");
    y(this, "opt");
    y(this, "cameraOpt");
    y(this, "scene");
    y(this, "localWork");
    y(this, "serviceWork");
    y(this, "topWork");
    y(this, "taskUpdateCameraId");
    y(this, "debounceUpdateCameraId");
    y(this, "debounceUpdateCache", /* @__PURE__ */ new Set());
    y(this, "mainThreadPostId");
    y(this, "combinePostMsg", /* @__PURE__ */ new Set());
    y(this, "methodBuilder");
    y(this, "cacheImages", /* @__PURE__ */ new Map());
    y(this, "imageResolveMap", /* @__PURE__ */ new Map());
    this.viewId = i, this.opt = t, this.scene = this.createScene({
      ...t.canvasOpt,
      container: t.container
    }), this.master = t.master;
    const e = J.bufferSize.full, s = J.bufferSize.sub;
    this.fullLayer = this.createLayer("fullLayer", this.scene, {
      ...t.layerOpt,
      bufferSize: this.viewId === ct ? e : s * 2
    }), this.topLayer = this.createLayer("topLayer", this.scene, {
      ...t.layerOpt,
      bufferSize: (this.viewId === ct, s),
      contextType: "2d"
    }), this.localLayer = this.createLayer("localLayer", this.scene, {
      ...t.layerOpt,
      bufferSize: (this.viewId === ct, s),
      contextType: "2d"
    }), this.serviceLayer = this.createLayer("serviceLayer", this.scene, {
      ...t.layerOpt,
      bufferSize: (this.viewId === ct, s),
      contextType: "2d"
    }), this.vNodes = new Xe(i, this.scene);
    const o = {
      thread: this,
      vNodes: this.vNodes
    };
    this.localWork = new no(o), this.serviceWork = new ao(o), this.topWork = new ro(o), this.vNodes.init(this.fullLayer), this.methodBuilder = new ko([
      Y.CopyNode,
      Y.SetColorNode,
      Y.DeleteNode,
      Y.RotateNode,
      Y.ScaleNode,
      Y.TranslateNode,
      Y.ZIndexNode,
      Y.SetFontStyle,
      Y.SetPoint,
      Y.SetLock,
      Y.SetShapeOpt
    ]).registerForMainThread(this);
  }
  getCachedImages(i) {
    var t;
    return (t = this.cacheImages.get(i)) == null ? void 0 : t.imageBitmap;
  }
  getCachedImagesByWorkId(i) {
    for (const [t, e] of this.cacheImages.entries())
      if (t === i && e.imageBitmap)
        return e.imageBitmap;
  }
  deleteCachedImagesByWorkId(i) {
    for (const [t, e] of this.cacheImages.entries())
      e.workId === i && (e.imageBitmap.close(), this.cacheImages.delete(t));
  }
  clearCacheImages() {
    this.cacheImages.forEach((i) => i.imageBitmap.close()), this.cacheImages.clear();
  }
  clearImageResolveMap() {
    this.imageResolveMap.forEach(({ timer: i }) => {
      i && clearTimeout(i);
    }), this.imageResolveMap.clear();
  }
  post(i) {
    this.combinePostMsg.add(i), this.runBatchPostData();
  }
  updateDpr(i) {
    this.scene.displayRatio = i;
  }
  async on(i) {
    if (!await this.methodBuilder.consumeForMainThread(i)) {
      const {
        msgType: t,
        toolsType: e,
        opt: s,
        dataType: o,
        workId: r,
        workState: n,
        imageSrc: a,
        imageBitmap: c,
        workIds: l,
        isLockSentEventCursor: h
      } = i, p = r == null ? void 0 : r.toString();
      switch (t) {
        case w.UpdateDpr:
          zt(i.dpr) && this.updateDpr(i.dpr);
          break;
        case w.AuthClear: {
          const { clearUids: d, localUid: u } = i;
          this.vNodes.setCanClearUids(d), this.vNodes.setLocalUid(u);
          break;
        }
        case w.Destroy:
          this.destroy();
          break;
        case w.Clear:
          this.clearAll();
          break;
        case w.UpdateCamera:
          await this.updateCamera(i);
          break;
        case w.UpdateTools:
          if (e && s) {
            const d = {
              toolsType: e,
              toolsOpt: s
            };
            this.topWork.canUseTopLayer(e) ? this.topWork.setToolsOpt(d) : this.localWork.setToolsOpt(d);
          }
          break;
        case w.CreateWork:
          if (p && s && e) {
            if (this.topWork.canUseTopLayer(e)) {
              this.topWork.getToolsOpt() || this.topWork.setToolsOpt({
                toolsType: e,
                toolsOpt: s
              }), this.topWork.setWorkOptions(p, s);
              break;
            }
            this.localWork.getToolsOpt() || this.localWork.setToolsOpt({
              toolsType: e,
              toolsOpt: s
            }), this.localWork.setWorkOptions(p, s);
          }
          break;
        case w.DrawWork:
          n === A.Done && o === x.Local ? (this.consumeDrawAll(o, i), e === k.LaserPen && h && this.post({
            sp: [
              {
                type: w.None,
                isLockSentEventCursor: h
              }
            ]
          })) : this.consumeDraw(o, i);
          break;
        case w.UpdateNode:
        case w.FullWork:
          if (e && this.topWork.canUseTopLayer(e)) {
            this.consumeDrawAll(o, i);
            break;
          }
          this.consumeFull(o, i);
          break;
        case w.RemoveNode:
          await this.removeNode(i);
          return;
        case w.Select:
          o === x.Service && (r === X ? this.localWork.updateFullSelectWork(i) : this.serviceWork.runSelectWork(i));
          break;
        case w.CursorBlur:
          this.localWork.cursorBlur();
          return;
        case w.CursorHover:
          this.localWork.cursorHover(i);
          break;
        case w.GetTextActive:
          o === x.Local && this.localWork.checkTextActive(i);
          break;
        case w.GetImageBitMap:
          if (a && c && r) {
            const d = r.toString();
            this.deleteCachedImagesByWorkId(d), this.cacheImages.set(a, {
              imageBitmap: c,
              workId: d
            });
            const u = this.imageResolveMap.get(a);
            if (u) {
              const { resolve: f, timer: m } = u;
              m && clearTimeout(m), f && f(a);
            }
          }
          break;
        case w.GetVNodeInfo:
          if (r && l) {
            const d = l.map((u) => this.vNodes.get(u));
            this.post({
              sp: [
                {
                  type: w.GetVNodeInfo,
                  dataType: x.Local,
                  workId: r,
                  vInfo: d
                }
              ]
            });
          }
          break;
      }
    }
  }
  getIconSize(i, t, e) {
    const s = i * e, o = t * e;
    return s <= 50 || o <= 50 ? [50, 50] : s <= 100 || o <= 100 ? [100, 100] : s <= 200 || o <= 200 ? [200, 200] : s <= 400 || o <= 400 ? [400, 400] : s <= 800 || o <= 800 ? [800, 800] : [1600, 1600];
  }
  async loadImageBitMap(i) {
    const { toolsType: t, opt: e, workId: s } = i;
    if (t === k.Image && e && s) {
      const o = s.toString(), { src: r, type: n, width: a, height: c, strokeColor: l } = e;
      if (!r || !n || !a || !c)
        return;
      let h = r;
      if (n === Mt.Iconify) {
        const [f, m] = this.getIconSize(a, c, this.opt.displayer.dpr);
        h = `${r}?width=${f}&height=${m}&color=${l}`;
      }
      if (this.cacheImages.has(h)) {
        const f = this.getCachedImages(h);
        if (f)
          return f;
      }
      if (this.imageResolveMap.has(h)) {
        const f = this.getCachedImagesByWorkId(o);
        if (f)
          return f;
      }
      const u = await new Promise((f) => {
        const m = this.imageResolveMap.get(h) || {
          resolve: void 0,
          timer: void 0
        };
        m.timer && clearTimeout(m.timer), m.resolve = f, m.timer = setTimeout(() => {
          const g = this.imageResolveMap.get(h);
          g != null && g.resolve && g.resolve(h);
        }, 5e3), this.imageResolveMap.set(h, m), this.opt.post({
          sp: [
            {
              imageSrc: h,
              workId: o,
              viewId: this.viewId,
              isgl: !!this.fullLayer.parent.gl,
              isSubWorker: !1,
              type: w.GetImageBitMap
            }
          ]
        });
      });
      return this.imageResolveMap.delete(u), this.getCachedImages(h);
    }
  }
  async removeNode(i) {
    const { dataType: t, workId: e, removeIds: s } = i, o = s || [];
    if (e && o.push(e.toString()), o.length)
      for (const r of o) {
        if (r === X) {
          await this.localWork.removeSelector(i);
          continue;
        }
        t === x.Local ? this.localWork.removeWork(i) : t === x.Service && this.serviceWork.removeWork(i), await this.localWork.colloctEffectSelectWork(i);
      }
  }
  async consumeFull(i, t) {
    const e = await this.localWork.colloctEffectSelectWork(t);
    e && i === x.Local && await this.localWork.consumeFull(e), e && i === x.Service && this.serviceWork.consumeFull(e);
  }
  setCameraOpt(i) {
    this.cameraOpt = i;
    const { scale: t, centerX: e, centerY: s, width: o, height: r } = i;
    (o !== this.scene.width || r !== this.scene.height) && this.updateScene({ width: o, height: r }), this.fullLayer.setAttribute("scale", [t, t]), this.fullLayer.setAttribute("translate", [-e, -s]), this.topLayer.setAttribute("scale", [t, t]), this.topLayer.setAttribute("translate", [-e, -s]), this.localLayer.setAttribute("scale", [t, t]), this.localLayer.setAttribute("translate", [-e, -s]), this.serviceLayer.setAttribute("scale", [t, t]), this.serviceLayer.setAttribute("translate", [-e, -s]);
  }
  runBatchPostData() {
    this.mainThreadPostId || (this.mainThreadPostId = requestAnimationFrame(
      this.combinePost.bind(this)
    ));
  }
  combinePostData() {
    var s;
    this.mainThreadPostId = void 0;
    const i = [];
    let t, e;
    for (const o of this.combinePostMsg.values()) {
      if ((s = o.sp) != null && s.length)
        for (const r of o.sp) {
          let n = !1;
          for (const a of i)
            if (ue(r, a)) {
              n = !0;
              break;
            }
          n || i.push(r);
        }
      zt(o.fullWorkerDrawCount) && (t = o.fullWorkerDrawCount), zt(o.consumeCount) && (e = o.consumeCount);
    }
    return this.combinePostMsg.clear(), {
      sp: i,
      fullWorkerDrawCount: t,
      consumeCount: e
    };
  }
  combinePost() {
    var e, s;
    const i = this.combinePostData(), t = (e = i.sp) == null ? void 0 : e.filter(
      (o) => o.type !== w.None || o.isLockSentEventCursor || o.needUndoTicker
    );
    t != null && t.length ? i.sp = t.map((o) => o.viewId ? o : { ...o, viewId: this.viewId }) : delete i.sp, i.consumeCount === void 0 && delete i.consumeCount, i.fullWorkerDrawCount === void 0 && delete i.fullWorkerDrawCount, (i != null && i.consumeCount || i != null && i.fullWorkerDrawCount || (s = i.sp) != null && s.length) && this.opt.post(i);
  }
  clearAll() {
    this.fullLayer.children.length && (this.fullLayer.parent.children.forEach((i) => {
      i.name !== "viewport" && i.remove();
    }), Q(this.fullLayer, this.fullLayer.parent)), this.clearCacheImages(), this.clearImageResolveMap(), this.localWork.clearAll(), this.topWork.clearAll(), this.serviceWork.clearAll(), this.vNodes.clear(), this.post({
      sp: [
        {
          type: w.Clear
        }
      ]
    });
  }
  consumeDrawAll(i, t) {
    const { toolsType: e, workId: s } = t;
    if (s) {
      const o = s.toString();
      if (e && this.topWork.canUseTopLayer(e)) {
        i === x.Local && (this.topWork.getLocalWorkShape(
          s.toString()
        ) || this.topWork.createLocalWork(t)), this.topWork.consumeDrawAll(t);
        return;
      }
      i === x.Local && (this.localWork.getWorkShape(o) || this.localWork.createLocalWork(t), this.localWork.consumeDrawAll(t, this.serviceWork));
    }
  }
  consumeDraw(i, t) {
    const { opt: e, workId: s, toolsType: o } = t;
    if (s && o && e) {
      const r = s.toString();
      if (this.topWork.canUseTopLayer(o)) {
        i === x.Local && (this.topWork.getLocalWorkShape(r) || this.topWork.createLocalWork(t)), this.topWork.consumeDraw(t);
        return;
      }
      i === x.Local ? (this.localWork.getWorkShape(r) || this.localWork.createLocalWork(t), this.localWork.consumeDraw(t, this.serviceWork)) : i === x.Service && this.serviceWork.consumeDraw(t);
      return;
    }
  }
  async updateCamera(i) {
    var s;
    const { cameraOpt: t, scenePath: e } = i;
    if (t && !ue(this.cameraOpt, t)) {
      if (this.taskUpdateCameraId && (clearTimeout(this.taskUpdateCameraId), this.taskUpdateCameraId = void 0), e) {
        let c = !1;
        for (const [l, h] of this.localWork.getWorkShapes().entries())
          switch (h.toolsType) {
            case k.Text:
            case k.BitMapEraser:
            case k.PencilEraser:
            case k.Eraser:
            case k.Selector:
            case k.LaserPen:
              break;
            default:
              l !== bt && l !== X && (c = !0);
              break;
          }
        if (c) {
          this.taskUpdateCameraId = setTimeout(() => {
            this.taskUpdateCameraId = void 0, this.updateCamera(i);
          }, pe);
          return;
        }
      }
      const o = /* @__PURE__ */ new Map();
      for (const [c, l] of this.vNodes.getNodesByType(k.Text).entries()) {
        const h = l.rect;
        o.set(c, be(h));
      }
      const r = new Set(o.keys());
      let n = !1;
      if (this.localWork.hasSelector()) {
        const c = (s = this.localWork.getSelector()) == null ? void 0 : s.selectIds;
        if (c) {
          n = !0;
          for (const l of c)
            r.add(l);
        }
      }
      let a = !1;
      if (this.serviceWork.selectorWorkShapes.size)
        for (const c of this.serviceWork.selectorWorkShapes.values()) {
          const l = c.selectIds;
          if (l) {
            a = !0;
            for (const h of l)
              r.add(h);
          }
        }
      if (this.setCameraOpt(t), this.vNodes.curNodeMap.size) {
        this.vNodes.clearTarget(), this.vNodes.updateHighLevelNodesRect(r), this.debounceUpdateCameraId && clearTimeout(this.debounceUpdateCameraId);
        for (const [c, l] of o.entries()) {
          const h = this.vNodes.get(c);
          if (h) {
            const p = l, d = h.rect, u = this.getSceneRect(), f = Rt(p, u), m = Rt(d, u);
            let g = !1;
            if ((f !== m || p.w !== d.w || p.h !== d.h || m === Vt.intersect) && (g = !0), g) {
              const { toolsType: I } = h;
              I === k.Text && this.debounceUpdateCache.add(c);
            }
          }
        }
        if (n && this.localWork.reRenderSelector(), a)
          for (const [
            c,
            l
          ] of this.serviceWork.selectorWorkShapes.entries())
            this.serviceWork.runSelectWork({
              workId: c,
              selectIds: l.selectIds,
              msgType: w.Select,
              dataType: x.Service,
              viewId: this.viewId
            });
        this.debounceUpdateCameraId = setTimeout(() => {
          var l;
          this.debounceUpdateCameraId = void 0;
          const c = [];
          for (const h of this.debounceUpdateCache.values()) {
            if ((l = this.fullLayer) == null ? void 0 : l.getElementsByName(h)[0]) {
              const d = this.vNodes.get(h);
              if (d) {
                const { toolsType: u, opt: f } = d, m = this.localWork.setFullWork({
                  toolsType: u,
                  opt: f,
                  workId: h
                });
                if (m) {
                  const g = this.getSceneRect();
                  c.push(
                    m.consumeServiceAsync({
                      isFullWork: !0,
                      replaceId: h,
                      boxRect: g
                    })
                  );
                }
              }
            }
            this.debounceUpdateCache.delete(h);
          }
          this.vNodes.updateLowLevelNodesRect(), this.vNodes.clearHighLevelIds();
        }, pe);
      }
    }
  }
  getSceneRect() {
    const { width: i, height: t } = this.scene;
    return {
      x: 0,
      y: 0,
      w: Math.floor(i),
      h: Math.floor(t)
    };
  }
  createScene(i) {
    return new oe({
      displayRatio: this.opt.displayer.dpr,
      depth: !1,
      desynchronized: !0,
      ...i,
      autoRender: !0,
      id: this.viewId,
      contextType: "2d"
    });
  }
  createLayer(i, t, e) {
    const { width: s, height: o } = e, r = `canvas-${i}`, n = t.layer(r, { ...e, offscreen: !1 }), a = new j({
      anchor: [0.5, 0.5],
      pos: [s * 0.5, o * 0.5],
      size: [s, o],
      name: "viewport",
      id: i
    });
    return n.append(a), a;
  }
  updateScene(i) {
    this.scene.attr({ ...i });
    const { width: t, height: e } = i;
    this.scene.width = t, this.scene.height = e, this.updateLayer({ width: t, height: e });
  }
  updateLayer(i) {
    const { width: t, height: e } = i;
    this.fullLayer.parent.setAttribute("width", t), this.fullLayer.parent.setAttribute("height", e), this.fullLayer.setAttribute("size", [t, e]), this.fullLayer.setAttribute("pos", [t * 0.5, e * 0.5]), this.topLayer.parent.setAttribute("width", t), this.topLayer.parent.setAttribute("height", e), this.topLayer.setAttribute("size", [t, e]), this.topLayer.setAttribute("pos", [t * 0.5, e * 0.5]), this.localLayer.parent.setAttribute("width", t), this.localLayer.parent.setAttribute("height", e), this.localLayer.setAttribute("size", [t, e]), this.localLayer.setAttribute("pos", [t * 0.5, e * 0.5]), this.serviceLayer.parent.setAttribute("width", t), this.serviceLayer.parent.setAttribute("height", e), this.serviceLayer.setAttribute("size", [t, e]), this.serviceLayer.setAttribute("pos", [t * 0.5, e * 0.5]);
  }
  destroy() {
    this.clearCacheImages(), this.clearImageResolveMap(), this.vNodes.clear(), this.fullLayer.remove(), q(this.fullLayer, this.fullLayer.parent), this.topLayer.remove(), q(this.topLayer, this.topLayer.parent), this.localLayer.remove(), q(this.localLayer, this.localLayer.parent), this.serviceLayer.remove(), q(this.serviceLayer, this.serviceLayer.parent), this.scene.remove(), this.localWork.destroy(), this.serviceWork.destroy(), this.topWork.destroy();
  }
}
class To {
  constructor(i, t) {
    y(this, "viewId");
    y(this, "fullLayer");
    y(this, "master");
    y(this, "opt");
    y(this, "scene");
    y(this, "mainThreadPostId");
    y(this, "combinePostMsg", /* @__PURE__ */ new Set());
    y(this, "workShapes", /* @__PURE__ */ new Map());
    y(this, "cacheImages", /* @__PURE__ */ new Map());
    y(this, "imageResolveMap", /* @__PURE__ */ new Map());
    this.viewId = i, this.opt = t, this.scene = this.createScene({
      ...t.canvasOpt,
      container: t.container
    }), this.master = t.master, this.fullLayer = this.createLayer("fullLayer", this.scene, {
      ...t.layerOpt,
      bufferSize: this.viewId === ct ? 6e3 : 3e3,
      contextType: "2d"
    });
  }
  getCachedImages(i) {
    var t;
    return (t = this.cacheImages.get(i)) == null ? void 0 : t.imageBitmap;
  }
  getCachedImagesByWorkId(i) {
    for (const [t, e] of this.cacheImages.entries())
      if (t === i && e.imageBitmap)
        return e.imageBitmap;
  }
  deleteCachedImagesByWorkId(i) {
    for (const [t, e] of this.cacheImages.entries())
      e.workId === i && (e.imageBitmap.close(), this.cacheImages.delete(t));
  }
  clearCacheImages() {
    this.cacheImages.forEach((i) => i.imageBitmap.close()), this.cacheImages.clear();
  }
  clearImageResolveMap() {
    this.imageResolveMap.forEach(({ timer: i }) => {
      i && clearTimeout(i);
    }), this.imageResolveMap.clear();
  }
  post(i) {
    this.combinePostMsg.add(i), this.runBatchPostData();
  }
  async on(i) {
    const { msgType: t, imageSrc: e, imageBitmap: s, workId: o } = i;
    switch (t) {
      case w.Snapshot: {
        await this.getSnapshot(i), this.destroy();
        return;
      }
      case w.BoundingBox: {
        await this.getBoundingRect(i), this.destroy();
        return;
      }
      case w.GetImageBitMap: {
        if (e && s && o) {
          const r = o.toString();
          this.deleteCachedImagesByWorkId(r), this.cacheImages.set(e, {
            imageBitmap: s,
            workId: r
          });
          const n = this.imageResolveMap.get(e);
          if (n) {
            const { resolve: a, timer: c } = n;
            c && clearTimeout(c), a && a(e);
          }
        }
        break;
      }
    }
  }
  getIconSize(i, t, e) {
    const s = i * e, o = t * e;
    return s <= 50 || o <= 50 ? [50, 50] : s <= 100 || o <= 100 ? [100, 100] : s <= 200 || o <= 200 ? [200, 200] : s <= 400 || o <= 400 ? [400, 400] : s <= 800 || o <= 800 ? [800, 800] : [1600, 1600];
  }
  async loadImageBitMap(i) {
    const { toolsType: t, opt: e, workId: s } = i;
    if (t === k.Image && e && s) {
      const o = s.toString(), { src: r, type: n, width: a, height: c, strokeColor: l } = e;
      if (!r || !n || !a || !c)
        return;
      let h = r;
      if (n === Mt.Iconify) {
        const [f, m] = this.getIconSize(a, c, this.opt.displayer.dpr);
        h = `${r}?width=${f}&height=${m}&color=${l}`;
      }
      if (this.cacheImages.has(h)) {
        const f = this.getCachedImages(h);
        if (f)
          return f;
      }
      if (this.imageResolveMap.has(h)) {
        const f = this.getCachedImagesByWorkId(o);
        if (f)
          return f;
      }
      const u = await new Promise((f) => {
        const m = this.imageResolveMap.get(h) || {
          resolve: void 0,
          timer: void 0
        };
        m.timer && clearTimeout(m.timer), m.resolve = f, m.timer = setTimeout(() => {
          const g = this.imageResolveMap.get(h);
          g != null && g.resolve && g.resolve(h);
        }, 5e3), this.imageResolveMap.set(h, m), this.opt.post({
          sp: [
            {
              imageSrc: h,
              workId: o,
              viewId: this.viewId,
              isgl: !!this.fullLayer.parent.gl,
              isSubWorker: !0,
              type: w.GetImageBitMap
            }
          ]
        });
      });
      return this.imageResolveMap.delete(u), this.getCachedImages(h);
    }
  }
  createWorkShapeNode(i) {
    return Lt({
      ...i,
      fullLayer: this.fullLayer,
      drawLayer: void 0
    });
  }
  setFullWork(i) {
    const { workId: t, opt: e, toolsType: s } = i;
    if (t && e && s) {
      const o = t.toString();
      let r;
      return t && this.workShapes.has(o) ? (r = this.workShapes.get(o), r == null || r.setWorkOptions(e)) : r = this.createWorkShapeNode({
        toolsOpt: e,
        toolsType: s,
        workId: o
      }), r ? (this.workShapes.set(o, r), r) : void 0;
    }
  }
  async runFullWork(i) {
    var s;
    const t = this.setFullWork(i), e = i.ops && yt(i.ops);
    if (t) {
      let o, r;
      const n = (s = t.getWorkId()) == null ? void 0 : s.toString();
      return t.toolsType === k.BackgroundSVG ? o = t.consumeService({
        isFullWork: !0,
        replaceId: n
      }) : t.toolsType === k.Image ? o = await t.consumeServiceAsync({
        isFullWork: !0,
        worker: this
      }) : t.toolsType === k.Text ? o = await t.consumeServiceAsync({
        isFullWork: !0,
        replaceId: n,
        isDrawLabel: !0
      }) : (o = t.consumeService({
        op: e,
        isFullWork: !0,
        replaceId: n
      }), r = (i == null ? void 0 : i.updateNodeOpt) && t.updataOptService(i.updateNodeOpt)), E(o, r);
    }
  }
  async getSnapshot(i) {
    const { scenePath: t, scenes: e, cameraOpt: s, w: o, h: r } = i;
    if (t && e && s) {
      this.setCameraOpt(s);
      for (const [a, c] of Object.entries(e))
        if (c != null && c.type)
          switch (c == null ? void 0 : c.type) {
            case w.UpdateNode:
            case w.FullWork: {
              const { opt: l } = c, h = {
                ...c,
                opt: l,
                workId: a,
                msgType: w.FullWork,
                dataType: x.Service,
                viewId: this.viewId
              };
              await this.runFullWork(h);
              break;
            }
          }
      let n;
      o && r && (n = {
        resizeWidth: o,
        resizeHeight: r
      });
      try {
        await this.getSnapshotRender({ scenePath: t, options: n });
      } catch (a) {
        const c = a && a instanceof Error ? a.message : a == null ? void 0 : a.toString();
        console.error(
          "[SnapshotThreadImpl] getSnapshotRender error",
          c
        ), this.post({
          sp: [
            {
              type: w.ReportError,
              reportString: `[SnapshotThreadImpl] getSnapshotRender error: ${c}`
            }
          ]
        });
      }
    }
  }
  getSceneRect() {
    const { width: i, height: t } = this.scene;
    return {
      x: 0,
      y: 0,
      w: Math.floor(i),
      h: Math.floor(t)
    };
  }
  getRectImageBitmap(i, t) {
    const e = Math.floor(i.x * this.opt.displayer.dpr), s = Math.floor(i.y * this.opt.displayer.dpr), o = i.w > 0 && Math.floor(i.w * this.opt.displayer.dpr || 1) || 1, r = i.h > 0 && Math.floor(i.h * this.opt.displayer.dpr || 1) || 1;
    return createImageBitmap(
      this.fullLayer.parent.canvas,
      e,
      s,
      o,
      r,
      t
    );
  }
  async getSnapshotRender(i) {
    var o;
    const { scenePath: t, options: e } = i;
    ((o = this.fullLayer) == null ? void 0 : o.parent).render();
    const s = await this.getRectImageBitmap(
      this.getSceneRect(),
      e
    );
    s && (this.post({
      sp: [
        {
          type: w.Snapshot,
          scenePath: t,
          imageBitmap: s,
          viewId: this.viewId,
          index: 0
        }
      ]
    }), this.fullLayer && Q(this.fullLayer, this.fullLayer.parent));
  }
  async getBoundingRect(i) {
    const { scenePath: t, scenes: e, cameraOpt: s } = i;
    if (t && e && s) {
      this.setCameraOpt(s);
      let o;
      for (const [r, n] of Object.entries(e))
        if (n != null && n.type)
          switch (n == null ? void 0 : n.type) {
            case w.UpdateNode:
            case w.FullWork: {
              const a = await this.runFullWork({
                ...n,
                workId: r,
                msgType: w.FullWork,
                dataType: x.Service,
                viewId: this.viewId
              });
              o = E(o, a);
              break;
            }
          }
      o && this.post({
        sp: [
          {
            type: w.BoundingBox,
            scenePath: t,
            rect: o
          }
        ]
      });
    }
  }
  setCameraOpt(i) {
    const { scale: t, centerX: e, centerY: s, width: o, height: r } = i;
    this.updateScene({ width: o, height: r }), this.fullLayer.setAttribute("scale", [t, t]), this.fullLayer.setAttribute("translate", [-e, -s]);
  }
  runBatchPostData() {
    this.mainThreadPostId || (this.mainThreadPostId = requestAnimationFrame(
      this.combinePost.bind(this)
    ));
  }
  combinePostData() {
    var t;
    this.mainThreadPostId = void 0;
    const i = [];
    for (const e of this.combinePostMsg.values())
      if ((t = e.sp) != null && t.length)
        for (const s of e.sp) {
          let o = !1;
          for (const r of i)
            if (lt(s, r)) {
              o = !0;
              break;
            }
          o || i.push(s);
        }
    return this.combinePostMsg.clear(), {
      sp: i
    };
  }
  combinePost() {
    var e, s;
    const i = this.combinePostData(), t = (e = i.sp) == null ? void 0 : e.filter(
      (o) => o.type !== w.None || o.isLockSentEventCursor || o.needUndoTicker
    );
    t != null && t.length ? i.sp = t.map((o) => o.viewId ? o : { ...o, viewId: this.viewId }) : delete i.sp, (s = i.sp) != null && s.length && this.opt.post(i);
  }
  createScene(i) {
    return new oe({
      displayRatio: this.opt.displayer.dpr,
      depth: !1,
      desynchronized: !0,
      ...i,
      autoRender: !1,
      contextType: "2d"
    });
  }
  createLayer(i, t, e) {
    const { width: s, height: o } = e, r = `canvas-${i}`, n = t.layer(r, e), a = new j({
      anchor: [0.5, 0.5],
      pos: [s * 0.5, o * 0.5],
      size: [s, o],
      name: "viewport",
      id: i
    });
    return n.append(a), a;
  }
  updateScene(i) {
    this.scene.attr({ ...i });
    const { width: t, height: e } = i;
    this.scene.width = t, this.scene.height = e, this.updateLayer({ width: t, height: e });
  }
  updateLayer(i) {
    const { width: t, height: e } = i;
    this.fullLayer.parent.setAttribute("width", t), this.fullLayer.parent.setAttribute("height", e), this.fullLayer.setAttribute("size", [t, e]), this.fullLayer.setAttribute("pos", [t * 0.5, e * 0.5]);
  }
  destroy() {
    this.clearCacheImages(), this.clearImageResolveMap(), this.fullLayer.remove(), q(this.fullLayer, this.fullLayer.parent), this.scene.remove();
  }
}
class Oo {
  constructor(i) {
    y(this, "mainThreadMap", /* @__PURE__ */ new Map());
    y(this, "snapshotThread");
    y(this, "master");
    y(this, "post", (i) => {
      const { fullWorkerDrawCount: t, sp: e, workerTasksqueueCount: s, consumeCount: o } = i;
      this.master.isBusy && H(s) && this.master.setWorkerTasksqueueCount(s), H(t) && this.master.setMaxDrawCount(t), H(o) && this.master.setConsumeCount(o), e && this.master.collectorSyncData(e);
    });
    this.master = i;
  }
  destroy() {
    this.mainThreadMap.clear();
  }
  createMainThread(i, t) {
    return new So(i, t);
  }
  createSnapshotThread(i, t) {
    return new To(i, t);
  }
  async consume(i) {
    var t, e, s, o;
    for (const r of i.values()) {
      const {
        msgType: n,
        viewId: a,
        tasksqueue: c,
        mainTasksqueueCount: l,
        layerOpt: h,
        offscreenCanvasOpt: p,
        cameraOpt: d,
        isSubWorker: u
      } = r;
      if (n === w.Console) {
        console.log(this);
        continue;
      }
      if (n === w.Init) {
        const m = (t = this.master.control.viewContainerManager.getView(a)) == null ? void 0 : t.displayer, g = m == null ? void 0 : m.canvasContainerRef.current;
        if (m && g && h && p) {
          const I = this.createMainThread(a, {
            displayer: m,
            container: g,
            layerOpt: h,
            master: this.master,
            canvasOpt: p,
            post: this.post
          });
          this.mainThreadMap.set(a, I), I && d && I.setCameraOpt(d);
        }
        continue;
      }
      if ((n === w.Snapshot || n === w.BoundingBox) && a === ((e = this.master.control.viewContainerManager.mainView) == null ? void 0 : e.id)) {
        const m = (s = this.master.control.viewContainerManager.getView(a)) == null ? void 0 : s.displayer, g = (o = m.snapshotContainerRef) == null ? void 0 : o.current;
        if (m && g && d) {
          g.style.width = `${d.width}px`, g.style.height = `${d.height}px`;
          const I = {
            ...de.defaultLayerOpt,
            offscreen: !1,
            width: d.width,
            height: d.height
          }, S = {
            ...de.defaultScreenCanvasOpt,
            width: d.width,
            height: d.height
          };
          this.snapshotThread = this.createSnapshotThread(a, {
            displayer: m,
            container: g,
            layerOpt: I,
            master: this.master,
            canvasOpt: S,
            post: this.post
          }), this.snapshotThread.on(r).then(() => {
            this.snapshotThread = void 0, g.innerHTML = "", g.style.width = "", g.style.height = "";
          });
          continue;
        }
      }
      if (n === w.GetImageBitMap && u && this.snapshotThread) {
        this.snapshotThread.on(r);
        continue;
      }
      if (n === w.TasksQueue && (c != null && c.size)) {
        for (const [m, g] of this.mainThreadMap.entries()) {
          const I = c.get(m);
          I && (await g.on(I), l && this.post({ workerTasksqueueCount: l }));
        }
        continue;
      }
      if (a === ds) {
        for (const m of this.mainThreadMap.values())
          m.on(r), n === w.Destroy && this.mainThreadMap.delete(a);
        continue;
      }
      const f = this.mainThreadMap.get(a);
      f && (f.on(r), n === w.Destroy && this.mainThreadMap.delete(a));
    }
  }
}
export {
  Oo as MainThreadManagerImpl
};

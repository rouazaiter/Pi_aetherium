var Lr = Object.defineProperty;
var Pr = (r, n, e) => n in r ? Lr(r, n, { enumerable: !0, configurable: !0, writable: !0, value: e }) : r[n] = e;
var I = (r, n, e) => Pr(r, typeof n != "symbol" ? n + "" : n, e);
import { toJS as Vt, autorun as fn, isRoom as G, isPlayer as En, InvisiblePlugin as Zi, RoomPhase as $i } from "white-web-sdk";
import "spritejs";
import { compress as Or, decompress as Rr } from "lz-string";
import "clipper-lib";
import "lineclip";
import wn from "xss";
import { debounce as Pt, cloneDeep as se, isEqual as le, isSet as Be, isNumber as ee, isBoolean as We, throttle as Hi, omit as ti, clone as ni } from "lodash";
import Tn from "eventemitter2";
import m, { useContext as Ce, useState as ae, useMemo as J, useEffect as ge, useRef as xe, useCallback as pt } from "react";
import Ln, { createPortal as Yr } from "react-dom";
import vt from "react-draggable";
import { Resizable as Ur } from "re-resizable";
const wI = "/fullWorker.js", TI = "/subWorker.js";
var ne = /* @__PURE__ */ ((r) => (r.selector = "selector", r.clicker = "clicker", r.laserPointer = "laserPointer", r.pencil = "pencil", r.rectangle = "rectangle", r.ellipse = "ellipse", r.shape = "shape", r.eraser = "eraser", r.pencilEraser = "pencilEraser", r.text = "text", r.straight = "straight", r.arrow = "arrow", r.hand = "hand", r.laserPen = "laserPen", r))(ne || {}), me = /* @__PURE__ */ ((r) => (r.Normal = "Normal", r.Stroke = "Stroke", r.Dotted = "Dotted", r.LongDotted = "LongDotted", r))(me || {}), qe = /* @__PURE__ */ ((r) => (r.Triangle = "triangle", r.Rhombus = "rhombus", r.Pentagram = "pentagram", r.SpeechBalloon = "speechBalloon", r.Star = "star", r.Polygon = "polygon", r))(qe || {}), W = /* @__PURE__ */ ((r) => (r.None = "None", r.ShowFloatBar = "ShowFloatBar", r.ZIndexFloatBar = "ZIndexFloatBar", r.DeleteNode = "DeleteNode", r.CopyNode = "CopyNode", r.ZIndexNode = "ZIndexNode", r.ZIndexBackgroundSVG = "ZIndexBackgroundSVG", r.RotateNode = "RotateNode", r.SetColorNode = "SetColorNode", r.TranslateNode = "TranslateNode", r.ScaleNode = "ScaleNode", r.OriginalEvent = "OriginalEvent", r.CreateScene = "CreateScene", r.ActiveCursor = "ActiveCursor", r.MoveCursor = "MoveCursor", r.CommandEditor = "CommandEditor", r.SetEditorData = "SetEditorData", r.SetFontStyle = "SetFontStyle", r.SetPoint = "SetPoint", r.SetLock = "SetLock", r.SetShapeOpt = "SetShapeOpt", r.CameraChange = "CameraChange", r.ActiveMethod = "ActiveMethod", r.HidFloatBarBtn = "HidFloatBarBtn", r))(W || {}), te = /* @__PURE__ */ ((r) => (r.MainEngine = "MainEngine", r.Cursor = "Cursor", r.BindMainView = "BindMainView", r))(te || {}), Br = /* @__PURE__ */ ((r) => (r[r.MainView = 0] = "MainView", r[r.Plugin = 1] = "Plugin", r[r.Both = 2] = "Both", r))(Br || {}), Ji = /* @__PURE__ */ ((r) => (r.longPencil = "longPencil", r))(Ji || {}), T = /* @__PURE__ */ ((r) => (r[r.Pencil = 1] = "Pencil", r[r.Eraser = 2] = "Eraser", r[r.PencilEraser = 3] = "PencilEraser", r[r.BitMapEraser = 4] = "BitMapEraser", r[r.Selector = 5] = "Selector", r[r.Clicker = 6] = "Clicker", r[r.Arrow = 7] = "Arrow", r[r.Hand = 8] = "Hand", r[r.LaserPen = 9] = "LaserPen", r[r.Text = 10] = "Text", r[r.Straight = 11] = "Straight", r[r.Rectangle = 12] = "Rectangle", r[r.Ellipse = 13] = "Ellipse", r[r.Star = 14] = "Star", r[r.Triangle = 15] = "Triangle", r[r.Rhombus = 16] = "Rhombus", r[r.Polygon = 17] = "Polygon", r[r.SpeechBalloon = 18] = "SpeechBalloon", r[r.Image = 19] = "Image", r[r.BackgroundSVG = 20] = "BackgroundSVG", r))(T || {}), q = /* @__PURE__ */ ((r) => (r[r.Local = 1] = "Local", r[r.Service = 2] = "Service", r[r.Worker = 3] = "Worker", r))(q || {}), x = /* @__PURE__ */ ((r) => (r[r.Pending = 0] = "Pending", r[r.Start = 1] = "Start", r[r.Doing = 2] = "Doing", r[r.Done = 3] = "Done", r[r.Unwritable = 4] = "Unwritable", r))(x || {}), R = /* @__PURE__ */ ((r) => (r[r.None = 0] = "None", r[r.Init = 1] = "Init", r[r.UpdateCamera = 2] = "UpdateCamera", r[r.UpdateTools = 3] = "UpdateTools", r[r.CreateWork = 4] = "CreateWork", r[r.DrawWork = 5] = "DrawWork", r[r.FullWork = 6] = "FullWork", r[r.UpdateNode = 7] = "UpdateNode", r[r.RemoveNode = 8] = "RemoveNode", r[r.Clear = 9] = "Clear", r[r.Select = 10] = "Select", r[r.Destroy = 11] = "Destroy", r[r.Snapshot = 12] = "Snapshot", r[r.BoundingBox = 13] = "BoundingBox", r[r.Cursor = 14] = "Cursor", r[r.TextUpdate = 15] = "TextUpdate", r[r.GetTextActive = 16] = "GetTextActive", r[r.TasksQueue = 17] = "TasksQueue", r[r.CursorHover = 18] = "CursorHover", r[r.CursorBlur = 19] = "CursorBlur", r[r.Console = 20] = "Console", r[r.GetImageBitMap = 23] = "GetImageBitMap", r[r.AuthWriter = 24] = "AuthWriter", r[r.AuthReader = 25] = "AuthReader", r[r.AuthClear = 26] = "AuthClear", r[r.AuthPage = 27] = "AuthPage", r[r.GetVNodeInfo = 28] = "GetVNodeInfo", r[r.UpdateDpr = 29] = "UpdateDpr", r[r.SetFontFace = 30] = "SetFontFace", r[r.ReportError = 31] = "ReportError", r[r.ReportLog = 32] = "ReportLog", r[r.BackgroundSVGUpdate = 33] = "BackgroundSVGUpdate", r[r.BackgroundSVGDelete = 34] = "BackgroundSVGDelete", r))(R || {}), nt = /* @__PURE__ */ ((r) => (r[r.None = 0] = "None", r[r.Bg = 1] = "Bg", r[r.ServiceFloat = 2] = "ServiceFloat", r[r.Float = 3] = "Float", r[r.TopFloat = 4] = "TopFloat", r))(nt || {}), Qt = /* @__PURE__ */ ((r) => (r[r.Cursor = 1] = "Cursor", r[r.TextCreate = 2] = "TextCreate", r))(Qt || {}), Lt = /* @__PURE__ */ ((r) => (r[r.Top = 1] = "Top", r[r.Bottom = 2] = "Bottom", r))(Lt || {}), Fe = /* @__PURE__ */ ((r) => (r[r.none = 1] = "none", r[r.all = 2] = "all", r[r.both = 3] = "both", r[r.proportional = 4] = "proportional", r))(Fe || {}), Gt = /* @__PURE__ */ ((r) => (r[r.inside = 0] = "inside", r[r.outside = 1] = "outside", r[r.intersect = 2] = "intersect", r))(Gt || {}), be = /* @__PURE__ */ ((r) => (r.renderPage = "renderPage", r.addPage = "addPage", r.deletePage = "deletePage", r.addElement = "addElement", r.deleteElement = "deleteElement", r.clearPage = "clearPage", r.clearViewScenePath = "clearViewScenePath", r.clearView = "clearView", r.syncUndoStack = "syncUndoStack", r.syncRedoStack = "syncRedoStack", r))(be || {}), et = /* @__PURE__ */ ((r) => (r.setAuth = "setAuth", r.setShow = "addShow", r.setHide = "setHide", r.setClear = "setClear", r))(et || {}), Zt = /* @__PURE__ */ ((r) => (r.Image = "image", r.Iconify = "iconify", r))(Zt || {}), $t = /* @__PURE__ */ ((r) => (r.url = "url", r.markmap = "markmap", r))($t || {});
const NI = "cursorhover", Xi = 20, pe = "mainView", Wr = {
  linear: (r) => r,
  easeInQuad: (r) => r * r,
  easeOutQuad: (r) => r * (2 - r),
  easeInOutQuad: (r) => r < 0.5 ? 2 * r * r : -1 + (4 - 2 * r) * r,
  easeInCubic: (r) => r * r * r,
  easeOutCubic: (r) => --r * r * r + 1,
  easeInOutCubic: (r) => r < 0.5 ? 4 * r * r * r : (r - 1) * (2 * r - 2) * (2 * r - 2) + 1,
  easeInQuart: (r) => r * r * r * r,
  easeOutQuart: (r) => 1 - --r * r * r * r,
  easeInOutQuart: (r) => r < 0.5 ? 8 * r * r * r * r : 1 - 8 * --r * r * r * r,
  easeInQuint: (r) => r * r * r * r * r,
  easeOutQuint: (r) => 1 + --r * r * r * r * r,
  easeInOutQuint: (r) => r < 0.5 ? 16 * r * r * r * r * r : 1 + 16 * --r * r * r * r * r,
  easeInSine: (r) => 1 - Math.cos(r * Math.PI / 2),
  easeOutSine: (r) => Math.sin(r * Math.PI / 2),
  easeInOutSine: (r) => -(Math.cos(Math.PI * r) - 1) / 2,
  easeInExpo: (r) => r <= 0 ? 0 : Math.pow(2, 10 * r - 10),
  easeOutExpo: (r) => r >= 1 ? 1 : 1 - Math.pow(2, -10 * r),
  easeInOutExpo: (r) => r <= 0 ? 0 : r >= 1 ? 1 : r < 0.5 ? Math.pow(2, 20 * r - 10) / 2 : (2 - Math.pow(2, -20 * r + 10)) / 2
};
class U {
  constructor(n = 0, e = 0, t = 1) {
    this.x = n, this.y = e, this.z = t;
  }
  get XY() {
    return [this.x, this.y];
  }
  setz(n) {
    return this.z = n, this;
  }
  setXY(n = this.x, e = this.y) {
    return this.x = n, this.y = e, this;
  }
  set(n = this.x, e = this.y, t = this.z) {
    return this.x = n, this.y = e, this.z = t, this;
  }
  setTo({ x: n = 0, y: e = 0, z: t = 1 }) {
    return this.x = n, this.y = e, this.z = t, this;
  }
  rot(n) {
    if (n === 0) return this;
    const { x: e, y: t } = this, i = Math.sin(n), o = Math.cos(n);
    return this.x = e * o - t * i, this.y = e * i + t * o, this;
  }
  rotWith(n, e) {
    if (e === 0) return this;
    const t = this.x - n.x, i = this.y - n.y, o = Math.sin(e), s = Math.cos(e);
    return this.x = n.x + (t * s - i * o), this.y = n.y + (t * o + i * s), this;
  }
  clone() {
    const { x: n, y: e, z: t } = this;
    return new U(n, e, t);
  }
  sub(n) {
    return this.x -= n.x, this.y -= n.y, this;
  }
  subXY(n, e) {
    return this.x -= n, this.y -= e, this;
  }
  subScalar(n) {
    return this.x -= n, this.y -= n, this;
  }
  add(n) {
    return this.x += n.x, this.y += n.y, this;
  }
  addXY(n, e) {
    return this.x += n, this.y += e, this;
  }
  addScalar(n) {
    return this.x += n, this.y += n, this;
  }
  clamp(n, e) {
    return this.x = Math.max(this.x, n), this.y = Math.max(this.y, n), e !== void 0 && (this.x = Math.min(this.x, e), this.y = Math.min(this.y, e)), this;
  }
  div(n) {
    return this.x /= n, this.y /= n, this;
  }
  divV(n) {
    return this.x /= n.x, this.y /= n.y, this;
  }
  mul(n) {
    return this.x *= n, this.y *= n, this;
  }
  mulV(n) {
    return this.x *= n.x, this.y *= n.y, this;
  }
  abs() {
    return this.x = Math.abs(this.x), this.y = Math.abs(this.y), this;
  }
  nudge(n, e) {
    const t = U.Tan(n, this);
    return this.add(t.mul(e));
  }
  neg() {
    return this.x *= -1, this.y *= -1, this;
  }
  cross(n) {
    return this.x = this.y * n.z - this.z * n.y, this.y = this.z * n.x - this.x * n.z, this;
  }
  dpr(n) {
    return U.Dpr(this, n);
  }
  cpr(n) {
    return U.Cpr(this, n);
  }
  len2() {
    return U.Len2(this);
  }
  len() {
    return U.Len(this);
  }
  pry(n) {
    return U.Pry(this, n);
  }
  per() {
    const { x: n, y: e } = this;
    return this.x = e, this.y = -n, this;
  }
  uni() {
    return U.Uni(this);
  }
  tan(n) {
    return U.Tan(this, n);
  }
  dist(n) {
    return U.Dist(this, n);
  }
  distanceToLineSegment(n, e) {
    return U.DistanceToLineSegment(n, e, this);
  }
  slope(n) {
    return U.Slope(this, n);
  }
  snapToGrid(n) {
    return this.x = Math.round(this.x / n) * n, this.y = Math.round(this.y / n) * n, this;
  }
  angle(n) {
    return U.Angle(this, n);
  }
  toAngle() {
    return U.ToAngle(this);
  }
  lrp(n, e) {
    return this.x = this.x + (n.x - this.x) * e, this.y = this.y + (n.y - this.y) * e, this;
  }
  equals(n, e) {
    return U.Equals(this, n, e);
  }
  equalsXY(n, e) {
    return U.EqualsXY(this, n, e);
  }
  norm() {
    const n = this.len();
    return this.x = n === 0 ? 0 : this.x / n, this.y = n === 0 ? 0 : this.y / n, this;
  }
  toFixed() {
    return U.ToFixed(this);
  }
  toString() {
    return U.ToString(U.ToFixed(this));
  }
  toJson() {
    return U.ToJson(this);
  }
  toArray() {
    return U.ToArray(this);
  }
  static Add(n, e) {
    return new U(n.x + e.x, n.y + e.y);
  }
  static AddXY(n, e, t) {
    return new U(n.x + e, n.y + t);
  }
  static Sub(n, e) {
    return new U(n.x - e.x, n.y - e.y);
  }
  static SubXY(n, e, t) {
    return new U(n.x - e, n.y - t);
  }
  static AddScalar(n, e) {
    return new U(n.x + e, n.y + e);
  }
  static SubScalar(n, e) {
    return new U(n.x - e, n.y - e);
  }
  static Div(n, e) {
    return new U(n.x / e, n.y / e);
  }
  static Mul(n, e) {
    return new U(n.x * e, n.y * e);
  }
  static DivV(n, e) {
    return new U(n.x / e.x, n.y / e.y);
  }
  static MulV(n, e) {
    return new U(n.x * e.x, n.y * e.y);
  }
  static Neg(n) {
    return new U(-n.x, -n.y);
  }
  static Per(n) {
    return new U(n.y, -n.x);
  }
  static Dist2(n, e) {
    return U.Sub(n, e).len2();
  }
  static Abs(n) {
    return new U(Math.abs(n.x), Math.abs(n.y));
  }
  static Dist(n, e) {
    return Math.hypot(n.y - e.y, n.x - e.x);
  }
  static Dpr(n, e) {
    return n.x * e.x + n.y * e.y;
  }
  static Cross(n, e) {
    return new U(
      n.y * e.z - n.z * e.y,
      n.z * e.x - n.x * e.z
      // A.z = A.x * V.y - A.y * V.x
    );
  }
  static Cpr(n, e) {
    return n.x * e.y - e.x * n.y;
  }
  static Len2(n) {
    return n.x * n.x + n.y * n.y;
  }
  static Len(n) {
    return Math.hypot(n.x, n.y);
  }
  static Pry(n, e) {
    return U.Dpr(n, e) / U.Len(e);
  }
  static Uni(n) {
    return U.Div(n, U.Len(n));
  }
  static Tan(n, e) {
    return U.Uni(U.Sub(n, e));
  }
  static Min(n, e) {
    return new U(Math.min(n.x, e.x), Math.min(n.y, e.y));
  }
  static Max(n, e) {
    return new U(Math.max(n.x, e.x), Math.max(n.y, e.y));
  }
  static From(n) {
    return new U().add(n);
  }
  static FromArray(n) {
    return new U(n[0], n[1]);
  }
  static Rot(n, e = 0) {
    const t = Math.sin(e), i = Math.cos(e);
    return new U(n.x * i - n.y * t, n.x * t + n.y * i);
  }
  static RotWith(n, e, t) {
    const i = n.x - e.x, o = n.y - e.y, s = Math.sin(t), a = Math.cos(t);
    return new U(e.x + (i * a - o * s), e.y + (i * s + o * a));
  }
  /**
   * Get the nearest point on a line with a known unit vector that passes through point A
   *
   * ```ts
   * Vec.nearestPointOnLineThroughPoint(A, u, Point)
   * ```
   *
   * @param A - Any point on the line
   * @param u - The unit vector for the line.
   * @param P - A point not on the line to test.
   */
  static NearestPointOnLineThroughPoint(n, e, t) {
    return U.Mul(e, U.Sub(t, n).pry(e)).add(n);
  }
  static NearestPointOnLineSegment(n, e, t, i = !0) {
    const o = U.Tan(e, n), s = U.Add(n, U.Mul(o, U.Sub(t, n).pry(o)));
    if (i) {
      if (s.x < Math.min(n.x, e.x)) return U.Cast(n.x < e.x ? n : e);
      if (s.x > Math.max(n.x, e.x)) return U.Cast(n.x > e.x ? n : e);
      if (s.y < Math.min(n.y, e.y)) return U.Cast(n.y < e.y ? n : e);
      if (s.y > Math.max(n.y, e.y)) return U.Cast(n.y > e.y ? n : e);
    }
    return s;
  }
  static DistanceToLineThroughPoint(n, e, t) {
    return U.Dist(t, U.NearestPointOnLineThroughPoint(n, e, t));
  }
  static DistanceToLineSegment(n, e, t, i = !0) {
    return U.Dist(t, U.NearestPointOnLineSegment(n, e, t, i));
  }
  static Snap(n, e = 1) {
    return new U(
      Math.round(n.x / e) * e,
      Math.round(n.y / e) * e
    );
  }
  static Cast(n) {
    return n instanceof U ? n : U.From(n);
  }
  static Slope(n, e) {
    return n.x === e.y ? NaN : (n.y - e.y) / (n.x - e.x);
  }
  static Angle(n, e) {
    return Math.atan2(e.y - n.y, e.x - n.x);
  }
  static Lrp(n, e, t) {
    return U.Sub(e, n).mul(t).add(n);
  }
  static Med(n, e) {
    return new U((n.x + e.x) / 2, (n.y + e.y) / 2);
  }
  static Equals(n, e, t = 1e-4) {
    return Math.abs(n.x - e.x) < t && Math.abs(n.y - e.y) < t;
  }
  static EqualsXY(n, e, t) {
    return n.x === e && n.y === t;
  }
  static EqualsXYZ(n, e, t = 1e-4) {
    return U.Equals(n, e, t) && Math.abs((n.z || 0) - (e.z || 0)) < t;
  }
  static Clockwise(n, e, t) {
    return (t.x - n.x) * (e.y - n.y) - (e.x - n.x) * (t.y - n.y) < 0;
  }
  static Rescale(n, e) {
    const t = U.Len(n);
    return new U(e * n.x / t, e * n.y / t);
  }
  static ScaleWithOrigin(n, e, t) {
    return U.Sub(n, t).mul(e).add(t);
  }
  static ScaleWOrigin(n, e, t) {
    return U.Sub(n, t).mulV(e).add(t);
  }
  static ToFixed(n, e = 2) {
    return new U(+n.x.toFixed(e), +n.y.toFixed(e), +n.z.toFixed(e));
  }
  static Nudge(n, e, t) {
    return U.Add(n, U.Tan(e, n).mul(t));
  }
  static ToString(n) {
    return `${n.x}, ${n.y}`;
  }
  static ToAngle(n) {
    let e = Math.atan2(n.y, n.x);
    return e < 0 && (e += Math.PI * 2), e;
  }
  static FromAngle(n, e = 1) {
    return new U(Math.cos(n) * e, Math.sin(n) * e);
  }
  static ToArray(n) {
    return [n.x, n.y, n.z];
  }
  static ToJson(n) {
    const { x: e, y: t, z: i } = n;
    return { x: e, y: t, z: i };
  }
  static Average(n) {
    const e = n.length, t = new U(0, 0);
    for (let i = 0; i < e; i++)
      t.add(n[i]);
    return t.div(e);
  }
  static Clamp(n, e, t) {
    return t === void 0 ? new U(
      Math.min(Math.max(n.x, e)),
      Math.min(Math.max(n.y, e))
    ) : new U(
      Math.min(Math.max(n.x, e), t),
      Math.min(Math.max(n.y, e), t)
    );
  }
  /**
   * Get an array of points (with simulated pressure) between two points.
   *
   * @param A - The first point.
   * @param B - The second point.
   * @param steps - The number of points to return.
   */
  static PointsBetween(n, e, t = 6) {
    const i = [];
    for (let o = 0; o < t; o++) {
      const s = Wr.easeInQuad(o / (t - 1)), a = U.Lrp(n, e, s);
      a.z = Math.min(1, 0.5 + Math.abs(0.5 - Fr(s)) * 0.65), i.push(a);
    }
    return i;
  }
  static SnapToGrid(n, e = 8) {
    return new U(
      Math.round(n.x / e) * e,
      Math.round(n.y / e) * e
    );
  }
}
const Fr = (r) => r < 0.5 ? 2 * r * r : -1 + (4 - 2 * r) * r;
class ve extends U {
  constructor(n = 0, e = 0, t = 0, i = { x: 0, y: 0 }, o = 0, s = 0) {
    super(n, e, t), this.x = n, this.y = e, this.z = t, this.v = i, this.t = o, this.a = s;
  }
  get timestamp() {
    return this.t;
  }
  get pressure() {
    return this.z;
  }
  get angleNum() {
    return this.a;
  }
  get XY() {
    return [this.x, this.y];
  }
  setA(n) {
    this.a = n;
  }
  setT(n) {
    this.t = n;
  }
  setv(n) {
    return this.v = { x: n.x, y: n.y }, this;
  }
  set(n = this.x, e = this.y, t = this.z, i = this.v, o = this.t, s = this.a) {
    return this.x = n, this.y = e, this.z = t, this.v = i, this.t = o, this.a = s, this;
  }
  clone() {
    const { x: n, y: e, z: t, v: i, t: o, a: s } = this, a = { x: i.x, y: i.y };
    return new ve(n, e, t, a, o, s);
  }
  distance(n) {
    return ve.GetDistance(this, n);
  }
  isNear(n, e) {
    return ve.IsNear(this, n, e);
  }
  getAngleByPoints(n, e) {
    return ve.GetAngleByPoints(n, this, e);
  }
  static Sub(n, e) {
    return new ve(n.x - e.x, n.y - e.y);
  }
  static Add(n, e) {
    return new ve(n.x + e.x, n.y + e.y);
  }
  static GetDistance(n, e) {
    return ve.Len(n.clone().sub(e));
  }
  static GetAngleByPoints(n, e, t) {
    const i = e.x - n.x, o = t.x - e.x, s = e.y - n.y, a = t.y - e.y;
    let c = 0;
    const l = Math.sqrt(i * i + s * s), h = Math.sqrt(o * o + a * a);
    if (l && h) {
      const u = i * o + s * a;
      c = Math.acos(u / (l * h)), c = c / Math.PI * 180;
      let g = i * a - s * o;
      g = g > 0 ? 1 : -1, c = 180 + g * c;
    }
    return c;
  }
  static IsNear(n, e, t) {
    return ve.Len(n.clone().sub(e)) < t;
  }
  static RotWith(n, e, t, i = 2) {
    const o = n.x - e.x, s = n.y - e.y, a = Math.sin(t), c = Math.cos(t), l = Math.pow(10, i), h = Math.floor((e.x + (o * c - s * a)) * l) / l, u = Math.floor((e.y + (o * a + s * c)) * l) / l;
    return new ve(h, u);
  }
  /**
   * 根据圆心和半径，获取圆上的等份点
   * @param o 圆心
   * @param radius 半径
   * @param average 均分数
   * @returns
   */
  static GetDotStroke(n, e, t = 16) {
    const i = new U(1, 1), o = Math.PI + 1e-3, s = ve.Add(n, ve.Sub(n, i).uni().per().mul(-e)), a = [];
    for (let c = 1 / t, l = c; l <= 1; l += c)
      a.push(ve.RotWith(s, n, o * 2 * l));
    return a;
  }
  /**
   * 根据圆心和圆上的起始点，获取半圆上的等份点
   * @param o 圆心
   * @param p 圆弧起始点
   * @param radian 1，逆时针180度 -1，顺时针
   * @param average 均分数
   * @returns
   */
  static GetSemicircleStroke(n, e, t = -1, i = 8) {
    const o = t * (Math.PI + 1e-3), s = [];
    for (let a = 1 / i, c = a; c <= 1; c += a)
      s.push(ve.RotWith(e, n, o * c));
    return s;
  }
}
var Bt = typeof globalThis < "u" ? globalThis : typeof window < "u" ? window : typeof global < "u" ? global : typeof self < "u" ? self : {};
function Pn(r) {
  return r && r.__esModule && Object.prototype.hasOwnProperty.call(r, "default") ? r.default : r;
}
var Vr = typeof Bt == "object" && Bt && Bt.Object === Object && Bt, Ki = Vr, Qr = Ki, Gr = typeof self == "object" && self && self.Object === Object && self, Zr = Qr || Gr || Function("return this")(), He = Zr, $r = He, Hr = $r.Symbol, Ot = Hr, ii = Ot, qi = Object.prototype, Jr = qi.hasOwnProperty, Xr = qi.toString, bt = ii ? ii.toStringTag : void 0;
function Kr(r) {
  var n = Jr.call(r, bt), e = r[bt];
  try {
    r[bt] = void 0;
    var t = !0;
  } catch {
  }
  var i = Xr.call(r);
  return t && (n ? r[bt] = e : delete r[bt]), i;
}
var qr = Kr, _r = Object.prototype, eo = _r.toString;
function to(r) {
  return eo.call(r);
}
var no = to, ri = Ot, io = qr, ro = no, oo = "[object Null]", so = "[object Undefined]", oi = ri ? ri.toStringTag : void 0;
function ao(r) {
  return r == null ? r === void 0 ? so : oo : oi && oi in Object(r) ? io(r) : ro(r);
}
var Rt = ao;
function co(r) {
  return r != null && typeof r == "object";
}
var yt = co;
function lo() {
  this.__data__ = [], this.size = 0;
}
var uo = lo;
function ho(r, n) {
  return r === n || r !== r && n !== n;
}
var On = ho, go = On;
function fo(r, n) {
  for (var e = r.length; e--; )
    if (go(r[e][0], n))
      return e;
  return -1;
}
var nn = fo, Mo = nn, po = Array.prototype, yo = po.splice;
function Io(r) {
  var n = this.__data__, e = Mo(n, r);
  if (e < 0)
    return !1;
  var t = n.length - 1;
  return e == t ? n.pop() : yo.call(n, e, 1), --this.size, !0;
}
var mo = Io, wo = nn;
function To(r) {
  var n = this.__data__, e = wo(n, r);
  return e < 0 ? void 0 : n[e][1];
}
var No = To, vo = nn;
function Co(r) {
  return vo(this.__data__, r) > -1;
}
var So = Co, ko = nn;
function xo(r, n) {
  var e = this.__data__, t = ko(e, r);
  return t < 0 ? (++this.size, e.push([r, n])) : e[t][1] = n, this;
}
var jo = xo, Do = uo, zo = mo, bo = No, Ao = So, Eo = jo;
function Ct(r) {
  var n = -1, e = r == null ? 0 : r.length;
  for (this.clear(); ++n < e; ) {
    var t = r[n];
    this.set(t[0], t[1]);
  }
}
Ct.prototype.clear = Do;
Ct.prototype.delete = zo;
Ct.prototype.get = bo;
Ct.prototype.has = Ao;
Ct.prototype.set = Eo;
var rn = Ct, Lo = rn;
function Po() {
  this.__data__ = new Lo(), this.size = 0;
}
var Oo = Po;
function Ro(r) {
  var n = this.__data__, e = n.delete(r);
  return this.size = n.size, e;
}
var Yo = Ro;
function Uo(r) {
  return this.__data__.get(r);
}
var Bo = Uo;
function Wo(r) {
  return this.__data__.has(r);
}
var Fo = Wo;
function Vo(r) {
  var n = typeof r;
  return r != null && (n == "object" || n == "function");
}
var ut = Vo, Qo = Rt, Go = ut, Zo = "[object AsyncFunction]", $o = "[object Function]", Ho = "[object GeneratorFunction]", Jo = "[object Proxy]";
function Xo(r) {
  if (!Go(r))
    return !1;
  var n = Qo(r);
  return n == $o || n == Ho || n == Zo || n == Jo;
}
var _i = Xo, Ko = He, qo = Ko["__core-js_shared__"], _o = qo, Mn = _o, si = function() {
  var r = /[^.]+$/.exec(Mn && Mn.keys && Mn.keys.IE_PROTO || "");
  return r ? "Symbol(src)_1." + r : "";
}();
function es(r) {
  return !!si && si in r;
}
var ts = es, ns = Function.prototype, is = ns.toString;
function rs(r) {
  if (r != null) {
    try {
      return is.call(r);
    } catch {
    }
    try {
      return r + "";
    } catch {
    }
  }
  return "";
}
var er = rs, os = _i, ss = ts, as = ut, cs = er, ls = /[\\^$.*+?()[\]{}|]/g, us = /^\[object .+?Constructor\]$/, hs = Function.prototype, ds = Object.prototype, gs = hs.toString, fs = ds.hasOwnProperty, Ms = RegExp(
  "^" + gs.call(fs).replace(ls, "\\$&").replace(/hasOwnProperty|(function).*?(?=\\\()| for .+?(?=\\\])/g, "$1.*?") + "$"
);
function ps(r) {
  if (!as(r) || ss(r))
    return !1;
  var n = os(r) ? Ms : us;
  return n.test(cs(r));
}
var ys = ps;
function Is(r, n) {
  return r == null ? void 0 : r[n];
}
var ms = Is, ws = ys, Ts = ms;
function Ns(r, n) {
  var e = Ts(r, n);
  return ws(e) ? e : void 0;
}
var It = Ns, vs = It, Cs = He, Ss = vs(Cs, "Map"), Rn = Ss, ks = It, xs = ks(Object, "create"), on = xs, ai = on;
function js() {
  this.__data__ = ai ? ai(null) : {}, this.size = 0;
}
var Ds = js;
function zs(r) {
  var n = this.has(r) && delete this.__data__[r];
  return this.size -= n ? 1 : 0, n;
}
var bs = zs, As = on, Es = "__lodash_hash_undefined__", Ls = Object.prototype, Ps = Ls.hasOwnProperty;
function Os(r) {
  var n = this.__data__;
  if (As) {
    var e = n[r];
    return e === Es ? void 0 : e;
  }
  return Ps.call(n, r) ? n[r] : void 0;
}
var Rs = Os, Ys = on, Us = Object.prototype, Bs = Us.hasOwnProperty;
function Ws(r) {
  var n = this.__data__;
  return Ys ? n[r] !== void 0 : Bs.call(n, r);
}
var Fs = Ws, Vs = on, Qs = "__lodash_hash_undefined__";
function Gs(r, n) {
  var e = this.__data__;
  return this.size += this.has(r) ? 0 : 1, e[r] = Vs && n === void 0 ? Qs : n, this;
}
var Zs = Gs, $s = Ds, Hs = bs, Js = Rs, Xs = Fs, Ks = Zs;
function St(r) {
  var n = -1, e = r == null ? 0 : r.length;
  for (this.clear(); ++n < e; ) {
    var t = r[n];
    this.set(t[0], t[1]);
  }
}
St.prototype.clear = $s;
St.prototype.delete = Hs;
St.prototype.get = Js;
St.prototype.has = Xs;
St.prototype.set = Ks;
var qs = St, ci = qs, _s = rn, ea = Rn;
function ta() {
  this.size = 0, this.__data__ = {
    hash: new ci(),
    map: new (ea || _s)(),
    string: new ci()
  };
}
var na = ta;
function ia(r) {
  var n = typeof r;
  return n == "string" || n == "number" || n == "symbol" || n == "boolean" ? r !== "__proto__" : r === null;
}
var ra = ia, oa = ra;
function sa(r, n) {
  var e = r.__data__;
  return oa(n) ? e[typeof n == "string" ? "string" : "hash"] : e.map;
}
var sn = sa, aa = sn;
function ca(r) {
  var n = aa(this, r).delete(r);
  return this.size -= n ? 1 : 0, n;
}
var la = ca, ua = sn;
function ha(r) {
  return ua(this, r).get(r);
}
var da = ha, ga = sn;
function fa(r) {
  return ga(this, r).has(r);
}
var Ma = fa, pa = sn;
function ya(r, n) {
  var e = pa(this, r), t = e.size;
  return e.set(r, n), this.size += e.size == t ? 0 : 1, this;
}
var Ia = ya, ma = na, wa = la, Ta = da, Na = Ma, va = Ia;
function kt(r) {
  var n = -1, e = r == null ? 0 : r.length;
  for (this.clear(); ++n < e; ) {
    var t = r[n];
    this.set(t[0], t[1]);
  }
}
kt.prototype.clear = ma;
kt.prototype.delete = wa;
kt.prototype.get = Ta;
kt.prototype.has = Na;
kt.prototype.set = va;
var tr = kt, Ca = rn, Sa = Rn, ka = tr, xa = 200;
function ja(r, n) {
  var e = this.__data__;
  if (e instanceof Ca) {
    var t = e.__data__;
    if (!Sa || t.length < xa - 1)
      return t.push([r, n]), this.size = ++e.size, this;
    e = this.__data__ = new ka(t);
  }
  return e.set(r, n), this.size = e.size, this;
}
var Da = ja, za = rn, ba = Oo, Aa = Yo, Ea = Bo, La = Fo, Pa = Da;
function xt(r) {
  var n = this.__data__ = new za(r);
  this.size = n.size;
}
xt.prototype.clear = ba;
xt.prototype.delete = Aa;
xt.prototype.get = Ea;
xt.prototype.has = La;
xt.prototype.set = Pa;
var Oa = xt;
function Ra(r, n) {
  for (var e = -1, t = r == null ? 0 : r.length; ++e < t && n(r[e], e, r) !== !1; )
    ;
  return r;
}
var Ya = Ra, Ua = It, Ba = function() {
  try {
    var r = Ua(Object, "defineProperty");
    return r({}, "", {}), r;
  } catch {
  }
}(), nr = Ba, li = nr;
function Wa(r, n, e) {
  n == "__proto__" && li ? li(r, n, {
    configurable: !0,
    enumerable: !0,
    value: e,
    writable: !0
  }) : r[n] = e;
}
var ir = Wa, Fa = ir, Va = On, Qa = Object.prototype, Ga = Qa.hasOwnProperty;
function Za(r, n, e) {
  var t = r[n];
  (!(Ga.call(r, n) && Va(t, e)) || e === void 0 && !(n in r)) && Fa(r, n, e);
}
var rr = Za, $a = rr, Ha = ir;
function Ja(r, n, e, t) {
  var i = !e;
  e || (e = {});
  for (var o = -1, s = n.length; ++o < s; ) {
    var a = n[o], c = t ? t(e[a], r[a], a, e, r) : void 0;
    c === void 0 && (c = r[a]), i ? Ha(e, a, c) : $a(e, a, c);
  }
  return e;
}
var an = Ja;
function Xa(r, n) {
  for (var e = -1, t = Array(r); ++e < r; )
    t[e] = n(e);
  return t;
}
var Ka = Xa, qa = Rt, _a = yt, ec = "[object Arguments]";
function tc(r) {
  return _a(r) && qa(r) == ec;
}
var nc = tc, ui = nc, ic = yt, or = Object.prototype, rc = or.hasOwnProperty, oc = or.propertyIsEnumerable, sc = ui(/* @__PURE__ */ function() {
  return arguments;
}()) ? ui : function(r) {
  return ic(r) && rc.call(r, "callee") && !oc.call(r, "callee");
}, sr = sc, ac = Array.isArray, cn = ac, Xt = { exports: {} };
function cc() {
  return !1;
}
var lc = cc;
Xt.exports;
(function(r, n) {
  var e = He, t = lc, i = n && !n.nodeType && n, o = i && !0 && r && !r.nodeType && r, s = o && o.exports === i, a = s ? e.Buffer : void 0, c = a ? a.isBuffer : void 0, l = c || t;
  r.exports = l;
})(Xt, Xt.exports);
var ar = Xt.exports, uc = 9007199254740991, hc = /^(?:0|[1-9]\d*)$/;
function dc(r, n) {
  var e = typeof r;
  return n = n ?? uc, !!n && (e == "number" || e != "symbol" && hc.test(r)) && r > -1 && r % 1 == 0 && r < n;
}
var gc = dc, fc = 9007199254740991;
function Mc(r) {
  return typeof r == "number" && r > -1 && r % 1 == 0 && r <= fc;
}
var cr = Mc, pc = Rt, yc = cr, Ic = yt, mc = "[object Arguments]", wc = "[object Array]", Tc = "[object Boolean]", Nc = "[object Date]", vc = "[object Error]", Cc = "[object Function]", Sc = "[object Map]", kc = "[object Number]", xc = "[object Object]", jc = "[object RegExp]", Dc = "[object Set]", zc = "[object String]", bc = "[object WeakMap]", Ac = "[object ArrayBuffer]", Ec = "[object DataView]", Lc = "[object Float32Array]", Pc = "[object Float64Array]", Oc = "[object Int8Array]", Rc = "[object Int16Array]", Yc = "[object Int32Array]", Uc = "[object Uint8Array]", Bc = "[object Uint8ClampedArray]", Wc = "[object Uint16Array]", Fc = "[object Uint32Array]", we = {};
we[Lc] = we[Pc] = we[Oc] = we[Rc] = we[Yc] = we[Uc] = we[Bc] = we[Wc] = we[Fc] = !0;
we[mc] = we[wc] = we[Ac] = we[Tc] = we[Ec] = we[Nc] = we[vc] = we[Cc] = we[Sc] = we[kc] = we[xc] = we[jc] = we[Dc] = we[zc] = we[bc] = !1;
function Vc(r) {
  return Ic(r) && yc(r.length) && !!we[pc(r)];
}
var Qc = Vc;
function Gc(r) {
  return function(n) {
    return r(n);
  };
}
var ln = Gc, Kt = { exports: {} };
Kt.exports;
(function(r, n) {
  var e = Ki, t = n && !n.nodeType && n, i = t && !0 && r && !r.nodeType && r, o = i && i.exports === t, s = o && e.process, a = function() {
    try {
      var c = i && i.require && i.require("util").types;
      return c || s && s.binding && s.binding("util");
    } catch {
    }
  }();
  r.exports = a;
})(Kt, Kt.exports);
var Yn = Kt.exports, Zc = Qc, $c = ln, hi = Yn, di = hi && hi.isTypedArray, Hc = di ? $c(di) : Zc, Jc = Hc, Xc = Ka, Kc = sr, qc = cn, _c = ar, el = gc, tl = Jc, nl = Object.prototype, il = nl.hasOwnProperty;
function rl(r, n) {
  var e = qc(r), t = !e && Kc(r), i = !e && !t && _c(r), o = !e && !t && !i && tl(r), s = e || t || i || o, a = s ? Xc(r.length, String) : [], c = a.length;
  for (var l in r)
    (n || il.call(r, l)) && !(s && // Safari 9 has enumerable `arguments.length` in strict mode.
    (l == "length" || // Node.js 0.10 has enumerable non-index properties on buffers.
    i && (l == "offset" || l == "parent") || // PhantomJS 2 has enumerable non-index properties on typed arrays.
    o && (l == "buffer" || l == "byteLength" || l == "byteOffset") || // Skip index properties.
    el(l, c))) && a.push(l);
  return a;
}
var lr = rl, ol = Object.prototype;
function sl(r) {
  var n = r && r.constructor, e = typeof n == "function" && n.prototype || ol;
  return r === e;
}
var Un = sl;
function al(r, n) {
  return function(e) {
    return r(n(e));
  };
}
var ur = al, cl = ur, ll = cl(Object.keys, Object), ul = ll, hl = Un, dl = ul, gl = Object.prototype, fl = gl.hasOwnProperty;
function Ml(r) {
  if (!hl(r))
    return dl(r);
  var n = [];
  for (var e in Object(r))
    fl.call(r, e) && e != "constructor" && n.push(e);
  return n;
}
var pl = Ml, yl = _i, Il = cr;
function ml(r) {
  return r != null && Il(r.length) && !yl(r);
}
var Bn = ml, wl = lr, Tl = pl, Nl = Bn;
function vl(r) {
  return Nl(r) ? wl(r) : Tl(r);
}
var Wn = vl, Cl = an, Sl = Wn;
function kl(r, n) {
  return r && Cl(n, Sl(n), r);
}
var xl = kl;
function jl(r) {
  var n = [];
  if (r != null)
    for (var e in Object(r))
      n.push(e);
  return n;
}
var Dl = jl, zl = ut, bl = Un, Al = Dl, El = Object.prototype, Ll = El.hasOwnProperty;
function Pl(r) {
  if (!zl(r))
    return Al(r);
  var n = bl(r), e = [];
  for (var t in r)
    t == "constructor" && (n || !Ll.call(r, t)) || e.push(t);
  return e;
}
var Ol = Pl, Rl = lr, Yl = Ol, Ul = Bn;
function Bl(r) {
  return Ul(r) ? Rl(r, !0) : Yl(r);
}
var Fn = Bl, Wl = an, Fl = Fn;
function Vl(r, n) {
  return r && Wl(n, Fl(n), r);
}
var Ql = Vl, qt = { exports: {} };
qt.exports;
(function(r, n) {
  var e = He, t = n && !n.nodeType && n, i = t && !0 && r && !r.nodeType && r, o = i && i.exports === t, s = o ? e.Buffer : void 0, a = s ? s.allocUnsafe : void 0;
  function c(l, h) {
    if (h)
      return l.slice();
    var u = l.length, g = a ? a(u) : new l.constructor(u);
    return l.copy(g), g;
  }
  r.exports = c;
})(qt, qt.exports);
var Gl = qt.exports;
function Zl(r, n) {
  var e = -1, t = r.length;
  for (n || (n = Array(t)); ++e < t; )
    n[e] = r[e];
  return n;
}
var $l = Zl;
function Hl(r, n) {
  for (var e = -1, t = r == null ? 0 : r.length, i = 0, o = []; ++e < t; ) {
    var s = r[e];
    n(s, e, r) && (o[i++] = s);
  }
  return o;
}
var hr = Hl;
function Jl() {
  return [];
}
var dr = Jl, Xl = hr, Kl = dr, ql = Object.prototype, _l = ql.propertyIsEnumerable, gi = Object.getOwnPropertySymbols, eu = gi ? function(r) {
  return r == null ? [] : (r = Object(r), Xl(gi(r), function(n) {
    return _l.call(r, n);
  }));
} : Kl, Vn = eu, tu = an, nu = Vn;
function iu(r, n) {
  return tu(r, nu(r), n);
}
var ru = iu;
function ou(r, n) {
  for (var e = -1, t = n.length, i = r.length; ++e < t; )
    r[i + e] = n[e];
  return r;
}
var Qn = ou, su = ur, au = su(Object.getPrototypeOf, Object), gr = au, cu = Qn, lu = gr, uu = Vn, hu = dr, du = Object.getOwnPropertySymbols, gu = du ? function(r) {
  for (var n = []; r; )
    cu(n, uu(r)), r = lu(r);
  return n;
} : hu, fr = gu, fu = an, Mu = fr;
function pu(r, n) {
  return fu(r, Mu(r), n);
}
var yu = pu, Iu = Qn, mu = cn;
function wu(r, n, e) {
  var t = n(r);
  return mu(r) ? t : Iu(t, e(r));
}
var Mr = wu, Tu = Mr, Nu = Vn, vu = Wn;
function Cu(r) {
  return Tu(r, vu, Nu);
}
var Su = Cu, ku = Mr, xu = fr, ju = Fn;
function Du(r) {
  return ku(r, ju, xu);
}
var zu = Du, bu = It, Au = He, Eu = bu(Au, "DataView"), Lu = Eu, Pu = It, Ou = He, Ru = Pu(Ou, "Promise"), Yu = Ru, Uu = It, Bu = He, Wu = Uu(Bu, "Set"), pr = Wu, Fu = It, Vu = He, Qu = Fu(Vu, "WeakMap"), Gu = Qu, Nn = Lu, vn = Rn, Cn = Yu, Sn = pr, kn = Gu, yr = Rt, jt = er, fi = "[object Map]", Zu = "[object Object]", Mi = "[object Promise]", pi = "[object Set]", yi = "[object WeakMap]", Ii = "[object DataView]", $u = jt(Nn), Hu = jt(vn), Ju = jt(Cn), Xu = jt(Sn), Ku = jt(kn), gt = yr;
(Nn && gt(new Nn(new ArrayBuffer(1))) != Ii || vn && gt(new vn()) != fi || Cn && gt(Cn.resolve()) != Mi || Sn && gt(new Sn()) != pi || kn && gt(new kn()) != yi) && (gt = function(r) {
  var n = yr(r), e = n == Zu ? r.constructor : void 0, t = e ? jt(e) : "";
  if (t)
    switch (t) {
      case $u:
        return Ii;
      case Hu:
        return fi;
      case Ju:
        return Mi;
      case Xu:
        return pi;
      case Ku:
        return yi;
    }
  return n;
});
var Gn = gt, qu = Object.prototype, _u = qu.hasOwnProperty;
function eh(r) {
  var n = r.length, e = new r.constructor(n);
  return n && typeof r[0] == "string" && _u.call(r, "index") && (e.index = r.index, e.input = r.input), e;
}
var th = eh, nh = He, ih = nh.Uint8Array, Ir = ih, mi = Ir;
function rh(r) {
  var n = new r.constructor(r.byteLength);
  return new mi(n).set(new mi(r)), n;
}
var Zn = rh, oh = Zn;
function sh(r, n) {
  var e = n ? oh(r.buffer) : r.buffer;
  return new r.constructor(e, r.byteOffset, r.byteLength);
}
var ah = sh, ch = /\w*$/;
function lh(r) {
  var n = new r.constructor(r.source, ch.exec(r));
  return n.lastIndex = r.lastIndex, n;
}
var uh = lh, wi = Ot, Ti = wi ? wi.prototype : void 0, Ni = Ti ? Ti.valueOf : void 0;
function hh(r) {
  return Ni ? Object(Ni.call(r)) : {};
}
var dh = hh, gh = Zn;
function fh(r, n) {
  var e = n ? gh(r.buffer) : r.buffer;
  return new r.constructor(e, r.byteOffset, r.length);
}
var Mh = fh, ph = Zn, yh = ah, Ih = uh, mh = dh, wh = Mh, Th = "[object Boolean]", Nh = "[object Date]", vh = "[object Map]", Ch = "[object Number]", Sh = "[object RegExp]", kh = "[object Set]", xh = "[object String]", jh = "[object Symbol]", Dh = "[object ArrayBuffer]", zh = "[object DataView]", bh = "[object Float32Array]", Ah = "[object Float64Array]", Eh = "[object Int8Array]", Lh = "[object Int16Array]", Ph = "[object Int32Array]", Oh = "[object Uint8Array]", Rh = "[object Uint8ClampedArray]", Yh = "[object Uint16Array]", Uh = "[object Uint32Array]";
function Bh(r, n, e) {
  var t = r.constructor;
  switch (n) {
    case Dh:
      return ph(r);
    case Th:
    case Nh:
      return new t(+r);
    case zh:
      return yh(r, e);
    case bh:
    case Ah:
    case Eh:
    case Lh:
    case Ph:
    case Oh:
    case Rh:
    case Yh:
    case Uh:
      return wh(r, e);
    case vh:
      return new t();
    case Ch:
    case xh:
      return new t(r);
    case Sh:
      return Ih(r);
    case kh:
      return new t();
    case jh:
      return mh(r);
  }
}
var Wh = Bh, Fh = ut, vi = Object.create, Vh = /* @__PURE__ */ function() {
  function r() {
  }
  return function(n) {
    if (!Fh(n))
      return {};
    if (vi)
      return vi(n);
    r.prototype = n;
    var e = new r();
    return r.prototype = void 0, e;
  };
}(), Qh = Vh, Gh = Qh, Zh = gr, $h = Un;
function Hh(r) {
  return typeof r.constructor == "function" && !$h(r) ? Gh(Zh(r)) : {};
}
var Jh = Hh, Xh = Gn, Kh = yt, qh = "[object Map]";
function _h(r) {
  return Kh(r) && Xh(r) == qh;
}
var ed = _h, td = ed, nd = ln, Ci = Yn, Si = Ci && Ci.isMap, id = Si ? nd(Si) : td, rd = id, od = Gn, sd = yt, ad = "[object Set]";
function cd(r) {
  return sd(r) && od(r) == ad;
}
var ld = cd, ud = ld, hd = ln, ki = Yn, xi = ki && ki.isSet, dd = xi ? hd(xi) : ud, mr = dd;
const vI = /* @__PURE__ */ Pn(mr);
var gd = Oa, fd = Ya, Md = rr, pd = xl, yd = Ql, Id = Gl, md = $l, wd = ru, Td = yu, Nd = Su, vd = zu, Cd = Gn, Sd = th, kd = Wh, xd = Jh, jd = cn, Dd = ar, zd = rd, bd = ut, Ad = mr, Ed = Wn, Ld = Fn, Pd = 1, Od = 2, Rd = 4, wr = "[object Arguments]", Yd = "[object Array]", Ud = "[object Boolean]", Bd = "[object Date]", Wd = "[object Error]", Tr = "[object Function]", Fd = "[object GeneratorFunction]", Vd = "[object Map]", Qd = "[object Number]", Nr = "[object Object]", Gd = "[object RegExp]", Zd = "[object Set]", $d = "[object String]", Hd = "[object Symbol]", Jd = "[object WeakMap]", Xd = "[object ArrayBuffer]", Kd = "[object DataView]", qd = "[object Float32Array]", _d = "[object Float64Array]", eg = "[object Int8Array]", tg = "[object Int16Array]", ng = "[object Int32Array]", ig = "[object Uint8Array]", rg = "[object Uint8ClampedArray]", og = "[object Uint16Array]", sg = "[object Uint32Array]", Ie = {};
Ie[wr] = Ie[Yd] = Ie[Xd] = Ie[Kd] = Ie[Ud] = Ie[Bd] = Ie[qd] = Ie[_d] = Ie[eg] = Ie[tg] = Ie[ng] = Ie[Vd] = Ie[Qd] = Ie[Nr] = Ie[Gd] = Ie[Zd] = Ie[$d] = Ie[Hd] = Ie[ig] = Ie[rg] = Ie[og] = Ie[sg] = !0;
Ie[Wd] = Ie[Tr] = Ie[Jd] = !1;
function Ht(r, n, e, t, i, o) {
  var s, a = n & Pd, c = n & Od, l = n & Rd;
  if (e && (s = i ? e(r, t, i, o) : e(r)), s !== void 0)
    return s;
  if (!bd(r))
    return r;
  var h = jd(r);
  if (h) {
    if (s = Sd(r), !a)
      return md(r, s);
  } else {
    var u = Cd(r), g = u == Tr || u == Fd;
    if (Dd(r))
      return Id(r, a);
    if (u == Nr || u == wr || g && !i) {
      if (s = c || g ? {} : xd(r), !a)
        return c ? Td(r, yd(s, r)) : wd(r, pd(s, r));
    } else {
      if (!Ie[u])
        return i ? r : {};
      s = kd(r, u, a);
    }
  }
  o || (o = new gd());
  var M = o.get(r);
  if (M)
    return M;
  o.set(r, s), Ad(r) ? r.forEach(function(w) {
    s.add(Ht(w, n, e, w, r, o));
  }) : zd(r) && r.forEach(function(w, p) {
    s.set(p, Ht(w, n, e, p, r, o));
  });
  var d = l ? c ? vd : Nd : c ? Ld : Ed, y = h ? void 0 : d(r);
  return fd(y || r, function(w, p) {
    y && (p = w, w = r[p]), Md(s, p, Ht(w, n, e, p, r, o));
  }), s;
}
var ag = Ht, cg = ag, lg = 1, ug = 4;
function hg(r) {
  return cg(r, lg | ug);
}
var dg = hg;
const un = /* @__PURE__ */ Pn(dg), vr = "1.1.33", gg = "@netless/appliance-plugin", ji = typeof OffscreenCanvas == "function" ? "worker" : "mainThread";
if (typeof window < "u") {
  let r = window.__netlessUA || "";
  r += ` ${gg}@${vr}`, window.__netlessUA = r;
}
const Se = {
  useWorker: "auto",
  useSimple: !1,
  useBackgroundThread: !1,
  syncOpt: {
    interval: 300,
    smoothSync: !0
  },
  cursor: {
    enable: !0,
    expirationTime: 5e3,
    moveDelayTime: 100
  },
  bufferSize: {
    full: 2e4,
    sub: 600
  },
  bezier: {
    enable: !0,
    maxDrawCount: 1 / 0,
    combineUnitTime: 300
  },
  pencilEraser: {
    useBitMap: !0,
    maxImageWidth: 1920,
    maxImageHeight: 1920,
    maxCombineTime: 500
  },
  strokeWidth: {
    min: 1,
    max: 10
  },
  textEditor: {
    showFloatBar: !0,
    canSelectorSwitch: !0,
    rightBoundBreak: !1,
    extendFontFaces: [],
    loadFontFacesTimeout: 1e4
  },
  longDottedStroke: {
    lineCap: "round",
    segment: 1,
    gap: 2
  },
  dottedStroke: {
    lineCap: "round",
    segment: 1,
    gap: 2
  },
  normalStroke: {
    lineCap: "round"
  },
  undoRedo: {
    enableGlobal: !1,
    maxStackLength: 20
  },
  markmap: {
    enable: !0,
    timeout: 1e4,
    options: {
      autoFit: !0,
      duration: 0,
      zoom: !1,
      pan: !1,
      maxInitialScale: 10
    }
  }
};
function $n(r) {
  return JSON.parse(Rr(r));
}
function _t(r) {
  return Or(JSON.stringify(r));
}
const ze = Object.keys, st = "++", $ = "selector", fg = "all";
var Mg = "__lodash_hash_undefined__";
function pg(r) {
  return this.__data__.set(r, Mg), this;
}
var yg = pg;
function Ig(r) {
  return this.__data__.has(r);
}
var mg = Ig, wg = tr, Tg = yg, Ng = mg;
function en(r) {
  var n = -1, e = r == null ? 0 : r.length;
  for (this.__data__ = new wg(); ++n < e; )
    this.add(r[n]);
}
en.prototype.add = en.prototype.push = Tg;
en.prototype.has = Ng;
var Hn = en;
function vg(r, n) {
  for (var e = -1, t = r == null ? 0 : r.length; ++e < t; )
    if (n(r[e], e, r))
      return !0;
  return !1;
}
var Cg = vg;
function Sg(r, n) {
  return r.has(n);
}
var Jn = Sg, kg = Hn, xg = Cg, jg = Jn, Dg = 1, zg = 2;
function bg(r, n, e, t, i, o) {
  var s = e & Dg, a = r.length, c = n.length;
  if (a != c && !(s && c > a))
    return !1;
  var l = o.get(r), h = o.get(n);
  if (l && h)
    return l == n && h == r;
  var u = -1, g = !0, M = e & zg ? new kg() : void 0;
  for (o.set(r, n), o.set(n, r); ++u < a; ) {
    var d = r[u], y = n[u];
    if (t)
      var w = s ? t(y, d, u, n, r, o) : t(d, y, u, r, n, o);
    if (w !== void 0) {
      if (w)
        continue;
      g = !1;
      break;
    }
    if (M) {
      if (!xg(n, function(p, f) {
        if (!jg(M, f) && (d === p || i(d, p, e, t, o)))
          return M.push(f);
      })) {
        g = !1;
        break;
      }
    } else if (!(d === y || i(d, y, e, t, o))) {
      g = !1;
      break;
    }
  }
  return o.delete(r), o.delete(n), g;
}
var Ag = bg;
function Eg(r) {
  var n = -1, e = Array(r.size);
  return r.forEach(function(t, i) {
    e[++n] = [i, t];
  }), e;
}
var Lg = Eg;
function Pg(r) {
  var n = -1, e = Array(r.size);
  return r.forEach(function(t) {
    e[++n] = t;
  }), e;
}
var Xn = Pg, Di = Ot, zi = Ir, Og = On, Rg = Ag, Yg = Lg, Ug = Xn, Bg = 1, Wg = 2, Fg = "[object Boolean]", Vg = "[object Date]", Qg = "[object Error]", Gg = "[object Map]", Zg = "[object Number]", $g = "[object RegExp]", Hg = "[object Set]", Jg = "[object String]", Xg = "[object Symbol]", Kg = "[object ArrayBuffer]", qg = "[object DataView]", bi = Di ? Di.prototype : void 0, pn = bi ? bi.valueOf : void 0;
function _g(r, n, e, t, i, o, s) {
  switch (e) {
    case qg:
      if (r.byteLength != n.byteLength || r.byteOffset != n.byteOffset)
        return !1;
      r = r.buffer, n = n.buffer;
    case Kg:
      return !(r.byteLength != n.byteLength || !o(new zi(r), new zi(n)));
    case Fg:
    case Vg:
    case Zg:
      return Og(+r, +n);
    case Qg:
      return r.name == n.name && r.message == n.message;
    case $g:
    case Jg:
      return r == n + "";
    case Gg:
      var a = Yg;
    case Hg:
      var c = t & Bg;
      if (a || (a = Ug), r.size != n.size && !c)
        return !1;
      var l = s.get(r);
      if (l)
        return l == n;
      t |= Wg, s.set(r, n);
      var h = Rg(a(r), a(n), t, i, o, s);
      return s.delete(r), h;
    case Xg:
      if (pn)
        return pn.call(r) == pn.call(n);
  }
  return !1;
}
var CI = _g;
function Yt(r, n, e) {
  return "#" + ((r << 16) + (n << 8) + e).toString(16).padStart(6, "0");
}
function Ae(r, n = 1) {
  return "rgba(" + parseInt("0x" + r.slice(1, 3)) + "," + parseInt("0x" + r.slice(3, 5)) + "," + parseInt("0x" + r.slice(5, 7)) + "," + n + ")";
}
function At(r, n, e, t = 1) {
  return `rgba(${r},${n},${e},${t})`;
}
function hn(r) {
  const n = r.split(","), e = parseInt(n[0].split("(")[1]), t = parseInt(n[1]), i = parseInt(n[2]), o = Number(n[3].split(")")[0]);
  return [Yt(e, t, i), o];
}
function Wt(r) {
  const n = r.split(","), e = parseInt(n[0].split("(")[1]), t = parseInt(n[1]), i = parseInt(n[2]), o = Number(n[3].split(")")[0]);
  return [e, t, i, o];
}
function Ft(r) {
  return r === "transparent";
}
var $e = /* @__PURE__ */ ((r) => (r[r.Text = 1] = "Text", r[r.Shape = 2] = "Shape", r))($e || {});
const Ue = class Ue {
  createProxy(n) {
    const e = new Proxy(n, {
      get(t, i, o) {
        const s = Ue.interceptors.hasOwnProperty(i) ? Ue.interceptors : t;
        return Reflect.get(s, i, o);
      }
    });
    return Ue.proxyToRaw.set(e, n), e;
  }
};
I(Ue, "proxyToRaw", /* @__PURE__ */ new WeakMap()), I(Ue, "interceptors", {
  entries(...n) {
    return Ue.proxyToRaw.get(this).entries(...n);
  },
  forEach(...n) {
    return Ue.proxyToRaw.get(this).forEach(...n);
  },
  size() {
    return Ue.proxyToRaw.get(this).size;
  },
  get(n) {
    return Ue.proxyToRaw.get(this).get(n);
  },
  set(n, e) {
    return Ue.proxyToRaw.get(this).set(n, e);
  },
  delete(n) {
    return Ue.proxyToRaw.get(this).delete(n);
  },
  clear() {
    return Ue.proxyToRaw.get(this).clear();
  }
});
let ft = Ue;
const Ai = 5;
class ef {
  constructor(n) {
    I(this, "internalMsgEmitter");
    I(this, "publicMsgEmitter");
    I(this, "control");
    I(this, "editors");
    I(this, "activeId");
    I(this, "proxyMap");
    I(this, "taskqueue", /* @__PURE__ */ new Map());
    I(this, "undoTickerId");
    I(this, "checkLocalTextBlur", Pt(() => {
      if (this.collector)
        for (const [n, e] of this.editors.entries())
          e.opt.workState !== x.Done && this.collector.isLocalId(n) && this.activeId !== n && (e.opt.text && e.opt.text.replace(/\s*,/g, "") ? this.unActive(n) : this.delete(n, !0, !0));
    }, 100));
    const { control: e, internalMsgEmitter: t, publicMsgEmitter: i } = n;
    this.control = e, this.internalMsgEmitter = t, this.publicMsgEmitter = i;
    const o = /* @__PURE__ */ new Map();
    this.proxyMap = new ft();
    const s = this, a = ft.interceptors.set;
    ft.interceptors.set = function(l, h) {
      return s.interceptors.set(l, h), a.call(this, l, h);
    };
    const c = ft.interceptors.delete;
    ft.interceptors.delete = function(l) {
      return s.interceptors.delete(l), c.call(this, l);
    }, this.editors = this.proxyMap.createProxy(o);
  }
  get collector() {
    return this.control.collector;
  }
  set UndoTickerId(n) {
    this.undoTickerId = n;
  }
  filterEditor(n) {
    const e = /* @__PURE__ */ new Map();
    return this.editors.forEach((t, i) => {
      t.viewId === n && e.set(i, t);
    }), e;
  }
  get interceptors() {
    return {
      set: (n, e) => {
        var d, y, w, p, f, N, S, C, k;
        if (!this.collector)
          return !0;
        const {
          viewId: t,
          scenePath: i,
          canSync: o,
          canWorker: s,
          type: a,
          opt: c,
          dataType: l,
          updateNodeOpt: h
        } = e;
        if (!s && !o)
          return !0;
        const g = ((d = this.collector) == null ? void 0 : d.isLocalId(n)) ? (y = this.collector) == null ? void 0 : y.transformKey(n) : n, M = ((w = this.collector) == null ? void 0 : w.storage[t]) && this.collector.storage[t][i] && this.collector.storage[t][i][g] || void 0;
        M ? M.toolsType === T.Text && (s && (this.control.worker.queryTaskBatchData({
          workId: n,
          msgType: R.UpdateNode
        }).forEach((v) => {
          var A;
          (A = this.control.worker) == null || A.taskBatchData.delete(v);
        }), (S = this.control.worker) == null || S.taskBatchData.add({
          workId: n,
          msgType: R.UpdateNode,
          dataType: l || q.Local,
          toolsType: T.Text,
          opt: c,
          viewId: t,
          scenePath: i,
          willRefresh: !0
          // willSyncService: canSync
        }), (C = this.control.worker) == null || C.runAnimation()), o && ((k = this.collector) == null || k.dispatch({
          type: R.UpdateNode,
          workId: n,
          toolsType: T.Text,
          opt: c,
          viewId: t,
          scenePath: i,
          updateNodeOpt: h
        }), c.workState === x.Done && this.undoTickerId && this.control.viewContainerManager.getUndoTickerId(t) === this.undoTickerId && (this.internalMsgEmitter.emit(
          "forceUndoTickerEnd",
          this.undoTickerId,
          t
        ), this.undoTickerId = void 0), c.workState === x.Done && this.control.runEffectWork(() => {
          this.control.worker.setShapeSelectorByWorkId(n, t);
        }))) : a === $e.Text && (o && ((p = this.collector) == null || p.dispatch({
          type: c.text && R.FullWork || R.CreateWork,
          workId: n,
          toolsType: T.Text,
          opt: c,
          isSync: !0,
          viewId: t,
          scenePath: i,
          updateNodeOpt: h
        })), s && ((f = this.control.worker) == null || f.taskBatchData.add({
          workId: n,
          msgType: c.text && R.FullWork || R.CreateWork,
          dataType: l || q.Local,
          toolsType: T.Text,
          opt: c,
          viewId: t,
          scenePath: i,
          willRefresh: !0
        }), (N = this.control.worker) == null || N.runAnimation()));
      },
      delete: (n) => {
        var a, c, l;
        if (!this.collector)
          return !0;
        const e = this.editors.get(n);
        if (!e)
          return !0;
        const { viewId: t, scenePath: i, canSync: o, canWorker: s } = e;
        if (!s && !o)
          return !0;
        s && ((a = this.control.worker) == null || a.taskBatchData.add({
          workId: n,
          toolsType: T.Text,
          msgType: R.RemoveNode,
          dataType: q.Local,
          viewId: t,
          scenePath: i,
          willRefresh: !0
        }), (c = this.control.worker) == null || c.runAnimation()), o && ((l = this.collector) == null || l.dispatch({
          type: R.RemoveNode,
          removeIds: [n],
          toolsType: T.Text,
          viewId: t,
          scenePath: i
        }), this.undoTickerId && this.control.viewContainerManager.getUndoTickerId(t) === this.undoTickerId && (this.internalMsgEmitter.emit(
          "forceUndoTickerEnd",
          this.undoTickerId,
          t
        ), this.undoTickerId = void 0));
      },
      clear() {
        return !0;
      }
    };
  }
  computeTextActive(n, e) {
    var o, s, a, c;
    const t = (o = this.control.viewContainerManager) == null ? void 0 : o.transformToScenePoint(
      n,
      e
    ), i = (s = this.control.viewContainerManager) == null ? void 0 : s.getCurScenePath(e);
    e && i && ((a = this.control.worker) == null || a.taskBatchData.add({
      msgType: R.GetTextActive,
      dataType: q.Local,
      op: t,
      viewId: e,
      scenePath: i
    }), (c = this.control.worker) == null || c.runAnimation());
  }
  checkEmptyTextBlur() {
    var e;
    let n = this.activeId;
    for (const [t, i] of this.editors.entries())
      if (i.opt.uid && i.opt.uid === ((e = this.collector) == null ? void 0 : e.uid)) {
        n = t;
        break;
      }
    if (n) {
      const t = this.editors.get(n);
      (t == null ? void 0 : t.opt.text) && (t == null ? void 0 : t.opt.text.replace(/\s*,/g, "")) ? this.unActive(n) : this.delete(n, !0, !0);
    }
    this.checkLocalTextBlur();
  }
  onCameraChange(n, e) {
    var t, i;
    for (const [o, s] of this.editors.entries())
      if (s.viewId === e) {
        const { boxPoint: a, boxSize: c } = s.opt, l = a && ((t = this.control.viewContainerManager) == null ? void 0 : t.transformToOriginPoint(
          a,
          s.viewId
        )), h = (i = this.control.viewContainerManager) == null ? void 0 : i.getCurScenePath(e);
        if (h && e) {
          const u = {
            x: l && l[0] || 0,
            y: l && l[1] || 0,
            w: c && c[0] || 0,
            h: c && c[1] || 0,
            opt: s.opt,
            scale: n.scale,
            type: $e.Text,
            viewId: e,
            scenePath: h,
            canWorker: !1,
            canSync: !1
          };
          this.editors.set(o, u), this.control.viewContainerManager.updateTextEditorInfo(
            e,
            this.activeId
          );
        }
      }
  }
  onServiceDerive(n) {
    var y, w, p;
    const { workId: e, opt: t, msgType: i, viewId: o, scenePath: s, dataType: a, updateNodeOpt: c } = n;
    if (!e || !o || !s)
      return;
    const l = e.toString();
    if (i === R.RemoveNode) {
      this.delete(l, !0, !0);
      return;
    }
    const { boxPoint: h, boxSize: u } = t, g = h && ((y = this.control.viewContainerManager) == null ? void 0 : y.transformToOriginPoint(
      h,
      o
    )), M = this.control.viewContainerManager.getView(o), d = {
      x: g && g[0] || 0,
      y: g && g[1] || 0,
      w: u && u[0] || 0,
      h: u && u[1] || 0,
      opt: t,
      type: $e.Text,
      canWorker: !0,
      canSync: !1,
      dataType: a,
      scale: ((w = M == null ? void 0 : M.cameraOpt) == null ? void 0 : w.scale) || 1,
      viewId: o,
      scenePath: s
    };
    if (t && t.workState === x.Done && !this.control.cursor.isEnableCursor && h && u) {
      let N = c == null ? void 0 : c.uid;
      if (!N) {
        const S = this.editors.get(l);
        N = S && ((p = S.opt) == null ? void 0 : p.uid);
      }
      N && this.control.cursor.collectServiceCursor({
        op: h && [
          h[0] + u[0],
          h[1] + u[1]
        ],
        uid: N,
        workState: x.Done,
        viewId: o,
        type: R.Cursor
      });
    }
    this.editors.set(l, d), a === q.Service && t.workState === x.Done && this.activeId === l && (this.activeId = void 0), this.control.viewContainerManager.updateTextEditorInfo(
      o,
      this.activeId
    );
  }
  updateForViewEdited(n, e) {
    var i;
    this.editors.set(n, e);
    const t = (i = this.taskqueue.get(n)) == null ? void 0 : i.resolve;
    t && t(e);
  }
  active(n, e) {
    var i;
    const t = this.editors.get(n);
    t && t.viewId && (t.opt.workState = x.Start, t.opt.uid = (i = this.collector) == null ? void 0 : i.uid, this.activeId = n, t.canWorker = !0, t.canSync = !0, e && t.viewId && this.addUndoTicker(e, t.viewId), this.editors.set(n, t), this.control.viewContainerManager.updateTextEditorInfo(
      t.viewId,
      this.activeId
    ));
  }
  unActive(n) {
    var t;
    const e = this.editors.get(n);
    if (e && e.viewId) {
      e.opt.workState = x.Done, e.opt.uid = void 0, e.canWorker = !0, e.canSync = !0, this.editors.set(n, e), this.activeId = void 0;
      const i = (t = this.control.viewContainerManager.getView(
        e.viewId
      )) == null ? void 0 : t.container;
      if (i) {
        const o = CSS.escape(n), s = i.querySelector(`.editor-box #${o}`);
        s && s.blur();
      }
      this.control.viewContainerManager.updateTextEditorInfo(
        e.viewId,
        this.activeId
      );
    }
  }
  addUndoTicker(n, e) {
    this.internalMsgEmitter.emit("addUndoTicker", n, e), this.undoTickerId = n;
  }
  createTextForMasterController(n, e) {
    var s;
    const { workId: t, isActive: i, ...o } = n;
    e && this.addUndoTicker(e, o.viewId), o.opt.uid = (s = this.collector) == null ? void 0 : s.uid, i && (this.activeId = t), o.dataType = q.Local, o.canWorker = !0, o.canSync = !0, this.editors.set(t, o), this.control.viewContainerManager.updateTextEditorInfo(
      o.viewId,
      this.activeId
    );
  }
  updateTextForMasterController(n, e) {
    var s;
    const { workId: t, ...i } = n;
    e && this.addUndoTicker(e, i.viewId);
    const o = this.editors.get(t) || {};
    i.opt && (i.opt.uid = (s = this.collector) == null ? void 0 : s.uid), i.dataType = q.Local, this.editors.set(t, { ...o, ...i }), this.control.viewContainerManager.updateTextEditorInfo(
      i.viewId,
      this.activeId
    );
  }
  async updateTextControllerWithEffectAsync(n, e) {
    var h;
    const { workId: t, ...i } = n;
    e && this.addUndoTicker(e, i.viewId);
    const o = this.editors.get(t) || {};
    i.opt && (i.opt.uid = (h = this.collector) == null ? void 0 : h.uid), i.dataType = q.Local;
    const s = { ...o, ...i };
    if (this.editors.set(t, s), this.control.viewContainerManager.updateTextEditorInfo(
      i.viewId,
      this.activeId
    ), this.taskqueue.has(t)) {
      const u = this.taskqueue.get(t);
      return u && (u.clocker && clearTimeout(u.clocker), this.taskqueue.delete(t)), await this.updateTextControllerWithEffectAsync(n);
    }
    const a = setTimeout(() => {
      var g;
      const u = (g = this.taskqueue.get(t)) == null ? void 0 : g.resolve;
      u && u(s);
    }, 50), c = await new Promise((u) => {
      this.taskqueue.set(t, { resolve: u, clocker: a });
    }), l = this.taskqueue.get(t);
    return l && (l.clocker && clearTimeout(l.clocker), this.taskqueue.delete(t)), c;
  }
  updateTextForWorker(n, e) {
    const { workId: t, isActive: i, ...o } = n, a = { ...this.editors.get(t) || {}, ...o };
    if (e && this.addUndoTicker(e, o.viewId), i) {
      a.canWorker = !1, a.canSync = !1, this.editors.set(t, a), this.active(t);
      return;
    }
    this.editors.set(t, a), this.control.viewContainerManager.updateTextEditorInfo(
      o.viewId,
      this.activeId
    );
  }
  get(n) {
    return this.editors.get(n);
  }
  delete(n, e, t) {
    const i = this.editors.get(n);
    if (i) {
      const o = i.viewId;
      i.canSync = e, i.canWorker = t, this.editors.delete(n), this.activeId === n && (this.activeId = void 0), this.control.viewContainerManager.updateTextEditorInfo(
        o,
        this.activeId
      );
    }
  }
  deleteBatch(n, e, t) {
    const i = /* @__PURE__ */ new Set();
    for (const o of n) {
      const s = this.editors.get(o);
      if (s) {
        const a = s.viewId;
        s.canSync = e, s.canWorker = t, this.editors.delete(o), this.activeId === o && (this.activeId = void 0), i.add(a);
      }
    }
    for (const o of i)
      this.control.viewContainerManager.updateTextEditorInfo(
        o,
        this.activeId
      );
  }
  clear(n, e) {
    this.editors.forEach((t, i) => {
      t.viewId === n && (e && (t.canSync = !1), t.canWorker = !1, this.editors.delete(i));
    }), this.activeId = void 0, this.control.viewContainerManager.updateTextEditorInfo(
      n,
      this.activeId
    );
  }
  destory() {
    this.editors.clear(), this.activeId = void 0;
  }
  insertText(n, e, t, i) {
    var d, y;
    const o = (d = this.control.room) == null ? void 0 : d.state.memberState;
    if (!o)
      return;
    const s = this.control.viewContainerManager.getView(n);
    if (!(s != null && s.focusScenePath))
      return;
    const a = s == null ? void 0 : s.cameraOpt, c = this.control.getToolsOpt(T.Text, o);
    if (!c)
      return;
    const l = (y = this.control.viewContainerManager) == null ? void 0 : y.transformToOriginPoint(
      [e, t],
      n
    ), u = wn(i, {
      whiteList: {},
      // 白名单为空，表示过滤所有标签
      stripIgnoreTag: !0,
      // 过滤所有非白名单标签的HTML
      stripIgnoreTagBody: ["script"]
      // script标签较特殊，需要过滤标签中间的内容
    }).split(/\n/), g = c.toolsOpt;
    g.text = _t(u), g.workState = x.Doing, g.boxPoint = [Math.round(e), Math.round(t)], g.boxSize = [g.fontSize, g.fontSize];
    const M = this.control.worker.now;
    return this.control.textEditorManager.checkEmptyTextBlur(), this.control.textEditorManager.createTextForMasterController(
      {
        workId: M.toString(),
        x: l && l[0] || 0,
        y: l && l[1] || 0,
        scale: (a == null ? void 0 : a.scale) || 1,
        opt: g,
        type: $e.Text,
        isActive: !0,
        viewId: n,
        scenePath: s.focusScenePath
      },
      M
    ), M.toString();
  }
  updateText(n, e) {
    const t = this.editors.get(n.toString());
    if (!t)
      return;
    const o = wn(e, {
      whiteList: {},
      // 白名单为空，表示过滤所有标签
      stripIgnoreTag: !0,
      // 过滤所有非白名单标签的HTML
      stripIgnoreTagBody: ["script"]
      // script标签较特殊，需要过滤标签中间的内容
    }).split(/\n/);
    t.opt.text = _t(o), this.control.textEditorManager.updateTextForMasterController(
      {
        workId: n,
        opt: t.opt,
        viewId: t.viewId,
        canSync: !0,
        canWorker: !0
      },
      this.control.worker.now
    );
  }
}
function tf(r) {
  return r;
}
var Cr = tf;
function nf(r, n, e) {
  switch (e.length) {
    case 0:
      return r.call(n);
    case 1:
      return r.call(n, e[0]);
    case 2:
      return r.call(n, e[0], e[1]);
    case 3:
      return r.call(n, e[0], e[1], e[2]);
  }
  return r.apply(n, e);
}
var rf = nf, of = rf, Ei = Math.max;
function sf(r, n, e) {
  return n = Ei(n === void 0 ? r.length - 1 : n, 0), function() {
    for (var t = arguments, i = -1, o = Ei(t.length - n, 0), s = Array(o); ++i < o; )
      s[i] = t[n + i];
    i = -1;
    for (var a = Array(n + 1); ++i < n; )
      a[i] = t[i];
    return a[n] = e(s), of(r, this, a);
  };
}
var af = sf;
function cf(r) {
  return function() {
    return r;
  };
}
var lf = cf, uf = lf, Li = nr, hf = Cr, df = Li ? function(r, n) {
  return Li(r, "toString", {
    configurable: !0,
    enumerable: !1,
    value: uf(n),
    writable: !0
  });
} : hf, gf = df, ff = 800, Mf = 16, pf = Date.now;
function yf(r) {
  var n = 0, e = 0;
  return function() {
    var t = pf(), i = Mf - (t - e);
    if (e = t, i > 0) {
      if (++n >= ff)
        return arguments[0];
    } else
      n = 0;
    return r.apply(void 0, arguments);
  };
}
var If = yf, mf = gf, wf = If, Tf = wf(mf), Nf = Tf, vf = Cr, Cf = af, Sf = Nf;
function kf(r, n) {
  return Sf(Cf(r, n, vf), r + "");
}
var xf = kf;
function jf(r, n, e, t) {
  for (var i = r.length, o = e + (t ? 1 : -1); t ? o-- : ++o < i; )
    if (n(r[o], o, r))
      return o;
  return -1;
}
var Df = jf;
function zf(r) {
  return r !== r;
}
var bf = zf;
function Af(r, n, e) {
  for (var t = e - 1, i = r.length; ++t < i; )
    if (r[t] === n)
      return t;
  return -1;
}
var Ef = Af, Lf = Df, Pf = bf, Of = Ef;
function Rf(r, n, e) {
  return n === n ? Of(r, n, e) : Lf(r, Pf, e);
}
var Yf = Rf, Uf = Yf;
function Bf(r, n) {
  var e = r == null ? 0 : r.length;
  return !!e && Uf(r, n, 0) > -1;
}
var Sr = Bf;
function Wf(r, n, e) {
  for (var t = -1, i = r == null ? 0 : r.length; ++t < i; )
    if (e(n, r[t]))
      return !0;
  return !1;
}
var kr = Wf;
function Ff(r, n) {
  for (var e = -1, t = r == null ? 0 : r.length, i = Array(t); ++e < t; )
    i[e] = n(r[e], e, r);
  return i;
}
var Vf = Ff, Qf = Hn, Gf = Sr, Zf = kr, $f = Vf, Hf = ln, Jf = Jn, Xf = 200;
function Kf(r, n, e, t) {
  var i = -1, o = Gf, s = !0, a = r.length, c = [], l = n.length;
  if (!a)
    return c;
  e && (n = $f(n, Hf(e))), t ? (o = Zf, s = !1) : n.length >= Xf && (o = Jf, s = !1, n = new Qf(n));
  e:
    for (; ++i < a; ) {
      var h = r[i], u = e == null ? h : e(h);
      if (h = t || h !== 0 ? h : 0, s && u === u) {
        for (var g = l; g--; )
          if (n[g] === u)
            continue e;
        c.push(h);
      } else o(n, u, t) || c.push(h);
    }
  return c;
}
var qf = Kf, Pi = Ot, _f = sr, eM = cn, Oi = Pi ? Pi.isConcatSpreadable : void 0;
function tM(r) {
  return eM(r) || _f(r) || !!(Oi && r && r[Oi]);
}
var nM = tM, iM = Qn, rM = nM;
function xr(r, n, e, t, i) {
  var o = -1, s = r.length;
  for (e || (e = rM), i || (i = []); ++o < s; ) {
    var a = r[o];
    n > 0 && e(a) ? n > 1 ? xr(a, n - 1, e, t, i) : iM(i, a) : t || (i[i.length] = a);
  }
  return i;
}
var oM = xr;
function sM() {
}
var aM = sM, yn = pr, cM = aM, lM = Xn, uM = 1 / 0, hM = yn && 1 / lM(new yn([, -0]))[1] == uM ? function(r) {
  return new yn(r);
} : cM, dM = hM, gM = Hn, fM = Sr, MM = kr, pM = Jn, yM = dM, IM = Xn, mM = 200;
function wM(r, n, e) {
  var t = -1, i = fM, o = r.length, s = !0, a = [], c = a;
  if (e)
    s = !1, i = MM;
  else if (o >= mM) {
    var l = n ? null : yM(r);
    if (l)
      return IM(l);
    s = !1, i = pM, c = new gM();
  } else
    c = n ? [] : a;
  e:
    for (; ++t < o; ) {
      var h = r[t], u = n ? n(h) : h;
      if (h = e || h !== 0 ? h : 0, s && u === u) {
        for (var g = c.length; g--; )
          if (c[g] === u)
            continue e;
        n && c.push(u), a.push(h);
      } else i(c, u, e) || (c !== a && c.push(u), a.push(h));
    }
  return a;
}
var TM = wM, NM = qf, vM = oM, Ri = TM;
function CM(r, n, e) {
  var t = r.length;
  if (t < 2)
    return t ? Ri(r[0]) : [];
  for (var i = -1, o = Array(t); ++i < t; )
    for (var s = r[i], a = -1; ++a < t; )
      a != i && (o[i] = NM(o[i] || s, r[a], n, e));
  return Ri(vM(o, 1), n, e);
}
var SM = CM, kM = Bn, xM = yt;
function jM(r) {
  return xM(r) && kM(r);
}
var DM = jM, zM = hr, bM = xf, AM = SM, EM = DM;
bM(function(r) {
  return AM(zM(r, EM));
});
function SI(r, n) {
  if (r && n) {
    const e = Math.min(r.x, n.x), t = Math.min(r.y, n.y), i = Math.max(r.x + r.w, n.x + n.w), o = Math.max(r.y + r.h, n.y + n.h), s = i - e, a = o - t;
    return { x: e, y: t, w: s, h: a };
  }
  return n || r;
}
function Kn(r, n) {
  if (r && n) {
    const e = Math.min(r.originX, n.originX), t = Math.min(r.originY, n.originY), i = Math.max(
      r.originX + r.width,
      n.originX + n.width
    ), o = Math.max(
      r.originY + r.height,
      n.originY + n.height
    ), s = i - e, a = o - t;
    return { originX: e, originY: t, width: s, height: a };
  }
  return n || r;
}
function jr(r, n = 0) {
  const e = { x: 0, y: 0, w: 0, h: 0 };
  let t = 1 / 0, i = 1 / 0, o = -1 / 0, s = -1 / 0;
  return r.forEach((a) => {
    const [c, l] = a.XY;
    t = Math.min(t, c - n), i = Math.min(i, l - n), o = Math.max(o, c + n), s = Math.max(s, l + n);
  }), e.x = t, e.y = i, e.w = o - t, e.h = s - i, e;
}
function kI(r, n) {
  return !(r.x + r.w < n.x || r.x > n.x + n.w || r.y + r.h < n.y || r.y > n.y + n.h);
}
function xI(r, n = 10) {
  return {
    x: Math.floor(r.x - n),
    y: Math.floor(r.y - n),
    w: Math.ceil(r.w + n * 2),
    h: Math.ceil(r.h + n * 2)
  };
}
function jI(r, n) {
  return {
    x: r.x + n[0],
    y: r.y + n[1],
    w: r.w,
    h: r.h
  };
}
function LM(r, n) {
  const e = new U(r.x, r.y), t = new U(r.x + r.w, r.y), i = new U(r.x + r.w, r.y + r.h), o = new U(r.x, r.y + r.h), s = new U(r.x + r.w / 2, r.y + r.h / 2), a = Math.PI * n / 180, c = U.RotWith(e, s, a), l = U.RotWith(t, s, a), h = U.RotWith(i, s, a), u = U.RotWith(o, s, a);
  return [c, l, h, u];
}
function DI(r, n) {
  const e = LM(r, n);
  return jr(e);
}
function zI(r, n, e) {
  const t = new U(r.x, r.y), i = new U(r.x + r.w, r.y), o = new U(r.x + r.w, r.y + r.h), s = new U(r.x, r.y + r.h), a = new U(e[0], e[1]), c = new U(n[0], n[1]), l = U.ScaleWOrigin(t, c, a), h = U.ScaleWOrigin(i, c, a), u = U.ScaleWOrigin(o, c, a), g = U.ScaleWOrigin(s, c, a);
  return jr([l, h, u, g]);
}
function bI(r, n, e) {
  const t = new U(n[0], n[1]);
  for (let i = 0; i < r.length; i += 3) {
    const o = new U(r[i], r[i + 1]), s = Math.PI * e / 180, a = U.RotWith(o, t, s);
    r[i] = a.x, r[i + 1] = a.y;
  }
}
function AI(r, n, e) {
  const t = new U(n[0], n[1]);
  for (let i = 0; i < r.length; i += 2) {
    const o = new U(r[i], r[i + 1]), s = Math.PI * e / 180, a = U.RotWith(o, t, s);
    r[i] = a.x, r[i + 1] = a.y;
  }
}
function EI(r, n, e, t = [0, 0]) {
  const i = new U(n[0], n[1]);
  for (let o = 0; o < r.length; o += 3) {
    const s = new U(r[o], r[o + 1]), a = new U(e[0], e[1]), c = U.ScaleWOrigin(s, a, i);
    r[o] = c.x + t[0], r[o + 1] = c.y + t[1];
  }
}
function LI(r, n, e, t = [0, 0]) {
  const i = new U(n[0], n[1]);
  for (let o = 0; o < r.length; o += 2) {
    const s = new U(r[o], r[o + 1]), a = new U(e[0], e[1]), c = U.ScaleWOrigin(s, a, i);
    r[o] = c.x + t[0], r[o + 1] = c.y + t[1];
  }
}
function PM(r, n) {
  return r[0] >= n.x && r[0] <= n.x + n.w && r[1] >= n.y && r[1] <= n.y + n.h;
}
function PI(r, n) {
  const e = r <= n ? 1 : r / n, t = n <= r ? 1 : n / r;
  return [e, t];
}
function OI(r) {
  for (const n of r)
    if (isNaN(n) || n === 1 / 0 || n === -1 / 0)
      return !1;
  return !0;
}
function RI(r) {
  return r !== Math.floor(r);
}
function YI(r) {
  const n = r.toString();
  return n.indexOf(".") !== -1 ? n.split(".")[1].length : 0;
}
function UI(r, n) {
  for (; n !== 0; )
    [r, n] = [n, r % n];
  return r;
}
function BI(r) {
  return `${tn(r.x)},${tn(r.y)} `;
}
function WI(r, n) {
  return `${tn((r.x + n.x) / 2)},${tn((r.y + n.y) / 2)} `;
}
function tn(r) {
  return +r.toFixed(4);
}
const ot = async (r, n, e = !1) => {
  let t, i;
  return new Promise(function(o) {
    if (e && requestIdleCallback)
      requestIdleCallback(
        () => {
          o(1);
        },
        { timeout: n }
      );
    else {
      if (typeof window.requestIdleCallback == "function") {
        const s = typeof n == "number" && !isNaN(n) && n > 0 ? n : 0;
        i = window.requestIdleCallback(
          () => {
            clearTimeout(t), o(1);
          },
          { timeout: s }
        );
      }
      t = setTimeout(() => {
        i && window.cancelIdleCallback(i), o(1);
      }, n || 0);
    }
  }).then(function() {
    r();
  }).catch(function() {
    r();
  });
}, Nt = (r) => Math.max(1, (window.devicePixelRatio || 1) / 1), FI = (r, n) => r.x + r.w < n.x || r.x > n.x + n.w || r.y + r.h < n.y || r.y > n.y + n.h ? Gt.outside : r.x >= n.x && r.y >= n.y && r.x + r.w <= n.x + n.w && r.y + r.h <= n.y + n.h ? Gt.inside : Gt.intersect;
function VI(r) {
  let n = 0;
  for (let e = 0; e < r.length; e++) {
    const t = r.charCodeAt(e);
    t >= 1 && t <= 126 || 65376 <= t && t <= 65439 ? n++ : n += 2;
  }
  return n;
}
function OM(r) {
  if (!r || typeof r != "string")
    return !1;
  const n = r.trim();
  if (!n)
    return !1;
  const e = [
    // 标题：以 # 开头（可能前面有空格）
    /^\s*#{1,6}\s+.+/m,
    // 列表：以 -、*、+ 开头，或数字列表
    /^\s*[-*+]\s+.+/m,
    /^\s*\d+\.\s+.+/m,
    // 链接：[text](url) 或 [text][ref]
    /\[([^\]]+)\]\(([^)]+)\)/,
    /\[([^\]]+)\]\[([^\]]+)\]/,
    // 代码块：``` 或 `
    /```[\s\S]*?```/,
    /`[^`]+`/,
    // 表格：包含 | 分隔符
    /^\s*\|.+\|/m,
    // 图片：![alt](url)
    /!\[([^\]]*)\]\(([^)]+)\)/,
    // 粗体：**text** 或 __text__
    /\*\*[^*]+\*\*/,
    /__[^_]+__/,
    // 斜体：*text* 或 _text_（但不在单词中间）
    /\*[^*]+\*/,
    /_[^_]+_/,
    // 删除线：~~text~~
    /~~[^~]+~~/,
    // 高亮：==text==
    /==[^=]+==/,
    // 复选框：[x] 或 [ ]
    /^\s*[-*+]\s+\[[ xX]\]\s+.+/m,
    // 引用：> 开头
    /^\s*>\s+.+/m,
    // 水平线：--- 或 ***
    /^\s*[-*]{3,}\s*$/m
  ];
  let t = 0;
  for (const s of e)
    s.test(n) && t++;
  const o = [
    /^\s*#{1,6}\s+/m,
    // 标题
    /^\s*[-*+]\s+/m,
    // 无序列表
    /^\s*\d+\.\s+/m,
    // 有序列表
    /```[\s\S]*?```/,
    // 代码块
    /^\s*\|.+\|/m
    // 表格
  ].some((s) => s.test(n));
  return t >= 2 || o;
}
class RM {
  constructor(n, e) {
    I(this, "plugin");
    I(this, "uid");
    I(this, "control");
    this.control = n, this.plugin = e, this.uid = e.displayer.uid;
  }
  getNamespaceData() {
    var n;
    return Vt((n = this.plugin) == null ? void 0 : n.attributes[this.namespace]) || {};
  }
  getAuthSpaceData() {
    var n;
    return Vt((n = this.plugin) == null ? void 0 : n.attributes[this.authSpace]) || {};
  }
  getGlobalUndoRedoStackData() {
    var n;
    return Vt((n = this.plugin) == null ? void 0 : n.attributes[this.globalUndoRedoStackSpace]) || {};
  }
  getUidFromKey(n) {
    return n.split(st).length === 2 && n.split(st)[0] || this.uid;
  }
  isLocalId(n) {
    return n.split(st).length === 1;
  }
  getLocalId(n) {
    return n.split(st)[1];
  }
  isSelector(n) {
    return this.getLocalId(n) === $;
  }
}
const Re = class Re extends RM {
  constructor(e, t, i) {
    super(e, t);
    I(this, "namespace");
    I(this, "authSpace");
    I(this, "globalUndoRedoStackSpace");
    I(this, "serviceStorage", {});
    I(this, "storage", {});
    I(this, "authStorage", {});
    I(this, "globalUndoRedoStackStorage", {});
    I(this, "stateDisposer");
    I(this, "authDisposer");
    I(this, "asyncClockState", !1);
    I(this, "limitNums", /* @__PURE__ */ new Map());
    I(this, "syncInterval");
    I(this, "limitComponents", Pt(
      (e, t) => {
        var s, a, c, l, h, u, g, M;
        const i = ((a = (s = this.storage) == null ? void 0 : s[e]) == null ? void 0 : a[t]) && Object.keys(this.storage[e][t]).length || 0;
        if (i === 0)
          return;
        let o = this.limitNums.get(`${e}/${t}`);
        if (!o) {
          const d = this.getLimitNum(i);
          this.limitNums.set(`${e}/${t}`, d), o = d;
        }
        if (o > 0 && i >= o) {
          const d = Re.componentsLimits.indexOf(o), y = d >= 0 && d < Re.componentsLimits.length - 1;
          if ((h = (l = (c = this.control.room) == null ? void 0 : c.callbacks) == null ? void 0 : l.listeners) != null && h.map.onObjectsLimit && ((g = (u = this.control.room) == null ? void 0 : u.callbacks) == null || g.dispatch(
            "onObjectsLimit",
            (p) => p(o, y)
          )), !y) {
            this.control.room.logger.error(
              `exceeds the limit of ${o} components, current: ${i}`
            ), (M = this.control.room) == null || M.disconnect().then(() => {
              var p, f, N, S, C;
              (N = (f = (p = this.control.room) == null ? void 0 : p.callbacks) == null ? void 0 : f.listeners) != null && N.map.onKickedWithReason && ((C = (S = this.control.room) == null ? void 0 : S.callbacks) == null || C.dispatch(
                "onKickedWithReason",
                (k) => k("kickByAdmin")
              ));
            });
            return;
          }
          console.warn(
            `exceeds the limit of ${o} components, current: ${i}`
          );
          const w = y ? Re.componentsLimits[d + 1] : 0;
          this.limitNums.set(`${e}/${t}`, w);
        }
      },
      2e3,
      { maxWait: 5e3 }
    ));
    I(this, "clearLimitComponents", (e, t) => {
      if (t)
        this.limitNums.delete(`${e}/${t}`);
      else
        for (const i of this.limitNums.keys())
          `${e}/`.indexOf(i) === 0 && this.limitNums.delete(i);
    });
    this.syncInterval = i, this.namespace = Re.namespace, this.authSpace = Re.authSpace, this.globalUndoRedoStackSpace = Re.globalUndoRedoStackSpace, this.serviceStorage = this.getNamespaceData(), this.storage = se(this.serviceStorage), this.authStorage = this.getAuthSpaceData(), this.globalUndoRedoStackStorage = {}, this.getLimitNums();
  }
  hasId(e, t, i) {
    const o = this.getLocalId(i) ? this.transformKey(i) : i;
    return !!(this.storage[e] && this.storage[e][t] && this.storage[e][t][o]);
  }
  getLimitNum(e) {
    return e >= Re.componentsLimits[0] ? Re.componentsLimits[1] : Re.componentsLimits[0];
  }
  getLimitNums() {
    for (const [e, t] of Object.entries(this.storage))
      if (e && t) {
        for (const [i, o] of Object.entries(t))
          if (i && o) {
            const s = this.getLimitNum(Object.keys(o).length);
            this.limitNums.set(`${e}/${i}`, s);
          }
      }
  }
  getViewIdBySecenPath(e) {
    const t = this.getNamespaceData();
    for (const [i, o] of Object.entries(t))
      if (o) {
        for (const s of Object.keys(o))
          if (s === e)
            return i;
      }
  }
  getScenePathData(e) {
    const t = this.getNamespaceData();
    for (const i of Object.values(t))
      if (i) {
        for (const o of Object.keys(i))
          if (o === e)
            return se(i[o]);
      }
  }
  hasTargetDataByViewId(e, t) {
    const i = this.getNamespaceData();
    if (i && i[e]) {
      for (const o of Object.values(i[e]))
        for (const s of Object.keys(o))
          if (s === t)
            return !0;
    }
  }
  getTargetDataByViewId(e, t) {
    const i = this.getNamespaceData();
    let o, s;
    if (i && i[e])
      for (const a of Object.values(i[e]))
        for (const c of Object.keys(a))
          c === t && (o = se(a[c]), s = c);
    return { data: o, scenePath: s };
  }
  getStorageData(e, t) {
    const i = this.getNamespaceData();
    return i && i[e] && se(i[e][t]) || void 0;
  }
  hasSelector(e, t) {
    const i = this.storage && this.storage[e] && this.storage[e][t];
    return !!(i && Object.keys(i).find(
      (o) => this.isOwn(o) && this.getLocalId(o) === $
    ));
  }
  hasIncludeSelector(e, t, i) {
    for (const o of Object.keys(this.storage))
      if (!(t && o !== t)) {
        for (const s of Object.keys(this.storage[o]))
          if (!(i && s !== i)) {
            for (const a of Object.keys(this.storage[o][s]))
              if (this.getLocalId(a) === $) {
                const c = this.storage[o][s][a], l = this.isLocalId(e) ? this.transformKey(e) : e;
                if (c && c.selectIds && c.selectIds.length && c.selectIds.includes(l))
                  return !0;
              }
          }
      }
    return !1;
  }
  addStorageStateListener(e) {
    this.stateDisposer = fn(async () => {
      var c, l, h, u, g, M;
      const t = this.getNamespaceData(), { diffView: i, diffScenePath: o, diffData: s } = this.diffFunByView(
        this.serviceStorage,
        t
      );
      this.serviceStorage = t;
      const a = {};
      if (Object.keys(i).length > 0)
        for (const [d, y] of Object.entries(i))
          y && !le((c = this.storage) == null ? void 0 : c[d], y.newValue) && (a.diffView || (a.diffView = {}), a.diffView[d] = y, this.storage || (this.storage = {}), y.newValue === void 0 ? delete this.storage[d] : this.storage[d] = se(y.newValue));
      if (Object.keys(o).length > 0) {
        for (const [d, y] of Object.entries(o))
          if (y) {
            const { viewId: w, newValue: p } = y;
            y && !le((h = (l = this.storage) == null ? void 0 : l[w]) == null ? void 0 : h[d], p) && (a.diffScenePath || (a.diffScenePath = {}), a.diffScenePath[d] = y, this.storage || (this.storage = {}), this.storage[w] || (this.storage[w] = {}), y.newValue === void 0 ? delete this.storage[w][d] : this.storage[w][d] = se(y.newValue));
          }
      }
      if (Object.keys(s).length > 0) {
        for (const [d, y] of Object.entries(s))
          if (y) {
            const { viewId: w, scenePath: p, newValue: f } = y;
            y && !le((M = (g = (u = this.storage) == null ? void 0 : u[w]) == null ? void 0 : g[p]) == null ? void 0 : M[d], f) && (a.diffData || (a.diffData = {}), a.diffData[d] = y, this.storage || (this.storage = {}), this.storage[w] || (this.storage[w] = {}), this.storage[w][p] || (this.storage[w][p] = {}), y.newValue === void 0 ? delete this.storage[w][p][d] : this.storage[w][p][d] = se(
              y.newValue
            ));
          }
      }
      Object.keys(a).length > 0 && e(a), this.control.worker.publicMsgEmitter.emit(
        "syncStorageChange",
        this.serviceStorage
      );
    });
  }
  removeStorageStateListener() {
    this.stateDisposer && this.stateDisposer();
  }
  diffFunByView(e, t) {
    const i = ze(e), o = ze(t), s = {}, a = {}, c = {};
    for (const l of o)
      if (!i.includes(l) || e[l] === void 0) {
        s[l] = {
          newValue: t[l],
          oldValue: void 0
        };
        continue;
      }
    for (const l of i) {
      if (le(e[l], t[l]))
        continue;
      if (!o.includes(l) || t[l] === void 0) {
        s[l] = {
          newValue: void 0,
          oldValue: e[l]
        };
        continue;
      }
      const h = this.diffFunByScenePath(
        e[l],
        t[l],
        l
      );
      Object.assign(a, h.diffScenePath), Object.assign(c, h.diffData);
    }
    return { diffData: c, diffScenePath: a, diffView: s };
  }
  diffFunByScenePath(e, t, i) {
    const o = ze(e), s = ze(t), a = {}, c = {};
    for (const l of s)
      if (!o.includes(l) || e[l] === void 0) {
        a[l] = {
          newValue: t[l],
          oldValue: void 0,
          viewId: i
        };
        continue;
      }
    for (const l of o) {
      if (le(e[l], t[l]))
        continue;
      if (!s.includes(l) || t[l] === void 0) {
        a[l] = {
          newValue: void 0,
          oldValue: e[l],
          viewId: i
        };
        continue;
      }
      const h = this.diffFunByKeys(
        e[l],
        t[l],
        l,
        i
      );
      Object.assign(c, h);
    }
    return { diffScenePath: a, diffData: c };
  }
  diffFunByKeys(e, t, i, o) {
    const s = ze(e), a = ze(t), c = {};
    for (const l of a)
      (!s.includes(l) || e[l] === void 0) && (c[l] = {
        oldValue: void 0,
        newValue: t[l],
        viewId: o,
        scenePath: i
      });
    for (const l of s)
      if (!le(e[l], t[l])) {
        if (!a.includes(l) || t[l] === void 0) {
          c[l] = {
            newValue: void 0,
            oldValue: e[l],
            viewId: o,
            scenePath: i
          };
          continue;
        }
        a.includes(l) && (c[l] = {
          oldValue: e[l],
          newValue: t[l],
          viewId: o,
          scenePath: i
        });
      }
    return c;
  }
  transformKey(e) {
    return this.uid + st + e;
  }
  isOwn(e) {
    return e.split(st)[0] === this.uid;
  }
  dispatch(e) {
    var w;
    const {
      type: t,
      workId: i,
      ops: o,
      index: s,
      opt: a,
      toolsType: c,
      removeIds: l,
      updateNodeOpt: h,
      op: u,
      selectIds: g,
      isSync: M,
      scenePath: d,
      viewId: y
    } = e;
    if (y)
      switch (t) {
        case R.Clear:
          const p = {};
          d && this.storage[y] && this.storage[y][d] ? (delete this.storage[y][d], this.setState(p, { isSync: M, viewId: y, scenePath: d })) : this.storage[y] && (delete this.storage[y], this.setState(p, { isSync: M, viewId: y, scenePath: "" })), ot(
            () => {
              var k, v;
              (k = this.control.pluginManager) == null || k.dispatchPluginEvent(
                "syncStorage",
                y,
                d
              ), this.control.isUseSimple || (v = this.control.miniMapManager) == null || v.updateMiniMapCamera(y), this.clearLimitComponents(y, d);
            },
            this.syncInterval,
            this.control.hasPolyfillMethod("requestIdleCallback")
          );
          break;
        case R.CreateWork:
          if (d && i && c && a) {
            const k = this.isLocalId(i.toString()) ? this.transformKey(i) : i;
            this.control.renderControl.isUsePageAuth(y) && this.control.renderControl.addRenderPageElementId({
              viewId: y,
              scenePath: d,
              elementId: k.toString()
            }), this.updateValue(
              k.toString(),
              {
                type: R.CreateWork,
                workId: i,
                toolsType: c,
                opt: a
              },
              { isSync: M, viewId: y, scenePath: d }
            );
          }
          break;
        case R.DrawWork:
          if (d && i && typeof s == "number" && (u != null && u.length)) {
            const k = this.isLocalId(i.toString()) ? this.transformKey(i) : i, v = this.storage[y] && this.storage[y][d] && this.storage[y][d][k] || void 0;
            if (v != null && v.ops || (v == null ? void 0 : v.type) === R.FullWork)
              return;
            const A = s ? ((v == null ? void 0 : v.op) || []).slice(0, s).concat(u) : u || (v == null ? void 0 : v.op), P = c || (v == null ? void 0 : v.toolsType), E = a || (v == null ? void 0 : v.opt), O = h || (v == null ? void 0 : v.updateNodeOpt);
            i && P && E && A && (this.control.renderControl.isUsePageAuth(y) && this.control.renderControl.addRenderPageElementId({
              viewId: y,
              scenePath: d,
              elementId: k.toString()
            }), this.updateValue(
              k.toString(),
              {
                ...v,
                type: R.DrawWork,
                workId: i,
                updateNodeOpt: O,
                toolsType: P,
                opt: E,
                op: A,
                index: s
              },
              { isSync: M, viewId: y, scenePath: d }
            ));
          }
          break;
        case R.FullWork:
          if (d && i) {
            const k = this.isLocalId(i.toString()) ? this.transformKey(i) : i, v = this.storage[y] && this.storage[y][d] && this.storage[y][d][k] || void 0, A = h || (v == null ? void 0 : v.updateNodeOpt), P = c || (v == null ? void 0 : v.toolsType), E = a || (v == null ? void 0 : v.opt), O = o || (v == null ? void 0 : v.ops);
            P && E && (this.control.renderControl.isUsePageAuth(y) && this.control.renderControl.addRenderPageElementId({
              viewId: y,
              scenePath: d,
              elementId: k.toString()
            }), this.updateValue(
              k.toString(),
              {
                type: R.FullWork,
                updateNodeOpt: A,
                workId: k,
                toolsType: P,
                opt: E,
                ops: O
              },
              { isSync: M, viewId: y, scenePath: d }
            ), ot(
              () => {
                var z, L;
                (z = this.control.pluginManager) == null || z.dispatchPluginEvent(
                  "syncStorage",
                  y,
                  d
                ), this.control.isUseSimple || (L = this.control.miniMapManager) == null || L.updateMiniMapCamera(y), this.limitComponents(y, d);
              },
              this.syncInterval,
              this.control.hasPolyfillMethod("requestIdleCallback")
            ));
          }
          break;
        case R.RemoveNode:
          if (d && (l != null && l.length)) {
            const k = l.map((v) => this.isLocalId(v + "") ? this.transformKey(v) : v);
            this.storage[y] && this.storage[y][d] && (Object.keys(this.storage[y][d]).map((v) => {
              k != null && k.includes(v) && (this.control.renderControl.isUsePageAuth(y) && this.control.renderControl.delRenderPageElementId({
                viewId: y,
                scenePath: d,
                elementId: v
              }), this.updateValue(v, void 0, { isSync: M, viewId: y, scenePath: d }));
            }), (w = this.control.pluginManager) == null || w.dispatchPluginEvent(
              "syncStorage",
              y,
              d
            ), this.control.isUseSimple || ot(
              () => {
                var v;
                (v = this.control.miniMapManager) == null || v.updateMiniMapCamera(y);
              },
              this.syncInterval,
              this.control.hasPolyfillMethod("requestIdleCallback")
            ));
          }
          break;
        case R.UpdateNode:
          if (d && i && (h || o || a)) {
            const k = this.isLocalId(i.toString()) ? this.transformKey(i) : i, v = this.storage[y] && this.storage[y][d] && this.storage[y][d][k] || void 0;
            v && (v.updateNodeOpt = h, (o || u) && (v.ops = o, v.op = u), a && (v.opt = a), v.type = R.FullWork, this.updateValue(k.toString(), v, {
              isSync: M,
              viewId: y,
              scenePath: d
            }), this.control.isUseSimple || ot(
              () => {
                var A;
                (A = this.control.miniMapManager) == null || A.updateMiniMapCamera(y);
              },
              this.syncInterval,
              this.control.hasPolyfillMethod("requestIdleCallback")
            ));
          }
          break;
        case R.Select:
          if (!d)
            return;
          let f;
          g != null && g.length && (f = g.map((k) => this.isLocalId(k + "") ? this.transformKey(k) : k));
          const N = this.transformKey($), S = this.storage[y] && this.storage[y][d] && this.storage[y][d][N] || void 0, C = a || (S == null ? void 0 : S.opt);
          f && this.checkOtherSelector(N, f, {
            isSync: M,
            viewId: y,
            scenePath: d
          }), this.updateValue(
            N,
            f && {
              type: R.Select,
              toolsType: T.Selector,
              opt: C,
              selectIds: f
            },
            { isSync: M, viewId: y, scenePath: d }
          );
          break;
      }
  }
  checkOtherSelector(e, t, i) {
    const { viewId: o, scenePath: s } = i;
    for (const a of Object.keys(this.storage[o][s]))
      if (a !== e && this.getLocalId(a) === $) {
        const c = this.storage[o][s][a];
        if (c && c.selectIds) {
          const l = c.selectIds.filter((h) => !t.includes(h));
          l.length > 0 && (c.selectIds = l), this.updateValue(a, l.length && c || void 0, i);
        }
      }
  }
  setState(e, t) {
    const { viewId: i, scenePath: o } = t, s = ze(e);
    for (let a = 0; a < s.length; a++) {
      const c = s[a], l = e[c];
      typeof l < "u" ? (this.storage[i] || (this.storage[i] = {}), this.storage[i][o] || (this.storage[i][o] = {}), this.storage[i][o][c] = l) : delete this.storage[i][o][c];
    }
    this.runSyncService(t);
  }
  updateValue(e, t, i) {
    const { viewId: o, scenePath: s, storage: a } = i, c = a || this.storage;
    t === void 0 ? delete c[o][s][e] : (c[o] || (c[o] = {}), c[o][s] || (c[o][s] = {}), c[o][s][e] = t), this.runSyncService(i);
  }
  runSyncService(e) {
    if (!this.control.renderControl.isWritable) {
      this.control.room && this.control.room.logger ? this.control.room.logger.error(
        "The current state is not writable and synchronization cannot be synchronized"
      ) : console.error(
        "The current state is not writable and synchronization cannot be synchronized"
      );
      return;
    }
    this.asyncClockState || (this.asyncClockState = !0, e.isSync ? (this.asyncClockState = !1, this.syncSerivice(e)) : ot(
      () => {
        this.asyncClockState = !1, this.syncSerivice(e);
      },
      this.syncInterval,
      this.control.hasPolyfillMethod("requestIdleCallback")
    ));
  }
  syncSerivice(e = {
    storage: this.storage
  }) {
    const { storage: t } = e, i = t || this.storage, o = ze(this.serviceStorage), s = ze(i), a = /* @__PURE__ */ new Map();
    for (const c of o) {
      if (!s.includes(c)) {
        a.set(c, void 0);
        continue;
      }
      le(this.serviceStorage[c], i[c]) || this.syncViewData(i, c);
    }
    for (const c of s)
      o.includes(c) || a.set(c, i[c]);
    if (a.size > 5)
      this.syncStorageView(i);
    else
      for (const [c, l] of a.entries())
        this.syncUpdataView(i, c, l);
  }
  syncViewData(e, t) {
    const i = ze(this.serviceStorage[t]), o = ze(e[t]), s = /* @__PURE__ */ new Map();
    for (const a of i) {
      if (!o.includes(a)) {
        s.set(a, void 0);
        continue;
      }
      le(
        this.serviceStorage[t][a],
        e[t][a]
      ) || this.syncScenePathData(e, t, a);
    }
    for (const a of o)
      i.includes(a) || s.set(a, e[t][a]);
    if (s.size > 5)
      this.syncStorageScenePath(t, e[t]);
    else
      for (const [a, c] of s.entries())
        this.syncUpdataScenePath(e, t, a, c);
  }
  syncScenePathData(e, t, i) {
    const o = ze(this.serviceStorage[t][i]), s = ze(e[t][i]), a = /* @__PURE__ */ new Map();
    for (const c of o) {
      if (!s.includes(c)) {
        a.set(c, void 0);
        continue;
      }
      le(
        this.serviceStorage[t][i][c],
        e[t][i][c]
      ) || a.set(c, e[t][i][c]);
    }
    for (const c of s)
      o.includes(c) || a.set(c, e[t][i][c]);
    if (a.size > 5)
      this.syncStorageKey(t, i, e[t][i]);
    else
      for (const [c, l] of a.entries())
        this.syncUpdataKey(e, t, i, c, l);
  }
  syncUpdataView(e, t, i) {
    var s;
    Object.keys(this.serviceStorage).length ? (s = this.plugin) == null || s.updateAttributes([this.namespace, t], i) : this.syncStorageView(e);
  }
  syncStorageView(e) {
    var t;
    (t = this.plugin) == null || t.updateAttributes([this.namespace], e);
  }
  syncUpdataScenePath(e, t, i, o) {
    var a;
    Object.keys(this.serviceStorage[t]).length ? (a = this.plugin) == null || a.updateAttributes([this.namespace, t, i], o) : this.syncStorageScenePath(t, e[t]);
  }
  syncStorageScenePath(e, t) {
    var i;
    (i = this.plugin) == null || i.updateAttributes([this.namespace, e], t);
  }
  syncUpdataKey(e, t, i, o, s) {
    var c;
    Object.keys(this.serviceStorage[t][i]).length ? (c = this.plugin) == null || c.updateAttributes(
      [this.namespace, t, i, o],
      s
    ) : this.syncStorageKey(t, i, e[t][i]);
  }
  syncStorageKey(e, t, i) {
    var o;
    (o = this.plugin) == null || o.updateAttributes([this.namespace, e, t], i);
  }
  keyTransformWorkId(e) {
    const t = e.split(st);
    return t.length === 2 ? t[1] : e;
  }
  destroy() {
    this.removeStorageStateListener(), this.serviceStorage = {}, this.storage = {}, this.removeAuthStateListener(), this.authStorage = {}, this.globalUndoRedoStackStorage = {};
  }
  transformToSerializableUndoStackItemData(e) {
    const { data: t, scenePath: i } = e, o = {};
    for (const s of t)
      o[s.key] = {
        dataType: s.dataType,
        data: s.data
      };
    return {
      data: o,
      scenePath: i
    };
  }
  dispatchUndoTicker(e) {
    const { viewId: t, undoTickerId: i, data: o, syncType: s, operation: a } = e, c = a === be.syncUndoStack ? "undoStack" : "redoStack", l = s === "delete" ? void 0 : this.transformToSerializableUndoStackItemData(o);
    if (this.plugin)
      if (this.plugin.getAttributesValue([this.globalUndoRedoStackSpace]))
        if (this.plugin.getAttributesValue([this.globalUndoRedoStackSpace, t]))
          if (this.plugin.getAttributesValue([
            this.globalUndoRedoStackSpace,
            t,
            c
          ]))
            this.globalUndoRedoStackStorage[t][c][i] = l, this.plugin.updateAttributes(
              [this.globalUndoRedoStackSpace, t, c, i],
              l
            );
          else {
            const h = { [i]: l };
            this.globalUndoRedoStackStorage[t][c] = h, this.plugin.updateAttributes(
              [this.globalUndoRedoStackSpace, t, c],
              h
            );
          }
        else {
          const h = {
            [c]: {
              [i]: l
            }
          };
          this.globalUndoRedoStackStorage[t] = h, this.plugin.updateAttributes(
            [this.globalUndoRedoStackSpace, t],
            h
          );
        }
      else {
        const h = {
          [t]: {
            [c]: {
              [i]: l
            }
          }
        };
        this.globalUndoRedoStackStorage = h, this.plugin.updateAttributes(this.globalUndoRedoStackSpace, h);
      }
  }
  addGlobalUndoRedoStackListener(e) {
    this.authDisposer = fn(async () => {
      const t = this.getGlobalUndoRedoStackData();
      if (le(this.globalUndoRedoStackStorage, t))
        return;
      const i = this.diffGlobalUndoRedoStackFun(
        this.globalUndoRedoStackStorage,
        t
      );
      this.globalUndoRedoStackStorage = t, e(i);
    });
  }
  diffGlobalUndoRedoStackFun(e, t) {
    const i = ze(e), o = ze(t), s = {};
    for (const a of o)
      if (!i.includes(a) || e[a] === void 0) {
        s[a] = {
          newValue: t[a],
          oldValue: void 0
        };
        continue;
      }
    for (const a of i)
      if (!le(e[a], t[a])) {
        if (!o.includes(a) || t[a] === void 0) {
          s[a] = {
            newValue: void 0,
            oldValue: e[a]
          };
          continue;
        }
        s[a] = {
          newValue: t[a],
          oldValue: e[a]
        };
      }
    return s;
  }
  dispatchPageAuth(e, t = !0) {
    var l, h, u, g, M, d, y, w, p, f, N, S, C, k, v, A, P, E, O, z, L, j, F, X, Y, Q, _, ce, ie, K, H, fe, Je, Xe, ht, tt, Ke, mt;
    const { operation: i, viewId: o, scenePath: s, pageId: a, pageData: c } = e;
    switch (i) {
      case be.addPage: {
        if (!o || !s || !a)
          return;
        const Ee = [this.authSpace];
        Object.keys(this.authStorage).length && Ee.push("pageAuth");
        const V = this.authStorage.pageAuth || {};
        this.authStorage.pageAuth = V, Object.keys(V).length && Ee.push(o), V[o] = V[o] || {}, Object.keys(V[o]).length && Ee.push(s), V[o][s] = V[o][s] || {
          render: (c == null ? void 0 : c.render) || "localSelf",
          pages: {}
        }, Object.keys(V[o][s].pages).length && Ee.push("pages", a);
        const B = {};
        if (c != null && c.elementIds)
          for (const Z of c.elementIds.keys())
            B[Z] = !0;
        V[o][s].pages[a] = B, t && (Ee.length === 1 ? (l = this.plugin) == null || l.updateAttributes(
          Ee,
          se(this.authStorage)
        ) : Ee.length === 2 ? (h = this.plugin) == null || h.updateAttributes(Ee, se(V)) : Ee.length === 3 ? (u = this.plugin) == null || u.updateAttributes(
          Ee,
          se(V[o])
        ) : Ee.length === 4 ? (g = this.plugin) == null || g.updateAttributes(
          Ee,
          se(V[o][s])
        ) : (M = this.plugin) == null || M.updateAttributes(Ee, se(B)));
        return;
      }
      case be.renderPage: {
        if (!o || !s || !(c != null && c.render))
          return;
        this.authStorage.pageAuth && this.authStorage.pageAuth[o] && this.authStorage.pageAuth[o][s] && c.render !== this.authStorage.pageAuth[o][s].render && (this.authStorage.pageAuth[o][s].render = c.render, t && ((d = this.plugin) == null || d.updateAttributes(
          [this.authSpace, "pageAuth", o, s, "render"],
          c.render
        )));
        return;
      }
      case be.deletePage: {
        if (!o || !s || !a)
          return;
        (N = (f = (p = (w = (y = this.authStorage) == null ? void 0 : y.pageAuth) == null ? void 0 : w[o]) == null ? void 0 : p[s]) == null ? void 0 : f.pages) != null && N[a] && (delete this.authStorage.pageAuth[o][s].pages[a], t && ((S = this.plugin) == null || S.updateAttributes(
          [this.authSpace, "pageAuth", o, s, "pages", a],
          void 0
        )));
        return;
      }
      case be.clearPage: {
        if (!o || !s || !a)
          return;
        (P = (A = (v = (k = (C = this.authStorage) == null ? void 0 : C.pageAuth) == null ? void 0 : k[o]) == null ? void 0 : v[s]) == null ? void 0 : A.pages) != null && P[a] && (delete this.authStorage.pageAuth[o][s].pages[a], t && ((E = this.plugin) == null || E.updateAttributes(
          [this.authSpace, "pageAuth", o, s, "pages", a],
          {}
        )));
        return;
      }
      case be.addElement: {
        if (!o || !s || !a || !(c != null && c.elementId))
          return;
        (F = (j = (L = (z = (O = this.authStorage) == null ? void 0 : O.pageAuth) == null ? void 0 : z[o]) == null ? void 0 : L[s]) == null ? void 0 : j.pages) != null && F[a] && (this.authStorage.pageAuth[o][s].pages[a][c.elementId] = !0, t && ((X = this.plugin) == null || X.updateAttributes(
          [
            this.authSpace,
            "pageAuth",
            o,
            s,
            "pages",
            a,
            c.elementId
          ],
          !0
        )));
        return;
      }
      case be.deleteElement: {
        if (!o || !s || !a || !(c != null && c.elementId))
          return;
        (K = (ie = (ce = (_ = (Q = (Y = this.authStorage) == null ? void 0 : Y.pageAuth) == null ? void 0 : Q[o]) == null ? void 0 : _[s]) == null ? void 0 : ce.pages) == null ? void 0 : ie[a]) != null && K[c.elementId] && (delete this.authStorage.pageAuth[o][s].pages[a][c.elementId], t && ((H = this.plugin) == null || H.updateAttributes(
          [
            this.authSpace,
            "pageAuth",
            o,
            s,
            "pages",
            a,
            c.elementId
          ],
          void 0
        )));
        return;
      }
      case be.clearViewScenePath: {
        if (!o || !s)
          return;
        (Xe = (Je = (fe = this.authStorage) == null ? void 0 : fe.pageAuth) == null ? void 0 : Je[o]) != null && Xe[s] && (delete this.authStorage.pageAuth[o][s], t && ((ht = this.plugin) == null || ht.updateAttributes(
          [this.authSpace, "pageAuth", o, s],
          void 0
        )));
        return;
      }
      case be.clearView: {
        if (!o)
          return;
        (Ke = (tt = this.authStorage) == null ? void 0 : tt.pageAuth) != null && Ke[o] && (delete this.authStorage.pageAuth[o], t && ((mt = this.plugin) == null || mt.updateAttributes(
          [this.authSpace, "pageAuth", o],
          void 0
        )));
        return;
      }
    }
  }
  dispatchRenderAuth(e, t = !0) {
    var l, h, u, g, M, d, y, w, p, f, N, S, C, k, v, A, P, E, O, z;
    const { operation: i, viewId: o, uid: s, renderData: a, isClear: c } = e;
    if (o)
      switch (i) {
        case et.setAuth: {
          if (!a || a.render === void 0 && a.hide === void 0 && a.clear === void 0) {
            this.authStorage.renderAuth && this.authStorage.renderAuth[o] && (delete this.authStorage.renderAuth[o], t && ((l = this.plugin) == null || l.updateAttributes(
              [this.authSpace, "renderAuth", o],
              void 0
            )));
            return;
          }
          const L = [this.authSpace];
          Object.keys(this.authStorage).length && L.push("renderAuth");
          const j = this.authStorage.renderAuth || {};
          Object.keys(j).length && L.push(o), j[o] = {}, this.authStorage.renderAuth = j;
          const { render: F, hide: X, clear: Y } = a;
          if (F === !0)
            j[o].render = !0;
          else if (Be(F)) {
            j[o].render = {};
            for (const Q of F.keys())
              j[o].render[Q] = !0;
          }
          if (X === !0)
            j[o].hide = !0;
          else if (Be(X)) {
            j[o].hide = {};
            for (const Q of X.keys())
              j[o].hide[Q] = !0;
          }
          if (Y === !0)
            j[o].clear = !0;
          else if (Be(Y)) {
            j[o].clear = {};
            for (const Q of Y.keys())
              j[o].clear[Q] = !0;
          }
          t && (L.length === 1 ? (h = this.plugin) == null || h.updateAttributes(
            L,
            se(this.authStorage)
          ) : L.length === 2 ? (u = this.plugin) == null || u.updateAttributes(L, se(j)) : (g = this.plugin) == null || g.updateAttributes(
            L,
            se(j[o])
          ));
          return;
        }
        case et.setShow: {
          if (!s)
            return;
          const L = [this.authSpace];
          Object.keys(this.authStorage).length && L.push("renderAuth");
          const j = this.authStorage.renderAuth || {};
          Object.keys(j).length && L.push(o), j[o] = j[o] || {}, this.authStorage.renderAuth = j, Object.keys(j[o]).length && L.push("render"), j[o].render = j[o].render || {}, j[o].render !== !0 && (Object.keys(j[o].render).length && L.push(s), j[o].render[s] = !0), j[o].hide !== !0 && ((M = j[o].hide) != null && M[s]) && (delete j[o].hide[s], t && ((d = this.plugin) == null || d.updateAttributes(
            [this.authSpace, "renderAuth", o, "hide", s],
            void 0
          ))), t && (L.length === 1 ? (y = this.plugin) == null || y.updateAttributes(
            L,
            se(this.authStorage)
          ) : L.length === 2 ? (w = this.plugin) == null || w.updateAttributes(L, se(j)) : L.length === 3 ? (p = this.plugin) == null || p.updateAttributes(
            L,
            se(j[o])
          ) : (f = this.plugin) == null || f.updateAttributes(L, !0));
          return;
        }
        case et.setHide: {
          if (!s)
            return;
          const L = [this.authSpace];
          Object.keys(this.authStorage).length && L.push("renderAuth");
          const j = this.authStorage.renderAuth || {};
          Object.keys(j).length && L.push(o), j[o] = j[o] || {}, this.authStorage.renderAuth = j, Object.keys(j[o]).length && L.push("hide"), j[o].hide = j[o].hide || {}, j[o].hide !== !0 && (Object.keys(j[o].hide).length && L.push(s), j[o].hide[s] = !0), j[o].render !== !0 && ((N = j[o].render) != null && N[s]) && (delete j[o].render[s], t && ((S = this.plugin) == null || S.updateAttributes(
            [this.authSpace, "renderAuth", o, "render", s],
            void 0
          ))), t && (L.length === 1 ? (C = this.plugin) == null || C.updateAttributes(
            L,
            se(this.authStorage)
          ) : L.length === 2 ? (k = this.plugin) == null || k.updateAttributes(L, se(j)) : L.length === 3 ? (v = this.plugin) == null || v.updateAttributes(
            L,
            se(j[o])
          ) : (A = this.plugin) == null || A.updateAttributes(L, !0));
          return;
        }
        case et.setClear: {
          if (!s)
            return;
          const L = [this.authSpace];
          Object.keys(this.authStorage).length && L.push("renderAuth");
          const j = this.authStorage.renderAuth || {};
          Object.keys(j).length && L.push(o), j[o] = j[o] || {}, this.authStorage.renderAuth = j, Object.keys(j[o]).length && L.push("clear"), j[o].clear = j[o].clear || {}, j[o].clear !== !0 && (Object.keys(j[o].clear).length && L.push(s), c ? j[o].clear[s] = !0 : delete j[o].clear[s]), t && (L.length === 1 ? (P = this.plugin) == null || P.updateAttributes(
            L,
            se(this.authStorage)
          ) : L.length === 2 ? (E = this.plugin) == null || E.updateAttributes(L, se(j)) : L.length === 3 ? (O = this.plugin) == null || O.updateAttributes(
            L,
            se(j[o])
          ) : (z = this.plugin) == null || z.updateAttributes(L, c ? !0 : void 0));
          return;
        }
      }
  }
  addAuthStateListener(e) {
    this.authDisposer = fn(async () => {
      const t = this.getAuthSpaceData(), { renderAuth: i, pageAuth: o } = t;
      let s, a;
      le(i, this.authStorage.renderAuth) || (s = this.diffAuthFun(
        this.authStorage.renderAuth || {},
        i || {}
      ), this.authStorage.renderAuth = i), le(o, this.authStorage.pageAuth) || (a = this.diffAuthPageFun(
        this.authStorage.pageAuth || {},
        o || {}
      ), this.authStorage.pageAuth = o), (s || a) && e(s, a);
    });
  }
  removeAuthStateListener() {
    this.authDisposer && this.authDisposer();
  }
  diffAuthFun(e, t) {
    const i = ze(e), o = ze(t), s = {};
    for (const a of o)
      if (!i.includes(a) || e[a] === void 0) {
        s[a] = {
          newValue: t[a],
          oldValue: void 0
        };
        continue;
      }
    for (const a of i)
      if (!le(e[a], t[a])) {
        if (!o.includes(a) || t[a] === void 0) {
          s[a] = {
            newValue: void 0,
            oldValue: e[a]
          };
          continue;
        }
        s[a] = {
          newValue: t[a],
          oldValue: e[a]
        };
      }
    return s;
  }
  diffAuthPageFun(e, t) {
    const i = ze(e), o = ze(t), s = {};
    for (const a of o)
      if (!i.includes(a) || e[a] === void 0) {
        s[a] = {
          newValue: t[a],
          oldValue: void 0
        };
        continue;
      }
    for (const a of i)
      if (!le(e[a], t[a])) {
        if (!o.includes(a) || t[a] === void 0) {
          s[a] = {
            newValue: void 0,
            oldValue: e[a]
          };
          continue;
        }
        s[a] = {
          newValue: t[a],
          oldValue: e[a]
        };
      }
    return s;
  }
};
I(Re, "namespace", "PluginState"), I(Re, "authSpace", "PluginAuth"), I(Re, "globalUndoRedoStackSpace", "GlobalUndoRedoStack"), // static syncInterval: number = 500;
I(Re, "componentsLimits", [500, 1e3]);
let xn = Re;
var Ve = /* @__PURE__ */ ((r) => (r[r.Draw = 1] = "Draw", r[r.Delete = 2] = "Delete", r[r.Update = 3] = "Update", r))(Ve || {});
class YM {
  constructor() {
    I(this, "roomMembers", []);
    I(this, "onChangeHooks", /* @__PURE__ */ new Set());
  }
  setRoomMembers(n) {
    this.executChangeUidHook(this.roomMembers, n), this.roomMembers = n;
  }
  executChangeUidHook(n, e) {
    const t = {
      online: e.map((i) => {
        var o;
        return ((o = i.payload) == null ? void 0 : o.uid) || i.session;
      }),
      offline: n.map((i) => {
        var o;
        return ((o = i.payload) == null ? void 0 : o.uid) || i.session;
      })
    };
    this.onChangeHooks.forEach((i) => i(t));
  }
  getAllRoomMembers() {
    return this.roomMembers;
  }
  getRoomMember(n) {
    return this.roomMembers.find((e) => {
      var t;
      return ((t = e.payload) == null ? void 0 : t.uid) === n;
    });
  }
  isOnLine(n) {
    return !!this.getRoomMember(n);
  }
  onUidChangeHook(n) {
    this.onChangeHooks.add(n);
  }
  destroy() {
    this.onChangeHooks.clear();
  }
}
class UM {
  constructor(n) {
    I(this, "expirationTime", Se.cursor.expirationTime);
    I(this, "isEnableCursor");
    I(this, "internalMsgEmitter");
    I(this, "publicMsgEmitter");
    I(this, "control");
    I(this, "eventName", "applianc-cursor");
    I(this, "displayer");
    I(this, "roomMember");
    I(this, "animationId");
    I(this, "maxLastSyncTime");
    I(this, "willSendEventWorkers", []);
    I(this, "willConsumeEventWorkers", /* @__PURE__ */ new Map());
    I(this, "sendEventTimerId");
    I(this, "animationDrawWorkers", /* @__PURE__ */ new Map());
    I(this, "animationEventWorkers", /* @__PURE__ */ new Map());
    I(this, "cursorInfoMap", /* @__PURE__ */ new Map());
    I(this, "doneRenderCursorInfoMap", /* @__PURE__ */ new Map());
    const { control: e, internalMsgEmitter: t, publicMsgEmitter: i } = n;
    this.internalMsgEmitter = t, this.publicMsgEmitter = i, this.expirationTime = ee(
      e.pluginOptions.extras.cursor.expirationTime
    ) ? e.pluginOptions.extras.cursor.expirationTime : this.expirationTime, this.control = e, this.roomMember = e.roomMember, this.maxLastSyncTime = this.control.pluginOptions.extras.syncOpt.interval, this.isEnableCursor = this.control.pluginOptions.extras.cursor.enable;
  }
  activeCollector() {
    this.control.plugin && this.isEnableCursor && (this.displayer = this.control.room || this.control.play, this.displayer.addMagixEventListener(
      this.eventName,
      this.mainMagixEventListener.bind(this)
    ));
  }
  mainMagixEventListener(n) {
    var a;
    const { event: e, payload: t } = n;
    if (e !== this.eventName)
      return;
    const { uid: i, op: o, type: s } = t;
    if (((a = this.control.collector) == null ? void 0 : a.uid) !== i && s === Qt.Cursor && (o != null && o.length)) {
      const c = this.willConsumeEventWorkers.get(i) || [];
      for (const l of o) {
        const { viewId: h } = l;
        ee(l.consumPoint[0]) && ee(l.consumPoint[1]) && (l.consumPoint = this.control.viewContainerManager.transformToOriginPoint(
          l.consumPoint,
          h
        )), this.control.renderControl.isFilterCursor(h) && c.unshift({
          ...l,
          timestamp: this.control.worker.now
        });
      }
      this.willConsumeEventWorkers.set(i, c), this.runAnimation();
    }
  }
  onFocusViewChange() {
    this.isEnableCursor && (this.checkDrawWorks(), this.runAnimation());
  }
  updateRoomMembers(n) {
    const { offline: e } = n;
    for (const t of this.cursorInfoMap.keys())
      t && e.includes(t) && this.cursorInfoMap.delete(t);
  }
  getKey(n, e) {
    return `${n}${st}${e}`;
  }
  getUidAndviewId(n) {
    const [e, t] = n.split(st);
    return { uid: e, viewId: t };
  }
  runAnimation() {
    this.animationId || (this.animationId = requestAnimationFrame(this.animationCursor.bind(this)));
  }
  checkDrawWorks() {
    var e;
    const n = this.control.viewContainerManager.focuedViewId;
    for (const [t, i] of this.animationDrawWorkers.entries()) {
      const { viewId: o, uid: s } = this.getUidAndviewId(t);
      s !== ((e = this.control.collector) == null ? void 0 : e.uid) && o !== n && i.workState === x.Done && (i.consumPoint = [void 0, void 0]);
    }
  }
  animationCursor() {
    this.animationId = void 0;
    const n = this.control.worker.now;
    for (const [t, i] of this.animationDrawWorkers.entries()) {
      const { uid: o, viewId: s } = this.getUidAndviewId(t), a = this.roomMember.getRoomMember(o);
      if (!a) {
        this.animationDrawWorkers.delete(t);
        continue;
      }
      const { consumPoint: c, timestamp: l, workState: h } = i, u = this.cursorInfoMap.get(s) || /* @__PURE__ */ new Map();
      c && ee(c[0]) && ee(c[1]) && l + this.expirationTime > n ? u.set(a.memberId, {
        x: c[0],
        y: c[1],
        roomMember: a,
        timestamp: l,
        type: 1,
        workState: h
      }) : u.delete(a.memberId), u.size ? this.cursorInfoMap.set(s, u) : this.cursorInfoMap.delete(s), this.animationDrawWorkers.delete(t);
    }
    const e = /* @__PURE__ */ new Set();
    for (const [t, i] of this.cursorInfoMap.entries())
      if (i)
        for (const [o, s] of i.entries())
          s.type === 1 && s.workState !== x.Done && e.add(o);
      else
        this.cursorInfoMap.delete(t);
    for (const [t, i] of this.willConsumeEventWorkers.entries()) {
      const o = this.roomMember.getRoomMember(t);
      if (ee(o == null ? void 0 : o.memberId) && !e.has(o.memberId)) {
        const s = i.pop();
        s ? this.animationEventWorkers.set(t, s) : this.willConsumeEventWorkers.delete(t);
      }
    }
    for (const [t, i] of this.animationEventWorkers.entries()) {
      const o = this.roomMember.getRoomMember(t);
      if (!o) {
        this.animationEventWorkers.delete(t);
        continue;
      }
      const { viewId: s, consumPoint: a, timestamp: c } = i, l = this.cursorInfoMap.get(s) || /* @__PURE__ */ new Map();
      a && ee(a[0]) && ee(a[1]) && c + this.expirationTime > n ? l.set(o.memberId, {
        x: a[0],
        y: a[1],
        roomMember: o,
        timestamp: c,
        type: 0
        /* Event */
      }) : l.delete(o.memberId), l.size ? this.cursorInfoMap.set(s, l) : this.cursorInfoMap.delete(s), this.animationEventWorkers.delete(t);
    }
    for (const t of this.doneRenderCursorInfoMap.keys())
      this.cursorInfoMap.has(t) || (this.doneRenderCursorInfoMap.delete(t), this.internalMsgEmitter.emit(
        [te.Cursor, t],
        []
      ));
    for (const [t, i] of this.cursorInfoMap.entries())
      if (i != null && i.size) {
        const o = [];
        for (const [a, c] of i.entries()) {
          const { timestamp: l, ...h } = c;
          l + this.expirationTime > n ? o.push(h) : (i.delete(a), i.size === 0 && (this.cursorInfoMap.delete(t), this.doneRenderCursorInfoMap.delete(t), this.internalMsgEmitter.emit(
            [te.Cursor, t],
            []
          )));
        }
        const s = this.doneRenderCursorInfoMap.get(t);
        le(s, o) || (this.doneRenderCursorInfoMap.set(t, o), this.internalMsgEmitter.emit(
          [te.Cursor, t],
          o
        ));
      } else
        this.cursorInfoMap.delete(t), this.doneRenderCursorInfoMap.delete(t), this.internalMsgEmitter.emit(
          [te.Cursor, t],
          []
        );
    (this.willConsumeEventWorkers.size || this.cursorInfoMap.size) && this.runAnimation();
  }
  sendEvent(n, e) {
    if (this.displayer && G(this.displayer)) {
      const t = ee(n[0]) && ee(n[1]) && this.control.viewContainerManager.transformToScenePoint(
        n,
        e
      ) || [void 0, void 0];
      this.willSendEventWorkers.push({ consumPoint: t, viewId: e }), this.sendEventTimerId || (this.sendEventTimerId = setTimeout(() => {
        this.dispatchMagixEvent();
      }, this.maxLastSyncTime));
    }
  }
  dispatchMagixEvent() {
    var n;
    this.displayer && G(this.displayer) && this.displayer.isWritable && this.displayer.dispatchMagixEvent(this.eventName, {
      type: Qt.Cursor,
      op: se(this.willSendEventWorkers),
      uid: (n = this.control.collector) == null ? void 0 : n.uid
    }), this.willSendEventWorkers.length = 0, this.sendEventTimerId && (clearTimeout(this.sendEventTimerId), this.sendEventTimerId = void 0);
  }
  collectServiceCursor(n) {
    var s;
    if (!this.isEnableCursor && !this.control.renderControl.isWritable)
      return;
    const { op: e, uid: t, workState: i, viewId: o } = n;
    if (t && e && i && o) {
      const a = this.roomMember.getRoomMember(t);
      if (!a || t === ((s = this.control.collector) == null ? void 0 : s.uid))
        return;
      const c = this.getKey(t, o);
      let l = [
        void 0,
        void 0
      ];
      if (ee(e[0]) && ee(e[1])) {
        const [u, g] = this.control.viewContainerManager.transformToOriginPoint(
          e,
          o
        );
        l = [u, g];
      }
      if (i === x.Start) {
        for (const u of this.cursorInfoMap.values())
          if (u != null && u.size)
            for (const g of u.keys())
              g === a.memberId && u.delete(g);
      }
      const h = {
        consumPoint: l,
        workState: i,
        viewId: o,
        timestamp: this.control.worker.now
      };
      if (i === x.Start || i === x.Done) {
        const u = this.willConsumeEventWorkers.get(t);
        i === x.Start && u && this.willConsumeEventWorkers.delete(t), u && i === x.Done && (h.timestamp = h.timestamp - this.expirationTime);
      }
      this.animationDrawWorkers.set(c, h), this.runAnimation();
    }
  }
  unabled() {
    var n;
    this.displayer && G(this.displayer) && (this.willSendEventWorkers.length = 0, this.displayer.dispatchMagixEvent(this.eventName, {
      type: Qt.Cursor,
      op: [
        {
          viewId: this.control.viewContainerManager.focuedViewId,
          consumPoint: [void 0, void 0]
        }
      ],
      uid: (n = this.control.collector) == null ? void 0 : n.uid
    }), this.sendEventTimerId && (clearTimeout(this.sendEventTimerId), this.sendEventTimerId = void 0)), this.isEnableCursor || this.stopAnimation();
  }
  clearViewCursor(n) {
    var e, t, i;
    for (const [o] of this.animationDrawWorkers.entries()) {
      const { uid: s } = this.getUidAndviewId(o);
      s === ((e = this.control.collector) == null ? void 0 : e.uid) && n === n && this.animationDrawWorkers.delete(o);
    }
    for (const [o] of this.willConsumeEventWorkers.entries())
      o === ((t = this.control.collector) == null ? void 0 : t.uid) && this.willConsumeEventWorkers.delete(o);
    for (const [o] of this.animationEventWorkers.entries())
      o === ((i = this.control.collector) == null ? void 0 : i.uid) && this.animationEventWorkers.delete(o);
    for (const [o] of this.cursorInfoMap.entries())
      o === o && (this.cursorInfoMap.delete(o), this.doneRenderCursorInfoMap.delete(o), this.internalMsgEmitter.emit(
        [te.Cursor, o],
        []
      ));
  }
  stopAnimation() {
    this.animationDrawWorkers.clear(), this.willConsumeEventWorkers.clear(), this.cursorInfoMap.clear();
  }
  destroy() {
    var n;
    (n = this.displayer) == null || n.removeMagixEventListener(this.eventName), this.stopAnimation();
  }
}
class Ze {
  constructor() {
    I(this, "useEmtMsg");
    I(this, "emtType");
    I(this, "control");
    I(this, "mainEngine");
    I(this, "collectListener", (n, e) => {
      this.collect(n, e);
    });
  }
  static dispatch(n, e, t) {
    var i;
    (i = ke.InternalMsgEmitter) == null || i.emit(
      [n, e],
      t
    );
  }
  get serviceColloctor() {
    return this.control.collector;
  }
  registerForMainEngine(n, e) {
    return this.emtType = n, this.control = e, this.mainEngine = e.worker, this.mainEngine.internalMsgEmitter.on(
      [this.emtType, this.emitEventType],
      this.collectListener
    ), this;
  }
  destroy() {
    this.emtType && this.mainEngine && this.mainEngine.internalMsgEmitter.off(
      [this.emtType, this.emitEventType],
      this.collectListener
    );
  }
  collectForLocalWorker(n, e) {
    var t, i, o;
    for (const [s, a] of n)
      (t = this.mainEngine) == null || t.queryTaskBatchData(a).forEach((c) => {
        var l;
        (l = this.mainEngine) == null || l.taskBatchData.delete(c);
      }), (i = this.mainEngine) == null || i.taskBatchData.add(s);
    if (e) {
      this.mainEngine.consume();
      return;
    }
    (o = this.mainEngine) == null || o.runAnimation();
  }
  collectForServiceWorker(n) {
    ot(
      () => {
        n.forEach((e) => {
          var t;
          (t = this.serviceColloctor) == null || t.dispatch(e);
        });
      },
      this.mainEngine.maxLastSyncTime,
      this.control.hasPolyfillMethod("requestIdleCallback")
    );
  }
  pause() {
    var n;
    if (this.lastEmtData) {
      this.useEmtMsg = !0;
      const e = { ...this.lastEmtData, workState: x.Done };
      this.collect(e, !0), (n = ke.InternalMsgEmitter) == null || n.emit(
        W.ActiveMethod,
        !1
      );
    }
  }
  recover() {
    var n;
    this.useEmtMsg && ke.InternalMsgEmitter.hasListeners(
      W.ActiveMethod
    ) && ((n = ke.InternalMsgEmitter) == null || n.emit(
      W.ActiveMethod,
      !0
    ));
  }
}
class BM extends Ze {
  constructor() {
    super(...arguments);
    I(this, "lastEmtData");
    I(this, "emitEventType", W.CopyNode);
  }
  collect(e) {
    var c;
    if (!this.serviceColloctor || !this.mainEngine)
      return;
    const { workIds: t, viewId: i } = e, o = this.control.viewContainerManager.getView(i);
    if (!(o != null && o.displayer))
      return;
    const s = o.focusScenePath, a = this.mainEngine.now;
    this.mainEngine.internalMsgEmitter.emit(
      "addUndoTicker",
      a,
      i
    );
    for (const l of t) {
      const h = l.toString(), g = this.serviceColloctor.isLocalId(h) ? this.serviceColloctor.transformKey(l) : h, M = (c = this.serviceColloctor.getStorageData(
        i,
        s
      )) == null ? void 0 : c[g];
      if (M) {
        if (l === $) {
          const d = M && this.copySelector({
            viewId: i,
            store: M
          });
          d && this.pasteSelector({
            ...d,
            viewId: i,
            scenePath: s,
            undoTickerId: a
          });
          break;
        }
        if (M.toolsType === T.Text && M.opt && M.opt.workState && M.opt.workState !== x.Done) {
          const d = M && this.copyText({
            viewId: i,
            store: M
          });
          d && this.pasteText({
            ...d,
            viewId: i,
            scenePath: s,
            key: g,
            store: M,
            undoTickerId: a
          });
          break;
        }
      }
    }
  }
  copyText(e) {
    const { viewId: t, store: i } = e, o = this.control.viewContainerManager.getView(t);
    if (!this.serviceColloctor || !o)
      return;
    const s = o == null ? void 0 : o.cameraOpt, a = s && [
      s.centerX,
      s.centerY
    ], c = i.opt, l = c.boxPoint && c.boxSize && [
      c.boxPoint[0] + c.boxSize[0] / 2,
      c.boxPoint[1] + c.boxSize[1] / 2
    ];
    return {
      bgCenter: a,
      textCenter: l
    };
  }
  pasteText(e) {
    var w, p;
    const {
      bgCenter: t,
      textCenter: i,
      store: o,
      key: s,
      viewId: a,
      scenePath: c,
      undoTickerId: l
    } = e, h = this.control.viewContainerManager.getView(a);
    if (!this.serviceColloctor || !h)
      return;
    const u = Math.floor(Math.random() * 30 + 1), g = ((w = h.cameraOpt) == null ? void 0 : w.scale) || 1, M = t && i && [
      t[0] - i[0] + u,
      t[1] - i[1] + u
    ] || [u / g, u / g], y = (this.serviceColloctor.isLocalId(s) ? s : this.serviceColloctor.getLocalId(s.toString())) + "-" + l;
    if (o.toolsType === T.Text && o.opt) {
      const f = o.opt;
      if (f && f.boxPoint && f.text) {
        f.workState = x.Done;
        const N = f.boxPoint;
        f.boxPoint = [
          Math.round(N[0] + M[0]),
          Math.round(N[1] + M[1])
        ], f.workState = x.Done;
        const S = this.control.viewContainerManager.transformToOriginPoint(
          f.boxPoint,
          a
        );
        this.control.textEditorManager.createTextForMasterController({
          workId: y,
          x: S[0],
          y: S[1],
          opt: f,
          scale: ((p = h.cameraOpt) == null ? void 0 : p.scale) || 1,
          type: $e.Text,
          isActive: !1,
          viewId: a,
          scenePath: c
        }), this.collectForServiceWorker([
          {
            ...o,
            opt: f,
            type: R.FullWork,
            workId: y,
            viewId: a,
            scenePath: c
          }
        ]);
      }
    }
  }
  copySelector(e) {
    var y, w, p, f, N, S, C, k, v;
    const { viewId: t, store: i } = e, o = this.control.viewContainerManager.getView(t), s = i.selectIds;
    if (!this.serviceColloctor || !(s != null && s.length) || !o)
      return;
    const a = /* @__PURE__ */ new Map(), c = {
      offset: {
        x: 0,
        y: 0
      },
      cameraOpt: {
        centerX: ((y = o.cameraOpt) == null ? void 0 : y.centerX) || 0,
        centerY: ((w = o.cameraOpt) == null ? void 0 : w.centerY) || 0,
        scale: ((p = o.cameraOpt) == null ? void 0 : p.scale) || 1
      }
    }, l = this.control.hasOffscreenCanvas && ((f = o.displayer.canvasBgRef.current) == null ? void 0 : f.getBoundingClientRect()) || ((N = o.displayer.canvasContainerRef.current) == null ? void 0 : N.getBoundingClientRect()), h = (C = (S = o.displayer) == null ? void 0 : S.floatBarRef.current) == null ? void 0 : C.getBoundingClientRect(), u = l && [
      l.x + l.width / 2,
      l.y + l.height / 2
    ], g = h && [
      h.x + h.width / 2,
      h.y + h.height / 2
    ], M = u && o.viewData && o.viewData.convertToPointInWorld({
      x: u[0],
      y: u[1]
    }), d = g && o.viewData && o.viewData.convertToPointInWorld({
      x: g[0],
      y: g[1]
    });
    M && d && (c.offset = {
      x: M.x - d.x,
      y: M.y - d.y
    });
    for (const A of s) {
      const P = (v = (k = this.serviceColloctor) == null ? void 0 : k.getStorageData(
        o.id,
        o.focusScenePath
      )) == null ? void 0 : v[A];
      P && a.set(A, P);
    }
    return {
      copyStores: a,
      copyCoordInfo: c
    };
  }
  pasteSelector(e) {
    var w, p, f, N;
    const { copyStores: t, copyCoordInfo: i, viewId: o, scenePath: s, undoTickerId: a } = e, c = this.control.viewContainerManager.getView(o);
    if (!t.size || !this.serviceColloctor || !c)
      return;
    const { offset: l, cameraOpt: h } = i, { scale: u } = h, g = Math.floor(Math.random() * 30 + 1), M = l && [
      l.x + g,
      l.y + g
    ] || [g / u, g / u], d = [], y = [];
    for (const [S, C] of t.entries()) {
      const v = (this.serviceColloctor.isLocalId(S) ? S : this.serviceColloctor.getLocalId(S.toString())) + "-" + a, A = { useAnimation: !1 };
      if (C.toolsType === T.Text && C.opt) {
        const P = C.opt;
        if (P && P.boxPoint && P.text) {
          P.workState = x.Done;
          const E = P.boxPoint;
          P.boxPoint = [
            Math.round(E[0] + M[0]),
            Math.round(E[1] + M[1])
          ], P.workState = x.Done;
          const O = this.control.viewContainerManager.transformToOriginPoint(
            P.boxPoint,
            o
          );
          this.control.textEditorManager.createTextForMasterController({
            workId: v,
            x: O[0],
            y: O[1],
            opt: P,
            scale: ((w = c.cameraOpt) == null ? void 0 : w.scale) || 1,
            type: $e.Text,
            isActive: !1,
            viewId: o,
            scenePath: s
          });
        }
        y.push({
          ...C,
          opt: P,
          type: R.FullWork,
          workId: v,
          viewId: o,
          scenePath: s
        });
        continue;
      }
      if ((C.toolsType === T.Image || C.toolsType === T.BackgroundSVG) && (C.opt.uuid = v, C.opt.centerX = C.opt.centerX + M[0], C.opt.centerY = C.opt.centerY + M[1]), C.ops) {
        const P = $n(C.ops).map(
          (O, z) => {
            const L = z % 3;
            return L === 0 ? O + M[0] : L === 1 ? O + M[1] : O;
          }
        ), E = _t(P);
        C.ops = E;
      }
      if ((p = C == null ? void 0 : C.opt) != null && p.eraserlines && (f = C == null ? void 0 : C.opt) != null && f.eraserlines.length)
        for (const P of (N = C == null ? void 0 : C.opt) == null ? void 0 : N.eraserlines) {
          const { op: E } = P;
          for (let O = 0; O < E.length; O++) {
            const z = E[O].map((L, j) => j % 2 ? L + M[1] : L + M[0]);
            P.op[O] = z;
          }
        }
      y.push({
        ...C,
        updateNodeOpt: A,
        type: R.FullWork,
        workId: v,
        viewId: o,
        scenePath: s
      }), d.push([
        {
          ...C,
          updateNodeOpt: A,
          workId: v,
          msgType: R.FullWork,
          dataType: q.Local,
          emitEventType: W.CopyNode,
          willSyncService: !1,
          willRefresh: !0,
          viewId: o,
          needUndoTicker: this.control.viewContainerManager.getUndoTickerId(o),
          isRunBackgroundThread: this.control.isUseBackgroundThread
        },
        {
          workId: v,
          msgType: R.FullWork,
          emitEventType: W.CopyNode
        }
      ]);
    }
    d.length && this.collectForLocalWorker(d), y.length && this.collectForServiceWorker(y);
  }
}
class WM extends Ze {
  constructor() {
    super(...arguments);
    I(this, "lastEmtData");
    I(this, "emitEventType", W.SetColorNode);
    I(this, "undoTickerId");
  }
  setTextColor(e, t, i, o) {
    const { fontColor: s, fontBgColor: a } = i;
    t.opt && (s && (t.opt.fontColor = s), a && (t.opt.fontBgColor = a), this.control.textEditorManager.updateTextForMasterController({
      workId: e,
      opt: t.opt,
      viewId: o,
      canSync: !0,
      canWorker: !0
    }));
  }
  collect(e) {
    var f, N;
    if (!this.serviceColloctor || !this.mainEngine)
      return;
    const {
      workIds: t,
      strokeColor: i,
      fillColor: o,
      fontColor: s,
      fontBgColor: a,
      viewId: c,
      workState: l,
      thickness: h,
      strokeType: u
    } = e, g = this.control.viewContainerManager.getView(c);
    if (!(g != null && g.displayer))
      return;
    const M = g.focusScenePath, d = [...t], y = this.serviceColloctor.storage, w = [], p = {};
    for ((l === x.Start || !this.undoTickerId && l === x.Done) && (this.undoTickerId = this.mainEngine.now, this.mainEngine.internalMsgEmitter.emit(
      "addUndoTicker",
      this.undoTickerId,
      c
    )); d.length; ) {
      const S = d.pop();
      if (!S)
        continue;
      const C = S.toString(), k = this.serviceColloctor.isLocalId(C), v = k ? this.serviceColloctor.transformKey(S) : C;
      let A = C;
      !k && this.serviceColloctor.isOwn(A) && (A = this.serviceColloctor.getLocalId(A));
      const P = (N = (f = y == null ? void 0 : y[c]) == null ? void 0 : f[M]) == null ? void 0 : N[v];
      if (P) {
        const E = P.updateNodeOpt || {};
        if (s || a) {
          if (s) {
            E.fontColor = s;
            const [z, L, j, F] = Wt(s);
            p.textColor = [z, L, j], p.textOpacity = F;
          }
          if (a)
            if (E.fontBgColor = Ft(a) ? "transparent" : a, Ft(a))
              p.textBgColor = void 0, p.textBgOpacity = void 0;
            else {
              const [z, L, j, F] = Wt(a);
              p.textBgColor = [z, L, j], p.textBgOpacity = F;
            }
          if (P.toolsType === T.Text && P.opt && l !== x.Start) {
            this.setTextColor(
              A,
              un(P),
              E,
              c
            ), l === x.Done && (this.undoTickerId = void 0);
            continue;
          }
        }
        if (i) {
          E.strokeColor = i;
          const [z, L, j, F] = Wt(i);
          p.strokeColor = [z, L, j], p.strokeOpacity = F;
        }
        if (o)
          if (E.fillColor = Ft(o) ? "transparent" : o, Ft(o))
            p.fillColor = void 0, p.fillOpacity = void 0;
          else {
            const [z, L, j, F] = Wt(o);
            p.fillColor = [z, L, j], p.fillOpacity = F;
          }
        h && (E.thickness = h, p.strokeWidth = h), u && (p.strokeType = u, E.strokeType = u, E.lineCap = this.control.getLineCap(u), E.lineDash = this.control.getLineDash(u));
        const O = {
          workId: A,
          msgType: R.UpdateNode,
          dataType: q.Local,
          updateNodeOpt: E,
          emitEventType: this.emitEventType,
          willRefresh: !0,
          willRefreshSelector: !0,
          willSyncService: !0,
          textUpdateForWoker: !0,
          viewId: c,
          needUndoTicker: l === x.Done && this.control.viewContainerManager.getUndoTickerId(c) || void 0
        };
        w.push([
          O,
          {
            workId: A,
            msgType: R.UpdateNode,
            emitEventType: this.emitEventType
          }
        ]);
      }
    }
    w.length && (l === x.Done && (this.undoTickerId = void 0), this.collectForLocalWorker(w)), Object.keys(p).length && setTimeout(() => {
      var S;
      (S = this.control.room) == null || S.setMemberState(p);
    }, 0);
  }
}
class FM extends Ze {
  constructor() {
    super(...arguments);
    I(this, "lastEmtData");
    I(this, "emitEventType", W.ZIndexNode);
    I(this, "zIndexMap", /* @__PURE__ */ new Map());
    I(this, "zIndexMapForBackgroundSVG", /* @__PURE__ */ new Map());
  }
  clearZIndex(e) {
    this.zIndexMap.delete(e), this.zIndexMapForBackgroundSVG.delete(e);
  }
  getMinZIndex(e) {
    var t;
    return ((t = this.zIndexMap.get(e)) == null ? void 0 : t.min) || 0;
  }
  getMinZIndexForBackgroundSVG(e) {
    var t;
    return ((t = this.zIndexMapForBackgroundSVG.get(e)) == null ? void 0 : t.min) || 0;
  }
  getMaxZIndex(e) {
    var t;
    return ((t = this.zIndexMap.get(e)) == null ? void 0 : t.max) || 0;
  }
  getMaxZIndexForBackgroundSVG(e) {
    var t;
    return ((t = this.zIndexMapForBackgroundSVG.get(e)) == null ? void 0 : t.max) || 0;
  }
  setMaxZIndex(e, t) {
    const i = this.getMinZIndex(t);
    this.zIndexMap.set(t, {
      min: i,
      max: e
    });
  }
  setMaxZIndexForBackgroundSVG(e, t) {
    const i = this.getMinZIndexForBackgroundSVG(t);
    this.zIndexMapForBackgroundSVG.set(t, {
      min: i,
      max: e
    });
  }
  setMinZIndex(e, t) {
    const i = this.getMaxZIndex(t);
    this.zIndexMap.set(t, {
      min: e,
      max: i
    });
  }
  setMinZIndexForBackgroundSVG(e, t) {
    const i = this.getMaxZIndexForBackgroundSVG(t);
    this.zIndexMapForBackgroundSVG.set(t, {
      min: e,
      max: i
    });
  }
  addMaxLayer(e) {
    const t = this.getMaxZIndex(e) + 1;
    this.setMaxZIndex(t, e);
  }
  addMaxLayerForBackgroundSVG(e) {
    const t = this.getMaxZIndexForBackgroundSVG(e) + 1;
    this.setMaxZIndexForBackgroundSVG(t, e);
  }
  addMinLayer(e) {
    const t = this.getMinZIndex(e) - 1;
    this.setMinZIndex(t, e);
  }
  addMinLayerForBackgroundSVG(e) {
    const t = this.getMinZIndexForBackgroundSVG(e) - 1;
    this.setMinZIndexForBackgroundSVG(t, e);
  }
  correct(e) {
    const t = e.sort((i, o) => i[1] - o[1]);
    for (let i = 1; i < t.length; i++) {
      const o = t[i][1], s = t[i - 1][1];
      o <= s && (t[i][1] = s + 1);
    }
    return t;
  }
  collect(e) {
    var d, y;
    if (!this.serviceColloctor || !this.mainEngine)
      return;
    const { workIds: t, layer: i, viewId: o } = e, s = this.control.viewContainerManager.getView(o);
    if (!(s != null && s.displayer))
      return;
    const a = s.focusScenePath, c = [...t], l = this.serviceColloctor.storage, h = [], u = [], g = [];
    for (; c.length; ) {
      const w = c.pop();
      if (!w)
        continue;
      const p = w.toString(), f = this.serviceColloctor.isLocalId(p), N = f ? this.serviceColloctor.transformKey(w) : p;
      let S = p;
      !f && this.serviceColloctor.isOwn(S) && (S = this.serviceColloctor.getLocalId(S));
      const C = un(l[o][a][N]);
      let k, v;
      if (C && S === $) {
        if (C.selectIds) {
          for (const E of C.selectIds) {
            const O = M(E, this.serviceColloctor), z = (y = (d = l == null ? void 0 : l[o]) == null ? void 0 : d[a]) == null ? void 0 : y[O];
            z && z.toolsType === T.BackgroundSVG ? (g.push(E), g.sort((L, j) => {
              var Y, Q, _, ce;
              const F = ((Q = (Y = l[M(L, this.serviceColloctor)]) == null ? void 0 : Y.opt) == null ? void 0 : Q.zIndex) || 0, X = ((ce = (_ = l[M(L, this.serviceColloctor)]) == null ? void 0 : _.opt) == null ? void 0 : ce.zIndex) || 0;
              return F > X ? 1 : L < j ? -1 : 0;
            })) : (u.push(E), u.sort((L, j) => {
              var Y, Q, _, ce;
              const F = ((Q = (Y = l[M(L, this.serviceColloctor)]) == null ? void 0 : Y.opt) == null ? void 0 : Q.zIndex) || 0, X = ((ce = (_ = l[M(L, this.serviceColloctor)]) == null ? void 0 : _.opt) == null ? void 0 : ce.zIndex) || 0;
              return F > X ? 1 : L < j ? -1 : 0;
            }));
          }
          const A = C.updateNodeOpt || {};
          A.zIndexLayer = i;
          const P = {
            workId: w,
            msgType: R.UpdateNode,
            dataType: q.Local,
            updateNodeOpt: A,
            emitEventType: this.emitEventType,
            willRefreshSelector: !0,
            willSyncService: !0,
            viewId: o
          };
          u.length && (i === Lt.Top ? (this.addMaxLayer(o), k = this.getMaxZIndex(o)) : (this.addMinLayer(o), k = this.getMinZIndex(o)), u.forEach((E) => {
            var j, F, X, Y, Q;
            const O = (j = this.serviceColloctor) == null ? void 0 : j.isLocalId(E);
            let z = O && ((F = this.serviceColloctor) == null ? void 0 : F.transformKey(E)) || E;
            const L = (Y = (X = l == null ? void 0 : l[o]) == null ? void 0 : X[a]) == null ? void 0 : Y[z];
            L && (!O && ((Q = this.serviceColloctor) != null && Q.isOwn(z)) && (z = this.serviceColloctor.getLocalId(z)), A.zIndex = k, L != null && L.opt && (L.opt.zIndex = k));
          })), g.length && (i === Lt.Top ? (this.addMaxLayerForBackgroundSVG(o), v = this.getMaxZIndexForBackgroundSVG(o)) : (this.addMinLayerForBackgroundSVG(o), v = this.getMinZIndexForBackgroundSVG(o)), g.forEach((E) => {
            var j, F, X, Y, Q;
            const O = (j = this.serviceColloctor) == null ? void 0 : j.isLocalId(E);
            let z = O && ((F = this.serviceColloctor) == null ? void 0 : F.transformKey(E)) || E;
            const L = (Y = (X = l == null ? void 0 : l[o]) == null ? void 0 : X[a]) == null ? void 0 : Y[z];
            L && (!O && ((Q = this.serviceColloctor) != null && Q.isOwn(z)) && (z = this.serviceColloctor.getLocalId(z)), A.zIndexForBackgroundSVG = v, L != null && L.opt && (L.opt.zIndex = v));
          })), P.willSerializeData = !0, h.push([
            P,
            {
              workId: w,
              msgType: R.UpdateNode,
              emitEventType: this.emitEventType
            }
          ]);
        }
        continue;
      }
    }
    h.length && this.collectForLocalWorker(h);
    function M(w, p) {
      return p.isLocalId(w) && p.transformKey(w) || w;
    }
  }
}
class VM extends Ze {
  constructor() {
    super(...arguments);
    I(this, "lastEmtData");
    I(this, "emitEventType", W.TranslateNode);
    I(this, "cachePosition");
  }
  collect(e, t) {
    var d, y, w;
    if (!this.serviceColloctor || !this.mainEngine)
      return;
    const { workIds: i, position: o, workState: s, viewId: a } = e;
    this.lastEmtData = e;
    const c = this.control.viewContainerManager.getView(a);
    if (!(c != null && c.displayer))
      return;
    const l = c.focusScenePath, h = [...i], u = (d = this.serviceColloctor) == null ? void 0 : d.storage, g = [], M = s === x.Start && this.mainEngine.now || void 0;
    for (M && this.mainEngine.internalMsgEmitter.emit(
      "addUndoTicker",
      M,
      a
    ); h.length; ) {
      const p = h.pop();
      if (!p) continue;
      const f = p.toString(), N = this.serviceColloctor.isLocalId(f), S = N && this.serviceColloctor.transformKey(p) || f;
      let C = f;
      !N && this.serviceColloctor.isOwn(C) && (C = this.serviceColloctor.getLocalId(C));
      const k = (w = (y = u == null ? void 0 : u[a]) == null ? void 0 : y[l]) == null ? void 0 : w[S];
      if (k && C === $) {
        if (k.selectIds) {
          const v = this.control.viewContainerManager.transformToScenePoint(
            [o.x, o.y],
            a
          );
          if (s === x.Start && (this.cachePosition = v), this.cachePosition) {
            const A = k.updateNodeOpt || {};
            A.translate = [
              v[0] - this.cachePosition[0],
              v[1] - this.cachePosition[1]
            ], A.workState = s, this.control.cursor.isEnableCursor ? A.useAnimation = !1 : (A.uid = this.serviceColloctor.uid, s === x.Start ? A.useAnimation = !1 : s === x.Done && (A.useAnimation = !0));
            const P = {
              workId: p,
              msgType: R.UpdateNode,
              dataType: q.Local,
              updateNodeOpt: A,
              emitEventType: this.emitEventType,
              willSyncService: !0,
              textUpdateForWoker: !0,
              viewId: a,
              smoothSync: this.mainEngine.smoothSync
            };
            s === x.Done && (P.textUpdateForWoker = !0, P.willSerializeData = !0, this.cachePosition = void 0, P.needUndoTicker = this.control.viewContainerManager.getUndoTickerId(a)), g.push([
              P,
              {
                workId: p,
                msgType: R.UpdateNode,
                emitEventType: this.emitEventType,
                workState: s
              }
            ]);
          }
        }
        continue;
      }
    }
    s === x.Start ? this.mainEngine.unWritable() : s === x.Done && (this.mainEngine.abled(), this.lastEmtData = void 0), g.length && this.collectForLocalWorker(g, t);
  }
}
class QM extends Ze {
  constructor() {
    super(...arguments);
    I(this, "lastEmtData");
    I(this, "emitEventType", W.DeleteNode);
  }
  collect(e) {
    var g, M;
    if (!this.serviceColloctor || !this.mainEngine)
      return;
    const { workIds: t, viewId: i } = e, o = this.control.viewContainerManager.getView(i);
    if (!(o != null && o.displayer))
      return;
    const s = o.focusScenePath, a = this.serviceColloctor.storage, c = [...t], l = [], h = [], u = this.mainEngine.now;
    for (; c.length; ) {
      const d = c.pop();
      if (!d)
        continue;
      const y = d.toString(), w = this.serviceColloctor.isLocalId(y), p = w ? this.serviceColloctor.transformKey(d) : y, f = (M = (g = a == null ? void 0 : a[i]) == null ? void 0 : g[s]) == null ? void 0 : M[p];
      if (f) {
        let N = y;
        if (!w && this.serviceColloctor.isOwn(N) && (N = this.serviceColloctor.getLocalId(N)), f.toolsType === T.Text) {
          this.control.textEditorManager.delete(N, !0, !0);
          continue;
        }
        h.push(N);
      }
    }
    h.length && (this.mainEngine.internalMsgEmitter.emit(
      "addUndoTicker",
      u,
      i
    ), l.push([
      {
        msgType: R.RemoveNode,
        emitEventType: W.DeleteNode,
        removeIds: h,
        dataType: q.Local,
        willSyncService: !0,
        willRefresh: !0,
        viewId: i,
        isRunBackgroundThread: this.control.isUseBackgroundThread,
        needUndoTicker: this.control.viewContainerManager.getUndoTickerId(i)
      },
      void 0
    ]), this.collectForLocalWorker(l));
  }
}
class GM extends Ze {
  constructor() {
    super(...arguments);
    I(this, "lastEmtData");
    I(this, "emitEventType", W.ScaleNode);
    I(this, "targetText", /* @__PURE__ */ new Map());
    I(this, "cacheTextInfo", /* @__PURE__ */ new Map());
  }
  async setTextStyle(e, t, i) {
    const o = await this.control.textEditorManager.updateTextControllerWithEffectAsync({
      workId: e,
      opt: t,
      viewId: i,
      canSync: !1,
      canWorker: !1
    });
    return { key: e, textInfo: o };
  }
  async collect(e, t) {
    var f, N, S, C, k, v, A;
    if (!this.serviceColloctor || !this.mainEngine)
      return;
    const {
      workIds: i,
      workState: o,
      viewId: s,
      originPoint: a,
      scenePoint: c,
      dir: l,
      scale: h,
      translate: u
    } = e;
    this.lastEmtData = e;
    const g = this.control.viewContainerManager.getView(s);
    if (!(g != null && g.displayer))
      return;
    const M = g.focusScenePath, d = [...i], y = (f = this.serviceColloctor) == null ? void 0 : f.storage, w = [], p = o === x.Start && this.mainEngine.now || void 0;
    for (p && this.mainEngine.internalMsgEmitter.emit(
      "addUndoTicker",
      p,
      s
    ); d.length; ) {
      const P = d.pop();
      if (!P)
        continue;
      const E = P.toString(), O = this.serviceColloctor.isLocalId(E), z = O && this.serviceColloctor.transformKey(P) || E;
      let L = E;
      !O && this.serviceColloctor.isOwn(L) && (L = this.serviceColloctor.getLocalId(L));
      const j = (S = (N = y == null ? void 0 : y[s]) == null ? void 0 : N[M]) == null ? void 0 : S[z];
      if (j && L === $ && j.selectIds) {
        const F = j.updateNodeOpt || {};
        if (F.dir = l, F.originPoint = a, F.scenePoint = c, F.workState = o, F.scale = h, F.translate = u, o === x.Start) {
          this.cacheTextInfo.clear();
          for (const Y of j.selectIds) {
            const Q = (C = this.serviceColloctor) == null ? void 0 : C.isLocalId(Y), _ = Q && ((k = this.serviceColloctor) == null ? void 0 : k.transformKey(Y)) || Y;
            let ce = _;
            !Q && ((v = this.serviceColloctor) != null && v.isOwn(_)) && (ce = this.serviceColloctor.getLocalId(_));
            const ie = this.control.textEditorManager.get(ce);
            ie && o === x.Start && this.targetText.set(ce, un(ie));
          }
        }
        if (this.targetText.size && o !== x.Start && h) {
          const Y = [];
          for (const [_, ce] of this.targetText.entries()) {
            const { opt: ie } = ce, K = Math.floor(ie.fontSize * h[0]), H = this.cacheTextInfo.get(_), fe = !H && ie.fontSize !== K || H && H.fontSize !== K || !1, Je = (A = this.control.textEditorManager.get(_)) == null ? void 0 : A.opt;
            if (fe && Je && ie.boxSize && ie.boxPoint) {
              const Xe = this.setTextStyle(
                _,
                { ...Je, fontSize: K },
                s
              );
              Y.push(Xe);
            }
          }
          const Q = await Promise.all(Y);
          for (const _ of Q)
            if (_) {
              const { key: ce, textInfo: ie } = _;
              this.cacheTextInfo.set(ce, {
                fontSize: ie.opt.fontSize,
                boxSize: ie.opt.boxSize,
                boxPoint: ie.opt.boxPoint
              });
            }
          F.textInfos = this.cacheTextInfo;
        }
        this.control.cursor.isEnableCursor ? F.useAnimation = !1 : (F.uid = this.serviceColloctor.uid, o === x.Start ? F.useAnimation = !1 : o === x.Done && (F.useAnimation = !0));
        const X = {
          workId: P,
          msgType: R.UpdateNode,
          dataType: q.Local,
          updateNodeOpt: F,
          emitEventType: this.emitEventType,
          willRefreshSelector: !0,
          willSyncService: !0,
          viewId: s,
          smoothSync: this.mainEngine.smoothSync
        };
        o === x.Done && (X.willSerializeData = !0, this.targetText.clear(), X.needUndoTicker = this.control.viewContainerManager.getUndoTickerId(s)), w.push([
          X,
          {
            workId: P,
            msgType: R.UpdateNode,
            emitEventType: this.emitEventType,
            workState: o
          }
        ]);
        continue;
      }
    }
    o === x.Start ? this.mainEngine.unWritable() : o === x.Done && (this.mainEngine.abled(), this.lastEmtData = void 0), w.length && this.collectForLocalWorker(w, t);
  }
}
class ZM extends Ze {
  constructor() {
    super(...arguments);
    I(this, "lastEmtData");
    I(this, "emitEventType", W.RotateNode);
    I(this, "cacheOriginRotate", 0);
  }
  collect(e, t) {
    var d, y, w, p, f, N, S;
    if (!this.serviceColloctor || !this.mainEngine)
      return;
    const { workIds: i, angle: o, workState: s, viewId: a } = e;
    this.lastEmtData = e;
    const c = this.control.viewContainerManager.getView(a);
    if (!(c != null && c.displayer))
      return;
    const l = c.focusScenePath, h = [...i], u = (d = this.serviceColloctor) == null ? void 0 : d.storage, g = [], M = s === x.Start && this.mainEngine.now || void 0;
    for (M && this.mainEngine.internalMsgEmitter.emit(
      "addUndoTicker",
      M,
      a
    ); h.length; ) {
      const C = h.pop();
      if (!C)
        continue;
      const k = C.toString(), v = this.serviceColloctor.isLocalId(k), A = v && this.serviceColloctor.transformKey(C) || k;
      let P = k;
      !v && this.serviceColloctor.isOwn(P) && (P = this.serviceColloctor.getLocalId(P));
      const E = (w = (y = u == null ? void 0 : u[a]) == null ? void 0 : y[l]) == null ? void 0 : w[A];
      if (E && P === $) {
        if (((p = E.selectIds) == null ? void 0 : p.length) === 1) {
          const O = E.selectIds[0];
          if (s === x.Start) {
            const F = ((f = this.serviceColloctor) == null ? void 0 : f.isLocalId(O)) && ((N = this.serviceColloctor) == null ? void 0 : N.transformKey(O)) || O, X = u[a][l][F];
            this.cacheOriginRotate = ((S = X == null ? void 0 : X.opt) == null ? void 0 : S.rotate) || 0;
          }
          const z = E.updateNodeOpt || {};
          z.angle = (o + this.cacheOriginRotate) % 360, z.workState = s, this.control.cursor.isEnableCursor ? z.useAnimation = !1 : (z.uid = this.serviceColloctor.uid, s === x.Start ? z.useAnimation = !1 : s === x.Done && (z.useAnimation = !0));
          const L = {
            workId: C,
            msgType: R.UpdateNode,
            dataType: q.Local,
            updateNodeOpt: z,
            emitEventType: this.emitEventType,
            willRefreshSelector: !1,
            willSyncService: !0,
            viewId: a,
            smoothSync: this.mainEngine.smoothSync
          };
          s === x.Done && (L.willRefreshSelector = !0, L.willSerializeData = !0, this.cacheOriginRotate = 0, L.needUndoTicker = this.control.viewContainerManager.getUndoTickerId(a)), g.push([
            L,
            {
              workId: C,
              msgType: R.UpdateNode,
              emitEventType: this.emitEventType,
              workState: s
            }
          ]);
        }
        continue;
      }
    }
    s === x.Start ? this.mainEngine.unWritable() : s === x.Done && (this.mainEngine.abled(), this.lastEmtData = void 0), g.length && this.collectForLocalWorker(g, t);
  }
}
function $M(r) {
  switch (r) {
    case T.Text:
      return $e.Text;
    case T.SpeechBalloon:
    case T.Star:
    case T.Ellipse:
    case T.Rectangle:
    case T.Triangle:
    case T.Rhombus:
    case T.Polygon:
      return $e.Shape;
  }
}
class HM extends Ze {
  constructor() {
    super(...arguments);
    I(this, "lastEmtData");
    I(this, "emitEventType", W.SetFontStyle);
    I(this, "timerId");
  }
  async setTextStyle(e, t, i, o) {
    const { bold: s, underline: a, lineThrough: c, italic: l, fontSize: h } = i;
    if (t.toolsType && $M(t.toolsType) === $e.Text) {
      t.opt && (s && (t.opt.bold = s), We(a) && (t.opt.underline = a), We(c) && (t.opt.lineThrough = c), l && (t.opt.italic = l), h && (t.opt.fontSize = h));
      const g = await this.control.textEditorManager.updateTextControllerWithEffectAsync(
        {
          workId: e,
          opt: t.opt,
          viewId: o,
          canSync: !1,
          canWorker: !1
          // waitWorker: true,
        }
      );
      return { key: e, textInfo: g };
    }
  }
  async collect(e) {
    var f, N, S, C, k, v;
    if (!this.serviceColloctor || !this.mainEngine)
      return;
    const { workIds: t, bold: i, italic: o, lineThrough: s, underline: a, viewId: c, fontSize: l } = e, h = this.control.viewContainerManager.getView(c);
    if (!(h != null && h.displayer))
      return;
    const u = h.focusScenePath, g = [...t], M = this.serviceColloctor.storage, d = {}, y = this.mainEngine.now;
    this.mainEngine.internalMsgEmitter.emit(
      "addUndoTicker",
      y,
      c
    ), this.control.textEditorManager.UndoTickerId = y;
    const w = [];
    for (; g.length; ) {
      const A = g.pop();
      if (!A)
        continue;
      const P = A.toString(), E = this.serviceColloctor.isLocalId(P), O = E ? this.serviceColloctor.transformKey(A) : P;
      let z = P;
      !E && this.serviceColloctor.isOwn(z) && (z = this.serviceColloctor.getLocalId(z));
      const L = (N = (f = M == null ? void 0 : M[c]) == null ? void 0 : f[u]) == null ? void 0 : N[O];
      if (L) {
        const j = L.updateNodeOpt || {};
        if (i && (j.bold = i, d.bold = i === "bold"), o && (j.italic = o, d.italic = o === "italic"), We(s) && (j.lineThrough = s, d.lineThrough = s), We(a) && (j.underline = a, d.underline = a), l && (j.fontSize = l, d.textSize = l), L.toolsType === T.Text && L.opt) {
          const F = this.setTextStyle(
            z,
            se(L),
            j,
            c
          );
          w.push(F);
          continue;
        }
        if (L && z === $ && ((S = L.selectIds) != null && S.length))
          for (const F of L.selectIds) {
            const X = (C = this.serviceColloctor) == null ? void 0 : C.isLocalId(F);
            let Y = X && ((k = this.serviceColloctor) == null ? void 0 : k.transformKey(F)) || F;
            const Q = M[c][u][Y] || void 0;
            if (!X && ((v = this.serviceColloctor) != null && v.isOwn(Y)) && (Y = this.serviceColloctor.getLocalId(Y)), Q && Q.toolsType === T.Text && L.opt) {
              const _ = this.setTextStyle(
                Y,
                se(Q),
                j,
                c
              );
              w.push(_);
              continue;
            }
          }
      }
    }
    const p = await Promise.all(w);
    for (const A of p)
      if (A) {
        const { key: P, textInfo: E } = A;
        E && (E.canSync = !0, E.canWorker = !0, this.control.textEditorManager.updateForViewEdited(P, E));
      }
    Object.keys(d).length && (this.timerId && (clearTimeout(this.timerId), this.timerId = void 0), this.timerId = setTimeout(() => {
      var A;
      this.timerId = void 0, (A = this.control.room) == null || A.setMemberState(d);
    }, 0));
  }
}
class JM extends Ze {
  constructor() {
    super(...arguments);
    I(this, "lastEmtData");
    I(this, "emitEventType", W.SetPoint);
    I(this, "undoTickerId");
  }
  collect(e) {
    var f, N, S;
    if (!this.serviceColloctor || !this.mainEngine)
      return;
    const { workId: t, pointMap: i, workState: o, viewId: s } = e, a = this.control.viewContainerManager.getView(s);
    if (!(a != null && a.displayer))
      return;
    const c = a.focusScenePath, l = (f = this.serviceColloctor) == null ? void 0 : f.storage, h = [], u = o === x.Start && this.mainEngine.now || void 0;
    u && (this.undoTickerId = u, this.mainEngine.internalMsgEmitter.emit(
      "addUndoTicker",
      u,
      s
    ));
    const g = t;
    if (!g)
      return;
    const M = g.toString(), d = this.serviceColloctor.isLocalId(M), y = d && this.serviceColloctor.transformKey(g) || M;
    let w = M;
    !d && this.serviceColloctor.isOwn(w) && (w = this.serviceColloctor.getLocalId(w));
    const p = (S = (N = l == null ? void 0 : l[s]) == null ? void 0 : N[c]) == null ? void 0 : S[y];
    if (p && w === $ && p.selectIds) {
      const C = p.updateNodeOpt || {};
      C.pointMap = i, C.workState = o;
      const k = {
        workId: g,
        msgType: R.UpdateNode,
        dataType: q.Local,
        updateNodeOpt: C,
        emitEventType: this.emitEventType,
        willRefreshSelector: !0,
        willSyncService: !0,
        viewId: s,
        needUndoTicker: o === x.Done && this.control.viewContainerManager.getUndoTickerId(s) || void 0
      };
      o === x.Done && (k.undoTickerId = this.undoTickerId), h.push([
        k,
        {
          workId: g,
          msgType: R.UpdateNode,
          emitEventType: this.emitEventType
        }
      ]);
    }
    o === x.Start ? this.mainEngine.unWritable() : o === x.Done && this.mainEngine.abled(), h.length && this.collectForLocalWorker(h);
  }
}
class XM extends Ze {
  constructor() {
    super(...arguments);
    I(this, "lastEmtData");
    I(this, "emitEventType", W.SetLock);
  }
  collect(e) {
    var g, M;
    if (!this.serviceColloctor || !this.mainEngine)
      return;
    const { workIds: t, isLocked: i, viewId: o } = e, s = this.control.viewContainerManager.getView(o);
    if (!(s != null && s.displayer))
      return;
    const a = s.focusScenePath, c = [...t], l = this.serviceColloctor.storage, h = [], u = this.mainEngine.now;
    for (this.mainEngine.internalMsgEmitter.emit(
      "addUndoTicker",
      u,
      o
    ); c.length; ) {
      const d = c.pop();
      if (!d)
        continue;
      const y = d.toString(), w = this.serviceColloctor.isLocalId(y), p = w ? this.serviceColloctor.transformKey(d) : y;
      let f = y;
      !w && this.serviceColloctor.isOwn(f) && (f = this.serviceColloctor.getLocalId(f));
      const N = (M = (g = l == null ? void 0 : l[o]) == null ? void 0 : g[a]) == null ? void 0 : M[p];
      if (N) {
        const S = N.updateNodeOpt || {};
        S.isLocked = i;
        const C = {
          workId: f,
          msgType: R.UpdateNode,
          dataType: q.Local,
          updateNodeOpt: S,
          emitEventType: this.emitEventType,
          willRefresh: !0,
          willRefreshSelector: !0,
          willSyncService: !0,
          viewId: o,
          needUndoTicker: this.control.viewContainerManager.getUndoTickerId(o)
        };
        h.push([
          C,
          {
            workId: f,
            msgType: R.UpdateNode,
            emitEventType: this.emitEventType
          }
        ]);
      }
    }
    h.length && this.collectForLocalWorker(h);
  }
}
class KM extends Ze {
  constructor() {
    super(...arguments);
    I(this, "lastEmtData");
    I(this, "emitEventType", W.SetShapeOpt);
  }
  collect(e) {
    var g, M;
    if (!this.serviceColloctor || !this.mainEngine)
      return;
    const { workIds: t, viewId: i, ...o } = e, s = this.control.viewContainerManager.getView(i);
    if (!(s != null && s.displayer))
      return;
    const a = s.focusScenePath, c = [...t], l = this.serviceColloctor.storage, h = [], u = this.mainEngine.now;
    for (this.mainEngine.internalMsgEmitter.emit(
      "addUndoTicker",
      u,
      i
    ); c.length; ) {
      const d = c.pop();
      if (!d)
        continue;
      const y = d.toString(), w = this.serviceColloctor.isLocalId(y), p = w ? this.serviceColloctor.transformKey(d) : y;
      let f = y;
      !w && this.serviceColloctor.isOwn(f) && (f = this.serviceColloctor.getLocalId(f));
      const N = (M = (g = l == null ? void 0 : l[i]) == null ? void 0 : g[a]) == null ? void 0 : M[p];
      if (N) {
        const S = {
          ...N.updateNodeOpt,
          ...o,
          willRefresh: !0
        };
        if (N && f === $) {
          const C = {
            workId: f,
            msgType: R.UpdateNode,
            dataType: q.Local,
            updateNodeOpt: S,
            emitEventType: this.emitEventType,
            willRefresh: !0,
            willRefreshSelector: !0,
            willSyncService: !0,
            viewId: i,
            needUndoTicker: this.control.viewContainerManager.getUndoTickerId(i)
          };
          h.push([
            C,
            {
              workId: f,
              msgType: R.UpdateNode,
              emitEventType: this.emitEventType
            }
          ]);
        }
      }
    }
    h.length && this.collectForLocalWorker(h);
  }
}
class re {
  constructor(n) {
    I(this, "builders", /* @__PURE__ */ new Map());
    this.builders = new Map(n.map((e) => [e, this.build(e)]));
  }
  build(n) {
    switch (n) {
      case W.TranslateNode:
        return new VM();
      case W.ZIndexNode:
        return new FM();
      case W.CopyNode:
        return new BM();
      case W.SetColorNode:
        return new WM();
      case W.DeleteNode:
        return new QM();
      case W.ScaleNode:
        return new GM();
      case W.RotateNode:
        return new ZM();
      case W.SetFontStyle:
        return new HM();
      case W.SetPoint:
        return new JM();
      case W.SetLock:
        return new XM();
      case W.SetShapeOpt:
        return new KM();
    }
  }
  getBuilder(n) {
    return this.builders.get(n);
  }
  registerForMainEngine(n, e) {
    return this.builders.forEach((t) => {
      t && t.registerForMainEngine(n, e);
    }), this;
  }
  destroy() {
    this.builders.forEach((n) => {
      n && n.destroy();
    }), this.builders.clear();
  }
  pause() {
    return this.builders.forEach((n) => {
      n && n.pause();
    }), this;
  }
  recover() {
    return this.builders.forEach((n) => {
      n && n.recover();
    }), this;
  }
  static emitMethod(n, e, t) {
    Ze.dispatch(n, e, t);
  }
  static activeListener(n) {
    ke.InternalMsgEmitter.on(
      W.ActiveMethod,
      n
    );
  }
  static unmountActiveListener(n) {
    ke.InternalMsgEmitter.off(
      W.ActiveMethod,
      n
    );
  }
}
const qM = async (r, n) => {
  const { src: e, isgl: t, isCache: i } = r;
  if (typeof Image == "function") {
    const o = new Image();
    return typeof e == "string" && (o.crossOrigin = "anonymous"), new Promise((s) => {
      o.onload = function() {
        createImageBitmap(o, {
          imageOrientation: t ? "flipY" : "none"
        }).then((a) => {
          s(a);
        });
      }, o.onerror = function() {
        s(void 0);
      }, o.src = e;
    });
  } else
    return fetch(e, {
      method: "GET",
      mode: "cors",
      cache: "default"
    }).then((o) => o.blob()).then((o) => createImageBitmap(o, {
      imageOrientation: t ? "flipY" : "none"
    }).then((s) => s));
}, Mt = class Mt {
  constructor() {
    I(this, "fontDB");
    I(this, "FONT_DB_NAME", "__APPLIANCE_PLUGIN_FONT_DB");
    I(this, "FONT_STORE_NAME", "fonts");
    I(this, "FONT_DB_VERSION", 1);
  }
  static getInstance() {
    return Mt.instance || (Mt.instance = new Mt()), Mt.instance;
  }
  async initFontDB() {
    return this.fontDB ? this.fontDB : new Promise((n, e) => {
      const t = indexedDB.open(this.FONT_DB_NAME, this.FONT_DB_VERSION);
      t.onerror = () => {
        e(new Error(`Failed to open font database: ${t.error}`));
      }, t.onsuccess = () => {
        this.fontDB = t.result, n(t.result);
      }, t.onupgradeneeded = (i) => {
        const o = i.target.result;
        o.objectStoreNames.contains(this.FONT_STORE_NAME) || o.createObjectStore(this.FONT_STORE_NAME, {
          keyPath: "fontFamily"
        }).createIndex("url", "url", { unique: !1 });
      };
    });
  }
  async getFontFromDB(n) {
    try {
      return this.fontDB || (this.fontDB = await this.initFontDB()), new Promise((e, t) => {
        const s = this.fontDB.transaction(
          [this.FONT_STORE_NAME],
          "readonly"
        ).objectStore(this.FONT_STORE_NAME).get(n);
        s.onsuccess = () => {
          s.result && s.result.buffer ? e(s.result.buffer) : e(null);
        }, s.onerror = () => {
          t(s.error);
        };
      });
    } catch (e) {
      return console.error(
        `Failed to get font from database for ${n}:`,
        e
      ), null;
    }
  }
  async saveFontToDB(n, e, t) {
    try {
      return this.fontDB || (this.fontDB = await this.initFontDB()), new Promise((i, o) => {
        const a = this.fontDB.transaction(
          [this.FONT_STORE_NAME],
          "readwrite"
        ).objectStore(this.FONT_STORE_NAME), c = {
          fontFamily: n,
          url: e,
          buffer: t,
          timestamp: Date.now()
        }, l = a.put(c);
        l.onsuccess = () => {
          i(!0);
        }, l.onerror = () => {
          o(l.error);
        };
      });
    } catch (i) {
      return console.error(
        `Failed to save font to database for ${n}:`,
        i
      ), !1;
    }
  }
  async clearFontCache() {
    try {
      return this.fontDB || (this.fontDB = await this.initFontDB()), new Promise((n, e) => {
        const o = this.fontDB.transaction(
          [this.FONT_STORE_NAME],
          "readwrite"
        ).objectStore(this.FONT_STORE_NAME).clear();
        o.onsuccess = () => {
          console.log("Font cache cleared successfully"), n(!0);
        }, o.onerror = () => {
          e(o.error);
        };
      });
    } catch (n) {
      return console.error("Failed to clear font cache:", n), !1;
    }
  }
  async getFontCacheStatus() {
    try {
      return this.fontDB || (this.fontDB = await this.initFontDB()), new Promise((n, e) => {
        const o = this.fontDB.transaction(
          [this.FONT_STORE_NAME],
          "readonly"
        ).objectStore(this.FONT_STORE_NAME).getAll();
        o.onsuccess = () => {
          const s = o.result || [], a = s.reduce((c, l) => c + (l.buffer ? l.buffer.byteLength : 0), 0);
          n({
            count: s.length,
            totalSize: a
          });
        }, o.onerror = () => {
          e(o.error);
        };
      });
    } catch (n) {
      return console.error("Failed to get font cache status:", n), { count: 0, totalSize: 0 };
    }
  }
};
I(Mt, "instance");
let jn = Mt;
class _M {
  get now() {
    var n;
    return ((n = this.control.room) == null ? void 0 : n.calibrationTimestamp) || Date.now();
  }
  get newWorkId() {
    return this.now.toString();
  }
  /** 设置当前选中的工具配置数据 */
  setCurrentToolsData(n) {
    this.currentToolsData = n;
  }
  getCurrentToolsData() {
    return this.currentToolsData;
  }
  /** 设置当前绘制任务数据 */
  setCurrentLocalWorkData(n) {
    this.currentLocalWorkData = n;
  }
  /** 获取当前激活的工作任务id */
  getWorkId() {
    return this.currentLocalWorkData.workId;
  }
  /** 获取当前work的工作状态 */
  get workState() {
    return this.currentLocalWorkData.workState;
  }
}
class ep extends _M {
  constructor(e) {
    super();
    /** 异步同步时间间隔 */
    I(this, "maxLastSyncTime", Se.syncOpt.interval);
    /** 合并绘制时间间隔 */
    I(this, "combineUnitTime", Se.bezier.combineUnitTime);
    /** 最大合并橡皮擦处理时间 */
    I(this, "maxCombineEraserTime", Se.pencilEraser.maxCombineTime);
    /** 平滑同步 */
    I(this, "smoothSync", Se.syncOpt.smoothSync);
    I(this, "isActive", !1);
    I(this, "currentToolsData");
    I(this, "currentLocalWorkData");
    I(this, "control");
    I(this, "internalMsgEmitter");
    I(this, "publicMsgEmitter");
    I(this, "taskBatchData", /* @__PURE__ */ new Set());
    I(this, "fullWorker");
    I(this, "subWorker");
    I(this, "fullWorkerUrl");
    I(this, "subWorkerUrl");
    I(this, "methodBuilder");
    I(this, "zIndexNodeMethod");
    /** master\fullwoker\subworker 三者高频绘制时队列化参数 */
    I(this, "subWorkerDrawCount", 0);
    I(this, "fullWorkerDrawCount", 0);
    I(this, "maxDrawCount", 0);
    I(this, "_consumeCount", 0);
    I(this, "_postCount", 0);
    I(this, "reRenders", /* @__PURE__ */ new Map());
    I(this, "localWorkViewId");
    I(this, "localPointsBatchData", /* @__PURE__ */ new Map());
    /** end */
    /** 是否任务队列化参数 */
    I(this, "tasksqueue", /* @__PURE__ */ new Map());
    I(this, "useTasksqueue", !1);
    I(this, "useTasksClockId");
    I(this, "mainTasksqueueCount");
    I(this, "workerTasksqueueCount");
    /** end */
    I(this, "snapshotMap", /* @__PURE__ */ new Map());
    I(this, "boundingRectMap", /* @__PURE__ */ new Map());
    I(this, "clearAllResolveMap", /* @__PURE__ */ new Map());
    I(this, "delayWorkStateToDoneResolve");
    I(this, "animationId");
    I(this, "tmpImageConfigMap", /* @__PURE__ */ new Map());
    I(this, "mainThread");
    I(this, "backgroundThreadManager");
    I(this, "willSelectorWorkId");
    I(this, "isLockSentEventCursor", !1);
    I(this, "getVNodeInfoResolveMap", /* @__PURE__ */ new Map());
    I(this, "setFontFacesResolve", {});
    I(this, "isLocalWorkActive", !1);
    /** IndexedDB工具类实例 */
    I(this, "indexDBUtils", jn.getInstance());
    I(this, "markmapIdResolveMap", /* @__PURE__ */ new Map());
    const { control: t, internalMsgEmitter: i, publicMsgEmitter: o } = e;
    this.control = t, this.maxLastSyncTime = this.control.pluginOptions.extras.syncOpt.interval, this.combineUnitTime = this.control.pluginOptions.extras.bezier.combineUnitTime || this.combineUnitTime, this.smoothSync = We(
      this.control.pluginOptions.extras.syncOpt.smoothSync
    ) ? this.control.pluginOptions.extras.syncOpt.smoothSync : this.smoothSync, this.maxCombineEraserTime = this.control.pluginOptions.extras.pencilEraser.maxCombineTime || this.maxCombineEraserTime, this.fullWorkerUrl = this.control.pluginOptions.cdn.fullWorkerUrl || "", this.subWorkerUrl = this.control.pluginOptions.cdn.subWorkerUrl || "", this.internalMsgEmitter = i, this.publicMsgEmitter = o, this.currentLocalWorkData = { workState: x.Pending };
  }
  get isBezier() {
    return this.control.pluginOptions.extras.bezier.enable;
  }
  destroy() {
    var e, t, i;
    (e = this.methodBuilder) == null || e.destroy(), this.unWritable(), this.taskBatchData.clear(), this.tasksqueue.clear(), this.tmpImageConfigMap.clear(), this.localPointsBatchData.clear(), this.isActive = !1, this.clearAllResolveMap.clear(), this.snapshotMap.clear(), this.boundingRectMap.clear(), (t = this.fullWorker) == null || t.terminate(), (i = this.subWorker) == null || i.terminate();
  }
  get consumeCount() {
    return this._consumeCount;
  }
  set consumeCount(e) {
    this._consumeCount = e;
  }
  get postCount() {
    return this._postCount;
  }
  set postCount(e) {
    this._postCount = e;
  }
  get viewContainerManager() {
    return this.control.viewContainerManager;
  }
  get collector() {
    return this.control.collector;
  }
  get isRunSubWork() {
    var t;
    const e = (t = this.currentToolsData) == null ? void 0 : t.toolsType;
    return e === T.Pencil || e === T.PencilEraser || e === T.BitMapEraser || e === T.LaserPen || e === T.Arrow || e === T.Straight || e === T.Ellipse || e === T.Rectangle || e === T.Star || e === T.Polygon || e === T.SpeechBalloon || e === T.Selector;
  }
  get isCanDrawWork() {
    var t;
    const e = (t = this.currentToolsData) == null ? void 0 : t.toolsType;
    return e === T.Pencil || e === T.LaserPen || e === T.Arrow || e === T.Straight || e === T.Ellipse || e === T.Rectangle || e === T.Star || e === T.Polygon || e === T.SpeechBalloon || e === T.Triangle || e === T.Rhombus || e === T.BitMapEraser || e === T.PencilEraser;
  }
  get isUseZIndex() {
    var t;
    const e = (t = this.currentToolsData) == null ? void 0 : t.toolsType;
    return e === T.Pencil || e === T.Arrow || e === T.Straight || e === T.Ellipse || e === T.Rectangle || e === T.Star || e === T.Polygon || e === T.SpeechBalloon || e === T.Text || e === T.Image;
  }
  get isCanRecordUndoRedo() {
    var t;
    const e = (t = this.currentToolsData) == null ? void 0 : t.toolsType;
    return e === T.Pencil || e === T.Eraser || e === T.PencilEraser || e === T.BitMapEraser || e === T.Arrow || e === T.Straight || e === T.Ellipse || e === T.Rectangle || e === T.Star || e === T.Polygon || e === T.SpeechBalloon || e === T.Text || e === T.Image;
  }
  get isCanSentCursor() {
    var t;
    const e = (t = this.currentToolsData) == null ? void 0 : t.toolsType;
    return e === T.Pencil || e === T.Text || e === T.LaserPen || e === T.Arrow || e === T.Straight || e === T.Ellipse || e === T.Rectangle || e === T.Star || e === T.Polygon || e === T.SpeechBalloon || e === T.Triangle || e === T.Rhombus || e === T.Eraser || e === T.PencilEraser || e === T.BitMapEraser || e === T.Selector;
  }
  get isPostCountType() {
    var t;
    const e = (t = this.currentToolsData) == null ? void 0 : t.toolsType;
    return e === T.Pencil || e === T.Arrow || e === T.Straight || e === T.Ellipse || e === T.Rectangle || e === T.Star || e === T.Polygon || e === T.SpeechBalloon || e === T.Triangle || e === T.Rhombus || e === T.Eraser || e === T.PencilEraser || e === T.BitMapEraser || e === T.Selector;
  }
  get isCanPostDraw() {
    var t;
    const e = (t = this.currentToolsData) == null ? void 0 : t.toolsType;
    return e === T.Pencil || e === T.PencilEraser || e === T.BitMapEraser;
  }
  get isCanStartEventConsum() {
    var t;
    const e = (t = this.currentToolsData) == null ? void 0 : t.toolsType;
    return e === T.Selector || e === T.Eraser || e === T.PencilEraser || e === T.BitMapEraser;
  }
  async init() {
    this.destroy(), await this.on(), this.internalMsgEmitterListener(), this.isActive = !0;
  }
  async useBackgroundThread() {
    console.info("use background thread");
    const { BackgroundThreadManagerImpl: e } = await import("./index-BH2WeNgx.mjs");
    this.backgroundThreadManager = new e(this);
  }
  async useMainThread() {
    console.info("no OffscreenCanvas"), this.control.hasExtendFontFaces && await document.fonts.ready.then(() => {
      this.control.room && this.control.room.logger.info(
        "set font faces in main thread success"
      );
    });
    const { MainThreadManagerImpl: e } = await import("./index-BZIAL5Sa.mjs");
    this.mainThread = new e(this);
  }
  async on() {
    if (this.control.isUseBackgroundThread && await this.useBackgroundThread(), !this.control.hasOffscreenCanvas) {
      await this.useMainThread();
      return;
    }
    if (!this.fullWorkerUrl || !this.subWorkerUrl) {
      console.error("no worker url config");
      return;
    }
    if (this.fullWorker = new Worker(this.fullWorkerUrl, { type: "classic" }), this.control.isUseSimple || (this.subWorker = new Worker(this.subWorkerUrl, { type: "classic" })), this.fullWorker.onmessage = (e) => {
      if (e.data) {
        const {
          render: t,
          sp: i,
          fullWorkerDrawCount: o,
          workerTasksqueueCount: s,
          viewId: a,
          consumeCount: c
        } = e.data;
        if (c && (this.consumeCount = c), this.isBusy && s && this.setWorkerTasksqueueCount(s), i != null && i.length && this.collectorSyncData(i), this.control.isUseSimple) {
          if (this.control.isUseOffscreenTransfer)
            return;
          t && (t != null && t.length) && this.viewContainerManager.render(t);
          return;
        }
        const l = (t == null ? void 0 : t.length) && t || [];
        if (o && a) {
          if (this.fullWorkerDrawCount = o, this.fullWorkerDrawCount < 1 / 0) {
            const h = Math.max(
              this.maxDrawCount,
              this.fullWorkerDrawCount
            );
            this.setMaxDrawCount(h), this.clearReRenders(this.fullWorkerDrawCount);
          } else
            this.setMaxDrawCount(0), this.clearReRenders(1 / 0);
          if (this.reRenders.size)
            for (const h of this.reRenders.values())
              l.push(...h);
        }
        l.length && this.viewContainerManager.render(l);
      }
    }, this.subWorker && (this.subWorker.onmessage = (e) => {
      if (e.data) {
        const { render: t, sp: i, subWorkerDrawCount: o, consumeCount: s } = e.data;
        s && (this.consumeCount = s), i != null && i.length && this.collectorSyncData(i);
        const a = (t == null ? void 0 : t.length) && t || [];
        if (ee(o)) {
          if (this.subWorkerDrawCount = o, this.fullWorkerDrawCount < 1 / 0) {
            const c = Math.max(
              this.maxDrawCount,
              this.subWorkerDrawCount
            );
            this.setMaxDrawCount(c);
          }
          if (t != null && t.length)
            if (this.subWorkerDrawCount > this.fullWorkerDrawCount) {
              const c = [];
              for (const l of t)
                l.imageBitmap && (l.isUnClose = !0, c.push(l));
              this.reRenders.set(this.subWorkerDrawCount, c);
            } else this.reRenders.size && this.clearReRenders(this.subWorkerDrawCount);
        }
        a.length && this.viewContainerManager.render(a);
      }
    }), this.control.hasExtendFontFaces) {
      const e = this.control.extendFontFaces, t = [], i = [];
      this.control.room && this.control.room.logger.info(
        `load before font faces ${JSON.stringify(e)}`
      ), await new Promise(async (o) => {
        this.setFontFacesResolve.resolve = o, this.setFontFacesResolve.timer = setTimeout(() => {
          this.setFontFacesResolve.resolve && (this.control.room ? this.control.room.logger.error(
            "set font faces timeout"
          ) : console.warn("set font faces timeout"), this.setFontFacesResolve.resolve(!1));
        }, this.control.loadFontFacesTimeout);
        try {
          for (const s of e) {
            let a;
            a = await this.indexDBUtils.getFontFromDB(
              s.fontFamily
            ) || void 0, a || (a = await this.loadFontInMainThread(s.src), a && a.byteLength > 0 && (await this.indexDBUtils.saveFontToDB(
              s.fontFamily,
              s.src,
              a
            ) || (this.control.room ? this.control.room.logger.warn(
              `Failed to save font ${s.fontFamily} to IndexedDB, but continuing with font loading`
            ) : console.warn(
              `Failed to save font ${s.fontFamily} to IndexedDB, but continuing with font loading`
            )))), a && a.byteLength > 0 ? (i.push({
              fontFamily: s.fontFamily,
              buffer: a
            }), t.push(a)) : this.control.room ? this.control.room.logger.warn(
              `Font ${s.fontFamily} could not be loaded from any source`
            ) : console.warn(
              `Font ${s.fontFamily} could not be loaded from any source`
            );
          }
          this.control.room && this.control.room.logger.info(
            "load end font faces"
          ), i.length ? this.fullWorker.postMessage(
            /* @__PURE__ */ new Set([
              {
                type: R.SetFontFace,
                fontBuffers: i
              }
            ]),
            t
          ) : this.setFontFacesResolve.resolve(!0);
        } catch (s) {
          this.setFontFacesResolve.timer && (clearTimeout(this.setFontFacesResolve.timer), this.setFontFacesResolve.timer = void 0), this.setFontFacesResolve.resolve && (this.setFontFacesResolve.resolve(!1), this.control.room ? this.control.room.logger.error(
            `set font faces error: ${s}`
          ) : console.error(`set font faces error: ${s}`));
        }
      }).then((o) => {
        this.setFontFacesResolve.resolve = void 0, this.setFontFacesResolve.timer = void 0, this.control.room ? o ? this.control.room.logger.info(
          "set font faces in web worker success"
        ) : this.control.room.logger.error(
          "set font faces in web worker error"
        ) : o || console.error("set font faces in web worker error");
      });
    }
  }
  /**
   * 清理字体缓存数据库
   */
  async clearFontCache() {
    return await this.indexDBUtils.clearFontCache();
  }
  /**
   * 获取字体缓存状态
   */
  async getFontCacheStatus() {
    return await this.indexDBUtils.getFontCacheStatus();
  }
  async loadFontInMainThread(e) {
    try {
      const t = await fetch(e);
      if (!t.ok)
        throw new Error(`HTTP ${t.status}: ${t.statusText}`);
      const i = await t.arrayBuffer();
      if (!i || i.byteLength === 0)
        throw new Error("Empty font data received");
      const o = new Uint8Array(i);
      if (!this.validateFontData(o))
        throw new Error("Invalid font data format: not a valid font file");
      return i;
    } catch (t) {
      this.control.room ? this.control.room.logger.error(
        `Failed to load font from ${e}: ${t}`
      ) : console.error(`Failed to load font from ${e}: ${t}`);
      return;
    }
  }
  // 验证字体数据格式
  validateFontData(e) {
    return e.length < 4 ? !1 : e[0] === 119 && e[1] === 79 && e[2] === 70 && e[3] === 50 || e[0] === 119 && e[1] === 79 && e[2] === 70 && e[3] === 70 || e[0] === 0 && e[1] === 1 && e[2] === 0 && e[3] === 0 || e[0] === 79 && e[1] === 84 && e[2] === 84 && e[3] === 79 || e.length > 36 && e[36] === 0 && e[37] === 1 && e[38] === 0 && e[39] === 0;
  }
  clearReRenders(e = 1 / 0) {
    for (const t of this.reRenders.keys())
      if (ee(Number(t)) && Number(t) <= e) {
        const i = this.reRenders.get(Number(t));
        i == null || i.forEach((o) => {
          var s;
          (s = o.imageBitmap) == null || s.close();
        }), this.reRenders.delete(t);
      }
  }
  get isBusy() {
    return this.getTasksqueueState() === x.Doing;
  }
  getLockSentEventCursor() {
    return this.isLockSentEventCursor;
  }
  setLockSentEventCursor(e) {
    this.isLockSentEventCursor = e;
  }
  getTasksqueueState() {
    return this.useTasksqueue && x.Doing || x.Done;
  }
  setMaxDrawCount(e) {
    this.maxDrawCount = e;
  }
  getFullWorkDrawCount() {
    return this.fullWorkerDrawCount;
  }
  getMaxDrawCount() {
    return this.maxDrawCount;
  }
  setSubWorkerDrawCount(e) {
    this.subWorkerDrawCount = e;
  }
  setConsumeCount(e) {
    this.consumeCount = e;
  }
  getConsumeCount() {
    return this.consumeCount;
  }
  setWorkerTasksqueueCount(e) {
    const t = Math.max(
      this.workerTasksqueueCount || 0,
      e
    );
    this.workerTasksqueueCount = t;
  }
  collectorSyncData(e) {
    var i, o, s, a;
    let t = !1;
    for (const c of e) {
      const {
        type: l,
        selectIds: h,
        opt: u,
        selectRect: g,
        strokeColor: M,
        fillColor: d,
        willSyncService: y,
        isSync: w,
        imageBitmap: p,
        canvasHeight: f,
        canvasWidth: N,
        rect: S,
        op: C,
        canTextEdit: k,
        points: v,
        selectorColor: A,
        canRotate: P,
        scaleType: E,
        textOpt: O,
        toolsType: z,
        workId: L,
        viewId: j,
        dataType: F,
        canLock: X,
        isLocked: Y,
        shapeOpt: Q,
        toolsTypes: _,
        thickness: ce,
        strokeType: ie,
        useStroke: K,
        isgl: H,
        imageSrc: fe,
        isSubWorker: Je,
        updateNodeOpt: Xe,
        setFontFaceResult: ht,
        failedFonts: tt,
        reportString: Ke,
        index: mt,
        imageElement: Ee
      } = c;
      if (l === R.SetFontFace) {
        ht && this.setFontFacesResolve.resolve && (tt && tt.length > 0 && (this.control.room ? this.control.room.logger.info(
          "set font face has failed fonts: " + tt.join(", ")
        ) : console.info(
          "set font face has failed fonts: " + tt.join(", ")
        )), this.setFontFacesResolve.resolve(ht));
        return;
      }
      if (!j)
        return;
      const V = c.scenePath || this.viewContainerManager.getCurScenePath(j);
      switch (l) {
        case R.Select: {
          const B = h != null && h.length ? { ...g, selectIds: h, canvasHeight: f, canvasWidth: N, points: v } : void 0;
          B && (u != null && u.strokeColor) && (B.selectorColor = u.strokeColor), B && A && (B.selectorColor = A), B && M && (B.strokeColor = M), B && (u != null && u.fillColor) && (B.fillColor = u.fillColor), B && d && (B.fillColor = d), B && We(P) && (B.canRotate = P), B && E && (B.scaleType = E), B && k && (B.canTextEdit = k), B && O && (B.textOpt = O), B && We(X) && (B.canLock = X), B && We(Y) && (B.isLocked = Y), B && Q && (B.shapeOpt = Q), B && _ && (B.toolsTypes = _), B && ce && (B.thickness = ce), B && ie && (B.strokeType = ie), B && We(K) && (B.useStroke = K), j && this.viewContainerManager.showFloatBar(j, !!B, B), y && ((i = this.collector) == null || i.dispatch({
            type: l,
            selectIds: h,
            opt: u,
            isSync: w,
            viewId: j,
            scenePath: V
          }));
          break;
        }
        case R.Snapshot:
          if ((p || Ee) && V) {
            const B = this.snapshotMap.get(V);
            if (B) {
              const { resolve: Z, value: oe } = B;
              if (oe && Z) {
                const ue = mt || 0;
                oe[ue] = p || Ee, oe.every((Me) => Me !== void 0) && Z(oe);
              }
            }
          }
          break;
        case R.BoundingBox:
          if (S && V) {
            const B = this.boundingRectMap.get(V);
            B && B(S);
          }
          break;
        case R.Cursor:
          C && this.control.cursor.collectServiceCursor({ ...c });
          break;
        case R.Clear:
          if (j) {
            this.viewContainerManager.showFloatBar(j, !1);
            const B = (o = this.clearAllResolveMap.get(j)) == null ? void 0 : o.resolve;
            B && B(j);
          }
          break;
        case R.BackgroundSVGDelete:
          this.backgroundThreadManager && j && this.backgroundThreadManager.consumeTargetOne(j, {
            ...c,
            msgType: R.BackgroundSVGDelete,
            viewId: j,
            dataType: q.Local
          });
          break;
        case R.BackgroundSVGUpdate:
          this.backgroundThreadManager && j && this.backgroundThreadManager.consumeTargetOne(j, {
            ...c,
            msgType: R.BackgroundSVGUpdate,
            viewId: j,
            dataType: q.Local
          });
          break;
        case R.TextUpdate:
          if (z === T.Text && L && j) {
            const B = this.viewContainerManager.transformToOriginPoint(
              (u == null ? void 0 : u.boxPoint) || [0, 0],
              j
            ), Z = (u == null ? void 0 : u.boxSize) || [0, 0], oe = (s = this.viewContainerManager.getView(j)) == null ? void 0 : s.cameraOpt;
            u ? this.control.textEditorManager.updateTextForWorker({
              x: B[0],
              y: B[1],
              w: Z[0],
              h: Z[1],
              scale: (oe == null ? void 0 : oe.scale) || 1,
              workId: L,
              opt: u,
              dataType: F,
              viewId: j,
              canSync: y || !1,
              canWorker: !1,
              updateNodeOpt: Xe
            }) : this.control.textEditorManager.delete(
              L,
              y || !1,
              !1
            );
          }
          break;
        case R.GetTextActive:
          z === T.Text && L && j && this.control.textEditorManager.updateTextForWorker(
            {
              workId: L,
              isActive: !0,
              viewId: j,
              dataType: q.Local,
              canWorker: !1,
              canSync: !0
            },
            this.now
          );
          break;
        case R.GetImageBitMap:
          fe && j && L && qM({
            src: fe,
            isgl: H
          }).then((B) => {
            var oe;
            const Z = /* @__PURE__ */ new Set();
            if (Z.add({
              msgType: R.GetImageBitMap,
              dataType: q.Local,
              workId: L,
              viewId: j,
              imageSrc: fe,
              isSubWorker: Je,
              imageBitmap: B
            }), !this.control.hasOffscreenCanvas)
              (oe = this.mainThread) == null || oe.consume(Z);
            else {
              const ue = Je && this.subWorker ? this.subWorker : this.fullWorker;
              B ? ue.postMessage(Z, [B]) : ue.postMessage(Z);
            }
          });
          break;
        case R.GetVNodeInfo:
          if (L) {
            const B = L.toString(), Z = (a = this.getVNodeInfoResolveMap.get(B)) == null ? void 0 : a.resolve, oe = { ...c, uuid: B };
            Z && Z(oe);
          }
          break;
        case R.ReportError:
        case R.ReportLog:
          Ke && (l === R.ReportError ? this.control.room ? this.control.room.logger.error(
            Ke
          ) : console.error(
            "[MasterControlForWorker] reportError",
            Ke
          ) : this.control.room ? this.control.room.logger.info(
            Ke
          ) : console.log("[MasterControlForWorker] reportLog", Ke));
          break;
        default:
          t = !0;
          break;
      }
    }
    t && this.collectorAsyncData(e);
  }
  foceUndoTickerEnd(e, t) {
    this.internalMsgEmitter.emit("forceUndoTickerEnd", e, t);
  }
  collectorAsyncData(e) {
    var t, i, o, s;
    for (const a of e) {
      const {
        type: c,
        op: l,
        workId: h,
        index: u,
        removeIds: g,
        ops: M,
        opt: d,
        updateNodeOpt: y,
        toolsType: w,
        isSync: p,
        viewId: f,
        isLockSentEventCursor: N,
        nextTasks: S,
        needUndoTicker: C
      } = a;
      if (!f) {
        console.error(
          "[MasterControlForWorker] collectorAsyncData on viewId in",
          a
        );
        return;
      }
      const k = a.scenePath || this.viewContainerManager.getCurScenePath(f);
      switch (c) {
        case R.None: {
          N && ot(
            () => {
              this.setLockSentEventCursor(!1);
            },
            this.maxLastSyncTime,
            this.control.hasPolyfillMethod("requestIdleCallback")
          ), C && Number(C) && ot(
            () => {
              this.foceUndoTickerEnd(Number(C), f);
            },
            0,
            this.control.hasPolyfillMethod("requestIdleCallback")
          );
          break;
        }
        case R.DrawWork: {
          (t = this.collector) == null || t.dispatch({
            type: c,
            op: l,
            opt: d,
            toolsType: w,
            workId: h,
            index: u,
            isSync: p,
            viewId: f,
            scenePath: k,
            updateNodeOpt: y
          });
          break;
        }
        case R.FullWork: {
          if ((i = this.collector) == null || i.dispatch({
            type: c,
            ops: M,
            workId: h,
            updateNodeOpt: y,
            opt: d,
            toolsType: w,
            isSync: p,
            viewId: f,
            scenePath: k
          }), w === T.BackgroundSVG && h && this.markmapIdResolveMap.has(h.toString())) {
            const v = this.markmapIdResolveMap.get(
              h.toString()
            );
            v != null && v.timer && (clearTimeout(v.timer), v.timer = void 0), v != null && v.resolve && v.resolve(h.toString()), this.markmapIdResolveMap.delete(h.toString());
          }
          this.willSelectorWorkId && h && h.toString() === this.willSelectorWorkId && this.control.runEffectWork(() => {
            this.setShapeSelectorByWorkId(
              this.willSelectorWorkId,
              f
            ), this.willSelectorWorkId = void 0;
          });
          break;
        }
        case R.UpdateNode: {
          (o = this.collector) == null || o.dispatch({
            type: c,
            updateNodeOpt: y,
            workId: h,
            opt: d,
            ops: M,
            op: l,
            isSync: p,
            viewId: f,
            scenePath: k
          });
          break;
        }
        case R.RemoveNode: {
          g && this.control.textEditorManager.deleteBatch(g, !1, !1), (s = this.collector) == null || s.dispatch({
            type: c,
            removeIds: g,
            isSync: p,
            viewId: f,
            scenePath: k
          }), this.willSelectorWorkId && (g != null && g.includes(this.willSelectorWorkId)) && (this.willSelectorWorkId = void 0);
          break;
        }
      }
      S && this.collectorAsyncData(S);
    }
  }
  async onLocalEventEnd(e, t) {
    var l;
    const i = this.viewContainerManager.getView(t);
    if (!i)
      return;
    const { focusScenePath: o, cameraOpt: s } = i, a = this.viewContainerManager.transformToScenePoint(e, t), c = [];
    for (const h of this.localPointsBatchData.keys())
      if (((l = this.currentToolsData) == null ? void 0 : l.toolsType) === T.Text) {
        const u = this.getLocalPointInfo(h), g = u == null ? void 0 : u.state;
        if (g && g === x.Start) {
          const M = this.currentLocalWorkData.toolsOpt;
          M.workState = x.Doing, M.boxPoint = [Math.round(a[0]), Math.round(a[1])], M.boxSize = [M.fontSize, M.fontSize], this.control.textEditorManager.checkEmptyTextBlur(), this.control.textEditorManager.createTextForMasterController(
            {
              workId: this.newWorkId,
              x: e[0],
              y: e[1],
              scale: (s == null ? void 0 : s.scale) || 1,
              opt: M,
              type: $e.Text,
              isActive: !0,
              viewId: t,
              scenePath: o
            },
            this.now
          );
        }
        this.deleteLocalPoint(h);
        continue;
      } else
        this.pushLocalPoint(h, a, x.Done, t), c.push(h);
    if (c.length) {
      try {
        if (await new Promise((u) => {
          setTimeout(async () => {
            c.forEach((g) => {
              this.setLocalPointIsFullWork(g);
            }), this.delayWorkStateToDoneResolve = u, this.consume();
          }, 0);
        }) && c[0]) {
          const u = c[0];
          c.forEach((g) => {
            this.deleteLocalPoint(g);
          }), this.willSelectorWorkId = u.toString();
        }
      } catch (h) {
        console.error("[MasterControlForWorker] onLocalEventEnd error:", h);
      }
      this.delayWorkStateToDoneResolve = void 0, c.length = 0;
    }
  }
  onLocalEventDoing(e, t) {
    var o;
    if (((o = this.currentToolsData) == null ? void 0 : o.toolsType) === T.Text)
      return;
    this.currentLocalWorkData.workState === x.Start && this.setCurrentLocalWorkData({
      ...this.currentLocalWorkData,
      workState: x.Doing
    });
    let i = !1;
    for (const [s, { state: a }] of this.localPointsBatchData.entries())
      if (this.isAbled() && a !== x.Pending) {
        const c = this.viewContainerManager.transformToScenePoint(e, t);
        this.pushLocalPoint(
          s,
          c,
          a === x.Start ? x.Doing : a,
          t
        ), i = !0;
      }
    if (i) {
      this.runAnimation();
      return;
    }
  }
  onLocalEventStart(e, t) {
    var c, l;
    this.viewContainerManager.focuedViewId !== t && this.viewContainerManager.setFocuedViewId(t), this.isCanDrawWork && this.control.room && !this.control.room.disableDeviceInputs && (this.control.room.disableDeviceInputs = !0);
    const i = (c = this.currentToolsData) == null ? void 0 : c.toolsType, o = i === T.Selector ? $ : this.newWorkId, s = this.setZIndex(t);
    this.setCurrentLocalWorkData({
      workState: x.Start,
      toolsOpt: s,
      viewId: t
    });
    const a = this.viewContainerManager.transformToScenePoint(e, t);
    if (this.pushLocalPoint(o, a, x.Start, t, s), i !== T.Text) {
      if (this.control.textEditorManager.checkEmptyTextBlur(), this.isCanRecordUndoRedo && this.internalMsgEmitter.emit("addUndoTicker", this.now, t), o && s && i && this.isCanStartEventConsum && this.prepareOnceWork({ workId: o, toolsOpt: s, viewId: t }, i), this.consumeCount = 0, this.postCount = 0, this.maxDrawCount = 0, this.fullWorkerDrawCount = 0, this.subWorkerDrawCount = 0, this.clearReRenders(1 / 0), this.isCanDrawWork) {
        const h = this.viewContainerManager.getCurScenePath(t);
        h && ((l = this.collector) != null && l.hasSelector(t, h)) && this.blurSelector(t, h);
      }
      this.consume();
    }
  }
  setLocalPointIsFullWork(e) {
    const t = this.getLocalPointInfo(e);
    t && (t.isFullWork = !0, this.localPointsBatchData.set(e, t));
  }
  pushLocalPoint(e, t, i, o, s) {
    let a = this.getLocalPointInfo(e);
    i === x.Start ? a = {
      state: x.Start,
      points: t,
      opt: s || this.currentLocalWorkData.toolsOpt,
      isFullWork: !1,
      viewId: o
    } : a && (a.state = i, a.points.push(t[0], t[1])), a && this.localPointsBatchData.set(e, a);
  }
  deleteLocalPoint(e) {
    this.localPointsBatchData.delete(e);
  }
  getLocalPointInfo(e) {
    return this.localPointsBatchData.get(e);
  }
  getLocalPointsInfo() {
    return this.localPointsBatchData;
  }
  correctStorage(e, t, i) {
    var u, g;
    const o = /* @__PURE__ */ new Map(), s = [], a = /* @__PURE__ */ new Map(), c = [];
    Object.keys(e).forEach((M) => {
      var w, p, f;
      const d = (p = (w = e[M]) == null ? void 0 : w.opt) == null ? void 0 : p.zIndex, y = (f = e[M]) == null ? void 0 : f.toolsType;
      ee(d) && (y && y === T.BackgroundSVG ? c.push([M, d]) : s.push([M, d]));
    });
    const l = s.length && ((u = this.zIndexNodeMethod) == null ? void 0 : u.correct(s)) || [], h = c.length && ((g = this.zIndexNodeMethod) == null ? void 0 : g.correct(c)) || [];
    this.zIndexNodeMethod && l.length && (this.zIndexNodeMethod.setMinZIndex(l[0][1] || 0, t), this.zIndexNodeMethod.setMaxZIndex(
      l[l.length - 1][1] || 0,
      t
    )), this.zIndexNodeMethod && h.length && (this.zIndexNodeMethod.setMinZIndexForBackgroundSVG(
      h[0][1] || 0,
      t
    ), this.zIndexNodeMethod.setMaxZIndexForBackgroundSVG(
      h[h.length - 1][1] || 0,
      t
    ));
    for (const [M, d] of l) {
      if (!e[M])
        continue;
      const y = e[M];
      y.opt && ee(y.opt.zIndex) && y.opt.zIndex !== d && (y.opt.zIndex = d, o.set(M, y));
    }
    o.size && o.forEach((M, d) => {
      var y;
      (y = this.collector) == null || y.updateValue(d, M, {
        viewId: t,
        scenePath: i,
        isSync: !0
      }), e[d] = M;
    });
    for (const [M, d] of h) {
      if (!e[M])
        continue;
      const y = e[M];
      y.opt && ee(y.opt.zIndex) && y.opt.zIndex !== d && (y.opt.zIndex = d, o.set(M, y));
    }
    return a.size && a.forEach((M, d) => {
      var y;
      (y = this.collector) == null || y.updateValue(d, M, {
        viewId: t,
        scenePath: i,
        isSync: !0
      }), e[d] = M;
    }), e;
  }
  async originalEventLintener(e, t, i) {
    var c, l, h;
    if (!this.isAbled())
      return;
    const o = (c = this.collector) == null ? void 0 : c.uid, s = this.viewContainerManager.getCurScenePath(i);
    if (o && !this.control.renderControl.isWritable)
      return;
    const a = ((l = this.currentToolsData) == null ? void 0 : l.toolsType) === T.Selector;
    switch ((h = this.currentToolsData) == null ? void 0 : h.toolsType) {
      case T.Eraser:
      case T.PencilEraser:
      case T.BitMapEraser: {
        if (o && !this.control.renderControl.isClearAble({ uid: o, viewId: i, scenePath: s }))
          return;
        break;
      }
      default: {
        if (o && !this.control.renderControl.isRenderAble({ uid: o, viewId: i, scenePath: s }))
          return;
        break;
      }
    }
    switch (e) {
      case x.Start:
        this.isLocalWorkActive = !0, this.setLocalWorkViewId(i), i && this.onLocalEventStart(t, i), a && this.viewContainerManager.showFloatBarBtn(i, !1);
        break;
      case x.Doing:
        i && i === this.getLocalWorkViewId() && this.onLocalEventDoing(t, i);
        break;
      case x.Done:
        i && i === this.getLocalWorkViewId() && (this.setCurrentLocalWorkData({
          ...this.currentLocalWorkData,
          workState: x.Done
        }), await this.onLocalEventEnd(t, i), setTimeout(() => {
          this.isLocalWorkActive = !1, this.setCurrentLocalWorkData({ workState: x.Pending }), a && this.viewContainerManager.showFloatBarBtn(i, !0), this.setLocalWorkViewId(void 0);
        }, 0));
        break;
    }
    this.isLocalWorkActive && this.viewContainerManager.focuedViewId === i && this.publicMsgEmitter.emit("workState", e, i), ot(
      () => {
        var u;
        (u = this.control.pluginManager) == null || u.dispatchPluginEvent(
          "localEvent",
          i,
          e,
          t
        );
      },
      Xi,
      this.control.hasPolyfillMethod("requestIdleCallback")
    );
  }
  getLocalWorkViewId() {
    return this.localWorkViewId;
  }
  setLocalWorkViewId(e) {
    this.localWorkViewId = e;
  }
  setCurrentToolsData(e) {
    var o, s, a;
    const t = e.toolsType, i = ((o = this.currentToolsData) == null ? void 0 : o.toolsType) !== e.toolsType;
    if (super.setCurrentToolsData(e), i) {
      const c = (s = this.viewContainerManager) == null ? void 0 : s.getAllViews();
      for (const l of c)
        if (l) {
          const { id: h, focusScenePath: u } = l;
          i && h && u && ((a = this.collector) != null && a.hasSelector(h, u) && this.blurSelector(h, u), this.control.textEditorManager.checkEmptyTextBlur());
        }
      this.taskBatchData.add({
        msgType: R.UpdateTools,
        dataType: q.Local,
        toolsType: t,
        opt: {
          ...e.toolsOpt,
          syncUnitTime: this.maxLastSyncTime
        },
        combineUnitTime: this.isBezier ? this.combineUnitTime : 1 / 0,
        maxCombineEraserTime: this.maxCombineEraserTime,
        isRunSubWork: this.isRunSubWork,
        viewId: fg,
        isRunBackgroundThread: this.control.isUseBackgroundThread
      }), this.runAnimation();
    }
  }
  prepareOnceWork(e, t) {
    const { workId: i, toolsOpt: o, viewId: s } = e, a = {
      msgType: R.CreateWork,
      dataType: q.Local,
      viewId: s,
      toolsType: t
    };
    this.queryTaskBatchData(a).forEach((c) => {
      this.taskBatchData.delete(c);
    }), this.taskBatchData.add({
      ...a,
      workId: i,
      opt: { ...o, syncUnitTime: this.maxLastSyncTime },
      dataType: q.Local,
      isRunSubWork: this.isRunSubWork
    }), this.runAnimation();
  }
  createViewWorker(e, t, i) {
    const { offscreenCanvasOpt: o, layerOpt: s, dpr: a, cameraOpt: c } = t, l = {
      msgType: R.Init,
      dataType: q.Local,
      viewId: e,
      offscreenCanvasOpt: o,
      layerOpt: s,
      dpr: a,
      cameraOpt: c,
      isRunSubWork: !0,
      isSafari: navigator.userAgent.indexOf("Safari") !== -1 && navigator.userAgent.indexOf("Chrome") === -1,
      combineUnitTime: this.isBezier ? this.combineUnitTime : 1 / 0,
      maxCombineEraserTime: this.maxCombineEraserTime,
      syncUnitTime: this.maxLastSyncTime,
      isUseSimple: this.control.isUseSimple,
      offscreenCanvas: i.transferControlToOffscreenCanvas
    };
    this.isBusy && this.destroyTaskQueue(), this.post(/* @__PURE__ */ new Set([l])), this.taskBatchData.size && this.consume();
  }
  destroyViewWorker(e, t = !1) {
    var i;
    this.getLocalWorkViewId() === e && this.setLocalWorkViewId(void 0), this.zIndexNodeMethod && this.zIndexNodeMethod.clearZIndex(e), this.taskBatchData.add({
      msgType: R.Destroy,
      dataType: q.Local,
      viewId: e,
      isRunSubWork: !0
    }), this.consume(), t || (i = this.collector) == null || i.dispatch({
      type: R.Clear,
      viewId: e
    }), this.control.renderControl.destoryByViewId(e, t);
  }
  onServiceDerive(e, t) {
    var d, y, w, p, f, N, S, C, k;
    const { newValue: i, oldValue: o, viewId: s, scenePath: a } = t, c = se(i) || {}, l = e;
    let h = c.type;
    if (!i && o) {
      if (l && ((d = this.collector) == null ? void 0 : d.getLocalId(l)) === $) {
        this.collector.isOwn(l) ? this.blurSelector(s, a) : this.blurSelector(s, a, l);
        return;
      }
      if (h = R.RemoveNode, o.toolsType === T.LaserPen)
        return;
    }
    const u = (y = this.viewContainerManager.getView(s)) == null ? void 0 : y.focusScenePath;
    if (u && u !== a)
      return;
    if ((w = this.collector) != null && w.isSelector(e)) {
      const v = (p = this.collector) == null ? void 0 : p.getUidFromKey(e);
      if (v && !this.control.roomMember.isOnLine(v))
        return;
    }
    if (h && l) {
      if (!this.control.renderControl.isFilterAble(
        { viewId: s, elementId: e, scenePath: a },
        h
      ))
        return;
      const v = c;
      if (v.workId = (f = this.collector) != null && f.isOwn(l) ? (N = this.collector) == null ? void 0 : N.getLocalId(l) : l, v.msgType = h, v.toolsType === T.LaserPen && (v.isRunSubWork = !0), v.toolsType === T.BackgroundSVG) {
        if (!this.control.isUseBackgroundThread)
          return;
        v.isRunBackgroundThread = !0;
      }
      if (v.dataType = q.Service, v.viewId = s, v.scenePath = a, v.useAnimation = this.smoothSync && ((S = v.updateNodeOpt) == null ? void 0 : S.useAnimation) || !1, v.selectIds && (v.selectIds = v.selectIds.map((A) => {
        var P, E;
        return (P = this.collector) != null && P.isOwn(A) ? (E = this.collector) == null ? void 0 : E.getLocalId(A) : A;
      })), v && v.toolsType === T.Text || (o == null ? void 0 : o.toolsType) === T.Text) {
        this.control.textEditorManager.onServiceDerive(v);
        return;
      }
      v.isEnableCursor = this.control.cursor.isEnableCursor, this.taskBatchData.add(v);
    }
    this.runAnimation();
    const g = t.newValue && ((C = t.newValue) == null ? void 0 : C.toolsType), M = t.newValue && ((k = t.newValue.opt) == null ? void 0 : k.zIndex);
    if (g && g === T.BackgroundSVG) {
      if (this.zIndexNodeMethod && ee(M)) {
        const v = this.zIndexNodeMethod.getMinZIndexForBackgroundSVG(s);
        this.zIndexNodeMethod.getMaxZIndexForBackgroundSVG(s) < M && this.zIndexNodeMethod.setMaxZIndexForBackgroundSVG(M, s), v > M && this.zIndexNodeMethod.setMinZIndexForBackgroundSVG(M, s);
      }
    } else if (this.zIndexNodeMethod && ee(M)) {
      const v = this.zIndexNodeMethod.getMinZIndex(s);
      this.zIndexNodeMethod.getMaxZIndex(s) < M && this.zIndexNodeMethod.setMaxZIndex(M, s), v > M && this.zIndexNodeMethod.setMinZIndex(M, s);
    }
  }
  pullServiceData(e, t, i = {
    isAsync: !1,
    useAnimation: !1
  }) {
    var c, l, h, u, g, M, d;
    let o = (c = this.collector) == null ? void 0 : c.getStorageData(e, t);
    const { isAsync: s, useAnimation: a } = i;
    if (o) {
      this.control.renderControl.isWritable && (o = this.correctStorage(o, e, t));
      const y = Object.keys(o);
      for (const w of y) {
        if ((l = this.collector) != null && l.isSelector(w)) {
          const f = (h = this.collector) == null ? void 0 : h.getUidFromKey(w);
          if (f && !this.control.roomMember.isOnLine(f))
            continue;
        }
        const p = (u = o[w]) == null ? void 0 : u.type;
        if (p && w) {
          if (!this.control.renderControl.isFilterAble(
            { viewId: e, elementId: w, scenePath: t },
            p
          ))
            continue;
          const f = o[w];
          if (f.workId = (g = this.collector) != null && g.isOwn(w) ? (M = this.collector) == null ? void 0 : M.getLocalId(w) : w, f.msgType = p, f.dataType = q.Service, f.viewId = e, f.scenePath = t, f.useAnimation = this.smoothSync && (f.ops ? !!a : (d = f.updateNodeOpt) == null ? void 0 : d.useAnimation) || !1, f.selectIds && (f.selectIds = f.selectIds.map((N) => {
            var S, C;
            return (S = this.collector) != null && S.isOwn(N) ? (C = this.collector) == null ? void 0 : C.getLocalId(N) : N;
          })), f.toolsType === T.Text) {
            this.control.textEditorManager.onServiceDerive(f);
            continue;
          }
          if (f.toolsType === T.BackgroundSVG) {
            if (!this.control.isUseBackgroundThread)
              continue;
            f.isRunBackgroundThread = !0;
          }
          s && f.updateNodeOpt && f.updateNodeOpt.useAnimation && (f.updateNodeOpt.useAnimation = !1), f.isEnableCursor = this.control.cursor.isEnableCursor, this.taskBatchData.add(f);
        }
        this.internalMsgEmitter.emit("excludeIds", y, e);
      }
      s ? this.consume() : this.runAnimation();
    }
  }
  runAnimation() {
    !this.animationId && !this.isBusy && (this.animationId = requestAnimationFrame(this.consume.bind(this)));
  }
  consume() {
    var t;
    this.animationId = void 0;
    const { viewId: e } = this.currentLocalWorkData;
    if (this.tasksqueue.size) {
      const i = this.consumeQueue(), o = !!i.size;
      if (this.taskBatchData.size)
        for (const s of this.taskBatchData.values())
          s.dataType === q.Service && (i.add(s), this.taskBatchData.delete(s));
      i.size ? (this.post(i), o && this.tasksqueue.clear()) : this.tasksqueue.size && (this.animationId = requestAnimationFrame(this.consume.bind(this)));
      return;
    }
    if (this.isAbled() && this.localPointsBatchData.size && e)
      for (const [
        i,
        { state: o, isFullWork: s, points: a, opt: c }
      ] of this.localPointsBatchData.entries()) {
        if (o === x.Done && !s || o === x.Start && !this.isCanStartEventConsum)
          continue;
        const l = a.map((h) => h);
        if (l.length) {
          if (this.isPostCountType && o === x.Doing) {
            if (this.postCount > this.consumeCount)
              continue;
            this.postCount++;
          }
          this.delayWorkStateToDoneResolve && o === x.Done && (this.delayWorkStateToDoneResolve(!0), this.setLockSentEventCursor(!0)), this.taskBatchData.add({
            op: l,
            workState: o,
            workId: i,
            dataType: q.Local,
            msgType: R.DrawWork,
            isRunSubWork: this.isRunSubWork,
            toolsType: (t = this.currentToolsData) == null ? void 0 : t.toolsType,
            viewId: e,
            scenePath: e && this.viewContainerManager.getCurScenePath(e),
            opt: c,
            smoothSync: this.smoothSync,
            isLockSentEventCursor: this.getLockSentEventCursor(),
            syncUnitTime: this.maxLastSyncTime,
            disableEraseImage: this.control.disableEraseImage,
            disableEraseText: this.control.disableEraseText,
            postCount: this.isPostCountType && this.postCount || void 0,
            fullWorkerDrawCount: this.isCanPostDraw && this.fullWorkerDrawCount || void 0,
            needUndoTicker: o === x.Done && this.viewContainerManager.getUndoTickerId(e) || void 0
          }), a.length = 0;
        }
      }
    this.taskBatchData.size && (this.post(this.taskBatchData), this.taskBatchData.clear()), this.taskBatchData.size && (this.animationId = requestAnimationFrame(this.consume.bind(this)));
  }
  unWritable() {
    this.setCurrentLocalWorkData({ workState: x.Unwritable }), this.setLockSentEventCursor(!0);
  }
  abled() {
    this.setCurrentLocalWorkData({ workState: x.Pending }), this.setLockSentEventCursor(!1);
  }
  isAbled() {
    return this.currentLocalWorkData.workState !== x.Unwritable;
  }
  postMainThread(e) {
    if (!this.control.hasOffscreenCanvas && this.mainThread) {
      this.mainThread.consume(e);
      return;
    }
  }
  postWebWorker(e) {
    if (this.control.isUseSimple) {
      const t = [...e].find(
        (i) => i.msgType === R.Init && i.offscreenCanvas
      );
      t != null && t.offscreenCanvas ? this.fullWorker.postMessage(e, [
        t.offscreenCanvas.bgCanvas,
        t.offscreenCanvas.localCanvas,
        t.offscreenCanvas.serviceCanvas
      ]) : this.fullWorker.postMessage(e);
      return;
    } else if (this.subWorker) {
      this.fullWorker.postMessage(e);
      const t = /* @__PURE__ */ new Set();
      for (const i of e.values()) {
        const o = i.msgType;
        (o === R.Init || o === R.Clear || o === R.Destroy || o === R.UpdateCamera || o === R.Console || i.isRunSubWork) && t.add(i);
      }
      t.size && this.subWorker.postMessage(t);
    }
  }
  post(e) {
    if (this.control.isUseBackgroundThread) {
      if (!this.backgroundThreadManager) {
        let i = [];
        i = se([...e]), setTimeout(() => {
          for (const o of i)
            this.taskBatchData.add(o);
        }, 0);
        return;
      }
      const t = /* @__PURE__ */ new Set();
      for (const i of e.values()) {
        const o = i.msgType;
        (o === R.Init || o === R.Clear || o === R.Destroy || o === R.Console || i.isRunBackgroundThread) && t.add(i);
      }
      t.size && this.backgroundThreadManager.consume(t);
    }
    if (!this.control.hasOffscreenCanvas) {
      if (!this.mainThread) {
        let t = [];
        t = se([...e]), setTimeout(() => {
          for (const i of t)
            this.taskBatchData.add(i);
        }, 0);
        return;
      }
      this.postMainThread(e);
      return;
    }
    this.postWebWorker(e);
  }
  updateNode(e, t, i, o) {
    this.taskBatchData.add({
      msgType: R.UpdateNode,
      workId: e,
      updateNodeOpt: t,
      viewId: i,
      scenePath: o,
      dataType: q.Local
    }), this.runAnimation();
  }
  destroyTaskQueue() {
    this.useTasksqueue = !1, this.useTasksClockId && (clearTimeout(this.useTasksClockId), this.useTasksClockId = void 0), this.mainTasksqueueCount = void 0, this.workerTasksqueueCount = void 0;
  }
  updateDpr(e, t) {
    this.queryTaskBatchData({
      msgType: R.UpdateDpr,
      dataType: q.Local,
      viewId: e
    }).forEach((i) => {
      this.taskBatchData.delete(i);
    }), this.taskBatchData.add({
      dataType: q.Local,
      msgType: R.UpdateDpr,
      viewId: e,
      dpr: t,
      isRunSubWork: !0
    }), this.runAnimation();
  }
  updateCamera(e, t) {
    var i;
    this.useTasksqueue || ((i = this.methodBuilder) == null || i.pause(), this.blurCursor(e), this.checkDrawingWork(e), this.useTasksqueue = !0, this.mainTasksqueueCount = 1, this.workerTasksqueueCount = 1), this.useTasksqueue && (this.tasksqueue.set(e, {
      msgType: R.UpdateCamera,
      dataType: q.Local,
      cameraOpt: {
        ...t,
        width: t.width,
        height: t.height
      },
      scenePath: this.viewContainerManager.getCurScenePath(e),
      isRunSubWork: !0,
      viewId: e
    }), this.consume(), this.useTasksClockId && clearTimeout(this.useTasksClockId), this.updateCameraDone());
  }
  updateCameraDone() {
    this.useTasksClockId = setTimeout(() => {
      var e;
      this.useTasksClockId = void 0, this.mainTasksqueueCount === this.workerTasksqueueCount ? (this.tasksqueue.size && this.consume(), this.useTasksqueue = !1, this.mainTasksqueueCount = void 0, this.workerTasksqueueCount = void 0, (e = this.methodBuilder) == null || e.recover(), this.runAnimation()) : this.updateCameraDone();
    }, this.maxLastSyncTime);
  }
  consumeQueue() {
    const e = /* @__PURE__ */ new Set();
    let t = !this.isBusy;
    if (this.isBusy && this.mainTasksqueueCount && this.workerTasksqueueCount && this.mainTasksqueueCount <= this.workerTasksqueueCount && (t = !0), t) {
      this.mainTasksqueueCount && this.workerTasksqueueCount && this.mainTasksqueueCount++, e.add({
        msgType: R.TasksQueue,
        dataType: q.Local,
        isRunSubWork: !0,
        isRunBackgroundThread: this.control.isUseBackgroundThread,
        mainTasksqueueCount: this.mainTasksqueueCount,
        tasksqueue: this.tasksqueue,
        viewId: ""
      });
      for (const [i, o] of this.tasksqueue.entries())
        o.cameraOpt && this.control.textEditorManager.onCameraChange(
          o.cameraOpt,
          i
        );
    }
    return e;
  }
  async clearViewScenePath(e, t, i) {
    var s, a;
    const o = this.viewContainerManager.getCurScenePath(e);
    if (i) {
      const c = (s = this.collector) == null ? void 0 : s.uid;
      if (c && !this.control.renderControl.isClearAble({ uid: c, viewId: e, scenePath: o }))
        return;
    }
    this.control.textEditorManager.clear(e, t), this.queryTaskBatchData({
      msgType: R.Clear,
      dataType: q.Local,
      viewId: e
    }).forEach((c) => {
      this.taskBatchData.delete(c);
    }), this.taskBatchData.add({
      dataType: q.Local,
      msgType: R.Clear,
      viewId: e
    }), this.runAnimation(), t || (this.control.renderControl.isUsePageAuth(e) && o ? this.control.renderControl.clearRenderPage(e, o) : (a = this.collector) == null || a.dispatch({
      type: R.Clear,
      viewId: e,
      scenePath: o
    })), this.zIndexNodeMethod && this.zIndexNodeMethod.clearZIndex(e);
    for (const [c, l] of this.localPointsBatchData.entries())
      l.viewId === e && this.localPointsBatchData.delete(c);
    await new Promise((c) => {
      const l = this.clearAllResolveMap.get(e) || {
        resolve: void 0,
        timer: void 0
      };
      l.timer && clearTimeout(l.timer), l.resolve = c, l.timer = setTimeout(() => {
        const h = this.clearAllResolveMap.get(e);
        h != null && h.resolve && h.resolve(e);
      }, this.maxLastSyncTime), this.clearAllResolveMap.set(e, l);
    }).then((c) => {
      this.clearAllResolveMap.delete(c);
    });
  }
  internalMsgEmitterListener() {
    var e;
    this.methodBuilder = new re([
      W.CopyNode,
      W.SetColorNode,
      W.DeleteNode,
      W.RotateNode,
      W.ScaleNode,
      W.TranslateNode,
      W.ZIndexNode,
      W.SetFontStyle,
      W.SetPoint,
      W.SetLock,
      W.SetShapeOpt
    ]).registerForMainEngine(te.MainEngine, this.control), this.zIndexNodeMethod = (e = this.methodBuilder) == null ? void 0 : e.getBuilder(
      W.ZIndexNode
    );
  }
  setZIndex(e) {
    const t = this.currentToolsData && se(this.currentToolsData.toolsOpt);
    return t && this.zIndexNodeMethod && this.isUseZIndex && (this.zIndexNodeMethod.addMaxLayer(e), t.zIndex = this.zIndexNodeMethod.getMaxZIndex(e)), t;
  }
  checkDrawingWork(e) {
    var o, s, a;
    let t = !1;
    const i = [];
    for (const [
      c,
      { state: l, viewId: h, points: u, opt: g }
    ] of this.localPointsBatchData.entries())
      if (e === h && l === x.Start || l === x.Doing) {
        if (l === x.Doing && this.isCanDrawWork) {
          Number(c) && Number(c) + 60 > this.now && (i.push(c.toString()), this.taskBatchData.add({
            msgType: R.RemoveNode,
            workId: c,
            viewId: h,
            dataType: q.Local,
            isRunSubWork: !0
          }));
          const M = u.map((d) => d);
          this.taskBatchData.add({
            op: M,
            workState: x.Done,
            workId: c,
            dataType: q.Local,
            msgType: R.DrawWork,
            isRunSubWork: this.isRunSubWork,
            toolsType: (o = this.currentToolsData) == null ? void 0 : o.toolsType,
            viewId: h,
            opt: g,
            scenePath: h && this.viewContainerManager.getCurScenePath(h)
          }), t = !0;
        }
        this.deleteLocalPoint(c);
      }
    if ((t || i.length) && (this.consume(), i.length)) {
      const c = (s = this.viewContainerManager.getView(e)) == null ? void 0 : s.focusScenePath;
      (a = this.collector) == null || a.dispatch({
        type: R.RemoveNode,
        removeIds: i,
        viewId: e,
        scenePath: c
      });
    }
  }
  removeDrawingWork(e) {
    var i, o;
    const t = [];
    for (const [
      s,
      { state: a, viewId: c }
    ] of this.localPointsBatchData.entries())
      (e === c && a === x.Start || a === x.Doing) && (this.deleteLocalPoint(s), a === x.Doing && this.isCanDrawWork && (t.push(s.toString()), this.taskBatchData.add({
        msgType: R.RemoveNode,
        workId: s,
        viewId: c,
        dataType: q.Local,
        isRunSubWork: !0
      })));
    if (t.length) {
      this.consume();
      const s = (i = this.viewContainerManager.getView(e)) == null ? void 0 : i.focusScenePath;
      (o = this.collector) == null || o.dispatch({
        type: R.RemoveNode,
        removeIds: t,
        viewId: e,
        scenePath: s
      });
    }
  }
  removeNodes(e, t) {
    var o, s;
    const i = (o = this.viewContainerManager.getView(e)) == null ? void 0 : o.focusScenePath;
    if (i && t.length) {
      for (const a of t)
        this.taskBatchData.add({
          msgType: R.RemoveNode,
          workId: a,
          viewId: e,
          dataType: q.Local,
          isRunSubWork: !1
        });
      this.consume(), (s = this.collector) == null || s.dispatch({
        type: R.RemoveNode,
        removeIds: t,
        viewId: e,
        scenePath: i
      });
    }
  }
  hoverCursor(e, t) {
    var i;
    if (((i = this.currentToolsData) == null ? void 0 : i.toolsType) === T.Selector && !this.isBusy) {
      const o = this.viewContainerManager.getView(t);
      if (o && o.displayer && o.displayer.vDom) {
        const c = o.displayer.vDom.state.floatBarData;
        if (c) {
          const { x: l, y: h, w: u, h: g } = c;
          if (PM(e, { x: l, y: h, w: u, h: g }))
            return;
        }
      }
      const s = this.viewContainerManager.transformToScenePoint(e, t), a = {
        msgType: R.CursorHover,
        dataType: q.Local,
        point: s,
        toolsType: this.currentToolsData.toolsType,
        opt: this.currentToolsData.toolsOpt,
        isRunSubWork: !1,
        viewId: t
      };
      this.queryTaskBatchData({
        msgType: R.CursorHover,
        dataType: q.Local,
        toolsType: this.currentToolsData.toolsType,
        viewId: t
      }).forEach((c) => {
        this.taskBatchData.delete(c);
      }), this.taskBatchData.add(a), this.runAnimation();
    }
  }
  blurCursor(e) {
    var i;
    if (((i = this.currentToolsData) == null ? void 0 : i.toolsType) !== T.Selector)
      return;
    const t = {
      msgType: R.CursorBlur,
      dataType: q.Local,
      isRunSubWork: !1,
      viewId: e
    };
    this.queryTaskBatchData({
      msgType: R.CursorHover,
      dataType: q.Local,
      viewId: e
    }).forEach((o) => {
      this.taskBatchData.delete(o);
    }), this.taskBatchData.add(t), this.consume();
  }
  sendCursorEvent(e, t) {
    var c, l, h;
    if (!this.currentLocalWorkData || this.currentLocalWorkData.workState === x.Unwritable || this.isBusy)
      return;
    const i = (c = this.currentToolsData) == null ? void 0 : c.toolsType;
    if (!this.useTasksqueue && this.workState === x.Pending && i === T.Selector && (ee(e[0]) && ee(e[1]) ? this.hoverCursor(e, t) : e[0] === void 0 && e[1] === void 0 && this.blurCursor(t)), !this.currentToolsData || !this.isCanSentCursor || !this.control.pluginOptions.extras.cursor.enable)
      return;
    const o = (l = this.collector) == null ? void 0 : l.uid, s = this.viewContainerManager.getCurScenePath(t);
    switch ((h = this.currentToolsData) == null ? void 0 : h.toolsType) {
      case T.Eraser:
      case T.PencilEraser:
      case T.BitMapEraser: {
        if (o && !this.control.renderControl.isClearAble({
          uid: o,
          viewId: t,
          filterLocalSelf: !1,
          scenePath: s
        }))
          return;
        break;
      }
      default:
        if (o && !this.control.renderControl.isWritable || o && !this.control.renderControl.isRenderAble({
          uid: o,
          viewId: t,
          filterLocalSelf: !1,
          scenePath: s
        }))
          return;
        break;
    }
    let a = [
      void 0,
      void 0
    ];
    this.currentToolsData && this.isCanSentCursor && !this.localPointsBatchData.size && !this.getLockSentEventCursor() && (a = e, this.control.cursor.sendEvent(a, t));
  }
  getBoundingRect(e) {
    var i, o;
    if (!((i = this.boundingRectMap) == null ? void 0 : i.get(e))) {
      const s = (o = this.collector) == null ? void 0 : o.getScenePathData(e);
      if (!s)
        return;
      if (Object.keys(s).forEach((a) => {
        var c;
        ((c = this.collector) == null ? void 0 : c.getLocalId(a)) === $ && delete s[a];
      }), Object.keys(s).length && this.viewContainerManager.mainView && this.viewContainerManager.mainView.cameraOpt) {
        const a = {
          msgType: R.BoundingBox,
          dataType: q.Local,
          scenePath: e,
          scenes: s,
          cameraOpt: { ...this.viewContainerManager.mainView.cameraOpt },
          isRunSubWork: !0,
          viewId: this.viewContainerManager.mainView.id
        };
        return this.taskBatchData.add(a), this.runAnimation(), new Promise((c) => {
          this.boundingRectMap.set(e, c);
        }).then((c) => (this.boundingRectMap.delete(e), c));
      }
    }
  }
  getSnapshot(e, t, i, o) {
    var a, c, l, h, u;
    if (!((a = this.snapshotMap) == null ? void 0 : a.get(e))) {
      const g = (c = this.collector) == null ? void 0 : c.getViewIdBySecenPath(e);
      if (!g)
        return;
      const M = (l = this.collector) == null ? void 0 : l.getStorageData(g, e);
      if (!M)
        return;
      if (Object.keys(M).forEach((d) => {
        var y;
        ((y = this.collector) == null ? void 0 : y.getLocalId(d)) === $ && delete M[d];
      }), Object.keys(M).length) {
        const d = this.viewContainerManager.mainView;
        if (!d)
          return;
        const y = t || ((h = d.cameraOpt) == null ? void 0 : h.width), w = i || ((u = d.cameraOpt) == null ? void 0 : u.height), p = {
          msgType: R.Snapshot,
          dataType: q.Local,
          scenePath: e,
          scenes: M,
          w: y,
          h: w,
          cameraOpt: o && {
            ...o,
            width: y,
            height: w
          } || d.cameraOpt,
          isRunSubWork: !0,
          isRunBackgroundThread: this.control.isUseBackgroundThread,
          viewId: g
        };
        return this.taskBatchData.add(p), this.runAnimation(), new Promise(
          (f) => {
            this.snapshotMap.set(e, {
              resolve: f,
              value: this.control.isUseBackgroundThread ? [void 0, void 0] : [void 0]
            });
          }
        ).then((f) => (this.snapshotMap.delete(e), f.reverse()));
      }
    }
  }
  queryTaskBatchData(e) {
    const t = [];
    if (e)
      for (const i of this.taskBatchData.values()) {
        let o = !0;
        for (const [s, a] of Object.entries(e))
          if (i[s] !== a) {
            o = !1;
            break;
          }
        o && t.push(i);
      }
    return t;
  }
  insertIconify(e, t) {
    var a;
    const i = this.viewContainerManager.getView(e);
    if (!i)
      throw new Error("insertIconify viewId is not exist");
    if (!t.src)
      throw new Error("iconify src must be required");
    if (!/^(http|https):\/\//.test(t.src))
      throw new Error("iconify src must be http or https");
    t.strokeColor || (t.strokeColor = (a = this.currentLocalWorkData.toolsOpt) == null ? void 0 : a.strokeColor);
    const s = i == null ? void 0 : i.focusScenePath;
    if (e && s) {
      const c = this.now;
      ke.InternalMsgEmitter.emit(
        "addUndoTicker",
        c,
        e
      );
      const l = { ...t, type: Zt.Iconify };
      this.zIndexNodeMethod && (this.zIndexNodeMethod.addMaxLayer(e), l.zIndex = this.zIndexNodeMethod.getMaxZIndex(e)), this.taskBatchData.add({
        msgType: R.FullWork,
        dataType: q.Local,
        toolsType: T.Image,
        workId: t.uuid,
        opt: l,
        viewId: e,
        willRefresh: !0,
        willSyncService: !0,
        needUndoTicker: c
      }), this.runAnimation();
    }
  }
  insertBackgroundSVG(e, t) {
    if (!this.control.isUseBackgroundThread)
      throw new Error("useBackgroundThread of pluginOptions should be true");
    const i = this.viewContainerManager.getView(e);
    if (!i)
      throw new Error("insertBackgroundSVG viewId is not exist");
    if (!t.src)
      throw new Error("src must be required");
    if (t.src && !/^(http|https):\/\//.test(t.src))
      throw new Error("iconify src must be http or https");
    const o = i == null ? void 0 : i.focusScenePath;
    if (e && o) {
      const s = this.now;
      ke.InternalMsgEmitter.emit(
        "addUndoTicker",
        s,
        e
      );
      const a = { ...t, type: $t.url };
      this.zIndexNodeMethod && (this.zIndexNodeMethod.addMaxLayerForBackgroundSVG(e), a.zIndex = this.zIndexNodeMethod.getMaxZIndexForBackgroundSVG(e)), this.taskBatchData.add({
        msgType: R.FullWork,
        dataType: q.Local,
        toolsType: T.BackgroundSVG,
        workId: t.uuid,
        opt: a,
        viewId: e,
        willRefresh: !0,
        willSyncService: !0,
        needUndoTicker: s,
        isRunBackgroundThread: !0
      }), this.runAnimation();
    }
  }
  async insertBackgroundMarkMap(e, t) {
    var s;
    if (!this.control.isUseBackgroundThread)
      throw new Error("useBackgroundThread of pluginOptions should be true");
    const i = this.viewContainerManager.getView(e);
    if (!i)
      throw new Error("instertBackgroundMarkMap viewId is not exist");
    if (!t.data)
      throw new Error("data must be required");
    if (t.data && !OM(t.data))
      throw new Error("data must be markdown string");
    const o = i == null ? void 0 : i.focusScenePath;
    if (e && o) {
      const a = t.uuid.toString();
      if ((s = this.collector) != null && s.hasId(e, o, a))
        throw new Error(
          "instertBackgroundMarkMap svgInfo.uuid is already exist"
        );
      if (this.markmapIdResolveMap.has(a))
        throw new Error(
          "instertBackgroundMarkMap svgInfo.uuid is already resolving"
        );
      const c = this.now;
      ke.InternalMsgEmitter.emit(
        "addUndoTicker",
        c,
        e
      );
      const l = { ...t, type: $t.markmap };
      return this.zIndexNodeMethod && (this.zIndexNodeMethod.addMaxLayerForBackgroundSVG(e), l.zIndex = this.zIndexNodeMethod.getMaxZIndexForBackgroundSVG(e)), this.taskBatchData.add({
        msgType: R.FullWork,
        dataType: q.Local,
        toolsType: T.BackgroundSVG,
        workId: a,
        opt: l,
        viewId: e,
        willRefresh: !0,
        willSyncService: !0,
        needUndoTicker: c,
        isRunBackgroundThread: !0
      }), this.runAnimation(), new Promise((h, u) => {
        this.markmapIdResolveMap.set(a, {
          resolve: h,
          timer: setTimeout(() => {
            const g = this.markmapIdResolveMap.get(a);
            g != null && g.timer && (g.timer = void 0), u(a);
          }, this.control.pluginOptions.extras.markmap.timeout)
        });
      }).then((h) => {
        if (h)
          return this.markmapIdResolveMap.delete(h), h;
      }).catch((h) => {
        throw this.markmapIdResolveMap.delete(h), new Error(
          `instertBackgroundMarkMap svgInfo.uuid ${h} insert timeout`
        );
      });
    }
  }
  async updateBackgroundMarkMap(e, t, i) {
    var l;
    const o = this.viewContainerManager.getView(e);
    if (!o)
      throw new Error("updateBackgroundMarkMap viewId is not exist");
    const s = o == null ? void 0 : o.focusScenePath;
    if (!s)
      throw new Error("updateBackgroundMarkMap scenePath is not exist");
    if ((l = this.collector) != null && l.hasId(e, s, t))
      throw new Error("instertBackgroundMarkMap svgInfo.uuid is already exist");
    if (this.markmapIdResolveMap.has(t))
      throw new Error(
        "instertBackgroundMarkMap svgInfo.uuid is already resolving"
      );
    const a = this.now;
    ke.InternalMsgEmitter.emit(
      "addUndoTicker",
      a,
      e
    );
    const c = { ...i, type: $t.markmap };
    this.taskBatchData.add({
      msgType: R.UpdateNode,
      dataType: q.Local,
      toolsType: T.BackgroundSVG,
      workId: t,
      opt: c,
      viewId: e,
      willRefresh: !0,
      willSyncService: !0,
      needUndoTicker: a,
      isRunBackgroundThread: !0
    }), this.runAnimation();
  }
  insertImage(e, t) {
    const i = this.viewContainerManager.getView(e);
    if (!i)
      throw new Error("insertImage viewId is not exist");
    if (t.src && !/^(http|https):\/\//.test(t.src))
      throw new Error("Image src must be http or https");
    const o = i == null ? void 0 : i.focusScenePath;
    if (e && o) {
      const { src: s, uuid: a } = t;
      if (a && !s) {
        this.tmpImageConfigMap.set(a, t);
        return;
      }
      const c = this.now;
      ke.InternalMsgEmitter.emit(
        "addUndoTicker",
        c,
        e
      );
      const l = { ...t, type: Zt.Image };
      this.zIndexNodeMethod && (this.zIndexNodeMethod.addMaxLayer(e), l.zIndex = this.zIndexNodeMethod.getMaxZIndex(e)), this.taskBatchData.add({
        msgType: R.FullWork,
        dataType: q.Local,
        toolsType: T.Image,
        workId: t.uuid,
        opt: l,
        viewId: e,
        willRefresh: !0,
        willSyncService: !0,
        needUndoTicker: c
      }), this.runAnimation();
    }
  }
  lockImage(e, t, i) {
    var a, c;
    const o = this.viewContainerManager.getView(e);
    if (!o)
      throw new Error("insertImage viewId is not exist");
    const s = o == null ? void 0 : o.focusScenePath;
    if (e && s && this.collector) {
      const l = this.collector.getStorageData(e, s);
      if (!l)
        return;
      for (const [h, u] of Object.entries(l))
        if (u && u.toolsType === T.Image && u.opt.uuid === t) {
          const g = this.now;
          ke.InternalMsgEmitter.emit(
            "addUndoTicker",
            g,
            e
          );
          const M = (a = this.collector) != null && a.isOwn(h) ? (c = this.collector) == null ? void 0 : c.getLocalId(h) : h, d = {
            ...u.opt,
            locked: i,
            type: Zt.Image
          };
          this.taskBatchData.add({
            msgType: R.FullWork,
            dataType: q.Local,
            toolsType: T.Image,
            workId: M,
            opt: d,
            viewId: e,
            willRefresh: !0,
            willSyncService: !0,
            needUndoTicker: g
          }), this.runAnimation();
          return;
        }
    }
  }
  completeImageUpload(e, t, i) {
    const o = this.viewContainerManager.getView(e);
    if (!o)
      throw new Error("insertImage viewId is not exist");
    const s = o == null ? void 0 : o.focusScenePath;
    if (e && s) {
      const a = this.tmpImageConfigMap.get(t);
      a && (a.src = i, this.insertImage(e, a), this.tmpImageConfigMap.delete(t));
      return;
    }
  }
  getImagesInformation(e) {
    const t = [];
    if (this.collector) {
      const i = this.collector.getScenePathData(e);
      if (!i)
        return t;
      for (const o of Object.values(i))
        if (o && o.toolsType === T.Image) {
          const s = o.opt;
          t.push({
            uuid: s.uuid,
            centerX: s.centerX,
            centerY: s.centerY,
            width: s.width,
            height: s.height,
            locked: s.locked,
            uniformScale: s.uniformScale,
            crossOrigin: s.crossOrigin
          });
        }
    }
    return t;
  }
  setShapeSelectorByWorkId(e, t) {
    this.taskBatchData.add({
      workId: $,
      selectIds: [e],
      msgType: R.Select,
      dataType: q.Local,
      viewId: t,
      willSyncService: !0
    }), this.runAnimation();
  }
  blurSelector(e, t, i) {
    this.taskBatchData.add({
      workId: i || $,
      selectIds: [],
      msgType: R.Select,
      dataType: q.Service,
      viewId: e,
      scenePath: t
    }), this.runAnimation();
  }
  consoleWorkerInfo() {
    this.taskBatchData.add({
      msgType: R.Console,
      dataType: q.Local,
      isRunSubWork: !0,
      viewId: ""
    }), this.consume();
  }
  sendClearUids(e, t) {
    var o, s;
    const i = /* @__PURE__ */ new Set();
    i.add({
      msgType: R.AuthClear,
      dataType: q.Local,
      viewId: e,
      clearUids: t,
      // scenePath: this.viewContainerManager.getCurScenePath(viewId),
      localUid: (o = this.collector) == null ? void 0 : o.uid
    }), this.control.hasOffscreenCanvas ? this.fullWorker.postMessage(i) : (s = this.mainThread) == null || s.consume(i);
  }
  async getVNodeInfo(e, t, i) {
    if (!this.getVNodeInfoResolveMap.has(e))
      return this.taskBatchData.add({
        msgType: R.GetVNodeInfo,
        dataType: q.Local,
        isRunSubWork: !0,
        viewId: t,
        workIds: i,
        workId: e
      }), this.consume(), await new Promise(
        (o) => {
          const s = this.getVNodeInfoResolveMap.get(e) || {
            resolve: void 0,
            timer: void 0
          };
          s.timer && clearTimeout(s.timer), s.resolve = o, s.timer = setTimeout(() => {
            const a = this.getVNodeInfoResolveMap.get(e);
            a != null && a.resolve && a.resolve({ uuid: e });
          }, this.maxLastSyncTime * 5), this.getVNodeInfoResolveMap.set(e, s);
        }
      ).then((o) => {
        const { uuid: s } = o;
        return this.getVNodeInfoResolveMap.delete(s), o;
      });
  }
}
class tp {
  constructor(n) {
    I(this, "internalMsgEmitter");
    I(this, "control");
    I(this, "roomHotkeyCheckers");
    I(this, "tmpCopyStore", /* @__PURE__ */ new Map());
    I(this, "tmpCopyCoordInfo");
    var i;
    const { control: e, internalMsgEmitter: t } = n;
    this.control = e, this.internalMsgEmitter = t, this.roomHotkeyCheckers = ((i = this.control.room) == null ? void 0 : i.viewsParams.hotKeys.nodes) || [];
  }
  get isUseSelf() {
    var n, e;
    return ((e = (n = this.control) == null ? void 0 : n.room) == null ? void 0 : e.disableDeviceInputs) || !1;
  }
  get isSelector() {
    var n;
    return ((n = this.control.worker.currentToolsData) == null ? void 0 : n.toolsType) === T.Selector;
  }
  get collector() {
    return this.control.collector;
  }
  get mainEngine() {
    return this.control.worker;
  }
  get keyboardKind() {
    return /^Mac/i.test(navigator.platform) ? "mac" : "windows";
  }
  getEventKey(n) {
    switch (n.type) {
      case "keydown":
        return "KeyDown";
    }
    return "KeyUp";
  }
  onActiveHotkey(n) {
    var i, o, s;
    const e = this.control.viewContainerManager.focuedViewId, t = (i = this.control.viewContainerManager.focuedView) == null ? void 0 : i.focusScenePath;
    if (e && t)
      switch (n) {
        case "delete":
          this.isSelector && ((o = this.collector) != null && o.hasSelector(e, t)) && re.emitMethod(
            te.MainEngine,
            W.DeleteNode,
            { workIds: [$], viewId: e }
          );
          break;
        case "copy":
          this.isSelector && ((s = this.collector) != null && s.hasSelector(e, t)) && this.copySelectorToTemp(e, t);
          break;
        case "paste":
          this.tmpCopyStore.size && this.pasteTempToFocusView(e, t);
          break;
      }
    (n === "changeToPencil" || n === "redo" || n === "undo") && this.onSelfActiveHotkey(n);
  }
  colloctHotkey(n) {
    if (this.isUseSelf) {
      const e = this.checkHotkey(n);
      e && this.onSelfActiveHotkey(e);
    }
  }
  onSelfActiveHotkey(n) {
    switch (n) {
      case "changeToPencil":
        this.setMemberState({
          currentApplianceName: ne.pencil,
          useNewPencil: !0
        });
        break;
      case "changeToArrow":
        this.setMemberState({ currentApplianceName: ne.arrow });
        break;
      case "changeToClick":
        this.setMemberState({ currentApplianceName: ne.clicker });
        break;
      case "changeToEllipse":
        this.setMemberState({ currentApplianceName: ne.ellipse });
        break;
      case "changeToEraser":
        this.setMemberState({ currentApplianceName: ne.eraser });
        break;
      case "changeToHand":
        this.setMemberState({ currentApplianceName: ne.hand });
        break;
      case "changeToLaserPointer":
        this.setMemberState({
          currentApplianceName: ne.laserPointer
        });
        break;
      case "changeToSelector":
        this.setMemberState({ currentApplianceName: ne.selector });
        break;
      case "changeToRectangle":
        this.setMemberState({ currentApplianceName: ne.rectangle });
        break;
      case "changeToStraight":
        this.setMemberState({ currentApplianceName: ne.straight });
        break;
      case "redo":
        this.control.room && !this.control.room.disableSerialization && this.control.viewContainerManager.redo();
        break;
      case "undo":
        this.control.room && !this.control.room.disableSerialization && this.control.viewContainerManager.undo();
        break;
      case "changeToText":
        this.setMemberState({ currentApplianceName: ne.text });
        break;
      case "changeToPencilEraser":
        this.setMemberState({
          currentApplianceName: ne.pencilEraser
        });
        break;
      case "delete":
      case "copy":
      case "paste":
        this.onActiveHotkey(n);
        break;
    }
  }
  checkHotkey(n) {
    for (const e of this.roomHotkeyCheckers) {
      const { kind: t, checker: i } = e;
      if (i(
        {
          nativeEvent: n,
          kind: this.getEventKey(n),
          key: n.key,
          altKey: n.altKey,
          ctrlKey: n.ctrlKey,
          shiftKey: n.shiftKey
        },
        this.keyboardKind
      ))
        return t;
    }
  }
  copySelectorToTemp(n, e) {
    var l, h;
    const t = this.control.viewContainerManager.getView(n), i = (h = (l = this.mainEngine) == null ? void 0 : l.methodBuilder) == null ? void 0 : h.getBuilder(
      W.CopyNode
    );
    if (!t || !this.collector || !i)
      return;
    const o = this.collector.transformKey($), s = this.collector.getStorageData(
      n,
      e
    );
    if (!s)
      return;
    const a = s[o], c = a && i.copySelector({
      viewId: n,
      store: a
    });
    c && (this.tmpCopyCoordInfo = c == null ? void 0 : c.copyCoordInfo, this.tmpCopyStore = c == null ? void 0 : c.copyStores);
  }
  pasteTempToFocusView(n, e) {
    var o, s;
    const t = this.control.viewContainerManager.getView(n), i = (s = (o = this.mainEngine) == null ? void 0 : o.methodBuilder) == null ? void 0 : s.getBuilder(
      W.CopyNode
    );
    if (!(!t || !this.tmpCopyCoordInfo || !this.tmpCopyStore.size || !this.collector || !i) && t.viewData && this.tmpCopyCoordInfo) {
      const a = se(this.tmpCopyCoordInfo.offset), c = this.tmpCopyCoordInfo.cameraOpt, l = t.viewData.camera;
      a.x = a.x + l.centerX - c.centerX, a.y = a.y + l.centerY - c.centerY;
      const h = this.control.worker.now;
      this.control.worker.internalMsgEmitter.emit(
        "addUndoTicker",
        h,
        n
      ), i.pasteSelector({
        viewId: n,
        scenePath: e,
        copyStores: se(this.tmpCopyStore),
        copyCoordInfo: {
          offset: a,
          cameraOpt: l
        },
        undoTickerId: h
      });
    }
  }
  setMemberState(n) {
    var e;
    (e = this.control.room) == null || e.setMemberState(n);
  }
}
class np {
  constructor(n) {
    I(this, "control");
    I(this, "roomMemberManager");
    I(this, "worker");
    I(this, "publicMsgEmitter");
    I(this, "renderUids", /* @__PURE__ */ new Map());
    I(this, "pageAuth", /* @__PURE__ */ new Map());
    I(this, "isLocalRender", !1);
    I(this, "isCurWritable", !1);
    var i;
    const { control: e, publicMsgEmitter: t } = n;
    this.control = e, this.publicMsgEmitter = t, this.roomMemberManager = e.roomMember, this.worker = e.worker, this.isCurWritable = ((i = this.control.room) == null ? void 0 : i.isWritable) || !1;
  }
  get isWritable() {
    return this.isCurWritable;
  }
  onWritableChange(n) {
    this.isCurWritable = n;
  }
  addPage(n, e = !0) {
    var l, h;
    const {
      viewId: t,
      render: i = "localSelf",
      elementIds: o,
      pageId: s = (l = this.control.collector) == null ? void 0 : l.uid
    } = n, a = this.pageAuth.get(t) || /* @__PURE__ */ new Map(), c = this.control.viewContainerManager.getCurScenePath(t);
    if (c && s) {
      const u = a.get(
        c
      ) || {
        render: "localSelf",
        pages: /* @__PURE__ */ new Map()
      };
      e && (u.render = i);
      const g = o && this.cloneElementIds(t, c, o, s) || /* @__PURE__ */ new Set();
      u.pages.set(s, g), a.set(c, u), this.pageAuth.set(t, a), e && this.pageAuthEffect(t), this.isLocalRender = !1, (h = this.control.collector) == null || h.dispatchPageAuth(
        {
          operation: be.addPage,
          viewId: t,
          scenePath: c,
          pageId: s,
          pageData: {
            render: i,
            elementIds: g
          }
        },
        !0
      );
    }
  }
  delPage(n, e) {
    var s, a;
    const t = this.pageAuth.get(e);
    if (!t)
      return;
    const i = [...t.keys()].find((c) => {
      const l = t.get(c);
      return !!(l && l.pages.has(n));
    });
    if (!i)
      return;
    const o = t.get(i);
    if (o) {
      const { render: c, pages: l } = o, h = c === "localSelf" ? (s = this.control.collector) == null ? void 0 : s.uid : c, u = l.get(n);
      u != null && u.size && this.delElementIds(e, i, u), l.delete(n), h === n && this.pageAuthEffect(e);
    }
    (a = this.control.collector) == null || a.dispatchPageAuth(
      {
        operation: be.deletePage,
        viewId: e,
        scenePath: i,
        pageId: n
      },
      !0
    );
  }
  delViewPage(n, e = !0) {
    var i;
    this.pageAuth.get(n) && (this.pageAuth.delete(n), e && ((i = this.control.collector) == null || i.dispatchPageAuth(
      {
        operation: be.clearView,
        viewId: n
      },
      e
    )));
  }
  clearRenderPage(n, e, t = !0) {
    var a, c;
    const i = this.pageAuth.get(n);
    if (!i)
      return;
    const o = i.get(e);
    let s;
    if (o) {
      const { render: l, pages: h } = o, u = l === "localSelf" ? (a = this.control.collector) == null ? void 0 : a.uid : l;
      if (u) {
        if (t) {
          const g = h.get(u);
          g && this.delElementIds(n, e, g);
        }
        h.set(u, /* @__PURE__ */ new Set()), s = u;
      }
    }
    t && s && ((c = this.control.collector) == null || c.dispatchPageAuth(
      {
        operation: be.clearPage,
        viewId: n,
        scenePath: e,
        pageId: s
      },
      t
    ));
  }
  hasPage(n, e, t) {
    const i = this.pageAuth.get(e);
    if (!i)
      return !1;
    const o = i.get(t);
    return o ? o.pages.has(n) : !1;
  }
  getPageInfo(n, e, t) {
    const i = this.pageAuth.get(e);
    if (!i)
      return;
    const o = i.get(t);
    if (o)
      return o.pages.get(n);
  }
  addRenderPageElementId(n, e = !0, t = !1) {
    var h, u;
    const { viewId: i, elementId: o, scenePath: s } = n, a = this.pageAuth.get(i);
    if (!a)
      throw new Error(`no view info in viewId: ${i}`);
    const c = a.get(s);
    let l;
    if (c) {
      const { render: g, pages: M } = c, d = g === "localSelf" ? (h = this.control.collector) == null ? void 0 : h.uid : g;
      if (d && M.has(d)) {
        const y = M.get(d) || /* @__PURE__ */ new Set();
        y.add(o), M.set(d, y), l = d;
      }
    }
    t && this.pageAuthEffect(i), e && l && ((u = this.control.collector) == null || u.dispatchPageAuth(
      {
        operation: be.addElement,
        viewId: i,
        scenePath: s,
        pageId: l,
        pageData: {
          elementId: o
        }
      },
      e
    ));
  }
  delRenderPageElementId(n, e = !0, t = !1) {
    var h, u;
    const { viewId: i, elementId: o, scenePath: s } = n, a = this.pageAuth.get(i);
    if (!a)
      throw new Error(`no view info in viewId: ${i}`);
    const c = a.get(s);
    let l;
    if (c) {
      const { render: g, pages: M } = c, d = g === "localSelf" ? (h = this.control.collector) == null ? void 0 : h.uid : g;
      if (d && M.has(d)) {
        const y = M.get(d);
        y && (y.delete(o), M.set(d, y), l = d);
      }
    }
    t && this.pageAuthEffect(i), e && l && ((u = this.control.collector) == null || u.dispatchPageAuth(
      {
        operation: be.deleteElement,
        viewId: i,
        scenePath: s,
        pageId: l,
        pageData: {
          elementId: o
        }
      },
      e
    ));
  }
  setPageRender(n, e, t, i = !0) {
    var a;
    const o = this.pageAuth.get(n);
    if (!o)
      throw new Error(`no view info in viewId: ${n}`);
    const s = o.get(e);
    s && (s.render = t), e === this.control.viewContainerManager.getCurScenePath(n) && this.pageAuthEffect(n), i || (this.isLocalRender = !0), i && (this.isLocalRender = !1, (a = this.control.collector) == null || a.dispatchPageAuth(
      {
        operation: be.renderPage,
        viewId: n,
        scenePath: e,
        pageData: {
          render: t
        }
      },
      i
    ));
  }
  isRenderAble(n) {
    var c, l;
    const { uid: e, viewId: t, filterLocalSelf: i = !0, scenePath: o } = n, s = this.renderUids.get(t);
    if (s) {
      const { render: h, hide: u } = s;
      if (h === void 0 && u === void 0)
        return !0;
      if (Be(h)) {
        if (h.has(e) || i && ((c = this.control.collector) == null ? void 0 : c.uid) === e && h.has("localSelf"))
          return !0;
      } else if (h === !0 && (!u || Be(u) && !u.has(e) && !(i && u.has("localSelf") && ((l = this.control.collector) == null ? void 0 : l.uid) === e)))
        return !0;
      return !1;
    }
    const a = this.pageAuth.get(t);
    if (a && o) {
      const h = a.get(o);
      if (h) {
        const { render: u } = h;
        if (u)
          return !(u === "localSelf" && !i);
      }
    }
    return !0;
  }
  isClearAble(n) {
    var c;
    const { uid: e, viewId: t, filterLocalSelf: i = !0, scenePath: o } = n, s = this.renderUids.get(t);
    if (s) {
      const { clear: l } = s;
      return !!(l === void 0 || l === !0 || Be(l) && (l.has(e) || i && ((c = this.control.collector) == null ? void 0 : c.uid) === e && l.has("localSelf")));
    }
    const a = this.pageAuth.get(t);
    if (a && o) {
      const l = a.get(o);
      if (l) {
        const { render: h } = l;
        if (h)
          return !(h === "localSelf" && !i);
      }
    }
    return !0;
  }
  isFilterAble(n, e) {
    var c, l, h, u;
    const { viewId: t, scenePath: i, elementId: o } = n, s = this.renderUids.get(t);
    if (s && o) {
      const { render: g, hide: M } = s;
      if (g === void 0 && M === void 0)
        return !0;
      const d = (c = this.control.collector) == null ? void 0 : c.getUidFromKey(o);
      return d ? !!(Be(g) && (g.has(d) || g.has("localSelf") && ((l = this.control.collector) == null ? void 0 : l.uid) === d) || g === !0 && (!M || Be(M) && !M.has(d) && !(M.has("localSelf") && ((h = this.control.collector) == null ? void 0 : h.uid) === d))) : !1;
    }
    const a = this.pageAuth.get(t);
    if (a && i && o) {
      const g = a.get(i);
      if (g) {
        const { render: M, pages: d } = g;
        if (!M)
          return !0;
        const y = (u = this.control.collector) == null ? void 0 : u.uid, w = M === "localSelf" ? y : M, p = w && d.get(w) || void 0;
        if (p)
          switch (e) {
            case R.RemoveNode:
              return !0;
            default:
              return p.has(o);
          }
        return !1;
      }
    }
    return !0;
  }
  isFilterCursor(n) {
    if (this.isUsePageAuth(n)) {
      const e = this.control.viewContainerManager.getCurScenePath(n), t = this.pageAuth.get(n);
      if (e && t) {
        const i = t.get(e);
        if (i) {
          const { render: o } = i;
          return o !== "localSelf";
        }
      }
    }
    return !0;
  }
  isUsePageAuth(n) {
    var i;
    if (!this.pageAuth.has(n))
      return !1;
    const t = this.control.viewContainerManager.getCurScenePath(n);
    return t && ((i = this.pageAuth.get(n)) == null ? void 0 : i.has(t)) || !1;
  }
  publishRender(n) {
    var s;
    const { viewId: e, option: t, isSync: i = !0 } = n, o = {};
    if (t) {
      const { render: a, hide: c, clear: l } = t;
      a && (o.render = a === !0 && !0 || new Set(a)), c && (o.hide = c === !0 && !0 || new Set(c)), l && (o.clear = l === !0 && !0 || new Set(l)), this.renderUids.set(e, o);
    } else
      this.renderUids.delete(e);
    i && ((s = this.control.collector) == null || s.dispatchRenderAuth(
      {
        operation: et.setAuth,
        viewId: e,
        renderData: o
      },
      i
    )), this.renderEffect(e);
  }
  publishOneRender(n, e, t, i = !0) {
    var a;
    const o = this.renderUids.get(n);
    let s;
    switch (t) {
      case "show":
        o ? (o.render || (o.render = /* @__PURE__ */ new Set()), Be(o.render) && o.render.add(e), Be(o.hide) && o.hide.delete(e)) : this.renderUids.set(n, { render: /* @__PURE__ */ new Set([e]) }), s = et.setShow;
        break;
      case "hide":
        o ? (o.hide || (o.hide = /* @__PURE__ */ new Set()), Be(o.hide) && o.hide.add(e), Be(o.render) && o.render.delete(e)) : this.renderUids.set(n, { hide: /* @__PURE__ */ new Set([e]) }), s = et.setHide;
        break;
    }
    o ? this.renderUids.set(n, o) : this.renderUids.delete(n), i && s && ((a = this.control.collector) == null || a.dispatchRenderAuth(
      {
        operation: s,
        viewId: n,
        uid: e
      },
      i
    )), this.renderEffect(n);
  }
  publishOneClear(n, e, t, i = !0) {
    var s;
    const o = this.renderUids.get(n);
    t ? o ? (o.clear || (o.clear = /* @__PURE__ */ new Set()), Be(o.clear) && o.clear.add(e)) : this.renderUids.set(n, { clear: /* @__PURE__ */ new Set([e]) }) : o && Be(o.clear) && o.clear.has(e) && o.clear.delete(e), o ? this.renderUids.set(n, o) : this.renderUids.delete(n), i && ((s = this.control.collector) == null || s.dispatchRenderAuth(
      {
        operation: et.setClear,
        viewId: n,
        uid: e,
        isClear: t
      },
      i
    )), this.renderEffect(n);
  }
  syncRenderUids(n, e, t, i) {
    const o = e === !0 ? !0 : e && Object.keys(e) ? new Set(Object.keys(e)) : void 0, s = t === !0 ? !0 : t && Object.keys(t) ? new Set(Object.keys(t)) : void 0, a = i === !0 ? !0 : i && Object.keys(i) ? new Set(Object.keys(i)) : void 0;
    o || s || a ? this.renderUids.set(n, { render: o, hide: s, clear: a }) : this.renderUids.delete(n), this.renderEffect(n), this.publicMsgEmitter.emit("syncRenderUids", n, {
      render: o,
      hide: s,
      clear: a
    });
  }
  syncPageAuth(n, e, t = !1) {
    let i = !1;
    if (!e)
      this.pageAuth.delete(n);
    else {
      const o = this.pageAuth.get(n) || /* @__PURE__ */ new Map();
      for (const [s, a] of Object.entries(e)) {
        const { render: c, pages: l } = a, h = o.get(s), u = this.isLocalRender ? h == null ? void 0 : h.render : c;
        if (u) {
          u !== (h == null ? void 0 : h.render) && (i = !0);
          const g = /* @__PURE__ */ new Map();
          for (const [d, y] of Object.entries(l))
            g.set(d, new Set(Object.keys(y)));
          const M = {
            render: u,
            pages: g
          };
          g.size && o.set(s, M);
        }
      }
      o.size && (this.pageAuth.set(n, o), this.publicMsgEmitter.emit("syncPageAuth", n, o));
    }
    (t || i) && this.pageAuthEffect(n);
  }
  destoryByViewId(n, e = !1) {
    var t;
    this.renderUids.has(n) && (this.renderUids.delete(n), e || (t = this.control.collector) == null || t.dispatchRenderAuth({
      operation: et.setAuth,
      viewId: n
    })), this.pageAuth.has(n) && this.control.renderControl.delViewPage(n, !e);
  }
  renderEffect(n) {
    var t;
    const e = (t = this.control.viewContainerManager.getView(n)) == null ? void 0 : t.focusScenePath;
    e && this.worker.clearViewScenePath(n, !0).then(() => {
      var i;
      this.worker.pullServiceData(n, e, {
        isAsync: !0,
        useAnimation: !1
      }), this.worker.sendClearUids(n, (i = this.renderUids.get(n)) == null ? void 0 : i.clear);
    });
  }
  pageAuthEffect(n) {
    var t;
    const e = (t = this.control.viewContainerManager.getView(n)) == null ? void 0 : t.focusScenePath;
    e && this.worker.clearViewScenePath(n, !0).then(() => {
      this.worker.pullServiceData(n, e, {
        isAsync: !0,
        useAnimation: !1
      }), this.worker.sendClearUids(n, !0);
    });
  }
  cloneElementIds(n, e, t, i) {
    var a, c;
    const o = (a = this.control.collector) == null ? void 0 : a.getStorageData(n, e), s = /* @__PURE__ */ new Set();
    if (o) {
      for (const l of Object.keys(o))
        if (t.has(l)) {
          const h = `${l}_${i}`;
          if (s.add(h), !Object.keys(o).includes(h)) {
            const u = se(o[l]);
            (c = this.control.collector) == null || c.updateValue(
              h,
              {
                type: R.FullWork,
                ...u,
                workId: h
              },
              { isSync: !0, viewId: n, scenePath: e }
            );
          }
        }
    }
    return s;
  }
  delElementIds(n, e, t) {
    var o, s;
    const i = (o = this.control.collector) == null ? void 0 : o.getStorageData(n, e);
    if (i)
      for (const a of Object.keys(i))
        t.has(a) && Object.keys(i).includes(a) && ((s = this.control.collector) == null || s.updateValue(a, void 0, {
          isSync: !0,
          viewId: n,
          scenePath: e
        }));
  }
}
class ip {
  constructor(n) {
    I(this, "internalMsgEmitter");
    I(this, "control");
    I(this, "maps", /* @__PURE__ */ new Map());
    I(this, "showId");
    I(this, "color");
    I(this, "timer");
    I(this, "updateMiniMapCamera", Pt(
      async (n) => {
        const e = this.maps.get(n);
        if (e) {
          const t = await this.getMiniMapCamera(n);
          if (t) {
            const i = t.width / t.scale, o = t.height / t.scale, s = e.miniMapCamera.width / e.miniMapCamera.scale, a = e.miniMapCamera.height / e.miniMapCamera.scale;
            (i >= s || o >= a) && (e.miniMapCamera = t);
            const c = e.miniMapCanvas.current;
            if (c) {
              const l = c.getContext("2d");
              if (l) {
                const h = this.control.viewContainerManager.getView(n);
                if (!(h != null && h.focusScenePath))
                  return;
                const { miniMapCamera: u, contianer: g } = e, { width: M, height: d } = g.getBoundingClientRect(), y = Math.min(
                  M / u.width,
                  d / u.height
                );
                l.clearRect(0, 0, c.width, c.height), c.width = u.width, c.height = u.height, c.style.width = `${u.width * y}px`, c.style.height = `${u.height * y}px`, await this.control.screenshotToCanvas(
                  l,
                  h.focusScenePath,
                  u.width,
                  u.height,
                  u
                ), this.listener.emitAsync([n, "viewCameraUpate"], 1);
              }
            }
          }
        }
      },
      2e3,
      { maxWait: 5e3 }
    ));
    var i;
    const { control: e, internalMsgEmitter: t } = n;
    this.control = e, this.internalMsgEmitter = t, this.color = ((i = this.control.worker.currentToolsData) == null ? void 0 : i.toolsOpt.strokeColor) || "#000";
  }
  get listener() {
    return this.internalMsgEmitter;
  }
  get activeViewId() {
    return this.showId;
  }
  get highlightColor() {
    return this.color;
  }
  setHighlightColor(n) {
    this.color = n;
  }
  async createMiniMap(n, e, t) {
    const i = this.control.viewContainerManager.getView(n);
    if (!i)
      throw new Error("view not found");
    const o = i.cameraOpt;
    if (!o)
      throw new Error("view camera not found");
    const { width: s, height: a } = e.getBoundingClientRect();
    if (!s || !a)
      throw new Error("contianer width or height is 0");
    const c = m.createRef();
    this.maps.set(n, {
      contianer: e,
      miniMapCanvas: c,
      miniMapCamera: t,
      viewCamera: o
    }), this.showId = n, this.control.viewContainerManager.setActiveMiniMap(this.showId, !0);
    const l = Math.min(
      s / t.width,
      a / t.height
    ), h = await new Promise((u) => {
      this.listener.once([n, "miniMapCreate"], () => {
        const g = c.current;
        g && (g.width = t.width, g.height = t.height, g.style.width = `${t.width * l}px`, g.style.height = `${t.height * l}px`), u(!0);
      }), this.timer = setTimeout(() => {
        u(!1);
      }, 2e3);
    });
    return this.timer && clearTimeout(this.timer), h;
  }
  async getMiniMapCamera(n) {
    const e = this.control.viewContainerManager.getView(n);
    if (!e)
      return;
    const { cameraOpt: t, focusScenePath: i } = e;
    if (!t || !i)
      return;
    const o = {
      originX: t.centerX - t.width / t.scale / 2,
      originY: t.centerY - t.height / t.scale / 2,
      width: t.width / t.scale,
      height: t.height / t.scale
    };
    let s = await this.control.getBoundingRect(i) || o;
    s = Kn(s, o) || o;
    let a = s.width, c = s.height, l = 1;
    const h = a > t.width && Math.min(t.width / a, l) || l, u = c > t.height && Math.min(t.height / c, l) || l;
    return h <= u ? (a = h < 1 && t.width || a, c = Math.floor(c * h) + 1, l = h) : h > u && (c = u < 1 && t.height || c, a = Math.floor(a * u) + 1, l = u), {
      scale: l,
      centerX: s.originX + s.width / 2,
      centerY: s.originY + s.height / 2,
      width: a,
      height: c
    };
  }
  getMiniMapCanvas(n) {
    const e = this.maps.get(n);
    return (e == null ? void 0 : e.miniMapCanvas.current) || null;
  }
  updateViewCamera(n, e) {
    const t = this.maps.get(n);
    t && (t.viewCamera = e, this.listener.emitAsync([n, "viewCameraUpate"], 1));
  }
  destroyMiniMap(n) {
    const e = n || this.showId;
    e && (this.maps.delete(e), this.control.viewContainerManager.setActiveMiniMap(e, !1));
  }
}
class QI {
  constructor() {
    I(this, "control");
    I(this, "_listeners", {});
  }
  _inject(n) {
    this.control = n;
  }
  _onEvent(n, e) {
    this._listeners[n] && this._listeners[n](...e);
  }
  callbacks(n, e) {
    this._listeners[n] = e;
  }
  removeCallback(n) {
    delete this._listeners[n];
  }
}
class rp {
  constructor(n) {
    I(this, "control");
    I(this, "worker");
    I(this, "publicMsgEmitter");
    I(this, "plugins", /* @__PURE__ */ new Map());
    const { control: e, publicMsgEmitter: t } = n;
    this.control = e, this.publicMsgEmitter = t, this.worker = e.worker;
  }
  use(n) {
    this.plugins.set(n.kind, n), n._inject(this.control), n.onCreate();
  }
  destroy() {
    this.plugins.forEach((n) => {
      this.plugins.delete(n.kind), n.onDestroy();
    });
  }
  dispatchPluginEvent(n, ...e) {
    this.plugins.forEach((t) => {
      t._onEvent(n, e);
    });
  }
}
const lt = class lt {
  constructor(n) {
    I(this, "version", vr);
    I(this, "plugin");
    I(this, "room");
    I(this, "play");
    I(this, "collector");
    I(this, "hasSwitchToSelectorEffect");
    I(this, "snapshootStateMap");
    I(this, "_disableDeviceInputs");
    I(this, "_disableEraseImage");
    I(this, "_disableEraseText");
    I(this, "_disableCameraTransform");
    I(this, "_disableUseWorker", !1);
    I(this, "effectResolve");
    I(this, "hotkeyManager");
    I(this, "pluginOptions");
    I(this, "roomMember");
    I(this, "cursor");
    I(this, "textEditorManager");
    I(this, "worker");
    I(this, "renderControl");
    I(this, "miniMapManager");
    I(this, "pluginManager");
    I(this, "_isPolyfill", {
      requestIdleCallback: !1,
      cancelIdleCallback: !1
    });
    /** 监听路径变化 */
    I(this, "onSceneChange", async (n, e) => {
      var s;
      const t = this.viewContainerManager.getView(e);
      t != null && t.focusScenePath && (s = this.collector) != null && s.hasSelector(e, t.focusScenePath) && this.worker.blurSelector(e, t.focusScenePath), this.textEditorManager.checkEmptyTextBlur();
      const i = t == null ? void 0 : t.displayer;
      i && (i.setActive(!1), await i.stopEventHandler());
      const o = n;
      o && this.viewContainerManager.setViewScenePath(e, o), i == null || i.setActive(!0);
    });
    /** 监听房间成员变化 */
    I(this, "onRoomMembersChange", (n) => {
      this.roomMember.setRoomMembers(Vt(n));
    });
    /** 监听房间教具变更 */
    I(this, "onMemberChange", Hi(
      (n) => {
        var s, a;
        if (!this.room || !this.worker || !n || !this.room.isWritable)
          return;
        const e = this.getToolsKey(n), t = (s = this.worker.getCurrentToolsData()) == null ? void 0 : s.toolsType, i = this.viewContainerManager.focuedViewId;
        i && t === T.Selector && e !== T.Selector && this.worker.blurCursor(i);
        const o = this.getToolsOpt(e, n);
        this.worker.setCurrentToolsData(o), (a = this.pluginManager) == null || a.dispatchPluginEvent("setToolkey", e), this.effectViewContainer(e), this.effectResolve && this.effectResolve(!0);
      },
      100,
      { leading: !1 }
    ));
    I(this, "internalSceneChange", (n, e) => {
      var t, i, o, s;
      this.isUseSimple || ((t = this.miniMapManager) == null ? void 0 : t.activeViewId) === n && ((i = this.miniMapManager) == null || i.destroyMiniMap(n)), (o = this.pluginManager) == null || o.dispatchPluginEvent("sceneChange", n, e), (s = this.worker) == null || s.clearViewScenePath(n, !0).then(() => {
        var a;
        (a = this.worker) == null || a.pullServiceData(n, e, {
          isAsync: !0,
          useAnimation: !1
        });
      });
    });
    I(this, "internalCameraChange", (n, e) => {
      var t, i;
      (t = this.worker) == null || t.updateCamera(n, e), this.isUseSimple || (i = this.miniMapManager) == null || i.updateViewCamera(n, e);
    });
    var s;
    const { displayer: e, plugin: t, options: i } = n;
    this.plugin = t, this.room = G(e) ? e : void 0, this.play = En(e) ? e : void 0, this.room && (this._disableDeviceInputs = this.room.disableDeviceInputs, this._disableEraseImage = this.room.disableEraseImage, this._disableEraseText = !1), this.pluginOptions = i, ((s = i.extras) == null ? void 0 : s.useWorker) === "mainThread" ? this._disableUseWorker = !0 : this._disableUseWorker = ji !== "worker", this.roomMember = new YM();
    const o = {
      control: this,
      internalMsgEmitter: lt.InternalMsgEmitter,
      publicMsgEmitter: lt.PublicMsgEmitter
    };
    this.cursor = new UM(o), this.textEditorManager = new ef(o), this.worker = new ep(o), this.hotkeyManager = new tp(o), this.renderControl = new np(o), this.pluginManager = new rp(o), this.isUseSimple || (this.miniMapManager = new ip(o));
  }
  hasPolyfillMethod(n) {
    return this._isPolyfill[n];
  }
  registerPolyfillMethod(n) {
    this._isPolyfill[n] = !0;
  }
  get hasExtendFontFaces() {
    return this.extendFontFaces.length > 0;
  }
  get extendFontFaces() {
    var n;
    return ((n = this.pluginOptions.extras.textEditor) == null ? void 0 : n.extendFontFaces) ?? Se.textEditor.extendFontFaces;
  }
  get loadFontFacesTimeout() {
    var n;
    return ((n = this.pluginOptions.extras.textEditor) == null ? void 0 : n.loadFontFacesTimeout) ?? Se.textEditor.loadFontFacesTimeout;
  }
  get disableEraseText() {
    return this._disableEraseText || !1;
  }
  set disableEraseText(n) {
    this._disableEraseText = n;
  }
  get disableEraseImage() {
    return this._disableEraseImage || !1;
  }
  set disableEraseImage(n) {
    this._disableEraseImage = n;
  }
  get disableDeviceInputs() {
    return this._disableDeviceInputs || !1;
  }
  set disableDeviceInputs(n) {
    var t;
    this._disableDeviceInputs = n;
    const e = (t = this.worker.getCurrentToolsData()) == null ? void 0 : t.toolsType;
    e && this.effectViewContainer(e);
  }
  get disableCameraTransform() {
    return this._disableCameraTransform || !1;
  }
  set disableCameraTransform(n) {
    this._disableCameraTransform = n;
  }
  get disableUseWorker() {
    return this._disableUseWorker || !1;
  }
  set disableUseWorker(n) {
    this._disableUseWorker = n;
  }
  get hasOffscreenCanvas() {
    return this._disableUseWorker ? !1 : ji === "worker";
  }
  get canUseTransferControlToOffscreen() {
    return this._disableUseWorker ? !1 : !!HTMLCanvasElement.prototype.transferControlToOffscreen;
  }
  get isUseOffscreenTransfer() {
    return this.isUseSimple && this.hasOffscreenCanvas && this.canUseTransferControlToOffscreen;
  }
  /** 是否使用简单模式, 简单模式则无需开启 miniMap 和 plugin */
  get isUseSimple() {
    return this.pluginOptions.extras.useSimple;
  }
  /** 是否使用背景画布, 默认false */
  get isUseBackgroundThread() {
    return this.pluginOptions.extras.useBackgroundThread;
  }
  bindPlugin(n) {
    this.plugin = n, this.collector && (this.collector.removeStorageStateListener(), this.collector.removeAuthStateListener()), this.collector = new xn(
      this,
      n,
      this.pluginOptions.extras.syncOpt.interval
    ), this.activePlugin();
  }
  /** 销毁 */
  destroy() {
    var n, e, t, i, o, s, a;
    this.roomMember.destroy(), (n = this.collector) == null || n.destroy(), (e = this.worker) == null || e.destroy(), (t = this.viewContainerManager) == null || t.destroy(), (i = this.cursor) == null || i.destroy(), (o = this.textEditorManager) == null || o.destory(), (s = this.pluginManager) == null || s.destroy(), (a = this.miniMapManager) == null || a.destroyMiniMap();
  }
  /** 清空当前获焦路径下的所有内容 */
  cleanCurrentScene() {
    const n = this.worker.now, e = this.worker.getLocalWorkViewId() || this.viewContainerManager.focuedViewId;
    e && (lt.InternalMsgEmitter.emit(
      "addUndoTicker",
      n,
      e
    ), this.worker.clearViewScenePath(e, !1, !0).then(() => {
      lt.InternalMsgEmitter.emit(
        "forceUndoTickerEnd",
        n,
        e
      );
    }));
  }
  async setWritable(n) {
    var e, t;
    if (!n) {
      (e = this.cursor) == null || e.unabled(), this.textEditorManager.checkEmptyTextBlur();
      const i = this.viewContainerManager.getAllViews();
      for (const o of i) {
        o != null && o.focusScenePath && (t = this.collector) != null && t.hasSelector(o.id, o.focusScenePath) && this.worker.blurSelector(o.id, o.focusScenePath);
        const s = o == null ? void 0 : o.displayer;
        s && (s.setActive(!1), await s.stopEventHandler());
      }
    }
    await new Promise((i) => {
      setTimeout(() => {
        i();
      }, 500);
    });
  }
  /** 监听读写状态变更 */
  onWritableChange(n) {
    var i;
    n ? this.worker.abled() : this.worker.unWritable(), ((i = this.worker.getCurrentToolsData()) == null ? void 0 : i.toolsType) === T.Text && this.viewContainerManager.focuedViewId && (n ? this.viewContainerManager.activeTextEditor(
      this.viewContainerManager.focuedViewId
    ) : this.viewContainerManager.unActiveTextEditor(
      this.viewContainerManager.focuedViewId
    )), this.renderControl.onWritableChange(n), this.viewContainerManager.getAllViews().forEach((o) => {
      const s = o == null ? void 0 : o.displayer;
      s && (s.bindToolsClass(), n ? s.setActive(!0) : s.setActive(!1));
    });
  }
  /** 获取当前工具key */
  getToolsKey(n) {
    const e = n.currentApplianceName;
    switch (this.hasSwitchToSelectorEffect = !1, e) {
      case ne.text:
        return n.textCompleteToSelector && (this.hasSwitchToSelectorEffect = !0), T.Text;
      case ne.pencil:
        return n.useLaserPen ? T.LaserPen : T.Pencil;
      case ne.eraser:
        return T.Eraser;
      case ne.pencilEraser:
        return this.pluginOptions.extras.pencilEraser.useBitMap === !1 ? T.PencilEraser : T.BitMapEraser;
      case ne.selector:
        return T.Selector;
      case ne.arrow:
        return n.arrowCompleteToSelector && (this.hasSwitchToSelectorEffect = !0), T.Arrow;
      case ne.straight:
        return n.straightCompleteToSelector && (this.hasSwitchToSelectorEffect = !0), T.Straight;
      case ne.ellipse:
        return n.ellipseCompleteToSelector && (this.hasSwitchToSelectorEffect = !0), T.Ellipse;
      case ne.rectangle:
        return n.rectangleCompleteToSelector && (this.hasSwitchToSelectorEffect = !0), T.Rectangle;
      case ne.shape:
        if (n.shapeCompleteToSelector && (this.hasSwitchToSelectorEffect = !0), n.shapeType === qe.Pentagram || n.shapeType === qe.Star)
          return T.Star;
        if (n.shapeType === qe.Polygon || n.shapeType === qe.Triangle || n.shapeType === qe.Rhombus)
          return T.Polygon;
        if (n.shapeType === qe.SpeechBalloon)
          return T.SpeechBalloon;
        break;
    }
    return T.Clicker;
  }
  /** 获取当前工具默认配置 */
  getToolsOpt(n, e) {
    if (n === T.Clicker)
      return {
        toolsType: n,
        toolsOpt: {}
      };
    const t = {
      strokeColor: At(
        e.strokeColor[0],
        e.strokeColor[1],
        e.strokeColor[2],
        e.strokeOpacity || 1
      ),
      thickness: e.strokeWidth,
      isOpacity: (e == null ? void 0 : e.strokeOpacity) && e.strokeOpacity < 1 || (e == null ? void 0 : e.fillOpacity) && e.fillOpacity < 1 || (e == null ? void 0 : e.textOpacity) && e.textOpacity < 1 || (e == null ? void 0 : e.textBgOpacity) && e.textBgOpacity < 1 || !1
    };
    switch (n) {
      case T.Text:
        t.fontFamily = window.getComputedStyle(document.documentElement).getPropertyValue("font-family"), t.fontSize = (e == null ? void 0 : e.textSizeOverride) || (e == null ? void 0 : e.textSize) || Number(window.getComputedStyle(document.body).fontSize), t.textAlign = (e == null ? void 0 : e.textAlign) || "left", t.verticalAlign = (e == null ? void 0 : e.verticalAlign) || "middle", t.fontColor = (e == null ? void 0 : e.textColor) && At(
          e.textColor[0],
          e.textColor[1],
          e.textColor[2],
          e.textOpacity || 1
        ) || t.strokeColor || "rgba(0,0,0,1)", t.fontBgColor = Array.isArray(e == null ? void 0 : e.textBgColor) && At(
          e.textBgColor[0],
          e.textBgColor[1],
          e.textBgColor[2],
          e.textBgOpacity || 1
        ) || "transparent", t.bold = (e == null ? void 0 : e.bold) && "bold" || void 0, t.italic = (e == null ? void 0 : e.italic) && "italic" || void 0, t.underline = (e == null ? void 0 : e.underline) || void 0, t.lineThrough = (e == null ? void 0 : e.lineThrough) || void 0, t.text = "", t.strokeColor = void 0;
        break;
      case T.Pencil: {
        const i = (e == null ? void 0 : e.strokeType) || me.Normal;
        t.strokeType = i, t.lineCap = this.getLineCap(i), t.lineDash = this.getLineDash(i);
        break;
      }
      case T.PencilEraser:
      case T.BitMapEraser:
      case T.Eraser:
        t.thickness = Math.min(
          4,
          Math.max(1, Math.floor(e.pencilEraserSize || 1))
        ) - 1;
        break;
      case T.LaserPen: {
        t.duration = (e == null ? void 0 : e.duration) || 1, e != null && e.strokeType && (e == null ? void 0 : e.strokeType) === me.Stroke ? t.strokeType = me.Normal : t.strokeType = (e == null ? void 0 : e.strokeType) || me.Normal;
        const i = t.strokeType;
        t.lineCap = this.getLineCap(i), t.lineDash = this.getLineDash(i);
        break;
      }
      case T.Arrow:
      case T.Straight: {
        e != null && e.strokeType && (e == null ? void 0 : e.strokeType) === me.Stroke ? t.strokeType = me.Normal : t.strokeType = (e == null ? void 0 : e.strokeType) || me.Normal;
        const i = t.strokeType;
        t.strokeType = i, t.lineCap = this.getLineCap(i), t.lineDash = this.getLineDash(i);
        break;
      }
      case T.Ellipse:
      case T.Rectangle:
      case T.Star:
      case T.Polygon:
      case T.SpeechBalloon: {
        e != null && e.strokeType && (e == null ? void 0 : e.strokeType) === me.Stroke ? t.strokeType = me.Normal : t.strokeType = (e == null ? void 0 : e.strokeType) || me.Normal;
        const i = t.strokeType;
        t.lineCap = this.getLineCap(i), t.lineDash = this.getLineDash(i), n === T.Star && (e.shapeType === qe.Pentagram ? (t.vertices = 10, t.innerVerticeStep = 2, t.innerRatio = 0.4) : e != null && e.vertices && (e != null && e.innerVerticeStep) && (e != null && e.innerRatio) && (t.vertices = e.vertices, t.innerVerticeStep = e.innerVerticeStep, t.innerRatio = e.innerRatio)), n === T.Polygon && (e.shapeType === qe.Triangle ? t.vertices = 3 : e.shapeType === qe.Rhombus ? t.vertices = 4 : e.vertices && (t.vertices = e.vertices)), t.fillColor = (e == null ? void 0 : e.fillColor) && At(
          e.fillColor[0],
          e.fillColor[1],
          e.fillColor[2],
          e == null ? void 0 : e.fillOpacity
        ) || "transparent", n === T.SpeechBalloon && (t.placement = e.placement || "bottomLeft");
        break;
      }
    }
    return {
      toolsType: n,
      toolsOpt: t
    };
  }
  getLineCap(n) {
    switch (n) {
      case me.Normal:
        return this.pluginOptions.extras.normalStroke.lineCap || Se.normalStroke.lineCap;
      case me.Dotted:
        return this.pluginOptions.extras.dottedStroke.lineCap || Se.dottedStroke.lineCap;
      case me.LongDotted:
        return this.pluginOptions.extras.longDottedStroke.lineCap || Se.longDottedStroke.lineCap;
      default:
        return;
    }
  }
  getLineDash(n) {
    switch (n) {
      case me.Normal:
        return;
      case me.Dotted: {
        const { segment: e, gap: t } = this.pluginOptions.extras.dottedStroke || Se.dottedStroke;
        return [e, t];
      }
      case me.LongDotted: {
        const { segment: e, gap: t } = this.pluginOptions.extras.longDottedStroke || Se.longDottedStroke;
        return [e, t];
      }
      default:
        return;
    }
  }
  /** 激活当前view容器*/
  effectViewContainer(n) {
    var e, t, i, o, s;
    if (this.disableDeviceInputs === !0)
      this.room.disableDeviceInputs = !0, (e = this.worker) == null || e.unWritable(), (t = this.cursor) == null || t.unabled();
    else
      switch (n !== T.Text && this.viewContainerManager.focuedViewId && this.viewContainerManager.unActiveTextEditor(
        this.viewContainerManager.focuedViewId
      ), n) {
        case T.Text:
        case T.Pencil:
        case T.LaserPen:
        case T.Arrow:
        case T.Straight:
        case T.Rectangle:
        case T.Ellipse:
        case T.Star:
        case T.Polygon:
        case T.SpeechBalloon:
        case T.Triangle:
        case T.Rhombus:
        case T.Eraser:
        case T.PencilEraser:
        case T.BitMapEraser:
        case T.Selector:
          this.room.disableDeviceInputs = !0, (i = this.worker) == null || i.abled(), n === T.Text && this.viewContainerManager.focuedViewId && this.viewContainerManager.activeTextEditor(
            this.viewContainerManager.focuedViewId
          );
          break;
        default:
          this.room.disableDeviceInputs = !1, (o = this.worker) == null || o.unWritable(), (s = this.cursor) == null || s.unabled();
          break;
      }
    setTimeout(() => {
      this.viewContainerManager.getAllViews().forEach((c) => {
        c != null && c.displayer && c.displayer.bindToolsClass();
      });
    }, 0);
  }
  /** 异步获取指定路径下绘制内容的区域大小 */
  async getBoundingRect(n) {
    var t, i, o, s, a;
    const e = await ((t = this.worker) == null ? void 0 : t.getBoundingRect(n));
    if (e) {
      const c = ((o = (i = this.viewContainerManager.mainView) == null ? void 0 : i.viewData) == null ? void 0 : o.convertToPointInWorld({
        x: e.x,
        y: e.y
      })) || { x: e.x, y: e.y }, l = ((a = (s = this.viewContainerManager.mainView) == null ? void 0 : s.viewData) == null ? void 0 : a.camera.scale) || 1;
      return {
        width: Math.floor(e.w / l) + 1,
        height: Math.floor(e.h / l) + 1,
        originX: c.x,
        originY: c.y
      };
    }
  }
  /** 异步获取指定路径下的的快照并绘制到指定的画布上 */
  async screenshotToCanvas(n, e, t, i, o, s, a) {
    const c = await this.worker.getSnapshot(
      e,
      t,
      i,
      o
    );
    c && c.forEach((l) => {
      if (l)
        try {
          console.log(
            "[screenshotToCanvas] Drawing ImageBitmap:",
            l.width,
            l.height
          ), n.drawImage(l, s || 0, a || 0);
        } catch (h) {
          console.error(
            "[screenshotToCanvas] Failed to draw ImageBitmap:",
            h
          );
        }
      l instanceof ImageBitmap ? l.close() : l instanceof HTMLImageElement && l.remove();
    });
  }
  /** 异步获取指定路径下的缩略图 */
  async scenePreview(n, e) {
    var s, a, c, l;
    const t = (s = this.collector) == null ? void 0 : s.getViewIdBySecenPath(n);
    if (!t)
      return;
    const i = this.viewContainerManager.getView(t);
    if (!i || !((a = i.cameraOpt) != null && a.width) || !((c = i.cameraOpt) != null && c.height))
      return;
    const o = await ((l = this.worker) == null ? void 0 : l.getSnapshot(n));
    if (o && this.worker) {
      const h = document.createElement("canvas"), u = h.getContext("2d"), { width: g, height: M } = i.cameraOpt;
      h.width = g, h.height = M, u && (o.forEach((d) => {
        d && (u.drawImage(d, 0, 0), d instanceof ImageBitmap ? d.close() : d instanceof HTMLImageElement && d.remove());
      }), e.crossOrigin = "anonymous", e.src = h.toDataURL(), e.onload = () => {
        h.remove();
      }, e.onerror = () => {
        h.remove(), e.remove();
      });
    }
  }
  switchToText() {
    var n;
    (n = this.room) == null || n.setMemberState({
      currentApplianceName: ne.text
    });
  }
  /** 切换到选择工具 */
  switchToSelector() {
    var n;
    (n = this.room) == null || n.setMemberState({
      currentApplianceName: ne.selector
    });
  }
  /** 开始执行副作用 */
  async runEffectWork(n) {
    if (this.hasSwitchToSelectorEffect) {
      const e = await new Promise((t) => {
        this.switchToSelector(), this.effectResolve = t;
      });
      this.effectResolve = void 0, e && n && n();
    }
  }
  setSnapshootData() {
    var n;
    this.room && (this.snapshootStateMap = /* @__PURE__ */ new Map(), this.snapshootStateMap.set("memberState", (n = this.room) == null ? void 0 : n.state.memberState));
  }
  getSnapshootData(n) {
    var e;
    return (e = this.snapshootStateMap) == null ? void 0 : e.get(n);
  }
  clearSnapshootData() {
    var n;
    (n = this.snapshootStateMap) == null || n.clear(), this.snapshootStateMap = void 0;
  }
  useBitMapEraser(n) {
    this.pluginOptions.extras.pencilEraser.useBitMap = n;
  }
  consoleWorkerInfo() {
    var n;
    (n = this.worker) == null || n.consoleWorkerInfo();
  }
  filterRenderByUid(n, e, t = !0) {
    var o;
    this.viewContainerManager.getView(n) && ((o = this.renderControl) == null || o.publishRender({ viewId: n, option: e, isSync: t }));
  }
  cancelFilterRender(n, e = !0) {
    var t;
    (t = this.renderControl) == null || t.publishRender({
      viewId: n,
      isSync: e
    });
  }
  async createMiniMap(n, e) {
    var i, o, s;
    if (this.isUseSimple)
      throw new Error("simple mode not support createMiniMap");
    const t = await ((i = this.miniMapManager) == null ? void 0 : i.getMiniMapCamera(n));
    if (t && await ((o = this.miniMapManager) == null ? void 0 : o.createMiniMap(
      n,
      e,
      t
    ))) {
      const c = (s = this.miniMapManager) == null ? void 0 : s.getMiniMapCanvas(n);
      if (c) {
        const l = c.getContext("2d");
        if (l) {
          const h = this.viewContainerManager.getView(n);
          h != null && h.focusScenePath && this.screenshotToCanvas(
            l,
            h.focusScenePath,
            t.width,
            t.height,
            t
          );
        }
      }
    }
  }
  async destroyMiniMap(n) {
    var e;
    (e = this.miniMapManager) == null || e.destroyMiniMap(n);
  }
  insertIconify(n, e) {
    var i;
    if (this.viewContainerManager.getView(n) && this.room) {
      const o = this.room.state.memberState, s = At(
        o.strokeColor[0],
        o.strokeColor[1],
        o.strokeColor[2],
        o.strokeOpacity || 1
      );
      (i = this.worker) == null || i.insertIconify(n, { ...e, strokeColor: s });
    }
  }
  insertBackgroundSVG(n, e) {
    var t;
    if (!this.isUseBackgroundThread)
      throw new Error("useBackgroundThread of pluginOptions should be true");
    (t = this.worker) == null || t.insertBackgroundSVG(n, e);
  }
  async insertBackgroundMarkMap(n, e) {
    var t;
    if (!this.isUseBackgroundThread)
      throw new Error("useBackgroundThread of pluginOptions should be true");
    return await ((t = this.worker) == null ? void 0 : t.insertBackgroundMarkMap(n, e));
  }
  updateBackgroundMarkMap(n, e, t) {
    var o;
    const i = this.viewContainerManager.getView(n);
    if (i && this.collector && this.collector.hasTargetDataByViewId(n, e)) {
      const { data: s, scenePath: a } = this.collector.getTargetDataByViewId(
        n,
        e
      );
      if (!a)
        throw new Error("updateBackgroundMarkMap scenePath is not exist");
      const c = i.focusScenePath;
      if (c && c !== a)
        throw new Error(
          "updateBackgroundMarkMap scenePath is not the focus scene path"
        );
      if (s && a)
        (o = this.worker) == null || o.updateBackgroundMarkMap(n, e, t);
      else
        throw new Error(
          "updateBackgroundMarkMap data or scenePath is not exist"
        );
    }
  }
  usePlugin(n) {
    var e;
    return (e = this.pluginManager) == null ? void 0 : e.use(n);
  }
  async stopDraw(n) {
    var t;
    const e = this.viewContainerManager.getView(n);
    e && await ((t = e.displayer) == null ? void 0 : t.stopEventHandler());
  }
  async setViewLocalScenePathChange(n, e) {
    this.viewContainerManager.getView(n) && await this.onSceneChange(e, n);
  }
};
I(lt, "InternalMsgEmitter", new Tn()), I(lt, "PublicMsgEmitter", new Tn());
let ke = lt;
var Dn = /* @__PURE__ */ ((r) => (r.Undo = "undo", r.Redo = "redo", r))(Dn || {});
const he = class he {
  createProxy(n, e, t) {
    const i = new Proxy(n, {
      get(o, s, a) {
        if (typeof s == "string" && /^\d+$/.test(s))
          return Reflect.get(o, s, a);
        const c = he.interceptors.hasOwnProperty(s) ? he.interceptors : o, l = Reflect.get(c, s, a);
        return typeof l == "function" && c === he.interceptors ? l.bind(i) : l;
      },
      set(o, s, a, c) {
        const l = he.proxyCallbacks.get(c), h = (l == null ? void 0 : l.arrayType) || "undo";
        if (s === "length") {
          const u = o.length, g = [];
          if (a < u)
            for (let d = a; d < u; d++)
              g.push(o[d]);
          const M = Reflect.set(o, s, a, c);
          if (l != null && l.callback)
            for (let d = 0; d < g.length; d++)
              l.callback({
                operation: "delete",
                index: a + d,
                oldValue: g[d],
                arrayType: h
              });
          return M;
        }
        return typeof s == "string" && /^\d+$/.test(s), Reflect.set(o, s, a, c);
      },
      deleteProperty(o, s) {
        const a = he.proxyCallbacks.get(i), c = (a == null ? void 0 : a.arrayType) || "undo";
        if (typeof s == "string" && /^\d+$/.test(s)) {
          const l = parseInt(s, 10), h = o[l], u = Reflect.deleteProperty(o, s);
          return a != null && a.callback && h !== void 0 && a.callback({
            operation: "delete",
            index: l,
            oldValue: h,
            arrayType: c
          }), u;
        }
        return Reflect.deleteProperty(o, s);
      }
    });
    return he.proxyToRaw.set(i, n), he.proxyCallbacks.set(i, { callback: e, arrayType: t }), i;
  }
};
I(he, "proxyToRaw", /* @__PURE__ */ new WeakMap()), I(he, "proxyCallbacks", /* @__PURE__ */ new WeakMap()), I(he, "interceptors", {
  entries(...n) {
    return he.proxyToRaw.get(this).entries(...n);
  },
  forEach(...n) {
    return he.proxyToRaw.get(this).forEach(...n);
  },
  map(...n) {
    return he.proxyToRaw.get(this).map(...n);
  },
  filter(...n) {
    return he.proxyToRaw.get(this).filter(...n);
  },
  find(...n) {
    return he.proxyToRaw.get(this).find(...n);
  },
  findIndex(...n) {
    return he.proxyToRaw.get(this).findIndex(...n);
  },
  includes(...n) {
    return he.proxyToRaw.get(this).includes(...n);
  },
  indexOf(...n) {
    return he.proxyToRaw.get(this).indexOf(...n);
  },
  slice(...n) {
    return he.proxyToRaw.get(this).slice(...n);
  },
  concat(...n) {
    return he.proxyToRaw.get(this).concat(...n);
  },
  push(...n) {
    const e = he.proxyToRaw.get(this), t = this, i = he.proxyCallbacks.get(t), o = (i == null ? void 0 : i.arrayType) || "undo", s = e.length, a = e.push(...n);
    if (i != null && i.callback)
      for (let c = s; c < e.length; c++)
        i.callback({
          operation: "add",
          index: c,
          newValue: e[c],
          arrayType: o
        });
    return a;
  },
  customPush(...n) {
    return he.proxyToRaw.get(this).push(...n);
  },
  pop() {
    const n = he.proxyToRaw.get(this), e = this, t = he.proxyCallbacks.get(e), i = (t == null ? void 0 : t.arrayType) || "undo", o = n.length, s = n[o - 1], a = n.pop();
    return t != null && t.callback && o > 0 && t.callback({
      operation: "delete",
      index: o - 1,
      oldValue: s,
      arrayType: i
    }), a;
  },
  shift() {
    const n = he.proxyToRaw.get(this), e = this, t = he.proxyCallbacks.get(e), i = (t == null ? void 0 : t.arrayType) || "undo", o = n[0], s = n.shift();
    return t != null && t.callback && n.length > 0 && t.callback({
      operation: "delete",
      index: 0,
      oldValue: o,
      arrayType: i
    }), s;
  },
  unshift(...n) {
    const e = he.proxyToRaw.get(this), t = this, i = he.proxyCallbacks.get(t), o = (i == null ? void 0 : i.arrayType) || "undo", s = e.unshift(...n);
    if (i != null && i.callback)
      for (let a = 0; a < n.length; a++)
        i.callback({
          operation: "add",
          index: a,
          newValue: e[a],
          arrayType: o
        });
    return s;
  },
  splice(n, e, ...t) {
    const i = he.proxyToRaw.get(this), o = this, s = he.proxyCallbacks.get(o), a = (s == null ? void 0 : s.arrayType) || "undo", c = e ?? i.length - n, l = i.slice(n, n + c), h = i.splice(n, c, ...t);
    if (s != null && s.callback) {
      for (let u = 0; u < l.length; u++)
        s.callback({
          operation: "delete",
          index: n + u,
          oldValue: l[u],
          arrayType: a
        });
      for (let u = 0; u < t.length; u++)
        s.callback({
          operation: "add",
          index: n + u,
          newValue: i[n + u],
          arrayType: a
        });
    }
    return h;
  },
  customSplice(n, e, ...t) {
    const i = he.proxyToRaw.get(this), o = e ?? i.length - n;
    return i.splice(n, o, ...t);
  },
  get length() {
    return he.proxyToRaw.get(this).length;
  },
  set length(n) {
    const e = he.proxyToRaw.get(this), t = this, i = he.proxyCallbacks.get(t), o = (i == null ? void 0 : i.arrayType) || "undo", s = e.length, a = [];
    if (n < s)
      for (let c = n; c < s; c++)
        a.push(e[c]);
    if (e.length = n, i != null && i.callback)
      for (let c = 0; c < a.length; c++)
        i.callback({
          operation: "delete",
          index: n + c,
          oldValue: a[c],
          arrayType: o
        });
  }
});
let zn = he;
const rt = class rt {
  constructor(n) {
    I(this, "emitter", new Tn());
    I(this, "undoStack");
    I(this, "redoStack");
    I(this, "worker");
    I(this, "control");
    I(this, "room");
    I(this, "viewId");
    I(this, "enableGlobal");
    I(this, "isTicking");
    I(this, "_undoTickerId");
    I(this, "scenePath");
    I(this, "tickStartStorerCache");
    I(this, "excludeIds", /* @__PURE__ */ new Set());
    I(this, "onChangeScene", Pt(() => {
      const n = this.control.viewContainerManager.getCurScenePath(
        this.viewId
      );
      if (n) {
        const e = this.undoStack.filter(
          (i) => i.scenePath === n
        ).length, t = this.redoStack.filter(
          (i) => i.scenePath === n
        ).length;
        this.emitter.emitAsync("onCanUndoStepsUpdate", e, this.viewId), this.emitter.emitAsync("onCanRedoStepsUpdate", t, this.viewId);
      }
    }, rt.waitTime));
    const { control: e, internalMsgEmitter: t, viewId: i, enableGlobal: o } = n;
    this.control = e, this.emitter = t, rt.MaxStackLength = this.control.pluginOptions.extras.undoRedo.maxStackLength || Se.undoRedo.maxStackLength;
    const s = new zn(), a = [], c = [];
    this.undoStack = s.createProxy(
      a,
      (l) => {
        this.control.renderControl.isWritable && this.handleArrayChange(l, be.syncUndoStack);
      },
      Dn.Undo
    ), this.redoStack = s.createProxy(
      c,
      (l) => {
        this.control.renderControl.isWritable && this.handleArrayChange(l, be.syncRedoStack);
      },
      Dn.Redo
    ), this.room = e.room, this.worker = e.worker, this.isTicking = !1, this.viewId = i, this.enableGlobal = o;
  }
  handleArrayChange(n, e) {
    if (!this.collector || !this.enableGlobal)
      return;
    const { operation: t, oldValue: i, newValue: o } = n;
    let s, a;
    t === "delete" && i ? (s = i, a = i.id) : t === "add" && o && (s = o, a = o.id), s && a && this.collector.dispatchUndoTicker({
      operation: e,
      syncType: t,
      viewId: this.viewId,
      undoTickerId: a,
      data: s
    });
  }
  get collector() {
    return this.control.collector;
  }
  get undoTickerId() {
    return this._undoTickerId;
  }
  get tmpTickStartStorerCache() {
    return this.tickStartStorerCache;
  }
  get uid() {
    var n;
    return ((n = this.control.collector) == null ? void 0 : n.uid) || "";
  }
  addExcludeIds(n) {
    if (this.isTicking)
      for (const e of n)
        this.excludeIds.add(e);
  }
  undoTickerStart(n, e) {
    var t, i;
    if (this.collector && (this.undoTickerId !== n || this.scenePath !== e)) {
      this.undoTickerId && this.viewId && this.scenePath && this.undoTickerEndSync(this.undoTickerId, this.viewId, this.scenePath), this.isTicking = !0;
      const o = this.collector.transformKey($);
      this.excludeIds.add(o), this._undoTickerId = n, this.scenePath = e;
      const s = ((t = this.collector) == null ? void 0 : t.storage[this.viewId]) && ((i = this.collector) == null ? void 0 : i.storage[this.viewId][this.scenePath]) || {};
      this.tickStartStorerCache = new Map(Object.entries(se(s)));
    }
  }
  undoTickerEnd(n, e) {
    this.undoTickerId && n === this.undoTickerId && e === this.scenePath ? this.undoTickerEndSync(n, this.viewId, e) : (this.isTicking = !1, this.scenePath = void 0, this.tickStartStorerCache = void 0, this._undoTickerId = void 0, this.excludeIds.clear());
  }
  undoTickerEndSync(n, e, t, i = !0) {
    var o, s;
    if (n === this.undoTickerId && t === this.scenePath && e === this.viewId && this.tickStartStorerCache) {
      const a = ((o = this.collector) == null ? void 0 : o.storage[e]) && ((s = this.collector) == null ? void 0 : s.storage[e][t]) || {}, c = this.diffFun(
        this.tickStartStorerCache,
        new Map(Object.entries(a)),
        !0
      );
      c.size && (this.undoStack.push({
        id: `${n}-${this.uid}`,
        data: se(c),
        scenePath: t
      }), this.undoStack.length > rt.MaxStackLength && this.undoStack.shift(), i && this.emitter.emitAsync(
        "onCanUndoStepsUpdate",
        this.undoStack.length,
        this.viewId
      ), this.redoStack.length && (this.redoStack.length = 0, i && this.emitter.emitAsync(
        "onCanRedoStepsUpdate",
        this.redoStack.length,
        this.viewId
      ))), this.isTicking = !1, this.scenePath = void 0, this.tickStartStorerCache = void 0, this._undoTickerId = void 0, this.excludeIds.clear();
    }
  }
  transformToUndoStackItem(n) {
    const e = [];
    for (const [t, i] of Object.entries(n)) {
      const o = /* @__PURE__ */ new Set();
      for (const [s, a] of Object.entries(
        i.data
      ))
        o.add({
          key: s,
          dataType: a.dataType,
          data: a.data
        });
      e.push({
        id: t,
        scenePath: i.scenePath,
        data: o
      });
    }
    return e;
  }
  pullServiceData(n) {
    if (n) {
      const { undoStack: e, redoStack: t } = n;
      if (e) {
        const i = this.transformToUndoStackItem(e);
        for (let o = this.undoStack.length - 1; o >= 0; o--) {
          const s = this.undoStack[o];
          i.findIndex((c) => c.id === s.id) === -1 && this.undoStack.customSplice(o, 1);
        }
        for (const o of i) {
          const s = this.undoStack.findIndex((a) => a.id === o.id);
          s !== -1 ? this.undoStack[s] = o : this.undoStack.customPush(o);
        }
        this.undoStack.sort((o, s) => {
          const a = parseInt(o.id.split("-")[0]), c = parseInt(s.id.split("-")[0]);
          return a - c;
        }), this.emitter.emitAsync(
          "onCanUndoStepsUpdate",
          this.undoStack.length,
          this.viewId
        );
      }
      if (t) {
        const i = this.transformToUndoStackItem(t);
        for (let o = this.redoStack.length - 1; o >= 0; o--) {
          const s = this.redoStack[o];
          i.findIndex((c) => c.id === s.id) === -1 && this.redoStack.customSplice(o, 1);
        }
        for (const o of i) {
          const s = this.redoStack.findIndex((a) => a.id === o.id);
          s !== -1 ? this.redoStack[s] = o : this.redoStack.customPush(o);
        }
        this.redoStack.sort((o, s) => {
          const a = parseInt(o.id.split("-")[0]);
          return parseInt(s.id.split("-")[0]) - a;
        }), this.emitter.emitAsync(
          "onCanRedoStepsUpdate",
          this.redoStack.length,
          this.viewId
        );
      }
    } else
      this.clear();
  }
  undo(n) {
    var o, s;
    let e;
    if (this.undoTickerId && this.tickStartStorerCache && this.scenePath) {
      const a = ((o = this.collector) == null ? void 0 : o.storage[this.viewId]) && ((s = this.collector) == null ? void 0 : s.storage[this.viewId][n]) || {}, c = this.diffFun(
        this.tickStartStorerCache,
        new Map(Object.entries(a)),
        !1
      ), l = this.diffFun(
        this.tickStartStorerCache,
        new Map(Object.entries(a)),
        !0
      );
      c.size === l.size ? (this.undoStack.push({
        id: `${this.undoTickerId}-${this.uid}`,
        data: se(c),
        scenePath: n
      }), this.undoStack.length > rt.MaxStackLength && this.undoStack.shift()) : c.size && !l.size && (e = {
        id: `${this.undoTickerId}-${this.uid}`,
        data: se(c),
        scenePath: n
      }), this.isTicking = !1, this.scenePath = void 0, this.tickStartStorerCache = void 0, this._undoTickerId = void 0, this.excludeIds.clear();
    }
    if (e)
      this.refreshPlugin(e);
    else {
      let a = this.undoStack.length - 1;
      for (; a >= 0; ) {
        if (this.undoStack[a].scenePath === n) {
          const l = this.undoStack[a];
          l && (this.redoStack.push(l), l.data && this.refreshPlugin(l)), this.undoStack.splice(a, 1);
          break;
        }
        a--;
      }
    }
    const t = this.undoStack.filter(
      (a) => a.scenePath === n
    ).length, i = this.redoStack.filter(
      (a) => a.scenePath === n
    ).length;
    return this.emitter.emitAsync("onCanUndoStepsUpdate", t, this.viewId), this.emitter.emitAsync("onCanRedoStepsUpdate", i, this.viewId), t;
  }
  redo(n) {
    let e = this.redoStack.length - 1;
    for (; e >= 0; ) {
      if (this.redoStack[e].scenePath === n) {
        const s = this.redoStack[e];
        s && (this.undoStack.push(s), s.data && this.refreshPlugin(s, !0)), this.redoStack.splice(e, 1);
        break;
      }
      e--;
    }
    const t = this.undoStack.filter(
      (o) => o.scenePath === n
    ).length, i = this.redoStack.filter(
      (o) => o.scenePath === n
    ).length;
    return this.emitter.emitAsync("onCanUndoStepsUpdate", t, this.viewId), this.emitter.emitAsync("onCanRedoStepsUpdate", i, this.viewId), i;
  }
  clear() {
    this.clearUndo(), this.clearRedo();
  }
  clearUndo() {
    this.undoStack.length = 0, this.emitter.emitAsync(
      "onCanUndoStepsUpdate",
      this.undoStack.length,
      this.viewId
    );
  }
  clearRedo() {
    this.redoStack.length = 0, this.emitter.emitAsync(
      "onCanRedoStepsUpdate",
      this.redoStack.length,
      this.viewId
    );
  }
  canUndo() {
    return !!this.undoStack.length;
  }
  canRedo() {
    return !!this.redoStack.length;
  }
  onFocusView() {
    const n = this.control.viewContainerManager.getCurScenePath(
      this.viewId
    );
    if (n) {
      const e = this.undoStack.filter(
        (i) => i.scenePath === n
      ).length, t = this.redoStack.filter(
        (i) => i.scenePath === n
      ).length;
      this.emitter.emitAsync("onCanUndoStepsUpdate", e, this.viewId), this.emitter.emitAsync("onCanRedoStepsUpdate", t, this.viewId);
    }
  }
  especialDiff(n, e, t, i) {
    if (e === Ve.Update)
      switch (t == null ? void 0 : t.toolsType) {
        case T.Text: {
          if (i != null && i.opt && (t != null && t.opt)) {
            const s = ti(i.opt, [
              "uid",
              "workState"
            ]), a = ti(t.opt, [
              "uid",
              "workState"
            ]);
            if (n && le(s, a))
              return !0;
            i.opt.workState !== x.Done && (i.opt.workState = x.Done), t.opt.workState !== x.Done && (t.opt.workState = x.Done);
          }
          break;
        }
      }
    else if (e === Ve.Draw || e === Ve.Delete)
      switch (t == null ? void 0 : t.toolsType) {
        case T.Text: {
          if (t != null && t.opt) {
            const s = t.opt.text;
            if (n && !s)
              return !0;
            t.opt.workState !== x.Done && (t.opt.workState = x.Done);
          }
          break;
        }
      }
    return !1;
  }
  diffFun(n, e, t = !0) {
    const i = /* @__PURE__ */ new Set(), o = n.keys(), s = e.keys();
    for (const a of o) {
      if (this.excludeIds.has(a))
        continue;
      const c = n.get(a), l = se(e.get(a));
      if (c && l) {
        if (le(l, c) || this.especialDiff(t, Ve.Update, l, c))
          continue;
        i.add({
          dataType: Ve.Update,
          key: a,
          data: [c, l]
        });
        continue;
      }
      this.especialDiff(t, Ve.Delete, c) || c && i.add({
        dataType: Ve.Delete,
        key: a,
        data: c
      });
    }
    for (const a of s) {
      if (this.excludeIds.has(a))
        continue;
      const c = se(e.get(a));
      if (c && !n.has(a)) {
        if (this.especialDiff(t, Ve.Draw, c))
          continue;
        i.add({
          dataType: Ve.Draw,
          key: a,
          data: c
        });
      }
    }
    return i;
  }
  isDrawEffectiveScene(n, e) {
    const { key: t } = n;
    return !e.includes(t);
  }
  isDeleteEffectiveScene(n, e) {
    const { key: t } = n;
    return !!e.includes(t);
  }
  isOldEffectiveScene(n, e) {
    const { key: t } = n;
    return !!e.includes(t);
  }
  isNewEffectiveScene(n, e) {
    const { key: t } = n;
    return !!e.includes(t);
  }
  refreshPlugin(n, e = !1) {
    var a, c, l, h, u, g, M, d, y, w, p, f, N, S, C, k, v, A, P, E, O;
    let t;
    const { scenePath: i } = n, o = n.data;
    if (!o || !this.collector)
      return;
    const s = se(this.collector.storage);
    for (const z of o.values()) {
      const { dataType: L, data: j, key: F } = z, X = this.collector.getStorageData(this.viewId, i), Y = X && Object.keys(X) || [];
      switch (L) {
        case Ve.Draw:
          if (t = e ? this.isDrawEffectiveScene(z, Y) : this.isDeleteEffectiveScene(z, Y), t)
            if (e && !Array.isArray(j)) {
              if ((a = j.updateNodeOpt) != null && a.useAnimation && (j.updateNodeOpt.useAnimation = !1), ((c = this.collector) == null ? void 0 : c.getLocalId(F)) === $ && ((l = this.collector) != null && l.isOwn(z.key))) {
                const Q = j.selectIds;
                if (Q) {
                  const _ = Y.filter(
                    (ie) => {
                      var K, H;
                      return ((K = this.collector) == null ? void 0 : K.getLocalId(ie)) === $ && !((H = this.collector) != null && H.isOwn(ie));
                    }
                  ).map(
                    (ie) => {
                      var K;
                      return (K = this.collector) == null ? void 0 : K.storage[this.viewId][i][ie];
                    }
                  );
                  let ce = !1;
                  for (const ie of _)
                    for (let K = 0; K < Q.length; K++)
                      (h = ie == null ? void 0 : ie.selectIds) != null && h.includes(Q[K]) && (delete Q[K], ce = !0);
                  ce && (j.selectIds = Q.filter((ie) => !!ie));
                }
              }
              (u = this.collector) == null || u.updateValue(z.key, j, {
                viewId: this.viewId,
                scenePath: i,
                isSync: !0,
                storage: s
              });
            } else !e && !Array.isArray(z.data) && ((g = this.collector) == null || g.updateValue(z.key, void 0, {
              viewId: this.viewId,
              scenePath: i,
              isSync: !0,
              storage: s
            }));
          break;
        case Ve.Delete:
          if (t = e ? this.isDeleteEffectiveScene(z, Y) : this.isDrawEffectiveScene(z, Y), t) {
            if (e && !Array.isArray(j))
              (M = this.collector) == null || M.updateValue(F, void 0, {
                viewId: this.viewId,
                scenePath: i,
                isSync: !0,
                storage: s
              });
            else if (!e && !Array.isArray(j)) {
              if ((d = j.updateNodeOpt) != null && d.useAnimation && (j.updateNodeOpt.useAnimation = !1), ((y = this.collector) == null ? void 0 : y.getLocalId(z.key)) === $ && ((w = this.collector) != null && w.isOwn(z.key))) {
                const Q = j.selectIds;
                if (Q) {
                  const _ = Y.filter(
                    (ie) => {
                      var K, H;
                      return ((K = this.collector) == null ? void 0 : K.getLocalId(ie)) === $ && !((H = this.collector) != null && H.isOwn(ie));
                    }
                  ).map(
                    (ie) => {
                      var K;
                      return (K = this.collector) == null ? void 0 : K.storage[this.viewId][i][ie];
                    }
                  );
                  let ce = !1;
                  for (const ie of _)
                    for (let K = 0; K < Q.length; K++)
                      (p = ie == null ? void 0 : ie.selectIds) != null && p.includes(Q[K]) && (delete Q[K], ce = !0);
                  ce && (j.selectIds = Q.filter((ie) => !!ie));
                }
              }
              (f = this.collector) == null || f.updateValue(z.key, z.data, {
                viewId: this.viewId,
                scenePath: i,
                isSync: !0,
                storage: s
              });
            }
          }
          break;
        case Ve.Update:
          if (t = e ? this.isNewEffectiveScene(z, Y) : this.isOldEffectiveScene(z, Y), t) {
            if (e && Array.isArray(j) && j.length === 2) {
              const Q = j[1];
              if ((N = Q.updateNodeOpt) != null && N.useAnimation && (Q.updateNodeOpt.useAnimation = !1), ((S = this.collector) == null ? void 0 : S.getLocalId(z.key)) === $ && ((C = this.collector) != null && C.isOwn(z.key))) {
                const _ = Q.selectIds;
                if (_) {
                  const ce = Y.filter(
                    (K) => {
                      var H, fe;
                      return ((H = this.collector) == null ? void 0 : H.getLocalId(K)) === $ && !((fe = this.collector) != null && fe.isOwn(K));
                    }
                  ).map(
                    (K) => {
                      var H;
                      return (H = this.collector) == null ? void 0 : H.storage[this.viewId][i][K];
                    }
                  );
                  let ie = !1;
                  for (const K of ce)
                    for (let H = 0; H < _.length; H++)
                      K != null && K.selectIds && ((k = K.selectIds) != null && k.includes(_[H])) && (delete _[H], ie = !0);
                  ie && (Q.selectIds = _.filter((K) => !!K));
                }
              }
              (v = this.collector) == null || v.updateValue(F, Q, {
                viewId: this.viewId,
                scenePath: i,
                isSync: !0,
                storage: s
              });
            } else if (!e && Array.isArray(j) && j.length === 2) {
              const Q = j[0];
              if ((A = Q.updateNodeOpt) != null && A.useAnimation && (Q.updateNodeOpt.useAnimation = !1), ((P = this.collector) == null ? void 0 : P.getLocalId(z.key)) === $ && ((E = this.collector) != null && E.isOwn(z.key))) {
                const _ = Q.selectIds;
                if (_) {
                  const ce = Y.filter(
                    (K) => {
                      var H, fe;
                      return ((H = this.collector) == null ? void 0 : H.getLocalId(K)) === $ && !((fe = this.collector) != null && fe.isOwn(K));
                    }
                  ).map(
                    (K) => {
                      var H;
                      return (H = this.collector) == null ? void 0 : H.storage[this.viewId][i][K];
                    }
                  );
                  let ie = !1;
                  for (const K of ce)
                    for (let H = 0; H < _.length; H++)
                      K != null && K.selectIds && K.selectIds.includes(_[H]) && (delete _[H], ie = !0);
                  ie && (Q.selectIds = _.filter((K) => !!K));
                }
              }
              (O = this.collector) == null || O.updateValue(z.key, Q, {
                viewId: this.viewId,
                scenePath: i,
                isSync: !0,
                storage: s
              });
            }
          }
          break;
      }
    }
  }
};
I(rt, "sdkCallBack"), I(rt, "MaxStackLength", Se.undoRedo.maxStackLength), I(rt, "waitTime", 100);
let bn = rt;
function op(r) {
  return r instanceof TouchEvent || r instanceof window.TouchEvent || (r == null ? void 0 : r.touches) && (r == null ? void 0 : r.touches.length) || (r == null ? void 0 : r.changedTouches) && (r == null ? void 0 : r.changedTouches.length);
}
function Jt(r) {
  return op(r) && (r.touches && r.touches.length === 1 || r.changedTouches && r.changedTouches.length === 1);
}
function Dr(r) {
  return r.touches && r.touches.length ? {
    x: r.touches[0].pageX,
    y: r.touches[0].pageY
  } : r.changedTouches && r.changedTouches.length ? {
    x: r.changedTouches[0].pageX,
    y: r.changedTouches[0].pageY
  } : {
    x: r.pageX,
    y: r.pageY
  };
}
class Et {
  constructor(n) {
    I(this, "internalMsgEmitter");
    I(this, "control");
    I(this, "mainView");
    I(this, "appViews", /* @__PURE__ */ new Map());
    I(this, "undoTickerStart", (n, e) => {
      const t = this.getView(e);
      t && t.displayer && t.focusScenePath && t.displayer.commiter.undoTickerStart(n, t.focusScenePath);
    });
    I(this, "undoTickerEnd", (n, e) => {
      const t = this.getView(e);
      t && t.displayer && t.focusScenePath && t.displayer.commiter.undoTickerEnd(n, t.focusScenePath);
    });
    I(this, "addExcludeIds", (n, e) => {
      const t = this.getView(e);
      t && t.displayer && t.focusScenePath && t.displayer.commiter.addExcludeIds(n);
    });
    const { control: e, internalMsgEmitter: t } = n;
    this.control = e, this.internalMsgEmitter = t, this.internalMsgEmitter.on("addUndoTicker", this.undoTickerStart), this.internalMsgEmitter.on("forceUndoTickerEnd", this.undoTickerEnd), this.internalMsgEmitter.on("excludeIds", this.addExcludeIds);
  }
  get worker() {
    return this.control.worker;
  }
  getUndoTickerId(n) {
    const e = this.getView(n);
    if (e && e.displayer && e.focusScenePath)
      return e.displayer.commiter.undoTickerId;
  }
  undo() {
    const n = this.focuedView;
    let e = 0;
    if (n) {
      const t = n.focusScenePath;
      e = n.displayer.commiter.undo(t) || 0;
    }
    return e;
  }
  redo() {
    const n = this.focuedView;
    let e = 0;
    if (n) {
      const t = n.focusScenePath;
      e = n.displayer.commiter.redo(t) || 0;
    }
    return e;
  }
  validator(n, e, t) {
    var s;
    const i = ni(n[e]), o = ni(t);
    e === "focusScenePath" && t && !le(i, o) && (this.control.internalSceneChange(n.id, o), (s = this.focuedView) == null || s.displayer.commiter.onChangeScene()), e === "cameraOpt" && !le(i, o) && this.control.internalCameraChange(n.id, o);
  }
  destroyAppView(n, e = !1) {
    const t = this.appViews.get(n);
    t && (this.control.textEditorManager.clear(n, e), t.displayer.destroy(), this.appViews.delete(n));
  }
  createMianView(n) {
    this.mainView = new Proxy(n, {
      set: (e, t, i) => (this.worker.isActive && this.validator(e, t, i), e[t] = i, !0)
    });
  }
  createAppView(n) {
    const e = n.id, t = new Proxy(n, {
      set: (i, o, s) => (this.worker.isActive && this.validator(i, o, s), i[o] = s, !0)
    });
    this.appViews.set(e, t);
  }
  isAppView(n) {
    return n !== pe && this.appViews.has(n);
  }
  getView(n) {
    var e;
    return n === pe ? this.mainView : (e = this.appViews) == null ? void 0 : e.get(n);
  }
  getCurScenePath(n) {
    const e = this.getView(n);
    if (e)
      return e.focusScenePath;
  }
  getAllViews() {
    return [this.mainView, ...this.appViews.values()];
  }
  setViewScenePath(n, e) {
    var t;
    if (n === pe && this.mainView)
      this.mainView.focusScenePath = e;
    else {
      const i = n && ((t = this.appViews) == null ? void 0 : t.get(n)) || void 0;
      i && (i.focusScenePath = e);
    }
  }
  setViewData(n, e) {
    var t;
    if (n === pe && this.mainView)
      this.mainView.viewData = e;
    else {
      const i = n && ((t = this.appViews) == null ? void 0 : t.get(n)) || void 0;
      i && (i.viewData = e);
    }
  }
  setFocuedViewId(n) {
    var e;
    this.focuedViewId = n, n === pe ? this.focuedView = this.mainView : this.focuedView = n && ((e = this.appViews) == null ? void 0 : e.get(n)) || void 0, this.worker.currentToolsData && this.worker.currentToolsData.toolsType === T.Text && this.activeTextEditor(n), this.control.cursor.onFocusViewChange(), this.focuedView && this.focuedView.displayer.commiter.onFocusView();
  }
  setViewFocusScenePath(n, e) {
    var i;
    let t;
    n === pe ? t = this.mainView : t = (i = this.appViews) == null ? void 0 : i.get(n), t && (t.focusScenePath = e);
  }
  /** 销毁 */
  destroy() {
    var n;
    this.internalMsgEmitter.off("addUndoTicker", this.undoTickerStart), this.internalMsgEmitter.off("excludeIds", this.addExcludeIds), this.internalMsgEmitter.off("forceUndoTickerEnd", this.undoTickerEnd), (n = this.mainView) == null || n.displayer.destroy(), this.control.destroyMiniMap(pe), this.appViews.forEach((e) => {
      this.destroyAppView(e.id, !0), this.worker.destroyViewWorker(e.id), this.control.destroyMiniMap(e.id);
    });
  }
  setFocuedViewCameraOpt(n) {
    if (this.focuedView) {
      const e = this.focuedView.cameraOpt;
      le(e, n) || (this.focuedView.cameraOpt = n);
    }
  }
  /** view中心点坐标转换成页面原始坐标 */
  transformToOriginPoint(n, e) {
    const t = this.getView(e);
    if (t != null && t.viewData) {
      const i = t.viewData.convertToPointOnScreen(n[0], n[1]);
      return [i.x, i.y];
    }
    return n;
  }
  /** 页面坐标转换成view中心点坐标 */
  transformToScenePoint(n, e) {
    const t = this.getView(e);
    if (t != null && t.viewData) {
      const i = t.viewData.convertToPointInWorld({ x: n[0], y: n[1] });
      return [i.x, i.y];
    }
    return n;
  }
  /** 绘制view */
  render(n) {
    var e, t, i, o, s, a, c, l, h, u, g, M, d, y, w, p, f;
    for (const N of n) {
      const {
        rect: S,
        imageBitmap: C,
        isClear: k,
        isUnClose: v,
        drawCanvas: A,
        clearCanvas: P,
        viewId: E
      } = N, O = (e = this.getView(E)) == null ? void 0 : e.displayer;
      if (O && S) {
        const {
          dpr: z,
          canvasBgRef: L,
          canvasFloatRef: j,
          canvasServiceFloatRef: F,
          canvasTopRef: X
        } = O, Y = Math.floor(S.w * z), Q = Math.floor(S.h * z), _ = S.x * z, ce = S.y * z;
        if (k) {
          switch (P) {
            case nt.TopFloat:
              (i = (t = X == null ? void 0 : X.current) == null ? void 0 : t.getContext("2d")) == null || i.clearRect(_, ce, Y, Q);
              break;
            case nt.Float:
              (s = (o = j.current) == null ? void 0 : o.getContext("2d")) == null || s.clearRect(_, ce, Y, Q);
              break;
            case nt.ServiceFloat:
              (c = (a = F.current) == null ? void 0 : a.getContext("2d")) == null || c.clearRect(_, ce, Y, Q);
              break;
            case nt.Bg:
              (h = (l = L.current) == null ? void 0 : l.getContext("2d")) == null || h.clearRect(_, ce, Y, Q);
              break;
          }
          continue;
        }
        if (A && C)
          switch (A) {
            case nt.TopFloat:
              (g = (u = X == null ? void 0 : X.current) == null ? void 0 : u.getContext("2d")) == null || g.drawImage(C, 0, 0, Y, Q, _, ce, Y, Q);
              break;
            case nt.Float: {
              (d = (M = j.current) == null ? void 0 : M.getContext("2d")) == null || d.drawImage(C, 0, 0, Y, Q, _, ce, Y, Q);
              break;
            }
            case nt.ServiceFloat: {
              (w = (y = F.current) == null ? void 0 : y.getContext("2d")) == null || w.drawImage(C, 0, 0, Y, Q, _, ce, Y, Q);
              break;
            }
            case nt.Bg: {
              (f = (p = L.current) == null ? void 0 : p.getContext("2d")) == null || f.drawImage(C, 0, 0, Y, Q, _, ce, Y, Q);
              break;
            }
          }
        if (v)
          continue;
        C == null || C.close();
      }
    }
  }
  /** 是否绘制浮动选框 */
  showFloatBar(n, e, t) {
    const i = this.getView(n), o = i == null ? void 0 : i.displayer.vDom;
    o && o.showFloatBar(e, t);
  }
  /** 激活文字编辑器 */
  activeTextEditor(n) {
    const e = this.getView(n), t = e == null ? void 0 : e.displayer.vDom;
    t && t.activeTextEditor();
  }
  /** 不激活文字编辑器 */
  unActiveTextEditor(n) {
    const e = this.getView(n), t = e == null ? void 0 : e.displayer.vDom;
    t && t.unActiveTextEditor();
  }
  /** 是否显示浮动选框按钮 */
  showFloatBarBtn(n, e) {
    const t = this.getView(n), i = t == null ? void 0 : t.displayer.vDom;
    i && i.showFloatBarBtn(e);
  }
  /** 更新文字编辑器信息 */
  updateTextEditorInfo(n, e) {
    const t = this.getView(n), i = t == null ? void 0 : t.displayer.vDom;
    i && i.updateTextEditorInfo(e);
  }
  setActiveMiniMap(n, e) {
    const t = this.getView(n), i = t == null ? void 0 : t.displayer.vDom;
    i && i.setActiveMiniMap(e && n || void 0);
  }
}
I(Et, "defaultCameraOpt", {
  centerX: 0,
  centerY: 0,
  scale: 1
}), I(Et, "defaultScreenCanvasOpt", {
  autoRender: !1,
  contextType: "2d"
}), I(Et, "defaultLayerOpt", {
  offscreen: !0,
  handleEvent: !1,
  depth: !1
});
class zr {
  constructor(n, e, t) {
    I(this, "control");
    I(this, "internalMsgEmitter");
    I(this, "commiter");
    // 公共私有属性
    I(this, "cachePoint");
    I(this, "cacheCursorPoint");
    I(this, "active", !0);
    I(this, "toolsKey");
    I(this, "activePointers", /* @__PURE__ */ new Set());
    // 用于追踪活动的指针ID，检测多指触点
    I(this, "currentDrawingPointerId");
    // 当前正在绘制的指针ID
    I(this, "cursorMouseMove");
    I(this, "cursorMouseMoveDebounced");
    I(this, "cursorMouseMoveThrottled");
    I(this, "cursorMouseMoveFun");
    I(this, "_transferControlToOffscreenCanvas");
    I(this, "cursorMouseLeave", () => {
      this.control.pluginOptions.extras.cursor.enable ? this.cursorMouseMoveThrottled.cancel() : this.cursorMouseMoveDebounced.cancel(), this.cacheCursorPoint = [void 0, void 0], this.worker.sendCursorEvent(this.cacheCursorPoint, this.viewId);
    });
    // 事件处理方法
    I(this, "mousedown", (n) => {
      var e;
      if (this.active && n.button === 0 && this.viewId) {
        this.toolsKey = (e = this.worker.getCurrentToolsData()) == null ? void 0 : e.toolsType, !this.worker.isAbled() && this.toolsKey && this.control.effectViewContainer(this.toolsKey), this.worker.checkDrawingWork(this.viewId), this.reflashContainerOffset();
        const t = this.getPoint(n);
        this.cachePoint = t, t && (this.worker.originalEventLintener(
          x.Start,
          t,
          this.viewId
        ), document.addEventListener("mousemove", this.mousemove, !1), document.addEventListener("mouseleave", this.mouseup, !1), document.addEventListener("mouseup", this.mouseup, !1));
      }
    });
    I(this, "mousemove", (n) => {
      if (this.active && this.viewId) {
        const e = this.getPoint(n);
        if (this.cachePoint = e, this.checkConsumeCount()) return;
        e && this.worker.originalEventLintener(
          x.Doing,
          e,
          this.viewId
        );
      }
    });
    I(this, "mouseup", (n) => {
      if (this.active && n.button === 0 && this.viewId) {
        const e = this.getPoint(n) || this.cachePoint;
        e ? (this.worker.originalEventLintener(x.Done, e, this.viewId).finally(() => {
          document.removeEventListener("mouseleave", this.mouseup, !1), document.removeEventListener("mousemove", this.mousemove, !1), document.removeEventListener("mouseup", this.mouseup, !1);
        }), this.cachePoint = void 0) : (document.removeEventListener("mouseleave", this.mouseup, !1), document.removeEventListener("mousemove", this.mousemove, !1), document.removeEventListener("mouseup", this.mouseup, !1));
      }
    });
    I(this, "touchstart", (n) => {
      var e;
      if (this.active) {
        if (!Jt(n)) {
          this.worker.unWritable(), this.viewId && this.worker.removeDrawingWork(this.viewId);
          return;
        }
        if (this.viewId) {
          this.toolsKey = (e = this.worker.getCurrentToolsData()) == null ? void 0 : e.toolsType, !this.worker.isAbled() && this.toolsKey && this.control.effectViewContainer(this.toolsKey), this.worker.checkDrawingWork(this.viewId), this.reflashContainerOffset();
          const t = this.getPoint(n);
          this.cachePoint = t, t && (this.worker.originalEventLintener(
            x.Start,
            t,
            this.viewId
          ), document.addEventListener("touchmove", this.touchmove, !1), document.addEventListener("touchend", this.touchend, !1));
        }
      }
    });
    I(this, "touchmove", (n) => {
      if (this.active) {
        if (!Jt(n)) {
          this.worker.unWritable(), this.worker.removeDrawingWork(this.viewId);
          return;
        }
        if (this.viewId) {
          const e = this.getPoint(n);
          if (this.cachePoint = e, this.checkConsumeCount()) return;
          e && this.worker.originalEventLintener(
            x.Doing,
            e,
            this.viewId
          );
        }
      }
    });
    I(this, "touchend", (n) => {
      if (this.active) {
        if (!Jt(n) || !this.worker.isAbled()) {
          this.worker.unWritable(), this.worker.removeDrawingWork(this.viewId);
          return;
        }
        if (this.viewId) {
          const e = this.getPoint(n) || this.cachePoint;
          e ? (this.worker.originalEventLintener(x.Done, e, this.viewId).finally(() => {
            document.removeEventListener("touchmove", this.touchmove, !1), document.removeEventListener("touchend", this.touchend, !1);
          }), this.cachePoint = void 0) : (document.removeEventListener("touchmove", this.touchmove, !1), document.removeEventListener("touchend", this.touchend, !1));
        }
      }
    });
    I(this, "keydown", (n) => {
      n.stopPropagation(), n.stopImmediatePropagation(), this.control.hotkeyManager.colloctHotkey(n);
    });
    I(this, "pointerdown", (n) => {
      var e;
      if (this.active) {
        if (n.pointerType === "touch" && (this.activePointers.add(n.pointerId), this.activePointers.size > 1)) {
          this.worker.unWritable(), this.viewId && this.worker.removeDrawingWork(this.viewId), document.removeEventListener("pointermove", this.pointermove, !1), document.removeEventListener("pointerup", this.pointerup, !1), document.removeEventListener("pointercancel", this.pointerup, !1), this.currentDrawingPointerId = void 0, this.cachePoint = void 0, this.activePointers.clear();
          return;
        }
        if (n.button === 0 && this.viewId) {
          this.toolsKey = (e = this.worker.getCurrentToolsData()) == null ? void 0 : e.toolsType, !this.worker.isAbled() && this.toolsKey && this.control.effectViewContainer(this.toolsKey), this.worker.checkDrawingWork(this.viewId), this.reflashContainerOffset();
          const t = this.getPoint(n);
          this.cachePoint = t, t && (this.currentDrawingPointerId = n.pointerId, this.worker.originalEventLintener(
            x.Start,
            t,
            this.viewId
          ), document.addEventListener("pointermove", this.pointermove, !1), document.addEventListener("pointerup", this.pointerup, !1), document.addEventListener("pointercancel", this.pointerup, !1));
        }
      }
    });
    I(this, "pointermove", (n) => {
      if (this.active) {
        if (n.pointerType === "touch") {
          if (this.activePointers.size > 1) {
            this.worker.unWritable(), this.worker.removeDrawingWork(this.viewId);
            return;
          }
          if (this.currentDrawingPointerId !== void 0 && n.pointerId !== this.currentDrawingPointerId)
            return;
          if (!this.worker.isAbled()) {
            this.worker.unWritable(), this.worker.removeDrawingWork(this.viewId);
            return;
          }
        }
        if (this.viewId) {
          const e = this.getPoint(n);
          if (this.cachePoint = e, this.checkConsumeCount()) return;
          e && this.worker.originalEventLintener(
            x.Doing,
            e,
            this.viewId
          );
        }
      }
    });
    I(this, "pointerup", (n) => {
      if (this.active && (n.pointerType === "touch" && this.activePointers.delete(n.pointerId), !(this.currentDrawingPointerId !== void 0 && n.pointerId !== this.currentDrawingPointerId))) {
        if (this.currentDrawingPointerId === void 0) {
          document.removeEventListener("pointermove", this.pointermove, !1), document.removeEventListener("pointerup", this.pointerup, !1), document.removeEventListener("pointercancel", this.pointerup, !1);
          return;
        }
        if (n.pointerType === "touch" && (this.activePointers.size > 0 || !this.worker.isAbled())) {
          this.worker.unWritable(), this.worker.removeDrawingWork(this.viewId), document.removeEventListener("pointermove", this.pointermove, !1), document.removeEventListener("pointerup", this.pointerup, !1), document.removeEventListener("pointercancel", this.pointerup, !1), this.cachePoint = void 0, this.currentDrawingPointerId = void 0;
          return;
        }
        if (n.button === 0 && this.viewId) {
          const e = this.getPoint(n) || this.cachePoint;
          e ? (this.worker.originalEventLintener(x.Done, e, this.viewId).finally(() => {
            document.removeEventListener(
              "pointermove",
              this.pointermove,
              !1
            ), document.removeEventListener("pointerup", this.pointerup, !1), document.removeEventListener(
              "pointercancel",
              this.pointerup,
              !1
            );
          }), this.cachePoint = void 0, this.currentDrawingPointerId = void 0) : (document.removeEventListener("pointermove", this.pointermove, !1), document.removeEventListener("pointerup", this.pointerup, !1), document.removeEventListener("pointercancel", this.pointerup, !1), this.currentDrawingPointerId = void 0);
        }
        n.pointerType === "touch" && this.activePointers.size === 0 && this.activePointers.clear();
      }
    });
    this.control = n, this.internalMsgEmitter = e;
    const i = {
      control: this.control,
      internalMsgEmitter: this.internalMsgEmitter,
      viewId: t,
      enableGlobal: !!this.control.pluginOptions.extras.undoRedo.enableGlobal
    };
    this.commiter = new bn(i), this.cursorMouseMoveFun = (o) => {
      const s = this.getPoint(o);
      this.cacheCursorPoint && le(s, this.cacheCursorPoint) || (this.cacheCursorPoint = s, s && this.worker.sendCursorEvent(s, this.viewId));
    }, this.cursorMouseMoveDebounced = Pt(
      this.cursorMouseMoveFun,
      this.moveDelayTime,
      { trailing: !0 }
    ), this.cursorMouseMoveThrottled = Hi(
      this.cursorMouseMoveFun,
      this.moveDelayTime,
      { leading: !0 }
    ), this.cursorMouseMove = (o) => {
      this.control.pluginOptions.extras.cursor.enable ? this.cursorMouseMoveThrottled(o) : this.cursorMouseMoveDebounced(o);
    }, this.setActive(n.renderControl.isWritable);
  }
  get moveDelayTime() {
    var n, e;
    return ((e = (n = this.control.pluginOptions.extras) == null ? void 0 : n.cursor) == null ? void 0 : e.moveDelayTime) ?? Se.cursor.moveDelayTime;
  }
  // 公共方法
  setTransferControlToOffscreenCanvas(n) {
    this._transferControlToOffscreenCanvas = {
      bgCanvas: n.bgCanvas.transferControlToOffscreen(),
      serviceCanvas: n.serviceCanvas.transferControlToOffscreen(),
      localCanvas: n.localCanvas.transferControlToOffscreen()
    };
  }
  get transferControlToOffscreenCanvas() {
    return this._transferControlToOffscreenCanvas;
  }
  get worker() {
    return this.control.worker;
  }
  bindToolsClass() {
    var i, o;
    const n = (o = (i = this.worker) == null ? void 0 : i.currentToolsData) == null ? void 0 : o.toolsType, e = this.control.renderControl.isWritable, t = this.worker.isAbled();
    switch (n) {
      case T.Text:
      case T.Pencil:
      case T.LaserPen:
      case T.Arrow:
      case T.Straight:
      case T.Rectangle:
      case T.Ellipse:
      case T.Star:
      case T.Polygon:
      case T.SpeechBalloon:
      case T.Eraser:
      case T.PencilEraser:
      case T.BitMapEraser:
      case T.Selector:
        this.eventTragetElement && (this.eventTragetElement.className = e && t ? `netless-whiteboard ${n === T.Selector ? "cursor-selector" : n === T.Text ? "cursor-text" : n === T.Eraser || n === T.PencilEraser || n === T.BitMapEraser ? "cursor-eraser" : n === T.Pencil || n === T.LaserPen ? "cursor-pencil" : "cursor-arrow"}` : "netless-whiteboard");
        break;
    }
  }
  mountView() {
    this.setCanvassStyle(), this.control.viewContainerManager.mountView(this.viewId);
  }
  updateSize() {
    this.setCanvassStyle(), this.reflashContainerOffset();
  }
  reflashContainerOffset() {
    this.eventTragetElement && (this.containerOffset = this.getContainerOffset(this.eventTragetElement, {
      x: 0,
      y: 0
    }));
  }
  get parentElement() {
    var n;
    return (n = this.eventTragetElement) == null ? void 0 : n.parentElement;
  }
  destroy() {
    this.eventTragetElement && this.removeDisplayerEvent(this.eventTragetElement), this.vDom = void 0, this.control.cursor.clearViewCursor(this.viewId), this.internalMsgEmitter.removeAllListeners([
      te.Cursor,
      this.viewId
    ]);
  }
  getPoint(n) {
    const e = Dr(n);
    if (e && ee(e.x) && ee(e.y))
      return [
        e.x - this.containerOffset.x,
        e.y - this.containerOffset.y
      ];
  }
  getActive() {
    return this.active;
  }
  setActive(n) {
    this.active = n, this.control.isUseBackgroundThread && this.backgroundContainerRef && this.backgroundContainerRef.current && this.backgroundContainerRef.current.classList.toggle(
      "appliance-plugin-background-isWritable",
      n
    );
  }
  focusElement() {
    var n;
    (n = this.eventTragetElement) == null || n.focus();
  }
  async stopEventHandler() {
    this.cachePoint && (await this.worker.originalEventLintener(
      x.Done,
      this.cachePoint,
      this.viewId
    ).finally(() => {
      document.removeEventListener("pointermove", this.pointermove, !1), document.removeEventListener("pointerup", this.pointerup, !1), document.removeEventListener("pointercancel", this.pointerup, !1);
    }), this.cachePoint = void 0, this.activePointers.clear(), this.currentDrawingPointerId = void 0);
  }
  getTranslate(n) {
    const t = (n.style.WebkitTransform || getComputedStyle(n, "").getPropertyValue("-webkit-transform") || n.style.transform || getComputedStyle(n, "").getPropertyValue("transform")).match(/-?[0-9]+\.?[0-9]*/g), i = t && parseInt(t[0]) || 0, o = t && parseInt(t[1]) || 0;
    return [i, o];
  }
  getContainerOffset(n, e) {
    var o;
    const t = this.getTranslate(n);
    let i = {
      x: e.x + n.offsetLeft - n.scrollLeft + t[0],
      y: e.y + n.offsetTop - n.scrollTop + t[1]
    };
    return (o = n.offsetParent) != null && o.nodeName && n.offsetParent.nodeName !== "BODY" && (i = this.getContainerOffset(
      n.offsetParent,
      i
    )), i;
  }
  bindDisplayerEvent(n) {
    n.addEventListener("pointerdown", this.pointerdown, !1), n.addEventListener("mousemove", this.cursorMouseMove, !1), n.addEventListener("mouseleave", this.cursorMouseLeave, !1), n.addEventListener("keydown", this.keydown, !0);
  }
  removeDisplayerEvent(n) {
    n.removeEventListener("pointerdown", this.pointerdown, !1), document.removeEventListener("pointermove", this.pointermove, !1), document.removeEventListener("pointerup", this.pointerup, !1), document.removeEventListener("pointerleave", this.pointerup, !1), n.removeEventListener("mousemove", this.cursorMouseMove, !1), n.removeEventListener("mouseleave", this.cursorMouseLeave, !1), n.removeEventListener("keydown", this.keydown, !0);
  }
  // 公共方法 - 从子类提取
  get isBezier() {
    return this.control.pluginOptions.extras.bezier.enable;
  }
  get maxDrawCount() {
    return this.control.pluginOptions.extras.bezier.maxDrawCount || Se.bezier.maxDrawCount;
  }
  /** 检查是否超过最大绘制数量 */
  checkConsumeCount() {
    const n = this.worker.getConsumeCount();
    return this.cachePoint && this.toolsKey && this.toolsKey === T.Pencil && n > this.maxDrawCount ? (this.stopEventHandler().finally(() => {
      this.worker.publicMsgEmitter.emit(
        "forceStop",
        Ji.longPencil,
        this.viewId
      );
    }), !0) : !1;
  }
}
class sp extends zr {
  constructor(e, t, i) {
    super(t, i, e);
    I(this, "_viewId");
    this._viewId = e;
  }
  get viewId() {
    return this._viewId;
  }
  setViewId(e) {
    this._viewId = e;
  }
}
class br extends zr {
  constructor(n, e) {
    super(n, e, pe);
  }
  get viewId() {
    return pe;
  }
  updateSize() {
    this.setCanvassStyle();
  }
  destroy() {
    super.destroy(), this.control.cursor.destroy();
  }
}
const ap = "index-module__Container__nLsM3", cp = "index-module__BackgroundBox__zfthZ", lp = "index-module__BackgroundSvgBox__Fv1ay", up = "index-module__BackgroundSnapshotContent__NFLAl", hp = "index-module__BackgroundSnapshotBox__NEM-C", dp = "index-module__CanvasBox__j2Xe-", gp = "index-module__FloatCanvas__d1YR7", fp = "index-module__TopFloatCanvas__7IaCw", Mp = "index-module__CanvasBoxSub__hcMeK", pp = "index-module__SnapshotBox__--eeE", yp = "index-module__FloatBar__cm-EL", Ip = "index-module__RotateBtn__HSSkf", mp = "index-module__ResizeBtn__yjvda", wp = "index-module__CursorBox__2UHvI", Tp = "index-module__TextEditorContainer__Qm8KC", Np = "index-module__ResizeTowBox__HOllX", vp = "index-module__FloatBarBtn__FJrOG", De = {
  Container: ap,
  BackgroundBox: cp,
  BackgroundSvgBox: lp,
  BackgroundSnapshotContent: up,
  BackgroundSnapshotBox: hp,
  CanvasBox: dp,
  FloatCanvas: gp,
  TopFloatCanvas: fp,
  CanvasBoxSub: Mp,
  SnapshotBox: pp,
  FloatBar: yp,
  RotateBtn: Ip,
  ResizeBtn: mp,
  CursorBox: wp,
  TextEditorContainer: Tp,
  ResizeTowBox: Np,
  FloatBarBtn: vp
}, Cp = {
  delete: "data:image/svg+xml;base64,",
  duplicate: "data:image/svg+xml;base64,",
  "layer-pressed": "data:image/svg+xml;base64,PHN2ZyB3aWR0aD0iMzIiIGhlaWdodD0iMzIiIHhtbG5zPSJodHRwOi8vd3d3LnczLm9yZy8yMDAwL3N2ZyI+PHBhdGggZD0ibTE2IDEwLTYgNCA2IDQgNi00em0tNiA4IDYgNCA2LTQiIHN0cm9rZT0iIzMzODFGRiIgZmlsbD0ibm9uZSIgc3Ryb2tlLWxpbmVjYXA9InJvdW5kIiBzdHJva2UtbGluZWpvaW49InJvdW5kIi8+PC9zdmc+",
  layer: "data:image/svg+xml;base64,PHN2ZyB3aWR0aD0iMzIiIGhlaWdodD0iMzIiIHhtbG5zPSJodHRwOi8vd3d3LnczLm9yZy8yMDAwL3N2ZyI+PHBhdGggZD0ibTE2IDEwLTYgNCA2IDQgNi00em0tNiA4IDYgNCA2LTQiIHN0cm9rZT0iIzQ0NEU2MCIgZmlsbD0ibm9uZSIgc3Ryb2tlLWxpbmVjYXA9InJvdW5kIiBzdHJva2UtbGluZWpvaW49InJvdW5kIi8+PC9zdmc+",
  rotate: "data:image/svg+xml;base64,",
  "rotation-button": "data:image/svg+xml;base64,",
  rotation: "data:image/svg+xml;base64,",
  "font-colors-active": "data:image/svg+xml;base64,PHN2ZyB3aWR0aD0iMTYiIGhlaWdodD0iMTYiIHhtbG5zPSJodHRwOi8vd3d3LnczLm9yZy8yMDAwL3N2ZyI+PHBhdGggZD0ibTQgMTEgNC05IDQgOU02IDdoNCIgc3Ryb2tlPSIjMzM4MUZGIiBmaWxsPSJub25lIiBzdHJva2UtbGluZWNhcD0icm91bmQiIHN0cm9rZS1saW5lam9pbj0icm91bmQiLz48L3N2Zz4=",
  "font-colors": "data:image/svg+xml;base64,PHN2ZyB3aWR0aD0iMTYiIGhlaWdodD0iMTYiIHhtbG5zPSJodHRwOi8vd3d3LnczLm9yZy8yMDAwL3N2ZyI+PHBhdGggZD0ibTQgMTEgNC05IDQgOU02IDdoNCIgc3Ryb2tlPSIjNDQ0RTYwIiBmaWxsPSJub25lIiBzdHJva2UtbGluZWNhcD0icm91bmQiIHN0cm9rZS1saW5lam9pbj0icm91bmQiLz48L3N2Zz4=",
  "to-bottom": "data:image/svg+xml;base64,",
  "to-top": "data:image/svg+xml;base64,",
  "bold-active": "data:image/svg+xml;base64,",
  bold: "data:image/svg+xml;base64,",
  "underline-active": "data:image/svg+xml;base64,PHN2ZyB3aWR0aD0iMTYiIGhlaWdodD0iMTYiIHhtbG5zPSJodHRwOi8vd3d3LnczLm9yZy8yMDAwL3N2ZyI+PHBhdGggZD0iTTEyIDJ2NWE0IDQgMCAxIDEtOCAwVjJoME0zIDE0aDEwIiBzdHJva2U9IiMzMzgxRkYiIGZpbGw9Im5vbmUiIHN0cm9rZS1saW5lY2FwPSJyb3VuZCIgc3Ryb2tlLWxpbmVqb2luPSJyb3VuZCIvPjwvc3ZnPg==",
  underline: "data:image/svg+xml;base64,PHN2ZyB3aWR0aD0iMTYiIGhlaWdodD0iMTYiIHhtbG5zPSJodHRwOi8vd3d3LnczLm9yZy8yMDAwL3N2ZyI+PHBhdGggZD0iTTEyIDJ2NWE0IDQgMCAxIDEtOCAwVjJoME0zIDE0aDEwIiBzdHJva2U9IiM0NDRFNjAiIGZpbGw9Im5vbmUiIHN0cm9rZS1saW5lY2FwPSJyb3VuZCIgc3Ryb2tlLWxpbmVqb2luPSJyb3VuZCIvPjwvc3ZnPg==",
  "line-through-active": "data:image/svg+xml;base64,",
  "line-through": "data:image/svg+xml;base64,",
  "italic-active": "data:image/svg+xml;base64,PHN2ZyB3aWR0aD0iMTYiIGhlaWdodD0iMTYiIHhtbG5zPSJodHRwOi8vd3d3LnczLm9yZy8yMDAwL3N2ZyI+PHBhdGggZD0iTTcgMTQgOSAyTTUgMTRoNE03IDJoNCIgc3Ryb2tlPSIjMzM4MUZGIiBmaWxsPSJub25lIiBzdHJva2UtbGluZWNhcD0icm91bmQiIHN0cm9rZS1saW5lam9pbj0icm91bmQiLz48L3N2Zz4=",
  italic: "data:image/svg+xml;base64,PHN2ZyB3aWR0aD0iMTYiIGhlaWdodD0iMTYiIHhtbG5zPSJodHRwOi8vd3d3LnczLm9yZy8yMDAwL3N2ZyI+PHBhdGggZD0iTTcgMTQgOSAyTTUgMTRoNE03IDJoNCIgc3Ryb2tlPSIjNDQ0RTYwIiBmaWxsPSJub25lIiBzdHJva2UtbGluZWNhcD0icm91bmQiIHN0cm9rZS1saW5lam9pbj0icm91bmQiLz48L3N2Zz4=",
  "unlock-new": "data:image/svg+xml;base64,",
  "lock-new": "data:image/svg+xml;base64,",
  shapes: "data:image/svg+xml;base64,",
  "shapes-active": "data:image/svg+xml;base64,",
  "font-style-active": "data:image/svg+xml;base64,",
  "font-style": "data:image/svg+xml;base64,",
  polygon: "data:image/svg+xml;base64,",
  "polygon-active": "data:image/svg+xml;base64,",
  "polygon-vertex": "data:image/svg+xml;base64,",
  star: "data:image/svg+xml;base64,",
  "star-active": "data:image/svg+xml;base64,",
  "star-innerVertex": "data:image/svg+xml;base64,",
  "star-innerRatio": "data:image/svg+xml;base64,",
  speechBallon: "data:image/svg+xml;base64,",
  "speechBallon-active": "data:image/svg+xml;base64,",
  "speechBallon-placement": "data:image/svg+xml;base64,"
};
function Le(r) {
  return Cp[r];
}
const Sp = (r) => {
  const { workIds: n, mananger: e } = r;
  return /* @__PURE__ */ m.createElement(
    "div",
    {
      className: "button normal-button",
      style: { touchAction: "none" },
      onPointerUp: (t) => {
        t.cancelable && t.preventDefault(), t.stopPropagation(), t.nativeEvent.stopImmediatePropagation(), re.emitMethod(
          te.MainEngine,
          W.DeleteNode,
          {
            workIds: n || [$],
            viewId: e.viewId
          }
        );
      }
    },
    /* @__PURE__ */ m.createElement("img", { alt: "icon", src: Le("delete") })
  );
}, kp = (r) => {
  const { workIds: n, viewId: e } = r;
  return /* @__PURE__ */ m.createElement(
    "div",
    {
      className: "button normal-button",
      onPointerUp: (t) => {
        t.stopPropagation(), re.emitMethod(
          te.MainEngine,
          W.CopyNode,
          { workIds: n || [$], viewId: e }
        );
      }
    },
    /* @__PURE__ */ m.createElement("img", { alt: "icon", src: Le("duplicate") })
  );
}, Yi = (r) => {
  const { icon: n, onPointerUpHandler: e } = r;
  return /* @__PURE__ */ m.createElement("div", { className: "button normal-button", onPointerUp: e }, /* @__PURE__ */ m.createElement("img", { src: Le(n) }));
}, xp = (r) => {
  const { open: n, setOpen: e, floatBarRef: t } = r, { floatBarData: i, mananger: o } = Ce(Te), [s, a] = ae([]), c = J(() => {
    if (t != null && t.current && ee(i == null ? void 0 : i.y) && (o != null && o.height)) {
      if (t.current.offsetTop && t.current.offsetTop + i.y > 180) {
        const u = {};
        return u.top = "inherit", u.bottom = 50, u;
      } else if (!t.current.offsetTop && (o == null ? void 0 : o.height) - t.current.offsetTop - i.y < 120) {
        const u = {};
        return u.top = "inherit", u.bottom = 50, u;
      }
    }
  }, [t == null ? void 0 : t.current, i, o]), l = J(() => n ? /* @__PURE__ */ m.createElement("div", { className: "image-layer-menu", style: c }, /* @__PURE__ */ m.createElement(
    Yi,
    {
      icon: "to-top",
      onPointerUpHandler: (u) => {
        u.stopPropagation(), re.emitMethod(
          te.MainEngine,
          W.ZIndexNode,
          {
            workIds: [$],
            layer: Lt.Top,
            viewId: o == null ? void 0 : o.viewId
          }
        );
      }
    }
  ), /* @__PURE__ */ m.createElement(
    Yi,
    {
      icon: "to-bottom",
      onPointerUpHandler: (u) => {
        u.stopPropagation(), re.emitMethod(
          te.MainEngine,
          W.ZIndexNode,
          {
            workIds: [$],
            layer: Lt.Bottom,
            viewId: o == null ? void 0 : o.viewId
          }
        );
      }
    }
  )) : null, [n, c]), h = (u) => {
    u.stopPropagation(), u.nativeEvent.stopImmediatePropagation(), e(!n);
  };
  return ge(() => {
    le(i == null ? void 0 : i.selectIds, s) || i != null && i.selectIds && !le(i == null ? void 0 : i.selectIds, s) && (a(i == null ? void 0 : i.selectIds), e(!1));
  }, [n, i, s, e]), /* @__PURE__ */ m.createElement(
    "div",
    {
      className: `button normal-button ${n && "active"}`,
      onPointerUp: h
    },
    l,
    /* @__PURE__ */ m.createElement("img", { alt: "icon", src: Le(n ? "layer-pressed" : "layer") })
  );
}, Ut = (r) => {
  r.stopPropagation(), r.nativeEvent.stopImmediatePropagation();
}, jp = () => /* @__PURE__ */ m.createElement(
  "svg",
  {
    style: { marginLeft: "2px" },
    viewBox: "0 0 1025 1024",
    width: "14",
    height: "14"
  },
  /* @__PURE__ */ m.createElement(
    "path",
    {
      d: "M1016.5248 493.8752c-9.984-9.984-26.2144-9.984-36.1984 0l-183.6032 183.6032c-29.952 29.952-78.6944 29.952-108.5952 0l-239.2064-239.2064c-14.4384-14.4384-22.3744-33.6896-22.3744-54.3232s7.936-39.8848 22.3744-54.3232l183.6032-183.6032c9.984-9.984 9.984-26.2144 0-36.1984s-26.2144-9.984-36.1984 0l-183.6032 183.6032c-24.1152 24.1152-37.376 56.2176-37.376 90.5216 0 14.592 2.4576 28.8256 7.0656 42.1888l-374.8864 374.8864c-4.8128 4.8128-7.4752 11.3152-7.4752 18.1248l0 76.8c0 14.1312 11.4688 25.6 25.6 25.6l486.4 0c6.8096 0 13.312-2.7136 18.1248-7.4752l170.0864-170.0864c13.3632 4.6592 27.5968 7.0656 42.1888 7.0656 34.2528 0 66.4064-13.2608 90.5216-37.376l183.6032-183.6032c9.984-9.984 9.984-26.2144 0-36.1984zM501.4016 870.4l-450.2016 0 0-40.6016 358.5024-358.5024c1.024 1.0752 1.9968 2.1504 3.0208 3.1744l239.2064 239.2064c1.024 1.024 2.0992 2.048 3.1744 3.0208l-153.7024 153.7024z",
      fill: "#000000"
    }
  )
), Ar = (r) => {
  const { activeColor: n, onPointerUpHandler: e } = r;
  return /* @__PURE__ */ m.createElement(
    "div",
    {
      className: `font-color-button ${n === "transparent" ? "active" : ""}`,
      onPointerUp: e
    },
    /* @__PURE__ */ m.createElement("div", { className: "circle none" })
  );
}, dn = (r) => {
  const { color: n, activeColor: e, onPointerUpHandler: t } = r;
  return /* @__PURE__ */ m.createElement(
    "div",
    {
      className: `font-color-button ${n === e ? "active" : ""}`,
      onPointerUp: t
    },
    /* @__PURE__ */ m.createElement(
      "div",
      {
        className: "circle",
        style: { backgroundColor: Ae(n, 1) }
      }
    )
  );
}, gn = (r) => {
  const { opacity: n, activeColor: e, setCurOpacity: t } = r, [i, o] = ae({ x: 108, y: 0 }), s = xe(0);
  if (ge(() => {
    o({ x: n * 100 + 8, y: 0 });
  }, []), !e)
    return null;
  const a = (u, g) => {
    const M = Math.min(Math.max(u - 8, 0), 100) / 100;
    (n !== M || g === x.Done) && t(M, e, g);
  }, c = (u, g) => {
    g.x !== (i == null ? void 0 : i.x) && o({ x: g.x, y: 0 }), !s.current && (s.current = setTimeout(() => {
      s.current = 0, a(g.x, x.Doing);
    }, 100));
  }, l = () => {
    t(n, e, x.Start);
  }, h = (u, g) => {
    g.x !== (i == null ? void 0 : i.x) && o({ x: g.x, y: 0 }), s.current && (clearTimeout(s.current), s.current = 0), a(g.x, x.Done);
  };
  return /* @__PURE__ */ m.createElement(
    "div",
    {
      className: "font-color-opacity",
      style: { marginLeft: "10px" },
      onPointerUp: (u) => {
        s.current && (clearTimeout(s.current), s.current = 0);
        const g = u.nativeEvent.offsetX, M = Math.min(Math.max(g - 12, 0), 100) / 100;
        o({ x: M * 100 + 8, y: 0 }), t(M, e, x.Done);
      }
    },
    /* @__PURE__ */ m.createElement(
      "div",
      {
        className: "range-color",
        style: {
          background: `linear-gradient(to right, ${Ae(e, 0)}, ${Ae(e, 1)})`
        }
      }
    ),
    /* @__PURE__ */ m.createElement("div", { className: "range-opacity" }, /* @__PURE__ */ m.createElement(
      vt,
      {
        bounds: "parent",
        axis: "x",
        position: i,
        onDrag: c,
        onStart: l,
        onStop: h
      },
      /* @__PURE__ */ m.createElement(
        "div",
        {
          className: "circle",
          style: {
            backgroundColor: Ae(e, n)
          },
          onPointerUp: Ut
        }
      )
    ))
  );
}, Dp = (r) => {
  const { min: n, max: e, activeNumber: t, setActiveNumber: i, color: o } = r, [s, a] = ae({ x: 108, y: 0 }), c = xe(0);
  if (ge(() => {
    const M = Math.ceil((t - n) / (e - n) * 100) + 8;
    a({ x: M, y: 0 });
  }, []), !t)
    return null;
  const l = (M, d) => {
    const y = Math.ceil(Math.max(M - 8, 0) / 100 * (e - n) + n);
    (t !== y || d === x.Done) && i(y, d);
  }, h = (M, d) => {
    d.x !== (s == null ? void 0 : s.x) && a({ x: d.x, y: 0 }), !c.current && (c.current = setTimeout(() => {
      c.current = 0, l(d.x, x.Doing);
    }, 100));
  }, u = () => {
    i(t, x.Start);
  }, g = (M, d) => {
    d.x !== (s == null ? void 0 : s.x) && a({ x: d.x, y: 0 }), c.current && (clearTimeout(c.current), c.current = 0), l(d.x, x.Done);
  };
  return /* @__PURE__ */ m.createElement(
    "div",
    {
      className: "font-color-opacity",
      style: { marginLeft: "10px" },
      onPointerUp: (M) => {
        c.current && (clearTimeout(c.current), c.current = 0);
        const d = M.nativeEvent.offsetX, y = Math.ceil(
          Math.max(d - 12, 0) / 100 * (e - n) + n
        ), w = Math.ceil((y - n) / (e - n) * 100) + 8;
        a({ x: w, y: 0 }), i(y, x.Done);
      }
    },
    /* @__PURE__ */ m.createElement(
      "div",
      {
        className: "range-color",
        style: {
          backgroundColor: o
        }
      }
    ),
    /* @__PURE__ */ m.createElement("div", { className: "range-opacity" }, /* @__PURE__ */ m.createElement(
      vt,
      {
        bounds: "parent",
        axis: "x",
        position: s,
        onDrag: h,
        onStart: u,
        onStop: g
      },
      /* @__PURE__ */ m.createElement(
        "div",
        {
          className: "circle",
          style: {
            backgroundColor: o
          },
          onPointerUp: Ut
        },
        /* @__PURE__ */ m.createElement("div", { className: "range-opacity-data-icon" }, t)
      )
    ))
  );
}, zp = (r) => {
  const { useStroke: n, color: e, activeStrokeType: t, setStrokeType: i } = r;
  return /* @__PURE__ */ m.createElement("div", { className: "stroke-type-box" }, !!n && /* @__PURE__ */ m.createElement(
    "div",
    {
      className: `stroke-type-box-item Stroke ${t === me.Stroke && "active"}`,
      style: { backgroundColor: e },
      onPointerUp: () => {
        i(me.Stroke);
      }
    }
  ), /* @__PURE__ */ m.createElement(
    "div",
    {
      className: `stroke-type-box-item Normal ${t === me.Normal && "active"}`,
      style: { borderColor: e },
      onPointerUp: () => {
        i(me.Normal);
      }
    }
  ), /* @__PURE__ */ m.createElement(
    "div",
    {
      className: `stroke-type-box-item Dotted ${t === me.Dotted && "active"}`,
      style: { borderColor: e },
      onPointerUp: () => {
        i(me.Dotted);
      }
    }
  ), /* @__PURE__ */ m.createElement(
    "div",
    {
      className: `stroke-type-box-item LongDotted ${t === me.LongDotted && "active"}`,
      style: { borderColor: e },
      onPointerUp: () => {
        i(me.LongDotted);
      }
    }
  ));
}, bp = (r) => {
  const { open: n, setOpen: e, floatBarRef: t } = r, { floatBarData: i, floatBarColors: o, mananger: s, setFloatBarData: a } = Ce(Te), [c, l] = ae(), [h, u] = ae(1);
  ge(() => {
    if (i != null && i.strokeColor) {
      const [f, N] = hn(i.strokeColor);
      l(f), u(N);
    }
  }, [i]);
  const g = J(() => {
    if (t != null && t.current && i && ee(i == null ? void 0 : i.y) && (s != null && s.height)) {
      if (t.current.offsetTop && t.current.offsetTop + i.y > 180) {
        const f = {};
        return f.top = "inherit", f.bottom = 50, f;
      } else if (!t.current.offsetTop && (s == null ? void 0 : s.height) - t.current.offsetTop - i.y < 120) {
        const f = {};
        return f.top = "inherit", f.bottom = 50, f;
      }
    }
  }, [i, t, s == null ? void 0 : s.height]), M = J(() => /* @__PURE__ */ m.createElement(
    gn,
    {
      key: "strokeColors",
      opacity: h,
      activeColor: c,
      setCurOpacity: (f, N, S) => {
        u(f);
        const C = Ae(N, f);
        i != null && i.strokeColor && a({ strokeColor: C }), re.emitMethod(
          te.MainEngine,
          W.SetColorNode,
          {
            workIds: [$],
            strokeColor: C,
            workState: S,
            viewId: s == null ? void 0 : s.viewId
          }
        );
      }
    }
  ), [
    h,
    c,
    i == null ? void 0 : i.strokeColor,
    s == null ? void 0 : s.viewId,
    a
  ]), d = J(() => c && i && i.thickness ? /* @__PURE__ */ m.createElement(
    Dp,
    {
      key: "strokeThickness",
      min: Math.min(
        i.thickness,
        (s == null ? void 0 : s.control.pluginOptions.extras.strokeWidth.min) || Se.strokeWidth.min
      ),
      max: Math.max(
        i.thickness,
        (s == null ? void 0 : s.control.pluginOptions.extras.strokeWidth.max) || Se.strokeWidth.max
      ),
      activeNumber: i.thickness,
      setActiveNumber: (f, N) => {
        i != null && i.thickness && a({ thickness: f }), re.emitMethod(
          te.MainEngine,
          W.SetColorNode,
          {
            workIds: [$],
            thickness: f,
            workState: N,
            viewId: s == null ? void 0 : s.viewId
          }
        );
      },
      color: c
    }
  ) : null, [
    c,
    i,
    s == null ? void 0 : s.control.pluginOptions.extras.strokeWidth.max,
    s == null ? void 0 : s.control.pluginOptions.extras.strokeWidth.min,
    s == null ? void 0 : s.viewId,
    a
  ]), y = J(() => c && i && (i != null && i.strokeType) ? /* @__PURE__ */ m.createElement(
    zp,
    {
      key: "strokeType",
      useStroke: i == null ? void 0 : i.useStroke,
      activeStrokeType: i.strokeType,
      setStrokeType: (f) => {
        i.strokeType && a({ strokeType: f }), re.emitMethod(
          te.MainEngine,
          W.SetColorNode,
          {
            workIds: [$],
            strokeType: f,
            workState: x.Done,
            viewId: s == null ? void 0 : s.viewId
          }
        );
      },
      color: c
    }
  ) : null, [c, i, s == null ? void 0 : s.viewId, a]), w = J(() => n ? /* @__PURE__ */ m.createElement(
    "div",
    {
      className: "font-colors-menu",
      style: g,
      onPointerUp: (f) => {
        f.stopPropagation(), f.nativeEvent.stopImmediatePropagation();
      }
    },
    y,
    d,
    /* @__PURE__ */ m.createElement("div", { className: "font-colors-line" }),
    o.concat().map((f, N) => {
      const S = Yt(...f);
      return /* @__PURE__ */ m.createElement(
        dn,
        {
          key: N,
          color: S,
          activeColor: c,
          onPointerUpHandler: (C) => {
            C.stopPropagation(), l(S);
            const k = Ae(S, h);
            i != null && i.strokeColor && (i.strokeColor = k, a({ strokeColor: k })), re.emitMethod(
              te.MainEngine,
              W.SetColorNode,
              {
                workIds: [$],
                workState: x.Done,
                strokeColor: k,
                viewId: s == null ? void 0 : s.viewId
              }
            );
          }
        }
      );
    }),
    M
  ) : null, [
    n,
    g,
    y,
    d,
    o,
    M,
    c,
    h,
    i,
    s == null ? void 0 : s.viewId,
    a
  ]), p = J(() => c ? /* @__PURE__ */ m.createElement(
    "div",
    {
      className: "color-bar-ring",
      style: { backgroundColor: Ae(c, h) }
    },
    /* @__PURE__ */ m.createElement("div", { className: "circle" })
  ) : null, [c, h]);
  return /* @__PURE__ */ m.createElement(
    "div",
    {
      className: `button normal-button font-colors-icon ${n && "active"}`,
      onPointerUp: (f) => {
        f.stopPropagation(), f.nativeEvent.stopImmediatePropagation(), e(!n);
      }
    },
    p,
    w
  );
}, Ap = (r) => {
  const { open: n, setOpen: e, floatBarRef: t } = r, { floatBarData: i, floatBarColors: o, mananger: s, setFloatBarData: a } = Ce(Te), [c, l] = ae(), [h, u] = ae(1);
  ge(() => {
    if (i != null && i.fillColor) {
      const [w, p] = (i == null ? void 0 : i.fillColor) === "transparent" && ["transparent", 1] || hn(i.fillColor);
      l(w), u(p);
    }
  }, [i]);
  const g = J(() => {
    if (t != null && t.current && ee(i == null ? void 0 : i.y) && (s != null && s.height)) {
      if (t.current.offsetTop && t.current.offsetTop + i.y > 200) {
        const w = {};
        return w.top = "inherit", w.bottom = 50, w;
      } else if (!t.current.offsetTop && (s == null ? void 0 : s.height) - t.current.offsetTop - i.y < 140) {
        const w = {};
        return w.top = "inherit", w.bottom = 50, w;
      }
    }
  }, [t, i, s == null ? void 0 : s.height]), M = J(() => c && c !== "transparent" ? /* @__PURE__ */ m.createElement(
    gn,
    {
      key: "fillColors",
      opacity: h || 0,
      activeColor: c,
      setCurOpacity: (w, p, f) => {
        u(w);
        const N = Ae(p, w);
        i != null && i.fillColor && (i.fillColor = N, a({ fillColor: N })), re.emitMethod(
          te.MainEngine,
          W.SetColorNode,
          {
            workIds: [$],
            fillColor: c && Ae(p, w),
            workState: f,
            viewId: s == null ? void 0 : s.viewId
          }
        );
      }
    }
  ) : null, [c, h, i, s == null ? void 0 : s.viewId, a]), d = J(() => n ? /* @__PURE__ */ m.createElement(
    "div",
    {
      className: "font-colors-menu",
      style: g,
      onPointerUp: (w) => {
        w.stopPropagation(), w.nativeEvent.stopImmediatePropagation();
      }
    },
    /* @__PURE__ */ m.createElement(
      Ar,
      {
        activeColor: c,
        onPointerUpHandler: (w) => {
          w.stopPropagation(), l("transparent");
          const p = "transparent";
          i != null && i.fillColor && (i.fillColor = p, a({ fillColor: p })), re.emitMethod(
            te.MainEngine,
            W.SetColorNode,
            {
              workIds: [$],
              workState: x.Done,
              fillColor: p,
              viewId: s == null ? void 0 : s.viewId
            }
          );
        }
      }
    ),
    o.map((w, p) => {
      const f = Yt(...w);
      return /* @__PURE__ */ m.createElement(
        dn,
        {
          key: p,
          color: f,
          activeColor: c,
          onPointerUpHandler: (N) => {
            N.cancelable && N.preventDefault(), N.stopPropagation(), l(f);
            const S = Ae(f, h);
            i != null && i.fillColor && (i.fillColor = S, a({ fillColor: S })), re.emitMethod(
              te.MainEngine,
              W.SetColorNode,
              {
                workIds: [$],
                workState: x.Done,
                fillColor: S,
                viewId: s == null ? void 0 : s.viewId
              }
            );
          }
        }
      );
    }),
    M
  ) : null, [
    n,
    g,
    c,
    o,
    M,
    i,
    s == null ? void 0 : s.viewId,
    a,
    h
  ]), y = J(() => {
    const w = c && c !== "transparent" && Ae(c, h) || "transparent";
    return /* @__PURE__ */ m.createElement("div", { className: "color-bar-fill" }, /* @__PURE__ */ m.createElement("div", { className: "circle", style: { backgroundColor: w } }));
  }, [c, h]);
  return /* @__PURE__ */ m.createElement(
    "div",
    {
      className: `button normal-button font-colors-icon ${n && "active"}`,
      onPointerUp: (w) => {
        w.stopPropagation(), w.nativeEvent.stopImmediatePropagation(), e(!n);
      }
    },
    y,
    d
  );
}, Ep = (r) => {
  const {
    open: n,
    setOpen: e,
    textOpt: t,
    workIds: i,
    floatBarRef: o
  } = r, { floatBarColors: s, mananger: a, setFloatBarData: c, floatBarData: l } = Ce(Te), [h, u] = ae(), [g, M] = ae(1);
  ge(() => {
    if (t != null && t.fontColor) {
      const [f, N] = (t == null ? void 0 : t.fontColor) === "transparent" && ["transparent", 0] || hn(t.fontColor);
      u(f), M(N);
    }
  }, [t == null ? void 0 : t.fontColor]);
  const d = J(() => {
    if (o != null && o.current && l && ee(l == null ? void 0 : l.y) && (a != null && a.height)) {
      if (o.current.offsetTop && o.current.offsetTop + l.y > 180) {
        const f = {};
        return f.top = "inherit", f.bottom = 50, f;
      } else if (!o.current.offsetTop && (a == null ? void 0 : a.height) - o.current.offsetTop - l.y < 120) {
        const f = {};
        return f.top = "inherit", f.bottom = 50, f;
      }
    }
  }, [o, l, a == null ? void 0 : a.height]), y = J(() => h && h !== "transparent" ? /* @__PURE__ */ m.createElement(
    gn,
    {
      key: "fontColors",
      opacity: g,
      activeColor: h,
      setCurOpacity: (f, N, S) => {
        M(f);
        const C = Ae(N, f);
        l != null && l.textOpt && (l.textOpt.fontColor = C, c({ textOpt: l.textOpt })), re.emitMethod(
          te.MainEngine,
          W.SetColorNode,
          {
            workIds: i || [$],
            fontColor: h && C,
            workState: S,
            viewId: a == null ? void 0 : a.viewId
          }
        );
      }
    }
  ) : null, [
    c,
    h,
    g,
    a == null ? void 0 : a.control.room,
    a == null ? void 0 : a.viewId,
    l == null ? void 0 : l.textOpt,
    i
  ]), w = J(() => n ? /* @__PURE__ */ m.createElement(
    "div",
    {
      className: "font-colors-menu",
      style: d,
      onPointerUp: (f) => {
        f.stopPropagation(), f.nativeEvent.stopImmediatePropagation();
      }
    },
    s.map((f, N) => {
      const S = Yt(...f);
      return /* @__PURE__ */ m.createElement(
        dn,
        {
          key: N,
          color: S,
          activeColor: h,
          onPointerUpHandler: (C) => {
            C.stopPropagation(), u(S);
            const k = Ae(S, g);
            l != null && l.textOpt && (l.textOpt.fontColor = k, c({ textOpt: l.textOpt })), re.emitMethod(
              te.MainEngine,
              W.SetColorNode,
              {
                workIds: i || [$],
                workState: x.Done,
                fontColor: k,
                viewId: a == null ? void 0 : a.viewId
              }
            );
          }
        }
      );
    }),
    y
  ) : null, [
    c,
    n,
    s,
    y,
    h,
    g,
    l == null ? void 0 : l.textOpt,
    i,
    a == null ? void 0 : a.viewId,
    d
  ]), p = J(() => {
    const f = h && h !== "transparent" && Ae(h, g) || "transparent";
    return /* @__PURE__ */ m.createElement("div", { className: "color-bar" }, /* @__PURE__ */ m.createElement("div", { className: "color-bar-color", style: { backgroundColor: f } }));
  }, [h, g]);
  return /* @__PURE__ */ m.createElement(
    "div",
    {
      className: `button normal-button font-colors-icon ${n && "active"}`,
      onPointerUp: (f) => {
        f.stopPropagation(), f.nativeEvent.stopImmediatePropagation(), e(!n);
      }
    },
    /* @__PURE__ */ m.createElement("img", { alt: "icon", src: Le("font-colors") }),
    p,
    w
  );
}, Lp = (r) => {
  const {
    open: n,
    setOpen: e,
    textOpt: t,
    workIds: i,
    floatBarRef: o
  } = r, { floatBarColors: s, mananger: a, setFloatBarData: c, floatBarData: l } = Ce(Te), [h, u] = ae(), [g, M] = ae(1);
  ge(() => {
    if (t != null && t.fontBgColor) {
      const [f, N] = (t == null ? void 0 : t.fontBgColor) === "transparent" && ["transparent", 1] || hn(t.fontBgColor);
      u(f), M(N);
    }
  }, [t == null ? void 0 : t.fontBgColor]);
  const d = J(() => {
    if (o != null && o.current && l && ee(l == null ? void 0 : l.y) && (a != null && a.height)) {
      if (o.current.offsetTop && o.current.offsetTop + l.y > 180) {
        const f = {};
        return f.top = "inherit", f.bottom = 50, f;
      } else if (!o.current.offsetTop && (a == null ? void 0 : a.height) - o.current.offsetTop - l.y < 120) {
        const f = {};
        return f.top = "inherit", f.bottom = 50, f;
      }
    }
  }, [o, l, a == null ? void 0 : a.height]), y = J(() => h && h !== "transparent" ? /* @__PURE__ */ m.createElement(
    gn,
    {
      key: "fontBgColor",
      opacity: g || 0,
      activeColor: h,
      setCurOpacity: (f, N, S) => {
        M(f);
        const C = Ae(N, f);
        l != null && l.textOpt && (l.textOpt.fontBgColor = C, c({ textOpt: l.textOpt })), re.emitMethod(
          te.MainEngine,
          W.SetColorNode,
          {
            workIds: i || [$],
            fontBgColor: h && Ae(N, f),
            workState: S,
            viewId: a == null ? void 0 : a.viewId
          }
        );
      }
    }
  ) : null, [
    h,
    g,
    l == null ? void 0 : l.textOpt,
    i,
    a == null ? void 0 : a.viewId,
    c
  ]), w = J(() => n ? /* @__PURE__ */ m.createElement(
    "div",
    {
      className: "font-colors-menu",
      style: d,
      onPointerUp: (f) => {
        f.stopPropagation(), f.nativeEvent.stopImmediatePropagation();
      }
    },
    /* @__PURE__ */ m.createElement(
      Ar,
      {
        activeColor: h,
        onPointerUpHandler: (f) => {
          f.stopPropagation();
          const N = "transparent";
          u(N), l != null && l.textOpt && (l.textOpt.fontBgColor = N, c({ textOpt: l.textOpt })), re.emitMethod(
            te.MainEngine,
            W.SetColorNode,
            {
              workIds: i || [$],
              workState: x.Done,
              fontBgColor: N,
              viewId: a == null ? void 0 : a.viewId
            }
          );
        }
      }
    ),
    s.map((f, N) => {
      const S = Yt(...f);
      return /* @__PURE__ */ m.createElement(
        dn,
        {
          key: N,
          color: S,
          activeColor: h,
          onPointerUpHandler: (C) => {
            C.stopPropagation(), u(S);
            const k = Ae(S, g);
            l != null && l.textOpt && (l.textOpt.fontBgColor = k, c({ textOpt: l.textOpt })), re.emitMethod(
              te.MainEngine,
              W.SetColorNode,
              {
                workIds: i || [$],
                workState: x.Done,
                fontBgColor: Ae(S, g),
                viewId: a == null ? void 0 : a.viewId
              }
            );
          }
        }
      );
    }),
    y
  ) : null, [
    c,
    n,
    s,
    y,
    g,
    h,
    l == null ? void 0 : l.textOpt,
    i,
    a == null ? void 0 : a.viewId,
    d
  ]), p = J(() => {
    const f = h && h !== "transparent" && Ae(h, g) || "transparent";
    return /* @__PURE__ */ m.createElement("div", { className: "color-bar", style: { marginTop: 0 } }, /* @__PURE__ */ m.createElement("div", { className: "color-bar-color", style: { backgroundColor: f } }));
  }, [h, g]);
  return /* @__PURE__ */ m.createElement(
    "div",
    {
      className: `button normal-button font-colors-icon ${n && "active"}`,
      onPointerUp: (f) => {
        f.stopPropagation(), f.nativeEvent.stopImmediatePropagation(), e(!n);
      }
    },
    /* @__PURE__ */ m.createElement(jp, null),
    p,
    w
  );
}, Pp = (r) => {
  const { bold: n, setBold: e, workIds: t, viewId: i } = r, o = (s) => {
    const a = n === "bold" ? "normal" : "bold";
    s == null || s.stopPropagation(), e(a), re.emitMethod(
      te.MainEngine,
      W.SetFontStyle,
      { workIds: t, viewId: i, bold: a }
    );
  };
  return /* @__PURE__ */ m.createElement("div", { className: "font-style-button", onPointerUp: o }, /* @__PURE__ */ m.createElement("img", { alt: "icon", src: Le(n === "bold" ? "bold-active" : "bold") }));
}, Op = (r) => {
  const { underline: n, setUnderline: e, workIds: t, viewId: i } = r, o = (s) => {
    const a = !n;
    s == null || s.stopPropagation(), e(a), re.emitMethod(
      te.MainEngine,
      W.SetFontStyle,
      { workIds: t, viewId: i, underline: a }
    );
  };
  return /* @__PURE__ */ m.createElement("div", { className: "font-style-button", onPointerUp: o }, /* @__PURE__ */ m.createElement(
    "img",
    {
      alt: "icon",
      src: Le(n ? "underline-active" : "underline")
    }
  ));
}, Rp = (r) => {
  const { lineThrough: n, setLineThrough: e, workIds: t, viewId: i } = r, o = (s) => {
    const a = !n;
    s == null || s.stopPropagation(), e(a), re.emitMethod(
      te.MainEngine,
      W.SetFontStyle,
      { workIds: t, viewId: i, lineThrough: a }
    );
  };
  return /* @__PURE__ */ m.createElement("div", { className: "font-style-button", onPointerUp: o }, /* @__PURE__ */ m.createElement(
    "img",
    {
      alt: "icon",
      src: Le(n ? "line-through-active" : "line-through")
    }
  ));
}, Yp = (r) => {
  const { italic: n, setItalic: e, workIds: t, viewId: i } = r, o = (s) => {
    const a = n === "italic" ? "normal" : "italic";
    s == null || s.stopPropagation(), e(a), re.emitMethod(
      te.MainEngine,
      W.SetFontStyle,
      { workIds: t, viewId: i, italic: a }
    );
  };
  return /* @__PURE__ */ m.createElement("div", { className: "font-style-button", onPointerUp: o }, /* @__PURE__ */ m.createElement(
    "img",
    {
      alt: "icon",
      src: Le(n === "italic" ? "italic-active" : "italic")
    }
  ));
}, Up = (r) => {
  const {
    open: n,
    setOpen: e,
    textOpt: t,
    workIds: i,
    style: o,
    floatBarRef: s
  } = r, { mananger: a, floatBarData: c } = Ce(Te), [l, h] = ae("normal"), [u, g] = ae("normal"), [M, d] = ae(!1), [y, w] = ae(!1);
  ge(() => {
    t != null && t.bold && h(t.bold), We(t == null ? void 0 : t.underline) && d(t.underline || !1), We(t == null ? void 0 : t.lineThrough) && w(t.lineThrough || !1), t != null && t.italic && g(t.italic);
  }, [t]);
  const p = J(() => {
    if (o && (s != null && s.current) && ee(c == null ? void 0 : c.y) && (a != null && a.height)) {
      if (s.current.offsetTop && s.current.offsetTop + c.y > 180) {
        const N = {};
        return N.top = "inherit", N.bottom = 50, N;
      } else if (!s.current.offsetTop && (a == null ? void 0 : a.height) - s.current.offsetTop - c.y < 120) {
        const N = {};
        return N.top = "inherit", N.bottom = 50, N;
      }
    }
  }, [o, s == null ? void 0 : s.current, c, a]), f = J(() => n ? /* @__PURE__ */ m.createElement(
    "div",
    {
      className: "font-style-menu",
      style: p,
      onPointerUp: (N) => {
        N.stopPropagation(), N.nativeEvent.stopImmediatePropagation();
      }
    },
    /* @__PURE__ */ m.createElement(
      Pp,
      {
        workIds: i || [$],
        bold: l,
        setBold: h,
        viewId: a == null ? void 0 : a.viewId
      }
    ),
    /* @__PURE__ */ m.createElement(
      Op,
      {
        workIds: i || [$],
        underline: M,
        setUnderline: d,
        viewId: a == null ? void 0 : a.viewId
      }
    ),
    /* @__PURE__ */ m.createElement(
      Rp,
      {
        workIds: i || [$],
        lineThrough: y,
        setLineThrough: w,
        viewId: a == null ? void 0 : a.viewId
      }
    ),
    /* @__PURE__ */ m.createElement(
      Yp,
      {
        workIds: i || [$],
        italic: u,
        setItalic: g,
        viewId: a == null ? void 0 : a.viewId
      }
    )
  ) : null, [
    n,
    i,
    l,
    a == null ? void 0 : a.viewId,
    M,
    y,
    u,
    p
  ]);
  return /* @__PURE__ */ m.createElement(
    "div",
    {
      className: `button normal-button ${n && "active"}`,
      onPointerUp: (N) => {
        N.stopPropagation(), N.nativeEvent.stopImmediatePropagation(), e(!n);
      }
    },
    /* @__PURE__ */ m.createElement(
      "img",
      {
        alt: "icon",
        src: Le(n ? "font-style-active" : "font-style")
      }
    ),
    f
  );
}, _e = Object.freeze([
  12,
  14,
  18,
  24,
  36,
  48,
  64,
  80,
  144,
  288
]), wt = [
  "top",
  "topLeft",
  "topRight",
  "bottom",
  "bottomLeft",
  "bottomRight",
  "left",
  "leftTop",
  "leftBottom",
  "right",
  "rightTop",
  "rightBottom"
], Bp = /* @__PURE__ */ new Map([
  [
    "selector",
    "data:image/png;base64,"
  ],
  [
    "pencil",
    "data:image/png;base64,"
  ],
  [
    "eraser",
    "data:image/png;base64,"
  ],
  [
    "text",
    "data:image/svg+xml;base64,"
  ],
  [
    "shape",
    "data:image/svg+xml;base64,"
  ]
]), Wp = (r) => {
  const { style: n, onClickHandler: e } = r;
  return /* @__PURE__ */ m.createElement(
    "div",
    {
      className: "font-size-menu",
      style: n,
      onPointerUp: (t) => {
        t.stopPropagation(), t.nativeEvent.stopImmediatePropagation();
      }
    },
    _e.map((t) => /* @__PURE__ */ m.createElement(
      "div",
      {
        className: "font-size-btn",
        key: t,
        onPointerUp: () => {
          e(t);
        }
      },
      t
    ))
  );
}, Fp = (r) => {
  const n = xe(null), {
    open: e,
    setOpen: t,
    textOpt: i,
    workIds: o,
    floatBarRef: s
  } = r, { mananger: a, floatBarData: c } = Ce(Te), [l, h] = ae(0), u = _e.length - 1;
  ge(() => {
    i != null && i.fontSize && (h(i.fontSize), n.current && (n.current.value = i.fontSize.toString()));
  }, [i == null ? void 0 : i.fontSize]);
  const g = J(() => {
    if (s != null && s.current && ee(c == null ? void 0 : c.y) && (a != null && a.height)) {
      if (s.current.offsetTop && s.current.offsetTop + c.y > 180) {
        const p = {};
        return p.top = "inherit", p.bottom = 35, p;
      } else if (!s.current.offsetTop && (a == null ? void 0 : a.height) - s.current.offsetTop - c.y < 120) {
        const p = {};
        return p.top = "inherit", p.bottom = 35, p;
      }
    }
  }, [s == null ? void 0 : s.current, c, a]);
  function M(p) {
    h(p), p && p >= _e[0] && p <= _e[u] && re.emitMethod(
      te.MainEngine,
      W.SetFontStyle,
      {
        workIds: o || [$],
        fontSize: p,
        viewId: a == null ? void 0 : a.viewId
      }
    );
  }
  const d = (p) => {
    var f;
    (f = n.current) == null || f.blur(), t(!1), w(p);
  }, y = J(() => e ? /* @__PURE__ */ m.createElement(Wp, { onClickHandler: d, style: g }) : null, [e, d, g]), w = (p) => {
    p > _e[u] && (p = _e[u]), p < _e[0] && (p = _e[0]), M(p);
  };
  return /* @__PURE__ */ m.createElement(
    "div",
    {
      className: "button normal-button font-size-barBtn",
      style: { width: 50 },
      onPointerUp: (p) => {
        p.stopPropagation(), p.nativeEvent.stopImmediatePropagation();
      }
    },
    /* @__PURE__ */ m.createElement(
      "input",
      {
        className: "font-size-input",
        ref: n,
        onTouchEnd: () => {
          n.current && n.current.focus();
        },
        onClick: () => {
          t(!e), n.current && n.current.focus();
        },
        onKeyDown: (p) => {
          if (p.stopPropagation(), p.nativeEvent.stopImmediatePropagation(), p.key === "Backspace") {
            const f = window.getSelection(), N = f == null ? void 0 : f.getRangeAt(0);
            if (N != null && N.collapsed)
              return p.cancelable && p.preventDefault(), document.execCommand("delete", !1), !1;
          }
          return !1;
        },
        onKeyUp: (p) => {
          var f;
          if (p.stopPropagation(), p.nativeEvent.stopImmediatePropagation(), n.current) {
            const N = n.current.value, S = parseInt(N);
            isNaN(S) ? n.current.value = "0" : n.current.value = S.toString(), S && p.key === "Enter" && (w(S), (f = n.current) == null || f.blur(), t(!1));
          }
        }
      }
    ),
    /* @__PURE__ */ m.createElement("div", { className: "font-size-btns" }, /* @__PURE__ */ m.createElement(
      "div",
      {
        className: "font-size-add",
        onPointerUp: () => {
          w(l + _e[0]);
        }
      }
    ), /* @__PURE__ */ m.createElement(
      "div",
      {
        className: "font-size-cut",
        onPointerUp: () => {
          w(l - _e[0]);
        }
      }
    )),
    y
  );
}, Vp = (r) => {
  const { workIds: n, mananger: e, islocked: t } = r;
  return /* @__PURE__ */ m.createElement(
    "div",
    {
      className: "button normal-button",
      onPointerUp: (i) => {
        i.stopPropagation(), re.emitMethod(
          te.MainEngine,
          W.SetLock,
          {
            workIds: n || [$],
            isLocked: !t,
            viewId: e == null ? void 0 : e.viewId
          }
        );
      }
    },
    /* @__PURE__ */ m.createElement("img", { alt: "icon", src: Le(t ? "unlock-new" : "lock-new") })
  );
}, An = (r) => {
  const { icon: n, min: e, max: t, step: i, value: o, onInputHandler: s } = r, [a, c] = ae(0), l = xe(null), h = (u) => {
    u > t && (u = t), u < e && (u = e), c(u), s(u), l.current && (l.current.value = u.toString());
  };
  return ge(() => {
    o && (c(o), l.current && (l.current.value = o.toString()));
  }, [o]), /* @__PURE__ */ m.createElement("div", { className: "button input-button" }, /* @__PURE__ */ m.createElement("img", { src: Le(n) }), /* @__PURE__ */ m.createElement(
    "input",
    {
      className: "input-number",
      type: "text",
      ref: l,
      onPointerUp: () => {
        l.current && l.current.focus();
      },
      onKeyDown: (u) => {
        if (u.stopPropagation(), u.nativeEvent.stopImmediatePropagation(), u.key === "Backspace") {
          const g = window.getSelection(), M = g == null ? void 0 : g.getRangeAt(0);
          if (M != null && M.collapsed)
            return u.cancelable && u.preventDefault(), document.execCommand("delete", !1), !1;
        }
      },
      onKeyUp: (u) => {
        if (u.stopPropagation(), u.nativeEvent.stopImmediatePropagation(), l.current) {
          const g = l.current.value, M = parseInt(g);
          isNaN(M) ? l.current.value = "0" : l.current.value = M.toString();
        }
      },
      onChange: (u) => {
        const g = u.target.value, M = parseInt(g);
        M && M && M >= e && M <= t && h(M);
      }
    }
  ), /* @__PURE__ */ m.createElement("div", { className: "input-number-btns" }, /* @__PURE__ */ m.createElement(
    "div",
    {
      className: "input-number-add",
      onPointerUp: () => {
        h(a + i);
      }
    }
  ), /* @__PURE__ */ m.createElement(
    "div",
    {
      className: "input-number-cut",
      onPointerUp: () => {
        h(a - i);
      }
    }
  )));
}, Qp = (r) => {
  const { icon: n, min: e, max: t, step: i, value: o, onInputHandler: s } = r;
  return /* @__PURE__ */ m.createElement("div", { className: "button input-button" }, /* @__PURE__ */ m.createElement("img", { src: Le(n) }), /* @__PURE__ */ m.createElement(
    Gp,
    {
      min: e,
      max: t,
      step: i,
      value: o,
      onInputHandler: s
    }
  ));
}, Gp = (r) => {
  const { value: n, min: e, max: t, onInputHandler: i } = r, [o, s] = ae({ x: 0, y: 0 });
  ge(() => {
    s({ x: n * 100, y: 0 });
  }, []);
  const a = (h, u) => {
    let g = Math.floor(Math.max(u.x, e * 100));
    g = Math.floor(Math.min(g, t * 100)), u.x !== (o == null ? void 0 : o.x) && s({ x: g, y: 0 });
    const M = g / 100;
    n !== M && i(M);
  }, c = () => {
  }, l = (h, u) => {
    let g = Math.floor(Math.max(u.x, e * 100));
    g = Math.floor(Math.min(g, t * 100)), u.x !== (o == null ? void 0 : o.x) && s({ x: g, y: 0 });
    const M = g / 100;
    n !== M && i(M);
  };
  return /* @__PURE__ */ m.createElement(
    "div",
    {
      className: "range-number-container",
      onPointerUp: (h) => {
        const u = h.nativeEvent.offsetX - 6;
        let g = Math.floor(Math.max(u, e * 100));
        g = Math.floor(Math.min(g, t * 100)), s({ x: g, y: 0 });
        const M = g / 100;
        n !== M && i(M);
      }
    },
    /* @__PURE__ */ m.createElement("div", { className: "range-number-color" }),
    /* @__PURE__ */ m.createElement("div", { className: "range-number" }, /* @__PURE__ */ m.createElement(
      vt,
      {
        bounds: "parent",
        axis: "x",
        position: o,
        onDrag: a,
        onStart: c,
        onStop: l
      },
      /* @__PURE__ */ m.createElement("div", { className: "circle", onPointerUp: Ut })
    ))
  );
}, Zp = (r) => {
  const { icon: n, value: e, onChangeHandler: t, style: i } = r, [o, s] = ae(0), [a, c] = ae(), l = xe(null), h = pt(
    (M) => {
      M >= wt.length && (M = 0), M < 0 && (M = wt.length - 1), s(M), t(wt[M]), c(!1), l.current && (l.current.value = wt[M]);
    },
    [t]
  ), u = J(() => {
    if (i && i.bottom) {
      const M = {};
      return M.top = "inherit", M.bottom = 50, M;
    }
  }, [i]);
  ge(() => {
    e && (s(wt.indexOf(e)), l.current && (l.current.value = e));
  }, [e]);
  const g = J(() => a ? /* @__PURE__ */ m.createElement(
    $p,
    {
      options: wt,
      onClickHandler: h,
      style: u
    }
  ) : null, [a, h, u]);
  return /* @__PURE__ */ m.createElement("div", { className: "button input-button" }, /* @__PURE__ */ m.createElement("img", { src: Le(n) }), /* @__PURE__ */ m.createElement(
    "input",
    {
      readOnly: !0,
      className: "input-number",
      type: "text",
      ref: l,
      onPointerUp: () => {
        l.current && (l.current.focus(), c(!a));
      }
    }
  ), /* @__PURE__ */ m.createElement("div", { className: "input-number-btns" }, /* @__PURE__ */ m.createElement(
    "div",
    {
      className: "input-number-add",
      onPointerUp: () => {
        h(o + 1);
      }
    }
  ), /* @__PURE__ */ m.createElement(
    "div",
    {
      className: "input-number-cut",
      onPointerUp: () => {
        h(o - 1);
      }
    }
  )), g);
}, $p = (r) => {
  const { options: n, style: e, onClickHandler: t } = r;
  return /* @__PURE__ */ m.createElement("div", { className: "select-option-menu", style: e }, n.map((i, o) => /* @__PURE__ */ m.createElement(
    "div",
    {
      className: "select-option-btn",
      key: i,
      onPointerUp: () => {
        t(o);
      }
    },
    i
  )));
}, Hp = (r) => {
  const { innerRatio: n, innerVerticeStep: e, vertices: t, mananger: i, setShapeOpt: o } = r, s = (l) => {
    o({ vertices: l }), re.emitMethod(
      te.MainEngine,
      W.SetShapeOpt,
      {
        workIds: [$],
        toolsType: T.Star,
        viewId: i.viewId,
        vertices: l
      }
    );
  }, a = (l) => {
    o({ innerVerticeStep: l }), re.emitMethod(
      te.MainEngine,
      W.SetShapeOpt,
      {
        workIds: [$],
        toolsType: T.Star,
        viewId: i.viewId,
        innerVerticeStep: l
      }
    );
  }, c = (l) => {
    o({ innerRatio: l }), re.emitMethod(
      te.MainEngine,
      W.SetShapeOpt,
      {
        workIds: [$],
        toolsType: T.Star,
        viewId: i.viewId,
        innerRatio: l
      }
    );
  };
  return /* @__PURE__ */ m.createElement(m.Fragment, null, /* @__PURE__ */ m.createElement(
    An,
    {
      value: t,
      icon: "polygon-vertex",
      min: 3,
      max: 100,
      step: 1,
      onInputHandler: s
    }
  ), /* @__PURE__ */ m.createElement(
    An,
    {
      value: e,
      icon: "star-innerVertex",
      min: 1,
      max: 100,
      step: 1,
      onInputHandler: a
    }
  ), /* @__PURE__ */ m.createElement(
    Qp,
    {
      value: n,
      icon: "star-innerRatio",
      min: 0.1,
      max: 1,
      step: 0.1,
      onInputHandler: c
    }
  ));
}, Jp = (r) => {
  const { mananger: n, vertices: e, setShapeOpt: t } = r, i = (o) => {
    t({ vertices: o }), re.emitMethod(
      te.MainEngine,
      W.SetShapeOpt,
      {
        workIds: [$],
        toolsType: T.Polygon,
        viewId: n.viewId,
        vertices: o
      }
    );
  };
  return /* @__PURE__ */ m.createElement(
    An,
    {
      value: e,
      icon: "polygon-vertex",
      min: 3,
      max: 100,
      step: 1,
      onInputHandler: i
    }
  );
}, Xp = (r) => {
  const { mananger: n, placement: e, setShapeOpt: t } = r, i = (o) => {
    t({ placement: o }), re.emitMethod(
      te.MainEngine,
      W.SetShapeOpt,
      {
        workIds: [$],
        toolsType: T.SpeechBalloon,
        viewId: n.viewId,
        placement: o
      }
    );
  };
  return /* @__PURE__ */ m.createElement(
    Zp,
    {
      value: e,
      icon: "speechBallon-placement",
      onChangeHandler: i
    }
  );
}, In = (r) => {
  const { icon: n, isActive: e, onPointerUpHandler: t } = r;
  return /* @__PURE__ */ m.createElement(
    "div",
    {
      className: `button tab-button ${e ? "active" : ""}`,
      onPointerUp: t
    },
    /* @__PURE__ */ m.createElement("img", { src: Le(n) })
  );
}, Kp = (r) => {
  const { toolsTypes: n, style: e, mananger: t, shapeOpt: i, setShapeOpt: o } = r, [s, a] = ae();
  ge(() => {
    n.includes(T.Polygon) ? a(T.Polygon) : n.includes(T.Star) ? a(T.Star) : a(T.SpeechBalloon);
  }, [n]);
  const c = (g) => {
    a(g);
  }, l = J(() => s === T.Polygon && t && i.vertices ? /* @__PURE__ */ m.createElement(
    Jp,
    {
      vertices: i.vertices,
      mananger: t,
      setShapeOpt: o
    }
  ) : null, [t, s, i, o]), h = J(() => s === T.Star && t && i.vertices && i.innerVerticeStep && i.innerRatio ? /* @__PURE__ */ m.createElement(
    Hp,
    {
      mananger: t,
      vertices: i.vertices,
      innerVerticeStep: i.innerVerticeStep,
      innerRatio: i.innerRatio,
      setShapeOpt: o
    }
  ) : null, [t, s, i, o]), u = J(() => s === T.SpeechBalloon && t && i.placement ? /* @__PURE__ */ m.createElement(
    Xp,
    {
      mananger: t,
      placement: i.placement,
      setShapeOpt: o
    }
  ) : null, [s, t, i.placement, o]);
  return /* @__PURE__ */ m.createElement(
    "div",
    {
      className: "shapeOpt-sub-menu",
      style: e,
      onPointerUp: Ut
    },
    /* @__PURE__ */ m.createElement("div", { className: "shapeOpt-sub-menu-tabs" }, n.includes(T.Polygon) && /* @__PURE__ */ m.createElement(
      In,
      {
        isActive: s === T.Polygon,
        icon: s === T.Polygon ? "polygon-active" : "polygon",
        onPointerUpHandler: c.bind(
          void 0,
          T.Polygon
        )
      }
    ) || null, n.includes(T.Star) && /* @__PURE__ */ m.createElement(
      In,
      {
        isActive: s === T.Star,
        icon: s === T.Star ? "star-active" : "star",
        onPointerUpHandler: c.bind(void 0, T.Star)
      }
    ) || null, n.includes(T.SpeechBalloon) && /* @__PURE__ */ m.createElement(
      In,
      {
        isActive: s === T.SpeechBalloon,
        icon: s === T.SpeechBalloon ? "speechBallon-active" : "speechBallon",
        onPointerUpHandler: c.bind(
          void 0,
          T.SpeechBalloon
        )
      }
    ) || null),
    /* @__PURE__ */ m.createElement("div", { className: "shapeOpt-sub-menu-content" }, l, h, u)
  );
}, qp = (r) => {
  const {
    open: n,
    setOpen: e,
    floatBarRef: t,
    toolsTypes: i,
    shapeOpt: o
  } = r, { floatBarData: s, mananger: a, setFloatBarData: c } = Ce(Te), l = xe(null), h = J(() => {
    if (t != null && t.current && ee(s == null ? void 0 : s.x) && ee(s == null ? void 0 : s.y) && (a != null && a.height)) {
      if (t.current.offsetTop && t.current.offsetTop + s.y > 200) {
        const d = {};
        return d.top = "inherit", d.bottom = 50, d;
      } else if (!t.current.offsetTop && (a == null ? void 0 : a.height) - t.current.offsetTop - (s == null ? void 0 : s.y) < 140) {
        const d = {};
        return d.top = "inherit", d.bottom = 50, d;
      }
    }
  }, [t, s == null ? void 0 : s.x, s == null ? void 0 : s.y, a == null ? void 0 : a.height]), u = pt(
    (d) => {
      c({ shapeOpt: { ...o, ...d } });
    },
    [c, o]
  ), g = J(() => n && i && a && o ? /* @__PURE__ */ m.createElement(
    Kp,
    {
      shapeOpt: o,
      style: h,
      toolsTypes: i,
      mananger: a,
      setShapeOpt: u
    }
  ) : null, [
    n,
    h,
    i,
    a,
    o,
    u
  ]), M = () => {
    e(!n);
  };
  return /* @__PURE__ */ m.createElement(
    "div",
    {
      ref: l,
      className: `button normal-button ${n && "active"}`,
      onPointerUp: M
    },
    g,
    /* @__PURE__ */ m.createElement("img", { alt: "icon", src: Le(n ? "shapes-active" : "shapes") })
  );
}, Er = m.memo(
  (r) => {
    const { textOpt: n, workIds: e, noLayer: t, position: i, noDelete: o, noCopy: s } = r, { floatBarData: a, mananger: c } = Ce(Te), [l, h] = m.useState(
      0
      /* none */
    ), [u, g] = m.useState(), M = xe(null);
    ge(() => {
      var E, O, z, L;
      const v = {}, A = (a == null ? void 0 : a.w) || ((E = n == null ? void 0 : n.boxSize) == null ? void 0 : E[0]) || 0, P = (a == null ? void 0 : a.h) || ((O = n == null ? void 0 : n.boxSize) == null ? void 0 : O[1]) || 0;
      if (i && A && P && (c != null && c.width) && (c != null && c.height)) {
        if (i.y < 60 ? i.y + P < c.height - 60 ? v.bottom = -120 : i.y + P < c.height ? v.bottom = -58 : i.y > 0 ? v.top = 62 : v.top = -i.y + 62 : v.top = 0, i.x < 0)
          v.left = -i.x + 3;
        else if (i.x + (((z = M.current) == null ? void 0 : z.offsetWidth) || A) > c.width) {
          const j = c.width - (((L = M.current) == null ? void 0 : L.offsetWidth) || A) - i.x;
          v.left = j;
        }
        g(v);
        return;
      }
      g(void 0);
    }, [
      M,
      i,
      a == null ? void 0 : a.w,
      a == null ? void 0 : a.h,
      c == null ? void 0 : c.width,
      c == null ? void 0 : c.height,
      n == null ? void 0 : n.boxSize
    ]);
    const d = J(() => a != null && a.fillColor ? /* @__PURE__ */ m.createElement(
      Ap,
      {
        floatBarRef: M,
        open: l === 3,
        setOpen: (v) => {
          h(
            v === !0 ? 3 : 0
            /* FillColor */
          );
        }
      }
    ) : null, [a == null ? void 0 : a.fillColor, l, M]), y = J(() => a != null && a.strokeColor ? /* @__PURE__ */ m.createElement(
      bp,
      {
        floatBarRef: M,
        open: l === 2,
        setOpen: (v) => {
          h(
            v === !0 ? 2 : 0
            /* StrokeColor */
          );
        }
      }
    ) : null, [a == null ? void 0 : a.strokeColor, l, M]), w = J(() => n != null && n.fontColor && (c != null && c.viewId) ? /* @__PURE__ */ m.createElement(
      Ep,
      {
        floatBarRef: M,
        open: l === 4,
        setOpen: (v) => {
          h(
            v === !0 ? 4 : 0
            /* TextColor */
          );
        },
        textOpt: n,
        workIds: e
      }
    ) : null, [n, l, e, c, M]), p = J(() => n != null && n.fontBgColor && (c != null && c.viewId) ? /* @__PURE__ */ m.createElement(
      Lp,
      {
        floatBarRef: M,
        open: l === 5,
        setOpen: (v) => {
          h(
            v === !0 ? 5 : 0
            /* TextBgColor */
          );
        },
        textOpt: n,
        workIds: e
      }
    ) : null, [n, l, e, c, M]), f = J(() => n && (c != null && c.viewId) ? /* @__PURE__ */ m.createElement(
      Up,
      {
        floatBarRef: M,
        open: l === 6,
        setOpen: (v) => {
          h(
            v === !0 ? 6 : 0
            /* FontStyle */
          );
        },
        textOpt: n,
        workIds: e,
        style: u
      }
    ) : null, [n, l, e, c, u, M]), N = J(() => n && (c != null && c.viewId) ? /* @__PURE__ */ m.createElement(
      Fp,
      {
        open: l === 7,
        setOpen: (v) => {
          h(
            v === !0 ? 7 : 0
            /* FontSize */
          );
        },
        textOpt: n,
        workIds: e,
        floatBarRef: M
      }
    ) : null, [n, l, e, c, M]), S = J(() => t ? null : /* @__PURE__ */ m.createElement(
      xp,
      {
        open: l === 1,
        setOpen: (v) => {
          h(
            v === !0 ? 1 : 0
            /* Layer */
          );
        },
        floatBarRef: M
      }
    ), [t, l, M]), C = J(() => a != null && a.canLock && c ? /* @__PURE__ */ m.createElement(
      Vp,
      {
        workIds: e,
        mananger: c,
        islocked: a.isLocked
      }
    ) : null, [a, c, e]), k = J(() => c && (c != null && c.viewId) && u && (a != null && a.shapeOpt) && (a != null && a.toolsTypes) ? /* @__PURE__ */ m.createElement(
      qp,
      {
        open: l === 8,
        setOpen: (v) => {
          h(
            v === !0 ? 8 : 0
            /* ShapeOpt */
          );
        },
        floatBarRef: M,
        workIds: e,
        toolsTypes: a.toolsTypes,
        shapeOpt: a.shapeOpt
      }
    ) : null, [a, c, l, u, e, M]);
    return /* @__PURE__ */ m.createElement("div", { className: "appliance-plugin-floatbtns", style: u, ref: M }, c && !o && /* @__PURE__ */ m.createElement(Sp, { workIds: e, mananger: c }), S, C, !!(c != null && c.viewId) && !s && /* @__PURE__ */ m.createElement(kp, { workIds: e, viewId: c.viewId }), k, f, N, w, p, y, d);
  }
), _p = (r) => {
  const { data: n, workId: e, updateOptInfo: t, handlePointerDown: i, handlePointerUp: o } = r, { opt: s, scale: a, translate: c, x: l, y: h } = n, u = xe(null), { floatBarData: g } = Ce(Te), M = J(() => {
    const {
      fontSize: w,
      fontFamily: p,
      underline: f,
      fontColor: N,
      lineThrough: S,
      textAlign: C,
      strokeColor: k,
      lineHeight: v,
      bold: A,
      italic: P
    } = s, E = v || w * 1.2, O = {
      fontSize: `${w}px`,
      lineHeight: `${E}px`,
      color: "transparent",
      caretColor: N,
      borderColor: k,
      minHeight: `${E}px`,
      pointerEvents: "none",
      paddingRight: `${P === "italic" ? Math.round(w * 0.3) : 5}px`
    };
    return p && (O.fontFamily = `${p}`), (S || f) && (O.textDecoration = `${S && "line-through" || ""}${f && " underline" || ""}`), C && (O.textAlign = `${C}`), A && (O.fontWeight = `${A}`), P && (O.fontStyle = `${P}`), O;
  }, [s]), d = J(() => {
    const w = [];
    if (s.text) {
      const p = $n(s.text);
      for (let f = 0; f < p.length; f++) {
        const N = p[f];
        N === "" ? w.push(/* @__PURE__ */ m.createElement("div", { key: f })) : w.push(/* @__PURE__ */ m.createElement("div", { key: f }, N));
      }
    }
    return w.length ? w : /* @__PURE__ */ m.createElement("div", null);
  }, [s.text]), y = J(() => {
    if (!g) return {};
    const w = `scale(${a || 1}) ${c && "translate(" + c[0] + "px," + c[1] + "px)" || ""}`;
    return {
      left: `${l - g.x}px`,
      top: `${h - g.y}px`,
      zIndex: 1,
      transform: w,
      transformOrigin: "left top",
      opacity: 0,
      pointerEvents: "auto"
    };
  }, [a, c, l, h, g]);
  return ge(() => {
    var w, p;
    if ((w = u.current) != null && w.offsetWidth && ((p = u.current) != null && p.offsetHeight)) {
      const f = s.boxSize;
      if ((f == null ? void 0 : f[0]) !== u.current.offsetWidth || f[1] !== u.current.offsetHeight || !f) {
        const N = [
          u.current.offsetWidth,
          u.current.offsetHeight
        ];
        t({
          activeTextId: e,
          update: {
            boxSize: N,
            workState: x.Done
          }
        });
      }
    }
  }, [s.fontSize, s.italic, s.bold, s.boxSize, e, t]), /* @__PURE__ */ m.createElement(
    "div",
    {
      className: "editor-box readonly",
      style: y,
      onPointerDown: () => {
        i(e);
      },
      onPointerUp: () => {
        o();
      }
    },
    /* @__PURE__ */ m.createElement("div", { className: "editor", ref: u, style: M }, d)
  );
}, ey = (r) => {
  const { mananger: n } = Ce(Te), e = J(() => {
    var i;
    const t = (o) => {
      var h, u;
      if (!n) return;
      const { activeTextId: s, update: a, syncData: c } = o, l = s && se(
        ((h = n.control.textEditorManager) == null ? void 0 : h.get(s)) || ((u = r.editors) == null ? void 0 : u.get(s))
      );
      l && l.opt && (l.opt = {
        ...l.opt,
        ...a
      }, c && (l.canSync = Object.keys(c).includes("canSync") && c.canSync || l.canSync, l.canWorker = Object.keys(c).includes("canWorker") && c.canWorker || l.canWorker), n.control.textEditorManager.updateForViewEdited(
        s,
        l
      ));
    };
    if ((i = r.editors) != null && i.size) {
      const o = [];
      return r.editors.forEach((s, a) => {
        if (r.selectIds && r.selectIds.includes(a)) {
          const c = /* @__PURE__ */ m.createElement(
            _p,
            {
              key: a,
              workId: a,
              data: s,
              updateOptInfo: t,
              handlePointerDown: r.handleTextEditorPointerDown,
              handlePointerUp: r.handleTextEditorPointerUp
            }
          );
          o.push(c);
        }
      }), o;
    }
    return null;
  }, [r.editors, r.selectIds, n]);
  return /* @__PURE__ */ m.createElement(m.Fragment, null, e);
}, ty = (r) => {
  const { position: n, textOpt: e, workIds: t, noLayer: i, noCopy: o, noDelete: s, style: a } = r, c = xe(null);
  return /* @__PURE__ */ m.createElement("div", { className: De.FloatBarBtn, ref: c, style: a }, /* @__PURE__ */ m.createElement(
    Er,
    {
      position: n,
      textOpt: e,
      workIds: t,
      noLayer: i,
      noCopy: o,
      noDelete: s
    }
  ));
}, ny = (r) => {
  const {
    data: n,
    workId: e,
    showFloatBtns: t,
    handleKeyUp: i,
    handleFocus: o,
    runAnimation: s,
    updateSize: a,
    rightBoundBreak: c
  } = r, { activeTextEditor: l, activeTextId: h, mananger: u } = Ce(Te), { opt: g, scale: M, translate: d, x: y, y: w } = n, {
    fontSize: p,
    fontFamily: f,
    underline: N,
    fontColor: S,
    lineThrough: C,
    textAlign: k,
    lineHeight: v,
    bold: A,
    italic: P
  } = g, E = xe(null), O = J(() => {
    const V = p, B = v || V * 1.2, Z = {
      fontSize: `${V}px`,
      lineHeight: `${B}px`,
      caretColor: S,
      color: "transparent",
      minHeight: `${B}px`,
      paddingRight: `${P === "italic" ? Math.round(V * 0.3) : 5}px`,
      borderWidth: 0
    };
    return f && (Z.fontFamily = `${f}`), (C || N) && (Z.textDecoration = `${C && "line-through" || ""} ${N && " underline" || ""}`), k && (Z.textAlign = `${k}`), A && (Z.fontWeight = `${A}`), P && (Z.fontStyle = `${P}`), Z;
  }, [
    A,
    S,
    f,
    p,
    P,
    v,
    C,
    M,
    k,
    N
  ]), z = xe(!1), L = xe(6), j = xe({
    fontSize: O.fontSize,
    lineHeight: O.lineHeight,
    fontStyle: O.fontStyle,
    fontWeight: O.fontWeight,
    fontFamily: O.fontFamily
  }), F = J(() => h === e ? "hover" : g.workState === x.Doing || g.workState === x.Start ? "editing" : l ? "active" : "none", [h, e, g.workState, l]);
  function X(V) {
    if (E.current) {
      const B = window == null ? void 0 : window.getSelection(), Z = E.current, oe = (ue, Me) => {
        var de;
        const ye = document.createRange();
        if (Me.childNodes.length > 0) {
          const je = Me.lastChild;
          if (je) {
            const Pe = je.lastChild;
            Pe && Pe.nodeType === Node.TEXT_NODE ? ye.setStart(Pe, ((de = Pe.textContent) == null ? void 0 : de.length) || 0) : ye.setStart(je, 0);
          } else
            ye.setStart(Me, 0);
        } else
          ye.setStart(Me, 0);
        ye.collapse(!0), ue.removeAllRanges(), ue.addRange(ye);
      };
      if (B && Z)
        if (V && Jt(V.nativeEvent)) {
          const { x: ue, y: Me } = Dr(V.nativeEvent);
          if (document.caretPositionFromPoint) {
            const ye = document.caretPositionFromPoint(ue, Me);
            if (ye) {
              const de = document.createRange();
              de.setStart(ye.offsetNode, ye.offset), de.collapse(!0), B.removeAllRanges(), B.addRange(de);
            } else
              oe(B, Z);
          } else if (document.caretRangeFromPoint) {
            const ye = document.caretRangeFromPoint(ue, Me);
            ye ? (B.removeAllRanges(), B.addRange(ye)) : oe(B, Z);
          } else
            oe(B, Z);
        } else
          oe(B, Z);
      E.current.focus();
    }
  }
  const Y = J(() => {
    const V = `scale(${M || 1}) ${d && "translate(" + d[0] + "px," + d[1] + "px)" || ""}`;
    return {
      left: `${y - Ai * (M || 1)}px`,
      top: `${w - Ai * (M || 1)}px`,
      zIndex: 2,
      transform: V,
      transformOrigin: "left top",
      borderColor: F === "editing" || F === "hover" ? S : "transparent"
    };
  }, [M, d, y, w, S, F]), Q = J(() => {
    let V = "";
    const B = "<div></div>";
    if (g.text) {
      const Z = $n(g.text);
      for (let oe = 0; oe < Z.length; oe++) {
        const ue = Z[oe];
        if (ue === "")
          V = V + B;
        else {
          const Me = ue.replace(
            /[<>&]/g,
            (ye) => ({
              "<": "&lt;",
              ">": "&gt;",
              "&": "&amp;"
            })[ye]
          );
          V = V + `<div>${Me}</div>`;
        }
      }
    } else
      V = B;
    return V;
  }, [g.fontBgColor, g.text]), _ = J(() => {
    var V, B;
    if (F === "hover") {
      if (M && t && ee(y) && ee(w) && g.boxSize && ee((V = g.boxSize) == null ? void 0 : V[0]) && ee((B = g.boxSize) == null ? void 0 : B[1])) {
        const Z = { x: y, y: w }, oe = {
          left: y,
          top: w,
          width: g.boxSize[0] * M,
          height: g.boxSize[1] * M
        };
        return /* @__PURE__ */ m.createElement(
          ty,
          {
            position: Z,
            style: oe,
            textOpt: g,
            workIds: [e],
            noLayer: !0,
            noCopy: !0,
            noDelete: !0
          }
        );
      }
      return null;
    }
    return null;
  }, [g, t, e, y, w, M, F]);
  ge(() => {
    h === e && E.current && (document.activeElement !== E.current && X(), o(E.current));
  }, [h, o, e]), ge(() => {
    if (E.current) {
      const V = parseInt(getComputedStyle(E.current).paddingRight, 10) + parseInt(getComputedStyle(E.current).borderWidth, 10);
      L.current = Number(V);
    }
  }, []), ge(() => {
    if (E.current) {
      const V = {
        fontSize: O.fontSize,
        lineHeight: O.lineHeight,
        fontStyle: O.fontStyle,
        fontWeight: O.fontWeight,
        fontFamily: O.fontFamily
      };
      if (!le(j.current, V)) {
        j.current = V;
        const B = [
          E.current.offsetWidth,
          E.current.offsetHeight
        ];
        a(e, B);
      }
    }
  }, [O, e, a]);
  const ce = pt(
    (V) => {
      var Me, ye;
      const B = u == null ? void 0 : u.parentElement;
      if (!B || !E.current || !c) return !1;
      const Z = E.current.lastChild;
      if (!Z || Z instanceof Text)
        return !1;
      const oe = Z == null ? void 0 : Z.getBoundingClientRect(), ue = B == null ? void 0 : B.getBoundingClientRect();
      if (!oe || !ue) return !1;
      if (oe.right + V > ue.right) {
        const de = Z.lastChild;
        if (!(de instanceof Text))
          return !1;
        const je = de.length || 0;
        let Pe = 0, Ne = 0, Ye = je;
        for (; Ne < Ye; ) {
          const Oe = Math.floor((Ne + Ye) / 2), at = document.createRange();
          at.setStart(de, 0), at.setEnd(de, Oe);
          const Dt = at.getBoundingClientRect(), _n = ue.right - Dt.right;
          if (_n < V) {
            if (Oe > 0) {
              const dt = document.createRange();
              dt.setStart(de, 0), dt.setEnd(de, Oe - 1);
              const zt = dt.getBoundingClientRect();
              if (ue.right - zt.right >= V) {
                Pe = Oe - 1;
                break;
              }
            }
            Ye = Oe;
          } else {
            const dt = Oe + 1;
            if (dt < je) {
              const zt = document.createRange();
              zt.setStart(de, 0), zt.setEnd(de, dt);
              const ei = zt.getBoundingClientRect();
              if (ue.right - ei.right <= V && _n > V) {
                Pe = Oe;
                break;
              }
            }
            Ne = dt;
          }
        }
        if (!Pe) {
          const Oe = document.createRange();
          Oe.setStart(de, 0), Oe.setEnd(de, Ne);
          const at = Oe.getBoundingClientRect();
          ue.right - at.right > V ? Pe = Ne : Ne > 0 && (Pe = Ne - 1);
        }
        if (Pe > 0) {
          const Oe = ((Me = de.textContent) == null ? void 0 : Me.slice(0, Pe)) || "", at = ((ye = de.textContent) == null ? void 0 : ye.slice(Pe)) || "";
          de.textContent = Oe;
          const Dt = document.createElement("div");
          return at && (Dt.textContent = at), E.current.appendChild(Dt), ce(V), !0;
        }
      }
      return !1;
    },
    [u == null ? void 0 : u.parentElement, g.fontBgColor, c]
  );
  function ie(V) {
    if (V.stopPropagation(), V.nativeEvent.stopImmediatePropagation(), V.key === "Backspace") {
      const B = window.getSelection(), Z = B == null ? void 0 : B.getRangeAt(0);
      if (Z != null && Z.collapsed)
        return V.cancelable && V.preventDefault(), document.execCommand("delete", !1), !1;
    }
    return !1;
  }
  function K(V, B) {
    const Z = window == null ? void 0 : window.getSelection();
    if (Z != null && Z.rangeCount && V && Z && Z.anchorNode) {
      const oe = Z.anchorOffset, ue = Z.anchorNode.textContent || "", Me = ue.slice(0, oe) || "", ye = ue.slice(oe) || "", de = Me.concat(V) || "";
      H(de, ye, B);
    }
  }
  function H(V, B, Z) {
    const oe = window == null ? void 0 : window.getSelection();
    if (!(oe != null && oe.rangeCount) || !oe.anchorNode) return;
    const ue = V.split(/\n/);
    let Me = oe.anchorNode.parentNode;
    Me === Z && (Me = oe.anchorNode);
    const ye = [...Z.childNodes], de = [];
    let je, Pe = oe.anchorOffset;
    for (; ye.length; ) {
      const Ne = ye.pop();
      if (Ne) {
        if (Ne === Me) {
          let Ye = ue[0];
          ue.length === 1 && (Pe = ue[0].length, Ye = ue[0].concat(B)), Me.nodeName === "#text" ? (Me.textContent = Ye, je = Me) : Me.nodeName === "DIV" && (Me.innerText = Ye, je = Me.childNodes[0]);
          break;
        }
        de.push(Ne), Z.removeChild(Ne);
      }
    }
    if (ue.length > 1)
      for (let Ne = 1; Ne < ue.length; Ne++) {
        let Ye = ue[Ne];
        const Oe = document.createElement("div");
        Ne === ue.length - 1 && (Pe = Ye.length, Ye = Ye.concat(B)), Oe.innerText = Ye, Z.appendChild(Oe), Ne === ue.length - 1 && (je = Oe.childNodes[0]);
      }
    if (de.length) {
      de.reverse();
      for (const Ne of de)
        Z.appendChild(Ne);
    }
    if (je && je.nodeName === "#text") {
      oe.deleteFromDocument();
      const Ne = document.createRange(), Ye = Math.min(Pe, (je == null ? void 0 : je.length) || 0);
      Ne.setStart(je, Ye), Ne.collapse(!0), oe.removeAllRanges(), oe.addRange(Ne);
    }
  }
  function fe(V) {
    if (V.cancelable && V.preventDefault(), V.stopPropagation(), V.nativeEvent.stopImmediatePropagation(), E.current) {
      const B = (V.clipboardData || window.clipboardData).getData(
        "text"
      ), Z = window == null ? void 0 : window.getSelection();
      if (!(Z != null && Z.rangeCount)) return;
      if (B && Z && Z.anchorNode) {
        const oe = wn(B, {
          whiteList: {},
          // 白名单为空，表示过滤所有标签
          stripIgnoreTag: !0,
          // 过滤所有非白名单标签的HTML
          stripIgnoreTagBody: ["script"]
          // script标签较特殊，需要过滤标签中间的内容
        });
        K(oe, E.current), ce((M || 1) * L.current), i(E.current);
      }
      s();
    }
    return !1;
  }
  function Je(V) {
    var Z;
    const B = V.querySelector("div");
    if (B) {
      const oe = document.createTreeWalker(V, NodeFilter.SHOW_TEXT, null);
      let ue;
      const Me = [];
      for (; (ue = oe.nextNode()) !== null; )
        ue.parentNode === V && Me.push(ue);
      if (Me.length > 0) {
        for (const de of Me)
          B.appendChild(de);
        const ye = window.getSelection();
        if (ye) {
          const de = document.createRange(), je = B.lastChild;
          je && je.nodeType === Node.TEXT_NODE && (de.setStart(je, ((Z = je.textContent) == null ? void 0 : Z.length) || 0), de.collapse(!0), ye.removeAllRanges(), ye.addRange(de));
        }
      }
    }
  }
  const Xe = pt(() => {
    const V = E.current;
    s(() => {
      if (V) {
        if (Je(V), !z.current && ce((M || 1) * L.current)) {
          const B = window.getSelection();
          if (!(B != null && B.rangeCount)) return !1;
          const Z = document.createRange(), oe = E.current.lastChild;
          Z.selectNodeContents(oe), Z.collapse(!1), B.removeAllRanges(), B.addRange(Z);
        }
        i(V);
      }
    });
  }, [s, i, ce, M]);
  function ht() {
    z.current = !0, s();
  }
  function tt() {
    z.current = !1, Xe();
  }
  function Ke() {
    Xe(), s();
  }
  const mt = pt(() => {
    if (F === "active" && E.current && E.current.parentElement) {
      const V = E.current.parentElement;
      V.style.borderColor = S;
    }
  }, [F, S]), Ee = pt(() => {
    if (F === "active" && E.current && E.current.parentElement) {
      const V = E.current.parentElement;
      V.style.borderColor = "transparent";
    }
  }, [F]);
  return /* @__PURE__ */ m.createElement(m.Fragment, null, _, /* @__PURE__ */ m.createElement(
    "div",
    {
      className: `editor-box ${F}`,
      style: Y,
      onPointerUp: () => {
        var V;
        if (F === "editing") {
          s(() => {
            var B;
            (B = document.activeElement) == null || B.blur(), u == null || u.control.textEditorManager.checkEmptyTextBlur();
          });
          return;
        }
        if (F === "active" && u) {
          if ((V = u.control.collector) != null && V.hasIncludeSelector(e)) {
            s(() => {
              var B;
              (B = document.activeElement) == null || B.blur(), u == null || u.control.textEditorManager.checkEmptyTextBlur();
            });
            return;
          }
          if (u && h !== e) {
            u.control.textEditorManager.checkEmptyTextBlur(), u.control.textEditorManager.active(
              e,
              u.worker.now
            );
            return;
          }
        }
      },
      onMouseOver: mt,
      onMouseLeave: Ee
    },
    /* @__PURE__ */ m.createElement(
      "div",
      {
        id: e,
        suppressContentEditableWarning: !0,
        contentEditable: !0,
        className: "editor",
        ref: E,
        style: O,
        onKeyDown: ie,
        onPaste: fe,
        onTouchEnd: X,
        onInput: Xe,
        onCompositionStart: ht,
        onCompositionUpdate: Ke,
        onCompositionEnd: tt,
        dangerouslySetInnerHTML: { __html: Q }
      }
    )
  ));
}, iy = (r) => {
  const n = xe(null), { mananger: e } = Ce(Te), t = xe(), i = pt(
    (s) => {
      var u, g;
      const { activeTextId: a, update: c, syncData: l } = s;
      if (!e) return;
      const h = a && se(
        ((u = e.control.textEditorManager) == null ? void 0 : u.get(a)) || ((g = r.editors) == null ? void 0 : g.get(a))
      );
      h && h.opt && (h.opt = {
        ...h.opt,
        ...c
      }, l && (h.canSync = Object.keys(l).includes("canSync") && l.canSync || h.canSync, h.canWorker = Object.keys(l).includes("canWorker") && l.canWorker || h.canWorker), e.control.textEditorManager.updateForViewEdited(
        a,
        h
      ));
    },
    [e, r.editors]
  ), o = J(() => {
    var u;
    const s = (g) => {
      const M = [];
      for (let d = 0; d < g.childNodes.length; d++) {
        const y = g.childNodes[d];
        if (y.nodeName === "#text" && d === 0) {
          const w = y.textContent.split(/\n/), p = w.pop();
          M.push(...w), p && M.push(p);
        } else if (y.nodeName === "DIV") {
          const w = y.innerText.split(
            /\n/
          );
          if (w.length === 2 && w[0] === "" && w[1] === "")
            M.push("");
          else {
            const p = w.shift();
            (p || p === "") && M.push(p);
            const f = w.pop();
            M.push(...w), f && M.push(f);
          }
        }
      }
      return M;
    }, a = (g) => {
      if (!e) return;
      const M = g.id, d = e.control.textEditorManager.get(M), y = x.Doing, w = [
        g.offsetWidth,
        g.offsetHeight
      ];
      (!d || d && (!le(d.opt.boxSize, w) || !le(d.opt.workState, y))) && i({
        activeTextId: M,
        update: {
          boxSize: w,
          workState: y
        },
        syncData: {
          canSync: !0,
          canWorker: !0
        }
      });
    }, c = (g) => {
      var N, S;
      if (!e) return;
      const M = s(g), d = g.id, y = e.control.textEditorManager.get(d), w = _t(M), p = x.Doing, f = [g.offsetWidth, g.offsetHeight];
      if (!y || y && (y.opt.text !== w || !le(y.opt.boxSize, f) || !le(y.opt.workState, p))) {
        const C = d && se(
          ((N = e.control.textEditorManager) == null ? void 0 : N.get(d)) || ((S = r.editors) == null ? void 0 : S.get(d))
        );
        C && C.opt && (C.opt = {
          ...C.opt,
          text: w,
          boxSize: f,
          workState: p
        }, C.canSync = !0, C.canWorker = !0, e.control.textEditorManager.updateForViewEdited(d, C)), e.control.textEditorManager.publicMsgEmitter.emit(
          "textChange",
          d,
          g,
          M,
          C
        );
      }
    }, l = (g, M) => {
      if (!e) return;
      const d = g, y = e.control.textEditorManager.get(d);
      if (y && !le(y.opt.boxSize, M)) {
        const w = se(y);
        w.opt = {
          ...w.opt,
          boxSize: M
        }, w.canSync = !1, w.canWorker = !1, e.control.textEditorManager.updateForViewEdited(d, w);
      }
    }, h = (g) => {
      t.current && cancelAnimationFrame(t.current), t.current = requestAnimationFrame(() => {
        n != null && n.current && (n == null || n.current.scrollTo({
          left: 0,
          behavior: "instant"
        }), g && g(), t.current = void 0);
      });
    };
    if ((u = r.editors) != null && u.size) {
      const g = [];
      return r.editors.forEach((M, d) => {
        g.push(
          /* @__PURE__ */ m.createElement(
            ny,
            {
              key: d,
              workId: d,
              data: M,
              showFloatBtns: r.showFloatBtns || !1,
              handleFocus: a,
              handleKeyUp: c,
              updateSize: l,
              runAnimation: h,
              rightBoundBreak: r.rightBoundBreak || !1
            }
          )
        );
      }), g;
    }
    return null;
  }, [
    r.editors,
    r.showFloatBtns,
    r.rightBoundBreak,
    e,
    i
  ]);
  return /* @__PURE__ */ m.createElement("div", { className: `${r.className}`, ref: n }, o);
}, ry = () => {
  const { floatBarData: r } = Ce(Te);
  return /* @__PURE__ */ m.createElement(
    "div",
    {
      className: "appliance-plugin-hightlight-box",
      style: { borderColor: r == null ? void 0 : r.selectorColor }
    },
    /* @__PURE__ */ m.createElement(
      "div",
      {
        className: "point LT nwse-resize",
        style: { backgroundColor: r == null ? void 0 : r.selectorColor }
      }
    ),
    /* @__PURE__ */ m.createElement(
      "div",
      {
        className: "point LC ew-resize",
        style: { backgroundColor: r == null ? void 0 : r.selectorColor }
      }
    ),
    /* @__PURE__ */ m.createElement(
      "div",
      {
        className: "point LB nesw-resize",
        style: { backgroundColor: r == null ? void 0 : r.selectorColor }
      }
    ),
    /* @__PURE__ */ m.createElement(
      "div",
      {
        className: "point TC ns-resize",
        style: { backgroundColor: r == null ? void 0 : r.selectorColor }
      }
    ),
    /* @__PURE__ */ m.createElement(
      "div",
      {
        className: "point RT nesw-resize",
        style: { backgroundColor: r == null ? void 0 : r.selectorColor }
      }
    ),
    /* @__PURE__ */ m.createElement(
      "div",
      {
        className: "point RC ew-resize",
        style: { backgroundColor: r == null ? void 0 : r.selectorColor }
      }
    ),
    /* @__PURE__ */ m.createElement(
      "div",
      {
        className: "point RB nwse-resize",
        style: { backgroundColor: r == null ? void 0 : r.selectorColor }
      }
    ),
    /* @__PURE__ */ m.createElement(
      "div",
      {
        className: "point BC ns-resize",
        style: { backgroundColor: r == null ? void 0 : r.selectorColor }
      }
    )
  );
}, oy = () => {
  const { floatBarData: r, mananger: n } = Ce(Te), [e, t] = ae(), [i, o] = ae();
  ge(() => {
    const c = [];
    if (n && ee(r == null ? void 0 : r.x) && ee(r == null ? void 0 : r.y) && (r != null && r.points)) {
      const l = n.viewId;
      if (n.control.viewContainerManager.getView(l))
        for (const u of r.points) {
          const g = n.control.viewContainerManager.transformToOriginPoint(
            u,
            l
          );
          c.push(g);
        }
      c[0] && t({
        x: c[0][0] - ((r == null ? void 0 : r.x) || 0),
        y: c[0][1] - ((r == null ? void 0 : r.y) || 0)
      }), c[1] && o({
        x: c[1][0] - ((r == null ? void 0 : r.x) || 0),
        y: c[1][1] - ((r == null ? void 0 : r.y) || 0)
      });
    }
  }, [n, r == null ? void 0 : r.points, r == null ? void 0 : r.x, r == null ? void 0 : r.y]);
  const s = J(() => e ? /* @__PURE__ */ m.createElement(
    "div",
    {
      className: "point point-dot",
      style: {
        borderColor: r == null ? void 0 : r.selectorColor,
        left: e.x,
        top: e.y
      }
    }
  ) : null, [r == null ? void 0 : r.selectorColor, e]), a = J(() => i ? /* @__PURE__ */ m.createElement(
    "div",
    {
      className: "point point-dot",
      style: {
        borderColor: r == null ? void 0 : r.selectorColor,
        left: i.x,
        top: i.y
      }
    }
  ) : null, [i, r == null ? void 0 : r.selectorColor]);
  return /* @__PURE__ */ m.createElement(
    "div",
    {
      className: "appliance-plugin-hightlight-box",
      style: { borderColor: r == null ? void 0 : r.selectorColor }
    },
    s,
    a
  );
}, sy = () => {
  const { floatBarData: r } = Ce(Te);
  return /* @__PURE__ */ m.createElement(
    "div",
    {
      className: "appliance-plugin-hightlight-box",
      style: { borderColor: r == null ? void 0 : r.selectorColor }
    },
    /* @__PURE__ */ m.createElement(
      "div",
      {
        className: "point LT nwse-resize",
        style: { backgroundColor: r == null ? void 0 : r.selectorColor }
      }
    ),
    /* @__PURE__ */ m.createElement(
      "div",
      {
        className: "point LB nesw-resize",
        style: { backgroundColor: r == null ? void 0 : r.selectorColor }
      }
    ),
    /* @__PURE__ */ m.createElement(
      "div",
      {
        className: "point RT nesw-resize",
        style: { backgroundColor: r == null ? void 0 : r.selectorColor }
      }
    ),
    /* @__PURE__ */ m.createElement(
      "div",
      {
        className: "point RB nwse-resize",
        style: { backgroundColor: r == null ? void 0 : r.selectorColor }
      }
    )
  );
}, ay = () => {
  const { floatBarData: r } = Ce(Te);
  return /* @__PURE__ */ m.createElement(
    "div",
    {
      className: "appliance-plugin-hightlight-box",
      style: {
        borderColor: r == null ? void 0 : r.selectorColor
      }
    },
    /* @__PURE__ */ m.createElement(
      "img",
      {
        className: "lock",
        alt: "lock",
        src: "https://sdk.netless.link/resource/icons/lock.svg"
      }
    )
  );
}, cy = (r) => {
  const { className: n } = r, { floatBarData: e, angle: t, setAngle: i, setOperationType: o, mananger: s } = Ce(Te), [a, c] = ae(
    x.Pending
  ), [l, h] = ae(), [u, g] = ae(!1), [M, d] = ae(new ve()), [y, w] = ae(new ve()), [p, f] = ae(!1), N = xe(0), S = xe(null), C = (E) => {
    if (!E) {
      f(!0), g(!1), o(W.None), c(x.Pending);
      return;
    }
    f(!1);
  }, k = (E, O) => {
    N.current && (clearTimeout(N.current), N.current = 0), c(x.Done), g(!1);
    const z = Math.round(
      ve.GetAngleByPoints(
        M,
        y,
        new ve(O.x, O.y)
      )
    ) || 0;
    i(z), o(W.None), re.emitMethod(
      te.MainEngine,
      W.RotateNode,
      {
        workIds: [$],
        angle: z,
        workState: x.Done,
        viewId: s == null ? void 0 : s.viewId
      }
    ), s == null || s.focusElement();
  };
  ge(() => (re.activeListener(C), () => {
    re.unmountActiveListener(C);
  }), []), ge(() => {
    if (e && (a === x.Pending || a === x.Done)) {
      const E = {
        x: (e == null ? void 0 : e.x) - 20,
        y: (e == null ? void 0 : e.y) + (e == null ? void 0 : e.h)
      }, O = {
        x: (e == null ? void 0 : e.x) + (e == null ? void 0 : e.w) / 2,
        y: (e == null ? void 0 : e.y) + (e == null ? void 0 : e.h) / 2
      };
      w(new ve(O.x, O.y)), d(new ve(E.x, E.y)), h({ x: E.x, y: E.y });
    }
  }, [e, a]);
  const v = (E, O) => {
    if (s != null && s.control.worker.isBusy) {
      f(!0), c(x.Pending);
      return;
    }
    N.current && (clearTimeout(N.current), N.current = 0), c(x.Start), g(!0);
    const z = Math.round(
      ve.GetAngleByPoints(
        M,
        y,
        new ve(O.x, O.y)
      )
    ) || 0;
    i(z), o(W.RotateNode), re.emitMethod(
      te.MainEngine,
      W.RotateNode,
      {
        workIds: [$],
        angle: 0,
        workState: x.Start,
        viewId: s == null ? void 0 : s.viewId
      }
    );
  }, A = (E, O) => {
    if (s != null && s.control.worker.isBusy) {
      f(!0), k(E, O);
      return;
    }
    k(E, O);
  }, P = (E, O) => {
    if (s != null && s.control.worker.isBusy) {
      f(!0), k(E, O);
      return;
    }
    N.current || (h({ x: O.x, y: O.y }), g(!0), N.current = setTimeout(() => {
      N.current = 0;
      const z = Math.round(
        ve.GetAngleByPoints(
          M,
          y,
          new ve(O.x, O.y)
        )
      ) || 0;
      i(z), o(W.RotateNode), c(x.Doing), re.emitMethod(
        te.MainEngine,
        W.RotateNode,
        {
          workIds: [$],
          angle: z,
          workState: x.Doing,
          viewId: s == null ? void 0 : s.viewId
        }
      );
    }, 60));
  };
  return /* @__PURE__ */ m.createElement(
    vt,
    {
      disabled: p,
      position: l,
      onStart: v,
      onDrag: P,
      onStop: A
    },
    /* @__PURE__ */ m.createElement(
      "div",
      {
        className: `${n}`,
        style: e ? {
          left: 0,
          top: 0,
          touchAction: "none"
        } : void 0
      },
      !u && /* @__PURE__ */ m.createElement(
        "div",
        {
          className: "appliance-plugin-rotate-btn",
          style: { backgroundColor: e == null ? void 0 : e.selectorColor }
        },
        /* @__PURE__ */ m.createElement("img", { alt: "icon", src: Le("rotation-button") })
      ),
      /* @__PURE__ */ m.createElement(
        "div",
        {
          ref: S,
          className: `appliance-plugin-rotate-mouse-pointer ${u ? "active" : ""}`
        },
        /* @__PURE__ */ m.createElement("img", { alt: "icon", src: Le("rotation") }),
        /* @__PURE__ */ m.createElement("div", { className: "angle-icon" }, t, "°")
      )
    )
  );
}, ly = m.memo(
  (r) => {
    const { editors: n, canSelectorSwitch: e } = r, { floatBarData: t, setOperationType: i, mananger: o, isNoPointerEvents: s } = Ce(Te), [a, c] = ae(), [l, h] = ae(!1), u = xe(0), g = xe(), M = xe(x.Pending), d = J(() => !!((t == null ? void 0 : t.scaleType) === Fe.none && (t != null && t.canLock)), [t]), y = J(() => t && t.w && t.h ? {
      position: "absolute",
      left: 0,
      top: 0,
      width: t.w,
      height: t.h,
      zIndex: 2,
      pointerEvents: d || s ? "none" : "auto",
      touchAction: "none"
    } : { display: "none", pointerEvents: "none" }, [t, d, s]), w = (v) => {
      if (!v || t != null && t.isLocked) {
        h(!0), M.current = x.Pending, i(W.None);
        return;
      }
      h(!1);
    }, p = (v, A) => {
      u.current && (clearTimeout(u.current), u.current = 0);
      const P = { x: A.x, y: A.y };
      i(W.None), M.current = x.Done, c(P), re.emitMethod(
        te.MainEngine,
        W.TranslateNode,
        {
          workIds: [$],
          position: P,
          workState: x.Done,
          viewId: o == null ? void 0 : o.viewId
        }
      );
    };
    ge(() => (re.activeListener(w), () => {
      re.unmountActiveListener(w);
    }), []), ge(() => {
      if (t && (t != null && t.isLocked) || o != null && o.control.worker.isBusy) {
        h(!0);
        return;
      }
      h(!1);
    }, [t, o == null ? void 0 : o.control.worker.isBusy]), ge(() => {
      (M.current === x.Pending || M.current === x.Done) && ee(t == null ? void 0 : t.x) && ee(t == null ? void 0 : t.y) && c({ x: t == null ? void 0 : t.x, y: t == null ? void 0 : t.y });
    }, [t == null ? void 0 : t.x, t == null ? void 0 : t.y]);
    const f = () => {
      if (o != null && o.control.worker.isBusy) {
        h(!0);
        return;
      }
      u.current && (clearTimeout(u.current), u.current = 0), ee(t == null ? void 0 : t.x) && ee(t == null ? void 0 : t.y) && (i(W.TranslateNode), M.current = x.Start, re.emitMethod(
        te.MainEngine,
        W.TranslateNode,
        {
          workIds: [$],
          position: { x: t == null ? void 0 : t.x, y: t == null ? void 0 : t.y },
          workState: x.Start,
          viewId: o == null ? void 0 : o.viewId
        }
      ));
    }, N = (v, A) => {
      var O;
      o != null && o.control.worker.isBusy && h(!0);
      const P = M.current, E = g.current;
      if (p(v, A), g.current && (g.current = void 0), P === x.Start && (n != null && n.size) && E) {
        const z = (O = o == null ? void 0 : o.control.viewContainerManager.getView(
          o.viewId
        )) == null ? void 0 : O.focusScenePath;
        z && (o == null || o.control.worker.blurSelector(o.viewId, z));
      }
      o == null || o.focusElement();
    }, S = (v, A) => {
      if (o != null && o.control.worker.isBusy) {
        h(!0), p(v, A);
        return;
      }
      if (u.current)
        return;
      const P = { x: A.x, y: A.y };
      c(P), u.current = setTimeout(() => {
        u.current = 0, (P.x !== (a == null ? void 0 : a.x) || P.y !== (a == null ? void 0 : a.y)) && (g.current && (g.current = void 0), M.current = x.Doing, re.emitMethod(
          te.MainEngine,
          W.TranslateNode,
          {
            workIds: [$],
            position: P,
            workState: x.Doing,
            viewId: o == null ? void 0 : o.viewId
          }
        ));
      }, 60);
    }, C = (v) => {
      e && (g.current = v);
    }, k = () => {
      o && e && M.current === x.Start && g.current && o.control.textEditorManager.active(g.current);
    };
    return /* @__PURE__ */ m.createElement(
      vt,
      {
        disabled: l,
        position: a,
        onStart: f,
        onDrag: S,
        onStop: N
      },
      /* @__PURE__ */ m.createElement("div", { style: y }, /* @__PURE__ */ m.createElement(
        uy,
        {
          editors: n,
          selectIds: (t == null ? void 0 : t.selectIds) || [],
          handleTextEditorPointerDown: C,
          handleTextEditorPointerUp: k
        }
      ))
    );
  },
  (r, n) => !!le(r, n)
), uy = m.memo(
  (r) => {
    const {
      editors: n,
      selectIds: e,
      handleTextEditorPointerDown: t,
      handleTextEditorPointerUp: i
    } = r;
    return n ? /* @__PURE__ */ m.createElement(
      ey,
      {
        editors: n,
        selectIds: e || [],
        handleTextEditorPointerDown: t,
        handleTextEditorPointerUp: i
      }
    ) : null;
  },
  (r, n) => !(!le(r.editors, n.editors) || !le(r.selectIds, n.selectIds))
), hy = (r) => {
  const { className: n } = r, [e, t] = ae({ x: 0, y: 0, h: 0, w: 0 }), [i, o] = ae(), [s, a] = ae(), [c, l] = ae(), { floatBarData: h, mananger: u, isNoPointerEvents: g, setOperationType: M } = Ce(Te), [d, y] = ae(), [w, p] = ae(!1), f = xe(0);
  ge(() => {
    if (h && u) {
      const O = {
        x: h.x,
        y: h.y,
        w: h.w,
        h: h.h
      };
      t(O);
    }
    return re.activeListener(S), () => {
      re.unmountActiveListener(S);
    };
  }, []), ge(() => {
    if (u != null && u.control.worker.isBusy) {
      p(!0);
      return;
    }
    p(!1);
  }, [u == null ? void 0 : u.control.worker.isBusy]);
  const N = () => {
    if (f.current && (clearTimeout(f.current), f.current = 0), d) {
      const { scale: O, translate: z } = d;
      M(W.None), re.emitMethod(
        te.MainEngine,
        W.ScaleNode,
        {
          workIds: [$],
          scale: O,
          dir: c,
          translate: z,
          originPoint: i,
          scenePoint: s,
          workState: x.Done,
          viewId: u == null ? void 0 : u.viewId
        }
      );
    }
    u == null || u.focusElement();
  }, S = (O) => {
    if (!O) {
      p(!0);
      return;
    }
    p(!1);
  }, C = (O) => [
    [O.x, O.y],
    [O.x + O.w, O.y],
    [O.x + O.w, O.y + O.h],
    [O.x, O.y + O.h]
  ], k = (O, z) => {
    switch (z) {
      case "top":
      case "topLeft":
      case "left":
        return O[2];
      case "topRight":
        return O[3];
      case "right":
      case "bottomRight":
      case "bottom":
        return O[0];
      case "bottomLeft":
        return O[1];
    }
  }, v = (O, z, L, j) => {
    let F = !1, X = !1;
    const Y = {
      x: e.x,
      y: e.y,
      w: e.w,
      h: e.h
    }, Q = [1, 1], _ = [0, 0];
    switch (O) {
      case "top": {
        const H = z.height + e.h;
        H < 0 ? (X = !0, Y.y = e.y + e.h, Y.h = Math.abs(H)) : (Y.y = e.y - z.height, Y.h = e.h + z.height);
        break;
      }
      case "topLeft": {
        const H = z.width + e.w;
        H < 0 ? (F = !0, Y.x = e.x + e.w, Y.w = Math.abs(H)) : (Y.x = e.x - z.width, Y.w = e.w + z.width);
        const fe = z.height + e.h;
        fe < 0 ? (X = !0, Y.y = e.y + e.h, Y.h = Math.abs(fe)) : (Y.y = e.y - z.height, Y.h = e.h + z.height);
        break;
      }
      case "topRight": {
        const H = z.width + e.w;
        H < 0 ? (F = !0, Y.x = e.x + H, Y.w = Math.abs(H)) : Y.w = e.w + z.width;
        const fe = z.height + e.h;
        fe < 0 ? (X = !0, Y.y = e.y + e.h, Y.h = Math.abs(fe)) : (Y.y = e.y - z.height, Y.h = e.h + z.height);
        break;
      }
      case "bottom": {
        const H = z.height + e.h;
        H < 0 ? (X = !0, Y.y = e.y + H, Y.h = Math.abs(H)) : Y.h = e.h + z.height;
        break;
      }
      case "bottomLeft": {
        const H = z.width + e.w;
        H < 0 ? (F = !0, Y.x = e.x + e.w, Y.w = Math.abs(H)) : (Y.x = e.x - z.width, Y.w = e.w + z.width);
        const fe = z.height + e.h;
        fe < 0 ? (X = !0, Y.y = e.y + fe, Y.h = Math.abs(fe)) : Y.h = e.h + z.height;
        break;
      }
      case "left": {
        const H = z.width + e.w;
        H < 0 ? (F = !0, Y.x = e.x + e.w, Y.w = Math.abs(H)) : (Y.x = e.x - z.width, Y.w = e.w + z.width);
        break;
      }
      case "bottomRight": {
        const H = z.width + e.w;
        H < 0 ? (F = !0, Y.x = e.x + H, Y.w = Math.abs(H)) : Y.w = e.w + z.width;
        const fe = z.height + e.h;
        fe < 0 ? (X = !0, Y.y = e.y + fe, Y.h = Math.abs(fe)) : Y.h = e.h + z.height;
        break;
      }
      case "right": {
        const H = z.width + e.w;
        H < 0 ? (F = !0, Y.x = e.x + H, Y.w = Math.abs(H)) : Y.w = e.w + z.width;
        break;
      }
    }
    Q[0] = Y.w / e.w, Q[1] = Y.h / e.h;
    const ie = C(Y).map(
      (H) => L.control.viewContainerManager.transformToScenePoint(
        H,
        L.viewId
      )
    ), K = k(ie, O);
    return X && (_[1] = K[1] - j[1]), F && (_[0] = K[0] - j[0]), { scale: Q, translate: _ };
  }, A = (O, z) => {
    if (f.current && (clearTimeout(f.current), f.current = 0), u != null && u.control.worker.isBusy) {
      p(!0);
      return;
    }
    if (h != null && h.w && (h != null && h.h) && u) {
      const L = {
        x: h.x,
        y: h.y,
        w: h.w,
        h: h.h
      }, j = C(L), F = j.map(
        (Q) => u.control.viewContainerManager.transformToScenePoint(
          Q,
          u.viewId
        )
      ), X = k(j, z), Y = k(F, z);
      t(L), o(X), a(Y), l(z), M(W.ScaleNode), re.emitMethod(
        te.MainEngine,
        W.ScaleNode,
        {
          workIds: [$],
          originPoint: X,
          scenePoint: Y,
          workState: x.Start,
          viewId: u.viewId
        }
      );
    }
  }, P = (O, z, L, j) => {
    if (!(!u || !i || !s)) {
      if (u != null && u.control.worker.isBusy) {
        p(!0), N();
        return;
      }
      f.current || (f.current = setTimeout(() => {
        f.current = 0;
        const { scale: F, translate: X } = v(z, j, u, s);
        (!le(F, d == null ? void 0 : d.scale) || !le(X, d == null ? void 0 : d.translate)) && (y({ scale: F, translate: X }), M(W.ScaleNode), re.emitMethod(
          te.MainEngine,
          W.ScaleNode,
          {
            workIds: [$],
            scale: F,
            dir: z,
            translate: X,
            originPoint: i,
            scenePoint: s,
            workState: x.Doing,
            viewId: u.viewId
          }
        ));
      }, 60));
    }
  }, E = () => {
    u != null && u.control.worker.isBusy && p(!0), N();
  };
  return /* @__PURE__ */ m.createElement(
    Ur,
    {
      className: `${n}`,
      boundsByDirection: !0,
      minWidth: -(((h == null ? void 0 : h.x) || 0) + ((h == null ? void 0 : h.w) || 0)),
      minHeight: -(((h == null ? void 0 : h.y) || 0) + ((h == null ? void 0 : h.h) || 0)),
      size: {
        width: (h == null ? void 0 : h.w) || 0,
        height: (h == null ? void 0 : h.h) || 0
      },
      style: {
        position: "absolute",
        left: h == null ? void 0 : h.x,
        top: h == null ? void 0 : h.y,
        pointerEvents: g ? "none" : "auto"
      },
      enable: w ? !1 : {
        top: (h == null ? void 0 : h.scaleType) === Fe.all && !0 || !1,
        right: (h == null ? void 0 : h.scaleType) === Fe.all && !0 || !1,
        bottom: (h == null ? void 0 : h.scaleType) === Fe.all && !0 || !1,
        left: (h == null ? void 0 : h.scaleType) === Fe.all && !0 || !1,
        topRight: !0,
        bottomRight: !0,
        bottomLeft: !0,
        topLeft: !0
      },
      lockAspectRatio: (h == null ? void 0 : h.scaleType) === Fe.proportional,
      onResizeStart: A,
      onResize: P,
      onResizeStop: E
    }
  );
}, Ui = (r) => {
  const { id: n, pos: e, pointMap: t, type: i } = r, { setOperationType: o, mananger: s, floatBarData: a } = Ce(Te), [c, l] = ae({
    x: 0,
    y: 0
  }), [h, u] = ae(
    x.Pending
  ), [g, M] = ae(!1), d = xe(0), y = J(() => a ? {
    left: 0,
    top: 0
  } : {}, [a]), w = (C) => {
    if (!C || a != null && a.isLocked) {
      M(!0), u(x.Pending), o(W.None);
      return;
    }
    M(!1);
  };
  ge(() => (re.activeListener(w), () => {
    re.unmountActiveListener(w);
  }), []), ge(() => {
    ee(e.x) && ee(e.y) && (h === x.Pending || h === x.Done) && l({ x: e.x, y: e.y });
  }, [e, h]), ge(() => {
    if (a && (a != null && a.isLocked) || s != null && s.control.worker.isBusy) {
      M(!0);
      return;
    }
    M(!1);
  }, [a, s == null ? void 0 : s.control.worker.isBusy]);
  const p = (C, k) => {
    d.current && (clearTimeout(d.current), d.current = 0), o(W.None), u(x.Done);
    const v = { x: k.x, y: k.y };
    if (l(v), s != null && s.control.viewContainerManager) {
      const A = s.control.viewContainerManager.transformToScenePoint(
        [v.x, v.y],
        s.viewId
      ), P = t.get(n);
      P && i === "start" ? P[0] = A : P && i === "end" && (P[1] = A), re.emitMethod(
        te.MainEngine,
        W.SetPoint,
        {
          workId: $,
          pointMap: t,
          workState: x.Done,
          viewId: s == null ? void 0 : s.viewId
        }
      );
    }
    s == null || s.focusElement();
  }, f = () => {
    if (d.current && (clearTimeout(d.current), d.current = 0), s != null && s.control.worker.isBusy) {
      M(!0);
      return;
    }
    ee(e.x) && ee(e.y) && (o(W.SetPoint), u(x.Start), re.emitMethod(
      te.MainEngine,
      W.SetPoint,
      {
        workId: $,
        pointMap: t,
        workState: x.Start,
        viewId: s == null ? void 0 : s.viewId
      }
    ));
  }, N = (C, k) => {
    if (s != null && s.control.worker.isBusy) {
      M(!0), p(C, k);
      return;
    }
    if (d.current)
      return;
    const v = { x: k.x, y: k.y };
    l(v), d.current = setTimeout(() => {
      if (d.current = 0, (v.x !== (k == null ? void 0 : k.x) || v.y !== (k == null ? void 0 : k.y)) && (s != null && s.control.viewContainerManager)) {
        const A = s.control.viewContainerManager.transformToScenePoint(
          [v.x, v.y],
          s.viewId
        ), P = t.get(n);
        P && i === "start" ? P[0] = A : P && i === "end" && (P[1] = A), u(x.Doing), re.emitMethod(
          te.MainEngine,
          W.SetPoint,
          {
            workId: $,
            pointMap: t,
            workState: x.Doing,
            viewId: s == null ? void 0 : s.viewId
          }
        );
      }
    }, 60);
  }, S = (C, k) => {
    s != null && s.control.worker.isBusy && M(!0), p(C, k);
  };
  return /* @__PURE__ */ m.createElement(
    vt,
    {
      disabled: g,
      position: c,
      onStart: f,
      onDrag: N,
      onStop: S
    },
    /* @__PURE__ */ m.createElement(
      "div",
      {
        className: "appliance-plugin-point-draggable-btn",
        style: y
      }
    )
  );
}, dy = (r) => {
  const { className: n } = r, { floatBarData: e, mananger: t } = Ce(Te), [i, o] = ae(), [s, a] = ae(), [c, l] = ae(
    /* @__PURE__ */ new Map()
  );
  ge(() => {
    const g = [];
    if (t && (e != null && e.points)) {
      const M = t.viewId;
      if (t.control.viewContainerManager.getView(M))
        for (const y of e.points) {
          const w = t.control.viewContainerManager.transformToOriginPoint(
            y,
            M
          );
          g.push(w);
        }
      e != null && e.selectIds && e.selectIds.length === 1 && (c.set(e.selectIds[0], e.points), l(c));
    }
    g[0] && o({ x: g[0][0], y: g[0][1] }), g[1] && a({ x: g[1][0], y: g[1][1] });
  }, [t, e == null ? void 0 : e.points, e == null ? void 0 : e.selectIds, c]);
  const h = J(() => i && (e != null && e.selectIds) ? /* @__PURE__ */ m.createElement(
    Ui,
    {
      pos: i,
      type: "start",
      id: e.selectIds[0],
      pointMap: c
    }
  ) : null, [i, e == null ? void 0 : e.selectIds, c]), u = J(() => s && (e != null && e.selectIds) ? /* @__PURE__ */ m.createElement(
    Ui,
    {
      pos: s,
      type: "end",
      id: e.selectIds[0],
      pointMap: c
    }
  ) : null, [s, e == null ? void 0 : e.selectIds, c]);
  return /* @__PURE__ */ m.createElement("div", { className: `${n}` }, h, u);
}, gy = m.forwardRef(
  (r, n) => {
    const { floatBarData: e, operationType: t } = Ce(Te), i = J(() => (e == null ? void 0 : e.scaleType) !== Fe.all || t === W.RotateNode ? null : /* @__PURE__ */ m.createElement(ry, null), [e, t]), o = J(() => (e == null ? void 0 : e.scaleType) !== Fe.both || t === W.RotateNode ? null : /* @__PURE__ */ m.createElement(oy, null), [e, t]), s = J(() => (e == null ? void 0 : e.scaleType) !== Fe.proportional || t === W.RotateNode ? null : /* @__PURE__ */ m.createElement(sy, null), [e, t]), a = J(() => (e == null ? void 0 : e.scaleType) === Fe.none && (e != null && e.canLock) ? /* @__PURE__ */ m.createElement(ay, null) : null, [e]);
    return /* @__PURE__ */ m.createElement(
      "div",
      {
        ref: n,
        style: e ? {
          transform: `translate(${e.x}px,${e.y}px)`,
          width: e.w,
          height: e.h,
          pointerEvents: "none"
        } : void 0
      },
      a,
      i,
      o,
      s,
      r.children
    );
  }
), fy = m.memo(
  (r) => {
    const { isHide: n, operationType: e, floatBarData: t } = r;
    return n && e === W.None && ee(t == null ? void 0 : t.x) && ee(t == null ? void 0 : t.y) && ee(t == null ? void 0 : t.w) && ee(t == null ? void 0 : t.h) ? /* @__PURE__ */ m.createElement(
      "div",
      {
        className: De.FloatBarBtn,
        style: {
          left: t == null ? void 0 : t.x,
          top: t == null ? void 0 : t.y,
          width: t.w,
          height: t.h
        }
      },
      /* @__PURE__ */ m.createElement(
        Er,
        {
          position: { x: t.x, y: t.y },
          textOpt: t == null ? void 0 : t.textOpt,
          noLayer: t == null ? void 0 : t.isLocked
        }
      )
    ) : null;
  },
  (r, n) => !!le(r, n)
), My = m.forwardRef(
  (r, n) => {
    const { floatBarData: e, operationType: t, mananger: i } = Ce(Te), { editors: o, isHide: s, canSelectorSwitch: a } = r, c = J(() => {
      var u;
      return e != null && e.canRotate && ((u = e == null ? void 0 : e.selectIds) == null ? void 0 : u.length) === 1 && (t === W.None || t === W.RotateNode) ? /* @__PURE__ */ m.createElement(cy, { className: De.RotateBtn }) : null;
    }, [e, t]), l = J(() => ((e == null ? void 0 : e.scaleType) === Fe.all || (e == null ? void 0 : e.scaleType) === Fe.proportional) && (t === W.None || t === W.ScaleNode) && ee(e == null ? void 0 : e.x) && ee(e == null ? void 0 : e.y) ? /* @__PURE__ */ m.createElement(hy, { className: De.ResizeBtn }) : null, [
      e == null ? void 0 : e.scaleType,
      t,
      e == null ? void 0 : e.x,
      e == null ? void 0 : e.y
    ]), h = J(() => (e == null ? void 0 : e.scaleType) === Fe.both && (t === W.None || t === W.SetPoint) ? /* @__PURE__ */ m.createElement(dy, { className: De.ResizeTowBox }) : null, [e == null ? void 0 : e.scaleType, t]);
    return /* @__PURE__ */ m.createElement(
      "div",
      {
        className: De.FloatBar,
        onPointerOver: (u) => (Ut(u), i == null || i.control.worker.blurCursor(i.viewId), !1)
      },
      /* @__PURE__ */ m.createElement(gy, { ref: n }),
      /* @__PURE__ */ m.createElement(ly, { editors: o, canSelectorSwitch: a }),
      c,
      l,
      h,
      /* @__PURE__ */ m.createElement(
        fy,
        {
          isHide: s,
          operationType: t,
          floatBarData: e
        }
      )
    );
  }
), py = m.memo(
  My,
  (r, n) => !!le(r, n)
);
class yy extends m.Component {
  constructor(e) {
    super(e);
    I(this, "renderAvatar", (e) => {
      var i;
      const t = `rgb(${e.memberState.strokeColor[0]}, ${e.memberState.strokeColor[1]}, ${e.memberState.strokeColor[2]})`;
      if (this.detectAvatar(e)) {
        const o = this.getCursorName(e);
        return /* @__PURE__ */ m.createElement(
          "img",
          {
            className: "cursor-selector-avatar",
            style: {
              width: o ? 19 : 28,
              height: o ? 19 : 28,
              position: o ? "initial" : "absolute",
              borderColor: o ? "white" : t,
              marginRight: o ? 4 : 0
            },
            src: (i = e.payload) == null ? void 0 : i.avatar,
            alt: "avatar"
          }
        );
      } else
        return null;
    });
    I(this, "getOpacity", (e) => {
      const t = this.getCursorName(e), i = this.detectAvatar(e);
      return t === void 0 && i === void 0 ? 0 : 1;
    });
    I(this, "getCursorName", (e) => {
      if (e.payload)
        return e.payload.nickName || e.payload.cursorName || e.payload.uid;
    });
    I(this, "getThemeClass", (e) => e.payload && e.payload.theme ? "cursor-inner-mellow" : "cursor-inner");
    I(this, "getCursorBackgroundColor", (e) => {
      const t = this.getCursorName(e);
      return e.payload && e.payload.cursorBackgroundColor ? e.payload.cursorBackgroundColor : t ? `rgb(${e.memberState.strokeColor[0]}, ${e.memberState.strokeColor[1]}, ${e.memberState.strokeColor[2]})` : void 0;
    });
    I(this, "getCursorTextColor", (e) => e.payload && e.payload.cursorTextColor ? e.payload.cursorTextColor : "#FFFFFF");
    I(this, "getCursorTagBackgroundColor", (e) => e.payload && e.payload.cursorTagBackgroundColor ? e.payload.cursorTagBackgroundColor : this.getCursorBackgroundColor(e));
    I(this, "detectAvatar", (e) => !!(e.payload && e.payload.avatar));
    I(this, "renderTag", (e) => {
      if (e.payload && e.payload.cursorTagName)
        return /* @__PURE__ */ m.createElement(
          "span",
          {
            className: "cursor-tag-name",
            style: {
              backgroundColor: this.getCursorTagBackgroundColor(e)
            }
          },
          e.payload.cursorTagName
        );
    });
    I(this, "renderCursor", (e) => {
      const t = Bp.get(e);
      return t && this.props.isEnableCursor ? /* @__PURE__ */ m.createElement(
        "img",
        {
          className: `cursor-${e}-image`,
          src: t,
          alt: `${e}Cursor`
        }
      ) : null;
    });
  }
  render() {
    const { roomMember: e } = this.props, t = this.getCursorName(e);
    switch (e.memberState.currentApplianceName) {
      case ne.pencil:
        return /* @__PURE__ */ m.createElement("div", { className: "cursor-box" }, /* @__PURE__ */ m.createElement(
          "div",
          {
            className: "cursor-mid cursor-pencil-offset",
            style: {
              transform: "translate(-50%, -100%)",
              marginLeft: "5px",
              marginTop: "4px"
            }
          },
          /* @__PURE__ */ m.createElement("div", { className: "cursor-name" }, /* @__PURE__ */ m.createElement(
            "div",
            {
              style: {
                opacity: this.getOpacity(e),
                backgroundColor: this.getCursorBackgroundColor(e),
                color: this.getCursorTextColor(e)
              },
              className: this.getThemeClass(e)
            },
            this.renderAvatar(e),
            /* @__PURE__ */ m.createElement("span", { className: "cursor-name-text" }, t),
            this.renderTag(e)
          )),
          /* @__PURE__ */ m.createElement("div", null, this.renderCursor("pencil"))
        ));
      case ne.selector:
        return /* @__PURE__ */ m.createElement("div", { className: "cursor-box" }, /* @__PURE__ */ m.createElement(
          "div",
          {
            className: "cursor-mid cursor-selector-offset",
            style: {
              transform: "translate(-50%, -100%)",
              marginLeft: "3px",
              marginTop: "17px"
            }
          },
          /* @__PURE__ */ m.createElement("div", { className: "cursor-name" }, /* @__PURE__ */ m.createElement(
            "div",
            {
              style: {
                opacity: this.getOpacity(e),
                backgroundColor: this.getCursorBackgroundColor(e),
                color: this.getCursorTextColor(e)
              },
              className: this.getThemeClass(e)
            },
            this.renderAvatar(e),
            /* @__PURE__ */ m.createElement("span", { className: "cursor-name-text" }, t),
            this.renderTag(e)
          )),
          /* @__PURE__ */ m.createElement("div", null, this.renderCursor("selector"))
        ));
      case ne.eraser:
      case ne.pencilEraser:
        return /* @__PURE__ */ m.createElement("div", { className: "cursor-box" }, /* @__PURE__ */ m.createElement(
          "div",
          {
            className: "cursor-mid cursor-eraser-offset",
            style: {
              transform: "translate(-50%, -100%)",
              marginLeft: "2px",
              marginTop: "8px"
            }
          },
          /* @__PURE__ */ m.createElement("div", { className: "cursor-name" }, /* @__PURE__ */ m.createElement(
            "div",
            {
              style: {
                opacity: this.getOpacity(e),
                backgroundColor: this.getCursorBackgroundColor(e),
                color: this.getCursorTextColor(e)
              },
              className: this.getThemeClass(e)
            },
            this.renderAvatar(e),
            /* @__PURE__ */ m.createElement("span", { className: "cursor-name-text" }, t),
            this.renderTag(e)
          )),
          /* @__PURE__ */ m.createElement("div", null, this.renderCursor("eraser"))
        ));
      case ne.text:
        return /* @__PURE__ */ m.createElement("div", { className: "cursor-box" }, /* @__PURE__ */ m.createElement(
          "div",
          {
            className: "cursor-mid cursor-text-offset",
            style: { transform: "translate(-50%, -100%)", marginLeft: "0px" }
          },
          /* @__PURE__ */ m.createElement("div", { className: "cursor-name" }, /* @__PURE__ */ m.createElement(
            "div",
            {
              style: {
                opacity: this.getOpacity(e),
                backgroundColor: this.getCursorBackgroundColor(e),
                color: this.getCursorTextColor(e)
              },
              className: this.getThemeClass(e)
            },
            this.renderAvatar(e),
            /* @__PURE__ */ m.createElement("span", { className: "cursor-name-text" }, t),
            this.renderTag(e)
          )),
          /* @__PURE__ */ m.createElement("div", null, this.renderCursor("text"))
        ));
      case ne.rectangle:
      case ne.arrow:
      case ne.straight:
      case ne.shape:
      case ne.ellipse:
        return /* @__PURE__ */ m.createElement("div", { className: "cursor-box" }, /* @__PURE__ */ m.createElement(
          "div",
          {
            className: "cursor-mid cursor-shape-offset",
            style: {
              transform: "translate(-50%, -100%)",
              marginLeft: "-3px",
              marginTop: "19px"
            }
          },
          /* @__PURE__ */ m.createElement("div", { className: "cursor-name" }, /* @__PURE__ */ m.createElement(
            "div",
            {
              style: {
                opacity: this.getOpacity(e),
                backgroundColor: this.getCursorBackgroundColor(e),
                color: this.getCursorTextColor(e)
              },
              className: this.getThemeClass(e)
            },
            this.renderAvatar(e),
            /* @__PURE__ */ m.createElement("span", { className: "cursor-name-text" }, t),
            this.renderTag(e)
          )),
          /* @__PURE__ */ m.createElement("div", null, this.renderCursor("shape"))
        ));
      default:
        return null;
    }
  }
}
const Iy = (r) => {
  var s;
  const { className: n, info: e, isEnableCursor: t } = r, { roomMember: i, ...o } = e || {};
  return /* @__PURE__ */ m.createElement(
    "div",
    {
      "data-cursor-uid": (s = i == null ? void 0 : i.payload) == null ? void 0 : s.uid,
      className: `${n}`,
      style: o ? {
        transform: `translate(${o.x}px, ${o.y}px)`
      } : { display: "none" }
    },
    i && /* @__PURE__ */ m.createElement(
      yy,
      {
        roomMember: i,
        isEnableCursor: t
      }
    )
  );
}, my = (r) => {
  const { className: n, manager: e } = r, [t, i] = ae();
  ge(() => (e.internalMsgEmitter.on(
    [te.Cursor, e.viewId],
    o
  ), () => {
    e.internalMsgEmitter.off(
      [te.Cursor, e.viewId],
      o
    );
  }), [e]);
  function o(a) {
    i(a);
  }
  return J(() => {
    if (t != null && t.length) {
      const a = t.map((c) => {
        var l;
        return c.roomMember ? /* @__PURE__ */ m.createElement(
          Iy,
          {
            key: (l = c.roomMember) == null ? void 0 : l.memberId,
            className: n,
            info: c,
            isEnableCursor: e.control.cursor.isEnableCursor
          }
        ) : null;
      });
      return /* @__PURE__ */ m.createElement(m.Fragment, null, a);
    }
    return null;
  }, [t]);
}, wy = (r) => {
  const { viewId: n, miniMapCanvas: e, highlightColor: t, miniMapManager: i, contianer: o } = r, [s, a] = ae();
  ge(() => (c(), i.listener.addListener(
    [n, "viewCameraUpate"],
    c
  ), i.listener.emitAsync([n, "miniMapCreate"], !0), () => {
    i.listener.removeListener(
      [n, "viewCameraUpate"],
      c
    );
  }), []);
  const c = () => {
    const u = i.maps.get(n);
    u && l(u.miniMapCamera, u.viewCamera);
  }, l = (u, g) => {
    const { width: M, height: d } = o.getBoundingClientRect(), y = Math.min(
      M / u.width,
      d / u.height
    ), w = g.width / g.scale, p = g.height / g.scale, f = [g.centerX, g.centerY], N = [
      u.centerX,
      u.centerY
    ], S = [
      f[0] - N[0],
      f[1] - N[1]
    ], C = Math.min(
      u.scale * y,
      u.scale * y
    ), k = [
      S[0] * C,
      S[1] * C,
      w,
      p,
      C
    ];
    a(k);
  }, h = (u) => {
    var g;
    if (i.control.renderControl.isWritable) {
      const M = u.nativeEvent.target || u.nativeEvent.srcElement;
      if (M && u.nativeEvent && s) {
        const { width: d, height: y } = M.getBoundingClientRect(), [w, p] = [d / 2, y / 2], [f, N] = [
          (u.nativeEvent.offsetX - w) / s[4],
          (u.nativeEvent.offsetY - p) / s[4]
        ], S = i.maps.get(n);
        if (S != null && S.viewCamera) {
          const [C, k] = [
            S.miniMapCamera.centerX + f,
            S.miniMapCamera.centerY + N
          ];
          (g = i.control.room) == null || g.moveCamera({
            centerX: C,
            centerY: k,
            animationMode: "immediately"
          });
        }
      }
    }
  };
  return /* @__PURE__ */ m.createElement(
    "div",
    {
      style: {
        position: "relative",
        width: "100%",
        height: "100%",
        overflow: "hidden",
        display: "flex",
        flexDirection: "column",
        justifyContent: "center",
        alignItems: "center",
        pointerEvents: "auto"
      },
      onClick: h
    },
    /* @__PURE__ */ m.createElement("canvas", { style: { pointerEvents: "none" }, ref: e }),
    s && /* @__PURE__ */ m.createElement(
      "div",
      {
        className: "minimap-highlightBox",
        style: {
          position: "absolute",
          top: "50%",
          left: "50%",
          width: s[2] * s[4],
          height: s[3] * s[4],
          boxSizing: "content-box",
          border: `2px solid ${t}`,
          transform: `translate(calc(-50% + ${s[0]}px), calc(-50% + ${s[1]}px))`,
          pointerEvents: "none"
        }
      }
    )
  );
}, Ty = (r) => {
  const { miniMapManager: n, active: e, className: t } = r, i = J(() => {
    const o = n.maps;
    if (e) {
      const s = o.get(e);
      if (s) {
        const { miniMapCamera: a, viewCamera: c, ...l } = s;
        if (l.contianer && a && c) {
          const h = n.highlightColor;
          return Yr(
            /* @__PURE__ */ m.createElement(
              wy,
              {
                viewId: e,
                ...l,
                highlightColor: h,
                miniMapManager: n
              }
            ),
            l.contianer
          );
        }
      }
    }
    return null;
  }, [e, n]);
  return /* @__PURE__ */ m.createElement("div", { className: t }, i);
}, Te = m.createContext({
  viewId: "",
  mananger: void 0,
  floatBarColors: [],
  floatBarData: void 0,
  dpr: 1,
  angle: 0,
  operationType: W.None,
  scale: [1, 1],
  isNoPointerEvents: !1,
  activeTextEditor: !1,
  activeTextId: void 0,
  setAngle: () => {
  },
  setOperationType: () => {
  },
  setFloatBarData: () => {
  }
});
class qn extends m.Component {
  constructor(e) {
    var i, o;
    super(e);
    I(this, "showTextFloatBar");
    I(this, "canSelectorSwitch");
    I(this, "rightBoundBreak");
    I(this, "setAngle", (e) => {
      this.setState({ angle: e });
    });
    I(this, "setOperationType", (e) => {
      this.setState({ operationType: e });
    });
    this.state = {
      floatBarData: void 0,
      showFloatBar: !1,
      showFloatBarBtn: !0,
      dpr: 1,
      angle: 0,
      operationType: W.None,
      scale: [1, 1],
      editors: this.editors,
      activeTextId: (i = this.props.mananger.control.textEditorManager) == null ? void 0 : i.activeId,
      activeMiniMapViewId: (o = this.props.mananger.control.miniMapManager) == null ? void 0 : o.activeViewId
    };
    const t = this.props.mananger.control.pluginOptions.extras.textEditor;
    this.showTextFloatBar = We(t.showFloatBar) ? t.showFloatBar : Se.textEditor.showFloatBar, this.canSelectorSwitch = We(t == null ? void 0 : t.canSelectorSwitch) ? t.canSelectorSwitch : Se.textEditor.canSelectorSwitch, this.rightBoundBreak = We(t == null ? void 0 : t.rightBoundBreak) ? t.rightBoundBreak : Se.textEditor.rightBoundBreak;
  }
  get editors() {
    return un(
      this.props.mananger.control.textEditorManager.filterEditor(
        this.props.mananger.viewId
      )
    );
  }
  componentDidMount() {
    this.props.mananger.vDom = this, this.props.mananger.mountView(), this.setState({ dpr: this.props.mananger.dpr });
  }
  componentWillUnmount() {
  }
  setActiveMiniMap(e) {
    this.setState({
      activeMiniMapViewId: e
    });
  }
  showFloatBarBtn(e) {
    this.setState({ showFloatBarBtn: e });
  }
  showFloatBar(e, t) {
    const i = e && t && { ...this.state.floatBarData, ...t } || void 0;
    this.setState({
      showFloatBar: e,
      floatBarData: i,
      angle: 0
    });
  }
  activeTextEditor() {
    this.setState({ activeTextEditor: !0 });
  }
  unActiveTextEditor() {
    this.setState({ activeTextEditor: !1 });
  }
  updateTextEditorInfo(e) {
    this.setState({
      activeTextId: e,
      editors: this.editors
    });
  }
  setFloatBarData(e) {
    this.state.floatBarData && this.setState({
      floatBarData: { ...this.state.floatBarData, ...e }
    });
  }
  render() {
    var t, i, o, s, a, c;
    const e = !!((i = (t = this.props.mananger.control) == null ? void 0 : t.room) != null && i.floatBarOptions);
    return /* @__PURE__ */ m.createElement("div", { className: De.Container }, !this.props.mananger.control.hasOffscreenCanvas && /* @__PURE__ */ m.createElement(
      "div",
      {
        className: De.CanvasBox,
        ref: this.props.refs.canvasContainerRef
      }
    ) || /* @__PURE__ */ m.createElement("div", { className: De.CanvasBox }, /* @__PURE__ */ m.createElement(
      "canvas",
      {
        id: "bgCanvas",
        ref: this.props.refs.canvasBgRef,
        style: { width: "100%", height: "100%" }
      }
    ), /* @__PURE__ */ m.createElement(
      "canvas",
      {
        id: "serviceCanvas",
        className: De.FloatCanvas,
        ref: this.props.refs.canvasServiceFloatRef,
        style: { width: "100%", height: "100%" }
      }
    ), /* @__PURE__ */ m.createElement(
      "canvas",
      {
        id: "localCanvas",
        className: De.FloatCanvas,
        ref: this.props.refs.canvasFloatRef,
        style: { width: "100%", height: "100%" }
      }
    ), !this.props.mananger.control.isUseSimple && /* @__PURE__ */ m.createElement(
      "div",
      {
        id: "subLocalCanvas",
        className: De.CanvasBoxSub,
        ref: this.props.refs.subLocalCanvasContainerRef
      }
    ), !this.props.mananger.control.isUseSimple && /* @__PURE__ */ m.createElement(
      "canvas",
      {
        id: "topCanvas",
        className: De.TopFloatCanvas,
        ref: this.props.refs.canvasTopRef,
        style: { width: "100%", height: "100%" }
      }
    )), !this.props.mananger.control.hasOffscreenCanvas && this.props.refs.snapshotContainerRef && /* @__PURE__ */ m.createElement(
      "div",
      {
        className: De.SnapshotBox,
        ref: this.props.refs.snapshotContainerRef
      }
    ) || null, this.props.mananger.control.isUseBackgroundThread && !!this.props.refs.backgroundContainerRef && /* @__PURE__ */ m.createElement("div", { className: De.BackgroundBox }, /* @__PURE__ */ m.createElement(
      "div",
      {
        className: De.BackgroundSvgBox,
        ref: this.props.refs.backgroundContainerRef
      }
    ), !!this.props.refs.backgroundSnapshotContainerRef && /* @__PURE__ */ m.createElement("div", { className: De.BackgroundSnapshotBox }, /* @__PURE__ */ m.createElement(
      "div",
      {
        className: De.BackgroundSnapshotContent,
        ref: this.props.refs.backgroundSnapshotContainerRef
      }
    ))) || null, /* @__PURE__ */ m.createElement(
      Te.Provider,
      {
        value: {
          viewId: this.props.viewId,
          mananger: this.props.mananger,
          floatBarColors: e && ((a = (s = (o = this.props.mananger.control) == null ? void 0 : o.room) == null ? void 0 : s.floatBarOptions) == null ? void 0 : a.colors) || [],
          isNoPointerEvents: !this.state.showFloatBarBtn,
          floatBarData: this.state.floatBarData,
          dpr: this.state.dpr,
          angle: this.state.angle,
          operationType: this.state.operationType,
          scale: this.state.scale,
          activeTextEditor: this.state.activeTextEditor,
          activeTextId: this.state.activeTextId,
          setAngle: this.setAngle.bind(this),
          setOperationType: this.setOperationType.bind(this),
          setFloatBarData: this.setFloatBarData.bind(this)
        }
      },
      this.state.showFloatBar && /* @__PURE__ */ m.createElement(
        py,
        {
          ref: this.props.refs.floatBarRef,
          editors: this.state.editors,
          isHide: e && this.state.showFloatBarBtn,
          canSelectorSwitch: this.canSelectorSwitch
        }
      ) || null,
      ((c = this.state.editors) == null ? void 0 : c.size) && /* @__PURE__ */ m.createElement(
        iy,
        {
          className: De.TextEditorContainer,
          showFloatBtns: this.showTextFloatBar && e && this.state.showFloatBarBtn || !1,
          editors: this.state.editors,
          rightBoundBreak: this.rightBoundBreak
        }
      ) || null
    ), /* @__PURE__ */ m.createElement(
      my,
      {
        className: De.CursorBox,
        manager: this.props.mananger
      }
    ), !this.props.mananger.control.isUseSimple && /* @__PURE__ */ m.createElement(
      Ty,
      {
        className: De.MiniMapBox,
        active: this.state.activeMiniMapViewId,
        miniMapManager: this.props.mananger.control.miniMapManager
      }
    ));
  }
}
class Ny extends br {
  constructor(e, t) {
    super(e, t);
    I(this, "width", 1e3);
    I(this, "height", 1e3);
    I(this, "dpr", Nt());
    I(this, "vDom");
    I(this, "eventTragetElement");
    I(this, "snapshotContainerRef");
    I(this, "backgroundSnapshotContainerRef");
    I(this, "canvasContainerRef", m.createRef());
    I(this, "subLocalCanvasContainerRef");
    I(this, "canvasTopRef");
    I(this, "canvasServiceFloatRef", m.createRef());
    I(this, "canvasFloatRef", m.createRef());
    I(this, "canvasBgRef", m.createRef());
    I(this, "backgroundContainerRef");
    I(this, "floatBarRef", m.createRef());
    I(this, "containerOffset", { x: 0, y: 0 });
    this.control.hasOffscreenCanvas || (this.snapshotContainerRef = m.createRef()), this.control.isUseSimple || (this.canvasTopRef = m.createRef(), this.subLocalCanvasContainerRef = m.createRef()), this.control.isUseBackgroundThread && (this.backgroundContainerRef = m.createRef(), this.backgroundSnapshotContainerRef = m.createRef());
  }
  setCanvassStyle() {
    var e, t;
    if (this.eventTragetElement) {
      const i = this.eventTragetElement.offsetWidth || this.width, o = this.eventTragetElement.offsetHeight || this.height, s = Nt();
      s !== this.dpr && (this.dpr = s, this.control.worker.updateDpr(this.viewId, this.dpr)), i && o && this.canvasContainerRef.current && (i !== this.width || o !== this.height) && (this.width = i, this.height = o, this.canvasContainerRef.current.style.width = `${i}px`, this.canvasContainerRef.current.style.height = `${o}px`), i && o && this.snapshotContainerRef && this.snapshotContainerRef.current && (i !== this.width || o !== this.height) && (this.width = i, this.height = o), i && o && this.canvasBgRef.current && (i !== this.width || o !== this.height) && (this.width = i, this.height = o, this.control.isUseOffscreenTransfer || (this.canvasBgRef.current.width = i * this.dpr, this.canvasBgRef.current.height = o * this.dpr, this.canvasFloatRef.current && (this.canvasFloatRef.current.width = i * this.dpr, this.canvasFloatRef.current.height = o * this.dpr), this.canvasServiceFloatRef.current && (this.canvasServiceFloatRef.current.width = i * this.dpr, this.canvasServiceFloatRef.current.height = o * this.dpr), (e = this.canvasTopRef) != null && e.current && (this.canvasTopRef.current.width = i * this.dpr, this.canvasTopRef.current.height = o * this.dpr))), i && o && this.backgroundContainerRef && ((t = this.backgroundContainerRef) != null && t.current) && (i !== this.width || o !== this.height) && (this.backgroundContainerRef.current.style.width = `${i}px`, this.backgroundContainerRef.current.style.height = `${o}px`, this.backgroundContainerRef.current.classList.toggle(
        "appliance-plugin-background-isWritable",
        this.control.renderControl.isWritable
      ));
    }
  }
  destroy() {
    if (super.destroy(), this.eventTragetElement) {
      const e = this.eventTragetElement.parentElement;
      if (e) {
        const t = e.querySelectorAll(
          ".appliance-plugin-main-view-displayer"
        );
        for (const i of t)
          i.remove();
      }
    }
  }
  createMainViewDisplayer(e) {
    const t = document.createElement("div");
    return t.className = "appliance-plugin-main-view-displayer", e.appendChild(t), this.eventTragetElement = e.children[0], this.containerOffset = this.getContainerOffset(
      this.eventTragetElement,
      this.containerOffset
    ), this.bindToolsClass(), Ln.render(
      /* @__PURE__ */ m.createElement(
        qn,
        {
          viewId: this.viewId,
          mananger: this,
          refs: {
            canvasServiceFloatRef: this.canvasServiceFloatRef,
            canvasFloatRef: this.canvasFloatRef,
            canvasBgRef: this.canvasBgRef,
            floatBarRef: this.floatBarRef,
            canvasTopRef: this.canvasTopRef,
            canvasContainerRef: this.canvasContainerRef,
            snapshotContainerRef: this.snapshotContainerRef,
            subLocalCanvasContainerRef: this.subLocalCanvasContainerRef,
            backgroundContainerRef: this.backgroundContainerRef,
            backgroundSnapshotContainerRef: this.backgroundSnapshotContainerRef
          }
        }
      ),
      t
    ), this.control.room && this.bindDisplayerEvent(this.eventTragetElement), this;
  }
}
class vy extends sp {
  constructor(e, t, i) {
    super(e, t, i);
    I(this, "dpr", Nt());
    I(this, "width", 1e3);
    I(this, "height", 1e3);
    I(this, "vDom");
    I(this, "eventTragetElement");
    I(this, "canvasContainerRef", m.createRef());
    I(this, "subLocalCanvasContainerRef");
    I(this, "canvasTopRef");
    I(this, "canvasServiceFloatRef", m.createRef());
    I(this, "canvasFloatRef", m.createRef());
    I(this, "canvasBgRef", m.createRef());
    I(this, "backgroundContainerRef");
    I(this, "floatBarRef", m.createRef());
    I(this, "containerOffset", { x: 0, y: 0 });
    this.control.isUseSimple || (this.canvasTopRef = m.createRef(), this.subLocalCanvasContainerRef = m.createRef()), this.control.isUseBackgroundThread && (this.backgroundContainerRef = m.createRef());
  }
  setCanvassStyle() {
    var e, t;
    if (this.eventTragetElement) {
      const i = Nt();
      if (i !== this.dpr && (this.dpr = i, this.control.worker.updateDpr(this.viewId, this.dpr)), this.canvasContainerRef.current) {
        const o = this.eventTragetElement.offsetWidth || this.width, s = this.eventTragetElement.offsetHeight || this.height;
        o && s && (o !== this.width || s !== this.height) && (this.width = o, this.height = s, this.canvasContainerRef.current.style.width = `${o}px`, this.canvasContainerRef.current.style.height = `${s}px`);
      }
      if (this.canvasBgRef.current) {
        const o = this.eventTragetElement.offsetWidth || this.width, s = this.eventTragetElement.offsetHeight || this.height;
        o && s && (o !== this.width || s !== this.height) && (this.width = o, this.height = s, this.control.isUseOffscreenTransfer || (this.canvasBgRef.current.width = o * this.dpr, this.canvasBgRef.current.height = s * this.dpr, this.canvasFloatRef.current && (this.canvasFloatRef.current.width = o * this.dpr, this.canvasFloatRef.current.height = s * this.dpr), this.canvasServiceFloatRef.current && (this.canvasServiceFloatRef.current.width = o * this.dpr, this.canvasServiceFloatRef.current.height = s * this.dpr), (e = this.canvasTopRef) != null && e.current && (this.canvasTopRef.current.width = o * this.dpr, this.canvasTopRef.current.height = s * this.dpr)));
      }
      if (this.backgroundContainerRef && ((t = this.backgroundContainerRef) != null && t.current)) {
        const o = this.eventTragetElement.offsetWidth || this.width, s = this.eventTragetElement.offsetHeight || this.height;
        o && s && (o !== this.width || s !== this.height) && (this.backgroundContainerRef.current.style.width = `${o}px`, this.backgroundContainerRef.current.style.height = `${s}px`, this.backgroundContainerRef.current.classList.toggle(
          "appliance-plugin-background-isWritable",
          this.control.renderControl.isWritable
        ));
      }
    }
  }
  createAppViewDisplayer(e, t) {
    const i = document.createElement("div");
    return i.className = "appliance-plugin-app-view-displayer", t.appendChild(i), this.eventTragetElement = t.children[0], this.containerOffset = this.getContainerOffset(
      this.eventTragetElement,
      this.containerOffset
    ), this.bindToolsClass(), Ln.render(
      /* @__PURE__ */ m.createElement(
        qn,
        {
          viewId: e,
          mananger: this,
          refs: {
            canvasServiceFloatRef: this.canvasServiceFloatRef,
            canvasFloatRef: this.canvasFloatRef,
            canvasBgRef: this.canvasBgRef,
            floatBarRef: this.floatBarRef,
            canvasTopRef: this.canvasTopRef,
            canvasContainerRef: this.canvasContainerRef,
            subLocalCanvasContainerRef: this.subLocalCanvasContainerRef,
            backgroundContainerRef: this.backgroundContainerRef
          }
        }
      ),
      i
    ), this.control.room && this.bindDisplayerEvent(this.eventTragetElement), this;
  }
}
class Qe extends Et {
  constructor(e) {
    super(e);
    I(this, "focuedViewId");
    I(this, "focuedView");
    I(this, "control");
    /** 针对windowmanager的focusedChange先于onAppViewMounted*/
    I(this, "tmpFocusedViewId");
    I(this, "checkScaleTimer");
    I(this, "onMainViewRelease", async (e) => {
      this.control.textEditorManager.clear(pe, !0), this.mainViewDestroy(e), await new Promise((i) => {
        setTimeout(() => {
          i(!0);
        }, 0);
      }), this.onMainViewMounted(e);
      const t = this.control.getSnapshootData("memberState");
      this.control.room && this.control.onMemberChange(t), this.control.clearSnapshootData();
    });
    I(this, "mainViewDestroy", (e) => {
      var t;
      if (this.mainView && this.mainView.displayer) {
        this.mainView.displayer.destroy();
        const i = e.divElement;
        if (i) {
          const o = i.getElementsByClassName(
            "appliance-plugin-main-view-displayer"
          );
          for (const s of o)
            s.remove();
        }
        if (this.mainView.container && i !== this.mainView.container) {
          const o = this.mainView.container.getElementsByClassName(
            "appliance-plugin-main-view-displayer"
          );
          for (const s of o)
            s.remove();
        }
        (t = this.control.worker) == null || t.destroyViewWorker(this.mainView.id, !0), this.mainView.displayer.destroy(), this.mainView = void 0;
      }
    });
    I(this, "onMainViewMounted", (e) => {
      const t = e.divElement;
      if (!t || !e.focusScenePath || !(e.focusScenePath || e.scenePath))
        return;
      const o = new Ny(
        this.control,
        ke.InternalMsgEmitter
      ), s = e.size.width || o.width, a = e.size.height || o.height, l = {
        dpr: o.dpr,
        originalPoint: [s / 2, a / 2],
        offscreenCanvasOpt: {
          ...Qe.defaultScreenCanvasOpt,
          width: s,
          height: a
        },
        layerOpt: {
          ...Qe.defaultLayerOpt,
          width: s,
          height: a
        },
        cameraOpt: {
          ...Qe.defaultCameraOpt,
          width: s,
          height: a
        }
      }, { scale: h, ...u } = e.camera;
      l.cameraOpt = {
        ...l.cameraOpt,
        ...u,
        scale: h === 1 / 0 ? 1 : h
      }, this.createMianView({
        id: pe,
        container: t,
        displayer: o,
        focusScenePath: e.focusScenePath,
        cameraOpt: l.cameraOpt,
        viewData: e
      }), this.setFocuedViewId(pe), o.createMainViewDisplayer(t), e.callbacks.on("onSizeUpdated", this.onMainViewSizeUpdated), e.callbacks.on("onCameraUpdated", this.onMainViewCameraUpdated), e.callbacks.on(
        "onActiveHotkey",
        this.onActiveHotkeyChange.bind(this)
      );
    });
    I(this, "onMainViewSizeUpdated", async () => {
      await new Promise((e) => {
        setTimeout(() => {
          e(!0);
        }, 0);
      }), this.mainView && this.mainView.viewData && this.updateMainViewCamera();
    });
    I(this, "onMainViewCameraUpdated", async () => {
      await new Promise((e) => {
        setTimeout(() => {
          e(!0);
        }, 0);
      }), this.updateMainViewCamera();
    });
    I(this, "updateMainViewCamera", () => {
      if (this.mainView && this.mainView.viewData) {
        const e = this.mainView.viewData.camera;
        if (!ee(e.scale) || e.scale === 1 / 0)
          return;
        const t = this.mainView.cameraOpt;
        if (t) {
          const i = t.width, o = t.height, { width: s, height: a } = this.mainView.viewData.size;
          (s !== i || a !== o) && this.mainView.displayer.updateSize();
          const c = e.scale === 1 / 0 ? 1 : e.scale, l = e.centerX || 0, h = e.centerY || 0, u = {
            ...t,
            scale: c,
            centerX: l,
            centerY: h,
            width: s,
            height: a
          };
          le(t, u) || (this.mainView.cameraOpt = u), this.checkScaleTimer && e.scale == 1 / 0 && (clearTimeout(this.checkScaleTimer), this.checkScaleTimer = void 0), !this.checkScaleTimer && e.scale === 1 / 0 && this.mainView.viewData && this.mainView.viewData.camera.scale === 1 / 0 && (this.checkScaleTimer = setTimeout(() => {
            var g, M;
            (M = (g = this.mainView) == null ? void 0 : g.viewData) == null || M.moveCamera({
              scale: c,
              centerX: l,
              centerY: h,
              animationMode: "immediately"
            }), this.checkScaleTimer = void 0;
          }, 500));
        }
      }
    });
    I(this, "onAppViewMounted", async (e) => {
      var g;
      const { appId: t, view: i } = e, o = i.divElement;
      if (!o || !i.focusScenePath)
        return;
      const s = this.appViews.get(t);
      if (s && s.displayer) {
        let M = o.getElementsByClassName(
          "appliance-plugin-app-view-displayer"
        );
        for (const d of M)
          d.remove();
        if (s.container && s.container !== o) {
          M = s.container.getElementsByClassName(
            "appliance-plugin-app-view-displayer"
          );
          for (const d of M)
            d.remove();
        }
        this.destroyAppView(e.appId, !0), (g = this.control.worker) == null || g.destroyViewWorker(t, !0), await new Promise((d) => {
          setTimeout(() => {
            d(!0);
          }, 0);
        });
      }
      const a = new vy(
        t,
        this.control,
        ke.InternalMsgEmitter
      ), c = i.size.width || a.width, l = i.size.height || a.height, u = {
        dpr: a.dpr,
        originalPoint: [c / 2, l / 2],
        offscreenCanvasOpt: {
          ...Qe.defaultScreenCanvasOpt,
          width: c,
          height: l
        },
        layerOpt: {
          ...Qe.defaultLayerOpt,
          width: c,
          height: l
        },
        cameraOpt: {
          ...Qe.defaultCameraOpt,
          ...i.camera,
          width: c,
          height: l
        }
      };
      if (this.createAppView({
        id: t,
        container: o,
        displayer: a,
        cameraOpt: u.cameraOpt,
        focusScenePath: i.focusScenePath,
        viewData: i
      }), a.createAppViewDisplayer(t, o), i.callbacks.on(
        "onSizeUpdated",
        this.onAppViewSizeUpdated.bind(this, t)
      ), i.callbacks.on(
        "onCameraUpdated",
        this.onAppViewCameraUpdated.bind(this, t)
      ), i.callbacks.on("onActiveHotkey", this.onActiveHotkeyChange.bind(this)), this.tmpFocusedViewId === t) {
        const M = this.control.worker.getLocalWorkViewId();
        M && M !== t ? this.setFocuedViewId(M) : this.setFocuedViewId(t), this.tmpFocusedViewId = void 0;
      }
      setTimeout(() => {
        this.onAppViewCameraUpdated(t);
      }, 0);
    });
    I(this, "onAppViewSizeUpdated", async (e) => {
      await new Promise((i) => {
        setTimeout(() => {
          i(!0);
        }, 0);
      });
      const t = this.appViews.get(e);
      t && t.viewData && this.updateAppCamera(e);
    });
    I(this, "onAppViewCameraUpdated", async (e) => {
      await new Promise((t) => {
        setTimeout(() => {
          t(!0);
        }, 0);
      }), this.updateAppCamera(e);
    });
    I(this, "updateAppCamera", (e) => {
      const t = this.appViews.get(e);
      if (t && t.viewData) {
        const i = t.viewData.camera, o = t.cameraOpt;
        if (o) {
          const { width: s, height: a } = t.viewData.size, c = o.width, l = o.height;
          (s !== c || a !== l) && t.displayer.updateSize();
          const h = i.scale === 1 / 0 ? 1 : i.scale, u = i.centerX || 0, g = i.centerY || 0, M = {
            ...o,
            scale: h,
            centerX: u,
            centerY: g,
            width: s,
            height: a
          };
          le(M, o) || (t.cameraOpt = M);
        }
      }
    });
    this.control = e.control;
  }
  getViewInitData(e) {
    var i;
    const t = this.getView(e);
    if (t) {
      const { width: o, height: s, dpr: a } = t.displayer;
      (i = this.control.pluginOptions.extras.canvasOpt) != null && i.contextType && (Qe.defaultScreenCanvasOpt.contextType = this.control.pluginOptions.extras.canvasOpt.contextType);
      const c = {
        dpr: a,
        originalPoint: [o / 2, s / 2],
        offscreenCanvasOpt: {
          ...Qe.defaultScreenCanvasOpt,
          width: o,
          height: s
        },
        layerOpt: {
          ...Qe.defaultLayerOpt,
          width: o,
          height: s
        },
        cameraOpt: {
          ...Qe.defaultCameraOpt,
          width: o,
          height: s
        }
      };
      if (t.viewData) {
        const { scale: l, ...h } = t.viewData.camera;
        c.cameraOpt = {
          ...c.cameraOpt,
          ...h,
          scale: l === 1 / 0 ? 1 : l
        };
      }
      return c;
    }
  }
  async mountView(e) {
    var i, o, s;
    const t = this.getView(e);
    if (t) {
      e === ((i = this.mainView) == null ? void 0 : i.id) && (this.control.cursor.activeCollector(), await this.control.activeWorker()), this.control.isUseOffscreenTransfer && t.displayer.canvasBgRef.current && t.displayer.canvasServiceFloatRef.current && t.displayer.canvasFloatRef.current && t.displayer.setTransferControlToOffscreenCanvas({
        bgCanvas: t.displayer.canvasBgRef.current,
        serviceCanvas: t.displayer.canvasServiceFloatRef.current,
        localCanvas: t.displayer.canvasFloatRef.current
      });
      const a = this.getViewInitData(e);
      if (a && ((o = this.control.worker) == null || o.createViewWorker(
        e,
        a,
        t.displayer
      )), t.focusScenePath && this.control.collector) {
        if (this.control.collector.authStorage) {
          const { renderAuth: c, pageAuth: l } = this.control.collector.authStorage;
          if (c) {
            const h = c[e];
            if (h) {
              const { render: u, hide: g, clear: M } = h;
              this.control.renderControl.syncRenderUids(
                e,
                u,
                g,
                M
              );
              return;
            }
          }
          if (l) {
            const h = l[e];
            if (h) {
              this.control.renderControl.syncPageAuth(
                e,
                h,
                !0
              );
              return;
            }
          }
        }
        this.control.worker.pullServiceData(e, t.focusScenePath, {
          isAsync: !0,
          useAnimation: !1
        }), this.control.pluginOptions.extras.undoRedo.enableGlobal && t.displayer.commiter && ((s = this.control.collector) != null && s.globalUndoRedoStackStorage[t.id]) && t.displayer.commiter.pullServiceData(
          this.control.collector.globalUndoRedoStackStorage[t.id]
        );
      }
    }
  }
  listenerWindowManager(e) {
    e.emitter.on("focusedChange", (t) => {
      const i = t || pe;
      if (this.focuedViewId !== i)
        if (this.getView(i)) {
          const s = this.control.worker.getLocalWorkViewId();
          s && s !== i ? this.setFocuedViewId(s) : this.setFocuedViewId(i);
        } else
          this.tmpFocusedViewId = i;
    }), e.emitter.on("mainViewScenePathChange", (t) => {
      this.control.onSceneChange(t, pe);
    }), e.emitter.on("onMainViewMounted", this.onMainViewMounted), e.emitter.on("onAppViewMounted", this.onAppViewMounted), e.emitter.on("onMainViewRebind", this.onMainViewRelease), e.emitter.on("onBoxClose", (t) => {
      this.appViews.get(t.appId) && (this.destroyAppView(t.appId), this.control.worker.destroyViewWorker(t.appId));
    }), e.emitter.on("onAppScenePathChange", (t) => {
      const { appId: i, view: o } = t;
      this.control.onSceneChange(o.focusScenePath, i);
    }), e.emitter.on("appsChange", (t) => {
      for (const i of this.appViews.keys())
        t.includes(i) || (this.destroyAppView(i), this.control.worker.destroyViewWorker(i, !0));
    });
  }
  onActiveHotkeyChange(e) {
    this.control.hotkeyManager.onActiveHotkey(e);
  }
}
class ct extends ke {
  constructor(e) {
    super(e);
    I(this, "windowManager");
    I(this, "viewContainerManager");
    const t = {
      control: this,
      internalMsgEmitter: ke.InternalMsgEmitter,
      publicMsgEmitter: ke.PublicMsgEmitter
    };
    this.viewContainerManager = new Qe(t);
  }
  init() {
  }
  activePlugin() {
    this.collector && (this.collector.addAuthStateListener(
      (e, t) => {
        e && Object.keys(e).forEach((i) => {
          var s;
          const o = (s = e[i]) == null ? void 0 : s.newValue;
          this.renderControl.syncRenderUids(
            i,
            o == null ? void 0 : o.render,
            o == null ? void 0 : o.hide,
            o == null ? void 0 : o.clear
          );
        }), t && Object.keys(t).forEach((i) => {
          var s;
          const o = (s = t[i]) == null ? void 0 : s.newValue;
          this.renderControl.syncPageAuth(i, o);
        });
      }
    ), this.collector.addStorageStateListener((e) => {
      var a, c;
      if ((a = this.collector) != null && a.storage && Object.keys(this.collector.storage).length === 0) {
        (c = this.worker) == null || c.clearViewScenePath(pe, !0);
        return;
      }
      const { diffView: t, diffScenePath: i, diffData: o } = e, s = /* @__PURE__ */ new Map();
      if (t) {
        let l;
        Object.keys(t).forEach((h) => {
          var g;
          const u = t[h];
          if (u && !u.newValue)
            (g = this.worker) == null || g.clearViewScenePath(h, !0), u.oldValue && (l = u.oldValue);
          else if (u && u.newValue) {
            for (const M of Object.keys(u.newValue))
              this.worker.pullServiceData(h, M, {
                isAsync: !1,
                useAnimation: !1
              });
            l = u.newValue;
          }
          if (l)
            for (const M of Object.values(l)) {
              const d = /* @__PURE__ */ new Set();
              for (const y of Object.keys(M))
                y && d.add(y);
              s.set(h, d);
            }
        });
      }
      if (i) {
        let l;
        Object.keys(i).forEach((h) => {
          var g;
          const u = i[h];
          if (u && u.viewId && !u.newValue ? ((g = this.worker) == null || g.clearViewScenePath(u.viewId, !0), u.oldValue && (l = u.oldValue)) : u && u.viewId && u.newValue && (this.worker.pullServiceData(u.viewId, h, {
            isAsync: !1,
            useAnimation: !1
          }), l = u.newValue), u != null && u.viewId && l) {
            const M = /* @__PURE__ */ new Set();
            for (const d of Object.keys(l))
              d && M.add(d), s.set(u.viewId, M);
          }
        });
      }
      o && Object.keys(o).forEach((l) => {
        var u, g;
        const h = o[l];
        if (h) {
          const { viewId: M } = h, d = s.get(M) || /* @__PURE__ */ new Set();
          d.add(l), s.set(M, d), (u = this.worker) == null || u.onServiceDerive(l, h), (g = this.miniMapManager) == null || g.updateMiniMapCamera(M);
        }
      });
      for (const [l, h] of s.entries())
        ke.InternalMsgEmitter.emit(
          "excludeIds",
          [...h],
          l
        );
    }), this.pluginOptions.extras.undoRedo.enableGlobal && this.collector.addGlobalUndoRedoStackListener(
      (e) => {
        Object.keys(e).forEach((t) => {
          var s;
          const i = e[t], o = (s = this.viewContainerManager.getView(t)) == null ? void 0 : s.displayer.commiter;
          o && o.pullServiceData(i == null ? void 0 : i.newValue);
        });
      }
    ), this.worker.isActive && this.viewContainerManager.getAllViews().forEach((e) => {
      e && e.focusScenePath && this.worker.pullServiceData(e.id, e.focusScenePath, {
        isAsync: !0,
        useAnimation: !1
      });
    }), this.room && this.roomMember.onUidChangeHook((e) => {
      var t, i;
      (t = this.collector) != null && t.serviceStorage && this.viewContainerManager.getAllViews().forEach((o) => {
        var s, a, c, l;
        if (o && o.focusScenePath && ((s = this.collector) != null && s.serviceStorage[o.id]) && this.collector.serviceStorage[o.id][o.focusScenePath]) {
          const h = Object.keys(
            this.collector.serviceStorage[o.id][o.focusScenePath]
          );
          for (const u of h) {
            const g = (a = this.collector) == null ? void 0 : a.getUidFromKey(u);
            if (g && !e.online.includes(g))
              if ((c = this.collector) != null && c.isSelector(u))
                this.renderControl.isWritable && ((l = this.collector) == null || l.updateValue(u, void 0, {
                  viewId: o.id,
                  scenePath: o.focusScenePath,
                  isSync: !0
                })), this.worker.blurSelector(o.id, o.focusScenePath, u);
              else {
                const M = this.textEditorManager.editors.get(u);
                M && M.opt && (M.opt.workState === x.Doing || M.opt.workState === x.Start) && (M.opt.text && M.opt.text.replace(/\s*,/g, "") ? this.textEditorManager.unActive(u) : this.textEditorManager.delete(u));
              }
          }
        }
      }), (i = this.cursor) == null || i.updateRoomMembers(e);
    }));
  }
  async activeWorker() {
    await this.worker.init();
  }
  setWindowManager(e) {
    var t, i, o, s, a;
    this.windowManager = e, (i = (t = this.windowManager) == null ? void 0 : t.mainView) != null && i.divElement && this.viewContainerManager.onMainViewMounted(this.windowManager.mainView), (a = (s = (o = this.windowManager.appManager) == null ? void 0 : o.viewManager) == null ? void 0 : s.views) != null && a.size && this.windowManager.appManager.viewManager.views.forEach((c, l) => {
      this.viewContainerManager.onAppViewMounted({ appId: l, view: c });
    }), this.viewContainerManager.listenerWindowManager(this.windowManager);
  }
}
const b = class b extends Zi {
  constructor() {
    super(...arguments);
    I(this, "onPhaseChanged", (e) => {
      var t;
      e === $i.Reconnecting && ((t = b.currentManager) == null || t.setSnapshootData());
    });
    I(this, "updateRoomWritable", () => {
      var e;
      (e = b.currentManager) == null || e.onWritableChange(
        this.displayer.isWritable
      );
    });
    I(this, "roomStateChangeListener", async (e) => {
      var t, i;
      e != null && e.roomMembers && ((t = b.currentManager) == null || t.onRoomMembersChange(
        e.roomMembers
      )), !(G(this.displayer) && !this.displayer.isWritable) && e.memberState && ((i = b.currentManager) == null || i.onMemberChange(
        e.memberState
      ));
    });
  }
  static async getInstance(e, t) {
    var w;
    t.options && (((w = t.options.extras) == null ? void 0 : w.useWorker) === "mainThread" && (t.options.cdn.fullWorkerUrl = "", t.options.cdn.subWorkerUrl = ""), (t.options.extras && t.options.extras.useWorker !== "mainThread" || !t.options.extras) && !t.options.cdn.fullWorkerUrl && !t.options.cdn.subWorkerUrl && b.logger.error(
      "[ApplianceMultiPlugin] you must adaptor options cdn fullWorkerUrl and subWorkerUrl"
    ));
    const i = e.displayer, o = i.callbacks.on, s = i.callbacks.off, a = i.callbacks.once, c = e.cleanCurrentScene, l = e.mainView.setMemberState;
    let h;
    G(i) && (h = i.setWritable), t != null && t.logger && (b.logger = t.logger);
    let u = i.getInvisiblePlugin(
      b.kind
    );
    u || (u = await b.createApplianceMultiPlugin(
      i,
      b.kind
    )), i && u && b.createCurrentManager(
      e,
      {
        cdn: t.options.cdn,
        extras: {
          ...Se,
          ...t.options.extras
        }
      },
      u
    );
    const g = {
      displayer: i,
      windowManager: e,
      currentManager: b.currentManager,
      maxScreenshotWidth: 10 * 1024,
      maxScreenshotHeight: 10 * 1024,
      getBoundingRectAsync: async function(p) {
        var S;
        b.logger.info(
          "[ApplianceMultiPlugin] getBoundingRectAsync"
        );
        const f = (e.mainView || i).getBoundingRect(p), N = await ((S = b.currentManager) == null ? void 0 : S.getBoundingRect(p));
        return !f.width || !f.height ? N : Kn(f, N);
      },
      screenshotToCanvasAsync: async function(p, f, N, S, C, k) {
        var z;
        b.logger.info(
          "[ApplianceMultiPlugin] screenshotToCanvasAsync"
        );
        let v = N, A = S, P = C.scale;
        v > this.maxScreenshotWidth && (P = this.maxScreenshotWidth / v * P, v = this.maxScreenshotWidth), A > this.maxScreenshotHeight && (P = Math.min(this.maxScreenshotHeight / A * P, P), A = this.maxScreenshotHeight), C.scale = P;
        const E = document.createElement("canvas"), O = E.getContext("2d");
        E.width = v * (k || 1), E.height = A * (k || 1), O && ((e.mainView || i).screenshotToCanvas(
          O,
          f,
          v,
          A,
          C,
          k
        ), p.drawImage(
          E,
          Math.floor((N - v) / 2),
          Math.floor((S - A) / 2),
          v * (k || 1),
          A * (k || 1),
          0,
          0,
          v,
          A
        ), E.remove()), b.currentManager && await ((z = b.currentManager) == null ? void 0 : z.screenshotToCanvas(
          p,
          f,
          v,
          A,
          C,
          Math.floor((N - v) / 2),
          Math.floor((S - A) / 2)
        ));
      },
      scenePreviewAsync: async function(p, f, N, S, C) {
        b.logger.info(
          "[ApplianceMultiPlugin] scenePreviewAsync"
        ), (e.mainView || i).scenePreview(
          p,
          f,
          N,
          S,
          C
        );
        const k = document.createElement("img");
        k.className = "appliance-plugin-fill-scene-snapshot-img", f.append(k), getComputedStyle(f).position || (f.style.position = "relative"), b.currentManager && await b.currentManager.scenePreview(
          p,
          k
        );
      },
      fillSceneSnapshotAsync: async function(p, f, N, S, C) {
        b.logger.info(
          "[ApplianceMultiPlugin] fillSceneSnapshotAsync"
        ), f.innerHTML = "", (e.mainView || i).fillSceneSnapshot(
          p,
          f,
          N,
          S,
          C
        );
        const k = N || e.mainView.size.width, v = S || e.mainView.size.height;
        let A = await this.getBoundingRectAsync(p);
        A || (A = {
          originX: 0,
          originY: 0,
          width: k,
          height: v
        });
        let P = Math.max(A == null ? void 0 : A.width, k), E = Math.max(A == null ? void 0 : A.height, v), O = 1;
        const z = P > k && Math.min(k / P, O) || O, L = E > v && Math.min(v / E, O) || O;
        z <= L ? (P = z < 1 && k || P, E = Math.floor(E * z) + 1, O = z) : z > L && (E = L < 1 && v || E, P = Math.floor(P * L) + 1, O = L);
        const j = {
          scale: O,
          centerX: A.originX + A.width / 2,
          centerY: A.originY + A.height / 2
        }, F = document.createElement("canvas"), X = F.getContext("2d");
        F.width = P, F.height = E, X && await this.screenshotToCanvasAsync(
          X,
          p,
          P,
          E,
          j,
          devicePixelRatio
        );
        const Y = document.createElement("img");
        Y.className = "appliance-plugin-fill-scene-snapshot-img", Y.src = F.toDataURL(), Y.onload = () => {
          F.remove();
        }, Y.onerror = () => {
          F.remove(), Y.remove();
        }, f.append(Y), getComputedStyle(f).position || (f.style.position = "relative");
      },
      _callbacksOn: function(p, f) {
        b.logger.info(
          `[ApplianceMultiPlugin] callbacks ${p}`
        ), (p === "onCanUndoStepsUpdate" || p === "onCanRedoStepsUpdate") && G(i) && i.isWritable ? ct.InternalMsgEmitter.on(p, f) : o.call(i.callbacks, p, f);
      },
      _callbacksOnce: function(p, f) {
        b.logger.info(
          `[ApplianceMultiPlugin] callbacks ${p}`
        ), (p === "onCanUndoStepsUpdate" || p === "onCanRedoStepsUpdate") && G(i) && i.isWritable ? ct.InternalMsgEmitter.on(p, f) : a.call(i.callbacks, p, f);
      },
      _callbacksOff: function(p, f) {
        b.logger.info(
          `[ApplianceMultiPlugin] callbacks ${p}`
        ), (p === "onCanUndoStepsUpdate" || p === "onCanRedoStepsUpdate") && G(i) && i.isWritable ? ct.InternalMsgEmitter.off(p, f) : s.call(i.callbacks, p, f);
      },
      canRedoSteps() {
        if (b.logger.info("[ApplianceMultiPlugin] canRedoSteps"), b.currentManager && G(i) && !i.disableSerialization) {
          const p = b.currentManager.viewContainerManager.focuedView, f = p == null ? void 0 : p.focusScenePath;
          if (p && f)
            return p.displayer.commiter.redoStack.filter(
              (N) => N.scenePath === f
            ).length;
        }
        return 0;
      },
      canUndoSteps() {
        if (b.logger.info("[ApplianceMultiPlugin] canUndoSteps"), b.currentManager && G(i) && !i.disableSerialization) {
          const p = b.currentManager.viewContainerManager.focuedView, f = p == null ? void 0 : p.focusScenePath;
          if (p && f)
            return p.displayer.commiter.undoStack.filter(
              (C) => C.scenePath === f
            ).length;
        }
        return 0;
      },
      undo: function() {
        return b.logger.info("[ApplianceMultiPlugin] undo"), b.currentManager && G(i) && !i.disableSerialization && i.isWritable ? b.currentManager.viewContainerManager.undo() : 0;
      },
      redo: function() {
        return b.logger.info("[ApplianceMultiPlugin] redo"), b.currentManager && G(i) && !i.disableSerialization && i.isWritable ? b.currentManager.viewContainerManager.redo() : 0;
      },
      cleanCurrentScene: function() {
        b.logger.info(
          "[ApplianceMultiPlugin] cleanCurrentScene"
        ), b.currentManager && G(i) && i.isWritable && (b.currentManager.cleanCurrentScene(), c.call(e));
      },
      insertImage: function(p) {
        if (b.logger.info("[ApplianceMultiPlugin] insertImage"), b.currentManager && G(i) && i.isWritable) {
          const f = b.currentManager.viewContainerManager.focuedViewId || pe;
          b.currentManager.worker.insertImage(
            f,
            p
          );
        }
      },
      lockImage: function(p, f) {
        if (b.logger.info("[ApplianceMultiPlugin] lockImage"), b.currentManager && G(i) && i.isWritable) {
          const N = b.currentManager.viewContainerManager.focuedViewId || pe;
          b.currentManager.worker.lockImage(
            N,
            p,
            f
          );
        }
      },
      completeImageUpload: function(p, f) {
        if (b.logger.info(
          "[ApplianceMultiPlugin] completeImageUpload"
        ), b.currentManager && G(i) && i.isWritable) {
          const N = b.currentManager.viewContainerManager.focuedViewId || pe;
          b.currentManager.worker.completeImageUpload(
            N,
            p,
            f
          );
        }
      },
      getImagesInformation: function(p) {
        return b.logger.info(
          "[ApplianceMultiPlugin] completeImageUpload"
        ), b.currentManager && G(i) && i.isWritable ? b.currentManager.worker.getImagesInformation(
          p
        ) : [];
      },
      insertText: function(p, f, N) {
        if (b.logger.info("[ApplianceMultiPlugin] insertText"), b.currentManager && G(i) && i.isWritable) {
          const S = b.currentManager.viewContainerManager.focuedViewId || pe;
          return b.currentManager.textEditorManager.insertText(
            S,
            p,
            f,
            N
          );
        }
      },
      updateText: function(p, f) {
        b.logger.info("[ApplianceMultiPlugin] updateText"), b.currentManager && G(i) && i.isWritable && b.currentManager.textEditorManager.updateText(
          p,
          f
        );
      },
      callbacks: () => (b.logger.info(
        "[ApplianceMultiPlugin] callbacks bind"
      ), {
        ...i.callbacks,
        on: g._callbacksOn.bind(g),
        once: g._callbacksOnce.bind(g),
        off: g._callbacksOff.bind(g)
      }),
      destroy() {
        b.currentManager && (b.logger.info("[ApplianceMultiPlugin] destroy"), b.currentManager.destroy(), b.currentManager = void 0);
      },
      setMemberState(p) {
        var N;
        b.logger.info(
          `[ApplianceMultiPlugin] setMemberState ${JSON.stringify(p)}`
        );
        const f = Object.keys(p);
        if (G(i) && i.isWritable) {
          if (f.includes("currentApplianceName"))
            if (p.currentApplianceName && p.currentApplianceName === ne.laserPen) {
              if ((N = b.currentManager) != null && N.isUseSimple)
                throw new Error(
                  "[ApplianceMultiPlugin] laserPen must be not support in simple mode"
                );
              p.currentApplianceName = ne.pencil, p.useLaserPen = !0;
            } else p.currentApplianceName && p.currentApplianceName === ne.pencil && (p.useLaserPen = !1);
          l.call(
            e.mainView,
            p
          );
          return;
        }
        throw new Error(
          "[ApplianceMultiPlugin] setMemberState must be called in writable room"
        );
      },
      createMiniMap: async function(p, f) {
        if (b.logger.info(
          "[ApplianceMultiPlugin] createMiniMap"
        ), b.currentManager && G(i) && i.isWritable)
          return b.currentManager.createMiniMap(p, f);
      },
      destroyMiniMap: async function(p) {
        if (b.logger.info(
          "[ApplianceMultiPlugin] destroyeMiniMap"
        ), b.currentManager && G(i) && i.isWritable)
          return b.currentManager.destroyMiniMap(p);
      },
      filterRenderByUid(p, f, N) {
        if (b.logger.info(
          "[ApplianceMultiPlugin] filterRenderByUid"
        ), b.currentManager && G(i) && i.isWritable)
          return b.currentManager.filterRenderByUid(
            p,
            f,
            N
          );
      },
      cancelFilterRender(p, f) {
        if (b.logger.info(
          "[ApplianceMultiPlugin] filterRenderByUid"
        ), b.currentManager && G(i) && i.isWritable)
          return b.currentManager.cancelFilterRender(
            p,
            f
          );
      },
      addListener: (p, f) => {
        b.logger.info(
          `[ApplianceMultiPlugin] addListener ${p}`
        ), ct.PublicMsgEmitter.on(p, f);
      },
      removeListener: (p, f) => {
        b.logger.info(
          `[ApplianceMultiPlugin] removeListener ${p}`
        ), ct.PublicMsgEmitter.off(p, f);
      },
      usePlugin: (p) => {
        var f;
        b.logger.info(
          "[ApplianceMultiPlugin] usePlugin",
          p.kind
        ), (f = b.currentManager) == null || f.usePlugin(p);
      },
      getDisableDeviceInputs() {
        if (b.currentManager && G(i))
          return b.currentManager.disableDeviceInputs;
      },
      setDisableDeviceInputs(p) {
        b.logger.info(
          `[ApplianceMultiPlugin] setDisableDeviceInputs ${p}`
        ), b.currentManager && G(i) && (b.currentManager.disableDeviceInputs = p);
      },
      getDisableCameraTransform() {
        if (b.currentManager && G(i))
          return b.currentManager.disableCameraTransform;
      },
      setDisableCameraTransform(p) {
        b.logger.info(
          `[ApplianceMultiPlugin] setDisableCameraTransform ${p}`
        ), b.currentManager && G(i) && (b.currentManager.disableCameraTransform = p);
      },
      getDisableEraseImage() {
        if (b.currentManager && G(i))
          return b.currentManager.disableEraseImage;
      },
      setDisableEraseImage(p) {
        b.logger.info(
          `[ApplianceMultiPlugin] setDisableEraseImage ${p}`
        ), b.currentManager && G(i) && (b.currentManager.disableEraseImage = p);
      },
      setWritable: async function(p) {
        var f;
        b.logger.info(
          `[ApplianceMultiPlugin] setWritable ${p}`
        ), G(i) && h && (await ((f = b.currentManager) == null ? void 0 : f.setWritable(p)), await h.call(i, p));
      },
      requestIdleCallback: (p, f) => {
        const N = (f == null ? void 0 : f.timeout) || 0, S = performance.now();
        let C;
        const k = new MessageChannel();
        let v;
        const A = (P) => {
          try {
            p({
              didTimeout: P,
              timeRemaining: () => {
                const O = performance.now() - S;
                return Math.max(0, N - O);
              }
            });
          } catch (E) {
            b.logger.error(
              "[ApplianceMultiPlugin] requestIdleCallback error:",
              E
            );
          } finally {
            k.port1.close(), k.port2.close(), C !== void 0 && cancelAnimationFrame(C), v !== void 0 && clearTimeout(v);
          }
        };
        return k.port1.onmessage = () => {
          C = requestAnimationFrame(() => {
            N > 0 && performance.now() - S >= N ? A(!0) : A(!1);
          });
        }, k.port2.postMessage(null), N > 0 && (v = setTimeout(() => {
          A(!0);
        }, N)), v || C || 0;
      },
      cancelIdleCallback: (p) => {
        try {
          clearTimeout(p), cancelAnimationFrame(p);
        } catch (f) {
          b.logger.error(
            "[ApplianceMultiPlugin] cancelIdleCallback error:",
            f
          );
        }
      },
      injectMethodToObject: (p, f) => {
        if (b.logger.info(
          `[ApplianceMultiPlugin] injectMethodToObject ${f}`
        ), typeof p[f] == "function" || typeof p[f] > "u") {
          (f === "requestIdleCallback" || f === "cancelIdleCallback") && b.currentManager && b.currentManager.registerPolyfillMethod(
            f
          ), p[f] = g[f];
          return;
        }
        f === "callbacks" && (p.callbacks.on = g._callbacksOn.bind(g), p.callbacks.off = g._callbacksOff.bind(g), p.callbacks.once = g._callbacksOnce.bind(g));
      },
      blurText: () => {
        b.logger.info("[ApplianceMultiPlugin] blurText"), b.currentManager && G(i) && i.isWritable && b.currentManager.textEditorManager.checkEmptyTextBlur();
      },
      hasElements: (p, f) => {
        var N;
        if (b.logger.info(
          `[ApplianceMultiPlugin] hasElements ${p}`
        ), b.currentManager && G(i) && i.isWritable) {
          let S = p;
          if (!S) {
            const C = b.currentManager.viewContainerManager.focuedView;
            S = C == null ? void 0 : C.focusScenePath;
          }
          if (S) {
            const C = (N = b.currentManager.collector) == null ? void 0 : N.getScenePathData(
              S
            );
            if (C) {
              const k = Object.values(C);
              return k.length > 0 ? f ? k.some(
                (v) => f(v.toolsType)
              ) : !0 : !1;
            }
            return !1;
          }
          return !1;
        }
      },
      getElements: (p, f) => {
        var N;
        if (b.logger.info(
          `[ApplianceMultiPlugin] getElements ${p}`
        ), b.currentManager && G(i) && i.isWritable) {
          let S = p;
          if (!S) {
            const C = b.currentManager.viewContainerManager.focuedView;
            S = C == null ? void 0 : C.focusScenePath;
          }
          if (S) {
            const C = (N = b.currentManager.collector) == null ? void 0 : N.getScenePathData(
              S
            ), k = C && Object.values(C) || [];
            return C && k.length > 0 && f ? k.filter(
              (v) => f(v.toolsType)
            ) : k;
          }
          return [];
        }
      },
      stopDraw: (p) => {
        if (b.logger.info(
          `[ApplianceMultiPlugin] stopDraw ${p}`
        ), b.currentManager && G(i))
          return b.currentManager.stopDraw(p);
      },
      setViewLocalScenePathChange: async (p, f) => {
        b.logger.info(
          `[ApplianceMultiPlugin] setViewLocalScenePathChange ${p} ${f}`
        ), b.currentManager && await b.currentManager.setViewLocalScenePathChange(
          f,
          p
        );
      },
      insertMarkmap: async (p, f) => {
        if (b.logger.info(
          `[ApplianceMultiPlugin] insertMarkmap: ${p}, info: ${JSON.stringify(f)}`
        ), b.currentManager && b.currentManager.isUseBackgroundThread)
          return b.currentManager.worker.insertBackgroundMarkMap(
            p,
            f
          );
      },
      updateMarkmap: async (p, f, N) => {
        if (b.logger.info(
          `[ApplianceMultiPlugin] updateMarkmap: ${p}, markmapId: ${f}, info: ${JSON.stringify(N)}`
        ), b.currentManager && b.currentManager.isUseBackgroundThread)
          return b.currentManager.worker.updateBackgroundMarkMap(
            p,
            f,
            N
          );
      },
      insertBackgroundImage: async (p, f) => {
        if (b.logger.info(
          `[ApplianceMultiPlugin] insertBackgroundImage: ${p}, info: ${JSON.stringify(f)}`
        ), b.currentManager && b.currentManager.isUseBackgroundThread)
          return b.currentManager.worker.insertBackgroundSVG(
            p,
            f
          );
      }
    };
    g.injectMethodToObject(e, "undo"), g.injectMethodToObject(e, "redo"), g.injectMethodToObject(e, "cleanCurrentScene"), g.injectMethodToObject(e, "insertImage"), g.injectMethodToObject(e, "completeImageUpload"), g.injectMethodToObject(e, "lockImage"), g.injectMethodToObject(e, "insertText"), g.injectMethodToObject(e, "updateText"), g.injectMethodToObject(i, "getImagesInformation"), g.injectMethodToObject(i, "callbacks"), g.injectMethodToObject(i, "screenshotToCanvasAsync"), g.injectMethodToObject(i, "getBoundingRectAsync"), g.injectMethodToObject(i, "scenePreviewAsync"), g.injectMethodToObject(i, "fillSceneSnapshotAsync"), g.injectMethodToObject(e.mainView, "setMemberState"), G(i) && g.injectMethodToObject(i, "setWritable"), Object.defineProperty(e, "canUndoSteps", {
      get() {
        return g.canUndoSteps();
      }
    }), Object.defineProperty(e, "canRedoSteps", {
      get() {
        return g.canRedoSteps();
      }
    });
    const M = (p, f, N = 3) => {
      const S = e.focused || pe;
      N > 0 && S !== p ? setTimeout(() => {
        M(p, f, N - 1);
      }, 100) : e.emitter.emit("canUndoStepsChange", f);
    };
    ct.InternalMsgEmitter.on(
      "onCanUndoStepsUpdate",
      (p, f) => {
        M(f, p);
      }
    );
    const d = (p, f, N = 3) => {
      const S = e.focused || pe;
      N > 0 && S !== p ? setTimeout(() => {
        d(p, f, N - 1);
      }, 100) : e.emitter.emit("canRedoStepsChange", f);
    };
    ct.InternalMsgEmitter.on(
      "onCanRedoStepsUpdate",
      (p, f) => {
        d(f, p);
      }
    );
    const y = {
      ...g,
      callbacks: g.callbacks()
    };
    return Object.defineProperty(y, "disableDeviceInputs", {
      set(p) {
        g.setDisableDeviceInputs(p);
      },
      get() {
        return g.getDisableDeviceInputs();
      }
    }), Object.defineProperty(y, "disableCameraTransform", {
      set(p) {
        g.setDisableCameraTransform(p);
      },
      get() {
        return g.getDisableCameraTransform();
      }
    }), Object.defineProperty(y, "disableEraseImage", {
      set(p) {
        g.setDisableEraseImage(p);
      },
      get() {
        return g.getDisableEraseImage();
      }
    }), u && b.currentManager && (b.currentManager.bindPlugin(u), u.init(i)), e._appliancePlugin = y, e._appliancePlugin;
  }
  static onCreate(e) {
    e && b.currentManager && (b.currentManager.bindPlugin(
      e
    ), e.init(e.displayer));
  }
  static async createApplianceMultiPlugin(e, t) {
    try {
      await e.createInvisiblePlugin(b, {});
    } catch (o) {
      if (!e.isWritable) {
        await e.setWritable(!0);
        const s = await this.createApplianceMultiPlugin(e, t);
        return await e.setWritable(!1), s;
      }
      throw o;
    }
    let i = e.getInvisiblePlugin(t);
    return i || (i = await b.createApplianceMultiPlugin(e, t)), i;
  }
  // static onDestroy(plugin: ApplianceMultiPlugin) {}
  get isReplay() {
    return En(this.displayer);
  }
  get callbackName() {
    return this.isReplay ? "onPlayerStateChanged" : "onRoomStateChanged";
  }
  init(e) {
    var i, o;
    const t = e.state;
    (i = b.currentManager) == null || i.onRoomMembersChange(
      t.roomMembers
    ), G(e) && ((o = b.currentManager) == null || o.onMemberChange(
      t.memberState
    )), this.displayer.callbacks.on(
      this.callbackName,
      this.roomStateChangeListener
    ), this.displayer.callbacks.on(
      "onEnableWriteNowChanged",
      this.updateRoomWritable
    ), this.displayer.callbacks.on("onPhaseChanged", this.onPhaseChanged);
  }
  destroy() {
    var e;
    b.logger.info("[ApplianceMultiPlugin] passive destroy"), (e = b.currentManager) == null || e.destroy(), b.currentManager = void 0;
  }
};
// 组件类型，该组件的唯一识别符。应该取一个独特的名字，以和其他组件区分。
I(b, "kind", "appliance-multi-plugin"), I(b, "currentManager"), I(b, "logger", {
  info: console.log,
  warn: console.warn,
  error: console.error
}), I(b, "createCurrentManager", (e, t, i) => {
  b.currentManager && b.currentManager.destroy();
  const o = {
    plugin: i,
    displayer: e.displayer,
    options: t
  }, s = new ct(o);
  if (b.logger.info(
    "[ApplianceMultiPlugin] refresh ApplianceMultiManager object"
  ), s.setWindowManager(e), b.currentManager = s, e.room) {
    const a = s.hasOffscreenCanvas;
    e.room.logger.info(
      `[ApplianceMultiPlugin] use ${a ? "worker" : "mainThread"}`
    );
  }
});
let Bi = b;
var Cy = He, Sy = function() {
  return Cy.Date.now();
}, ky = Sy, xy = /\s/;
function jy(r) {
  for (var n = r.length; n-- && xy.test(r.charAt(n)); )
    ;
  return n;
}
var Dy = jy, zy = Dy, by = /^\s+/;
function Ay(r) {
  return r && r.slice(0, zy(r) + 1).replace(by, "");
}
var Ey = Ay, Ly = Rt, Py = yt, Oy = "[object Symbol]";
function Ry(r) {
  return typeof r == "symbol" || Py(r) && Ly(r) == Oy;
}
var Yy = Ry, Uy = Ey, Wi = ut, By = Yy, Fi = NaN, Wy = /^[-+]0x[0-9a-f]+$/i, Fy = /^0b[01]+$/i, Vy = /^0o[0-7]+$/i, Qy = parseInt;
function Gy(r) {
  if (typeof r == "number")
    return r;
  if (By(r))
    return Fi;
  if (Wi(r)) {
    var n = typeof r.valueOf == "function" ? r.valueOf() : r;
    r = Wi(n) ? n + "" : n;
  }
  if (typeof r != "string")
    return r === 0 ? r : +r;
  r = Uy(r);
  var e = Fy.test(r);
  return e || Vy.test(r) ? Qy(r.slice(2), e ? 2 : 8) : Wy.test(r) ? Fi : +r;
}
var Zy = Gy, $y = ut, mn = ky, Vi = Zy, Hy = "Expected a function", Jy = Math.max, Xy = Math.min;
function Ky(r, n, e) {
  var t, i, o, s, a, c, l = 0, h = !1, u = !1, g = !0;
  if (typeof r != "function")
    throw new TypeError(Hy);
  n = Vi(n) || 0, $y(e) && (h = !!e.leading, u = "maxWait" in e, o = u ? Jy(Vi(e.maxWait) || 0, n) : o, g = "trailing" in e ? !!e.trailing : g);
  function M(k) {
    var v = t, A = i;
    return t = i = void 0, l = k, s = r.apply(A, v), s;
  }
  function d(k) {
    return l = k, a = setTimeout(p, n), h ? M(k) : s;
  }
  function y(k) {
    var v = k - c, A = k - l, P = n - v;
    return u ? Xy(P, o - A) : P;
  }
  function w(k) {
    var v = k - c, A = k - l;
    return c === void 0 || v >= n || v < 0 || u && A >= o;
  }
  function p() {
    var k = mn();
    if (w(k))
      return f(k);
    a = setTimeout(p, y(k));
  }
  function f(k) {
    return a = void 0, g && t ? M(k) : (t = i = void 0, s);
  }
  function N() {
    a !== void 0 && clearTimeout(a), l = 0, t = c = i = a = void 0;
  }
  function S() {
    return a === void 0 ? s : f(mn());
  }
  function C() {
    var k = mn(), v = w(k);
    if (t = arguments, i = this, c = k, v) {
      if (a === void 0)
        return d(c);
      if (u)
        return clearTimeout(a), a = setTimeout(p, n), M(c);
    }
    return a === void 0 && (a = setTimeout(p, n)), s;
  }
  return C.cancel = N, C.flush = S, C;
}
var qy = Ky, _y = qy, eI = ut, tI = "Expected a function";
function nI(r, n, e) {
  var t = !0, i = !0;
  if (typeof r != "function")
    throw new TypeError(tI);
  return eI(e) && (t = "leading" in e ? !!e.leading : t, i = "trailing" in e ? !!e.trailing : i), _y(r, n, {
    leading: t,
    maxWait: n,
    trailing: i
  });
}
var iI = nI;
const rI = /* @__PURE__ */ Pn(iI);
class oI extends br {
  constructor(e, t) {
    super(e, t);
    I(this, "width", 1e3);
    I(this, "height", 1e3);
    I(this, "dpr", Nt());
    I(this, "vDom");
    I(this, "eventTragetElement");
    I(this, "snapshotContainerRef");
    I(this, "backgroundContainerRef");
    I(this, "backgroundSnapshotContainerRef");
    I(this, "canvasContainerRef", m.createRef());
    I(this, "subLocalCanvasContainerRef");
    I(this, "canvasTopRef");
    I(this, "canvasServiceFloatRef", m.createRef());
    I(this, "canvasFloatRef", m.createRef());
    I(this, "canvasBgRef", m.createRef());
    I(this, "floatBarRef", m.createRef());
    I(this, "containerOffset", { x: 0, y: 0 });
    this.control.hasOffscreenCanvas || (this.snapshotContainerRef = m.createRef()), this.control.isUseSimple || (this.canvasTopRef = m.createRef(), this.subLocalCanvasContainerRef = m.createRef()), this.control.isUseBackgroundThread && (this.backgroundContainerRef = m.createRef(), this.backgroundSnapshotContainerRef = m.createRef());
  }
  setCanvassStyle() {
    var e;
    if (this.eventTragetElement) {
      const t = this.eventTragetElement.offsetWidth, i = this.eventTragetElement.offsetHeight, o = Nt();
      o !== this.dpr && (this.dpr = o, this.control.worker.updateDpr(this.viewId, this.dpr)), t && i && this.canvasContainerRef.current && (t !== this.width || i !== this.height) && (this.width = t, this.height = i), t && i && this.backgroundContainerRef && this.backgroundContainerRef.current && (t !== this.width || i !== this.height) && (this.width = t, this.height = i, this.backgroundContainerRef.current.style.width = `${t}px`, this.backgroundContainerRef.current.style.height = `${i}px`, this.backgroundContainerRef.current.classList.toggle(
        "appliance-plugin-background-isWritable",
        this.control.renderControl.isWritable
      )), t && i && this.canvasBgRef.current && (t !== this.width || i !== this.height) && (this.width = t, this.height = i, this.control.isUseOffscreenTransfer || (this.canvasBgRef.current.width = t * this.dpr, this.canvasBgRef.current.height = i * this.dpr, this.canvasFloatRef.current && (this.canvasFloatRef.current.width = t * this.dpr, this.canvasFloatRef.current.height = i * this.dpr), this.canvasServiceFloatRef.current && (this.canvasServiceFloatRef.current.width = t * this.dpr, this.canvasServiceFloatRef.current.height = i * this.dpr), (e = this.canvasTopRef) != null && e.current && (this.canvasTopRef.current.width = t * this.dpr, this.canvasTopRef.current.height = i * this.dpr)));
    }
  }
  createMainViewDisplayer(e) {
    return this.vDom || (this.containerOffset = this.getContainerOffset(
      e,
      this.containerOffset
    ), this.eventTragetElement = e.parentElement.children[0], e.innerHTML = "", Ln.render(
      /* @__PURE__ */ m.createElement(
        qn,
        {
          viewId: this.viewId,
          mananger: this,
          refs: {
            canvasServiceFloatRef: this.canvasServiceFloatRef,
            canvasFloatRef: this.canvasFloatRef,
            canvasBgRef: this.canvasBgRef,
            floatBarRef: this.floatBarRef,
            canvasTopRef: this.canvasTopRef,
            canvasContainerRef: this.canvasContainerRef,
            snapshotContainerRef: this.snapshotContainerRef,
            subLocalCanvasContainerRef: this.subLocalCanvasContainerRef,
            backgroundContainerRef: this.backgroundContainerRef,
            backgroundSnapshotContainerRef: this.backgroundSnapshotContainerRef
          }
        }
      ),
      e
    ), this.control.room && this.bindDisplayerEvent(this.eventTragetElement)), this;
  }
}
class it extends Et {
  constructor(e) {
    super(e);
    I(this, "focuedViewId");
    I(this, "control");
    I(this, "focuedView");
    this.control = e.control;
  }
  bindMainView() {
    if (!this.control.divMainView)
      return;
    const e = new oI(
      this.control,
      ke.InternalMsgEmitter
    ), { width: t, height: i, dpr: o } = e, s = {
      dpr: o,
      originalPoint: [t / 2, i / 2],
      offscreenCanvasOpt: {
        ...it.defaultScreenCanvasOpt,
        width: t,
        height: i
      },
      layerOpt: {
        ...it.defaultLayerOpt,
        width: t,
        height: i
      },
      cameraOpt: {
        ...it.defaultCameraOpt,
        width: t,
        height: i
      }
    }, a = this.control.room && this.control.room.mainView || this.control.play && this.control.play.mainView;
    if (a) {
      const { scale: c, ...l } = a.camera;
      s.cameraOpt = {
        ...s.cameraOpt,
        ...l,
        scale: c === 1 / 0 ? 1 : c
      }, this.createMianView({
        id: pe,
        displayer: e,
        focusScenePath: a.focusScenePath || a.scenePath,
        cameraOpt: s.cameraOpt,
        viewData: a
      }), this.setFocuedViewId(pe), e.createMainViewDisplayer(this.control.divMainView);
    }
  }
  getViewInitData(e) {
    var i;
    const t = this.getView(e);
    if (t) {
      const { width: o, height: s, dpr: a } = t.displayer;
      (i = this.control.pluginOptions.extras.canvasOpt) != null && i.contextType && (it.defaultScreenCanvasOpt.contextType = this.control.pluginOptions.extras.canvasOpt.contextType);
      const c = {
        dpr: a,
        originalPoint: [o / 2, s / 2],
        offscreenCanvasOpt: {
          ...it.defaultScreenCanvasOpt,
          width: o,
          height: s
        },
        layerOpt: {
          ...it.defaultLayerOpt,
          width: o,
          height: s
        },
        cameraOpt: {
          ...it.defaultCameraOpt,
          width: o,
          height: s
        }
      };
      if (t.viewData) {
        const { scale: l, ...h } = t.viewData.camera;
        c.cameraOpt = {
          ...c.cameraOpt,
          ...h,
          scale: l === 1 / 0 ? 1 : l
        };
      }
      return c;
    }
  }
  async mountView(e) {
    var i;
    const t = this.getView(e);
    if (t) {
      this.control.worker.isActive || (this.control.cursor.activeCollector(), this.control.isUseOffscreenTransfer && t.displayer.canvasBgRef.current && t.displayer.canvasServiceFloatRef.current && t.displayer.canvasFloatRef.current && t.displayer.setTransferControlToOffscreenCanvas({
        bgCanvas: t.displayer.canvasBgRef.current,
        serviceCanvas: t.displayer.canvasServiceFloatRef.current,
        localCanvas: t.displayer.canvasFloatRef.current
      }), await this.control.activeWorker());
      const o = this.getViewInitData(e);
      o && ((i = this.control.worker) == null || i.createViewWorker(
        e,
        o,
        t.displayer
      )), t.focusScenePath && this.control.collector && this.control.worker.pullServiceData(e, t.focusScenePath, {
        isAsync: !0,
        useAnimation: !1
      });
    }
  }
}
class Ge extends ke {
  constructor(e) {
    super(e);
    I(this, "viewContainerManager");
    I(this, "divMainView");
    I(this, "onCameraChange", rI(
      (e) => {
        var i;
        const t = this.viewContainerManager.mainView;
        t && t.cameraOpt && (t.cameraOpt.width !== e.width || t.cameraOpt.height !== e.height) && ((i = this.viewContainerManager.mainView) == null || i.displayer.updateSize()), this.viewContainerManager.setFocuedViewCameraOpt(e);
      },
      Xi,
      { leading: !1 }
    ));
    const t = {
      control: this,
      internalMsgEmitter: Ge.InternalMsgEmitter,
      publicMsgEmitter: Ge.PublicMsgEmitter
    };
    this.viewContainerManager = new it(t);
  }
  init() {
    Ge.InternalMsgEmitter.on(
      te.BindMainView,
      (e) => {
        this.divMainView = e, this.plugin && !this.viewContainerManager.mainView && this.viewContainerManager.bindMainView();
      }
    );
  }
  activePlugin() {
    this.plugin && this.divMainView && !this.viewContainerManager.mainView && this.viewContainerManager.bindMainView(), this.collector && (this.collector.addStorageStateListener((e) => {
      var a, c;
      if ((a = this.collector) != null && a.storage && Object.keys(this.collector.storage).length === 0) {
        (c = this.worker) == null || c.clearViewScenePath(pe, !0);
        return;
      }
      const { diffView: t, diffScenePath: i, diffData: o } = e, s = /* @__PURE__ */ new Map();
      if (t) {
        let l;
        Object.keys(t).forEach((h) => {
          var g;
          const u = t[h];
          if (u && !u.newValue)
            (g = this.worker) == null || g.clearViewScenePath(h, !0), u.oldValue && (l = u.oldValue);
          else if (u && u.newValue) {
            for (const M of Object.keys(u.newValue))
              this.worker.pullServiceData(h, M, {
                isAsync: !1,
                useAnimation: !0
              });
            l = u.newValue;
          }
          if (l)
            for (const M of Object.values(l)) {
              const d = /* @__PURE__ */ new Set();
              for (const y of Object.keys(M))
                y && d.add(y);
              s.set(h, d);
            }
        });
      }
      if (i) {
        let l;
        Object.keys(i).forEach((h) => {
          var g;
          const u = i[h];
          if (u && u.viewId && !u.newValue ? ((g = this.worker) == null || g.clearViewScenePath(u.viewId, !0), u.oldValue && (l = u.oldValue)) : u && u.viewId && u.newValue && (this.worker.pullServiceData(u.viewId, h, {
            isAsync: !1,
            useAnimation: !0
          }), l = u.newValue), u != null && u.viewId && l) {
            const M = /* @__PURE__ */ new Set();
            for (const d of Object.keys(l))
              d && M.add(d), s.set(u.viewId, M);
          }
        });
      }
      o && Object.keys(o).forEach((l) => {
        var u, g;
        const h = o[l];
        if (h) {
          const { viewId: M } = h, d = s.get(M) || /* @__PURE__ */ new Set();
          d.add(l), s.set(M, d), (u = this.worker) == null || u.onServiceDerive(l, h), (g = this.miniMapManager) == null || g.updateMiniMapCamera(M);
        }
      });
      for (const [l, h] of s.entries())
        ke.InternalMsgEmitter.emit(
          "excludeIds",
          [...h],
          l
        );
    }), this.room && (this.roomMember.onUidChangeHook((e) => {
      var t;
      (t = this.collector) != null && t.serviceStorage && this.viewContainerManager.getAllViews().forEach((i) => {
        var o, s, a, c, l, h;
        if (i && i.focusScenePath && ((o = this.collector) != null && o.serviceStorage[i.id]) && ((s = this.collector) != null && s.serviceStorage[i.id][i.focusScenePath])) {
          const u = Object.keys(
            (a = this.collector) == null ? void 0 : a.serviceStorage[i.id][i.focusScenePath]
          );
          for (const g of u) {
            const M = (c = this.collector) == null ? void 0 : c.getUidFromKey(g);
            if (M && !e.online.includes(M))
              if ((l = this.collector) != null && l.isSelector(g))
                this.renderControl.isWritable && ((h = this.collector) == null || h.updateValue(g, void 0, {
                  viewId: i.id,
                  scenePath: i.focusScenePath,
                  isSync: !0
                })), this.worker.blurSelector(i.id, i.focusScenePath, g);
              else {
                const d = this.textEditorManager.editors.get(g);
                d && d.opt && (d.opt.workState === x.Doing || d.opt.workState === x.Start) && (d.opt.text && d.opt.text.replace(/\s*,/g, "") ? this.textEditorManager.unActive(g) : this.textEditorManager.delete(g));
              }
          }
        }
      });
    }), this.worker.isActive && this.viewContainerManager.getAllViews().forEach((e) => {
      e && e.focusScenePath && this.worker.pullServiceData(e.id, e.focusScenePath, {
        isAsync: !0,
        useAnimation: !1
      });
    })));
  }
  async activeWorker() {
    await this.worker.init();
  }
}
const D = class D extends Zi {
  constructor() {
    super(...arguments);
    I(this, "onPhaseChanged", (e) => {
      var t;
      e === $i.Reconnecting && ((t = D.currentManager) == null || t.setSnapshootData());
    });
    I(this, "updateRoomWritable", () => {
      var e;
      (e = D.currentManager) == null || e.onWritableChange(
        this.displayer.isWritable
      );
    });
    I(this, "roomStateChangeListener", async (e) => {
      var t, i;
      D.currentManager instanceof Ge && (e.cameraState && D.currentManager.onCameraChange(e.cameraState), e.sceneState && D.currentManager.onSceneChange(
        e.sceneState.scenePath,
        pe
      )), e != null && e.roomMembers && ((t = D.currentManager) == null || t.onRoomMembersChange(
        e.roomMembers
      )), !(G(this.displayer) && !this.displayer.isWritable) && e.memberState && ((i = D.currentManager) == null || i.onMemberChange(
        e.memberState
      ));
    });
  }
  static async getInstance(e, t) {
    var M;
    t.options && (((M = t.options.extras) == null ? void 0 : M.useWorker) === "mainThread" && (t.options.cdn.fullWorkerUrl = "", t.options.cdn.subWorkerUrl = ""), (t.options.extras && t.options.extras.useWorker !== "mainThread" || !t.options.extras) && !t.options.cdn.fullWorkerUrl && !t.options.cdn.subWorkerUrl && D.logger.error(
      "[ApplianceSinglePlugin] you must adaptor options cdn fullWorkerUrl and subWorkerUrl"
    ));
    const i = e.callbacks.on, o = e.callbacks.off, s = e.callbacks.once, a = e.cleanCurrentScene.bind(
      e
    ), c = e.setMemberState.bind(e);
    let l;
    G(e) && (l = e.setWritable), t != null && t.logger && (D.logger = t.logger);
    let h = e.getInvisiblePlugin(
      D.kind
    );
    h || (h = await D.createAppliancePlugin(
      e,
      D.kind
    )), e && h && D.createCurrentManager(
      e,
      {
        cdn: t.options.cdn,
        extras: {
          ...Se,
          ...t.options.extras
        }
      },
      h
    ), h && D.currentManager && (D.currentManager.bindPlugin(h), h.init(e)), t != null && t.cursorAdapter && (D.cursorAdapter = t.cursorAdapter), D.effectInstance();
    const u = {
      displayer: e,
      currentManager: D.currentManager,
      maxScreenshotWidth: 10 * 1024,
      maxScreenshotHeight: 10 * 1024,
      _injectTargetObject: void 0,
      getBoundingRectAsync: async function(d) {
        var p;
        D.logger.info(
          "[ApplianceSinglePlugin] getBoundingRectAsync"
        );
        const y = e.getBoundingRect(d), w = await ((p = D.currentManager) == null ? void 0 : p.getBoundingRect(
          d
        ));
        return !y.width || !y.height ? w : Kn(y, w);
      },
      screenshotToCanvasAsync: async function(d, y, w, p, f, N) {
        var P;
        D.logger.info(
          "[ApplianceSinglePlugin] screenshotToCanvasAsync"
        );
        let S = w, C = p, k = f.scale;
        S > this.maxScreenshotWidth && (k = this.maxScreenshotWidth / S * k, S = this.maxScreenshotWidth), C > this.maxScreenshotHeight && (k = Math.min(this.maxScreenshotHeight / C * k, k), C = this.maxScreenshotHeight), f.scale = k;
        const v = document.createElement("canvas"), A = v.getContext("2d");
        v.width = S * (N || 1), v.height = C * (N || 1), A && (e.screenshotToCanvas(
          A,
          y,
          S,
          C,
          f,
          N
        ), d.drawImage(
          v,
          Math.floor((w - S) / 2),
          Math.floor((p - C) / 2),
          S * (N || 1),
          C * (N || 1),
          0,
          0,
          S,
          C
        ), v.remove()), D.currentManager && await ((P = D.currentManager) == null ? void 0 : P.screenshotToCanvas(
          d,
          y,
          S,
          C,
          f,
          Math.floor((w - S) / 2),
          Math.floor((p - C) / 2)
        ));
      },
      scenePreviewAsync: async function(d, y, w, p, f) {
        D.logger.info(
          "[ApplianceSinglePlugin] scenePreviewAsync"
        ), e.scenePreview(d, y, w, p, f);
        const N = document.createElement("img");
        N.className = "appliance-plugin-fill-scene-snapshot-img", y.append(N), getComputedStyle(y).position || (y.style.position = "relative"), D.currentManager && await D.currentManager.scenePreview(
          d,
          N
        );
      },
      fillSceneSnapshotAsync: async function(d, y, w, p, f) {
        var F, X;
        D.logger.info(
          "[ApplianceSinglePlugin] fillSceneSnapshotAsync"
        ), y.innerHTML = "", e.fillSceneSnapshot(d, y, w, p, f);
        const N = w || ((F = e.state.cameraState) == null ? void 0 : F.width) || 0, S = p || ((X = e.state.cameraState) == null ? void 0 : X.height) || 0;
        let C = await this.getBoundingRectAsync(d);
        C || (C = {
          originX: 0,
          originY: 0,
          width: N,
          height: S
        });
        let k = Math.max(C == null ? void 0 : C.width, N), v = Math.max(C == null ? void 0 : C.height, S), A = 1;
        const P = k > N && Math.min(N / k, A) || A, E = v > S && Math.min(S / v, A) || A;
        P <= E ? (k = P < 1 && N || k, v = Math.floor(v * P) + 1, A = P) : P > E && (v = E < 1 && S || v, k = Math.floor(k * E) + 1, A = E);
        const O = {
          scale: A,
          centerX: C.originX + C.width / 2,
          centerY: C.originY + C.height / 2
        }, z = document.createElement("canvas"), L = z.getContext("2d");
        z.width = k, z.height = v, L && await this.screenshotToCanvasAsync(
          L,
          d,
          k,
          v,
          O,
          devicePixelRatio
        );
        const j = document.createElement("img");
        j.className = "appliance-plugin-fill-scene-snapshot-img", j.src = z.toDataURL(), j.onload = () => {
          z.remove();
        }, j.onerror = () => {
          z.remove(), j.remove();
        }, y.append(j), getComputedStyle(y).position || (y.style.position = "relative");
      },
      _callbacksOn: function(d, y) {
        D.logger.info(
          `[ApplianceSinglePlugin] callbacks ${d}`
        ), (d === "onCanUndoStepsUpdate" || d === "onCanRedoStepsUpdate") && G(e) && e.isWritable ? Ge.InternalMsgEmitter.on(d, y) : i.call(e.callbacks, d, y);
      },
      _callbacksOnce: function(d, y) {
        D.logger.info(
          `[ApplianceSinglePlugin] callbacks ${d}`
        ), (d === "onCanUndoStepsUpdate" || d === "onCanRedoStepsUpdate") && G(e) && e.isWritable ? Ge.InternalMsgEmitter.on(d, y) : s.call(e.callbacks, d, y);
      },
      _callbacksOff: function(d, y) {
        D.logger.info(
          `[ApplianceSinglePlugin] callbacks ${d}`
        ), (d === "onCanUndoStepsUpdate" || d === "onCanRedoStepsUpdate") && G(e) && e.isWritable ? Ge.InternalMsgEmitter.off(d, y) : o.call(e.callbacks, d, y);
      },
      canRedoSteps() {
        if (D.logger.info(
          "[ApplianceSinglePlugin] canRedoSteps"
        ), D.currentManager && G(e) && !e.disableSerialization) {
          const d = D.currentManager.viewContainerManager.focuedView, y = d == null ? void 0 : d.focusScenePath;
          if (d && y)
            return d.displayer.commiter.redoStack.filter(
              (w) => w.scenePath === y
            ).length;
        }
        return 0;
      },
      canUndoSteps() {
        if (D.logger.info(
          "[ApplianceSinglePlugin] canUndoSteps"
        ), D.currentManager && G(e) && !e.disableSerialization) {
          const d = D.currentManager.viewContainerManager.focuedView, y = d == null ? void 0 : d.focusScenePath;
          if (d && y)
            return d.displayer.commiter.undoStack.filter(
              (w) => w.scenePath === y
            ).length;
        }
        return 0;
      },
      undo: function() {
        return D.logger.info("[ApplianceSinglePlugin] undo"), D.currentManager && G(e) && !e.disableSerialization ? D.currentManager.viewContainerManager.undo() : 0;
      },
      redo: function() {
        return D.logger.info("[ApplianceSinglePlugin] redo"), D.currentManager && G(e) && !e.disableSerialization ? D.currentManager.viewContainerManager.redo() : 0;
      },
      cleanCurrentScene: function(d) {
        D.logger.info(
          "[ApplianceSinglePlugin] cleanCurrentScene"
        ), D.currentManager && G(e) && e.isWritable && (D.currentManager.cleanCurrentScene(), a.call(e, d));
      },
      insertImage: function(d) {
        D.logger.info(
          "[ApplianceSinglePlugin] insertImage"
        ), D.currentManager && G(e) && e.isWritable && D.currentManager.worker.insertImage(
          pe,
          d
        );
      },
      lockImage: function(d, y) {
        D.logger.info("[ApplianceSinglePlugin] lockImage"), D.currentManager && G(e) && e.isWritable && D.currentManager.worker.lockImage(
          pe,
          d,
          y
        );
      },
      completeImageUpload: function(d, y) {
        D.logger.info(
          "[ApplianceSinglePlugin] completeImageUpload"
        ), D.currentManager && G(e) && e.isWritable && D.currentManager.worker.completeImageUpload(
          pe,
          d,
          y
        );
      },
      getImagesInformation: function(d) {
        return D.logger.info(
          "[ApplianceSinglePlugin] completeImageUpload"
        ), D.currentManager && G(e) && e.isWritable ? D.currentManager.worker.getImagesInformation(
          d
        ) : [];
      },
      insertText: function(d, y, w) {
        if (D.logger.info("[ApplianceSinglePlugin] insertText"), D.currentManager && G(e) && e.isWritable)
          return D.currentManager.textEditorManager.insertText(
            pe,
            d,
            y,
            w
          );
      },
      updateText: function(d, y) {
        D.logger.info("[ApplianceMultiPlugin] updateText"), D.currentManager && G(e) && e.isWritable && D.currentManager.textEditorManager.updateText(
          d,
          y
        );
      },
      callbacks: () => ({
        ...e.callbacks,
        on: u._callbacksOn.bind(u),
        once: u._callbacksOnce.bind(u),
        off: u._callbacksOff.bind(u)
      }),
      destroy() {
        D.currentManager && (D.logger.info("[ApplianceSinglePlugin] destroy"), D.currentManager.destroy(), D.currentManager = void 0, D.cursorAdapter = void 0);
      },
      setMemberState(d) {
        var w;
        D.logger.info(
          `[ApplianceSinglePlugin] setMemberState ${JSON.stringify(d)}`
        );
        const y = Object.keys(d);
        if (G(e) && e.isWritable) {
          if (y.includes("currentApplianceName"))
            if (d.currentApplianceName && d.currentApplianceName === ne.laserPen) {
              if ((w = D.currentManager) != null && w.isUseSimple)
                throw new Error(
                  "[ApplianceMultiPlugin] laserPen must be not support in simple mode"
                );
              d.currentApplianceName = ne.pencil, d.useLaserPen = !0;
            } else d.currentApplianceName && d.currentApplianceName === ne.pencil && (d.useLaserPen = !1);
          c(d);
          return;
        }
        throw new Error("setMemberState must be called in writable room");
      },
      createMiniMap: async function(d, y) {
        if (D.logger.info(
          "[ApplianceSinglePlugin] createMiniMap"
        ), D.currentManager && G(e) && e.isWritable)
          return D.currentManager.createMiniMap(
            d,
            y
          );
      },
      destroyMiniMap: async function(d) {
        if (D.logger.info(
          "[ApplianceMultiPlugin] destroyeMiniMap"
        ), D.currentManager && G(e) && e.isWritable)
          return D.currentManager.destroyMiniMap(d);
      },
      filterRenderByUid(d, y, w) {
        if (D.logger.info(
          "[ApplianceSinglePlugin] filterRenderByUid"
        ), D.currentManager && G(e) && e.isWritable)
          return D.currentManager.filterRenderByUid(
            d,
            y,
            w
          );
      },
      cancelFilterRender(d, y) {
        if (D.logger.info(
          "[ApplianceSinglePlugin] filterRenderByUid"
        ), D.currentManager && G(e) && e.isWritable)
          return D.currentManager.cancelFilterRender(
            d,
            y
          );
      },
      addListener: (d, y) => {
        D.logger.info(
          `[ApplianceSinglePlugin] addListener ${d}`
        ), Ge.PublicMsgEmitter.on(d, y);
      },
      removeListener: (d, y) => {
        D.logger.info(
          `[ApplianceSinglePlugin] removeListener ${d}`
        ), Ge.PublicMsgEmitter.off(d, y);
      },
      usePlugin: (d) => {
        var y;
        D.logger.info(
          "[ApplianceSinglePlugin] usePlugin",
          d.kind
        ), (y = D.currentManager) == null || y.usePlugin(d);
      },
      getDisableDeviceInputs() {
        if (D.currentManager && G(e))
          return D.currentManager.disableDeviceInputs;
      },
      setDisableDeviceInputs(d) {
        D.logger.info(
          `[ApplianceSinglePlugin] setDisableDeviceInputs ${d}`
        ), D.currentManager && G(e) && (D.currentManager.disableDeviceInputs = d);
      },
      getDisableCameraTransform() {
        if (D.currentManager && G(e))
          return D.currentManager.disableCameraTransform;
      },
      setDisableCameraTransform(d) {
        D.logger.info(
          `[ApplianceSinglePlugin] setDisableCameraTransform ${d}`
        ), D.currentManager && G(e) && (D.currentManager.disableCameraTransform = d);
      },
      getDisableEraseImage() {
        if (D.currentManager && G(e))
          return D.currentManager.disableEraseImage;
      },
      setDisableEraseImage(d) {
        D.logger.info(
          `[ApplianceSinglePlugin] setDisableEraseImage ${d}`
        ), D.currentManager && G(e) && (D.currentManager.disableEraseImage = d);
      },
      setWritable: async function(d) {
        var y;
        D.logger.info(
          `[ApplianceSinglePlugin] setWritable ${d}`
        ), G(e) && l && (await ((y = D.currentManager) == null ? void 0 : y.setWritable(d)), await l.call(e, d));
      },
      requestIdleCallback: (d, y) => {
        const w = (y == null ? void 0 : y.timeout) || 0, p = performance.now();
        let f;
        const N = new MessageChannel();
        let S;
        const C = (k) => {
          try {
            d({
              didTimeout: k,
              timeRemaining: () => {
                const A = performance.now() - p;
                return Math.max(0, w - A);
              }
            });
          } catch (v) {
            D.logger.error(
              "[ApplianceSinglePlugin] requestIdleCallback error:",
              v
            );
          } finally {
            N.port1.close(), N.port2.close(), f !== void 0 && cancelAnimationFrame(f), S !== void 0 && clearTimeout(S);
          }
        };
        return N.port1.onmessage = () => {
          f = requestAnimationFrame(() => {
            w > 0 && performance.now() - p >= w ? C(!0) : C(!1);
          });
        }, N.port2.postMessage(null), w > 0 && (S = setTimeout(() => {
          C(!0);
        }, w)), S || f || 0;
      },
      cancelIdleCallback: (d) => {
        try {
          clearTimeout(d), cancelAnimationFrame(d);
        } catch (y) {
          D.logger.error(
            "[ApplianceSinglePlugin] cancelIdleCallback error:",
            y
          );
        }
      },
      injectMethodToObject: (d, y) => {
        if (D.logger.info(
          `[ApplianceSinglePlugin] injectMethodToObject ${y}`
        ), typeof d[y] == "function" || typeof d[y] > "u") {
          d[y] = u[y];
          return;
        }
        y === "callbacks" && (d.callbacks.on = u._callbacksOn.bind(u), d.callbacks.off = u._callbacksOff.bind(u), d.callbacks.once = u._callbacksOnce.bind(u));
      },
      blurText: () => {
        D.logger.info("[ApplianceSinglePlugin] blurText"), D.currentManager && G(e) && e.isWritable && D.currentManager.textEditorManager.checkEmptyTextBlur();
      },
      hasElements: (d, y) => {
        var w;
        if (D.logger.info(
          `[ApplianceSinglePlugin] hasElements ${d}`
        ), D.currentManager && G(e) && e.isWritable) {
          let p = d;
          if (!p) {
            const f = D.currentManager.viewContainerManager.focuedView;
            p = f == null ? void 0 : f.focusScenePath;
          }
          if (p) {
            const f = (w = D.currentManager.collector) == null ? void 0 : w.getScenePathData(
              p
            );
            if (f) {
              const N = Object.values(f);
              return N.length > 0 ? y ? N.some(
                (S) => y(S.toolsType)
              ) : !0 : !1;
            }
            return !1;
          }
          return !1;
        }
      },
      getElements: (d, y) => {
        var w;
        if (D.logger.info(
          `[ApplianceSinglePlugin] getElements ${d}`
        ), D.currentManager && G(e) && e.isWritable) {
          let p = d;
          if (!p) {
            const f = D.currentManager.viewContainerManager.focuedView;
            p = f == null ? void 0 : f.focusScenePath;
          }
          if (p) {
            const f = (w = D.currentManager.collector) == null ? void 0 : w.getScenePathData(
              p
            ), N = f && Object.values(f) || [];
            return f && N.length > 0 && y ? N.filter(
              (S) => y(S.toolsType)
            ) : N;
          }
          return [];
        }
      },
      stopDraw: (d) => {
        if (D.logger.info(
          `[ApplianceSinglePlugin] stopDraw ${d}`
        ), D.currentManager && G(e))
          return D.currentManager.stopDraw(d);
      },
      setViewLocalScenePathChange: async (d, y) => {
        D.logger.info(
          `[ApplianceSinglePlugin] setViewLocalScenePathChange ${d} ${y}`
        ), D.currentManager && await D.currentManager.setViewLocalScenePathChange(
          d,
          y
        );
      },
      insertMarkmap: async (d, y) => {
        if (D.logger.info(
          `[ApplianceSinglePlugin] insertMarkmap: ${d}, info: ${JSON.stringify(y)}`
        ), D.currentManager && D.currentManager.isUseBackgroundThread)
          return D.currentManager.worker.insertBackgroundMarkMap(
            d,
            y
          );
      },
      updateMarkmap: async (d, y, w) => {
        if (D.logger.info(
          `[ApplianceSinglePlugin] updateMarkmap: ${d}, markmapId: ${y}, info: ${JSON.stringify(w)}`
        ), D.currentManager && D.currentManager.isUseBackgroundThread)
          return D.currentManager.worker.updateBackgroundMarkMap(
            d,
            y,
            w
          );
      },
      insertBackgroundImage: async (d, y) => {
        if (D.logger.info(
          `[ApplianceSinglePlugin] insertBackgroundImage: ${d}, info: ${JSON.stringify(y)}`
        ), D.currentManager && D.currentManager.isUseBackgroundThread)
          return D.currentManager.worker.insertBackgroundSVG(
            d,
            y
          );
      }
    };
    u.injectMethodToObject(e, "undo"), u.injectMethodToObject(e, "redo"), u.injectMethodToObject(e, "cleanCurrentScene"), u.injectMethodToObject(e, "insertImage"), u.injectMethodToObject(e, "completeImageUpload"), u.injectMethodToObject(e, "lockImage"), u.injectMethodToObject(e, "getImagesInformation"), u.injectMethodToObject(e, "insertText"), u.injectMethodToObject(e, "updateText"), u.injectMethodToObject(e, "callbacks"), u.injectMethodToObject(e, "screenshotToCanvasAsync"), u.injectMethodToObject(e, "getBoundingRectAsync"), u.injectMethodToObject(e, "scenePreviewAsync"), u.injectMethodToObject(e, "fillSceneSnapshotAsync"), u.injectMethodToObject(e, "setMemberState"), G(e) && u.injectMethodToObject(e, "setWritable"), Object.defineProperty(e, "canUndoSteps", {
      get() {
        return u.canUndoSteps();
      }
    }), Object.defineProperty(e, "canRedoSteps", {
      get() {
        return u.canRedoSteps();
      }
    });
    const g = {
      ...u,
      callbacks: u.callbacks()
    };
    return Object.defineProperty(g, "disableDeviceInputs", {
      set(d) {
        u.setDisableDeviceInputs(d);
      },
      get() {
        return u.getDisableDeviceInputs();
      }
    }), Object.defineProperty(g, "disableEraseImage", {
      set(d) {
        u.setDisableEraseImage(d);
      },
      get() {
        return u.getDisableEraseImage();
      }
    }), Object.defineProperty(g, "disableCameraTransform", {
      set(d) {
        u.setDisableCameraTransform(d);
      },
      get() {
        return u.getDisableCameraTransform();
      }
    }), e._appliancePlugin = g, e._appliancePlugin;
  }
  static onCreate(e) {
    e && D.currentManager && (D.currentManager.bindPlugin(
      e
    ), e.init(e.displayer));
  }
  static async createAppliancePlugin(e, t) {
    try {
      await e.createInvisiblePlugin(D, {});
    } catch (o) {
      if (!e.isWritable) {
        await e.setWritable(!0);
        const s = await this.createAppliancePlugin(e, t);
        return await e.setWritable(!1), s;
      }
      throw o;
    }
    let i = e.getInvisiblePlugin(t);
    return i || (i = await D.createAppliancePlugin(
      e,
      t
    )), i;
  }
  // static onDestroy(plugin: AppliancePlugin) {}
  /**
   * 房间实例化时，将当前实例对displayer外部API的添加内部处理逻辑;
   * @param displayer
   */
  static effectInstance() {
    if (D.cursorAdapter) {
      const e = D.cursorAdapter.onAddedCursor;
      D.cursorAdapter.onAddedCursor = function(t) {
        t.onCursorMemberChanged = (i) => {
          try {
            i.appliance === ne.pencil || i.appliance === ne.shape || i.appliance === ne.text || i.appliance === ne.arrow || i.appliance === ne.straight || i.appliance === ne.rectangle || i.appliance === ne.ellipse || i.appliance === ne.eraser || i.appliance === ne.selector ? t != null && t.divElement && (t.divElement.style.display = "none") : t != null && t.divElement && (t.divElement.style.display = "block");
          } catch (o) {
            throw o;
          }
        }, e.call(D.cursorAdapter, t);
      };
    }
  }
  get isReplay() {
    return En(this.displayer);
  }
  get callbackName() {
    return this.isReplay ? "onPlayerStateChanged" : "onRoomStateChanged";
  }
  init(e) {
    var i, o;
    const t = e.state;
    (i = D.currentManager) == null || i.onRoomMembersChange(
      t.roomMembers
    ), G(e) && t != null && t.memberState && ((o = D.currentManager) == null || o.onMemberChange(
      t.memberState
    )), this.displayer.callbacks.on(
      this.callbackName,
      this.roomStateChangeListener
    ), this.displayer.callbacks.on(
      "onEnableWriteNowChanged",
      this.updateRoomWritable
    ), this.displayer.callbacks.on("onPhaseChanged", this.onPhaseChanged);
  }
  destroy() {
    var e;
    D.logger.info(
      "[ApplianceSinglePlugin] passive destroy"
    ), (e = D.currentManager) == null || e.destroy(), D.currentManager = void 0, D.cursorAdapter = void 0;
  }
};
// 组件类型，该组件的唯一识别符。应该取一个独特的名字，以和其他组件区分。
I(D, "kind", "appliance-single-plugin"), I(D, "cursorAdapter"), I(D, "currentManager"), I(D, "logger", {
  info: console.log,
  warn: console.warn,
  error: console.error
}), I(D, "createCurrentManager", (e, t, i) => {
  D.currentManager && D.currentManager.destroy();
  const o = {
    plugin: i,
    displayer: e,
    options: t
  }, s = new Ge(o);
  if (s.init(), D.logger.info(
    "[ApplianceSinglePlugin] refresh ApplianceSingleManager object"
  ), D.currentManager = s, G(e)) {
    const a = s.hasOffscreenCanvas, c = s.isUseOffscreenTransfer, l = s.pluginOptions, h = s.isUseSimple;
    e.logger.info(
      `[ApplianceSinglePlugin] use ${a ? "worker" : "mainThread"}, isUseSimple: ${h} ${c ? "offscreenTransfer" : ""}, options: ${JSON.stringify(l)}`
    );
  }
});
let Qi = D;
const Tt = class Tt extends m.Component {
  constructor() {
    super(...arguments);
    I(this, "mainViewRef", null);
  }
  componentDidMount() {
    Tt.emiter || (Tt.emiter = Ge.InternalMsgEmitter), Tt.emiter.emit(
      te.BindMainView,
      this.mainViewRef
    );
  }
  render() {
    return /* @__PURE__ */ m.createElement(m.Fragment, null, this.props.children, /* @__PURE__ */ m.createElement(
      "div",
      {
        className: "appliance-plugin-main-view-displayer",
        ref: (e) => this.mainViewRef = e
      }
    ));
  }
};
I(Tt, "emiter");
let Gi = Tt;
export {
  xI as $,
  Su as A,
  Oa as B,
  Ag as C,
  Se as D,
  W as E,
  CI as F,
  Gn as G,
  ar as H,
  Jc as I,
  cn as J,
  $ as K,
  Wt as L,
  At as M,
  st as N,
  PI as O,
  ve as P,
  $n as Q,
  VI as R,
  fg as S,
  Ai as T,
  zI as U,
  Et as V,
  OI as W,
  vI as X,
  pe as Y,
  nt as Z,
  Rt as _,
  R as a,
  RI as a0,
  YI as a1,
  UI as a2,
  PM as a3,
  NI as a4,
  ot as a5,
  Xi as a6,
  wI as a7,
  TI as a8,
  Bi as a9,
  Qi as aa,
  Gi as ab,
  ne as ac,
  qe as ad,
  te as ae,
  Br as af,
  Ji as ag,
  Qt as ah,
  Lt as ai,
  be as aj,
  et as ak,
  QI as al,
  q as b,
  T as c,
  $t as d,
  WI as e,
  SI as f,
  Pn as g,
  FI as h,
  yt as i,
  Gt as j,
  un as k,
  U as l,
  LI as m,
  jI as n,
  DI as o,
  BI as p,
  AI as q,
  bI as r,
  EI as s,
  Zt as t,
  Fe as u,
  x as v,
  me as w,
  _t as x,
  jr as y,
  kI as z
};

import { FastboardApp, FastboardPlayer } from '@netless/fastboard-core/full';
export * from '@netless/fastboard-core/full';
import { FastboardProps, ReplayFastboardProps } from '@netless/fastboard-ui/full';
export * from '@netless/fastboard-ui/full';
export { GenericIcon, Language, Theme } from '@netless/fastboard-ui/full';

type MountProps = Omit<FastboardProps, "app">;
/**
 * @deprecated Use `createUI` instead.
 */
declare function mount(app: FastboardApp, div: HTMLDivElement, options?: MountProps): {
    update(props?: MountProps): void;
    destroy(): void;
};
type ReplayProps = Omit<ReplayFastboardProps, "player">;
/**
 * @deprecated Use `createReplayUI` instead.
 */
declare function replay(player: FastboardPlayer, div: HTMLDivElement, options?: ReplayProps): {
    update(props?: ReplayProps): void;
    destroy(): void;
};

export { MountProps, ReplayProps, mount, replay };
